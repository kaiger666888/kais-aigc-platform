# Archived Requirements: v2.1 候选资产配套 (candidate-asset-completeness)

**Shipped:** 2026-08-19 · 12/12 requirements satisfied · Audit: [v2.1-MILESTONE-AUDIT.md](../v2.1-MILESTONE-AUDIT.md)

## v2.1 Requirements — 候选资产配套 (candidate-asset-completeness)

**Motivation:** kmc (kais-movie-pipeline) 已收敛出统一的冗余候选生成模型 (双旋钮 `n_candidates`/`final_candidates`, runner.py:2279-2329): 候选带 `_v{N}` 命名 + manifest `all_first_frames[]` 全量索引 + `selected_*_variant` 选定回写 + review 协议 `chosen_variant_id` 人工换选通道。kap 侧资产管理中心的三态选定 (selected/candidate/eliminated) UI 与 API 已落地 (`AssetLibrary.tsx:35,782-800` + `assets-registry/index.ts:185`), 但**中间管道断裂**: ingest 平铺孤产不建组、kmc 选定结果无消费方、画布层选定无后端端点、`workflow_phase` 368/368 全空。本里程碑打通"kmc 冗余生成 ↔ kap 候选组织 ↔ 人工选定 ↔ 回写 kmc"闭环。

**Cross-repo scope:**
- 接收端主体: `kais-aigc-platform` (ingest / assets-registry / canvas v2 / infinite-canvas)
- 桥接只读约定: `kais-hermes-skills` 的 `iframe-manifest.json` / `hook-candidates.json` / review API 协议 (不修改 kmc 侧,除非桥接必需)

### INGEST — 候选建组 (接收端 ingest 管道)

- [x] **INGEST-01**: `src/routes/v1/pipeline/ingest/images.ts` 落库时识别候选组——按 kmc 变体命名约定 (`*_v{N}` 后缀 + canonical 无后缀版本) 或 manifest `all_first_frames[]/all_last_frames[]`, 同组候选写 `o_assets.assetsId` 指向 primary 资产, 不再平铺孤产
- [x] **INGEST-02**: ingest 读 kmc 选定结果 (`iframe-manifest.json` 的 `selected_first_variant`/`selected_last_variant`) 落 `isPrimaryView=true`; 其余候选 `isPrimaryView=false, state='active'`; 组内 primary 唯一性由 ingest 保证
- [x] **INGEST-03**: assetType 枚举统一为单一真值源——消灭 `images.ts:18` 的 `role|scene|tool` 与 assets-registry `index.ts:21` 的 `character|scene|prop` 两套词汇; 提供存量值兼容映射
- [x] **INGEST-04**: 存量已平铺资产回填建组——一次性幂等脚本 (dry-run 基线 + `--apply` + DB 备份先行, 沿用 Phase 47 backfill 模式)

### SELECT — 选定回写 (画布层端点 + kmc 桥接)

- [x] **SELECT-01**: 新增画布层选定后端端点 (对齐 `docs/canvas-review-integration.md` 方案B + `canvas-next-steps.md` Phase 3.2 规划): 持久化 `canvas_variant_groups.winner_node_id` + 组内节点 `is_winner`/curation 状态, 事务化写入
- [x] **SELECT-02**: 前端 `canvasStore.selectWinner` 改调后端端点 (不再仅本地乐观 + save-v2 整体落盘); 失败回滚复用 `variantOps` prevSnapshot
- [x] **SELECT-03**: 资产中心 `handleSelect` 与画布 variant group 联动——`o_assets.isPrimaryView` 变更同步更新对应 canvas 节点 winner 状态 (打通两套选定词汇)
- [x] **SELECT-04**: kmc 桥接——kap 侧选定变更生成 review resolve (`chosen_variant_id`), 经 review-platform API 使画布换选可被 kmc 30s 轮询读到, 影响下一次 p11b 渲染选帧

### PHASE — workflow_phase 可组织性

- [x] **PHASE-01**: ingest/sync-assets 路径自动写 `o_assets.workflow_phase` (从 kmc manifest 路径 `p{NN}` / DAG 推导), 新资产不再为空
- [x] **PHASE-02**: 存量 o_assets 回填 `workflow_phase` (一次性脚本, 数据源 = manifest/DAG + 目录约定, 与 INGEST-04 回填脚本合流)

### GUARD — 契约守护

- [x] **GUARD-01**: 候选分组契约测试——ingest 对 fixture manifest 产出的 o_assets 形状断言 (组数 / 组内 isPrimaryView 唯一 / state 值域 / assetsId 自洽)
- [x] **GUARD-02**: `verify-phase-N` 汇总脚本 (延续 v2.0 verify 传统)——断言 INGEST/SELECT/PHASE 行为 + 双端枚举/词汇映射无 drift

