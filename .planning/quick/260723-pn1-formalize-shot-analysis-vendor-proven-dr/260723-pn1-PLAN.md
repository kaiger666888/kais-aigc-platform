---
phase: quick-shot-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/shot-analysis/shot_analysis_driver.py
  - scripts/shot-analysis/README.md
  - src/routes/production/shot-analysis/_shared/config.ts
  - src/routes/production/shot-analysis/index.ts
  - src/router.ts
autonomous: true
requirements:
  - QA-01-vendor-driver
  - QA-02-production-route
  - QA-03-build-verify

must_haves:
  truths:
    - "Proven Python driver is vendored verbatim under scripts/shot-analysis/ (no logic changes)"
    - "POST /api/v1/production/shot-analysis accepts {video, shots, shot_id_range?, semantic?, subject?, grid_n?, fps?} and spawns the vendored driver"
    - "The route stages host-side video paths into the ComfyUI container via docker cp (container-visible paths pass through untouched)"
    - "Successful responses return the aggregated shot_XXX.json outputs the driver produced"
    - "Static build (node scripts/build.js) and typecheck (npm run lint) pass cleanly"
  artifacts:
    - path: "scripts/shot-analysis/shot_analysis_driver.py"
      provides: "Proven逐镜头运镜解构 driver (geometry + optional semantic + optional subject → shot_XXX.json)"
      min_lines: 180
    - path: "scripts/shot-analysis/README.md"
      provides: "Operator docs: prerequisites, CLI usage, 3 layers + flags, known limitations, validated example"
    - path: "src/routes/production/shot-analysis/_shared/config.ts"
      provides: "SHOT_ANALYSIS_CONFIG mirror of WAN22_CONFIG shape"
      exports: ["SHOT_ANALYSIS_CONFIG"]
    - path: "src/routes/production/shot-analysis/index.ts"
      provides: "Express router: POST /, zod-validated body, docker cp staging, spawn driver, aggregate JSON"
      exports: ["default"]
    - path: "src/router.ts"
      provides: "Route138 import + /api/v1/production/shot-analysis mount"
  key_links:
    - from: "src/routes/production/shot-analysis/index.ts"
      to: "scripts/shot-analysis/shot_analysis_driver.py"
      via: "child_process.execFileSync(pythonBin, [driverPath, --shots, --video, ...flags])"
      pattern: "execFileSync\\(|spawn\\("
    - from: "src/routes/production/shot-analysis/index.ts"
      to: "src/routes/production/shot-analysis/_shared/config.ts"
      via: "import { SHOT_ANALYSIS_CONFIG }"
      pattern: "SHOT_ANALYSIS_CONFIG"
    - from: "src/router.ts"
      to: "src/routes/production/shot-analysis/index.ts"
      via: "import route138 + app.use"
      pattern: "shot-analysis"
---

<objective>
Formalize the already-validated shot-analysis capability into the platform: (A) vendor the proven Python driver verbatim under `scripts/shot-analysis/`, (B) add a THIN production route at `src/routes/production/shot-analysis/` that stages video + spawns the driver + aggregates JSON (all ComfyUI logic stays in Python — no TS port), and (C) statically verify the build remains clean.

Purpose: Lock in the validated 2026-07-23 capability (geometry pan_right/18px + semantic 近景/follow/刀飞向画面右侧 on shot_003) behind a stable HTTP endpoint so it can be invoked from canvas / orchestration without re-running ad-hoc scripts.

Output: 5 files (2 vendored scripts + 2 new route files + 1 router edit). ~30% context budget. SINGLE plan, 3 tasks, atomic.
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
Project root: /data/workspace/kais-aigc-platform
Branch: feat/shot-analysis-route (already checked out — DO NOT change branches)

@/data/workspace/kais-aigc-platform/AGENTS.md
@/data/workspace/kais-aigc-platform/src/router.ts
@/data/workspace/kais-aigc-platform/src/routes/production/wan22/_shared/config.ts
@/data/workspace/kais-aigc-platform/src/routes/production/flux/sceneGenerate.ts
@/data/workspace/kais-aigc-platform/src/lib/responseFormat.ts
@/data/workspace/kais-aigc-platform/scripts/build.ts
@/tmp/shot_analysis_driver.py

<interfaces>
<!-- Existing contracts the executor imports / mirrors. No codebase exploration needed. -->

From src/routes/production/wan22/_shared/config.ts (mirror this shape):
```typescript
export const WAN22_CONFIG = {
  comfyuiUrl: process.env.WAN22_COMFYUI_URL || process.env.COMFYUI_URL || "http://localhost:8188",
  containerName: process.env.WAN22_CONTAINER_NAME || "comfyui-primary",
  outputDir: process.env.OUTPUT_DIR || "/mnt/agents/output/gpu1",
  comfyuiInputDir: "/root/ComfyUI/input",
  comfyuiOutputDir: "/root/ComfyUI/output",
  pollIntervalMs: 2000,
  pollTimeoutMs: 600_000,
};
```

From src/lib/responseFormat.ts (use these in route responses):
```typescript
export function success<T>(data: T | null = null, message: string = "成功"): ApiResponse;
export function error<T>(message: string = "", data: T | null = null): ApiResponse;  // NOTE: error() takes (message, data) — first arg is the message string
```

From src/routes/production/flux/sceneGenerate.ts (copy-to-container pattern to mirror):
```typescript
function copyToContainer(localPath: string, containerPath: string) {
  const { execSync } = require("child_process");
  try {
    execSync(`docker cp "${localPath}" ${FLUX_CONFIG.containerName}:"${containerPath}"`, { timeout: 30_000 });
  } catch {
    throw new Error(`Failed to copy ${localPath} to container`);
  }
}
```
Router pattern: `const router = express.Router(); router.post("/scene-generate", ...); export default router;`
Zod pattern: `const schema = z.object({...}); const params = schema.parse(req.body);` with `catch (err) { if (err instanceof z.ZodError) return res.status(400).json(error("VALIDATION_ERROR", err.errors)); res.status(500).json(error("INTERNAL", err.message)); }`

router.ts conventions:
- Line 1 hash comment: `// @routes-hash 99aafe6ba256de6315ab6e937f506b4c` (will become stale after edit — leave as-is; no automated hasher found, agent-sync.js is unrelated to route hashing)
- Imports sorted alphabetically within section; routeN = next free integer globally (highest existing = route137 → use route138)
- Production imports live at lines 30-56; production app.use mounts live at lines 169-195
- NOTE: existing production routes mount at `/api/production/...` (no v1). Per LOCKED DESIGN, shot-analysis mounts at `/api/v1/production/shot-analysis` — intentional deviation, do not "fix"

Driver CLI (from /tmp/shot_analysis_driver.py docstring + argparse):
```
python3 shot_analysis_driver.py --shots shots.json --video <container-visible-clip> \
    [--shot-id-range 1 3] [--semantic] [--subject] [--grid-n 20] [--fps 24]
```
Reads COMFYUI_URL from env (default http://localhost:8188). Writes per-shot JSON to <save_dir>/shot_XXX.json where save_dir defaults to absolute `/mnt/agents/output/gpu1/shot_analysis`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task A: Vendor proven driver + write operator README</name>
  <files>scripts/shot-analysis/shot_analysis_driver.py, scripts/shot-analysis/README.md</files>
  <action>
  1. Create directory `scripts/shot-analysis/` if it does not exist.
  2. Copy `/tmp/shot_analysis_driver.py` to `scripts/shot-analysis/shot_analysis_driver.py` BYTE-FOR-BYTE (verbatim). Do NOT modify logic, do NOT add/remove imports, do NOT "improve" it. Use `cp /tmp/shot_analysis_driver.py scripts/shot-analysis/shot_analysis_driver.py` then verify with `diff` (must produce no output). The driver is validated end-to-end as-is — any edit invalidates the validation.
  3. Create `scripts/shot-analysis/README.md` (Chinese, matching the project's comment language convention) documenting:
     - **What it does**: 逐镜头运镜解构 driver — reads `shots.json`, for each shot builds a ComfyUI workflow (geometry layer `ShotGeometryLK` + optional semantic `AILab_QwenVL_Advanced` + optional subject `SAM3Segment` + `SubjectMotionResidual`), POSTs to ComfyUI `/prompt`, polls `/history`, and `ShotJSONMerge` writes per-shot merged JSON.
     - **Prerequisites**: ComfyUI running (default `http://localhost:8188`) with the 3 镜头分析 nodes deployed via the kais-incremental aggregator (`ShotGeometryLK` / `SubjectMotionResidual` / `ShotJSONMerge`), the `AILab_QwenVL_Advanced` node, the `SAM3Segment` node, and the `Qwen3-VL-8B-Instruct` model present.
     - **CLI usage** (copy the argparse contract from the driver's `main()`):
       ```
       python3 shot_analysis_driver.py --shots shots.json --video <container-visible-clip> \
           [--shot-id-range LO HI] [--semantic] [--subject] [--grid-n N] [--fps F] \
           [--qwen-model Qwen3-VL-8B-Instruct] [--quant "8-bit (Balanced)"]
       ```
       Document every flag's purpose and default (`--grid-n` default 20, `--fps` default 24.0).
     - **The 3 layers + flags table**: geometry (always on), `--semantic` (requires Qwen3-VL-8B-Instruct, adds shot_scale/camera_primitive/camera_speed/subject_motion/lens_feel/lighting fields), `--subject` (requires `sam3.pt` at `/root/ComfyUI/models/sam3/sam3.pt`).
     - **Known Limitations** section:
       - Subject layer (`--subject`) is blocked by a sam3.pt download failure (HF xet CDN unreachable from the container; hf-mirror forces xet redirect). Geometry + semantic work end-to-end. This is documented only — do NOT attempt to fix.
       - `--video` path MUST be visible inside the ComfyUI container (the driver hands it to `VHS_LoadVideoPath` running in the container). `shots.json` is read on the HOST where the driver runs. The TS route handles staging via `docker cp`.
     - **Validated example**: shot_003 → geometry `pan_right` / `fast` / ~18px per frame + semantic `近景` / `follow` / `刀飞向画面右侧` / 雾气幽暗神秘. Validated 2026-07-23 on a test clip.
     - **Output**: `<save_dir>/shot_XXX.json` where `save_dir` defaults to absolute `/mnt/agents/output/gpu1/shot_analysis` so output lands on host-mounted storage. The node's `os.path.join(_OUT_DIR, save_dir, ...)` lets an absolute `save_dir` override `_OUT_DIR`.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && diff -q /tmp/shot_analysis_driver.py scripts/shot-analysis/shot_analysis_driver.py && test -f scripts/shot-analysis/README.md && grep -c -E 'ShotGeometryLK|AILab_QwenVL_Advanced|SAM3Segment|sam3.pt|pan_right|shot_003|/mnt/agents/output/gpu1/shot_analysis' scripts/shot-analysis/README.md | awk '{if ($1 >= 5) {exit 0} else {exit 1}}'</automated>
  </verify>
  <done>
  - `scripts/shot-analysis/shot_analysis_driver.py` exists and is byte-identical to `/tmp/shot_analysis_driver.py` (diff returns 0)
  - `scripts/shot-analysis/README.md` exists with all 3 layers, all CLI flags, both known limitations (sam3.pt + container-path), and the validated shot_003 example referenced
  </done>
</task>

<task type="auto">
  <name>Task B: Add THIN production route (config + index.ts) and register in router.ts</name>
  <files>src/routes/production/shot-analysis/_shared/config.ts, src/routes/production/shot-analysis/index.ts, src/router.ts</files>
  <behavior>
  - Body schema validation rejects missing `video` or `shots` with 400 + VALIDATION_ERROR.
  - When `video` starts with `/root/ComfyUI/` or `/mnt/agents/`, no `docker cp` is run (path used as-is).
  - When `video` is any other host path, `docker cp "<video>" <containerName>:<containerInputDir>/<basename>` runs; the container path is handed to the driver.
  - Driver args always include `--shots <shots> --video <containerVideoPath> --grid-n <n> --fps <f>`; appends `--semantic` / `--subject` / `--shot-id-range LO HI` only when provided.
  - On driver exit code 0: response 200 with `{ shots: [...shot JSONs...], count, outputDir }`.
  - On driver non-zero exit: response 500 with stderr tail in the error data.
  </behavior>
  <action>
  ALL ComfyUI workflow-building / polling logic STAYS in the Python driver. This route ONLY: validates body, stages the video into the container, spawns the driver synchronously, reads back the JSON files the driver wrote. No axios → ComfyUI in TS. No workflow JSON building in TS.

  1. **Create `src/routes/production/shot-analysis/_shared/config.ts`** mirroring `WAN22_CONFIG` shape. Exact content:
     ```typescript
     export const SHOT_ANALYSIS_CONFIG = {
       comfyuiUrl: process.env.SHOT_ANALYSIS_COMFYUI_URL || process.env.COMFYUI_URL || "http://localhost:8188",
       containerName: process.env.SHOT_ANALYSIS_CONTAINER_NAME || "comfyui-primary",
       outputDir: process.env.OUTPUT_DIR || "/mnt/agents/output/gpu1",
       driverPath: process.env.SHOT_ANALYSIS_DRIVER || "scripts/shot-analysis/shot_analysis_driver.py",
       pythonBin: process.env.SHOT_ANALYSIS_PYTHON || "python3",
       containerInputDir: "/root/ComfyUI/input",
       shotAnalysisDir: "/mnt/agents/output/gpu1/shot_analysis",
     };
     ```

  2. **Create `src/routes/production/shot-analysis/index.ts`** as a default-exported Express router exposing `POST /` (mounted externally at `/api/v1/production/shot-analysis`). Use top-level `import express from "express";`, `import { z } from "zod";`, `import { execFileSync } from "child_process";`, `import fs from "fs";`, `import path from "path";`, `import { success, error } from "@/lib/responseFormat";`, `import { SHOT_ANALYSIS_CONFIG } from "./_shared/config";`.

     - Define zod schema:
       ```typescript
       const bodySchema = z.object({
         video: z.string().min(1),
         shots: z.string().min(1),
         shot_id_range: z.tuple([z.number().int(), z.number().int()]).optional(),
         semantic: z.boolean().default(false),
         subject: z.boolean().default(false),
         grid_n: z.number().int().min(1).max(200).default(20),
         fps: z.number().min(1).max(120).default(24),
       });
       ```
     - `router.post("/", async (req: any, res: any) => { try { ... } catch (err) { ... } })`.
     - Inside the try:
       a. `const params = bodySchema.parse(req.body);`
       b. **Determine container video path.** If `params.video.startsWith("/root/ComfyUI/")` OR `params.video.startsWith("/mnt/agents/")` → use it directly. Otherwise compute `const containerVideo = path.posix.join(SHOT_ANALYSIS_CONFIG.containerInputDir, path.basename(params.video));` and run `execFileSync("docker", ["cp", params.video, `${SHOT_ANALYSIS_CONFIG.containerName}:${containerVideo}`], { timeout: 30_000 });` — mirror `copyToContainer` from `src/routes/production/flux/sceneGenerate.ts` but prefer `execFileSync` with an argv array (no shell interpolation; safer against paths with spaces).
       c. **Build driver argv** starting with `[SHOT_ANALYSIS_CONFIG.pythonBin, SHOT_ANALYSIS_CONFIG.driverPath, "--shots", params.shots, "--video", containerVideo, "--grid-n", String(params.grid_n), "--fps", String(params.fps)]`. Push `"--semantic"` if `params.semantic`, `"--subject"` if `params.subject`, and `"--shot-id-range", String(lo), String(hi)` if `shot_id_range` is provided.
       d. **Spawn synchronously** with `execFileSync(argv[0], argv.slice(1), { env: { ...process.env, COMFYUI_URL: SHOT_ANALYSIS_CONFIG.comfyuiUrl }, encoding: "utf8", maxBuffer: 16 * 1024 * 1024, timeout: 900_000 })`. Capture stdout to a variable for the response (`driverStdout`). Wrap the call so that on non-zero exit (`execFileSync` throws with `.status`, `.stderr`) you respond 500: `return res.status(500).json(error("SHOT_ANALYSIS_DRIVER_FAILED", { stderrTail: String(err.stderr || "").slice(-2000), stdout: String(err.stdout || "").slice(-2000) }));`
       e. **Aggregate JSON outputs.** Read directory `SHOT_ANALYSIS_CONFIG.shotAnalysisDir`; list files matching `^shot_\d{3}\.json$`; parse each; if `params.shot_id_range` is set, filter to `lo <= Number(match[1]) <= hi`. Sort by shot id ascending. Wrap file-read errors per-file (skip + log to stderr, do not fail the whole request) — partial results are valuable.
       f. `return res.json(success({ shots, count: shots.length, outputDir: SHOT_ANALYSIS_CONFIG.shotAnalysisDir, driverStdout: driverStdout.slice(-2000) }, "Shot analysis complete"));`
     - Catch: `if (err instanceof z.ZodError) return res.status(400).json(error("VALIDATION_ERROR", err.errors));` then `return res.status(500).json(error("SHOT_ANALYSIS_FAILED", err.message));`.
     - `const router = express.Router();` declared before use; `export default router;` at the bottom — follow `src/routes/production/flux/sceneGenerate.ts` structure.

  3. **Register in `src/router.ts`** (do NOT touch the `@routes-hash` comment — leave the stale hash; no automated hasher exists in this repo, agent-sync.js is for Toonflow asset sync and is unrelated):
     - Add `import route138 from "./routes/production/shot-analysis/index";` in ALPHABETICAL position within the production imports — between `route45 from "./routes/production/qwenTts/voiceId";` (line 48) and `route46 from "./routes/production/storyboard/addStoryboard";` (line 49). Alphabetically `shot-analysis` (s-h-o) precedes `storyboard` (s-t-o).
     - Add `app.use("/api/v1/production/shot-analysis", route138);` in the mount block between the qwenTts mount (`app.use("/api/production/qwenTts/voiceId", route45);`, line 187) and the storyboard mount (`app.use("/api/production/storyboard/addStoryboard", route46);`, line 188).
     - Use routeN = **138** (highest existing is route137; next free = 138).
     - NOTE: existing production routes use `/api/production/...` (no v1). Per LOCKED DESIGN, this route mounts at `/api/v1/production/shot-analysis`. Do not "normalize" to match siblings.

  CONSTRAINTS:
  - Do NOT modify the pre-existing dirty files: `src/routes/production/ltx/msr.ts`, `docs/architecture.md`, `docs/ltx-msr-bgm-detection.md`. Touch only the 3 files listed in `<files>` for this task.
  - All ComfyUI workflow construction and `/prompt` + `/history` polling stays in the Python driver. The TS route spawns the driver; it does NOT reimplement ComfyUI client logic.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc --noEmit 2>&1 | tail -40 && grep -n "shot-analysis" src/router.ts | head && test -f src/routes/production/shot-analysis/index.ts && test -f src/routes/production/shot-analysis/_shared/config.ts && ! grep -E "axios|/prompt|/history" src/routes/production/shot-analysis/index.ts</automated>
  </verify>
  <done>
  - `npm run lint` (tsc --noEmit) exits 0 with no errors mentioning `shot-analysis`
  - `src/router.ts` contains exactly one import line referencing `routes/production/shot-analysis/index` and exactly one `app.use("/api/v1/production/shot-analysis", route138);` mount
  - `src/routes/production/shot-analysis/index.ts` exports default Express router, uses zod for body validation, uses `execFileSync` for both `docker cp` and driver invocation, and contains NO axios / ComfyUI `/prompt` / ComfyUI `/history` calls (thin wrapper verified)
  - `src/routes/production/shot-analysis/_shared/config.ts` exports `SHOT_ANALYSIS_CONFIG` with the 7 fields specified
  </done>
</task>

<task type="auto">
  <name>Task C: Static build verification + targeted git add (no live calls)</name>
  <files>(no file edits — verification + targeted staging only)</files>
  <action>
  STATIC checks only. Do NOT start the dev server. Do NOT call ComfyUI. Do NOT `docker cp` anything. Do NOT run the Python driver.

  1. **Build (esbuild bundle):** run `cd /data/workspace/kais-aigc-platform && node scripts/build.js`. This bundles `src/app.ts` → single outfile `data/serve/app.js` (verify by reading `scripts/build.ts` — entryPoint `src/app.ts`, `bundle: true`, `outfile: 'data/serve/app.js'`). It is NOT file-by-file; there is no `build/src/routes/...` tree. A clean exit (exit code 0, "构建完成" / "Built" message) is the pass signal. If esbuild reports TS errors in the new route, fix them in source and re-run.
  2. **Typecheck:** run `npm run lint` (which is `tsc --noEmit`). Must exit 0.
  3. **Assert route is wired:** `grep -c 'shot-analysis' src/router.ts` must return `>= 2` (one import + one mount).
  4. **Assert bundle contains the route:** `grep -c 'shot-analysis' data/serve/app.js` must return `>= 1` (the route string and/or filename reference appears in the bundle — confirms the new module was included).
  5. **Targeted git add** — CRITICAL. The working tree has pre-existing modified files that are NOT ours:
     ```
     M docs/architecture.md
     M docs/ltx-msr-bgm-detection.md
     M src/routes/production/ltx/msr.ts
     ```
     Plus many untracked files. Stage ONLY our 5 deliverables explicitly:
     ```
     git add scripts/shot-analysis/shot_analysis_driver.py \
             scripts/shot-analysis/README.md \
             src/routes/production/shot-analysis/_shared/config.ts \
             src/routes/production/shot-analysis/index.ts \
             src/router.ts
     ```
     Do NOT `git add -A` or `git add .`. After staging, run `git status --short` and verify the only staged changes are the 5 files above (router.ts will show as M; the other 4 as A). If any of the pre-existing modified files appear staged, unstage them with `git restore --staged <path>`.
  6. Do NOT commit — the orchestrator / user decides commit timing. Leave the 5 files staged.

  NOTE on `@routes-hash` comment in `src/router.ts`: editing the file will make the hash stale. No automated hasher exists in this repo (verified: `scripts/agent-sync.js` syncs Toonflow assets, not route hashes). Leave the stale hash in place; do not attempt to recompute it manually. If a future ownership check fails on the hash, that is a separate cleanup task.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && node scripts/build.js && npm run lint && test $(grep -c 'shot-analysis' src/router.ts) -ge 2 && test $(grep -c 'shot-analysis' data/serve/app.js) -ge 1 && git diff --cached --name-only | sort | diff - <(printf 'scripts/shot-analysis/README.md\nscripts/shot-analysis/shot_analysis_driver.py\nsrc/router.ts\nsrc/routes/production/shot-analysis/_shared/config.ts\nsrc/routes/production/shot-analysis/index.ts\n')</automated>
  </verify>
  <done>
  - `node scripts/build.js` exits 0 (clean esbuild bundle to `data/serve/app.js`)
  - `npm run lint` exits 0 (tsc --noEmit clean)
  - `src/router.ts` contains the shot-analysis import + mount
  - `data/serve/app.js` bundle contains the shot-analysis reference (new module bundled in)
  - `git diff --cached --name-only` lists EXACTLY the 5 deliverable files — no `ltx/msr.ts`, no `docs/`, no other pollution
  </done>
</task>

</tasks>

<verification>
Phase-level checks (all static — no container, no ComfyUI, no live HTTP):
1. `diff -q /tmp/shot_analysis_driver.py scripts/shot-analysis/shot_analysis_driver.py` → identical (driver vendored verbatim).
2. `npm run lint` → exit 0 (full typecheck passes, including the new route).
3. `node scripts/build.js` → exit 0 (bundle builds cleanly).
4. `grep -c 'shot-analysis' src/router.ts` → >= 2 (import + mount).
5. `grep -cE 'axios|/prompt|/history' src/routes/production/shot-analysis/index.ts` → 0 (thin wrapper, no ComfyUI client logic in TS).
6. `git diff --cached --name-only` → exactly the 5 deliverables, nothing else staged.
</verification>

<success_criteria>
- The proven driver is vendored UNMODIFIED (diff against /tmp source returns no output).
- The new POST /api/v1/production/shot-analysis endpoint is typechecked, bundled, and mounted.
- The route is a THIN wrapper: it stages video, spawns the driver, aggregates JSON — NO ComfyUI client code in TS.
- Static build + lint pass cleanly.
- Git index contains exactly the 5 intended files; the pre-existing dirty `ltx/msr.ts` / `docs/*.md` are NOT staged.
</success_criteria>

<output>
Create `.planning/quick/260723-pn1-formalize-shot-analysis-vendor-proven-dr/260723-pn1-SUMMARY.md` when done (see summary template).
</output>
