---
context: research
task: character-ref-fidelity
status: in_progress
last_updated: 2026-07-24
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through actual failure this session. Acknowledge before proceeding.

- [ ] **External git wipes**: BOTH repos (`kais-aigc-platform` + `kais-shot-timeline`) had uncommitted edits wiped mid-session by external `git checkout/restore` (concurrent dev on infinite-canvas/flowgraph + v1.1 character_refs). → **Mitigation: commit work promptly; don't leave edits only in working tree.**
- [ ] **ComfyUI VRAM degradation**: after an OOM `execution_interrupted`, ComfyUI连环失败, `/free`+`/queue` 不能恢复, VRAM stuck ~12GB. → **Mitigation: `docker restart comfyui-primary` (preserves `--enable-triton-backend`); check 3090 (nvidia-smi line 2) before multi-shot runs.**
- [ ] **MSR ref order**: subjects = refFilenames[0..N-2] (LiconMSR slot1-4), **background = LAST** (`msr.ts:329-332`). NOT ref1=bg. The old inline comment was wrong.
- [ ] **Dreamina image2image**: multi-ref (3+) can hang at `gen_status:"querying"`; **single-ref single-view works (~30s)**. text2image works but is unfaithful (no real anchoring). For faithful chars: image2image single-ref on real video frames.

## Critical Anti-Patterns
| Pattern | Description | Severity | Prevention |
|---|---|---|---|
| Terse action prompts | action="缓缓抬头" → video model improvises unphysical motion | blocking | action must be full physical chain (who→steps→physics); schema now requires it |
| ref1=bg assumption | old comment said ref1=background (wrong) | advisory | subjects first, bg last |
| Uncommitted edits | wiped by concurrent git ops | blocking | commit promptly |

<current_state>
Regenerating character reference images for 《小江湖》EP01 to match the ORIGINAL VIDEO (user said Dreamina text2image cards didn't look like the real characters). Used Dreamina **image2image** anchored on real frames mined from the 93 shots.
- **3 chars approved by user**: caterpillar, beetle, mantis (i2i images in `workflows/ltx-2.3/shot-timeline-ep01-output/refs/char-source/*_i2i.png`).
- **Centipede**: v1 too short/details off → regenerated **v2 (16:9 wide, length-emphasized)** = `refs/char-source/centipede_i2i_v2.png` (2560×1440). **Awaiting user approval.**
- Compare page: http://100.124.72.88:8123/char-compare.html (real frames | ★new i2i | old card)
- Port 8123 static server running on the shot-timeline-ep01-output dir.
</current_state>

<completed_work>
- **Root fix (upstream, COMMITTED in kais-shot-timeline)**: `spec/schemas/prompts.schema.json#action` + README now require full physical action chain. Commit `761b4c1` on main (local, not pushed). SPEC.md action row edited in-tree but left uncommitted (concurrent WIP there).
- **Source data fix**: all 93 shots' `action` field in `prompts.json` upgraded to full physical chains (migration src: `data/oss/shot-timeline-ep01/action_chains.json`). Both `data/oss/` + `scripts/fixtures/` copies synced.
- **`scripts/gen-from-shot-timeline.ts`**: added `pickRefs()` (per-shot chars, no fallback; subjects-first/bg-last), `buildPrompts()` (refDescription=identity→global, prompt=action+camera→local), `CHAR_IDENTITY`, retired ACTION_OVERRIDE.
- **Character cards v1** (text2image, unfaithful): `refs/char_*_card.png` — being replaced by i2i.
- **Character i2i v1** (image2image, faithful): `refs/char-source/{caterpillar,beetle,mantis,centipede}_i2i.png` + centipede_i2i_v2.png.
- **Real character source frames**: `refs/char-source/{char}_shot{NNN}_{first,last}.jpg` (mined from kais-shot-timeline shot_frames).
- Validated end-to-end: shots 1,2,3 ran (v6) with root-fix source action; shots 1,8,17,37,66,90 ran (v4/v5) earlier.
- docs/ltx-msr-input-guide.md (authoritative MSR input guide); scripts/gen-xjh-charcards-dreamina.sh.
- Memory files written: reference_ltx_msr_input_guide, reference_dreamina_cli, reference_comfyui_vram_degradation, feedback_video_prompts_full_action_chain.
</completed_work>

<remaining_work>
1. **Get user approval on centipede_i2i_v2** (length+details). If still off → regen with better source frame (try `centipede_shot035_first` long-body, or 2-ref) / adjust prompt.
2. **Replace character refs**: swap `char_*_card.png` → the 4 i2i images (caterpillar_i2i, beetle_i2i, mantis_i2i, centipede_i2i_v2). Update `pickRefs()` in gen-from-shot-timeline.ts (char_caterpillar_card.png → new filenames) + ALL_REFS list.
3. **Re-run validation**: shots 1-3 (or 1,8,17,37,66,90) with new faithful refs; compare identity fidelity vs original.
4. **Full 93-shot generation** (~3-4hr GPU): FIRST add a **resilience loop** to the script (on FATAL: `docker restart comfyui-primary` + skip-done + resume), else it dies mid-run on VRAM degradation. Then background run.
</remaining_work>

<decisions_made>
- Character fidelity: image2image (anchored on real frames) over text2image (unfaithful). User-driven.
- Per-shot ref selection (pickRefs): only on-screen characters, NO fallback (user: "实事求是 不需要兜底逻辑").
- Action prompts: full physical chains, root-fixed in source prompts.json + upstream schema (user chose option A).
- Prompt split: refDescription=identity/scene/lighting/style (→PromptRelay global), prompt=action+camera (→local).
</decisions_made>

## Required Reading
1. `docs/ltx-msr-input-guide.md` — MSR input/prompt conventions (subjects-first/bg-last, refDescription/prompt split, 角色卡 spec)
2. `data/oss/shot-timeline-ep01/action_chains.json` — the 93 full action chains (migration source)
3. `scripts/gen-from-shot-timeline.ts` — pickRefs/buildPrompts logic
4. memory: reference_ltx_msr_input_guide, reference_dreamina_cli, reference_comfyui_vram_degradation, feedback_video_prompts_full_action_chain

## Infrastructure State
- ComfyUI: `comfyui-primary` container, int8_convrot LTX 2.3, RTX 3090 (cuda:0). Restart if VRAM degraded.
- Dreamina CLI: `/home/kai/.local/bin/dreamina` (OAuth, 5960 credits, maestro VIP).
- Static server: `python3 -m http.server 8123` on `workflows/ltx-2.3/shot-timeline-ep01-output/` (Tailscale 100.124.72.88).
- Source video shot_frames: `/data/workspace/kais-shot-timeline/output/虫虫武侠小故事《小江湖》第01话：.../shot_frames/` (186 frames).

<next_action>
1. Ask user: is `centipede_i2i_v2.png` (16:9 long) acceptable? (compare page char-compare.html)
2. If yes → rename/swap the 4 i2i images into place as the char refs, update `pickRefs()`/ALL_REFS filenames in gen-from-shot-timeline.ts.
3. Re-run shots 1-3 to validate fidelity; then build resilience loop + full 93.
</next_action>
