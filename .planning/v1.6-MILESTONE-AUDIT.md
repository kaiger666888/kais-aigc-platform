---
milestone: v1.6
milestone_name: Workflow Skill Contract
audit_date: 2026-06-15
status: tech_debt
phases_total: 7
phases_complete: 7
requirements_total: 36
requirements_complete: 35
requirements_deferred: 1
blockers: 0
---

# v1.6 Milestone Audit — Workflow Skill Contract

## Status: ✅ tech_debt (0 blockers, 1 deferred sign-off)

All 7 phases shipped. 35/36 requirements satisfied. The single outstanding item (COMPLIANCE-03) requires live Docker + GPU sign-off — not a code gap, an environment-gated verification step.

---

## Phase Results

| Phase | Name | Status | Verify Script | Assertions |
|-------|------|--------|---------------|------------|
| 28 | Skill Contract Spec + TS Interface | ✅ Complete | verify-phase-28.ts | 12/12 passed |
| 29 | DB Migration + Registry Skeleton | ✅ Complete | verify-phase-29.ts | 29/29 passed |
| 30 | Default Skill Seed + REST API | ✅ Complete | verify-phase-30.ts | 61/61 passed |
| 31 | Pipeline Callback Refactor | ✅ Complete | verify-phase-31.ts | 91/91 passed |
| 32 | Canvas Node Type Registry Integration | ✅ Complete | verify-phase-32.ts | 39/39 passed |
| 33 | kais-movie-agent Compliance + E2E | ✅ Complete (1 skipped) | verify-phase-33.ts | 23 passed, 1 skipped, 0 failed |
| 34 | Skill Author Documentation | ✅ Complete | verify-phase-34.ts | 22/22 passed |

**Total automated assertions:** 277 passed, 1 explicitly skipped, 0 failed.

---

## Requirements Coverage

### Fully satisfied (35/36)

- **CONTRACT-01..06** (6/6) — Phase 28
- **REGISTRY-01..06** (6/6) — Phase 29
- **API-01..06** (6/6) — Phase 30
- **PIPELINE-01..05** (5/5) — Phase 31
- **CANVAS-01..04** (4/4) — Phase 32
- **COMPLIANCE-01, 02, 04, 05** (4/5) — Phase 33
- **DOCS-01..04** (4/4) — Phase 34

### Deferred (1/36)

- **COMPLIANCE-03** — End-to-end golden-path through live Docker + GPU pipeline. Registry-lookup smoke test passes; live runtime exercise requires a running platform with GPU and a real movie-v1 project. Documented as `human_needed` in [33-VERIFICATION.md](phases/33-kais-movie-agent-compliance-e2e/33-VERIFICATION.md). Not a code gap — an environment-gated sign-off step.

---

## What Shipped

### Contract layer (Phases 28-30)
- `src/skills/contract.ts` — TypeScript SkillManifest + NodeTypeDecl + PhaseDecl interfaces (snake_case, versioning rules)
- `src/skills/validator.ts` — zod schema with structured ruleIds (NODE_ID_NAMESPACING, etc.)
- `src/skills/registry.ts` — frozen singleton with O(1) phaseById / nodeTypeById lookups, no silent fallback
- `src/skills/loader.ts` — boot loader hydrates from o_skillRegistry
- `src/skills/defaultSkill.ts` — MOVIE_V1_MANIFEST inlined (Phase 31), seedDefaultIfEmpty zero-config boot

### REST API surface (Phase 30)
- `GET /api/v1/skills` — list summaries
- `GET /api/v1/skills/:skillId` — full manifest
- `POST /api/v1/skills/register` — validate + INSERT + cache update
- `GET /api/v1/skills/:skillId/node-types` — declared node types
- `GET /api/v1/skills/:skillId/phases` — declared phase taxonomy

### Pipeline callback refactor (Phase 31)
- `phase-complete.ts` — uses `registry.phaseById(skill_id, phase).requires_review`; old REVIEW_REQUIRED_PHASES deleted
- `resume.ts` — uses `registry.phaseById(...).order`; old PHASE_ORDER deleted
- `submit-to-review.ts` — open `z.string()` + runtime taxonomy check (400 unknown phase, 500 unregistered skill); old closed enum deleted
- Equivalence regression guard (`verify-phase-31.ts`) with OLD_ snapshots

### Canvas integration (Phase 32)
- `FallbackNode.tsx` — visible "unknown node type" indicator; wired as ReactFlow `default`
- `FlowCanvas.tsx` — built-in renderers explicitly platform primitives (not movie-v1 properties); fetchSkillNodeTypes wired via useEffect
- `canvasApi.ts` — `fetchSkillNodeTypes(skillId)` calls `/api/v1/skills/:skillId/node-types`
- `projectData.ts` — hardcoded NODE_TYPES removed

### Compliance artifact + E2E (Phase 33)
- `docs/skill-author-guide/movie-v1.manifest.json` — install-ready 3810-byte artifact
- `scripts/gen-movie-v1-manifest.ts` — regenerates JSON from TS source on demand
- Register-flow round-trip verified against transient SQLite
- Negative: unknown-phase manifest registers cleanly (descriptive metadata)
- Negative: malformed manifest rejected with structured ruleId

### Documentation (Phase 34)
- `docs/skill-author-guide.md` — field reference, versioning, deploy order, anti-features, annotated example, troubleshooting

---

## Concerns / Follow-ups (non-blocking)

1. **COMPLIANCE-03 live sign-off.** Before production deployment, a human operator must run a real pipeline through the refactored callbacks on Docker + GPU infrastructure. Steps documented in 33-VERIFICATION.md § Human Verification Required.

2. **`src/routes/assets/uploadImage.ts` pre-existing tsc error** (`TS18046: 'err' is of type 'unknown'`, line 123). Not introduced by v1.6 phases; was flagged in Phase 31 SUMMARY as out of scope. Recommend a separate cleanup commit.

3. **PHASE_INGEST_MAP runtime refactor deferred.** Per Phase 31 CONTEXT.md `<deferred>`: the map's values are locked via the equivalence test for descriptive purposes, but the runtime ingest branching in phase-complete.ts still keys on `outputs[].type` directly. A future refactor could consult `phaseDecl.ingest_outputs` — not v1.6 scope.

4. **No standalone `kais-skill validate` CLI.** AUTHORS-03 deferred to v1.7+. Authors currently validate via the inline tsx snippet in the skill-author-guide.

5. **Single-tenant assumption.** v1.6 ships without auth on `/api/v1/skills/register`. Trusted internal network only. Multi-tenant hardening is a v1.7+ concern.

---

## Recommendation

**Ship v1.6.** All code work is complete; automated coverage is comprehensive (277 assertions across 7 phases). The single deferred item (COMPLIANCE-03) is a known environment-gated verification step, not a code or design gap. Recommend:

1. Operator runs live Docker + GPU pipeline smoke test (closes COMPLIANCE-03).
2. Fix the pre-existing `uploadImage.ts` tsc error in a follow-up commit (out of v1.6 scope).
3. Archive v1.6 via `/gsd:complete-milestone v1.6`.
