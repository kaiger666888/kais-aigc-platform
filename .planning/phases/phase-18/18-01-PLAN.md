---
phase: 18-engine-registration-task-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/gold-team/src/v6/engines/base.py
  - docker/gold-team/src/v6/engines/comfyui.py
  - docker/gold-team/src/v6/engines/cloud_base.py
  - docker/gold-team/src/v6/engines/docker_base.py
  - docker/gold-team/src/v6/engines/hunyuan3d.py
  - docker/gold-team/src/v6/engines/hunyuan3d_mv.py
  - docker/gold-team/src/v6/engines/tts.py
  - docker/gold-team/src/v6/engines/tts_http.py
  - docker/gold-team/src/v6/engines/mock.py
  - docker/gold-team/src/v6/engines/joycaption.py
  - docker/gold-team/src/v6/engines/docker_cli.py
  - docker/gold-team/tests/test_engine_registration.py
autonomous: true
requirements: [ENG-01, ENG-02, ENG-04]

must_haves:
  truths:
    - "BaseEngine has a backend_type property returning BackendType enum"
    - "Every engine subclass returns the correct BackendType value"
    - "No per-model Engine subclasses exist for ComfyUI models -- all use ComfyUIEngine"
    - "BackendType enum has exactly 5 values: comfyui, subprocess, cloud, docker, mock"
  artifacts:
    - path: "docker/gold-team/src/v6/engines/base.py"
      provides: "BackendType enum and backend_type property on BaseEngine"
      contains: "class BackendType"
    - path: "docker/gold-team/tests/test_engine_registration.py"
      provides: "Unit tests verifying backend_type for all engine classes"
      min_lines: 30
  key_links:
    - from: "docker/gold-team/src/v6/engines/comfyui.py"
      to: "docker/gold-team/src/v6/engines/base.py"
      via: "BackendType.COMFYUI override"
      pattern: "BackendType\\.COMFYUI"
    - from: "docker/gold-team/src/v6/engines/cloud_base.py"
      to: "docker/gold-team/src/v6/engines/base.py"
      via: "BackendType.CLOUD override"
      pattern: "BackendType\\.CLOUD"
---

<objective>
Add BackendType enum and backend_type property to BaseEngine, and override in all engine subclasses.

Purpose: Establish the backend-type classification system that Plan 02 and Plan 03 depend on. Every engine must report its backend type (COMFYUI / SUBPROCESS / CLOUD / DOCKER / MOCK) so that registration grouping and the engines API can display organized engine listings.
Output: BackendType enum in base.py, backend_type property on all 10+ engine classes, unit tests verifying correct classification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-18/18-RESEARCH.md
</context>

<interfaces>
<!-- Engine class hierarchy the executor needs to know -->

From src/v6/engines/base.py:
```python
class EngineStatus(str, enum.Enum):
    ONLINE = "online"
    OFFLINE = "offline"
    BUSY = "busy"
    ERROR = "error"

class BaseEngine(abc.ABC):
    @property
    @abc.abstractmethod
    def name(self) -> str: ...
    @property
    @abc.abstractmethod
    def engine_id(self) -> str: ...
    @property
    @abc.abstractmethod
    def capabilities(self) -> EngineCapabilities: ...
    # abstract methods: submit, poll, get_output, cancel, health
```

Engine subclass -> BaseEngine mapping:
- ComfyUIEngine(BaseEngine) -- comfyui.py
- BaseCloudEngine(BaseEngine) -- cloud_base.py
- DockerAPIEngine(BaseEngine) -- docker_base.py
- DockerPollingAPIEngine(DockerAPIEngine) -- docker_polling.py
- FaceFusionEngine(DockerAPIEngine) -- facefusion.py
- DockerCLIEngine(BaseEngine) -- docker_cli.py
- Hunyuan3DEngine(BaseEngine) -- hunyuan3d.py
- Hunyuan3DMvEngine(BaseEngine) -- hunyuan3d_mv.py
- TTSTracker(BaseEngine) -- tts.py
- TripleTrackTTSEngine(BaseEngine) -- tts_http.py
- MockEngine(BaseEngine) -- mock.py
- JoyCaptionEngine(BaseEngine) -- joycaption.py
- JimengEngine(BaseCloudEngine) -- cloud_jimeng.py
- KlingEngine(BaseCloudEngine) -- cloud_kling.py
- SeedanceEngine(BaseCloudEngine) -- cloud_seedance.py
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BackendType enum and property to BaseEngine + subclass overrides</name>
  <files>docker/gold-team/src/v6/engines/base.py, docker/gold-team/src/v6/engines/comfyui.py, docker/gold-team/src/v6/engines/cloud_base.py, docker/gold-team/src/v6/engines/docker_base.py, docker/gold-team/src/v6/engines/hunyuan3d.py, docker/gold-team/src/v6/engines/hunyuan3d_mv.py, docker/gold-team/src/v6/engines/tts.py, docker/gold-team/src/v6/engines/tts_http.py, docker/gold-team/src/v6/engines/mock.py, docker/gold-team/src/v6/engines/joycaption.py, docker/gold-team/src/v6/engines/docker_cli.py</files>
  <read_first>
    docker/gold-team/src/v6/engines/base.py
    docker/gold-team/src/v6/engines/comfyui.py
    docker/gold-team/src/v6/engines/cloud_base.py
    docker/gold-team/src/v6/engines/docker_base.py
    docker/gold-team/src/v6/engines/hunyuan3d.py
    docker/gold-team/src/v6/engines/hunyuan3d_mv.py
    docker/gold-team/src/v6/engines/tts.py
    docker/gold-team/src/v6/engines/tts_http.py
    docker/gold-team/src/v6/engines/mock.py
    docker/gold-team/src/v6/engines/joycaption.py
    docker/gold-team/src/v6/engines/docker_cli.py
  </read_first>
  <behavior>
    - Test 1: ComfyUIEngine().backend_type returns BackendType.COMFYUI
    - Test 2: BaseCloudEngine subclass (any) returns BackendType.CLOUD
    - Test 3: DockerAPIEngine subclass returns BackendType.DOCKER
    - Test 4: Hunyuan3DEngine returns BackendType.SUBPROCESS
    - Test 5: Hunyuan3DMvEngine returns BackendType.SUBPROCESS
    - Test 6: TTSTracker returns BackendType.SUBPROCESS
    - Test 7: TripleTrackTTSEngine returns BackendType.SUBPROCESS
    - Test 8: MockEngine returns BackendType.MOCK
    - Test 9: JoyCaptionEngine returns BackendType.COMFYUI
    - Test 10: DockerCLIEngine returns BackendType.DOCKER
  </behavior>
  <action>
    RED phase: Create `docker/gold-team/tests/test_engine_registration.py` with a `TestBackendTypeClassification` class containing 10 tests that instantiate each engine class (using minimal constructor args -- for classes requiring config/container_mgr, mock those args) and assert `engine.backend_type == BackendType.EXPECTED_VALUE`. Run tests to confirm they fail (BackendType not yet defined).

    GREEN phase: Implement the following changes:

    1. In `base.py`: Add `BackendType(str, enum.Enum)` class with values COMFYUI="comfyui", SUBPROCESS="subprocess", CLOUD="cloud", DOCKER="docker", MOCK="mock". Place it after the existing `EngineStatus` enum. Add a non-abstract `backend_type` property on `BaseEngine` that returns `BackendType.MOCK` as the default. Export `BackendType` from the module.

    2. In `comfyui.py`: Override `backend_type` to return `BackendType.COMFYUI`. Import `BackendType` from `base.py`.

    3. In `cloud_base.py`: Override `backend_type` in `BaseCloudEngine` to return `BackendType.CLOUD`. Import `BackendType` from `base.py`. This covers JimengEngine, KlingEngine, SeedanceEngine via inheritance.

    4. In `docker_base.py`: Override `backend_type` in `DockerAPIEngine` to return `BackendType.DOCKER`. Import `BackendType` from `base.py`. This covers DockerPollingAPIEngine and FaceFusionEngine via inheritance.

    5. In `docker_cli.py`: Override `backend_type` in `DockerCLIEngine` to return `BackendType.DOCKER`. Import `BackendType` from `base.py`.

    6. In `hunyuan3d.py`: Override `backend_type` in `Hunyuan3DEngine` to return `BackendType.SUBPROCESS`. Import `BackendType` from `base.py`.

    7. In `hunyuan3d_mv.py`: Override `backend_type` in `Hunyuan3DMvEngine` to return `BackendType.SUBPROCESS`. Import `BackendType` from `base.py`.

    8. In `tts.py`: Override `backend_type` in `TTSTracker` to return `BackendType.SUBPROCESS`. Import `BackendType` from `base.py`.

    9. In `tts_http.py`: Override `backend_type` in `TripleTrackTTSEngine` to return `BackendType.SUBPROCESS`. Import `BackendType` from `base.py`.

    10. In `mock.py`: No change needed -- inherits `BackendType.MOCK` from BaseEngine default.

    11. In `joycaption.py`: Override `backend_type` in `JoyCaptionEngine` to return `BackendType.COMFYUI` (it talks to ComfyUI, per research Pitfall 2 and assumption A3). Import `BackendType` from `base.py`.

    Run tests to confirm all 10 pass.
  </action>
  <verify>
    <automated>cd docker/gold-team && python3 -m pytest tests/test_engine_registration.py -v --tb=short</automated>
  </verify>
  <done>
    - BackendType enum exists in base.py with 5 values
    - BaseEngine.backend_type property returns MOCK by default
    - All 10 engine subclasses return correct BackendType values
    - All 10 backend_type classification tests pass
    - Existing 38 tests still pass (no regressions)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify no per-model ComfyUI Engine subclasses exist + regression</name>
  <files>docker/gold-team/tests/test_engine_registration.py</files>
  <read_first>
    docker/gold-team/tests/test_engine_registration.py
    docker/gold-team/src/v6/engines/comfyui.py
  </read_first>
  <action>
    Add two more tests to the existing `test_engine_registration.py` file:

    1. `test_no_comfyui_model_subclasses`: Grep the engines directory for any class that (a) inherits from BaseEngine and (b) has "comfyui" in its name but is NOT ComfyUIEngine or JoyCaptionEngine. Assert none found. This verifies ENG-02 -- no per-model Engine subclasses for ComfyUI models.

    2. `test_comfyui_engine_uses_workflow_builder`: Assert that `ComfyUIEngine.__module__` is `src.v6.engines.comfyui` and that the class does NOT have any model-specific methods (no methods matching pattern `build_*_workflow`). This confirms all ComfyUI models go through ComfyUIEngine + workflow_builder pattern.

    Run the full test suite to verify no regressions.
  </action>
  <verify>
    <automated>cd docker/gold-team && python3 -m pytest tests/test_engine_registration.py tests/test_executor_routing.py tests/test_workflow_builder.py -v --tb=short</automated>
  </verify>
  <done>
    - test_no_comfyui_model_subclasses passes (verifies ENG-02)
    - test_comfyui_engine_uses_workflow_builder passes
    - All 38 existing tests + all new tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal only | BackendType is a classification enum for internal use -- no untrusted input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | BackendType enum values | accept | Enum values are hardcoded, not user-supplied |
| T-18-SC | Tampering | pip installs | mitigate | No new packages installed in this plan |
</threat_model>

<verification>
cd docker/gold-team && python3 -m pytest tests/ -v --tb=short
</verification>

<success_criteria>
- BackendType enum exists with 5 values in base.py
- Every engine subclass returns correct BackendType
- No per-model ComfyUI Engine subclasses exist
- All tests pass (existing 38 + new tests)
</success_criteria>

<output>
Create `.planning/phases/phase-18/18-01-SUMMARY.md` when done
</output>
