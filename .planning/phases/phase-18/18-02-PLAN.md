---
phase: 18-engine-registration-task-routing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/gold-team/src/v6/executor.py
  - docker/gold-team/tests/test_executor_routing.py
autonomous: true
requirements: [TASK-01, TASK-02, TASK-03, TASK-04]

must_haves:
  truths:
    - "IMAGE_DRAW + params.extra.mode=ipadapter triggers build_flux_ipadapter_workflow"
    - "IMAGE_DRAW + params.extra.mode=pulid triggers build_pulid_flux_workflow"
    - "IMAGE_DRAW + params.extra.mode=instantid triggers build_flux_ipadapter_workflow (reuses IP-Adapter)"
    - "IMAGE_DRAW without params.extra.mode triggers default flux_dev workflow"
    - "params.extra.mode routing takes priority over model-based routing for IMAGE_DRAW tasks"
    - "Lip sync and frame interpolation routing still works (regression from Phase 17)"
  artifacts:
    - path: "docker/gold-team/src/v6/executor.py"
      provides: "IMAGE_DRAW params.extra.mode routing for character consistency"
      contains: "extra_mode.*ipadapter"
    - path: "docker/gold-team/tests/test_executor_routing.py"
      provides: "TestCharacterConsistencyRouting class with routing tests"
      contains: "TestCharacterConsistencyRouting"
  key_links:
    - from: "docker/gold-team/src/v6/executor.py"
      to: "docker/gold-team/src/v6/engines/workflow_builder.py"
      via: "build_flux_ipadapter_workflow and build_pulid_flux_workflow imports"
      pattern: "build_flux_ipadapter_workflow|build_pulid_flux_workflow"
---

<objective>
Add params.extra.mode routing for character consistency workflows (IP-Adapter, PuLID, InstantID) in the executor's IMAGE_DRAW branch.

Purpose: Enable IMAGE_DRAW tasks to select character consistency workflows via params.extra.mode instead of relying on legacy model-based routing. This completes TASK-03 and TASK-04. Lip sync (TASK-01) and frame interpolation (TASK-02) routing already works from Phase 17 -- regression tests confirm it continues working.
Output: IMAGE_DRAW + params.extra.mode routing in executor.py, comprehensive unit tests for character consistency routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-18/18-RESEARCH.md
@.planning/phases/phase-17/17-04-SUMMARY.md
</context>

<interfaces>
<!-- Workflow builder functions the executor calls -->

From src/v6/engines/workflow_builder.py:
```python
def build_flux_dev_workflow(
    prompt, negative_prompt="", width=1024, height=1024,
    steps=28, cfg_scale=3.5, seed=None, ...) -> dict[str, Any]

def build_flux_ipadapter_workflow(
    prompt, reference_image, negative_prompt="", width=1024, height=1024,
    steps=28, cfg_scale=3.5, weight=0.8, start_percent=0.0, end_percent=0.8,
    seed=None, filename_prefix="flux-ipadapter", ...) -> dict[str, Any]

def build_pulid_flux_workflow(
    image_name, prompt, negative_prompt="", width=1024, height=1024,
    steps=28, cfg_scale=3.5, weight=1.0, seed=None,
    filename_prefix="pulid_flux", ...) -> dict[str, Any]
```

From src/v6/models/task.py:
```python
class TaskType(str, enum.Enum):
    IMAGE_DRAW = "image_draw"
    IMAGE_PULID = "image_pulid"
    IMAGE_DRAW_IPADAPTER = "image_draw_ipadapter"
    # ... other types
```

From src/v6/executor.py (current IMAGE_DRAW routing -- lines 229-262):
The executor currently routes IMAGE_DRAW via model-based checks:
- model == "flux-dev" -> build_flux_dev_workflow (line 229)
- model == "flux-dev-ipa" -> build_flux_ipadapter_workflow (line 241)
- Then TaskType.IMAGE_PULID -> build_pulid_flux_workflow (line 263)
- Then else clause (line 480) -> build_flux_dev_workflow default
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add IMAGE_DRAW params.extra.mode routing for character consistency</name>
  <files>docker/gold-team/src/v6/executor.py, docker/gold-team/tests/test_executor_routing.py</files>
  <read_first>
    docker/gold-team/src/v6/executor.py
    docker/gold-team/tests/test_executor_routing.py
    docker/gold-team/src/v6/engines/workflow_builder.py
  </read_first>
  <behavior>
    - Test 1: IMAGE_DRAW + params.extra.mode="ipadapter" + reference_image -> workflow contains IPAdapterFluxLoader node
    - Test 2: IMAGE_DRAW + params.extra.mode="pulid" + image -> workflow contains PulidModelLoader node
    - Test 3: IMAGE_DRAW + params.extra.mode="instantid" + reference_image -> workflow contains IPAdapterFluxLoader node (reuses IP-Adapter)
    - Test 4: IMAGE_DRAW without params.extra.mode (no extra) -> default flux_dev workflow (contains UNETLoader, no IPAdapterFluxLoader)
    - Test 5: IMAGE_DRAW + params.extra.mode="ipadapter" without reference_image -> validation fails
    - Test 6: IMAGE_DRAW + params.extra.mode="pulid" without image -> validation fails
    - Test 7: IMAGE_DRAW + params.extra.mode="instantid" without reference_image -> validation fails
    - Test 8: Legacy model="flux-dev-ipa" still works (backward compatibility, ipadapter workflow built)
    - Test 9: params.extra.mode="ipadapter" takes priority over model="flux-dev" when both set
  </behavior>
  <action>
    RED phase: Add a `TestCharacterConsistencyRouting` class to `tests/test_executor_routing.py` with 9 tests. Each test creates a GenerationTask with type=IMAGE_DRAW and appropriate params, then simulates the executor routing logic to verify the correct workflow builder is selected. Run tests -- they should fail because the executor does not yet handle IMAGE_DRAW + params.extra.mode routing.

    GREEN phase: Modify `executor.py` `_execute_task` to add IMAGE_DRAW params.extra.mode routing. The key change is inserting a new explicit `elif task.type == TaskType.IMAGE_DRAW:` branch BEFORE the existing `model == "flux-dev"` and `model == "flux-dev-ipa"` checks. Within this branch, check `params.extra.mode` first:

    - If `extra_mode == "ipadapter"`: call `build_flux_ipadapter_workflow` with prompt, reference_image (from task.params["reference_image"]), and standard params. Validate reference_image is present, else set task FAILED.
    - If `extra_mode == "pulid"`: call `build_pulid_flux_workflow` with image_name (from task.params["image"] or task.params["reference_image"]), prompt, and standard params. Validate image is present, else set task FAILED.
    - If `extra_mode == "instantid"`: call `build_flux_ipadapter_workflow` (reuses IP-Adapter infrastructure) with prompt, reference_image (from task.params["reference_image"]), and standard params. Validate reference_image is present, else set task FAILED.
    - Else (no mode or unrecognized mode): fall through to the existing model-based routing chain for IMAGE_DRAW (flux-dev, flux-dev-ipa, then default FLUX Dev).

    IMPORTANT: The existing `model == "flux-dev"` check (line 229) and `model == "flux-dev-ipa"` check (line 241) must remain as fallback. The new IMAGE_DRAW params.extra.mode routing must be checked BEFORE these model-based checks so that params.extra.mode takes priority (per research Pitfall 3). The cleanest approach is to restructure lines 229-508 into:
      1. New explicit `elif task.type == TaskType.IMAGE_DRAW:` branch with params.extra.mode checks
      2. The else clause inside that branch falls through to existing model-based logic
      3. Remove the standalone `model == "flux-dev"` and `model == "flux-dev-ipa"` elif branches since they are now inside the IMAGE_DRAW handler

    Run all tests to verify both new tests and all 38 existing tests pass.
  </action>
  <verify>
    <automated>cd docker/gold-team && python3 -m pytest tests/test_executor_routing.py -v --tb=short</automated>
  </verify>
  <done>
    - IMAGE_DRAW + params.extra.mode routes to correct character consistency workflow
    - IP-Adapter, PuLID, and InstantID all route correctly
    - Missing reference image validation returns FAILED status
    - Legacy model-based routing still works as fallback
    - params.extra.mode takes priority over model param
    - All existing 38 tests pass (no regressions to lip sync, frame interp, TRELLIS routing)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client to executor | params.extra.mode value is untrusted client input |
| Executor to workflow_builder | mode value selects which builder function to call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02 | Tampering | params.extra.mode values | mitigate | Only recognized mode strings ("ipadapter", "pulid", "instantid") trigger special routing; unrecognized values fall through to default |
| T-18-03 | Tampering | reference_image param | accept | Passed to workflow_builder which constructs node graph -- no direct filesystem access from client input |
| T-18-SC | Tampering | pip installs | mitigate | No new packages installed in this plan |
</threat_model>

<verification>
cd docker/gold-team && python3 -m pytest tests/ -v --tb=short
</verification>

<success_criteria>
- IMAGE_DRAW + params.extra.mode="ipadapter" triggers build_flux_ipadapter_workflow
- IMAGE_DRAW + params.extra.mode="pulid" triggers build_pulid_flux_workflow
- IMAGE_DRAW + params.extra.mode="instantid" triggers build_flux_ipadapter_workflow (reuses IP-Adapter)
- Default IMAGE_DRAW still routes to flux_dev workflow
- Legacy model-based routing still works as fallback
- All 38+ tests pass with zero failures
</success_criteria>

<output>
Create `.planning/phases/phase-18/18-02-SUMMARY.md` when done
</output>
