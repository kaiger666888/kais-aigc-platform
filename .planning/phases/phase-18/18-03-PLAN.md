---
phase: 18-engine-registration-task-routing
plan: 03
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - docker/gold-team/src/v6/main.py
  - docker/gold-team/src/v6/routers/engines.py
  - docker/gold-team/tests/test_engine_registration.py
autonomous: true
requirements: [ENG-01, ENG-03]

must_haves:
  truths:
    - "Engine registration log shows engines grouped by backend type"
    - "ComfyUIEngine dual-engine (primary/auxiliary) registration visible in grouped log"
    - "/api/v1/engines response includes backend_type field for each engine"
    - "All engine types (ComfyUI, subprocess, cloud, Docker) register correctly after refactor"
  artifacts:
    - path: "docker/gold-team/src/v6/main.py"
      provides: "Grouped engine registration with backend-type section headers and summary"
      contains: "_format_registration_summary"
    - path: "docker/gold-team/src/v6/routers/engines.py"
      provides: "backend_type field in engine listing API response"
      contains: "backend_type"
  key_links:
    - from: "docker/gold-team/src/v6/main.py"
      to: "docker/gold-team/src/v6/engines/base.py"
      via: "BackendType enum import for grouping"
      pattern: "BackendType"
    - from: "docker/gold-team/src/v6/routers/engines.py"
      to: "docker/gold-team/src/v6/engines/base.py"
      via: "engine.backend_type property in API response"
      pattern: "backend_type"
---

<objective>
Restructure main.py engine registration into grouped sections by backend type, add a registration summary log, and update the engines API endpoint to include backend_type.

Purpose: Deliver the visible, organized engine registration that the ROADMAP success criteria require. The backend_type property from Plan 01 enables grouping. This plan wires it into registration logging and the API response. Plan 02's routing changes are independent -- this plan only depends on Plan 01 (BackendType property existing).
Output: Grouped registration in main.py with section headers and summary, backend_type in /api/v1/engines response, tests verifying grouping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-18/18-RESEARCH.md
@.planning/phases/phase-18/18-01-SUMMARY.md
</context>

<interfaces>
<!-- BackendType enum from Plan 01 -->

From src/v6/engines/base.py (after Plan 01):
```python
class BackendType(str, enum.Enum):
    COMFYUI = "comfyui"
    SUBPROCESS = "subprocess"
    CLOUD = "cloud"
    DOCKER = "docker"
    MOCK = "mock"

class BaseEngine(abc.ABC):
    @property
    def backend_type(self) -> BackendType:
        return BackendType.MOCK
```

From src/v6/main.py (current lifespan registration flow):
Lines 59-60: MockEngine registered
Lines 80-97: TTS engines registered (TTSTracker, TripleTrackTTSEngine)
Lines 99-115: Hunyuan3D engines registered
Lines 117-175: ComfyUI engines registered (dual/legacy)
Lines 177-193: JoyCaptionEngine registered
Lines 195-212: Cloud engines registered (Jimeng, Kling, Seedance)
Lines 216-265: Docker/YAML engines registered
Lines 276: Flat log "started (engines: [...])"

From src/v6/routers/engines.py (current list_engines response):
Lines 36-49: Engine dict without backend_type field
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restructure main.py registration into grouped sections with summary</name>
  <files>docker/gold-team/src/v6/main.py</files>
  <read_first>
    docker/gold-team/src/v6/main.py
    docker/gold-team/src/v6/engines/base.py
  </read_first>
  <acceptance_criteria>
    - main.py has a `_format_registration_summary(executor)` function defined before the lifespan function
    - Registration sections are labeled with comment headers: "-- ComfyUI Backend --", "-- Subprocess Backend --", "-- Cloud Backend --", "-- Docker/YAML Backend --", "-- Mock --"
    - Final startup log uses `_format_registration_summary` instead of flat engine ID list
    - No changes to the actual engine registration logic (same engines registered in same order)
    - Import of BackendType from src.v6.engines.base is present at top of file
  </acceptance_criteria>
  <action>
    1. Add `from src.v6.engines.base import BackendType` to the imports at the top of main.py.

    2. Define a module-level helper function `_format_registration_summary(executor)` before the `lifespan` function. This function iterates `executor.list_engines()`, groups them by `engine.backend_type.value`, and returns a formatted string with section headers `[COMFYUI]`, `[SUBPROCESS]`, `[CLOUD]`, `[DOCKER]`, `[MOCK]` listing the engine_id and name for each group. Sections appear in the order: comfyui, subprocess, cloud, docker, mock. Empty sections are omitted.

    3. Add section comment headers to the existing registration blocks in lifespan():
       - Before line 59 (MockEngine): "# -- Mock --"
       - Before line 62 (TTS unified server): "# -- Subprocess Backend --"
       - Before line 117 (ComfyUI engines): "# -- ComfyUI Backend --"
       - Before line 177 (JoyCaption): keep it grouped under ComfyUI section (move the comment "# Register JoyCaption engine..." under the ComfyUI section or add a sub-header)
       - Before line 195 (Cloud engines): "# -- Cloud Backend --"
       - Before line 216 (Docker/YAML): "# -- Docker/YAML Backend --"

    4. Replace the flat startup log at line 276:
       `logger.info("kais-gold-team V6.0 started (engines: %s)", [e.engine_id for e in executor.list_engines()])`
       with:
       `logger.info("kais-gold-team V6.0 started\n%s", _format_registration_summary(executor))`

    Do NOT change the actual registration logic -- same engines, same order, same try/except blocks. Only add comments and change the final log line.
  </action>
  <verify>
    <automated>cd docker/gold-team && python3 -c "from src.v6.main import _format_registration_summary; print('function exists')" && python3 -m pytest tests/ -v --tb=short</automated>
  </verify>
  <done>
    - _format_registration_summary function exists and works
    - Registration sections have clear backend-type comment headers
    - Startup log shows grouped summary instead of flat list
    - All existing tests pass (no changes to engine registration behavior)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add backend_type to engines API response + registration grouping tests</name>
  <files>docker/gold-team/src/v6/routers/engines.py, docker/gold-team/tests/test_engine_registration.py</files>
  <read_first>
    docker/gold-team/src/v6/routers/engines.py
    docker/gold-team/tests/test_engine_registration.py
  </read_first>
  <acceptance_criteria>
    - `/api/v1/engines` response includes `"backend_type": "comfyui"` (or appropriate value) for each executor-managed engine
    - `"backend_type"` field is present in the engine dict returned by `list_engines()`
    - Legacy engines (local-comfyui, cloud-*) have a reasonable backend_type value
    - Tests verify backend_type appears in engine dicts and _format_registration_summary groups correctly
  </acceptance_criteria>
  <action>
    1. In `routers/engines.py` `list_engines()`: Add `"backend_type": engine.backend_type.value` to the engine dict appended for executor-managed engines (after line 48, the "models" key). For the legacy "local-comfyui" entry, add `"backend_type": "comfyui"`. For legacy cloud providers, add `"backend_type": "cloud"`.

    2. Add tests to `tests/test_engine_registration.py`:
       - `test_format_registration_summary`: Create a mock executor with a few engines, call `_format_registration_summary`, verify the output contains section headers `[COMFYUI]`, `[SUBPROCESS]`, etc. with correct engine_ids grouped under each.
       - `test_engines_api_includes_backend_type`: Verify the `list_engines` endpoint logic includes backend_type in engine dicts (can be tested by calling the function logic directly or testing the response structure).

    Run full test suite to verify no regressions.
  </action>
  <verify>
    <automated>cd docker/gold-team && python3 -m pytest tests/ -v --tb=short</automated>
  </verify>
  <done>
    - /api/v1/engines response includes backend_type for every engine
    - _format_registration_summary produces correct grouped output
    - All tests pass (existing + new)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal API | /api/v1/engines is an internal status endpoint -- no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Information Disclosure | /api/v1/engines backend_type | accept | Backend type classification is not sensitive; endpoint is internal |
| T-18-SC | Tampering | pip installs | mitigate | No new packages installed in this plan |
</threat_model>

<verification>
cd docker/gold-team && python3 -m pytest tests/ -v --tb=short
</verification>

<success_criteria>
- Engine registration log shows engines grouped by backend type (COMFYUI, SUBPROCESS, CLOUD, DOCKER, MOCK)
- /api/v1/engines includes backend_type field for each engine
- ComfyUI dual-engine (primary/auxiliary) visible in grouped log
- All engine types register correctly after refactor
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/phase-18/18-03-SUMMARY.md` when done
</output>
