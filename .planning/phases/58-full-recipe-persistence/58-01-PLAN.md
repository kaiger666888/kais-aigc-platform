---
phase: 58-full-recipe-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/flowgraph-v3/ts/src/recipe.ts
  - packages/flowgraph-v3/ts/src/index.ts
  - packages/flowgraph-v3/ts/src/migrate.ts
  - packages/flowgraph-v3/ts/tests/migrate.test.ts
  - packages/infinite-canvas/src/v3/serialize.ts
  - packages/infinite-canvas/src/v3/__tests__/serialize.test.ts
  - scripts/verify-phase-51.ts
autonomous: true
requirements: [RECIPE-01, RECIPE-03]

must_haves:
  truths:
    - "migrate 从 V2 data 袋提取九键全集(prompt/negative/seed/modelVersion/lora/steps/cfg/quant/sageAttention,lora 深结构整袋保真)进 EventNodeV3.params"
    - "serialize 把 canonical params 九键写回 data 袋;params 缺键时同步 delete data 同键——rawData 陈旧值不复活(Pitfall 1 delete 传播)"
    - "serialize 拓宽与 migrate 拓宽同 plan 落地(graph:saved 同屏回读,单侧落地自噬——Pitfall 3 硬约束)"
    - "verify:phase-51 在断言注解更新后保持全绿(Phase 48 先例:旧 verify 断言随现实更新并注记)"
  artifacts:
    - path: "packages/flowgraph-v3/ts/src/recipe.ts"
      provides: "RECIPE_ROUNDTRIP_KEYS 九键映射表 + RECIPE_EDITABLE_FIELDS + RECIPE_KNOWN_KEYS 单点常量"
      contains: "RECIPE_ROUNDTRIP_KEYS"
    - path: "packages/flowgraph-v3/ts/src/migrate.ts"
      provides: "recipeParams 映射表驱动全集提取 + hasRecipe 任意键判定"
      contains: "RECIPE_ROUNDTRIP_KEYS"
    - path: "packages/infinite-canvas/src/v3/serialize.ts"
      provides: "反向覆盖九键写回 + delete 传播"
      contains: "RECIPE_ROUNDTRIP_KEYS"
    - path: "packages/flowgraph-v3/ts/src/index.ts"
      provides: "barrel 导出 recipe 模块"
      contains: "recipe.js"
    - path: "packages/flowgraph-v3/ts/tests/migrate.test.ts"
      provides: "Phase 58 全集提取用例"
      contains: "Phase 58"
    - path: "packages/infinite-canvas/src/v3/__tests__/serialize.test.ts"
      provides: "Phase 58 反向覆盖 + delete 传播用例"
      contains: "Phase 58"
  key_links:
    - from: "packages/infinite-canvas/src/v3/serialize.ts"
      to: "packages/flowgraph-v3/ts/src/recipe.ts"
      via: "runtime import { RECIPE_ROUNDTRIP_KEYS } from '@kais/flowgraph-v3'(路线 A 裁决)"
      pattern: "import \\{ RECIPE_ROUNDTRIP_KEYS \\} from '@kais/flowgraph-v3'"
    - from: "packages/flowgraph-v3/ts/src/migrate.ts"
      to: "packages/flowgraph-v3/ts/src/recipe.ts"
      via: "同包常量 import,映射循环消费"
      pattern: "RECIPE_ROUNDTRIP_KEYS"
    - from: "scripts/verify-phase-51.ts"
      to: "packages/infinite-canvas/src/v3/serialize.ts"
      via: "read() 文本断言(更新后允许唯一一条 Phase 58 常量运行时导入)"
      pattern: "RECIPE_ROUNDTRIP_KEYS"
---

<objective>
打通全配方持久化的数据通道:创建九键映射契约模块 recipe.ts,对称拓宽 serialize(V3→V2 反向覆盖 + delete 传播)与 migrate(V2→V3 全集提取)两个丢弃点。

Purpose: §14 窄通道的本质是 serialize.ts L244-251 与 migrate.ts recipeParams L155-162 两个对称丢弃点;两端之间的服务端/DB/adapter 全部通透(已验证)。graph:saved socket 保存后同屏全图重载,决定 serialize+migrate 必须同 plan 落地(本 plan)。
Output: recipe.ts 契约 + migrate/serialize 拓宽 + 双包单测扩展 + verify-phase-51 断言注解更新。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/58-full-recipe-persistence/58-CONTEXT.md
@.planning/phases/58-full-recipe-persistence/58-RESEARCH.md
@.planning/phases/58-full-recipe-persistence/58-PATTERNS.md
@.planning/phases/58-full-recipe-persistence/58-VALIDATION.md

<interfaces>
<!-- 执行者直接使用以下契约,无需再探索代码库。行号 2026-08-23 planner 复核吻合。 -->

From packages/flowgraph-v3/ts/src/types.ts L181-192 — GenerationParams(配方形状真值源,九具名键 + catchall):
prompt?: string / negative?: string / seed?: number / modelVersion?: string / lora?: {name: string, strength: number}[] / steps?: number / cfg?: number / quant?: string / sageAttention?: boolean

From packages/flowgraph-v3/ts/src/zod.ts L103-115 — generationParamsSchema(zod v3,.catchall(z.unknown()),零改动;.shape 九键即派生源)

From packages/infinite-canvas/src/v3/serialize.ts L244-251 — 现状反向覆盖(丢弃点①):
只 round-trip 三键:n.stage !== 'script' && p.prompt != null → data.prompt;p.seed != null → data.seed;p.modelVersion != null → data.engine。头注释 L10-11「对 @kais/flowgraph-v3 只许 import type」+ L34-36「窄通道(地雷 #3)声明」需同步更新。

From packages/flowgraph-v3/ts/src/migrate.ts L155-167 — 现状窄通道(丢弃点②):
recipeParams 只提取 d.prompt/d.seed/d.engine 三键(engine→modelVersion 键名改写);hasRecipe 同样只查三键(orphan 判定,L489-503 消费点)。

From packages/infinite-canvas/src/store/canvasStore.ts L644-648 — updateEventParams 删除语义(对照,零改动):
patch 值 undefined/null/'' → delete params[key](canonical「空 = 未设置」)。

From scripts/verify-phase-51.ts L160-167 — 活断言(本 plan Task 2 外科更新):
read serialize.ts → 过滤 from '@kais/flowgraph-v3' 行 → 断言全部 /\bimport\s+type\b/。51/52 对 serialize.ts 均只 read() 文本断言、无模块 import(planner 已 grep 验证),故运行时导入不炸脚本,只红这一条断言。

From packages/infinite-canvas/tsconfig.json L22 + vite.config.ts L12 — alias '@kais/flowgraph-v3' → ../flowgraph-v3/ts/src/index.ts(源码直指,运行时导入先例:canvasStore.ts L13 / FlowCanvas.tsx L15 / laneGeometry.ts L10)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: recipe.ts 九键映射契约 + migrate 全集提取</name>
  <files>packages/flowgraph-v3/ts/src/recipe.ts, packages/flowgraph-v3/ts/src/index.ts, packages/flowgraph-v3/ts/src/migrate.ts, packages/flowgraph-v3/ts/tests/migrate.test.ts</files>
  <read_first>
  - packages/flowgraph-v3/ts/src/zod.ts (L103-115 generationParamsSchema 九键形状——映射表 p 侧唯一合法来源)
  - packages/flowgraph-v3/ts/src/migrate.ts (L150-170 现状 recipeParams/hasRecipe;L489-503 orphan 消费点)
  - packages/flowgraph-v3/ts/tests/migrate.test.ts (L206-217 migrateStoryboard 局部图 helper 范式;L239-244 既有窄通道用例)
  - packages/flowgraph-v3/ts/src/layout.ts (L67 STAGE_ORDER 常量导出先例)
  - packages/flowgraph-v3/ts/src/index.ts (L5-13 barrel,.js 后缀惯例)
  </read_first>
  <action>
  1. 新建 packages/flowgraph-v3/ts/src/recipe.ts——**零 import 纯常量模块**(零依赖是 verify-phase-58 相对 import 的前提,PATTERNS 裁定):
     - 导出 RECIPE_ROUNDTRIP_KEYS: ReadonlyArray&lt;{ p: 'prompt'|'negative'|'seed'|'modelVersion'|'lora'|'steps'|'cfg'|'quant'|'sageAttention'; d: string }&gt;,恰好九对——p 侧类型用内联九键字面量联合,**非 keyof GenerationParams**(本文件零 import,无法引用类型别名——此为下方 acceptance「grep -c '^import' = 0」的前提,类型与值同源于 RECIPE_ROUNDTRIP_KEYS 自身)——prompt/prompt、negative/negative、seed/seed、modelVersion/engine(唯一非恒等映射)、lora/lora、steps/steps、cfg/cfg、quant/quant、sageAttention/sageAttention。**negative 必须在往返集**(CONTEXT 锁定「扩到 GenerationParams 全集」;往返≠可编辑,planner 裁决 2)。禁裸字符串数组(防 modelVersion↔engine 映射丢失,RESEARCH Anti-Pattern)。
     - 导出 RECIPE_EDITABLE_FIELDS: readonly 数组,恰好五键 steps/cfg/quant/sageAttention/lora(UI-SPEC §3 面板可编辑高级集;negative/prompt/seed/modelVersion 不在内)。
     - 导出 RECIPE_KNOWN_KEYS: 由 RECIPE_ROUNDTRIP_KEYS 的 p 侧派生(九键,popover 消费;与现 EventParamsPopover L27 的九键集语义等价)。
  2. index.ts barrel 追加一行 export * from './recipe.js'(照抄既有行惯例)。
  3. migrate.ts 拓宽(原位改写,禁手写键列表):recipeParams 由三 if 改为 for 循环消费 RECIPE_ROUNDTRIP_KEYS——d[dk] != null 时 params[pk] = d[dk];lora 数组整袋透传不逐项校验(catchall 已宽容)。hasRecipe 改为检查映射表任意 d 侧键在场(Pitfall 8:防「仅 steps 无 prompt/seed/engine」节点落 orphan import 种子)。
  4. migrate.test.ts 新增 describe 'Phase 58: recipeParams 全集提取',用例:(a) data 带 steps/cfg/quant/sageAttention/negative/lora 的节点 → params 九键全提取且 lora 深结构保真(name+strength);(b) data.engine → params.modelVersion 键名映射回归;(c) 仅 data.steps 无 prompt/seed/engine 的节点不再被 hasRecipe 判 false(构造局部图断言其生成事件 params.steps 在场,不落 orphan sourcePath 种子)。
  </action>
  <verify>
    <automated>cd packages/flowgraph-v3/ts && npx vitest run tests/migrate.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
  - grep -c "^import" packages/flowgraph-v3/ts/src/recipe.ts 输出 0(零依赖纯常量)
  - grep RECIPE_ROUNDTRIP_KEYS packages/flowgraph-v3/ts/src/migrate.ts 至少命中 2 处(import + 消费循环);旧三 if 手写提取(d.prompt != null 等)已从 recipeParams 函数体消失
  - recipe.ts 中 negative 出现在 RECIPE_ROUNDTRIP_KEYS(裁决 2);RECIPE_EDITABLE_FIELDS 恰好五键不含 negative
  - flowgraph-v3 全量 vitest(`npx vitest run`)130 基线不降,新 describe 'Phase 58: recipeParams 全集提取' 至少 3 条用例绿
  - index.ts 含 export * from './recipe.js'
  </acceptance_criteria>
  <done>九键映射契约导出;migrate 全集提取+lora 深透传+hasRecipe 拓宽;单测绿;tsc clean</done>
</task>

<task type="auto">
  <name>Task 2: serialize 反向覆盖拓宽 + delete 传播 + verify-phase-51 断言注解更新</name>
  <files>packages/infinite-canvas/src/v3/serialize.ts, packages/infinite-canvas/src/v3/__tests__/serialize.test.ts, scripts/verify-phase-51.ts</files>
  <read_first>
  - packages/infinite-canvas/src/v3/serialize.ts (L1-40 头注释纪律与地雷 #3 声明;L240-260 现状反向覆盖;L209-371 整体结构)
  - packages/infinite-canvas/src/v3/__tests__/serialize.test.ts (L1-30 头部 import 与根 schema 相对导入先例;L294-393 'Phase 52-02' describe + evt()/minimalGraph()/asset() helper)
  - packages/infinite-canvas/src/store/canvasStore.ts (L644-648 删除语义对照)
  - scripts/verify-phase-51.ts (L160-168 待更新的活断言原文)
  </read_first>
  <action>
  1. serialize.ts 顶部新增运行时 import { RECIPE_ROUNDTRIP_KEYS } from '@kais/flowgraph-v3'(**路线 A 裁决**:仓内运行时导入先例充分,alias 双通;既有 import type 类型导入行全部保留)。反向覆盖三 if 改为映射循环:对 RECIPE_ROUNDTRIP_KEYS 每对 {p: pk, d: dk}——script stage 且 pk === 'prompt' 时 continue(52-02 例外保留);v = params[pk],v != null 时 data[dk] = v,**否则 delete data[dk]**(delete 传播,Pattern 2 全文:防 rawData 陈旧值复活,顺手覆盖 prompt 清空 '' 同款潜在 bug)。
  2. 头注释两处同步更新:L10-11「只许 import type」纪律改为「运行时导入仅限纯常量(recipe.ts,Phase 58 起);类型导入照旧 import type」;L34-36「窄通道(地雷 #3)」声明改写为 Phase 58 解除声明(九键 round-trip + delete 传播)。
  3. scripts/verify-phase-51.ts L160-168 断言外科更新(Phase 48 先例):过滤逻辑改为——运行时导入行(非 import type)仅允许**恰好一条**且其导入绑定恰为 RECIPE_ROUNDTRIP_KEYS,其余 @kais/flowgraph-v3 行仍须 /\bimport\s+type\b/;断言名 detail 注记 "(Phase 58: runtime import allowed for pure-constant recipe.ts only)"。
  4. serialize.test.ts 新增 describe 'Phase 58: 全配方反向覆盖 + delete 传播'(照抄 'Phase 52-02' describe 结构),用例:(a) 九键写回——事件 params 带全套字段 → 产出资产 data 袋九键可见(engine 键对应 modelVersion 值);(b) delete 传播——构造 rawDataByNodeId Map 带 {steps: 50} 陈旧值、事件 params 无 steps → 输出 node.data.steps === undefined;(c) adapt∘serialize round-trip 九键保真(L332-336 同款 compose 断言);(d) 输出图过 FlowGraphV2Schema.safeParse(根 schema 相对 import,L23 先例)。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx vitest run src/v3/__tests__/serialize.test.ts && npx tsc -b && cd ../.. && npm run verify:phase-51</automated>
  </verify>
  <acceptance_criteria>
  - serialize.ts 含运行时导入 import { RECIPE_ROUNDTRIP_KEYS } from '@kais/flowgraph-v3',且既有 import type 行未删
  - serialize.ts 反向覆盖循环含 delete 分支(grep "delete data[" 命中);script stage prompt 例外仍在(grep "script" 相邻 prompt continue 逻辑在位)
  - serialize.ts 头注释不再含「窄通道(地雷 #3)」原句;verify-phase-51.ts 断言含 RECIPE_ROUNDTRIP_KEYS 允许逻辑与 Phase 58 注记
  - npm run verify:phase-51 全绿(51 聚合门不被打断)
  - serialize.test.ts 新 describe ≥4 条用例绿;infinite-canvas 全量 vitest 406 基线不降;tsc -b clean
  </acceptance_criteria>
  <done>serialize 九键写回+delete 传播;51 门注解更新后仍绿;单测绿;双包 tsc clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical params → data 袋(serialize) | 仓内纯变换,不跨信任边界 |
| data 袋 → EventNodeV3.params(migrate) | data 袋内容部分来自管线直写 DB(既定事实),migrate 是该输入的消费者 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering | serialize 拓宽把配方键写入 data 袋(新载体) | accept | data 袋本就是开放 record(管线直写 DB 为既定设计);值只进 JSON blob 与引擎请求体,无代码执行面;在场形状强制由 Plan 02 canvasAssetSchema 五分支落地 |
| T-58-02 | Tampering | migrate 提取的畸形 lora/steps 进 canonical | accept | generationParamsSchema.catchall 宽容不拒,但 validateFlowGraphV3 在 v3 直通模式对具名键保 shape floor(zod.ts 既有行为,零改动);上游 save-v2 校验兜底 |
| T-58-SC | Tampering | npm/pip installs | accept | 本 plan 零包安装(RESEARCH Package Legitimacy Audit:不适用,无 [ASSUMED] 包) |
</threat_model>

<verification>
- 双包全量 vitest:`cd packages/flowgraph-v3/ts && npx vitest run`(130 基线)+ `cd packages/infinite-canvas && npx vitest run`(406 基线)不降,且新增 'Phase 58' describe 全绿
- 三根 tsc:root `npx tsc --noEmit` + infinite-canvas `npx tsc -b` + flowgraph-v3 `npx tsc --noEmit` clean
- `npm run verify:phase-51` 全绿(51 断言注解更新后)
- RECIPE_ROUNDTRIP_KEYS 九键与 Object.keys(generationParamsSchema.shape) 严格相等(Plan 04 verify 门将机器锁死;此处以 recipe.ts 注释 + 单测 (a) 用例先行覆盖)
</verification>

<success_criteria>
- migrate 提取集 = 九键全集,lora 深结构保真(丢弃点②解除)
- serialize 反向覆盖 = 九键写回 + 缺键 delete(丢弃点①解除,清空语义成立)
- serialize 与 migrate 对称落地——同一 commit 序列内完成,graph:saved 同屏回读不再回退高级字段
- verify:phase-51 不因纪律变更而红(断言已外科注记)
</success_criteria>

<output>
Create `.planning/phases/58-full-recipe-persistence/58-01-SUMMARY.md` when done
</output>
