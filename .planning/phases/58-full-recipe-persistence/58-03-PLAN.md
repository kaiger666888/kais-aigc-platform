---
phase: 58-full-recipe-persistence
plan: 03
type: execute
wave: 3
depends_on: ["58-01", "58-02"]
files_modified:
  - packages/infinite-canvas/test/e2e/tests/phase58-recipe.mjs
autonomous: true
requirements: [RECIPE-01, RECIPE-02, RECIPE-03]

must_haves:
  truths:
    - "编辑 steps/cfg/lora 并保存后,/__mock/state wire 层节点 data 带新值(expect.poll 两段式,Pitfall 3)"
    - "page.reload 重开面板后,高级参数输入框仍显示编辑后的值(reload 往返保真,RECIPE-01)"
    - "点击重生成后,execute 请求体 body.params.steps/cfg/lora = 编辑值且未编辑的 quant 原样透传(RECIPE-02/03 整袋断言)"
    - "清空 steps 保存后,wire 层 data.steps 消失——delete 传播 e2e 防线(Pitfall 1)"
    - "落选变体高级控件随整块 disabled 且锁死文案可见"
    - "全量 e2e ≥62+新增 全绿(先 npm run build,e2e 测 dist,Pitfall 5)"
  artifacts:
    - path: "packages/infinite-canvas/test/e2e/tests/phase58-recipe.mjs"
      provides: "RECIPE-01/02/03 三层断言(wire/请求体/canonical)+ fixture 注入"
      min_lines: 150
  key_links:
    - from: "packages/infinite-canvas/test/e2e/tests/phase58-recipe.mjs"
      to: "packages/infinite-canvas/test/e2e/mock-backend/server.mjs"
      via: "save-v2 POST 注入带高级字段的 fixture(REGEN-01-c 范式)+ /__mock/state + getCalls"
      pattern: "save-v2"
    - from: "packages/infinite-canvas/test/e2e/tests/phase58-recipe.mjs"
      to: "packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx"
      via: "advanced-toggle/param-input-*/lora-* testid(UI-SPEC §7 契约)"
      pattern: "advanced-toggle"
---

<objective>
用 Playwright e2e 锁死全配方闭环:面板编辑 → 保存 → wire 反写 → reload 往返 → 重生成请求体整袋断言 → 清空不复活 → 落选只读。

Purpose: RECIPE-01/02/03 的行为级证明。mock DEFAULT_NODES 无高级字段(Pitfall 6),必须先经 save-v2 注入带 steps/cfg/lora/quant/sageAttention 的 fixture;e2e 服务 dist 产物,改源码后必须先 build(Pitfall 5)。
Output: phase58-recipe.mjs + 全量 e2e 回归绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/58-full-recipe-persistence/58-VALIDATION.md
@.planning/phases/58-full-recipe-persistence/58-RESEARCH.md
@.planning/phases/58-full-recipe-persistence/58-UI-SPEC.md

<interfaces>
<!-- helpers.mjs 既有导出(read_first 全文后直接消费,禁新增 testMode hook——UI-SPEC §7 锁定): -->
From packages/infinite-canvas/test/e2e/helpers.mjs:
- test, expect, loadCanvas, nodeSelector, getCalls, switchToCanvasView
- window.__kaisCanvas.getGraph()(main.tsx L43 bridge,canonical 层断言)

<!-- 三层断言范式(phase52-regen.mjs 实证): -->
① wire 层:page.request.get('/__mock/state') → s.canvas.nodes.find(n => n.id === X)?.data?.steps — expect.poll 两段式(先等 wire 再断言面板,防 graph:saved 回读时序)
② 请求体层:getCalls(page) → calls.find(c => c.path === '/api/canvas/execute') → exec.body.params?.steps
③ canonical 层:window.__kaisCanvas.getGraph() 直接断言 EventNodeV3.params

<!-- fixture 注入范式(phase52-regen.mjs L106-145,REGEN-01-c): -->
构造完整 graph(nodes + links)POST /api/canvas/v2/save-v2 → page.reload → switchToCanvasView。注入节点 data 须带 steps/cfg/lora/quant/sageAttention 全套初始值。

<!-- UI-SPEC §7 testid(Plan 02 已实现): -->
advanced-toggle(先点击展开,默认收起)/ param-input-steps / param-input-cfg / param-select-quant / param-select-sage / lora-row-{i} / lora-name-{i} / lora-strength-{i} / lora-remove-{i} / lora-add / advanced-readonly-seed / advanced-catchall / advanced-empty;锁死文案「落选变体配方已并入主事件 variantRecipes,不可单独编辑」
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fixture 注入 + 编辑保存 reload 往返用例组(RECIPE-01/03)</name>
  <files>packages/infinite-canvas/test/e2e/tests/phase58-recipe.mjs</files>
  <read_first>
  - packages/infinite-canvas/test/e2e/tests/phase52-regen.mjs (全文——fixture 注入 L106-145 / reload 往返 L87-100 / 三层断言 L51-84 / 头注释 dist 纪律 L15-16)
  - packages/infinite-canvas/test/e2e/helpers.mjs (全文——导出面与等待范式)
  - packages/infinite-canvas/test/e2e/mock-backend/server.mjs (L46-112 DEFAULT_NODES 现状:data 只有 prompt 无高级字段,Pitfall 6)
  - .planning/phases/58-full-recipe-persistence/58-UI-SPEC.md (§7 testid 契约)
  </read_first>
  <action>
  1. 新建 phase58-recipe.mjs,头注释照写 dist 纪律(改源码须先 cd packages/infinite-canvas && npm run build,地雷 #10 传统)+ 本文件断言三层说明。
  2. fixture 注入(每个 test 独立或 beforeAll 一次):照抄 REGEN-01-c 范式,构造含产生事件链的完整 graph POST /api/canvas/v2/save-v2,注入节点的 data 带 steps: 30 / cfg: 7 / quant: 'fp8' / sageAttention: true / lora: [{ name: 'xl-light', strength: 0.6 }](全套初始值);page.reload → switchToCanvasView。
  3. 用例组 A(RECIPE-01/03 编辑往返):双击注入节点开面板(openDetailPanel 范式:dblclick + detail-panel + prompt-section)→ click advanced-toggle 展开 → 断言输入框显示初始值 → 编辑 steps=50 / cfg=4.5 / lora 首行 strength=0.8 → 点「保存」→ ①wire 层 expect.poll 等 /__mock/state 里该节点 data.steps === 50 && data.cfg === 4.5 && data.lora[0].strength === 0.8(两段式,Pitfall 3);②canonical 层 getGraph() 事件 params 断言;③reload 往返:page.reload → switchToCanvasView → 重开面板 → click advanced-toggle → 断言 param-input-steps 值 50(REGEN-01-b 同款)。
  4. 用例组 B(lora 结构编辑):lora-add 追加行 → 填 name/strength → 保存 → wire 层 data.lora 长度 2 且新行结构 {name, strength} 保真(RECIPE-03 结构保真);lora-remove 删行 → 保存 → wire 层 data.lora 回到 1 行。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npm run build && npx playwright test phase58-recipe</automated>
  </verify>
  <acceptance_criteria>
  - 新文件含 ≥4 条 test(用例组 A 3 条:wire+canonical+reload;组 B 1 条结构编辑;或等价拆分)
  - 断言三层齐备:grep 同时命中 /__mock/state、getCalls 或 getGraph()(canonical)
  - fixture 注入走 save-v2 POST(grep "save-v2" 命中),非对 DEFAULT_NODES 直接断言原始字段(Pitfall 6)
  - 所有用例先 click advanced-toggle(grep "advanced-toggle" 命中)——默认收起契约
  - npx playwright test phase58-recipe 全绿(workers:1,:9876 mock)
  </acceptance_criteria>
  <done>编辑往返+lora 结构编辑 e2e 全绿,wire/canonical/reload 三层断言在位</done>
</task>

<task type="auto">
  <name>Task 2: regen 请求体整袋断言 + 清空 delete 传播 + 落选只读 + 全量回归(RECIPE-02/03)</name>
  <files>packages/infinite-canvas/test/e2e/tests/phase58-recipe.mjs</files>
  <read_first>
  - packages/infinite-canvas/test/e2e/tests/phase52-regen.mjs (regen 触发按钮选择器与 getCalls 断言范式 L73-84)
  - packages/infinite-canvas/test/e2e/tests/phase52-regen.mjs 落选用例(落选节点构造/选择器——锁死文案断言先例)
  - .planning/phases/58-full-recipe-persistence/58-RESEARCH.md (Pitfall 1 清空复活 / Pitfall 2 空 lora)
  </read_first>
  <action>
  在 phase58-recipe.mjs 追加用例组(同 fixture 基底):
  1. 用例组 C(RECIPE-02/03 请求体):面板仅编辑 steps=50 保存 → 点击「重生成」→ getCalls 找 /api/canvas/execute → 断言 body.params.steps === 50 **且** body.params.quant === 'fp8'、body.params.lora 深等于初始值(未编辑字段原样透传——整袋 spread 证明,窄通道不再丢弃);断言 body.params.cfg === 7(未动字段)。
  2. 用例组 D(Pitfall 1 delete 传播):清空 param-input-steps(留空)→ 保存 → expect.poll 等 /__mock/state 该节点 data 里 steps 键消失(s.canvas.nodes.find(...)?.data?.steps === undefined)→ reload 后 getGraph() 事件 params.steps === undefined(清空不复活)。
  3. 用例组 E(空 lora 归一化,Pitfall 2):删光 lora 行 → 保存 → wire 层 data.lora === undefined(非 []);面板 advanced 区显示「暂无 LoRA」空态。
  4. 用例组 F(落选只读):切到落选变体节点(52 落选用例同款构造)→ advanced-toggle 展开后 param-input-steps disabled + prompt-readonly-hint 含「落选变体」锁死文案。
  5. 全量回归:cd packages/infinite-canvas && npm run build && npm run test:e2e —— 62 基线 + 本文件新增全绿。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npm run build && npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
  - 用例组 C 断言 body.params 整袋:编辑值 + 未编辑 quant/lora 原样(grep "params?.steps\|params?.quant\|params?.lora" 命中)
  - 用例组 D 断言 data.steps === undefined 清空不复活(grep 命中 undefined 断言)
  - 用例组 E 断言 data.lora undefined 非 toEqual([])
  - 用例组 F 断言 disabled + 「落选变体」文案
  - phase58-recipe.mjs test 总数 ≥8;全量 test:e2e 13+1 文件、≥62+8 用例全绿
  - 62 基线零回归(phase52-regen 全部仍绿——本 phase 未改 regen 通道的证明)
  </acceptance_criteria>
  <done>RECIPE-02 请求体断言 + RECIPE-03 保留/清空/结构三面 e2e 证明;全量回归绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e → mock-backend :9876 | 隔离测试环境,无生产数据面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-03 | Tampering | e2e fixture 经 save-v2 注入写 mock state | accept | mock-backend 为 Playwright 专属隔离实例(:9876,config workers:1),零生产耦合;不新增 testMode hook(UI-SPEC 锁定,复用既有 /__mock/state 面仅读) |
| T-58-SC | Tampering | npm/pip installs | accept | 本 plan 零包安装 |
</threat_model>

<verification>
- cd packages/infinite-canvas && npm run build(必须先 build——e2e 测 dist,Pitfall 5)
- npx playwright test phase58-recipe 全绿
- npm run test:e2e 全量 ≥62+新增 全绿(62 基线零回归)
- (wave 门)npm run verify:phase-58 在 Plan 04 落地后一并跑
</verification>

<success_criteria>
- RECIPE-01:编辑保存 → wire 反写 → reload 面板值保持,三层断言齐
- RECIPE-02:regen 请求体 body.params 携带编辑后的 steps(且整袋透传未编辑字段)
- RECIPE-03:只改 steps 时 quant/lora 原样;清空 steps 不复活;空 lora 归一化 undefined;lora 行增删结构保真
- 62 e2e 基线零回归
</success_criteria>

<output>
Create `.planning/phases/58-full-recipe-persistence/58-03-SUMMARY.md` when done
</output>
