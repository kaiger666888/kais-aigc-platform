---
phase: 58-full-recipe-persistence
plan: 04
type: execute
wave: 3
depends_on: ["58-01", "58-02"]
files_modified:
  - scripts/verify-phase-58.ts
  - package.json
  - packages/infinite-canvas/test/e2e/probe-58-real.mjs
autonomous: true
requirements: [RECIPE-04, RECIPE-01]

must_haves:
  truths:
    - "verify:phase-58 断言 canvasAssetSchema 配方声明集 ↔ RECIPE_EDITABLE_FIELDS ↔ RECIPE_ROUNDTRIP_KEYS 高级子集三方集合相等——任一侧新增字段未同步另一侧时门变红(RECIPE-04)"
    - "九键全集交叉验证:RECIPE_ROUNDTRIP_KEYS p 侧 ↔ zod.ts generationParamsSchema 九键(regex 文本提取,避免跨版本 import zod 对象)"
    - "forced-failure 自检:shadow assert 全部必须 FAIL,证明门有能力变红"
    - "真机 a-p04-art4 改 steps+cfg → 保存 200 → reload 往返保真 → finally 恢复原图零足迹(delete 传播恰好是被测恢复语义)"
  artifacts:
    - path: "scripts/verify-phase-58.ts"
      provides: "RECIPE-04 聚合门(三方集合相等 + 交叉验证 + 文本证据 + S5 命令门 + forced-failure)"
      contains: "RECIPE_EDITABLE_FIELDS"
    - path: "package.json"
      provides: "verify:phase-58 命令注册"
      contains: "verify:phase-58"
    - path: "packages/infinite-canvas/test/e2e/probe-58-real.mjs"
      provides: "真机零足迹探针(:10588,捕获-恢复模式)"
      min_lines: 100
  key_links:
    - from: "scripts/verify-phase-58.ts"
      to: "packages/flowgraph-v3/ts/src/recipe.ts"
      via: "相对路径 import(零依赖纯常量文件,tsx 直连)"
      pattern: "from \"../packages/flowgraph-v3/ts/src/recipe\""
    - from: "scripts/verify-phase-58.ts"
      to: "src/lib/canvasAssetSchema.ts"
      via: "同仓 import assetDataSchemas(zod v4 同版本),取五分支 shape 键"
      pattern: "assetDataSchemas"
    - from: "packages/infinite-canvas/test/e2e/probe-58-real.mjs"
      to: "http://localhost:10588"
      via: "save-v2 POST 捕获/恢复 + load-v2 往返断言"
      pattern: "10588"
---

<objective>
落成 RECIPE-04 防漂移聚合门与真机零足迹探针:verify-phase-58 三方集合相等 + 命令门 + forced-failure;probe-58-real 复用 probe-52-real Part B 模式在生产 :10588 验证 RECIPE-01 真机往返。

Purpose: RECIPE-04 是 phase 验收门(「任一侧新增字段未同步另一侧时断言变红」);真机探针证明改造在 build/deploy 后的真实链路(非 mock)同样往返保真。三方集合断言构造方式为 planner 裁决 4:recipe.ts 零依赖相对 import + assetDataSchemas 同仓 import + zod.ts/migrate/serialize 文本证据。
Output: verify-phase-58.ts + package.json 注册 + probe-58-real.mjs + 真机验证记录。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/58-full-recipe-persistence/58-CONTEXT.md
@.planning/phases/58-full-recipe-persistence/58-RESEARCH.md
@.planning/phases/58-full-recipe-persistence/58-PATTERNS.md
@.planning/phases/58-full-recipe-persistence/58-VALIDATION.md

<!-- 与 Plan 03 同 wave、零文件重叠,可并行执行;本 plan 不依赖 phase58-recipe.mjs。 -->

<interfaces>
<!-- Plan 01/02 产物(本 plan 断言对象): -->
From packages/flowgraph-v3/ts/src/recipe.ts:
- RECIPE_ROUNDTRIP_KEYS: 九对 {p, d} 映射(modelVersion↔engine 唯一非恒等)
- RECIPE_EDITABLE_FIELDS: 五键 ['steps','cfg','quant','sageAttention','lora']
- 高级子集 = RECIPE_ROUNDTRIP_KEYS.p 侧去掉 prompt/negative/seed/modelVersion,应恰等于 RECIPE_EDITABLE_FIELDS

From src/lib/canvasAssetSchema.ts L61:
- export const assetDataSchemas: Record&lt;string, z.ZodSchema&gt;(五键 script/asset/storyboard/video/audio;每分支 shape 含五配方键,z.shape 取键名)

From packages/flowgraph-v3/ts/src/zod.ts L103-115:
- generationParamsSchema 九键字面量(prompt/negative/seed/modelVersion/lora/steps/cfg/quant/sageAttention)——regex 文本提取交叉验证源

<!-- verify-phase-52 范式(照抄骨架): -->
From scripts/verify-phase-52.ts:
- assert(cond, name, detail) / read(rel) / runCmd(name, cwdRel, cmd, tailLines) / TestResult 结构
- S5 命令门:三根 tsc + 双包 vitest tail;forced-failure:shadowAssert 组全必须 FAIL,意外 PASS 整门红
- package.json verify 注册块 L43-51(追加 verify:phase-58)

<!-- probe 范式(照抄 Part B): -->
From packages/infinite-canvas/test/e2e/probe-52-real.mjs:
- BASE localhost:10588 / QS projectId=9999&episodesId=1&testMode=1 / NODE_A a-p04-art4(prompt-only,生产零 steps 存量——RESEARCH Open Question 3 裁定同锚)
- stripUpdatedAt/deepEqual 深比对(L44-59,剔 meta.updatedAt/lastEventId);openPanel(L119-126 focus query → 画布 → 等 react-flow__node → detail-panel → prompt-section);finally saveV2(原图) 恢复 + 复核净足迹
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: verify-phase-58.ts 聚合门 + package.json 注册(RECIPE-04)</name>
  <files>scripts/verify-phase-58.ts, package.json</files>
  <read_first>
  - scripts/verify-phase-52.ts (全文——骨架/S5 命令门/forced-failure/read+regex 文本断言范式)
  - packages/flowgraph-v3/ts/src/recipe.ts (Plan 01 产物——被断言常量)
  - packages/flowgraph-v3/ts/src/migrate.ts (Task 1 后的映射循环——文本证据对象)
  - packages/infinite-canvas/src/v3/serialize.ts (Task 2 后的反向覆盖——文本证据对象)
  - src/lib/canvasAssetSchema.ts (五分支声明——shape 键断言对象)
  - packages/flowgraph-v3/ts/src/zod.ts (L103-115 九键——regex 交叉验证对象)
  - package.json (L43-51 verify 注册块)
  </read_first>
  <action>
  1. 新建 scripts/verify-phase-58.ts,骨架照抄 verify-phase-52.ts(assert/read/runCmd/TestResult):
  2. 三方集合相等(planner 裁决 4 构造):
     - 常量侧:import { RECIPE_ROUNDTRIP_KEYS, RECIPE_EDITABLE_FIELDS } from "../packages/flowgraph-v3/ts/src/recipe"(零依赖文件,tsx 相对 import 直连,PATTERNS 裁定)。
     - schema 侧:import { assetDataSchemas } from "../src/lib/canvasAssetSchema"(同仓 zod v4);断言五个类型键(script/asset/storyboard/video/audio)每分支 Object.keys(shape) 包含 RECIPE_EDITABLE_FIELDS 全部五键;并断言五分支配方键集合互相相等。
     - 三方相等:RECIPE_EDITABLE_FIELDS 排序后 === canvasAssetSchema 任一分支配方键(五键)排序后 === RECIPE_ROUNDTRIP_KEYS.p 侧去掉 prompt/negative/seed/modelVersion 后的排序集(高级子集)——三串字符串严格相等。
     - 九键交叉验证:read packages/flowgraph-v3/ts/src/zod.ts,regex 提取 generationParamsSchema 对象块九键字面量,排序后 === RECIPE_ROUNDTRIP_KEYS.p 侧排序集(**不 import zod 对象**——三处 zod 版本分裂,Pitfall 4/A3)。
  3. 消费证据文本断言(read + includes,52 范式):migrate.ts 含 RECIPE_ROUNDTRIP_KEYS import 与消费(禁手写键列表——断言旧式 "d.steps != null" 等独立 if 不在 recipeParams 区域回潮);serialize.ts 含运行时 import RECIPE_ROUNDTRIP_KEYS from '@kais/flowgraph-v3' + delete 传播行(delete data[dk] 或等价)+ script stage prompt 例外。
  4. nullish/optional 计数锁:canvasAssetSchema.ts 中配方五键 optional 声明计数(每键 ×5 分支,如 sageAttention: z.boolean().optional() 计 5;至少对 sageAttention/lora 两键计数锁,防分支漏声明)。
  5. S5 命令门(照抄 52 L211-216):三根 tsc(root --noEmit / infinite-canvas -b / flowgraph-v3 --noEmit)+ 双包 vitest run tail。
  6. forced-failure 自检(照抄 52 L218-234):shadowAssert 组构造必然失败断言(如断言 EDITABLE 含 'sampler'——A1 裁定 sampler 不存在,此断言必须 FAIL),全部 FAIL 才算门绿;意外 PASS 整门红。
  7. package.json scripts 块 L43-51 区追加 "verify:phase-58": "npx tsx scripts/verify-phase-58.ts"(仅此一行,不动其他注册)。
  </action>
  <verify>
    <automated>npm run verify:phase-58</automated>
  </verify>
  <acceptance_criteria>
  - npm run verify:phase-58 输出全绿;含三方集合相等断言、九键交叉验证、消费证据、S5 命令门、forced-failure 各段
  - grep -c "verify:phase-58" package.json = 1
  - 门能红:人为临时在 assetDataSchemas 注释掉 sageAttention 一行重跑,三方断言变红(执行时验证后还原)——forced-failure 机制 + 手测一次
  - verify 脚本无跨包 zod 对象 import(源断言:import 行仅 recipe 相对路径 + 同仓 canvasAssetSchema)
  - shadow assert 含 'sampler' 必然失败项(A1:sampler 不存在,字段集已锁定五键)
  </acceptance_criteria>
  <done>RECIPE-04 聚合门注册并全绿;forced-failure 证明门可红;三方集合机器锁死</done>
</task>

<task type="auto">
  <name>Task 2: probe-58-real.mjs 真机零足迹探针 + deploy + 验证</name>
  <files>packages/infinite-canvas/test/e2e/probe-58-real.mjs</files>
  <read_first>
  - packages/infinite-canvas/test/e2e/probe-52-real.mjs (全文——Part B 捕获-恢复 L93-188 / stripUpdatedAt+deepEqual L44-59 / openPanel L119-126 / 头注释部署前置纪律 L13)
  - .planning/phases/58-full-recipe-persistence/58-UI-SPEC.md (§7 data-testid 契约(L162)——advanced-toggle/param-input-*/lora-* 选择器真值源;本 plan 与 Plan 03 同 wave 3 并行、无依赖,**不读 phase58-recipe.mjs**——该文件并行执行时可能尚不存在)
  - scripts/deploy-canvas.sh (部署脚本用法)
  </read_first>
  <action>
  1. 部署前置(10588 跑 build 产物,须 rebuild+restart):cd packages/infinite-canvas && npm run build → 根仓 bash scripts/deploy-canvas.sh → npm run build:server → 重启 dev server(沿用 probe-52-real 头注释记载的重启方式);探针头注释照写此前置纪律。
  2. 新建 probe-58-real.mjs 照抄 probe-52-real Part B 骨架:BASE http://localhost:10588,QS projectId=9999&episodesId=1&testMode=1,NODE_A a-p04-art4(与 probe-52 同锚,生产零 steps 存量=纯增量无回填)。
  3. 流程:GET load-v2 捕获原图 → openPanel(focus=NODE_A → 点「画布」→ 等 .react-flow__node → detail-panel → prompt-section → **等 advanced-toggle 并 click 展开**)→ 编辑 param-input-steps=40 / param-input-cfg=6 → 点「保存」→ 等保存 200(probe-52 同款 saveStatuses 断言)→ 断言 wire:重新 load-v2 该节点 data.steps === 40 && data.cfg === 6(RECIPE-01 真机往返)→ 面板 reload 往返:focus 重进 → advanced-toggle 展开 → param-input-steps 值 40。
  4. finally 零足迹恢复(恢复即被测语义):saveV2 POST 原图(捕获态)→ 复核 load-v2 后 stripUpdatedAt 深比对原图相等(净足迹 = 0;新增键的删除恰好走 Plan 01 delete 传播)。
  5. 若 :10588 不可达或重启失败:探针输出 SKIP 理由并退出非零仅限断言段——延后探针不阻塞 verify 门(RESEARCH Environment Availability fallback 条款),但 SUMMARY 必须记录未跑原因。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && node test/e2e/probe-58-real.mjs</automated>
  </verify>
  <acceptance_criteria>
  - probe 输出:捕获 → 编辑 steps/cfg 保存 200 → wire 断言 40/6 → 面板往返 40 → 恢复 deepEqual 通过(净足迹 0)
  - 探针含 finally 恢复块与 stripUpdatedAt/deepEqual(grep 命中)
  - 探针头注释含部署前置序列(build → deploy-canvas.sh → build:server → restart)
  - 结束后 :10588 GET load-v2 返回图与捕获原图一致(无残留 steps/cfg 在 a-p04-art4)
  - SUMMARY 记录探针结果;若环境不可达,记录 SKIP 理由与补跑命令
  </acceptance_criteria>
  <done>真机 :10588 全配方往返保真实证;零足迹恢复;phase 收尾三件套(verify 门 + e2e + probe)齐绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe → 生产 :10588 / 生产 DB | 探针对生产图执行真实编辑与保存(唯一触生产面) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04 | Tampering | probe 写生产 canvas_nodes(编辑保存) | mitigate | 零足迹捕获-恢复模式:GET load-v2 先捕获原图,finally saveV2 原图恢复 + stripUpdatedAt deepEqual 复核净足迹=0;目标节点 a-p04-art4 生产零 steps 存量,编辑为纯增量 |
| T-58-05 | DoS | deploy/restart 时序错误导致探针打旧产物 | mitigate | 探针头注释锁定前置序列(build → deploy-canvas.sh → build:server → restart),10588 rebuild 纪律(memory 教训);重启失败输出 SKIP 而非假绿 |
| T-58-SC | Tampering | npm/pip installs | accept | 本 plan 零包安装 |
</threat_model>

<verification>
- npm run verify:phase-58 全绿(三方集合相等 + 九键交叉 + 消费证据 + 计数锁 + S5 命令门 + forced-failure)
- 门可红性:临时注释 canvasAssetSchema 一行配方声明 → 门变红 → 还原(执行时验证)
- node test/e2e/probe-58-real.mjs 零足迹通过(或显式 SKIP 记录)
- phase 收尾对齐 VALIDATION.md phase gate:verify 绿 + e2e 全量绿(Plan 03)+ probe 通过
</verification>

<success_criteria>
- RECIPE-04:canvasAssetSchema ↔ RECIPE_EDITABLE_FIELDS ↔ migrate 提取集三方一致被机器锁死;任一侧漂移即红;forced-failure 证明门有效
- RECIPE-01 真机实证:生产 :10588 编辑 steps/cfg → 保存 → 重载往返保真 → 零足迹恢复
- verify:phase-58 注册为 phase 验收门,Phase 50 式聚合门传统延续
</success_criteria>

<output>
Create `.planning/phases/58-full-recipe-persistence/58-04-SUMMARY.md` when done
</output>
