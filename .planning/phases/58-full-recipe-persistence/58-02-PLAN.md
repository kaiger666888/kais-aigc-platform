---
phase: 58-full-recipe-persistence
plan: 02
type: execute
wave: 2
depends_on: ["58-01"]
files_modified:
  - packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx
  - packages/infinite-canvas/src/components/eventParams/EventParamsPopover.tsx
  - src/lib/canvasAssetSchema.ts
autonomous: true
requirements: [RECIPE-01, RECIPE-03]

must_haves:
  truths:
    - "详情面板「高级参数」折叠区(默认收起)可编辑 steps/cfg/quant/sageAttention/lora;seed/modelVersion/catchall 只读展示"
    - "共享「保存」按钮治理整个编辑器:patch 只含 dirty 字段,清空 number/未设置 select → undefined,空 lora 数组归一化为 undefined(非 [],Pitfall 2)"
    - "dirty 时重生成 disabled(防半编辑);graph:saved 回读 setGraph 后 draft 重置,不残留 dirty(Pitfall 9)"
    - "落选变体/无事件 → 整块 PromptSection 含高级控件只读,锁死文案不变"
    - "popover KNOWN_KEYS 换共享常量源,视觉与分组渲染零改"
    - "canvasAssetSchema 五类型分支(audio/video/asset/storyboard/script)各声明五个 optional 配方字段,在场形状强制"
  artifacts:
    - path: "packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx"
      provides: "PromptSection 高级参数折叠区编辑器(52-03 三态扩展,零新状态)"
      contains: "advanced-toggle"
    - path: "packages/infinite-canvas/src/components/eventParams/EventParamsPopover.tsx"
      provides: "KNOWN_KEYS 换源 RECIPE_KNOWN_KEYS(一行换源)"
      contains: "RECIPE_KNOWN_KEYS"
    - path: "src/lib/canvasAssetSchema.ts"
      provides: "五类型配方字段 optional 声明(RECIPE-04 断言侧 + V5 形状强制)"
      contains: "sageAttention"
  key_links:
    - from: "packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx"
      to: "packages/infinite-canvas/src/store/canvasStore.ts"
      via: "persistEventParams(evt.id, dirtyFieldsPatch)——store 零改动,泛型 patch"
      pattern: "persistEventParams"
    - from: "packages/infinite-canvas/src/components/eventParams/EventParamsPopover.tsx"
      to: "packages/flowgraph-v3/ts/src/recipe.ts"
      via: "import { RECIPE_KNOWN_KEYS } from '@kais/flowgraph-v3'"
      pattern: "RECIPE_KNOWN_KEYS"
    - from: "src/lib/canvasAssetSchema.ts"
      to: "packages/flowgraph-v3/ts/src/recipe.ts"
      via: "形状字面量对齐(禁跨包 import zod 对象,Pitfall 4);集合相等由 verify-phase-58 机器锁死"
      pattern: "lora: z.array"
---

<objective>
把配方编辑能力交到用户手里:PromptSection 扩「高级参数」折叠区编辑器(UI-SPEC 契约),popover KNOWN_KEYS 换共享常量源,canvasAssetSchema 五类型声明配方字段。

Purpose: RECIPE-01 的编辑入口与 RECIPE-03 的复杂结构编辑都落在面板(CONTEXT 锁定面板为主编辑面,popover 只读);canvasAssetSchema 声明是 RECIPE-04 三方集合门的断言侧与 V5 在场形状强制。
Output: 面板高级参数编辑器 + popover 换源 + schema 五分支声明。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/58-full-recipe-persistence/58-CONTEXT.md
@.planning/phases/58-full-recipe-persistence/58-UI-SPEC.md
@.planning/phases/58-full-recipe-persistence/58-PATTERNS.md
@.planning/phases/58-full-recipe-persistence/58-RESEARCH.md

<!-- 依赖 Plan 01 产物:recipe.ts 契约(下方 interfaces)。本 plan 不改 serialize/migrate/store/execute.ts。 -->

<interfaces>
<!-- Plan 01 创建的契约(本 plan 消费): -->
From packages/flowgraph-v3/ts/src/recipe.ts (via '@kais/flowgraph-v3' barrel):
- RECIPE_EDITABLE_FIELDS: readonly ['steps','cfg','quant','sageAttention','lora'] — 面板可编辑高级集
- RECIPE_KNOWN_KEYS: 九键集合(prompt/negative/seed/modelVersion/lora/steps/cfg/quant/sageAttention 的 p 侧) — popover 消费

<!-- 既有接口(零改动,直接消费): -->
From packages/infinite-canvas/src/store/canvasStore.ts L623-689:
- persistEventParams(eventId: string, patch: Partial<GenerationParams>): 乐观写 + saveCanvasGraph + 失败外科回滚 + toast「配方保存失败已回滚: {message}」
- updateEventParams 语义: patch 值 undefined/null/'' → delete params[key](清空 = 删除键)

From packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx L617-739:
- PromptSection({ asset }): 三态(editable / readonly 落选 curation==='deprecated' / readonly 无事件)
- draft 重置范式 L633-638: useEffect(() => setDraft(canonicalPrompt), [evt?.id, canonicalPrompt])
- 保存范式 L676-683: disabled={!dirty || saving},dirty 时 background theme.node.script(#E0B665)
- 重生成 L694-697: executeNode(..., { prompt, params: { ...evt.params, prompt } }) — 整袋 spread,零改动

From src/lib/canvasAssetSchema.ts L61-141:
- assetDataSchemas: Record<string, z.ZodSchema>,五分支 audio L65 / video L78 / asset L93 / storyboard L116 / script L132
- 数组可选字段直接字面量声明先例: asset 分支 views: z.array(z.string()).optional()(L107-110)

<!-- UI-SPEC §7 testid 契约(e2e Plan 03 消费,必须逐字实现): -->
advanced-toggle(aria-expanded + data-state="collapsed|expanded" + data-dirty="true|false") / advanced-section(data-state) / param-input-steps / param-input-cfg / param-select-quant / param-select-sage / advanced-readonly-seed / advanced-readonly-modelVersion / lora-row-{i} / lora-name-{i} / lora-strength-{i} / lora-remove-{i}(aria-label="移除此 LoRA") / lora-add / advanced-catchall / advanced-empty
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PromptSection 高级参数折叠区编辑器</name>
  <files>packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx</files>
  <read_first>
  - packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx (L617-739 PromptSection 三态/保存/重生成/draft 重置范式——扩展基底)
  - .planning/phases/58-full-recipe-persistence/58-UI-SPEC.md (全文——组件契约/控件映射/文案/状态机/dirty 语义/testid,本 task 的验收标准)
  - packages/infinite-canvas/src/components/eventParams/EventParamsPopover.tsx (L177-185 KvRow 只读行控件对照;L116-146 catchall JSON.stringify 先例)
  - packages/infinite-canvas/src/store/canvasStore.ts (L623-689 persistEventParams/updateEventParams 语义)
  </read_first>
  <action>
  在 PromptSection 的 prompt textarea 之下新增「高级参数」折叠区(52-03 三态范式扩展,零新状态)。从 '@kais/flowgraph-v3' 导入 RECIPE_EDITABLE_FIELDS(驱动控件生成,禁本地重列字段)。UI-SPEC §1-§7 全量契约:
  1. 折叠区头:advanced-toggle 全宽按钮,SectionLabel 字型(11px/600/uppercase/0.05em,theme.text.secondary)+ chevron ▸/▾ + dirty 圆点 ●(10px,#E0B665,任一高级字段 dirty 时);**默认收起**,组件本地 state(切节点重置,可接受)。
  2. 控件映射(advanced-section 展开体,gap 8,marginTop 8,透明背景):steps → input[type=number](整数 min 1 max 100 step 1,param-input-steps);cfg → input[type=number](min 0 max 20 step 0.5,param-input-cfg);quant → native select(param-select-quant,选项 未设置/fp8/fp16/int8/bf16,**canonical 值不在选项列表时注入为额外选中项**,禁静默 coerce——RECIPE-03 保真);sageAttention → native select(param-select-sage,未设置/是/否);lora → 行编辑器(lora-row-{i}:name text input placeholder「LoRA 名称」+ strength number input min -1 max 2 step 0.05 宽 72px + lora-remove-{i} ✕ aria-label「移除此 LoRA」;lora-add「+ 添加 LoRA」追加 { name: '', strength: 1 })。空态:「暂无 LoRA」+同行添加按钮;展开且全空时 advanced-empty「暂无高级参数」。
  3. 只读行(KvRow 范式,mono 值):advanced-readonly-seed(seed 值 + caption「在事件芯片 popover 换 seed 重跑」——seed 编辑权在 popover reroll 通道,CONTEXT 锁定)/ advanced-readonly-modelVersion / advanced-catchall(RECIPE_KNOWN_KEYS 之外字段 JSON.stringify 只读展示,禁编辑——防误伤管线私有字段)。
  4. dirty/save 语义(UI-SPEC §5):dirty = promptDirty || stepsDirty || cfgDirty || quantDirty || sageDirty || loraDirty(lora 行深比较);共享「保存」按钮 patch **只含 dirty 字段**;清空 number/未设置 select → patch 传 undefined(触发 store 删除键);lora 保存前归一化——trim 后空名行丢弃,结果空数组 → patch 传 **undefined 非 []**(Pitfall 2);重生成 dirty 时 disabled + title「请先保存,再重生成(防半编辑误触发)」。
  5. draft 重置:各高级字段配同款 useEffect(依赖 evt?.id + 各 canonical 值,Pattern 3/Pitfall 9——防「保存回读后仍 dirty → regen 永久禁用」)。
  6. 三态:readonly 分支(落选/无事件)整块覆盖高级控件(disabled,值仍可展开查看);prompt-readonly-hint 两条文案逐字保留「落选变体配方已并入主事件 variantRecipes,不可单独编辑」/「无产生事件,prompt 不可编辑」(phase52 e2e 锁死,禁改)。
  7. 样式:控件 28px 高 background theme.bg.input / border theme.border.default / radius 6 / padding 4px 8px / fontSize 12 / 数值 mono(var(--cv-font-mono));focus border theme.border.strong;disabled theme.text.disabled;#E0B665 accent 仅 UI-SPEC 保留清单三处;禁 raw hex。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx tsc -b && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
  - testid 契约全在位:grep -c "param-input-steps\|param-select-quant\|lora-add\|advanced-catchall\|advanced-empty\|advanced-toggle" NodeDetailPanel.tsx ≥ 6,且 lora-remove 含 aria-label="移除此 LoRA"
  - advanced-toggle 含 data-state 与 data-dirty 属性(UI-SPEC §7)
  - lora 归一化代码可 grep:空数组路径传 undefined 非 [](源断言:归一化函数/三元含 undefined)
  - 锁死文案逐字在位:「落选变体配方已并入主事件 variantRecipes,不可单独编辑」「无产生事件,prompt 不可编辑」「在事件芯片 popover 换 seed 重跑」
  - infinite-canvas 全量 vitest 406 基线不降(52-03/52-08 既有面板用例不红);tsc -b clean
  - 重生成整袋 spread(L694-697 语义)未被改动
  </acceptance_criteria>
  <done>高级参数折叠区按 UI-SPEC 契约可用;三态/dirty/save/归一化语义正确;基线不降</done>
</task>

<task type="auto">
  <name>Task 2: popover KNOWN_KEYS 换源 + canvasAssetSchema 五类型配方字段声明</name>
  <files>packages/infinite-canvas/src/components/eventParams/EventParamsPopover.tsx, src/lib/canvasAssetSchema.ts</files>
  <read_first>
  - packages/infinite-canvas/src/components/eventParams/EventParamsPopover.tsx (L27 本地 KNOWN_KEYS 九键;L93 otherEntries 过滤;L116-146 分组渲染——三处中仅 L27 换源,其余零改)
  - src/lib/canvasAssetSchema.ts (L61-141 assetDataSchemas 五分支;L107-110 views 数组可选声明先例;audio/video 分支已有 engine 必填位)
  - packages/flowgraph-v3/ts/src/recipe.ts (Plan 01 产物——RECIPE_KNOWN_KEYS 语义)
  </read_first>
  <action>
  1. EventParamsPopover.tsx:删除 L27 本地 const KNOWN_KEYS = new Set([...]),改为 import { RECIPE_KNOWN_KEYS } from '@kais/flowgraph-v3' 并将 L93 引用换名(或本地 const KNOWN_KEYS = RECIPE_KNOWN_KEYS 一行别名,保持下游引用零改)。**视觉与分组渲染零改**:提示词/采样(seed·steps·cfg·modelVersion·quant·sageAttention)/LoRA/catchall 分组、「🎲 同配方换 seed 重跑」、otherEntries 过滤逻辑全部不动(CONTEXT 锁定 popover 只读+换 seed 定位)。
  2. canvasAssetSchema.ts:assetDataSchemas 五分支(audio L65 / video L78 / asset L93 / storyboard L116 / script L132)各追加五个 optional 字段声明(**直接字面量,planner 裁决 3 五类型全声明**——RESEARCH Open Question 1 建议,verify 断言集相等需要完整对照面):steps: z.number().optional() / cfg: z.number().optional() / quant: z.string().optional() / sageAttention: z.boolean().optional() / lora: z.array(z.object({ name: z.string(), strength: z.number() }).optional()).optional()(元素形状对齐 flowgraph-v3 zod.ts L109)。**根仓 zod v4,禁跨包 import zod 对象**(Pitfall 4,只抄形状字面量)。勿重复声明冲突键:audio/video 分支已有 engine 必填位——配方 round-trip 的 engine 写同键,保持现状勿动;prompt/seed 已声明分支不重复。schema/generated/frontend-zod-extensions.ts 为 AUTO-GENERATED,本 phase 不改(A4 裁定)。
  </action>
  <verify>
    <automated>npx tsc --noEmit && cd packages/infinite-canvas && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
  - EventParamsPopover.tsx 不含本地九键字面量(grep -c "new Set(\['prompt'" = 0),import RECIPE_KNOWN_KEYS from '@kais/flowgraph-v3' 在位;L93/L116-146 区域除 KNOWN_KEYS 来源外零 diff
  - canvasAssetSchema.ts 五分支各含五键:grep -c "sageAttention: z.boolean().optional()" = 5;grep -c "steps: z.number().optional()" = 5
  - canvasAssetSchema.ts 无 @kais/flowgraph-v3 import(根仓禁跨包,源断言)
  - root npx tsc --noEmit clean + infinite-canvas tsc -b clean
  - root 仓既有路由测试/vitest(若有)不受影响;save-v2 对未声明类型仍 strip 不拒(zod 默认语义,行为断言由 Plan 03 e2e 覆盖)
  </acceptance_criteria>
  <done>popover 单点消费共享常量;五类型 schema 声明齐全;tsc 双根 clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 面板输入 → persistEventParams → params | 用户输入首次成为 steps/cfg/lora 等字段真值的入口 |
| save-v2 请求体 → canvasAssetSchema 校验 | 新配方键跨入服务端结构化校验面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering | save-v2 data 袋注入畸形配方形状(如 lora: [{name: 1}]) | mitigate | canvasAssetSchema 五分支 optional 声明在场形状强制:lora 必须 {name: string, strength: number}[]、steps/cfg z.number()、sageAttention z.boolean()——畸形形状 400(V5) |
| T-58-02 | Tampering | 面板 number input 注入 NaN/Infinity → params 污染 | mitigate | input[type=number] + min/max/step(UI-SPEC §2 边界);updateEventParams 值透传,zod z.number() 在 v3 直通模式拒绝非有限数 |
| T-58-03 | Tampering | catchall 字段被做成可编辑(误伤管线私有字段) | mitigate | UI-SPEC/CONTEXT 锁定 catchall 只读 JSON.stringify 展示;RECIPE_EDITABLE_FIELDS 白名单驱动控件生成 |
| T-58-SC | Tampering | npm/pip installs | accept | 本 plan 零包安装 |
</threat_model>

<verification>
- npx tsc --noEmit(root)+ npx tsc -b(infinite-canvas)clean
- infinite-canvas 全量 vitest 406 基线不降(52-03 PromptSection / 52-08 既有面板用例零回归)
- UI-SPEC §7 testid 契约逐项 grep 在位(acceptance_criteria 列表)
- 锁死文案逐字未变(phase52 e2e 将在 Plan 03 全量回归中守护)
</verification>

<success_criteria>
- 用户可在面板高级参数折叠区编辑五高级字段,保存走共享 persistEventParams,清空/空 lora 归一化语义正确
- 落选/无事件整块只读;regen dirty 禁用;保存回读后 dirty 不残留
- popover 与面板同源消费 RECIPE_KNOWN_KEYS/RECIPE_EDITABLE_FIELDS(一处定义两处消费,CONTEXT 锁定)
- canvasAssetSchema 五类型配方声明就位——RECIPE-04 三方集合门的 schema 侧落点
</success_criteria>

<output>
Create `.planning/phases/58-full-recipe-persistence/58-02-SUMMARY.md` when done
</output>
