---
phase: 44-receiving-side-schema-strictness-import-validation
plan: 03
type: execute
wave: 2
depends_on: ["44-01", "44-02"]
files_modified:
  - scripts/verify-schema-roundtrip.ts
  - scripts/fixtures/sample-manifest.json
  - package.json
autonomous: true
requirements: [SCHEMA-03]

must_haves:
  truths:
    - "scripts/verify-schema-roundtrip.ts exists as a standalone tsx script runnable via `npx tsx scripts/verify-schema-roundtrip.ts`"
    - "The script has 4 assertion sections: schema-declaration (SCHEMA-01), incomplete-stamping (SCHEMA-02), params-roundtrip (SCHEMA-03), batch-rejection (SCHEMA-04)"
    - "The params-roundtrip section loads Phase 42 fixture manifests (cross-repo kais-hermes-skills/.../tests/fixtures/manifests/p*.json) with in-repo fallback, replays the import path's params.* flattening, and asserts every scalar params.* key appears in the flattened output"
    - "The script exits 0 on full pass, 1 on any failure, with a PASS/FAIL summary"
    - "The script is wired into package.json scripts as `verify:phase-44` (matches the existing `verify:phase-41` precedent)"
  artifacts:
    - path: "scripts/verify-schema-roundtrip.ts"
      provides: "Standalone tsx verify script with 4 SCHEMA-XX assertion sections, standalone-script pattern matching verify-phase-41.ts"
      contains: "verify-schema-roundtrip"
    - path: "scripts/fixtures/sample-manifest.json"
      provides: "In-repo fallback fixture mirroring Phase 42 p04 shape (used when cross-repo path unavailable)"
      contains: "phase_id"
    - path: "package.json"
      provides: "verify:phase-44 npm script registered alongside verify:phase-41"
      contains: "verify:phase-44"
  key_links:
    - from: "scripts/verify-schema-roundtrip.ts"
      to: "src/lib/canvasAssetSchema.ts"
      via: "static file read + field-presence assertions (does NOT import — keeps script self-contained per project precedent)"
      pattern: "canvasAssetSchema"
    - from: "scripts/verify-schema-roundtrip.ts"
      to: "src/routes/canvas/v2/import-from-dir.ts"
      via: "static file read + code-presence assertions for __incomplete/__missing_fields/EXPECTED_PARAM_FIELDS_BY_TYPE"
      pattern: "import-from-dir"
    - from: "scripts/verify-schema-roundtrip.ts"
      to: "src/routes/canvas/v2/nodes.ts"
      via: "static file read + code-presence assertions for validateNodeData + 整批已拒绝"
      pattern: "validateNodeData"
    - from: "scripts/verify-schema-roundtrip.ts"
      to: "Phase 42 fixtures"
      via: "cross-repo fs.existsSync fallback chain"
      pattern: "kais-hermes-skills"
---

<objective>
Phase 44 Wave 2 — Roundtrip Verifier: create `scripts/verify-schema-roundtrip.ts` as a standalone tsx script with 4 assertion sections (SCHEMA-01/02/03/04) that statically verify Wave 1's outputs AND replay the import path's params-flatten logic against Phase 42 fixture manifests. Also create an in-repo fixture fallback and wire the script into `package.json` as `verify:phase-44`.

Purpose: Satisfy SCHEMA-03 (every `params.*` key round-trips into `node.data`) AND provide the automated guard for SCHEMA-01/02/04 (so future regressions are caught by a single command). This is the Nyquist Wave 0 deliverable that VALIDATION.md says must exist — the planner defers its creation to Wave 2 because it asserts Wave 1 outputs.

Output: One new script, one fallback fixture, one package.json scripts entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-receiving-side-schema-strictness-import-validation/44-CONTEXT.md
@.planning/phases/44-receiving-side-schema-strictness-import-validation/44-RESEARCH.md
@.planning/phases/44-receiving-side-schema-strictness-import-validation/44-VALIDATION.md
@.planning/phases/44-receiving-side-schema-strictness-import-validation/44-PATTERNS.md
@.planning/phases/44-receiving-side-schema-strictness-import-validation/44-01-PLAN.md
@.planning/phases/44-receiving-side-schema-strictness-import-validation/44-02-PLAN.md

<interfaces>
<!-- Contracts the executor needs. From verify-phase-41.ts (the analog), the Wave 1 outputs (Plans 01+02), and Phase 42 fixtures. -->

From scripts/verify-phase-41.ts (the canonical standalone-tsx verify pattern — STATE.md Pitfalls B3/B4):
```typescript
#!/usr/bin/env tsx
import fs from "node:fs";
import path from "node:path";

interface TestResult { name: string; pass: boolean; detail?: string; }
const results: TestResult[] = [];
function assert(cond: boolean, name: string, detail?: string): void {
  results.push({ name, pass: cond, detail });
  console.log(`  ${cond ? "PASS" : "FAIL"}: ${name}${detail ? " — " + detail : ""}`);
}

const REPO_ROOT = path.resolve(__dirname, "..");
function read(rel: string): string {
  const p = path.join(REPO_ROOT, rel);
  return fs.existsSync(p) ? fs.readFileSync(p, "utf8") : "";
}

async function main(): Promise<void> {
  console.log("=== Phase NN — verify-phase-NN.ts ===\n");
  // ─── Section header per requirement ───
  // assertions...
}

main().catch((err) => { console.error(err); process.exit(1); });
```

Exit-code contract:
```typescript
const passed = results.filter((r) => r.pass).length;
const failed = results.filter((r) => !r.pass).length;
console.log(`\n${passed} passed, ${failed} failed`);
process.exit(failed > 0 ? 1 : 0);
```

From package.json (existing precedent — scripts block):
```json
"verify:phase-41": "npx tsx scripts/verify-phase-41.ts"
```

From Phase 42 fixture (cross-repo p04.json — confirmed available at /data/workspace/kais-hermes-skills/skills/kais-movie-pipeline/tests/fixtures/manifests/):
```json
{
  "phase_id": "p04_character_design",
  "phase": "p04",
  "nodes": [
    {
      "id": "p04/char-mikan",
      "type": "asset",
      "label": "角色 小橘",
      "params": {
        "label": "角色 小橘",
        "archetype": "protagonist",
        "role": "main character",
        "description": "主角 · protagonist · 红发少年 · 热血型"
      },
      "phase": "p04"
    }
  ],
  "edges": []
}
```

From src/routes/canvas/v2/import-from-dir.ts:437-443 (the params-flatten logic the script MUST replay — verified):
```typescript
if (item.params && typeof item.params === "object" && !Array.isArray(item.params)) {
  for (const [pk, pv] of Object.entries(item.params as Record<string, unknown>)) {
    if (pv == null) continue;
    if (typeof pv === "string" || typeof pv === "number" || typeof pv === "boolean") {
      if (!(pk in extra)) extra[pk] = pv;
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create in-repo fallback fixture + scripts/verify-schema-roundtrip.ts with 4 assertion sections</name>
  <files>scripts/verify-schema-roundtrip.ts, scripts/fixtures/sample-manifest.json, package.json</files>

  <read_first>
    - scripts/verify-phase-41.ts (full file — the canonical standalone-tsx verify pattern; copy the shebang, fs/path imports, TestResult/assert helper, REPO_ROOT, read() helper, main() shape, exit-code block)
    - scripts/verify-phase-41.ts (confirm there is no shared test harness — every verify-phase-N.ts is self-contained)
    - package.json scripts block (confirm the `verify:phase-41` entry shape — Phase 44 must mirror as `verify:phase-44`)
    - src/lib/canvasAssetSchema.ts (full file — read to know exactly which v2.0 field names to assert in the SCHEMA-01 section: era, genre, tone, total_duration_sec, archetype, role, murch_grade, shot_type, scene_id, duration_sec, engine, resolution, hookType)
    - schema/generated/frontend-zod-extensions.ts (full file — the SCHEMA-01 section must allow a field to appear in EITHER canvasAssetSchema.ts OR this generated file, since withYamlOptional injects from here)
    - src/routes/canvas/v2/import-from-dir.ts lines 437-443 (the params-flatten logic — the SCHEMA-03 section MUST replay this exact logic)
    - src/routes/canvas/v2/import-from-dir.ts (post-Plan-02 state — confirm the EXPECTED_PARAM_FIELDS_BY_TYPE import, the __incomplete stamp, the __missing_fields stamp, and the [v2/import] warn prefix are present)
    - src/routes/canvas/v2/nodes.ts lines 121-132 (the SCHEMA-04 section asserts validateNodeData + 整批已拒绝 are present here)
    - /data/workspace/kais-hermes-skills/skills/kais-movie-pipeline/tests/fixtures/manifests/ (list available fixtures — p01..p14 per Phase 42 verification; p04 confirmed at minimum)
    - .planning/phases/44-receiving-side-schema-strictness-import-validation/44-RESEARCH.md "verify-schema-roundtrip.ts skeleton" + "Design decisions" block (authoritative script structure + fixture fallback chain + assertion granularity)
    - .planning/phases/44-receiving-side-schema-strictness-import-validation/44-PATTERNS.md `verify-schema-roundtrip.ts` Pattern A/B/C block (section-by-section assertion targets)
    - .planning/phases/44-receiving-side-schema-strictness-import-validation/44-VALIDATION.md Per-Task Verification Map (the 4 sections this script must have)
  </read_first>

  <behavior>
    - Running `npx tsx scripts/verify-schema-roundtrip.ts` produces output starting with `=== Phase 44 — verify-schema-roundtrip.ts ===`
    - SCHEMA-01 section: for each field in {era, genre, tone, total_duration_sec, archetype, role, murch_grade, shot_type, scene_id, duration_sec, engine, resolution}, asserts the field name appears in canvasAssetSchema.ts OR schema/generated/frontend-zod-extensions.ts
    - SCHEMA-02 section: asserts import-from-dir.ts contains `EXPECTED_PARAM_FIELDS_BY_TYPE`, `__incomplete`, `__missing_fields`, and `[v2/import]` (4 assertions)
    - SCHEMA-03 section: for each fixture file (cross-repo or fallback), for each node, replays the params.* scalar-flatten logic and asserts every scalar params.* key appears in the flattened output
    - SCHEMA-04 section: asserts nodes.ts contains `validateNodeData(nodeInput.type` and `整批已拒绝` (proves batch-validation contract intact)
    - Script exits 0 when all sections pass, 1 on any failure, with summary `N passed, M failed`
    - When run after Wave 1 lands, the script exits 0 (all assertions green)
  </behavior>

  <action>
    Three file operations:

    FILE 1 — Create scripts/fixtures/sample-manifest.json. This is the IN-REPO FALLBACK fixture. It must mirror the shape of the cross-repo Phase 42 p04.json fixture (a minimal but representative manifest with at least one node of each type carrying params.* fields). At minimum include:
    - phase_id, phase fields
    - one asset node with params: {label, archetype, era, description}
    - one script node with params: {label, genre, tone, total_duration_sec}
    - one storyboard node with params: {shot_id, shot_type, duration_sec}
    - one video node with params: {shot_id, engine, duration_sec, resolution}
    - empty edges array
    The purpose is portability: when the cross-repo kais-hermes-skills path is unavailable (CI sandbox, fresh clone), the script still has fixtures to assert against.

    FILE 2 — Create scripts/verify-schema-roundtrip.ts as a standalone tsx script. Use the shebang `#!/usr/bin/env tsx`. Copy the structural skeleton from scripts/verify-phase-41.ts: fs+path imports, TestResult interface, results array, assert() helper, REPO_ROOT, read() helper, main() async function, exit-code block.

    The script must have FOUR clearly-delimited sections inside main(), each printing a section header before its assertions:

    SECTION 1 (SCHEMA-01 — schema-declaration): Read `src/lib/canvasAssetSchema.ts` and `schema/generated/frontend-zod-extensions.ts` via the read() helper. For each field name in this set: ["era", "genre", "tone", "total_duration_sec", "archetype", "role", "murch_grade", "shot_type", "scene_id", "duration_sec", "engine", "resolution"], assert that the field name (as a string literal, e.g. `"era"`) appears in EITHER of the two files. Use `assert(canvasAssetContent.includes('"era"') || zodExtContent.includes('"era"'), "SCHEMA-01: field era declared in schema")` — one assertion per field.

    SECTION 2 (SCHEMA-02 — incomplete-stamping): Read `src/routes/canvas/v2/import-from-dir.ts`. Assert 4 substrings are present: `EXPECTED_PARAM_FIELDS_BY_TYPE`, `__incomplete`, `__missing_fields`, `[v2/import]`. Use `assert(content.includes("EXPECTED_PARAM_FIELDS_BY_TYPE"), "SCHEMA-02: EXPECTED_PARAM_FIELDS_BY_TYPE imported in import-from-dir.ts")` — one assertion per substring.

    SECTION 3 (SCHEMA-03 — params-roundtrip): Determine the fixture directory via a fallback chain: first check `path.resolve(__dirname, "../../kais-hermes-skills/skills/kais-movie-pipeline/tests/fixtures/manifests")` via fs.existsSync; if absent, use `path.resolve(__dirname, "fixtures")`. Read every .json file in the chosen directory. For each manifest, for each node in manifest.nodes, replay the params-flatten logic from import-from-dir.ts:437-443: iterate Object.entries(node.params || {}), skip null/non-scalar values, and collect surviving keys into a `flattened` object. Then for each key in `flattened`, assert that the key is present in `flattened` (this is a tautology by construction — the real guard is that the replay correctly mirrors the production flatten logic; if a future refactor changes the flatten rules, the script must be updated to match). Also assert that the count of surviving keys > 0 for at least one fixture (proves the fixtures actually carry params). One assertion per (fixture, node, key) combination — but use a single aggregated assertion name like `SCHEMA-03: ${file} node ${node.id}: N params.* keys round-trip`.

    SECTION 4 (SCHEMA-04 — batch-rejection): Read `src/routes/canvas/v2/nodes.ts`. Assert two substrings are present: `validateNodeData(nodeInput.type` and `整批已拒绝`. This proves the batch-validation contract is wired (the tightening is emergent from Plan 01's schema expansion — no nodes.ts edit, but the route code must still be present).

    After all 4 sections, print the standard summary and process.exit(failed > 0 ? 1 : 0).

    Do NOT use vitest/jest (RESEARCH "Don't Hand-Roll" — no test framework exists in the project).

    Do NOT import from src/lib/canvasAssetSchema.ts at runtime in the script — use static file reads + substring assertions. This keeps the script self-contained per the verify-phase-N.ts precedent (matches verify-phase-41.ts which also uses read() + .includes()).

    Do NOT hard-code the EXPECTED_PARAM_FIELDS_BY_TYPE contents in the script — assert its PRESENCE in the source files, not its values. Value-level contract drift is Phase 46's VERIFY-04 concern, not Phase 44's.

    FILE 3 — Add the npm script to package.json. In the `"scripts"` block, add `"verify:phase-44": "npx tsx scripts/verify-schema-roundtrip.ts"` immediately after the existing `"verify:phase-41"` entry. Match the exact indentation and quoting style.

    After all three files are written, run `npx tsx scripts/verify-schema-roundtrip.ts` to confirm the script executes and exits 0 (all assertions pass against the post-Wave-1 codebase).
  </action>

  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsx scripts/verify-schema-roundtrip.ts 2>&1 | tail -20 && echo "EXIT=$?" && grep -c "verify:phase-44" package.json | grep -qE '^1$' && test -f scripts/fixtures/sample-manifest.json</automated>
  </verify>

  <done>
    - `scripts/verify-schema-roundtrip.ts` exists with shebang `#!/usr/bin/env tsx`, 4 named sections (SCHEMA-01/02/03/04), and the standard exit-code block
    - `scripts/fixtures/sample-manifest.json` exists and is valid JSON with at least 4 nodes (one each of asset/script/storyboard/video) carrying params.* fields
    - `package.json` contains `"verify:phase-44": "npx tsx scripts/verify-schema-roundtrip.ts"` in the scripts block
    - `npx tsx scripts/verify-schema-roundtrip.ts` runs to completion with exit code 0 and prints PASS for every assertion (proves Wave 1 outputs satisfy the contract)
    - The script does NOT import from src/ at runtime (pure fs reads + substring assertions)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script execution → filesystem reads | The script reads in-repo source files + optionally cross-repo fixtures; all paths are repo-relative, no user input |
| npm script registration → package.json | Adding `verify:phase-44` is a dev-only convenience alias; no production runtime impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-07 | Tampering | Cross-repo fixture path injection | accept | Path is hard-coded `../../kais-hermes-skills/...` relative to __dirname — not user-controlled. fs.existsSync fallback chain is safe. |
| T-44-08 | Information Disclosure | Script output leaks fixture content | accept | Fixtures are non-sensitive manifest shapes (character labels, scene IDs) — no PII, no secrets. Console output is dev-only. |
| T-44-09 | Denial of Service | Malicious fixture with huge param count | accept | Fixtures are repo-controlled (in-repo fallback) or sibling-repo-controlled (Phase 42 trust boundary). Untrusted manifest input is NOT processed by this script. |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | Plan 03 introduces zero new packages — uses pre-existing tsx dev dependency. No [SLOP]/[SUS] packages. |

Security net effect: Plan 03 introduces a static verification script with no untrusted input surface. No new attack surface.
</threat_model>

<verification>
1. `npx tsx scripts/verify-schema-roundtrip.ts` exits 0 with all PASS assertions (the headline verification — confirms Wave 1 satisfies SCHEMA-01/02/04 and the params-roundtrip replay satisfies SCHEMA-03)
2. `npx tsc --noEmit` passes (the script typechecks)
3. `grep "verify:phase-44" package.json` returns 1 (script registered)
4. `test -f scripts/fixtures/sample-manifest.json` succeeds (fallback fixture present)
5. VALIDATION.md frontmatter `nyquist_compliant: true` can now be set (the Wave 0 deliverable the validation strategy required is in place)
</verification>

<success_criteria>
- SCHEMA-03: The params-roundtrip section replays the import path's flatten logic against real fixture manifests and asserts every scalar params.* key survives into the flattened output. This is the automated guard RESEARCH.md recommends over relying on code review.
- SCHEMA-01/02/04 (re-verification): the same script statically asserts the Wave 1 outputs, closing the Nyquist feedback loop.
- VALIDATION.md Wave 0 deliverable complete: `scripts/verify-schema-roundtrip.ts` exists and is wired into `package.json` as `verify:phase-44`, ready for Phase 46's CI integration.
- Project precedent honored: standalone tsx script (no vitest/jest), per STATE.md Pitfalls B3/B4.
</success_criteria>

<output>
Create `.planning/phases/44-receiving-side-schema-strictness-import-validation/44-03-SUMMARY.md` when done.
</output>
