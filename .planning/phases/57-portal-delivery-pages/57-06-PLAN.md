---
phase: 57-portal-delivery-pages
plan: 06
type: execute
wave: 3
depends_on: ["57-05"]
files_modified:
  - packages/portal/src/pages/DeliveryPage.tsx
  - packages/portal/src/components/ReasonDialog.tsx
  - packages/portal/src/lib/gateOpsFlow.ts
  - packages/portal/src/__tests__/gateOpsFlow.test.ts
autonomous: true
requirements: [PORTAL-03]
must_haves:
  truths:
    - "D-10: G8 终审操作复用 54 GATE-03 通道——POST /api/canvas/v2/gate-ops（reviewId 取自 gate-state p13-gate 条目；action approve|reject；reject reason 必填 1-500）；零新端点、零直连 review-platform"
    - "54 语义全套保真：409 already-resolved = 幂等成功（重拉状态 + toast，不当错误弹）、乐观翻转 + 失败回滚 + toast、fail-closed 降级（gate-state 不可达时动作条禁用 + 降级横幅，绝不误判可放行）"
    - "词汇 = 54 锁定词表：放行/驳回/等你决策（需求 prose「通过/打回」映射，UI 不出现第三套词）；驳回 = 理由必填 + 二次确认组件内对话框（禁原生 confirm，54 C-4）"
    - "display 非 pending（已放行/驳回/豁免/legacy 无 reviewId）时动作条不渲染——只留状态行（54 语义）"
  artifacts:
    - path: "packages/portal/src/components/ReasonDialog.tsx"
      provides: "54 C-4 复刻：驳回理由输入 + 二次确认（textarea 必填 1-500 + [取消] ghost/[确认驳回] 玫 + Esc）"
    - path: "packages/portal/src/lib/gateOpsFlow.ts"
      provides: "终审操作流纯函数/状态机：planOp(gate, action, reason) → 乐观翻态/回滚/幂等三分支"
      exports: ["runTerminalOp", "terminalStates"]
  key_links:
    - from: "packages/portal/src/lib/gateOpsFlow.ts"
      to: "packages/infinite-canvas/src/services/canvasApi.ts"
      via: "import gateOps（canvasApi.ts:554-575 既有封装——URL/payload/错误形态复用）"
      pattern: "gateOps"
    - from: "packages/portal/src/components/ReasonDialog.tsx"
      to: "packages/infinite-canvas/src/components/gate/GateCenterBlock.tsx"
      via: "54 C-4 confirming-state 模式照抄（文案逐字 54-UI-SPEC Copywriting Contract）"
      pattern: "confirming"
---

<objective>
交付页 G8 终审操作面（PORTAL-03 成功标准 3 后半）：在 57-05 的终审卡上接通 gate-ops 通道——放行/驳回回写 kmc gate 状态（经 review-platform 桥，54 已建），全程无 telegram/CLI。

Purpose: 一套通道三处消费（53 选定/54 gate/57 G8）的第三处；22-phase 终点的「放行」动作在收片现场完成。
Output: gateOpsFlow 状态机 + ReasonDialog + 终审卡动作条接线。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/57-portal-delivery-pages/57-CONTEXT.md
@.planning/phases/57-portal-delivery-pages/57-UI-SPEC.md
@.planning/phases/57-portal-delivery-pages/57-RESEARCH.md
@.planning/phases/57-portal-delivery-pages/57-PATTERNS.md

<interfaces>
<!-- 通道（零改动复用） -->
POST /api/canvas/v2/gate-ops（src/routes/canvas/v2/gate-ops.ts）:
  schema :24-38 {projectId:number, episodesId:number, reviewId:number, action:'approve'|'reject'|'waive', reason?(reject/waive 必填 1-500), selected?}
  fail-closed scope 校验 :52-60（reviewId ∉ 当前集候选集 → 422）
  平台调用 :64-76；409 → 幂等成功 {applied:false, cause:'already-resolved'} :78-83；非 ok → 502 :84-88；成功 {applied:true, gateId} + fire-and-forget re-poll :90-92
GET /api/canvas/v2/gate-state（gate-state.ts:18-59；payload gates[] 每项 {gateId, display, reviewId?, note?, updatedAt?}——p13-gate 条目即操作对象）
<!-- 54 前端先例（照抄对象） -->
packages/infinite-canvas/src/components/gate/GateCenterBlock.tsx:79-97（runOp：gateOps → 乐观翻转 → already-resolved 重拉+toast → 失败回滚+toast）
packages/infinite-canvas/src/components/gate/GateCenterPanel.tsx（动作条 [放行][驳回][豁免] 高 32 按钮规范——57 终审只有 放行/驳回 两键，waive 不上交付页[收片人不做豁免——豁免是操作员语义，记录为 U-05 延伸裁决]）
54-UI-SPEC（.planning/phases/54-gate-gate-center-blocking-state-ux/54-UI-SPEC.md）§Copywriting Contract（驳回确认/placeholder/409 toast/处理中/降级——文案逐字源）+ §Color 按钮规范 + C-4 对话框
packages/infinite-canvas/src/services/canvasApi.ts:554-575（gateOps 封装——直接 import）
<!-- 设计契约 -->
.planning/phases/57-portal-delivery-pages/57-UI-SPEC.md §Component Inventory P-3 终审卡 + §Copywriting Contract（终审段）+ §State Matrix 终审卡行 + §Motion（pending 点 54 同拍 2.4s）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gateOpsFlow 状态机（纯函数）+ ReasonDialog + 终审卡接线</name>
  <files>packages/portal/src/lib/gateOpsFlow.ts, packages/portal/src/__tests__/gateOpsFlow.test.ts, packages/portal/src/components/ReasonDialog.tsx, packages/portal/src/pages/DeliveryPage.tsx</files>
  <read_first>
    - packages/infinite-canvas/src/components/gate/GateCenterBlock.tsx:60-100（runOp 全貌——乐观翻转/幂等/回滚三分支时序）
    - src/routes/canvas/v2/gate-ops.ts:24-92（payload/错误形态——状态机分支的输入契约）
    - packages/infinite-canvas/src/services/canvasApi.ts:554-575（gateOps 签名与返回）
    - 54-UI-SPEC §Copywriting Contract + C-4（文案与对话框结构逐字源）
    - 57-UI-SPEC §Component Inventory P-3 终审卡 + §Do-Not-Regress 3
  </read_first>
  <action>
1. 先写 `__tests__/gateOpsFlow.test.ts`（状态机纯函数，mock gateOps）：
   - 放行路径：pending → 调 approve（无 reason）→ 乐观翻 approve → resolved {applied:true} → 终态 approve。
   - 驳回路径：reason 空 → 不发请求（前置校验）；reason 合规 → reject → 乐观翻 reject。
   - 409 幂等：{applied:false, cause:'already-resolved'} → 不回滚、标记 needsRefetch（上层重拉 + 幂等 toast）。
   - 失败回滚：非 ok → 状态回 pending + error 标记（上层 toast）。
   - 422/502 形态归失败分支（原因透传）。
   - display 非 pending 或 reviewId 缺 → planOp 返回 {kind:'no-op'}（动作条不渲染的判定输入）。
2. `src/lib/gateOpsFlow.ts` 实现（类型驱动：输入 {gate, projectId, episodesId, api:{gateOps}}，输出状态转移 + 副作用指令——UI 层执行 toast/refetch）。
3. `src/components/ReasonDialog.tsx`（54 C-4 逐字复刻）：标题「驳回后管线将回滚到 {交付} 重跑（重试预算 3 次）。确认驳回？」（阶段名取 p13 注册表 name「交付」）；textarea placeholder「驳回理由（必填）：告诉管线哪里不过——将随决策存档。」min-height 80、必填 1-500；[取消] ghost / [确认驳回] 玫填充；Esc 关闭；焦点圈走既有 :focus-visible。
4. DeliveryPage 终审卡接线（57-05 留位处）：
   - display==='pending' 且 reviewId 存在 → 动作条 [放行]（冷白 primary，单点击即执行无二次确认——54 U-05）+ [驳回]（玫，开 ReasonDialog）。
   - 操作中行级「处理中…」（禁两键，禁全屏 loading）。
   - 成功/幂等/失败 toast 文案逐字 54（成功即状态行翻终态 + 动作条退场 `--cv-d-unhighlight` 220ms）；幂等后重拉 gate-state。
   - gateState null（降级）→ 动作条禁用 + 降级横幅（54 原文案 + [重试] 重拉）。
   - waive 不上交付页（收片人无豁免语义——U-05 延伸裁决，SUMMARY 记录）。
5. **设计检查步**：对照 57-UI-SPEC §Color（玫只出现在驳回按钮与驳回态点/标签；冷白 primary 只放行+播放键+焦点）+ §Motion（pending 点 2.4s 呼吸 54 同拍；退场 220ms）+ Do-Not-Regress 3 逐条核对。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/portal && npx vitest run src/__tests__/gateOpsFlow.test.ts && npx vitest run && npx tsc -p tsconfig.json --noEmit && cd ../.. && bash scripts/deploy-portal.sh >/dev/null 2>&1 && echo G8-OK</automated>
  </verify>
  <acceptance_criteria>
    - vitest 全绿（≥6 case：放行/驳回前置校验/409 幂等/回滚/no-op/降级输入）。
    - `grep -c "通过\|打回" packages/portal/src/pages/DeliveryPage.tsx packages/portal/src/components/ReasonDialog.tsx | awk -F: '{s+=$2} END{exit !(s==0)}'`——UI 零「通过/打回」字样（54 词表断言）。
    - `grep -c "confirm(\|alert(" packages/portal/src/components/ReasonDialog.tsx` === 0（禁原生）。
    - 直连 review-platform 零出现（`grep -rn "8090\|review-platform\|/api/v1/reviews" packages/portal/src` === 0——只经 gate-ops）。
    - 理由长度契约 1-500 在 ReasonDialog 与状态机双侧存在（grep 两处）。
  </acceptance_criteria>
  <done>交付页可完成 放行/驳回 → kmc gate 状态回写（经 54 通道）——成功标准 3 全句达成（无 telegram/CLI）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 交付页 → gate-ops 端点（既有 fail-closed 校验） | 写路径复用，无新校验面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-06a | Tampering | 越集 reviewId 操作 | mitigate | 服务端既有 fail-closed scope 校验（gate-ops.ts:52-60 → 422）；前端只传当前集 gate-state 的 reviewId |
| T-57-06b | DoS | 重复点击双发 | mitigate | 行级「处理中…」禁键 + 幂等语义兜底（409 = 成功） |
</threat_model>

<verification>
- Task 1 vitest 全绿 + tsc + 部署。
- 活体（若 review-platform 可达且有 pending p13 review）：放行一次 → gate-state 翻 approve；否则以 mock 单测 + 降级路径 UI 验收（54 先例口径）。
</verification>

<success_criteria>
- 通过/打回操作（UI 词：放行/驳回）回写 kmc gate 状态全程在交付页完成（成功标准 3）。
- 54 通道语义零漂移（409/回滚/降级三断言）。
</success_criteria>

<output>
Create `.planning/phases/57-portal-delivery-pages/57-06-SUMMARY.md` when done
</output>
