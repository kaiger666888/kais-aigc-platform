---
phase: 57-portal-delivery-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/portal/package.json
  - packages/portal/vite.config.ts
  - packages/portal/tsconfig.json
  - packages/portal/index.html
  - packages/portal/src/main.tsx
  - packages/portal/src/nav/kap-nav.ts
  - packages/portal/src/nav/kap-nav.css
  - packages/portal/src/pages/PortalHome.tsx
  - packages/portal/src/pages/DeliveryPage.tsx
  - packages/portal/src/pages/ToonflowEmbed.tsx
  - packages/portal/src/components/PipelineRibbon.tsx
  - packages/portal/src/lib/ribbon.ts
  - packages/portal/src/__tests__/ribbon.test.ts
  - packages/portal/src/services/portalApi.ts
  - scripts/build-kap-nav.mjs
  - scripts/deploy-portal.sh
  - src/app.ts
  - src/routes/canvas/projects.ts
autonomous: true
requirements: [PORTAL-01, PORTAL-02]
must_haves:
  truths:
    - "门户壳可运行访问：/portal（项目入口）、/deliver/:ep（交付页壳，数据面在 57-05/06）、/toonflow（iframe 嵌入）三前缀由门户 SPA 服务，注册在全局 SPA fallback（app.ts:238-252）之前——GET /deliver/1 返回门户 index（title 制片门户）而非 Toonflow"
    - "D-05: /canvas 深链重定向落地——GET /canvas?project=X&ep=Y&focus=F&zone=Z → 302 /infinite-canvas/?projectId=X&episodesId=Y&focus=F&zone=Z（白名单键翻译，目标写死站内）"
    - "D-06/D-07: KapNavbar 单源三宿主的第一宿主落地——vanilla custom element，门户页全宽档 40px；构建产物 data/assets/kap-nav.{js,css}（tokens.css 构建期 concat，token 零复制）"
    - "U-08: POST /api/canvas/projects 响应 episodes[] 增量加 phases 直方图（canvas_nodes GROUP BY phase_index；additive，ProjectSelector 既有消费不破）"
    - "共存零破坏：`/`、全局 fallback、/api/project/* 行为零改动（Do-Not-Regress 6）"
  artifacts:
    - path: "packages/portal/"
      provides: "门户壳新前端包（vite base '/portal/'，pathname switch 三路由）"
      exports: ["PortalHome", "DeliveryPage", "ToonflowEmbed", "PipelineRibbon", "KapNavbar(custom element)"]
    - path: "scripts/build-kap-nav.mjs"
      provides: "kap-nav esbuild 产物构建（IIFE js + tokens concat css → data/assets/）"
    - path: "scripts/deploy-portal.sh"
      provides: "portal 构建部署链（build → data/web/portal，deploy-canvas.sh 模式，含备份）"
    - path: "src/app.ts（门户段）"
      provides: "/portal /deliver /toonflow 挂载 + /canvas 302（全部在全局 fallback 之前）"
    - path: "src/routes/canvas/projects.ts"
      provides: "episodes[].phases 直方图（管线带 micro 数据源）"
  key_links:
    - from: "packages/portal/src/lib/ribbon.ts"
      to: "packages/infinite-canvas/src/constants/phaseRegistry.ts"
      via: "import PHASE_REGISTRY（@ic alias；55-D04 单源消费，禁止内联表）"
      pattern: "PHASE_REGISTRY"
    - from: "packages/portal/src/services/portalApi.ts"
      to: "packages/infinite-canvas/src/services/canvasApi.ts"
      via: "import fetchProjects/loadCanvasGraph/fetchGateState/gateOps（复用不重写）"
      pattern: "canvasApi"
    - from: "packages/portal/src/main.tsx"
      to: "packages/portal/src/nav/kap-nav.ts"
      via: "import 副作用注册 custom element"
      pattern: "customElements.define"
---

<objective>
建立门户壳骨架（PORTAL-01 成功标准 1 后半 + 成功标准 2 前半）：新前端包 packages/portal 三路由可运行、共享 navbar 第一宿主、门户首页（项目入口 + 集行 + 管线带 micro + 深链发码）、/canvas 302。交付页/嵌入页本 plan 出壳与空态，数据面与终审在 57-05/06。

Purpose: D-02/D-04 三件套（路由/导航/项目入口）可运行；D-05 深链 URL 契约对外稳定；门户成为四岛互链的锚点。
Output: packages/portal 新包 + kap-nav 产物链 + app.ts 门户段 + projects.ts 直方图 + deploy-portal.sh。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/57-portal-delivery-pages/57-CONTEXT.md
@.planning/phases/57-portal-delivery-pages/57-UI-SPEC.md
@.planning/phases/57-portal-delivery-pages/57-RESEARCH.md
@.planning/phases/57-portal-delivery-pages/57-PATTERNS.md

<interfaces>
<!-- 挂载模式与顺序（照抄对象） -->
src/app.ts:187-204 岛挂载模式（static + `app.get("/<name>/{*path}")` SPA fallback；注册于 JWT :254 之前）；:236-252 全局 SPA fallback（排除表 :240）——门户三前缀 + /canvas 必须物理写在 :238 之前
src/app.ts:102-108 /assets 挂载（u.getPath("assets")，maxAge 1d——kap-nav 产物文件名须带 hash/时间戳防陈旧缓存）
<!-- 画布既有参数事实 -->
packages/infinite-canvas/src/components/FlowCanvas.tsx:105-114 getInitialParams（已读 projectId/episodesId；57-03 才消费 focus/zone——本 plan 重定向透传即可）
packages/infinite-canvas/vite.config.ts:7-14（base './'、alias 先例；portal 对照差异见 UI-SPEC U-07/Pitfall 2）
<!-- 数据端点 -->
src/routes/canvas/projects.ts:23-89（o_project + canvas_nodes GROUP BY (project_id, episodes_id) :37-49；episodes[{id,nodeCount}] 升序 :62-81；消费者 ProjectSelector.tsx:20-36 只读 id/nodeCount）
src/types/database.d.ts:39-64 canvas_nodes（phase_index/phase_name 列已存在）
packages/infinite-canvas/src/services/canvasApi.ts:53-120 apiCall、:185-190 fetchProjects、:510-539 fetchGateState、:554-575 gateOps、:349 loadCanvasGraph
packages/infinite-canvas/src/constants/phaseRegistry.ts:19-91（PHASE_REGISTRY 22 条 + PipelinePhaseDef + DEREGISTERED_PHASE_PREFIXES）
packages/infinite-canvas/src/theme/tokens.css（--cv-* 全量；kap-nav.css 构建期 concat 源）
<!-- 设计契约 -->
.planning/phases/57-portal-delivery-pages/57-UI-SPEC.md §Color（accent reserved 5 处）、§Signature Element（管线带规格）、§Component Inventory P-1/P-2/P-4/P-5、§Copywriting Contract（全文案）、§Do-Not-Regress（红线 6/8/9）
scripts/deploy-canvas.sh:24-35（部署链模式：build → 备份 → cp dist）；scripts/build.js:39-55（esbuild 先例）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: packages/portal 骨架 + app.ts 门户段（三前缀挂载 + /canvas 302）+ 部署链</name>
  <files>packages/portal/package.json, packages/portal/vite.config.ts, packages/portal/tsconfig.json, packages/portal/index.html, packages/portal/src/main.tsx, packages/portal/src/pages/PortalHome.tsx, packages/portal/src/pages/DeliveryPage.tsx, packages/portal/src/pages/ToonflowEmbed.tsx, scripts/deploy-portal.sh, src/app.ts</files>
  <read_first>
    - packages/infinite-canvas/vite.config.ts（全文——alias/base/proxy/build 配置照抄式样）
    - packages/infinite-canvas/package.json、src/main.tsx:29-77（React 19 挂载式样）
    - src/app.ts:169-252（静态段全貌：岛挂载 → 全局 fallback；顺序敏感）
    - scripts/deploy-canvas.sh（部署链模式）
    - .planning/phases/57-portal-delivery-pages/57-UI-SPEC.md §Component Inventory P-2/P-4（页面结构）+ §Copywriting Contract
  </read_first>
  <action>
1. 新建 packages/portal（vite + React 19，devDependencies 仅根仓既有版本，零新增外部包）：
   - `vite.config.ts`：`base: '/portal/'`（Pitfall 2——/deliver 在前缀外必须绝对 base）；alias `'@' → ./src`、`'@ic' → ../infinite-canvas/src`；`define`/env 补 `VITE_OSS_ORIGIN`（canvasApi/mediaUrl 读它）；dev proxy `/api`、`/oss`、`/assets` → `http://localhost:10588`。
   - `index.html`：`<div id="root">` + `<title>制片门户</title>`（探活断言标记）+ `<link href="/assets/kap-nav.css">`（Task 2 产物；构建期不内联，注入档与 React 档共用一份）。
   - `src/main.tsx`：`import './nav/kap-nav'`（Task 2）+ pathname switch：`/portal`→PortalHome、`/deliver/:ep`→DeliveryPage（ep=pathname 数字段）、`/toonflow`→ToonflowEmbed；未匹配 → /portal。**不引 router 库**（UI-SPEC Registry Safety）。
   - 三页面本 task 出壳：PortalHome = 页头「项目」+ 数据区占位（Task 3 填充）；DeliveryPage = navbar + 无成片空态（copy 用 UI-SPEC 表）；ToonflowEmbed = navbar + 注释行「旧版工作台（共存期）——新工作请从门户与画布进入」+ `<iframe src="/" style={{width:'100%',height:'calc(100vh - 40px)',border:0}}>` + onerror fallback [直开旧版]（UI-SPEC P-4）。
2. `src/app.ts` 门户段（物理插在 :204 story-map 段之后、:210 arch 段之前——即全局 fallback :238 之前）：
   - `/portal` 岛挂载（模式照抄 :187-204：`data/web/portal` static + `/portal/{*path}` fallback）。
   - `GET /deliver/{*path}` 与 `GET /toonflow/{*path}` → `res.sendFile(data/web/portal/index.html)`（同 index 复用；无 static 需求——资产在 /portal/ 前缀下）。
   - `GET /canvas` 302：白名单键 project→projectId、ep→episodesId、focus、zone 透传，目标写死 `/infinite-canvas/`（防开放重定向；RESEARCH §Code Examples）。
3. `scripts/deploy-portal.sh`（deploy-canvas.sh 模式）：`cd packages/portal && npm run build` → 备份 `data/web/portal`（mv .bak.$(date +%s)，同 canvas 模式）→ `cp -r dist`。文件头注释目标 URL。
4. **设计检查步（frontend-design 纪律）**：对照 UI-SPEC §Spacing（内容列 1080px/留白档）、§Typography（t1 页头/mono ep 号）、§Color（accent 只出现在 navbar 当前项——本 task 表面无主 CTA）逐条自查后再提交。

**注意**：nodemon 监听 src/** 会自动重启 dev server 加载 app.ts 改动；但 `data/web/portal` 必须先由部署脚本存在（express.static 对不存在目录静默 404）——本 task 验证前先跑一次 deploy-portal.sh。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && bash scripts/deploy-portal.sh && sleep 1 && curl -sf http://localhost:10588/portal/ | grep -q "制片门户" && curl -s http://localhost:10588/deliver/1 | grep -q "制片门户" && curl -s http://localhost:10588/toonflow | grep -q "制片门户" && curl -sI "http://localhost:10588/canvas?project=3&ep=1&focus=n-abc&zone=p11b" | grep -E "^location: /infinite-canvas/\?projectId=3&episodesId=1&focus=n-abc&zone=p11b" && curl -s http://localhost:10588/ | head -c 4096 | grep -qi "toonflow" && echo PORTAL-SHELL-OK</automated>
  </verify>
  <acceptance_criteria>
    - `curl /portal/`、`/deliver/1`、`/toonflow` 均返回门户 index（title 制片门户）——不被全局 fallback 吞（Pitfall 1 反向断言）。
    - `/canvas?project=3&ep=1&focus=n-abc&zone=p11b` → 302 Location 精确等于 `/infinite-canvas/?projectId=3&episodesId=1&focus=n-abc&zone=p11b`。
    - `/` 与 `/api/project/getProject` 行为不变（根页仍 Toonflow；端点仍 200）。
    - `grep -c "react-router\|next\|@tanstack" packages/portal/package.json` === 0（零 router/UI 库）。
    - app.ts 中门户段代码行号 < 全局 fallback（`app.get("/*"` 或等价 catch-all）所在行（顺序断言：`grep -n` 两处取行号比较）。
  </acceptance_criteria>
  <done>三路由 + 302 + 部署链活体可访问；共存零破坏断言过。</done>
</task>

<task type="auto">
  <name>Task 2: KapNavbar custom element（单源）+ build-kap-nav 产物链</name>
  <files>packages/portal/src/nav/kap-nav.ts, packages/portal/src/nav/kap-nav.css, scripts/build-kap-nav.mjs</files>
  <read_first>
    - packages/infinite-canvas/src/components/canvas/icons.tsx（自包含可移植源纪律 + kind 属性驱动思想）
    - packages/infinite-canvas/src/theme/tokens.css（--cv-* 消费面；本元素只用既有 token）
    - .planning/phases/57-portal-delivery-pages/57-UI-SPEC.md §Component Inventory P-1（全宽/compact/注入三档规格）+ §Copywriting Contract（品牌/项词表）
    - scripts/build.js:39-55（esbuild 配置先例）
  </read_first>
  <action>
1. `kap-nav.ts`：vanilla custom element `<kap-navbar>`（light DOM 不用 shadow——token 级联）：
   - `static observedAttributes = ['data-active', 'compact']`；connectedCallback/attributeChangedCallback → render。
   - 项常量单处：`NAV_ITEMS = [{id:'portal',label:'门户',href:'/portal'},{id:'canvas',label:'画布',href:'/canvas'},{id:'story-map',label:'剧核',href:'/story-map/'},{id:'director-desk',label:'3D导演台',href:'/director-desk/'},{id:'toonflow',label:'Toonflow',href:'/toonflow'}]`。
   - active 判定：`data-active` 属性优先；否则按 `location.pathname` 前缀（`/portal|/deliver`→portal、`/infinite-canvas|/canvas`→canvas、`/story-map`→story-map、`/director-desk`→director-desk、`/toonflow`→toonflow）。
   - 全宽档：高 40px、底 `--cv-bg-panel`、下缘 1px `--cv-line-panel`、品牌 KAP（mono 600 冷白）链接 /portal + 5 项（12px，gap 16）；当前项冷白 + 2px 下划线；其余 `--cv-text-secondary`，hover 120ms（`--cv-d-select`）转 primary。compact 档：26px 高、项 11px（画布 topbar 用——本 task 只实现属性，嵌入在 57-03）。
   - 项为 `<a href>`（键盘可达；focus-visible 走页面既有 tokens.css 规则）。
2. `kap-nav.css`：只写结构样式（高/间距/下划线/hover 过渡），颜色全部 `var(--cv-*)`，**零 hex**。
3. `scripts/build-kap-nav.mjs`（esbuild，IIFE）→ `data/assets/kap-nav.<hash8>.js` + `data/assets/kap-nav.<hash8>.css`（css = `cat tokens.css kap-nav.css`，token 单源构建期合并）+ **稳定名副本** `kap-nav.js`/`kap-nav.css`（同内容——React 宿主 index.html 与注入中间件引稳定名，hash 名备缓存破坏对照）+ `data/assets/kap-nav.latest.json`（两文件名 manifest）。
4. portal `index.html` 的 `<link>` 与 Task 1 的 iframe 页等引用经 manifest 文件名（构建脚本同步 portal index.html 或 portal build 前置依赖 kap-nav 构建——deploy-portal.sh 先跑 build-kap-nav）。
5. **设计检查步**：对照 UI-SPEC §Component Inventory P-1 逐属性核对（40/26 档高、词表 5 项、active 语法）；§Do-Not-Regress 5（零新 hue/hex）。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && node scripts/build-kap-nav.mjs && ls data/assets/kap-nav.*.js data/assets/kap-nav.*.css >/dev/null && grep -c "#[0-9A-Fa-f]\{3,8\}" packages/portal/src/nav/kap-nav.css | grep -q "^0$" && grep -q "customElements.define('kap-navbar'" packages/portal/src/nav/kap-nav.ts && curl -sf "http://localhost:10588/assets/$(node -e "console.log(require('./data/assets/kap-nav.latest.json').js)")" | grep -q "kap-navbar" && echo NAV-OK</automated>
  </verify>
  <acceptance_criteria>
    - `data/assets/` 下产物 js+css 存在且经 `/assets` URL 可取（curl 200 含 kap-navbar）。
    - `kap-nav.css` 零 hex（grep 断言——token-only 红线）。
    - 产物 css 含 tokens.css 的 `--cv-bg-panel` 定义（concat 生效：`grep -c -- "--cv-bg-panel" <产物css>` ≥ 2——定义一次 + 消费一次）。
    - `kap-nav.ts` 无任何 import（自包含，可被三宿主引）。
  </acceptance_criteria>
  <done>navbar 单源 + 产物链就绪；门户页全宽档渲染（main.tsx 已 import 注册）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 门户首页（项目入口 + 集行 + 管线带 micro + 深链发码）+ projects.ts phases 直方图</name>
  <files>packages/portal/src/pages/PortalHome.tsx, packages/portal/src/components/PipelineRibbon.tsx, packages/portal/src/lib/ribbon.ts, packages/portal/src/__tests__/ribbon.test.ts, packages/portal/src/services/portalApi.ts, src/routes/canvas/projects.ts</files>
  <read_first>
    - src/routes/canvas/projects.ts:23-89（聚合查询全貌——扩展点 :37-49）
    - packages/infinite-canvas/src/components/ProjectSelector.tsx:20-36（既有消费者——additive 安全边界）
    - packages/infinite-canvas/src/constants/phaseRegistry.ts:19-91（PipelinePhaseDef 字段）
    - packages/infinite-canvas/src/components/canvas/PhaseColumns.tsx（phaseGroup 色带 + P0X 标签文法——管线带词汇源）
    - .planning/phases/57-portal-delivery-pages/57-UI-SPEC.md §Signature Element（管线带 micro 规格）+ §Component Inventory P-2 + §Copywriting Contract（门户文案表）+ §Color（accent reserved 第 5 条）
  </read_first>
  <action>
1. `src/routes/canvas/projects.ts` 扩展：在既有 (project_id, episodes_id) 聚合旁加一维 `phase_index` GROUP BY；episodes[] 每项增量加 `phases: Record<number, number>`（phaseIndex→count，只含 count>0 键）。**不改动**既有字段语义/排序（Pitfall 9）；null phase_index 不进直方图。
2. `packages/portal/src/lib/ribbon.ts`（纯函数，先写测试）：
   - `ribbonSegments(registry: PipelinePhaseDef[], counts: Record<number, number>): RibbonSegment[]`——按 sortKey 升序输出 `{code, name, group, sub, khsPrefix, phaseIndex, filled}`；filled = (counts[phaseIndex] ?? 0) > 0。
   - `ribbonHref(projectId: number, ep: number, zone?: string): string`——zone 缺省 `/canvas?project={id}&ep={ep}`；有则 `&zone={khsPrefix}`（D-05 格式唯一发码点之一）。
   - vitest `__tests__/ribbon.test.ts`：22 段恒等、sortKey 升序、sub 段标记（P03.5/P09b/P09c/P10c/P11a0/P11c 恰好 6 段 sub=true）、filled 判定、href 格式（含/不含 zone）。
3. `PipelineRibbon.tsx`（micro 档本 task；full 档 props 预留）：段 = `<button>`（focus-visible 可达），micro 高 8px、filled 段 phaseGroup weak 填充（`v3theme.modalityWeak` 同式 alpha 0.12）+ 1px 顶边线全饱和、空段发丝线描边、sub 段 60% 高底对齐、段间 2px；整条 hover tooltip（`{P0X} {名称} · {N} 节点` mono t4）；整条外层 `<a>` 包裹 → `ribbonHref`（段级 zone 跳转 full 档才有，micro 点击走集级 href）。聚合失败 → 整条不渲染（UI-SPEC §Signature 5）。
4. `portalApi.ts`：`import { fetchProjects } from '@ic/services/canvasApi'` re-export + `episodesOf(projects)` 派生助手。**禁止**重写 fetch 逻辑。
5. `PortalHome.tsx`：数据 = fetchProjects；状态机 = 加载骨架（3 行 quiet）/ 数据 / 空态（「暂无项目」+ body 见 UI-SPEC 表）/ 失败（重试 copy）。项目分节 = 项目名 t1 + meta `{N} 集 · {M} 资产`（mono 计数）+ 集行列表（行高 ≥28px、行间发丝线）：ep 号 mono t3 + 节点数 t4 tertiary + 管线带 micro（flex-1）+ [画布] [交付] ghost 按钮（28px）→ `ribbonHref` / `/deliver/{episodesId}`。
6. **设计检查步**：对照 UI-SPEC §Color accent reserved（本页冷白只允许 navbar 当前项 + 键盘焦点环 + 行选中态——无填充主按钮）；§Typography（mono 规则）；§Signature（micro 无 gate 点）逐条核对后提交。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/portal && npx vitest run src/__tests__/ribbon.test.ts && cd ../.. && npx tsc -p packages/portal/tsconfig.json --noEmit && bash scripts/deploy-portal.sh && curl -sf http://localhost:10588/api/canvas/projects -X POST -H 'content-type: application/json' -d '{}' | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const arr=j.data?.projects||j.projects||(Array.isArray(j.data)?j.data:[]);const eps=(arr[0]||{}).episodes||[];if(eps.length===0){console.log('NO-DATA-SKIP');return}if(eps[0].phases===undefined){process.exit(1)}console.log('PHASES-OK')})" && curl -sf http://localhost:10588/portal/ | grep -q "kap-navbar" && echo HOME-OK</automated>
  </verify>
  <acceptance_criteria>
    - vitest 全绿（22 段/sub 集合/href 三组断言）。
    - POST /api/canvas/projects 响应：有数据的库上 episodes[0].phases 为对象；库空时 NO-DATA-SKIP 分支放行（headless 渲染断言由 57-08 补）。既有字段 id/nodeCount 仍在。
    - `/portal/` HTML 含 `kap-navbar`；React 渲染后有项目分节（headless 抽查——若库空则以空态文案断言）。
    - 集行按钮 href 形如 `/canvas?project=..&ep=..`（D-05 格式；tsc 保证编译）。
    - `grep -rn "P0[1-9]\|选题" packages/portal/src/components/PipelineRibbon.tsx | grep -v import` 为空（无内联 phase 词表——词汇全来自注册表）。
  </acceptance_criteria>
  <done>门户首页三件套（路由/导航/项目入口）可运行——PORTAL-01 成功标准 1 后半达成；管线带 micro + 深链发码上线。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 → express 静态（门户页） | 公开静态面（与既有岛同层，先于 JWT） |
| /canvas 重定向 | 服务端 302，query 注入面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-02a | Tampering | /canvas 302 开放重定向 | mitigate | 目标路径写死 `/infinite-canvas/`；仅白名单四键重发；不回显任意 URL 参数（acceptance 断言 Location 精确形态） |
| T-57-02b | DoS | projects 聚合查询加维 | mitigate | 单条 GROUP BY 增一维（既有查询同量级）；无 N+1 |
| T-57-02c | Tampering | 门户挂载先于 JWT | accept | 与既有三岛同层（app.ts:187-204 先例）；/api 面不新增（V6 全开现状不变，非本期威胁面） |
</threat_model>

<verification>
- Task 1-3 各 automated verify 全绿（含 Pitfall 1 反向断言、token-only 断言、D-05 Location 精确断言）。
- `cd packages/infinite-canvas && npx tsc -b --pretty`（跨包 import 不破坏画布编译）。
- 人工：/portal 页视觉抽查看冷白是否只出现在 reserved 清单位置（设计检查步复核）。
</verification>

<success_criteria>
- 三路由 + /canvas 302 + navbar 产物链 + 门户首页可运行（成功标准 1 后半 + 2 前半）。
- 共存零破坏（/ 与 /api/project/* 断言不变）。
- 零新增依赖；token-only；phase 词汇单源。
</success_criteria>

<output>
Create `.planning/phases/57-portal-delivery-pages/57-02-SUMMARY.md` when done
</output>
