---
phase: 57-portal-delivery-pages
plan: 04
type: execute
wave: 2
depends_on: ["57-02"]
files_modified:
  - src/app.ts
  - src/lib/islandNavInjector.ts
autonomous: true
requirements: [PORTAL-02]
must_haves:
  truths:
    - "D-07: story-map 与 director-desk 两静态岛的 index.html 响应被注入共享 navbar（<kap-navbar data-active=\"...\"> + /assets/kap-nav.js/css 引用）——任一岛页面可抵其余三套（成功标准 2 收口）"
    - "注入 = express serve 时响应后处理（U-03）：幂等（已含 kap-navbar 则原样透传）、内存缓存、异常 fail-loud 不崩（透传原 HTML + 一次性 warn）；**不写磁盘**（Do-Not-Regress 9）"
    - "两岛自有内容零破坏：静态资产/子路由 SPA fallback 行为不变；story-map 自有站内 Navbar 保留（不吞不改）"
  artifacts:
    - path: "src/lib/islandNavInjector.ts"
      provides: "注入中间件工厂：createIslandNavInjector(prefix, activeId) —— text/html 响应拦截 + 标签插入 + 幂等缓存"
      exports: ["createIslandNavInjector"]
    - path: "src/app.ts（注入段）"
      provides: "/story-map 与 /director-desk 根路径及 SPA fallback 路由接注入（注册于两岛静态段之前/替代其 index 响应路径）"
  key_links:
    - from: "src/lib/islandNavInjector.ts"
      to: "data/assets/kap-nav.css|js"
      via: "插入 <link>/<script> 引用稳定名产物（57-02 build 产物）"
      pattern: "kap-nav"
---

<objective>
四岛共脸收口（PORTAL-02 成功标准 2 后半）：把共享 navbar 注入 story-map 与 director-desk 两个无源码可控的静态岛。Toonflow 岛已由 57-02 的 /toonflow 嵌入页覆盖；画布岛在 57-03。

Purpose: 任一页面可抵达其余三套。机制取 serve 时响应后处理（U-03 裁决：director-desk 无部署脚本、story-map 每次 rm -rf 重部署——deploy 期注入覆盖不了，serve 时单点幂等全覆盖；deploy-story-map.sh 既有部署链零改动）。
Output: islandNavInjector.ts + app.ts 两岛接线。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/57-portal-delivery-pages/57-CONTEXT.md
@.planning/phases/57-portal-delivery-pages/57-UI-SPEC.md
@.planning/phases/57-portal-delivery-pages/57-RESEARCH.md
@.planning/phases/57-portal-delivery-pages/57-PATTERNS.md

<interfaces>
<!-- 两岛服务段（注入落点） -->
src/app.ts:187-194（director-desk：static :190 + SPA fallback :191-193）
src/app.ts:196-204（story-map：static :200 + SPA fallback :201-203，HashRouter 注释 :197）
data/web/story-map/index.html（1,131 bytes：title 剧核 DramaCore；相对 ./assets 引用；body 仅 <div id="root">；自有 Navbar 在 bundle 内——kais-story-map/src/components/Navbar.tsx，Layout.tsx:65 挂载）
data/web/director-desk/index.html（575 bytes：title 3D导演台 Demo；绝对 /director-desk/assets 引用；body 仅 <div id="root">；无任何部署脚本——手动部署史）
<!-- navbar 产物 -->
data/assets/kap-nav.js|css（57-02 稳定名产物；<kap-navbar> custom element，data-active 属性驱动）
<!-- UI-SPEC 契约 -->
.planning/phases/57-portal-delivery-pages/57-UI-SPEC.md §Component Inventory P-1 注入档 + §Do-Not-Regress 9 + §Design Decisions Log U-03
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: islandNavInjector 中间件 + 两岛接线</name>
  <files>src/lib/islandNavInjector.ts, src/app.ts</files>
  <read_first>
    - src/app.ts:187-204（两岛服务段全貌——中间件须在此段之前/包裹 index 响应路径）
    - data/web/story-map/index.html、data/web/director-desk/index.html（真实 HTML 形态——插入点 <body...> 开标签后）
    - .planning/phases/57-portal-delivery-pages/57-UI-SPEC.md §Component Inventory P-1（注入形态）+ §Do-Not-Regress 9
  </read_first>
  <action>
1. 新建 `src/lib/islandNavInjector.ts`：
   ```typescript
   export function createIslandNavInjector(opts: { islandDir: string; activeId: string }) {
     // 返回 express handler：拦截 GET 根路径与 extensionless SPA 路径的 index.html 响应
     // 1) 读 index.html 一次（fs.readFile），插入：
     //    <link rel="stylesheet" href="/assets/kap-nav.css">
     //    <kap-navbar data-active="{activeId}"></kap-navbar>
     //    <script src="/assets/kap-nav.js" defer></script>
     //    插入点 = <body ...> 开标签之后（正则 /<body[^>]*>/i 首次命中；未命中 → 原样透传 + warn）
     // 2) 幂等：已含 'kap-navbar' 则跳过插入
     // 3) 缓存：注入结果 + mtime 记忆化（源文件变了才重读——story-map rm -rf 重部署后 inode/mtime 变即重注）
     // 4) 异常：readFile/正则任何异常 → 透传原文件流 + console.warn 一次（fail-loud 不崩）
     // 5) Content-Type: text/html; charset=utf-8 + Content-Length 重算
   }
   ```
2. `src/app.ts` 接线（两岛段改造，保持顺序仍在全局 fallback 之前）：
   - `/director-desk`：根与 SPA fallback 的 index 响应改走注入 handler（`app.get('/director-desk', ...)` + 既有 `'/director-desk/{*path}'` fallback 内 extensionless 分支走注入；带扩展名请求仍 express.static 直出）。
   - `/story-map` 同式（activeId 'story-map' / 'director-desk'）。
   - **注入仅在 html 响应**：子路径带扩展名（/story-map/assets/*.js）不经注入。
3. 验证前跑一次 `node scripts/build-kap-nav.mjs`（产物在）+ dev server 自动重启（nodemon 监听 src/**）。
4. **设计检查步**：注入产物对照 UI-SPEC P-1 注入档（40px 全宽档、文档流置顶不 fixed 覆盖——Pitfall 6 双层导航不重叠）；token 经产物 css 自带（tokens.css 已 concat）。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && sleep 1 && curl -s http://localhost:10588/story-map/ | grep -c "kap-navbar" | grep -q "^1$" && curl -s http://localhost:10588/director-desk/ | grep -c "kap-navbar" | grep -q "^1$" && curl -s http://localhost:10588/story-map/ | grep -q 'data-active="story-map"' && curl -s http://localhost:10588/director-desk/ | grep -q 'data-active="director-desk"' && curl -sI http://localhost:10588/story-map/assets/index-ZlW0egw4.js | grep -qi "200\|content-length" && ! curl -s http://localhost:10588/story-map/assets/index-ZlW0egw4.js | grep -q kap-navbar && curl -s http://localhost:10588/story-map/some-spa-route | grep -q kap-navbar && echo INJECT-OK</automated>
  </verify>
  <acceptance_criteria>
    - `/story-map/` 与 `/director-desk/` HTML 恰好含 1 处 `kap-navbar`（幂等——重复请求仍是 1）且 data-active 正确。
    - 带扩展名资产响应不含 kap-navbar（只注 HTML）且仍 200。
    - SPA 子路由（/story-map/some-spa-route）也含注入（extensionless fallback 走注入）。
    - `git status data/web/story-map data/web/director-desk` 无改动（不写磁盘——Do-Not-Regress 9）。
    - kap-nav.css 产物在响应中被引用（`grep kap-nav.css` 命中）。
  </acceptance_criteria>
  <done>四岛共脸收口：门户/画布（57-02/03）+ 两静态岛（本 plan）+ Toonflow（嵌入页）——任一页可抵其余三套（成功标准 2 全句达成）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| express → 本仓静态岛 HTML | 响应期改写（只对两个固定前缀的 text/html） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-04a | Tampering | HTML 注入面被泛化滥用 | mitigate | 中间件工厂只由两岛固定前缀调用；插入内容为静态常量串（无任何用户输入插值）；带扩展名请求不经注入 |
| T-57-04b | DoS | 每请求重读文件 | mitigate | mtime 记忆化缓存；异常路径透传 |
</threat_model>

<verification>
- Task 1 automated verify 全绿（幂等/扩展名排除/SPA 子路由/不写磁盘四组断言）。
- 人工：浏览器开 /story-map/ 看双层导航（kap-nav 上、站内 Navbar 下）视觉不重叠。
</verification>

<success_criteria>
- 两静态岛页面顶部出现共享 navbar 且能跳其余三套（成功标准 2）。
- 磁盘零改写、两岛自有功能零破坏。
</success_criteria>

<output>
Create `.planning/phases/57-portal-delivery-pages/57-04-SUMMARY.md` when done
</output>
