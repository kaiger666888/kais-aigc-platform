---
phase: 57-portal-delivery-pages
plan: 03
type: execute
wave: 2
depends_on: ["57-02"]
files_modified:
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
  - packages/infinite-canvas/src/lib/deepLink.ts
  - packages/infinite-canvas/src/__tests__/deepLink.test.ts
  - packages/infinite-canvas/vite.config.ts
  - packages/infinite-canvas/index.html
autonomous: true
requirements: [PORTAL-02]
must_haves:
  truths:
    - "D-05 深链画布半边闭环：/canvas?project&ep&focus&zone 经 57-02 的 302 到达 /infinite-canvas/ 后，focus → setFocusAssetNodeId(nodeId)、zone(khsPrefix) → PHASE_REGISTRY 查 phaseIndex → 该 phase 首个资产节点 → 同一 setFocusAssetNodeId——只复用既有 effect（FlowCanvas.tsx:762-786），不写第二套 viewport 机制"
    - "D-06: 画布 topbar 左簇嵌入 kap-nav compact 档（同源 import），48px topbar 高度与 calc(100vh - 48px) 布局零改动（Do-Not-Regress 1）"
    - "既有 ?projectId/?episodesId 参数、replaceState 回写（FlowCanvas.tsx:372-375）与 /infinite-canvas/ 直链完全兼容（回归断言）"
    - "落点不存在走既有 focusAssetNodeId toast 文案；zone 无节点时静默（只加载不跳）"
  artifacts:
    - path: "packages/infinite-canvas/src/lib/deepLink.ts"
      provides: "深链参数纯函数：parseDeepLink(search) + resolveDeepLinkTarget({focus, zone, nodes, registry})"
      exports: ["parseDeepLink", "resolveDeepLinkTarget", "DeepLinkTarget"]
    - path: "packages/infinite-canvas/src/components/FlowCanvas.tsx（改动段）"
      provides: "getInitialParams 扩展 focus/zone + 图加载后一次性消费 + topbar 左簇 kap-nav compact"
  key_links:
    - from: "packages/infinite-canvas/src/lib/deepLink.ts"
      to: "packages/infinite-canvas/src/constants/phaseRegistry.ts"
      via: "PHASE_REGISTRY.find(p => p.khsPrefix === zone)（55-D04 单源）"
      pattern: "PHASE_REGISTRY"
    - from: "packages/infinite-canvas/src/components/FlowCanvas.tsx"
      to: "packages/portal/src/nav/kap-nav.ts"
      via: "vite alias '@portal-nav' → ../portal/src/nav/kap-nav.ts；import 副作用注册"
      pattern: "kap-nav"
---

<objective>
打通 PORTAL-02 的画布半边：门户/交付页发出的 D-05 深链在画布落地（focus 精确节点 / zone 泳道定位），并把共享 navbar 以 compact 档嵌进画布 topbar（四岛共脸第二宿主）。

Purpose: 成功标准 2「项目页可深链跳转画布指定节点/泳道」的终点段；/navbar 画布宿主。
Output: deepLink.ts 纯函数 + FlowCanvas 两处改动（参数消费 / topbar 左簇）+ e2e 断言。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/57-portal-delivery-pages/57-CONTEXT.md
@.planning/phases/57-portal-delivery-pages/57-UI-SPEC.md
@.planning/phases/57-portal-delivery-pages/57-RESEARCH.md
@.planning/phases/57-portal-delivery-pages/57-PATTERNS.md

<interfaces>
<!-- 画布参数与聚焦链（只复用不改语义） -->
packages/infinite-canvas/src/components/FlowCanvas.tsx:105-114（getInitialParams——扩展点，现只读 projectId/episodesId）
packages/infinite-canvas/src/components/FlowCanvas.tsx:372-375（loadCanvas 后 replaceState 回写 projectId/episodesId——保持不动，focus/zone 留在 URL 可重放）
packages/infinite-canvas/src/components/FlowCanvas.tsx:762-786（focusAssetNodeId effect：fitView/选中/高亮 1.5s 清空/未放置 toast——55 锁「只复用不改」）
packages/infinite-canvas/src/store/canvasStore.ts:187,1090-1091（focusAssetNodeId 状态与 setFocusAssetNodeId action）
packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx:828（setFocusAssetNodeId 调用方先例）
<!-- topbar 结构（Do-Not-Regress 1） -->
FlowCanvas.tsx:809-873（topbar JSX；左簇 logo+「无限画布」标题块 :812-815 为出让位）、:889（calc(100vh - 48px)）、:1215-1225（topBarStyle 48px）
<!-- 注册表与图标 -->
packages/infinite-canvas/src/constants/phaseRegistry.ts:19-91（PipelinePhaseDef.khsPrefix/phaseIndex）
packages/portal/src/nav/kap-nav.ts（57-02 产物——compact 档已实现属性）
<!-- 测试基座 -->
packages/infinite-canvas/src/hooks/__tests__/canvasState.test.ts（纯函数 vitest 式样）；test/e2e/tests/phase52-regen.mjs + helpers.mjs（loadCanvas/nodeSelector/expect.poll；地雷 #10：e2e 跑 dist build，先 npm run build + deploy）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deepLink.ts 纯函数 + FlowCanvas 参数消费（focus/zone → setFocusAssetNodeId）</name>
  <files>packages/infinite-canvas/src/lib/deepLink.ts, packages/infinite-canvas/src/__tests__/deepLink.test.ts, packages/infinite-canvas/src/components/FlowCanvas.tsx</files>
  <read_first>
    - packages/infinite-canvas/src/components/FlowCanvas.tsx:100-130（getInitialParams 上下文）、:340-420（loadCanvas 流程与 auto-load 时机——ProjectSelector.tsx:38-42 auto-load 同时机）
    - packages/infinite-canvas/src/constants/phaseRegistry.ts（khsPrefix 清单——p035/p09b/p09c/p10c/p11a0/p11a/p11b/p11c/p12a/p12b/p13/p14/p15 等）
    - packages/infinite-canvas/src/v3/adapter.ts:375-400（buildPhaseCatalog——节点 phaseIndex 的数据面）
    - packages/infinite-canvas/src/hooks/__tests__/canvasState.test.ts（vitest 式样）
  </read_first>
  <action>
1. 先写 `__tests__/deepLink.test.ts`（行为表 it.each）：
   - parseDeepLink：四键解析 + 未知键忽略 + 缺省 null。
   - resolveDeepLinkTarget：
     - focus 命中 → {kind:'focus', nodeId}；
     - focus 缺、zone 命中注册表且该 phaseIndex 有节点 → {kind:'zone', nodeId:首个资产节点 id}；
     - zone 命中注册表但无节点 → {kind:'none'}（静默，UI-SPEC State Matrix）；
     - zone 不在注册表（如 p05 注销前缀/乱码）→ {kind:'none'} + console.warn（fail-loud 不崩，55 同哲学）；
     - focus/zone 同给 → focus 优先（RESEARCH Code Example 契约）。
2. `src/lib/deepLink.ts` 实现上述两纯函数（签名：`parseDeepLink(search: string): {projectId?: string; episodesId?: string; focus?: string; zone?: string}`；`resolveDeepLinkTarget(p: {focus?: string; zone?: string; nodes: Array<{id: string; data?: {phaseIndex?: number}}>}): DeepLinkTarget`——内部 import PHASE_REGISTRY 做 zone→phaseIndex）。节点集合用 store 派生 RF nodes（data.phaseIndex 已在 adapter 面上）。
3. FlowCanvas 接线（最小侵入）：
   - getInitialParams 增读 focus/zone（同函数追加两个 get）。
   - loadCanvas 成功 resolve 后**一次性**消费（useRef 守卫防重放）：resolveDeepLinkTarget → nodeId 则 `setFocusAssetNodeId(nodeId)`；此后一切效果走既有 effect（762-786 原语义——含未放置 toast）。
   - **不改** replaceState 回写行（focus/zone 留 URL，刷新可重放——记录为既定行为）。
4. **设计检查步**：本 task 无新视觉面；核对 State Matrix 深链三态（命中/落点不存在 toast/zone 无节点静默）与 UI-SPEC §Component Inventory P-6 逐条一致。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/__tests__/deepLink.test.ts && npx tsc -b --pretty && npm run build >/dev/null 2>&1 && echo BUILD-OK</automated>
  </verify>
  <acceptance_criteria>
    - vitest 全绿（含 focus 优先 / zone 无节点静默 / 注销前缀 warn 三行为）。
    - `grep -c "setFocusAssetNodeId" packages/infinite-canvas/src/components/FlowCanvas.tsx` 较改前 +1 且新增调用处无自写 fitView/viewport 代码（复用断言：新增段无 `fitView(`）。
    - getInitialParams 仍读 projectId/episodesId（回归：`grep -A6 "getInitialParams" FlowCanvas.tsx` 含两键）。
    - tsc -b + vite build 通过。
  </acceptance_criteria>
  <done>深链画布半边逻辑闭环 + 纯函数测试护栏。</done>
</task>

<task type="auto">
  <name>Task 2: topbar 左簇嵌 kap-nav compact + e2e 深链断言</name>
  <files>packages/infinite-canvas/src/components/FlowCanvas.tsx, packages/infinite-canvas/vite.config.ts, packages/infinite-canvas/index.html, packages/infinite-canvas/test/e2e/tests/phase57-deeplink.mjs</files>
  <read_first>
    - FlowCanvas.tsx:809-873（topbar JSX 全貌——左簇 :812-815 出让位、GateTodoChip :865-872 不动）、:889、:1215-1225
    - packages/portal/src/nav/kap-nav.ts（57-02 产物；compact 属性契约）
    - packages/infinite-canvas/vite.config.ts:8-14（alias 段——追加 '@portal-nav'）
    - packages/infinite-canvas/test/e2e/helpers.mjs（loadCanvas/nodeSelector/expect.poll 用法）+ test/e2e/tests/phase52-regen.mjs（e2e 模板；地雷 #10）
    - .planning/phases/57-portal-delivery-pages/57-UI-SPEC.md §Component Inventory P-1 compact 档 + §Do-Not-Regress 1
  </read_first>
  <action>
1. `vite.config.ts` alias 追加一项：`'@portal-nav': path.resolve(__dirname, '../portal/src/nav/kap-nav.ts')`（跨包相对 alias，flowgraph-v3 先例同式）。
2. FlowCanvas topbar 左簇：删除 logo+「无限画布」标题块（:812-815），替换为：
   ```tsx
   import '@portal-nav'                       // 副作用注册（模块顶部）
   ...
   <kap-navbar compact data-active="canvas" style={{ alignSelf: 'center' }} />
   ```
   约束：topbar 48px 高度、`calc(100vh - 48px)`、视图切换簇/导航箭头/ProjectSelector/GateTodoChip 全部零改动（Do-Not-Regress 1）；kap-nav compact 26px 垂直居中，不产生横向溢出（5 项 + 品牌实测宽记录进 SUMMARY）。
3. `index.html` 追加 `<link rel="stylesheet" href="/assets/kap-nav.css">`（稳定名副本——build-kap-nav.mjs 同时产出 hash 名与稳定名两份，见 57-02 Task 2；React 宿主与注入宿主引稳定名，hash 名仅作缓存破坏对照，权衡记录进 SUMMARY）。React 19 对 custom element 属性直传（data-active/compact 原样渲染）。
4. e2e `test/e2e/tests/phase57-deeplink.mjs`（dist build + 10588 真实后端，52 模板）：
   - case A（focus）：loadCanvas 助手式导航至 `/infinite-canvas/?projectId=P&episodesId=E&focus=<已知节点id>`（P/E/节点从 /api/canvas/projects + load-v2 取真实值）→ `expect.poll` 断言该节点 `.selected` 或 detail 打开（focus effect 副作用）。
   - case B（zone）：`&zone=p13`（或库内有节点的 phase）→ poll 断言 viewport 中心接近该 phase 列（`window.__kaisCanvas` 桥 getNodes + store viewport——helpers 既有）。
   - case C（回归）：无参数直链 `/infinite-canvas/?projectId=P&episodesId=E` 正常加载（首帧节点数 >0）。
5. **设计检查步**：compact 档对照 UI-SPEC P-1（26px/t3/5 项/当前项下划线冷白）；topbar 其余簇未被折行（e2e case C 附带断言视图切换簇可见宽 > 0）。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx tsc -b --pretty && npm run build >/dev/null 2>&1 && cd /data/workspace/kais-aigc-platform && bash scripts/deploy-canvas.sh >/dev/null 2>&1 && cd packages/infinite-canvas && npx playwright test test/e2e/tests/phase57-deeplink.mjs --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - e2e 三 case 全绿（focus 命中 / zone 定位 / 无参回归）。
    - `/infinite-canvas/` 页 HTML 含 `kap-navbar`（curl 断言）与 `kap-nav` css link。
    - `grep -c "calc(100vh - 48px)" FlowCanvas.tsx` 不变（===1）且 topbar 高度常量 48 无新增第二横条（`grep -c "kap-navbar" FlowCanvas.tsx` === 1）。
    - 画布编译/构建零回归（Task 1 verify 已含 tsc -b）。
  </acceptance_criteria>
  <done>深链端到端闭环（门户发码 → 302 → 画布聚焦）+ 画布 navbar 宿主上线；PORTAL-02 成功标准 2 的画布半边达成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL 参数 → 画布状态 | query 直达 store 聚焦动作（无写路径，只读定位） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-03a | Tampering | 恶意 zone/focus 值 | mitigate | 纯函数白名单解析（注册表外 → none + warn）；无 DOM 写/无 eval 面 |
| T-57-03b | DoS | 重放触发 fitView 动画 | accept | 一次性消费守卫（useRef）；刷新重放属既定 UX |
</threat_model>

<verification>
- Task 1 vitest + tsc；Task 2 e2e 三 case + HTML 断言。
- 人工：/infinite-canvas/ 顶栏视觉抽查（compact navbar 与视图切换簇并排不挤——Q6 裁决验证）。
</verification>

<success_criteria>
- /canvas?project&ep&focus|zone 从门户到画布聚焦全程无手动操作（成功标准 2 深链句）。
- topbar 嵌 navbar 后画布布局/交互零回归（Do-Not-Regress 1 断言组）。
</success_criteria>

<output>
Create `.planning/phases/57-portal-delivery-pages/57-03-SUMMARY.md` when done
</output>
