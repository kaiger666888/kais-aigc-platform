---
phase: 57-portal-delivery-pages
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/skills/contract.ts
  - src/skills/validator.ts
  - src/skills/defaultSkill.ts
  - src/skills/loader.ts
  - .planning/specs/SKILL-CONTRACT.md
  - docs/skill-author-guide.md
  - docs/skill-author-guide/movie-v1.manifest.json
  - scripts/verify-phase-57.ts
  - scripts/verify-phase-30.ts
  - scripts/verify-phase-31.ts
  - package.json
autonomous: true
requirements: [PORTAL-04]
must_haves:
  truths:
    - "D-13/D-16: movie-v1 phase_taxonomy 从 12 条重写为 22 条——id=PHASE_REGISTRY khsPrefix、order=sortKey 序、label=注册表 name、requires_review=有门、review_gate=derivedGateId（p03-gate 形态）或空（D-15）；直接切换无 legacy adapter"
    - "D-15 契约四处 lockstep：contract.ts PhaseDecl + validator.ts phaseDeclSchema(.strict()) + SKILL-CONTRACT.md PhaseDecl 表 + skill-author-guide.md §2.2 同步加 review_gate（可选字段——旧 manifest 行不带该键仍可加载，Pitfall 10）"
    - "D-14: scripts/verify-phase-57.ts 三方 drift 断言——taxonomy ↔ PHASE_REGISTRY ↔ GATE_CATALOG（含 review_gate 非空 ⇔ requires_review、值 ∈ derivedGateId 集、无门 phase 空）；forced-fail 段（内存改一条即红）"
    - "registry 加载链保真：o_skillRegistry 已 seed 旧 12 条行被更新为新常量（幂等 upgrade），loader 加载后 GET /api/v1/skills/phases 口径 22 条且 phase-complete.ts 的 requires_review gating 对新 id 不静默丢失（断言）"
    - "verify-phase-30/31 的 12 条断言 supersede：30 expectedPhaseIds 换 22 前缀清单（源自注册表）；31 OLD_* 快照保留为历史文档、其等价断言段迁移指向 verify-phase-57（不静默改 OLD_*）"
  artifacts:
    - path: "src/skills/defaultSkill.ts（taxonomy 段）"
      provides: "22 条 PhaseDecl（真值源内联常量——docs JSON 是生成物）"
    - path: "src/skills/contract.ts + validator.ts"
      provides: "PhaseDecl.review_gate?: string 契约与 zod .strict() 扩展"
    - path: "scripts/verify-phase-57.ts"
      provides: "三方 drift 契约测试 + forced-fail + npm verify:phase-57"
  key_links:
    - from: "src/skills/defaultSkill.ts"
      to: "packages/infinite-canvas/src/constants/phaseRegistry.ts"
      via: "taxonomy 22 条内容 = PHASE_REGISTRY 逐条映射（id←khsPrefix、order←sortKey、label←name、requires_review/review_gate←gateCatalog）"
      pattern: "PHASE_REGISTRY"
    - from: "scripts/verify-phase-57.ts"
      to: "src/lib/gateCatalog.ts"
      via: "review_gate 值域 = GATE_CATALOG deriveGateId 集（红线条目不标——platformInvisible 不进人工 review）"
      pattern: "GATE_CATALOG"
    - from: "scripts/verify-phase-57.ts"
      to: "/data/workspace/kais-hermes-skills/plugins/review_gates/gates.yaml"
      via: "js-yaml 只读解析（verify-phase-54 diffCatalogAgainst 先例）——四链真值源最终对齐 khs"
      pattern: "gates.yaml"
---

<objective>
PORTAL-04 全量：manifest taxonomy 12→22/16 重对齐 + review_gate 真实 gate 标注 + 三方 drift 契约测试 + 旧断言 supersede。纯 kap 侧（khs 只读）。

Purpose: 成功标准 4——registry 加载新 manifest 后 phase 查询与画布 zone 词汇一致（drift 断言通过）；55-D04 单一注册表成为 taxonomy 的唯一上游（manifest 不自维护）。
Output: 契约四处 lockstep + 22 条 taxonomy + 行升级 + verify-phase-57 + 30/31 supersede。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/57-portal-delivery-pages/57-CONTEXT.md
@.planning/phases/57-portal-delivery-pages/57-RESEARCH.md
@.planning/phases/57-portal-delivery-pages/57-PATTERNS.md

<interfaces>
<!-- manifest 真值源与生成物 -->
src/skills/defaultSkill.ts:55-152（MOVIE_V1_MANIFEST 内联常量；phase_taxonomy :111-124 现 12 条 PhaseDecl 字面量；:43 注释「12 entries」；:169-180 模块加载自检 validateManifest；:206-241 seedDefaultIfEmpty——只在空表跑，行升级需显式路径）
scripts/gen-movie-v1-manifest.ts:13-18（import 常量 → 写 docs/skill-author-guide/movie-v1.manifest.json；npx tsx 运行）
docs/skill-author-guide/movie-v1.manifest.json:53-162（生成物；现 12 条 order 0-11）
<!-- 契约链（lockstep 四处） -->
src/skills/contract.ts:98-104（PhaseDecl 接口）、:176-188（SkillManifest.phase_taxonomy）
src/skills/validator.ts:60-68（phaseDeclSchema .strict()——新键不加大 ≠ 会被拒）、:97-111（manifestSchema + .strict()）
.planning/specs/SKILL-CONTRACT.md:145-157（PhaseDecl 子接口表——contract.test.ts 双向漂移断言的对照面）
docs/skill-author-guide.md:65-88（§2.2 PhaseDecl shape 文档）
src/skills/__tests__/contract.test.ts:281-287（zod ↔ spec 双向断言；runner = scripts/verify-phase-28.ts:29）
<!-- 注册表与门快照（映射上游） -->
packages/infinite-canvas/src/constants/phaseRegistry.ts:19-91（PipelinePhaseDef：khsPrefix/sortKey/name/phaseIndex——taxonomy 三字段直接源）
src/lib/gateCatalog.ts:76-165（EXPECTED_GATE_COUNT=16、GATE_CATALOG、deriveGateId :53-57、isRedlineKey :59-61、GATE_DISPLAY_NAMES）
/data/workspace/kais-hermes-skills/plugins/review_gates/gates.yaml:57-321（16 门真值源；:45-52 注释 derived gate_id 是 canonical SSoT——review_gate 取值形态依据；:173 Gate 8 delivery）
<!-- 运行时消费暗礁 -->
src/routes/v1/pipeline/callback/phase-complete.ts:53-55（phaseDecl?.requires_review ?? false——id 改名静默丢 gating；inferAssetType(phase) 同行链）
src/skills/loader.ts:48-106（loadAllFromDB：行 JSON.parse + validateManifest，invalid 跳过——行升级后必须全量可加载）
src/routes/v1/skills/phases.ts:24-31（taxonomy 透传端点——22 条断言面）
<!-- 旧断言（supersede 对象） -->
scripts/verify-phase-30.ts:263-290（taxonomy.length===12 + expectedPhaseIds 12 id 清单）、:420-422（端点 12 条）
scripts/verify-phase-31.ts:48-52（OLD_* 快照 declared DO NOT EDIT）、:141-148（taxonomy.length===12 等价断言）
<!-- 契约测试先例 -->
scripts/verify-phase-55.ts:37-175（SIBLING_ROOT env + 脆弱解析即信号 + A-E 断言组）
scripts/verify-phase-54.ts:84-134（js-yaml 解析 gates.yaml + diffCatalogAgainst）、:497-506（forced-fail）
package.json:46-47（verify:phase-54/55 注册位）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 契约字段扩展（contract.ts + validator.ts + SKILL-CONTRACT.md + skill-author-guide.md 四处 lockstep）</name>
  <files>src/skills/contract.ts, src/skills/validator.ts, .planning/specs/SKILL-CONTRACT.md, docs/skill-author-guide.md</files>
  <read_first>
    - src/skills/contract.ts:90-110（PhaseDecl 上下文——字段注释风格）
    - src/skills/validator.ts:55-70（phaseDeclSchema .strict() 写法）
    - .planning/specs/SKILL-CONTRACT.md:140-160（PhaseDecl 表排版）
    - docs/skill-author-guide.md:60-90（§2.2 文档式样）
    - src/skills/__tests__/contract.test.ts:275-295（双向断言如何读 spec 表——新行会被自动覆盖到）
  </read_first>
  <action>
四处同步加 `review_gate`：
1. `contract.ts` PhaseDecl：`review_gate?: string`（可选——旧 manifest 行不带该键仍合法，Pitfall 10；注释：真实审核门 ID（gateCatalog derivedGateId 形态，如 'p03-gate'）；无门 phase 留空/缺省，D-15 机器可读语义）。
2. `validator.ts` phaseDeclSchema：`review_gate: z.string().optional()`（.strict() 保留——已知键白名单扩一）。
3. SKILL-CONTRACT.md PhaseDecl 表加一行（同排版）：字段名/类型/语义/可选性。
4. skill-author-guide.md §2.2 加同字段说明 + 「由平台 gate 快照对齐、skill 作者通常不手填」注记。
5. 跑 `npx tsx scripts/verify-phase-28.ts` 确认 contract.test 双向断言仍绿（新字段四处同步即绿）。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsx scripts/verify-phase-28.ts && grep -q "review_gate" src/skills/contract.ts src/skills/validator.ts .planning/specs/SKILL-CONTRACT.md docs/skill-author-guide.md && echo LOCKSTEP-OK</automated>
  </verify>
  <acceptance_criteria>
    - verify-phase-28 绿（zod ↔ spec 双向零漂移）。
    - 四文件各含 review_gate（grep 四命中）；validator 保持 `.strict()`（`grep -c "strict()" src/skills/validator.ts` 不减）。
  </acceptance_criteria>
  <done>契约面扩位完成；旧 manifest 兼容（可选字段）。</done>
</task>

<task type="auto">
  <name>Task 2: taxonomy 22 条重写（defaultSkill.ts）+ o_skillRegistry 行升级 + 生成物再生</name>
  <files>src/skills/defaultSkill.ts, src/skills/loader.ts, docs/skill-author-guide/movie-v1.manifest.json</files>
  <read_first>
    - src/skills/defaultSkill.ts:55-152（常量全貌 + :109-110 注释）、:206-241（seedDefaultIfEmpty 幂等风格——行升级照此风格）
    - packages/infinite-canvas/src/constants/phaseRegistry.ts:52-79（22 条 khsPrefix/sortKey/name 逐值——映射源，照抄勿手打）
    - src/lib/gateCatalog.ts:104-121（16 门 phase 键→derivedGateId；红线条目 :118-120 排除）
    - src/skills/loader.ts:48-106（行加载路径——升级后全量可加载的验收面）
    - src/routes/v1/skills/phases.ts:24-31（端点透传）
  </read_first>
  <action>
1. `defaultSkill.ts` phase_taxonomy 重写为 22 条（D-16 直接切换，无 adapter）：
   - 每条：`{ id: <khsPrefix>, order: <sortKey 序 0..21>, label: <注册表 name>, requires_review: <有门>, review_gate: <derivedGateId 或 ''>, ingest_outputs: <按 canvasType 映射：video→['videos']、audio→['audio']、script/storyboard→['storyboard'/'none'] 依原 12 条先例词汇> }`。
   - 有门 phase（13 个）：p01/p02/p03/p04/p06/p07/p09c/p10c/p11a0/p11a/p11b/p11c/p13 → review_gate 'p01-gate'…'p13-gate'；requires_review true。
   - 无门 phase（9 个）：p035/p08/p09/p09b/p10/p12a/p12b/p14/p15 → review_gate ''、requires_review false。
   - 红线 3 门**不**占 taxonomy 条目（它们是 p13 的子门，platformInvisible——54 U-06；taxonomy 条目数 = 22 恒定）。
   - 更新 :43 与 :109-110 的「12」注释为 22（防注释漂移）。
   - id 用 khsPrefix（p035 而非 p03.5）——与 D-05 zone 参数、phase-complete.ts 查找链同词汇（Pitfall 4）。
2. 行升级：`loader.ts`（或 defaultSkill.ts seed 段旁）加幂等 upgrade——`UPDATE o_skillRegistry SET manifest = <新常量 JSON> WHERE skill_id = 'movie-v1' AND manifest->phase_taxonomy 数量 !== 22`（better-sqlite3 同步事务；沿 seedDefaultIfEmpty 风格；升级后 registry.register 重放）。加载时打印一行升级日志。
3. `npx tsx scripts/gen-movie-v1-manifest.ts` 再生 docs JSON（verify-phase-33:194-197 逐字断言即绿）。
4. 端点冒烟：dev server 重启后 `GET /api/v1/skills/phases` 返回 22 条（loader 升级路径生效证明）。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsx scripts/verify-phase-33.ts && npx tsx -e "import('./src/skills/defaultSkill.ts').then(m=>{const t=m.MOVIE_V1_MANIFEST.phase_taxonomy;if(t.length!==22)process.exit(1);const withGate=t.filter(p=>p.review_gate);if(withGate.length!==13)process.exit(2);if(t.some(p=>p.review_gate?!p.requires_review:false))process.exit(3);console.log('TAXONOMY-22-OK')})" && curl -sf http://localhost:10588/api/v1/skills/phases | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const t=(j.data?.phases||j.phases||j.data||[]);if(t.length!==22)process.exit(1);console.log('ENDPOINT-22-OK')})"</automated>
  </verify>
  <acceptance_criteria>
    - verify-phase-33 绿（生成物 ↔ 常量逐字同步）。
    - 常量断言：22 条、13 条带 review_gate、review_gate 非空 ⇒ requires_review true。
    - 端点返回 22 条（行升级生效；若 dev server 未跑则先 `npm run dev:server` 冒烟——SUMMARY 记录）。
    - `grep -c "12 PhaseDecl\|12 entries" src/skills/defaultSkill.ts` === 0（注释同步）。
    - id 集与 PHASE_REGISTRY khsPrefix 集一致（Task 3 契约断言正式收口，此处先 grep p035/p11a0/p12b 三代表存在）。
  </acceptance_criteria>
  <done>22/16 taxonomy 上线 + 生成物再生 + 存量行升级——成功标准 4 前半。</done>
</task>

<task type="auto">
  <name>Task 3: verify-phase-57 三方 drift 契约测试 + forced-fail + 30/31 supersede + npm 注册</name>
  <files>scripts/verify-phase-57.ts, scripts/verify-phase-30.ts, scripts/verify-phase-31.ts, package.json</files>
  <read_first>
    - scripts/verify-phase-55.ts 全文（SIBLING_ROOT/env、脆弱解析注释、A-E 断言组、harness 式样）
    - scripts/verify-phase-54.ts:84-134（js-yaml gates.yaml 解析 + diffCatalogAgainst）与 :497-506（forced-fail 内存突变）
    - scripts/verify-phase-30.ts:260-295,415-425（12 断言上下文——supersede 改写点）
    - scripts/verify-phase-31.ts:45-55,135-150（OLD_* 快照声明 + 等价断言段——迁移对象）
    - package.json:46-47（scripts 注册位）
  </read_first>
  <action>
1. 新建 `scripts/verify-phase-57.ts`（npx tsx；自包含 assert + results[] + 退出码；SIBLING_ROOT = KAIS_HERMES_SKILLS_PATH ?? '/data/workspace/kais-hermes-skills'）——**Part 1 taxonomy 三方断言**（57-08 将加 Part 2 探活组）：
   - S 解析门：GATE_CATALOG import + gates.yaml 只读（js-yaml；解析 0 条 FAIL——脆弱即信号注释照抄 55）。
   - T1 集合等价：taxonomy id 集 ≡ PHASE_REGISTRY khsPrefix 集（双向 diff 输出）。
   - T2 顺序：taxonomy order 严格递增且与注册表 sortKey 升序同序（逐位比对 code）。
   - T3 门一致性（三方核心）：taxonomy review_gate 非空 ⇔ requires_review true；非空值 ∈ GATE_CATALOG derivedGateId 集 **且** ∈ gates.yaml 派生 id 集；有门 phase 的 gate 归属正确（review_gate 前缀 = 自身 khsPrefix）。
   - T4 无门 9 前缀清单恰为 review_gate 空（p035/p08/p09/p09b/p10/p12a/p12b/p14/p15）。
   - T5 运行时链：import defaultSkill 常量 → 每条 id 在常量中 requires_review 与注册表有门性一致（phase-complete.ts 暗礁反向断言，Pitfall 4）。
   - F forced-fail：内存中把一条 taxonomy review_gate 改坏 → 断言组必须红（能失败证明，50 先例）。
2. supersede：
   - verify-phase-30.ts：expectedPhaseIds 换 22 khsPrefix 清单（`import` 或字面量清单 + 注释指向 phaseRegistry 为源）；length===12 → 22；端点断言 420-422 同步 22。
   - verify-phase-31.ts：OLD_* 快照块原样保留（历史文档）；其「taxonomy 与 OLD_* 等价」断言段改为**跳过 + 指针注释**：「superseded by verify:phase-57 T1-T4（22-phase 对齐；OLD_* 是 pre-31 历史快照）」——不删快照不改其值。
   - 两脚本头部各加一行 supersede 注记（日期 + 指向）。
3. package.json scripts 注册 `"verify:phase-57": "npx tsx scripts/verify-phase-57.ts"`（verify:phase-55 相邻）。
4. 跑全链回归：verify:phase-28/33/30/31/57 五绿。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npm run verify:phase-57 && npx tsx scripts/verify-phase-30.ts && npx tsx scripts/verify-phase-31.ts && npx tsx scripts/verify-phase-33.ts && npx tsx scripts/verify-phase-28.ts && KAIS_HERMES_SKILLS_PATH=/tmp/nonexistent npm run verify:phase-57 >/dev/null 2>&1; [ $? -ne 0 ] || exit 1; echo TAXONOMY-GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - verify:phase-57 绿且输出含 T1-T5 + forced-fail 各段；env 指向不存在路径退出非 0（文件缺失门）。
    - verify-phase-30/31/33/28 四脚本全绿（supersede 后无红）。
    - 31 的 OLD_* 快照值未被修改（`git diff scripts/verify-phase-31.ts` 中 OLD_ 常量块零改动行）。
    - `grep -n "verify:phase-57" package.json` 命中 1 行。
  </acceptance_criteria>
  <done>三方 drift 断言上线（taxonomy ↔ 注册表 ↔ gates 快照 ↔ khs gates.yaml 四链对齐）——成功标准 4 全句达成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 契约脚本 → khs 仓库文件系统 | 只读 js-yaml 解析 gates.yaml |
| o_skillRegistry 表写（行升级） | 本仓 DB 一次性幂等升级 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-07a | Tampering | 行升级覆盖他 skill 行 | mitigate | UPDATE 限定 skill_id='movie-v1' 且条件（phase_taxonomy 数 ≠ 22）幂等；事务内；升级前 console 一行审计 |
| T-57-07b | Repudiation | requires_review 静默丢失 | mitigate | T3/T5 双向断言 + forced-fail（改一条即红） |
| T-57-07c | Tampering | 契约脚本 exec khs 代码 | accept | 只 js-yaml/regex 解析，无 spawn（55 同纪律；grep 断言可选加） |
</threat_model>

<verification>
- Task 1 verify-phase-28；Task 2 verify-phase-33 + 常量/端点断言；Task 3 五脚本全绿 + forced-fail + env 坏路径。
- khs 不被修改：`git -C /data/workspace/kais-hermes-skills status --porcelain` 无本 phase 新增变更。
</verification>

<success_criteria>
- taxonomy 22 条/13 真实 gate 标注，registry 加载后 phase 查询与画布 zone 词汇一致（T1 集合等价即证）。
- drift 断言可红可绿（forced-fail + env 门）；旧断言 supersede 显式化。
</success_criteria>

<output>
Create `.planning/phases/57-portal-delivery-pages/57-07-SUMMARY.md` when done
</output>
