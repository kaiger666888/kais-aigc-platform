---
phase: 57-portal-delivery-pages
plan: 08
type: execute
wave: 4
depends_on: ["57-03", "57-04", "57-05", "57-06", "57-07"]
files_modified:
  - scripts/verify-phase-57.ts
  - packages/portal/test/probe/portal-probe.mjs
  - packages/portal/package.json
autonomous: true
requirements: [PORTAL-01, PORTAL-02, PORTAL-03, PORTAL-04]
must_haves:
  truths:
    - "verify-phase-57 增 Part 2 探活聚合组：门户三路由 + /canvas 302 + 注入两岛 + navbar 产物 + 交付页壳 + Toonflow 共存（/ 与 /api/project/getProject 不变）——单命令收口四需求"
    - "portal-probe.mjs headless 探针（playwright，真实 10588）：/portal 渲染项目分节或空态、navbar 四岛 HTML 各含 kap-navbar、/deliver/:ep 渲染 hero 或无成片空态、深链 round-trip（/canvas?focus → 画布节点选中）"
    - "全部断言确定性：数据相关断言带存在性守卫（库空走空态分支断言，不跳过）"
  artifacts:
    - path: "scripts/verify-phase-57.ts（Part 2）"
      provides: "phase 57 最终门禁——verify:phase-57 单命令 = taxonomy 三方断言 + 探活聚合"
    - path: "packages/portal/test/probe/portal-probe.mjs"
      provides: "headless 真后端探针（canvas-real-screenshot.mjs 先例式）——回归可重放"
  key_links:
    - from: "scripts/verify-phase-57.ts"
      to: "http://localhost:10588（dev server）"
      via: "fetch 探活（/portal /deliver/1 /toonflow /canvas 302 /story-map /director-desk /infinite-canvas /api/canvas/projects /api/v1/skills/phases）"
      pattern: "localhost:10588"
---

<objective>
Phase 57 收口门禁（goal-backward 终验）：把四需求的成功标准变成可重放的单命令断言组。

Purpose: 执行器各 plan 的 verify 是局部的；本 plan 把 ROADMAP 成功标准 1-4 逐条落成聚合断言（含 headless 渲染级），供 verify-phase 与后续里程碑审计复用。
Output: verify-phase-57 Part 2 + portal-probe.mjs。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/57-portal-delivery-pages/57-CONTEXT.md
@.planning/phases/57-portal-delivery-pages/57-RESEARCH.md

<interfaces>
<!-- 聚合对象（全部为本 phase 已落地产物） -->
57-02: /portal /deliver/:ep /toonflow 挂载 + /canvas 302 + data/assets/kap-nav.* + projects.phases
57-03: /infinite-canvas kap-navbar + focus/zone 消费（e2e phase57-deeplink.mjs 三 case）
57-04: /story-map /director-desk 注入 <kap-navbar>
57-05/06: /deliver/:ep hero/清单/终审
57-07: verify-phase-57 Part 1（taxonomy 三方）
<!-- 探针先例 -->
packages/infinite-canvas/canvas-real-screenshot.mjs（真实后端 headless 探针模板——goto/waitForSelector/store 桥）
packages/infinite-canvas/test/e2e/helpers.mjs（__kaisCanvas 桥：getNodes 等）
<!-- 门禁先例 -->
scripts/verify-phase-50.ts（v2.1 终门：import-identity + spot-invariant 单脚本聚合模式）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: verify-phase-57 Part 2 探活聚合 + portal-probe headless 探针</name>
  <files>scripts/verify-phase-57.ts, packages/portal/test/probe/portal-probe.mjs</files>
  <read_first>
    - scripts/verify-phase-57.ts（57-07 产物——Part 1 结构与 harness；Part 2 追加在同 harness）
    - packages/infinite-canvas/canvas-real-screenshot.mjs（探针模板：chromium launch / goto / waitForSelector / console 收集）
    - .planning/ROADMAP.md §Phase 57（成功标准 1-4 逐句——断言映射源）
  </read_first>
  <action>
1. `scripts/verify-phase-57.ts` 追加 Part 2（HTTP 探活组，dev server 10588 必须在跑——开头 health 门 `GET /health`）：
   - P1 门户三路由：/portal、/deliver/1、/toonflow 均 200 且含「制片门户」。
   - P2 深链契约：/canvas?project=3&ep=1&focus=n-x&zone=p11b → 302 Location 精确形态（57-02 同式）。
   - P3 四岛共脸：/infinite-canvas/、/story-map/、/director-desk/、/portal/ 四 HTML 各含 kap-navbar（toonflow 经 /toonflow 页 + iframe src="/" 存在性）。
   - P4 共存：`/` 首字节仍 Toonflow（title 或体积特征）；POST /api/project/getProject 200。
   - P5 交付面：POST /api/canvas/projects 响应含 episodes[].phases；GET /api/v1/skills/phases 22 条。
   - 每条独立 assert 进 harness；服务不可达 = FAIL（不跳过）。
2. `packages/portal/test/probe/portal-probe.mjs`（playwright chromium，真实 10588）：
   - probe A 门户首页：goto /portal → navbar 可见（kap-navbar shadow-free 选择器 `[data-active="portal"], kap-navbar`）；项目分节或空态文案二择一断言（存在性守卫：先 fetch /api/canvas/projects 判有无数据再选分支——两分支都必须有断言文案）。
   - probe B 交付页：取真实 ep（同守卫）→ goto /deliver/{ep} → 断言三区块标题（成片/交付清单/终审）渲染；有 p13 数据再断言 `<video` src 非空，否则断言无成片空态文案。
   - probe C 深链 round-trip：goto /canvas?project&ep&focus=<load-v2 取真实节点 id> → 轮询断言节点 selected/detail（57-03 e2e 同口径，探针复跑）。
   - 退出码聚合；输出人读摘要（供 SUMMARY 粘贴）。
3. package.json（packages/portal）scripts 加 `"probe": "node test/probe/portal-probe.mjs"`；根仓 README 不动。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && curl -sf http://localhost:10588/health >/dev/null && npm run verify:phase-57 && cd packages/portal && node test/probe/portal-probe.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:phase-57` 单命令绿（Part 1 T1-T5/F + Part 2 P1-P5 全段输出）。
    - portal-probe 三 probe 全过（数据守卫分支有日志明示走了哪支）。
    - 探针对 404 路由可红（手动改坏一个 URL 验证失败路径——SUMMARY 记录）。
  </acceptance_criteria>
  <done>四需求成功标准全部进单命令门禁——Phase 57 verify 面收口。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 探针/探活 → 本仓 dev server | 只读 GET/既有 POST（projects） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-08a | Tampering | 探针写状态 | mitigate | 只读断言；唯一 POST 是既有 projects 查询 |
</threat_model>

<verification>
- Task 1 automated verify（verify:phase-57 + probe 双绿）。
- 人工：按 57-HUMAN-UAT 口径走一遍门户→画布→交付→放行动线（后续 verify-work 素材）。
</verification>

<success_criteria>
- ROADMAP 成功标准 1-4 每句至少一条聚合断言覆盖（映射表进 SUMMARY）。
- 门禁可红可绿、可重放。
</success_criteria>

<output>
Create `.planning/phases/57-portal-delivery-pages/57-08-SUMMARY.md` when done
</output>
