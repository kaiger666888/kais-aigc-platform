---
phase: 57-portal-delivery-pages
plan: 05
type: execute
wave: 2
depends_on: ["57-02"]
files_modified:
  - packages/portal/src/pages/DeliveryPage.tsx
  - packages/portal/src/lib/delivery.ts
  - packages/portal/src/components/PipelineRibbon.tsx
  - packages/portal/src/services/portalApi.ts
  - packages/portal/src/__tests__/delivery.test.ts
autonomous: true
requirements: [PORTAL-03]
must_haves:
  truths:
    - "D-09/D-12: /deliver/:ep 渲染成片 hero——master.mp4 经 resolveMediaUrl 喂 <video controls>（/oss 与 /local-file 原生 Range，零新流播代码），页面面向收片人：hero 主角、清单/管线带为辅"
    - "D-11: 交付清单 = load-v2 图内 p13（phaseIndex 16）节点透传（canvas_sync → save-v2 契约面）——不直扫文件系统、不手工配置、无新后端（U-10：projects 反查 projectId + load-v2 + gate-state 三既有端点组装）"
    - "A4: master 判定纯函数 pickMaster = p13 节点中 canvasType=video 且名/路径含 master（或唯一 video 兜底）；清单三型徽章 成片/交付包/质检报告（对齐 p13 OUTPUT_SLOTS 词汇，U-12）"
    - "管线带 full 档上线：22 段 + gate 四态点（gate-state 数据）+ 点击段 zone 深链（签名元素交付页形态）"
    - "终审卡本 plan 只出状态显示面（四态行 + redline 脚注）；操作条/理由框在 57-06"
  artifacts:
    - path: "packages/portal/src/lib/delivery.ts"
      provides: "交付页纯函数：pickMaster / classifyDeliveryNodes / resolveProjectId"
      exports: ["pickMaster", "classifyDeliveryNodes", "resolveProjectId", "DeliveryItem"]
    - path: "packages/portal/src/pages/DeliveryPage.tsx"
      provides: "交付页版面（hero + 交付清单 + 管线带 full + 终审卡状态面）"
  key_links:
    - from: "packages/portal/src/pages/DeliveryPage.tsx"
      to: "packages/infinite-canvas/src/services/canvasApi.ts"
      via: "import loadCanvasGraph/fetchGateState/fetchProjects（零重写）"
      pattern: "canvasApi"
    - from: "packages/portal/src/lib/delivery.ts"
      to: "packages/infinite-canvas/src/utils/mediaUrl.ts"
      via: "import resolveMediaUrl（D-12 与 53 变体墙同链）"
      pattern: "resolveMediaUrl"
    - from: "packages/portal/src/components/PipelineRibbon.tsx"
      to: "src/lib/gateCatalog.ts"
      via: "GATE_PIPELINE_ORDER/deriveGateId/红线条目过滤（gate 点词汇与红线下行 54 U-06）"
      pattern: "GATE_CATALOG"
---

<objective>
交付页数据面与版面（PORTAL-03 成功标准 3 前半）：/deliver/:ep 组装三既有端点渲染 master.mp4 播放、交付清单、管线带 full、终审卡状态显示。

Purpose: 22-phase 终点当前无 UI——本 plan 给管线情感终点落 UI 骨肉；终审操作在 57-06 接 gate-ops。
Output: delivery.ts 纯函数 + DeliveryPage 版面 + PipelineRibbon full 档。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/57-portal-delivery-pages/57-CONTEXT.md
@.planning/phases/57-portal-delivery-pages/57-UI-SPEC.md
@.planning/phases/57-portal-delivery-pages/57-RESEARCH.md
@.planning/phases/57-portal-delivery-pages/57-PATTERNS.md

<interfaces>
<!-- 数据端点（全既有，零新建后端） -->
POST /api/canvas/projects（src/routes/canvas/projects.ts:23-89；57-02 后 episodes 含 phases 直方图——反查 projectId 用 episodes[].id）
POST /api/canvas/v2/load-v2（src/routes/canvas/v2/load-v2.ts:43-91；body {projectId, episodesId}；返回全图节点含 phaseName/data.phaseIndex/data.filePath/isWinner——canvasApi.ts:349 loadCanvasGraph）
GET /api/canvas/v2/gate-state?projectId&episodesId（src/routes/canvas/v2/gate-state.ts:18-59；GateStatePayload gates[] 四态，gateStateService.ts:47-54；canvasApi.ts:510-539 fetchGateState）
<!-- p13 词汇与工件 -->
khs pipeline/phases/p13_delivery.py:54-55（OUTPUT_SLOTS = master-mp4 / delivery-package / master-qc）
packages/infinite-canvas/src/constants/phaseRegistry.ts:76（P13 条目 phaseIndex 16、canvasType video、assetType delivery）
src/lib/assetTypes.ts:36（type 'delivery' 词汇）；packages/infinite-canvas/src/components/assetManager/assetManagerData.ts:381-382（master_mp4/delivery_package subtype 词汇先例）
import-from-dir.ts:1068-1082（p13 节点 data.filePath 已 fsToOssUrl OSS 化——resolveMediaUrl 可解析）
<!-- 媒体链 -->
packages/infinite-canvas/src/utils/mediaUrl.ts:89-103（resolveMediaUrl：http/data 直通、/oss 前缀、绝对 FS → /oss 映射、兜底 local-file）
src/app.ts:71/83/166（/oss acceptRanges、fallback static acceptRanges、/local-file sendFile——Range 全原生）
<!-- gate 词汇 -->
src/lib/gateCatalog.ts:104-143（GATE_CATALOG 16 条 / GATE_DISPLAY_NAMES「p13-gate」=成片交付 / GATE_PIPELINE_ORDER）、:59-61（isRedlineKey——红线条目过滤）
<!-- 设计契约 -->
.planning/phases/57-portal-delivery-pages/57-UI-SPEC.md §Component Inventory P-3（版式图）、§Signature Element（full 档）、§Copywriting Contract（交付页文案表）、§Color、§State Matrix、§Motion Contract（进场编排）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: delivery.ts 纯函数（pickMaster / 清单分类 / projectId 反查）+ 测试</name>
  <files>packages/portal/src/lib/delivery.ts, packages/portal/src/__tests__/delivery.test.ts</files>
  <read_first>
    - packages/infinite-canvas/src/utils/mediaUrl.ts:33-103（filePath 形态与解析规则——fixture 造值依据）
    - src/routes/canvas/v2/load-v2.ts 响应节点形状（graph nodes data 字段：phaseIndex/filePath/output_key）
    - packages/infinite-canvas/src/components/assetManager/assetManagerData.ts:589-600,1355-1370（master/package 判定启发式先例——A4 规则同源）
    - packages/infinite-canvas/src/constants/phaseRegistry.ts（P13 phaseIndex=16 常量来源——禁止字面量 16 内联，import P13 条目）
  </read_first>
  <action>
先写测试后实现（vitest，fixture 图节点工厂）：
1. `resolveProjectId(projects, ep)`：episodes[].id 反查（Q5——一集属一项目；未命中 → null）。
2. `classifyDeliveryNodes(p13Nodes): {master?: DeliveryNode; items: DeliveryItem[]}`：
   - master 判定（A4）：canvasType=video（或 assetType=delivery 且 data.filePath 以 .mp4 结尾）+ 名/路径 /master/i 命中 → 优先；无命中且唯一 video → 兜底；多个无 master 标记 → 取 filePath 含 mp4 的第一个（确定性排序：名称字典序）。
   - items 三型（U-12）：成片（master 节点）/ 交付包（名/路径 /package|包/i）/ 质检报告（其余，master-qc 词汇）；每项 {id, label, size?, filePath, kind}。
   - 空 p13Nodes → {master: undefined, items: []}（空态判定输入）。
3. `masterSrc(master)` = resolveMediaUrl(master.filePath)（re-export 薄封）。
4. 测试覆盖：master 命中/唯一 video 兜底/多 video 无标记确定性/空集四分支 + 三型分类 + resolveProjectId 命中与未命中。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/portal && npx vitest run src/__tests__/delivery.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - vitest 全绿（≥8 case：三分支 master + 分类 + 反查）。
    - delivery.ts 无字面量 phaseIndex（`grep -c "=== 16\|== 16" src/lib/delivery.ts` === 0——P13 常量经注册表条目）。
    - 无 fs/network import（纯函数：`grep -c "node:fs\|fetch(" src/lib/delivery.ts` === 0）。
  </acceptance_criteria>
  <done>交付页数据判定层纯函数 + 护栏。</done>
</task>

<task type="auto">
  <name>Task 2: DeliveryPage 版面（hero + 交付清单 + 管线带 full + 终审卡状态面）</name>
  <files>packages/portal/src/pages/DeliveryPage.tsx, packages/portal/src/components/PipelineRibbon.tsx, packages/portal/src/services/portalApi.ts</files>
  <read_first>
    - .planning/phases/57-portal-delivery-pages/57-UI-SPEC.md §Component Inventory P-3（版式 ASCII 图——hero 左 2/3 + 终审卡右 1/3 sticky、窄屏堆叠）+ §Copywriting Contract（交付页全部文案）+ §Motion Contract（hero→管线带→清单 40ms 递进一次编排）+ §State Matrix
    - packages/infinite-canvas/src/components/variants/VariantWall.tsx（53 剧场：主角在上/信息在下的层级与 video 用法先例）
    - packages/infinite-canvas/src/services/canvasApi.ts:510-539（fetchGateState 返回 payload.gates 形状）
    - src/lib/gateCatalog.ts:125-143（p13-gate 显示名）+ :59-61（红线条目）
    - packages/portal/src/lib/ribbon.ts（57-02 Task 3——full 档扩展基座）
  </read_first>
  <action>
1. `portalApi.ts` 扩展：`loadDelivery(ep)` —— fetchProjects → resolveProjectId → 并行 loadCanvasGraph + fetchGateState（Promise.all；gate-state 失败不阻塞版面——终审卡走降级态）。返回 {project, graph, gateState|null}。
2. `PipelineRibbon.tsx` 扩 full 档（props {variant:'micro'|'full', gates?}: full 时段下挂 10px 四态点——gate-state gates[] 按 GATE_PIPELINE_ORDER 映射段（gateCatalog deriveGateId 前缀 ↔ 注册表 khsPrefix；红线条目 platformInvisible 不加点，54 U-06）；段 hover tooltip 加 门名·四态词；段点击 → `ribbonHref(project, ep, khsPrefix)` zone 深链。micro 档行为不变（57-02 回归）。
3. `DeliveryPage.tsx` 版面（UI-SPEC P-3 版式逐条落地）：
   - 头行：ep 号 mono + 项目名 + [去画布]（`/canvas?project&ep&zone=p13`）。
   - hero：`<video controls preload="metadata" src={masterSrc}>` 16:9、壳 `--cv-bg-panel` + 发丝边；无 master → 无成片空态（copy 表 + [去画布看 P13]）。
   - 管线带 full（hero 下、全内容列宽）；清单空 → 「交付清单为空」文案表。
   - 终审卡（右 1/3 sticky）：本 plan 状态显示面——p13-gate 条目四态行（10px 点 + 11px/600 标签 + `p13-gate` mono 标注，54 色映射）+ 最新 note 截断 + redline 脚注灰字（「p13 红线子门为本地自动扫描，不进入人工决策。」）+ gateState null → 降级横幅文案（54 原文）；**动作条区域留 57-06 位**（display:none 占位注释）。
   - 进场编排一次：hero→管线带→清单 `--cv-d-panel` 240ms + `--cv-d-ancestor-step` 40ms 递进（reduced-motion 静止）。
   - hero 加载失败（video error 事件）→ hero 内联提示 + [直开文件]（resolveMediaUrl 新窗）。
4. **设计检查步（frontend-design 纪律，交付页是本期签名 UI 的一半）**：逐条对照 UI-SPEC——§Color accent 只出现在播放键/已播段/焦点环（终审放行按钮 57-06 才有）；§Typography mono 规则（ep 号/P0X/gate id/尺寸）；§Spacing hero 2xl=48px 上下；§Signature full 档规格（24px 高/sub 60%/gate 10px 点/点击深链）；§Do-Not-Regress 8（无场景维度色）。自查记录进 SUMMARY。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/portal && npx vitest run && npx tsc -p tsconfig.json --noEmit && cd ../.. && bash scripts/deploy-portal.sh >/dev/null 2>&1 && EP=$(curl -sf -X POST http://localhost:10588/api/canvas/projects -H 'content-type: application/json' -d '{}' | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const ps=j.data?.projects||j.projects||(Array.isArray(j.data)?j.data:[]);for(const p of ps){if((p.episodes||[]).length){process.stdout.write(String(p.episodes[0].id));break}}})") && [ -n "$EP" ] && curl -s http://localhost:10588/deliver/$EP | grep -q "制片门户" && echo DELIVER-OK</automated>
  </verify>
  <acceptance_criteria>
    - vitest 全绿（ribbon/delivery 两套）+ tsc 零错。
    - 有数据的库上 `/deliver/{真实ep}` 返回门户壳（后续渲染断言见 57-08 headless）。
    - DeliveryPage 无内联 gate/phase 词表：`grep -cE "成片交付|P13 · 交付" packages/portal/src/pages/DeliveryPage.tsx` === 0（词汇经 gateCatalog/phaseRegistry import——显示名「成片交付」经 GATE_DISPLAY_NAMES）。
    - `<video` 元素 src 链接经 resolveMediaUrl（`grep -c "masterSrc\|resolveMediaUrl" DeliveryPage.tsx` ≥ 2；无裸 filePath 直拼）。
    - 无 fs 扫描/新后端调用（`grep -c "readdir\|/api/delivery" packages/portal/src` 全域 === 0）。
  </acceptance_criteria>
  <done>交付页版面 + 数据面可访问；master.mp4 可播（有 p13 数据的集）；终审状态可见——成功标准 3 前两句达成（播放 + 清单展示）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 → /api/canvas/v2/*（既有端点） | 只读消费（gate-ops 写路径在 57-06） |
| 媒体 URL 解析 → /oss、/local-file | 既有 allowlist 面（app.ts:123-130），本期零新增 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-05a | Info Disclosure | /deliver/:ep 无鉴权展示成片 | accept | 与既有岛/画布同层（V6 全开现状）；不新增数据面——全部经既有端点；终审操作面 57-06 沿用 gate-ops 既有校验 |
| T-57-05b | Tampering | filePath 注入 video src | mitigate | resolveMediaUrl 白名单解析（http/data/oss/FS 映射四分支）；无 HTML 注入面（React 转义） |
</threat_model>

<verification>
- Task 1 vitest；Task 2 vitest + tsc + 部署探活。
- 人工：有 p13 数据的集浏览器验收 hero 播放/Range 拖动（D-12）+ 管线带点击跳画布 zone。
</verification>

<success_criteria>
- /deliver/:ep 三区块（成片/交付清单/终审状态）+ 管线带 full 全部渲染；零新建后端、零 fs 扫描（D-11/U-10）。
- master.mp4 经 resolveMediaUrl + Range 可流播（成功标准 3 前半）。
</success_criteria>

<output>
Create `.planning/phases/57-portal-delivery-pages/57-05-SUMMARY.md` when done
</output>
