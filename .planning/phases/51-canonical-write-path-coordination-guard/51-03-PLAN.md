---
phase: 51-canonical-write-path-coordination-guard
plan: 03
type: execute
wave: 3
# 51-02 亦改 canvasStore.ts,文件级顺序显式化(plan-checker WARNING 修复)
depends_on: ["51-01", "51-02"]
files_modified:
  - packages/infinite-canvas/src/components/CanvasContextMenu.tsx
  - packages/infinite-canvas/src/store/canvasStore.ts
  - packages/infinite-canvas/src/store/__tests__/deleteNode.test.ts
autonomous: true
requirements: [WRITE-02]
must_haves:
  truths:
    - "CanvasContextMenu 不再直接 import canvasApi 的 approveNode/rejectNode;右键审核改调 store.approveNode/rejectNode(canonical optimistic + 回滚)"
    - "删除操作带画布内轻量确认 UI(非浏览器原生 confirm())"
    - "store.deleteNode 经 applyGraphTransform 移除节点 + 触及 links + variantGroups 成员清理,随后走统一 saveCanvasGraph(save-v2)持久化;失败回滚 prevGraph + error toast"
    - "不新增专用 delete 端点(CONTEXT 锁定)"
    - "本文件内 legacy 类型使用清理:add-node 处理器删除、SCAIL2 块改 plain object"
  artifacts:
    - path: "packages/infinite-canvas/src/components/CanvasContextMenu.tsx"
      provides: "右键菜单 canonical 化(approve/reject/delete)"
      contains: "store.approveNode"
    - path: "packages/infinite-canvas/src/store/canvasStore.ts"
      contains: "deleteNode"
    - path: "packages/infinite-canvas/src/store/__tests__/deleteNode.test.ts"
      provides: "deleteNode 图变换 + 持久化调用 + 失败回滚 vitest"
  key_links:
    - from: "packages/infinite-canvas/src/components/CanvasContextMenu.tsx"
      to: "packages/infinite-canvas/src/store/canvasStore.ts"
      via: "handleApprove/handleReject → store.approveNode/rejectNode;handleDelete → 确认 UI → store.deleteNode"
      pattern: "approveNode\\|rejectNode\\|deleteNode"
    - from: "packages/infinite-canvas/src/store/canvasStore.ts"
      to: "src/routes/canvas/v2/save-v2.ts"
      via: "deleteNode 内调 saveCanvasGraph(51-01 已切换 save-v2 全量替换)"
      pattern: "saveCanvasGraph"
---

<objective>
右键菜单是当前唯一绕行 canonical 的审核/删除写入点。本 plan 把 handleApprove/handleReject 改调既有 `store.approveNode/rejectNode`(canonical optimistic + 回滚已实现,此前仅 ReviewActionButtons 消费);handleDelete 增加画布内轻量确认 + 新 store.deleteNode(canonical 图变换 + save-v2 统一持久化,不新增 delete 端点);顺带清理本文件的 legacy 类型使用(为 51-04 的类型删除扫清消费者)。

"删除后不复活"的集成断言在 51-05 verify-phase-51 S2 落地;本 plan 交付 store 级单测 + source-shape 证据。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/51-canonical-write-path-coordination-guard/51-CONTEXT.md
@.planning/phases/51-canonical-write-path-coordination-guard/51-RESEARCH.md

<interfaces>
<!-- 已直读核实 -->

CanvasContextMenu.tsx 现状:
- L3:`import { executeNode, approveNode, rejectNode, requestNodeScore, orchestrateCanvas, saveCanvasGraph } from '../services/canvasApi'`
- L136 handleBatchExecute:51-01 已把 canvasToFlowGraph 切换为 serializeGraphToV2(本 plan 不重碰该逻辑)
- L153 handleApprove → canvasApi.approveNode(projectId, episodesId, nodeId);L170 handleReject → canvasApi.rejectNode(..., rejectReason.trim())——均绕过 canonical
- 驳回原因 textarea 是本文件既有轻量确认模式(删除确认 UI 复用此风格,CONTEXT 锁定不用原生 confirm())
- add-node 处理器消费 legacy 类型(ScriptNodeData/StoryboardNodeData/VideoNodeData/AudioNodeData);SCAIL2 块使用 legacy 类型注解

store 现状(canvasStore.ts):
- `approveNode`(L453)/`rejectNode`(L489):canonical optimistic + prevGraph 回滚 + error toast 已实现
- `applyGraphTransform`(L315);`rawDataByNodeId` 与 `graph` 可供 serializeGraphToV2(51-01)
- research 地雷 #12:store 内 approveNode/rejectNode 的 graph-null 旧路径在右键改线后无消费者,但 ReviewActionButtons 仍用 store action——保留旧路径无害,本 phase 不强行清理(最小改动)

deleteNode 建议语义(research Task 4):
1. applyGraphTransform:移除节点 + 所有 source/target 触及它的 links + variantGroups 成员清理(若节点是 winner 清 winnerNodeId;组清空则删组)
2. 走统一 saveCanvasGraph(save-v2 全量替换)持久化
3. 失败按 approveNode 范式回滚 prevGraph + showToast error
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: handleApprove/handleReject 改调 store.approveNode/rejectNode</name>
  <files>packages/infinite-canvas/src/components/CanvasContextMenu.tsx</files>
  <read_first>packages/infinite-canvas/src/components/CanvasContextMenu.tsx (L1-30 import 区、L140-185 handleApprove/handleReject 全文), packages/infinite-canvas/src/store/canvasStore.ts (approveNode L453-488、rejectNode L489-525 签名与回滚/toast 行为), packages/infinite-canvas/src/components/ReviewActionButtons.tsx (store action 消费范式)</read_first>
  <action>
handleApprove 改为调 `store.approveNode(nodeId)`;handleReject 改为调 `store.rejectNode(nodeId, rejectReason.trim())`(驳回原因 textarea 交互保留,仅换写入通道)。从 L3 import 中移除 approveNode/rejectNode 两个 canvasApi 符号(executeNode/requestNodeScore/orchestrateCanvas/saveCanvasGraph 若仍被本文件其他处理器使用则保留)。store 内乐观更新 + 回滚 + toast 已内置,菜单层不再自管 loading/错误提示(与 ReviewActionButtons 消费方式对齐)。不改 ReviewActionButtons。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx tsc -b --pretty 2>&1 | tail -3; grep -n "approveNode\|rejectNode" packages/infinite-canvas/src/components/CanvasContextMenu.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CanvasContextMenu.tsx 的 canvasApi import 行不再含 approveNode/rejectNode(精确符号,注意与 store action 同名区分:文件内对 approveNode/rejectNode 的调用必须来自 store hook 解构)
    - `grep -n "store.approveNode\|useCanvasStore" packages/infinite-canvas/src/components/CanvasContextMenu.tsx` 显示审核调用经 store
    - `cd packages/infinite-canvas && npx tsc -b` exit 0;`npm test` 全绿
  </acceptance_criteria>
  <done>右键审核走 canonical optimistic 通道,菜单层零自管回滚逻辑。</done>
</task>

<task type="auto">
  <name>Task 2: store.deleteNode + 删除确认 UI + save-v2 持久化 + vitest</name>
  <files>packages/infinite-canvas/src/store/canvasStore.ts, packages/infinite-canvas/src/store/__tests__/deleteNode.test.ts (new), packages/infinite-canvas/src/components/CanvasContextMenu.tsx</files>
  <read_first>packages/infinite-canvas/src/store/canvasStore.ts (approveNode L453 的 optimistic+回滚+持久化范式全文、applyGraphTransform、rawDataByNodeId), packages/infinite-canvas/src/v3/serialize.ts (51-01 serializeGraphToV2 签名), packages/flowgraph-v3/ts/src/types.ts (variantGroups/winnerNodeId 结构), packages/infinite-canvas/src/components/CanvasContextMenu.tsx (handleDelete 现状 + 驳回原因 textarea 确认模式), packages/infinite-canvas/src/store/__tests__/selectWinner.test.ts (mock canvasApi 范式)</read_first>
  <action>
1. canvasStore 新增 `deleteNode(nodeId: string): Promise<void>`(命名 Claude discretion):
   a. graph===null 或节点不存在 → 早退;
   b. 快照 prevGraph;applyGraphTransform 内移除该节点 + 所有 source/target 触及它的 links + variantGroups 清理(节点是组 winner 则清 winnerNodeId;组成员清空则删整组);
   c. 调 saveCanvasGraph(projectId, episodesId, serializeGraphToV2(newGraph, rawDataByNodeId)) 持久化(复用 51-01 通道,不新增端点);
   d. catch → set 回 prevGraph + showToast(err.message, 'error')(完全复刻 approveNode 回滚范式)。
2. 新建 __tests__/deleteNode.test.ts:mock canvasApi.saveCanvasGraph(沿用 selectWinner.test.ts 的 mock 范式),断言:
   a. 删除后 canonical graph 无该节点、无触及它的 link;
   b. 节点是 variantGroup winner 时 winnerNodeId 清空;组清空时组被删;
   c. saveCanvasGraph 被调用且 payload 经 FlowGraphV2 形状(节点集中无被删 id);
   d. mock reject 时 prevGraph 回滚 + error toast 入列。
3. CanvasContextMenu handleDelete:加内联确认 UI(复用本文件驳回原因 textarea 的轻量模式——点击"删除"先展开确认区,确认/取消按钮;不用浏览器原生 confirm(),CONTEXT 锁定);确认后调 `store.deleteNode(nodeId)`。持久化/回滚/toast 全在 store 内,菜单层不重复实现。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx vitest run src/store/__tests__/deleteNode.test.ts 2>&1 | tail -5 && npx tsc -b --pretty 2>&1 | tail -3; grep -c "confirm(" packages/infinite-canvas/src/components/CanvasContextMenu.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/infinite-canvas && npx vitest run src/store/__tests__/deleteNode.test.ts` exit 0,4 断言组全绿
    - `grep -c 'deleteNode' packages/infinite-canvas/src/store/canvasStore.ts` ≥ 1
    - CanvasContextMenu.tsx 中 handleDelete 路径含确认状态(useState 类)与 deleteNode 调用;`grep -c 'confirm(' packages/infinite-canvas/src/components/CanvasContextMenu.tsx` = 0(无原生 confirm)
    - 全仓库无新增 delete 路由:`grep -rn "canvas/delete\|/delete" src/routes/canvas/ --include='*.ts' | grep -vi 'save-v2\|v2/' ` 无新端点命中
    - `cd packages/infinite-canvas && npx tsc -b` exit 0;`npm test` 全绿
  </acceptance_criteria>
  <done>删除经确认 → canonical 图变换 → save-v2 持久化,失败回滚;store 单测四组断言锁死。</done>
</task>

<task type="auto">
  <name>Task 3: CanvasContextMenu legacy 类型使用清理(add-node 处理器 + SCAIL2 块)</name>
  <files>packages/infinite-canvas/src/components/CanvasContextMenu.tsx</files>
  <read_first>packages/infinite-canvas/src/components/CanvasContextMenu.tsx (全文,定位 add-node 处理器与 SCAIL2 块), packages/infinite-canvas/src/types/canvas.ts (ScriptNodeData/StoryboardNodeData/VideoNodeData/AudioNodeData 定义,本 plan 只读不改——类型本体删除归 51-04)</read_first>
  <action>
清理本文件对 legacy 类型(ScriptNodeData/StoryboardNodeData/VideoNodeData/AudioNodeData)的全部消费,为 51-04 删除类型本体扫清引用:
1. add-node 处理器(创建 Script/Video/Audio/Storyboard 旧类型节点的入口)整体删除——nodeTypes 已全部路由 AssetCardNode,旧节点类型不可创建;
2. SCAIL2 块:去掉 legacy 类型注解,改 plain object(保持运行时行为不变);
3. 删除后 `grep -n "ScriptNodeData\|StoryboardNodeData\|VideoNodeData\|AudioNodeData" packages/infinite-canvas/src/components/CanvasContextMenu.tsx` 应为 0 命中。
**文件划界:本 plan 独占 CanvasContextMenu.tsx;51-04(W2 并行)不动该文件。** 若 add-node UI 入口(menu 项)随之失去功能,一并移除对应菜单项,不留死按钮。
  </action>
  <verify>
    <automated>grep -n "ScriptNodeData\|StoryboardNodeData\|VideoNodeData\|AudioNodeData" packages/infinite-canvas/src/components/CanvasContextMenu.tsx | wc -l; cd packages/infinite-canvas && npx tsc -b --pretty 2>&1 | tail -3 && npm test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - CanvasContextMenu.tsx 中四个 legacy 类型名 0 命中
    - 无指向已删 add-node 处理器的菜单项残留(tsc 与人工 diff 双重确认)
    - `cd packages/infinite-canvas && npx tsc -b` exit 0;`npm test` 全绿
  </acceptance_criteria>
  <done>CanvasContextMenu 不再消费任何 legacy 节点类型,51-04 可安全删除类型本体。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户右键操作 → canonical graph → save-v2 | 删除是不可逆写操作(全量替换语义),误删/失败半截状态是主要风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-03-01 | Tampering | 误删节点 | mitigate | 画布内轻量确认 UI(CONTEXT 锁定,禁原生 confirm);source-shape 断言确认态存在 |
| T-51-03-02 | Tampering | 删除后持久化失败致前后端不一致 | mitigate | approveNode 同款 prevGraph 回滚 + error toast;单测 mock reject 锁死 |
| T-51-03-03 | Tampering | 删除残留 variantGroup 悬空引用(winner 指向不存在节点) | mitigate | 图变换含 winnerNodeId 清理 + 空组删除;单测断言 |
| T-51-03-04 | Elevation of Privilege | 新增 delete 端点扩大攻击面 | avoid | CONTEXT 锁定不新增端点,复用 save-v2 既有 zod 校验通道;acceptance grep 断言无新路由 |
| T-51-03-SC | Tampering | package installs | accept | 零新包 |

**结论:内部重构,无新增端点/攻击面;删除安全性由确认 UI + 回滚 + 单测覆盖。无 high 级威胁。**
</threat_model>

<verification>
- `cd packages/infinite-canvas && npm test` 全绿(deleteNode 新套件 + 既有无回归)
- `cd packages/infinite-canvas && npx tsc -b` exit 0
- CanvasContextMenu source-shape:无 canvasApi approveNode/rejectNode import;handleDelete 含确认态 + deleteNode;无原生 confirm()
- 无新增 delete 端点
</verification>

<success_criteria>
- WRITE-02 落地:右键审核/删除经 store.approveNode/rejectNode/deleteNode 落 canonical 路径;删除带确认且经 save-v2 持久化(重载不复活的集成断言由 51-05 S2 完成闭环)
- CanvasContextMenu legacy 类型消费清零,为 WRITE-04 类型删除扫清道路
</success_criteria>

<output>
Create `.planning/phases/51-canonical-write-path-coordination-guard/51-03-SUMMARY.md` when done. 记录:deleteNode 最终命名、确认 UI 实现要点、add-node 移除的菜单项清单。
</output>
