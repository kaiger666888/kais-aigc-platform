---
phase: 51-canonical-write-path-coordination-guard
plan: 04
type: execute
wave: 2
depends_on: ["51-01", "51-03"]
files_modified:
  - packages/infinite-canvas/src/components/nodes/ScriptNode.tsx
  - packages/infinite-canvas/src/components/nodes/VideoNode.tsx
  - packages/infinite-canvas/src/components/nodes/AudioNode.tsx
  - packages/infinite-canvas/src/components/nodes/StoryboardNode.tsx
  - packages/infinite-canvas/src/components/nodes/AssetNode.tsx
  - packages/infinite-canvas/src/components/VariantGroupDetail.tsx
  - packages/infinite-canvas/src/components/BranchPanel.tsx
  - packages/infinite-canvas/src/components/StructuredFieldPanel.tsx
  - packages/infinite-canvas/src/components/ScoreBadge.tsx
  - packages/infinite-canvas/src/components/VariantBadge.tsx
  - packages/infinite-canvas/src/components/FeedbackBadge.tsx
  - packages/infinite-canvas/src/utils/flowDataMapper.ts
  - packages/infinite-canvas/src/types/canvas.ts
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
  - packages/infinite-canvas/package.json
  - package-lock.json
autonomous: true
requirements: [WRITE-04]
must_haves:
  truths:
    - "12 个死代码文件(~3,200 行)全部不存在;flowDataMapper.ts 整体删除(含死导出 flowDataToCanvas/flowGraphToCanvas)"
    - "ScriptNodeData/StoryboardNodeData/VideoNodeData/AudioNodeData 四个 legacy 类型在活代码中 0 命中"
    - "badges/NodeBadges.tsx 与 badges/ScoreMiniBar.tsx 未被删除(C 层活组件,红线条款)"
    - "packages/infinite-canvas/package.json dependencies 含 @kais/flowgraph-v3(与仓库本地包声明方式一致);vite alias/tsconfig paths 保留"
    - "根 tsc --noEmit 与 packages/infinite-canvas tsc -b 双根干净;双包 vitest 全绿"
    - "本 plan 不修改 CanvasContextMenu.tsx(51-03 独占)"
  artifacts:
    - path: "packages/infinite-canvas/package.json"
      contains: "@kais/flowgraph-v3"
  key_links:
    - from: "packages/infinite-canvas/package.json"
      to: "packages/flowgraph-v3"
      via: "dependencies file: 声明(消除幽灵依赖;实际解析仍由 vite alias/tsconfig paths 承担)"
      pattern: "@kais/flowgraph-v3"
---

<objective>
死代码清除 + 依赖正名:删除 12 个已核实无引用的文件(4 个旧节点渲染器 + AssetNode + VariantGroupDetail + BranchPanel + StructuredFieldPanel + 3 个旧徽章 + flowDataMapper,约 3,200 行),随调用点清理删除 4 个 legacy 类型,并把 `@kais/flowgraph-v3` 声明进 packages/infinite-canvas dependencies 消除幽灵依赖。

**红线(地雷,CONTEXT/RESEARCH 双重锁定):`badges/NodeBadges.tsx` 与 `badges/ScoreMiniBar.tsx` 是活的 C 层组件(经 variants/registerCInteractions 注册、被 AssetCardNode 消费),绝对不可删。** BranchPanel 删除是 ROADMAP 决策 #6"先删后建"的前半——Phase 55 NAV-06 将在干净地基上重写,删除不留副本(git 历史即档案)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/51-canonical-write-path-coordination-guard/51-CONTEXT.md
@.planning/phases/51-canonical-write-path-coordination-guard/51-RESEARCH.md

<interfaces>
<!-- 死代码清单(CONTEXT 已核实无引用) -->
| 文件 | 行数 |
|---|---|
| nodes/ScriptNode.tsx | 258 |
| nodes/VideoNode.tsx | 294 |
| nodes/AudioNode.tsx | 239 |
| nodes/StoryboardNode.tsx | 218 |
| nodes/AssetNode.tsx | 270 |
| VariantGroupDetail.tsx | 722 |
| BranchPanel.tsx | 235 |
| StructuredFieldPanel.tsx | 329 |
| ScoreBadge.tsx | 74 |
| VariantBadge.tsx | 117 |
| FeedbackBadge.tsx | 151 |
| utils/flowDataMapper.ts | 410 |

已核实事实:
- nodeTypes 注册(FlowCanvas L71-91)已全部路由 AssetCardNode,4 旧渲染器安全可删
- flowDataMapper 消费方已在 51-01(FlowCanvas/CanvasContextMenu 调用点)全部清除;死导出 flowDataToCanvas/flowGraphToCanvas 一并随文件删除(CONTEXT 锁定整文件删除)
- legacy 类型消费方:CanvasContextMenu add-node(51-03 已清)+ flowDataMapper(本 plan 删)——清完后 types/canvas.ts 中类型本体无消费者
- packages/flowgraph-v3 内层 ts/package.json 名为 "flowgraph-v3"(与外层 @kais/flowgraph-v3 不一致),声明依赖时注意核对外层包名
- vite.config.ts/tsconfig paths alias 当前承担 flowgraph-v3 解析,声明依赖后**保留** alias(地雷 #10)
- src/routes/canvas/static/assets/*.js.map 中 7 处 canvasToFlowGraph 是构建产物,不算引用,grep 断言须排除 static/** 与 data/web/**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除 12 个死代码文件 + FlowCanvas 死 import 清理</name>
  <files>上表 12 文件(delete), packages/infinite-canvas/src/components/FlowCanvas.tsx (死 import 清理)</files>
  <read_first>packages/infinite-canvas/src/components/FlowCanvas.tsx (L40-95 import 区 + nodeTypes 注册,确认无指向 12 文件的引用残留), packages/infinite-canvas/src/components/nodes/AssetCardNode.tsx (确认 NodeBadges/ScoreMiniBar 活引用,红线核对), packages/infinite-canvas/src/components/badges/NodeBadges.tsx, packages/infinite-canvas/src/components/badges/ScoreMiniBar.tsx (确认存在且本任务不触碰)</read_first>
  <action>
1. 删除前先全量复核(防御性,清单虽经 CONTEXT 核实):对 12 个文件逐一 `grep -rn "<basename 去扩展名>" packages/infinite-canvas/src --include='*.ts*'` 确认 0 活引用(import/注册/dynamic import);若发现任何活引用,停止并在 SUMMARY 记录,改为跳过该文件。
2. 用 `git rm`(或 rm,交由 git 追踪)删除 12 个文件;**不可删 badges/NodeBadges.tsx 与 badges/ScoreMiniBar.tsx**(红线)。
3. FlowCanvas.tsx:清理指向已删文件/flowDataMapper 的死 import 与死注册项(nodeTypes 中若残留旧渲染器映射行一并删除——注意 L71-91 已全路由 AssetCardNode,预期仅剩 import 清理)。**除死 import/死注册外不动 FlowCanvas 任何逻辑**(socket 区归 51-02、保存区归 51-01,均已落地)。
4. 不触碰 CanvasContextMenu.tsx(51-03 独占)。
  </action>
  <verify>
    <automated>for f in ScriptNode VideoNode AudioNode StoryboardNode AssetNode VariantGroupDetail BranchPanel StructuredFieldPanel ScoreBadge VariantBadge FeedbackBadge flowDataMapper; do find packages/infinite-canvas/src -name "$f.*" | grep -v __tests__; done | wc -l; ls packages/infinite-canvas/src/components/badges/NodeBadges.tsx packages/infinite-canvas/src/components/badges/ScoreMiniBar.tsx; cd packages/infinite-canvas && npx tsc -b --pretty 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 12 个文件全部不存在(find 0 命中)
    - `ls packages/infinite-canvas/src/components/badges/NodeBadges.tsx packages/infinite-canvas/src/components/badges/ScoreMiniBar.tsx` 两文件均存在(红线)
    - `grep -rn "flowDataMapper\|VariantGroupDetail\|BranchPanel\|StructuredFieldPanel" packages/infinite-canvas/src --include='*.ts*'` 0 命中(排除 __tests__ 中同步清理后的引用)
    - `cd packages/infinite-canvas && npx tsc -b` exit 0;`npm test` 全绿(若有引用已删组件的测试文件,一并删除并在 SUMMARY 记录)
  </acceptance_criteria>
  <done>~3,200 行死代码清除,红线活组件完好,编译与测试绿。</done>
</task>

<task type="auto">
  <name>Task 2: legacy 类型本体删除(types/canvas.ts)</name>
  <files>packages/infinite-canvas/src/types/canvas.ts</files>
  <read_first>packages/infinite-canvas/src/types/canvas.ts (ScriptNodeData/StoryboardNodeData/VideoNodeData/AudioNodeData 定义与其余活类型), 51-03 的 CanvasContextMenu diff(消费方已清零的实证)</read_first>
  <action>
删除 types/canvas.ts 中 ScriptNodeData/StoryboardNodeData/VideoNodeData/AudioNodeData 四个 legacy 类型定义及仅服务它们的过期注释;保留其余活类型不动。删除前 `grep -rn "ScriptNodeData\|StoryboardNodeData\|VideoNodeData\|AudioNodeData" packages/infinite-canvas/src src/ --include='*.ts*'` 确认消费方仅剩 types/canvas.ts 自身(51-03 已清 CanvasContextMenu、Task 1 已删 flowDataMapper);若有残留消费方,先清理再删类型。
  </action>
  <verify>
    <automated>grep -rn "ScriptNodeData\|StoryboardNodeData\|VideoNodeData\|AudioNodeData" packages/infinite-canvas/src src/ --include='*.ts*' | wc -l; cd packages/infinite-canvas && npx tsc -b --pretty 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 四个 legacy 类型名在 packages/infinite-canvas/src 与 src/ 合计 0 命中
    - types/canvas.ts 其余导出类型保持(tsc 证明无连带破坏)
    - `cd packages/infinite-canvas && npx tsc -b` exit 0;`npm test` 全绿
  </acceptance_criteria>
  <done>legacy 类型本体删除,类型层死代码清零。</done>
</task>

<task type="auto">
  <name>Task 3: @kais/flowgraph-v3 依赖声明 + 全量构建验证</name>
  <files>packages/infinite-canvas/package.json, package-lock.json (npm install 更新)</files>
  <read_first>packages/infinite-canvas/package.json (现有 dependencies 与本地包声明惯例), packages/flowgraph-v3/package.json (外层包名 @kais/flowgraph-v3 与版本), packages/flowgraph-v3/ts/package.json (内层名 flowgraph-v3——声明时用外层名,勿混), packages/infinite-canvas/vite.config.ts (alias 区,确认保留)</read_first>
  <action>
1. packages/infinite-canvas/package.json dependencies 增加 `"@kais/flowgraph-v3": "file:../flowgraph-v3"`(与仓库现有本地包声明方式一致;CONTEXT 锁定保留 vite alias/tsconfig paths——alias 仍承担实际解析,依赖声明消除幽灵依赖语义)。
2. 根目录 `npm install` 更新 package-lock(地雷 #10:加 file: 依赖后必须重跑 install)。
3. 全量验证:根 `npx tsc --noEmit`、`cd packages/infinite-canvas && npx tsc -b`、`cd packages/infinite-canvas && npm test`、`cd packages/flowgraph-v3/ts && npm test` 四项全绿。
4. `cd packages/infinite-canvas && npm run build`(tsc -b && vite build)确认 vite 构建在声明依赖后仍通过(alias 保留,解析路径不变)。
  </action>
  <verify>
    <automated>grep -c '"@kais/flowgraph-v3"' packages/infinite-canvas/package.json; npx tsc --noEmit 2>&1 | tail -3; cd packages/infinite-canvas && npx tsc -b --pretty 2>&1 | tail -3 && npm test 2>&1 | tail -3 && npm run build 2>&1 | tail -3; cd ../flowgraph-v3/ts && npm test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"@kais/flowgraph-v3"' packages/infinite-canvas/package.json` ≥ 1 且在 dependencies 块内
    - vite.config.ts alias 区保留指向 flowgraph-v3 的条目(diff 确认未删)
    - 根 `npx tsc --noEmit` exit 0;`cd packages/infinite-canvas && npx tsc -b` exit 0
    - packages/infinite-canvas `npm test` 与 packages/flowgraph-v3/ts `npm test` 双绿
    - `cd packages/infinite-canvas && npm run build` exit 0
  </acceptance_criteria>
  <done>幽灵依赖正名,双根编译 + 双包测试 + vite 构建全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 删除操作 → 活代码 | 误删活组件(尤其红线 NodeBadges/ScoreMiniBar)导致运行时崩溃 |
| npm install(file: 依赖)→ 依赖树 | 本地 file: 声明,无外部供应链面变化 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-04-01 | Tampering | 误删活组件 | mitigate | 删除前逐文件 grep 复核 + 红线双文件存在性进 acceptance;git 历史可恢复(先删后建为 ROADMAP 决策 #6) |
| T-51-04-02 | Tampering | 类型删除连带破坏活类型 | mitigate | 消费方清零 grep 前置 + tsc 双根门 |
| T-51-04-03 | Tampering | 依赖声明破坏 vite 解析 | mitigate | alias 保留为 acceptance 条款;npm run build 全量构建门 |
| T-51-04-SC | Tampering | package installs | accept | file: 本地依赖,无新外部包;package-lock 更新为预期内 |

**结论:纯删除 + 声明,无运行时攻击面;风险全为工程完整性,由 grep/tsc/build 门覆盖。无 high 级威胁。**
</threat_model>

<verification>
- 12 文件不存在 + 红线双文件存在
- 四 legacy 类型名 0 命中
- package.json dependencies 含 @kais/flowgraph-v3;vite alias 保留
- 根 tsc --noEmit + 包 tsc -b + 双包 vitest + vite build 全绿
</verification>

<success_criteria>
- WRITE-04 落地:死代码 ~3,200 行清除,legacy 类型与过期注释移除,@kais/flowgraph-v3 进 dependencies;双根 tsc 干净 + 既有 vitest 全绿(成功标准 4)
- CanvasContextMenu.tsx 零触碰(与 51-03 文件划界)
</success_criteria>

<output>
Create `.planning/phases/51-canonical-write-path-coordination-guard/51-04-SUMMARY.md` when done. 记录:逐文件 grep 复核结果(尤其任何与 CONTEXT 清单不符的发现)、连带删除的测试文件清单、删除总行数(git diff --stat)。
</output>
