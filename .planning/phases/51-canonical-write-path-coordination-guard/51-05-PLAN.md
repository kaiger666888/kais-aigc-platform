---
phase: 51-canonical-write-path-coordination-guard
plan: 05
type: execute
wave: 3
depends_on: ["51-01", "51-02", "51-03", "51-04"]
files_modified:
  - scripts/verify-phase-51.ts
  - package.json
  - .planning/specs/COORD-01-khs2-parallel-coordination.md
  - .planning/ROADMAP.md
  - .planning/phases/51-canonical-write-path-coordination-guard/51-VALIDATION.md
autonomous: true
requirements: [WRITE-01, WRITE-02, WRITE-03, WRITE-04, COORD-01]
must_haves:
  truths:
    - "scripts/verify-phase-51.ts 遵循 verify-phase-49 隔离 chdir 范式(mkdtemp + chdir 先于 import utils barrel),生产 data/db2.sqlite 永不触碰"
    - "S2 集成断言:真 saveFullGraph → 真 saveFullGraph(减节点减边)→ 真 loadFullGraph,断言被删节点与其边不存在、其余节点字节不变;同图二次 save → load 无 diff(幂等)"
    - "S1/S3/S4/S5 grep + source-shape 门全部内置于 verify-phase-51,exit 0 代表 phase 51 全部成功标准的自动化部分成立"
    - "package.json scripts 含 verify:phase-51(对齐 verify:phase-50 行)"
    - ".planning/specs/COORD-01-khs2-parallel-coordination.md 存在,含变更面限定 + 排序约束 + plan 开工 checklist 复制块(含 kais-hermes-skills 工作树干净检查项)"
    - "ROADMAP.md 架构决策 #4 含指向 COORD-01 spec 的引用"
    - "verify 脚本含一组 forced-failure 自检(gate 能真的失败,延续 50-02 先例)"
  artifacts:
    - path: "scripts/verify-phase-51.ts"
      provides: "Phase 51 聚合契约门(S1-S5 + forced-failure 自检)"
      contains: "verify-phase-51"
    - path: "package.json"
      contains: "verify:phase-51"
    - path: ".planning/specs/COORD-01-khs2-parallel-coordination.md"
      provides: "khs2 v2.4 并行冲突管理规范 + plan 开工 checklist 复制块"
      contains: "工作树干净"
  key_links:
    - from: "scripts/verify-phase-51.ts"
      to: "src/lib/canvasRelationalStore.ts"
      via: "隔离 chdir 后动态 import 真实模块(零逻辑重实现)"
      pattern: "canvasRelationalStore"
    - from: ".planning/ROADMAP.md"
      to: ".planning/specs/COORD-01-khs2-parallel-coordination.md"
      via: "架构决策 #4 引用"
      pattern: "COORD-01-khs2-parallel-coordination"
---

<objective>
Phase 51 收尾聚合门 + COORD-01 成文。verify-phase-51.ts 把前 4 个 plan 的全部自动化验证收敛为单一契约门(GUARD 收尾传统,ROADMAP 决策 #7):S1 保存通道、S2 右键审核/删除(含"删除不复活"真实模块集成断言)、S3 canonical 回写、S4 死代码/依赖、S5 COORD-01 成文。COORD-01 规范文档落地 .planning/specs/(SKILL-CONTRACT.md 先例),ROADMAP 补引用,本 plan 首用其 checklist 作示范。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-canonical-write-path-coordination-guard/51-CONTEXT.md
@.planning/phases/51-canonical-write-path-coordination-guard/51-RESEARCH.md

<interfaces>
<!-- 关键约束(STATE.md 49-01 教训 + research Task 4/Task 6) -->

canvasRelationalStore.saveFullGraph/loadFullGraph **绑定 @/utils/db 单例**(无 db 注入参数),db 路径 = process.cwd()/data/db2.sqlite——必须采用 verify-phase-49 隔离 chdir 范式:mkdtemp + 拷贝最小运行所需 + `process.chdir(tmpdir)` **先于** `await import("../src/utils")` barrel + `await import("../src/lib/canvasRelationalStore")`,store 即在临时文件库上启动。
STATE.md 教训:app-db knex pool 与长驻 :memory: section 同进程时 insert 不落(49-01)——本方案用临时文件库单进程规避;若加 endpoint dispatch section 须走 spawned 子进程(本 plan 不需要,S2 直接驱动 saveFullGraph/loadFullGraph)。

verify-phase-50 范式(scripts/verify-phase-50.ts):section 化契约组、真实模块驱动零重实现、forced-failure 自检证明 gate 能失败、npm script 注册 verify:phase-NN。

S2 集成断言组(research Task 4):构造 FlowGraphV2(含节点 X + 边)→ 真 saveFullGraph → 真 saveFullGraph(去掉 X 及其边,模拟删除后保存)→ 真 loadFullGraph → 断言 X 不存在、其边不存在、其余节点字节不变;另加幂等组(同图二次 save → load 无 diff)。

COORD-01 spec 内容(research Task 6):①变更面限定(field-map / canvas_sync / manifest schema 三层,不碰 khs2 v2.4 在改 phases 内部算法);②排序约束(涉 p04/p09 输出字段映射的 phase 排在 khs2 v2.4 Phase 25 验收之后);③plan 开工 checklist 复制块,含 `git -C /data/workspace/kais-hermes-skills status --porcelain` 为空检查。

grep gate 范围纪律(地雷 #5):canvasToFlowGraph 等 grep 断言范围限 packages/infinite-canvas/src + src/,排除 src/routes/canvas/static/** 与 data/web/** 构建产物。

本 plan 自身开工 checklist(首用 COORD-01 示范):本 plan 不触碰 kmc 侧任何文件,变更面检查 N/A,但仍在 SUMMARY 中记录 checklist 执行结果作示范实例。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/verify-phase-51.ts(S1-S5 + forced-failure 自检)+ npm 注册</name>
  <files>scripts/verify-phase-51.ts (new), package.json (add one script entry)</files>
  <read_first>scripts/verify-phase-49.ts (隔离 chdir 范式——mkdtemp/chdir/import 顺序,逐行读), scripts/verify-phase-50.ts (section 化 + forced-failure 自检 + 汇总输出格式), src/lib/canvasRelationalStore.ts (saveFullGraph/loadFullGraph 签名与 db 绑定), src/types/flowgraph-v2-schema.ts (构造 fixture 图的合法形状), package.json (verify:phase-50 行,注册位置)</read_first>
  <action>
新建 scripts/verify-phase-51.ts,section 化(沿用 verify-phase-50 的输出/退出码约定):
- **S1 WRITE-01**:① `canvasToFlowGraph` 在 packages/infinite-canvas/src 与 src/ 0 命中(排除 src/routes/canvas/static/**、data/web/**);② src/routes/canvas/save.ts 不存在 + src/router.ts 无 `routes/canvas/save"` import;③ canvasApi.ts 含 `'/canvas/v2/save-v2'`;④ FlowCanvas.tsx handleSave 区含 `showToast(` 且含 serializeGraphToV2;⑤ serialize.ts 存在且其对 @kais/flowgraph-v3 的 import 全为 import type;⑥ adapter.ts 含 `case 'error'`。
- **S2 WRITE-02**:① source-shape:CanvasContextMenu.tsx 的 canvasApi import 行不含 approveNode/rejectNode 符号、含 deleteNode 调用与确认态、无 `confirm(`;② **集成断言(隔离 chdir)**:mkdtemp + chdir 先于动态 import utils barrel 与 canvasRelationalStore;构造合法 FlowGraphV2(≥3 节点含 X + ≥2 边含触及 X 的边)→ saveFullGraph → saveFullGraph(去 X 及其边)→ loadFullGraph → 断言 X 不存在、其边不存在、其余节点数据逐字节相等;③ 幂等:同图二次 saveFullGraph → loadFullGraph 无 diff。chdir 范式逐行复刻 verify-49(含临时库清理)。
- **S3 WRITE-03**:① MetaRenderer.tsx 含 `updateAssetMeta(` 且 setField 区无 flat data 覆写写法;② FlowCanvas.tsx socket 区含 `applySocketNodeState(` 与 `applySocketNodePreview(`;③ canvasStore.ts 含三 action 定义。
- **S4 WRITE-04**:① 12 个死代码文件逐一不存在;② badges/NodeBadges.tsx 与 badges/ScoreMiniBar.tsx 存在(红线反向断言);③ 四 legacy 类型名 0 命中;④ packages/infinite-canvas/package.json dependencies 含 @kais/flowgraph-v3。
- **S5 COORD-01**:① .planning/specs/COORD-01-khs2-parallel-coordination.md 存在;② 含"工作树干净"checklist 条款与 `kais-hermes-skills` 字样;③ ROADMAP.md 含 `COORD-01-khs2-parallel-coordination` 引用。
- **forced-failure 自检**:收尾跑一组必失败断言(如断言一个已知不存在文件存在)并确认 gate 报 FAIL——证明门能真的失败(50-02 先例);自检失败输出单独标记,不计入总通过数,若自检意外 PASS 则整体 exit 1。
全脚本无逻辑重实现:集成组只调真实 saveFullGraph/loadFullGraph;grep 组用 fs 读文件 + 正则。
package.json scripts 在 verify:phase-50 行旁加 `"verify:phase-51": "npx tsx scripts/verify-phase-51.ts"`。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -3; npm run verify:phase-51 2>&1 | tail -15; echo "exit=$?"; ls -la data/db2.sqlite | awk '{print $6,$7,$8}'; grep -c "verify:phase-51" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:phase-51` exit 0,S1-S5 全 PASS + forced-failure 自检正确报 FAIL
    - 运行前后 `data/db2.sqlite` mtime 不变(隔离 chdir 实证,生产库零触碰)
    - verify-phase-51.ts 中 `process.chdir` 出现在任何 `src/utils`/`canvasRelationalStore` 动态 import 之前(source 行序断言)
    - 脚本不含对 saveFullGraph/loadFullGraph 的逻辑重实现(`grep -c 'INSERT INTO\|UPDATE ' scripts/verify-phase-51.ts` = 0)
    - `npx tsc --noEmit` exit 0;package.json 含 verify:phase-51 条目
  </acceptance_criteria>
  <done>单一契约门收敛 phase 51 全部自动化验证,隔离范式保护生产库,forced-failure 证明门有效。</done>
</task>

<task type="auto">
  <name>Task 2: COORD-01 规范成文 + ROADMAP 引用</name>
  <files>.planning/specs/COORD-01-khs2-parallel-coordination.md (new), .planning/ROADMAP.md (架构决策 #4 补引用一句)</files>
  <read_first>.planning/specs/ (SKILL-CONTRACT.md 等先例的文档结构), .planning/ROADMAP.md (架构决策 #4 当前文本), 51-RESEARCH.md Task 6 (spec 三段内容)</read_first>
  <action>
1. 新建 .planning/specs/COORD-01-khs2-parallel-coordination.md,三段:
   ①**变更面限定**:v3.0 期间 kmc(kais-hermes-skills)侧变更仅限 field-map / canvas_sync / manifest schema 三层;不碰 khs2 v2.4 在改的 phases 内部算法(列举受保护面:kmc 22 phases 内部实现);
   ②**排序约束**:涉及 p04/p09 输出字段映射的 phase(Phase 53 VAR-01)排在 khs2 v2.4 Phase 25 验收完成之后;
   ③**plan 开工 checklist 复制块**(markdown 代码块,供后续每个涉 kmc 侧的 PLAN.md 原文引用):含 `git -C /data/workspace/kais-hermes-skills status --porcelain` 输出为空(工作树干净)、与上游同步确认、变更面自查(本 plan 是否只碰契约/映射层)三项。
2. ROADMAP.md 架构决策 #4 末尾补一句:`规范全文与 plan 开工 checklist 见 .planning/specs/COORD-01-khs2-parallel-coordination.md`。
3. 不动 gsd-plan-phase skill(用户级共享 skill,不硬编码项目专属规则——research Task 6 裁定)。
  </action>
  <verify>
    <automated>ls .planning/specs/COORD-01-khs2-parallel-coordination.md && grep -c "工作树干净" .planning/specs/COORD-01-khs2-parallel-coordination.md && grep -c "kais-hermes-skills" .planning/specs/COORD-01-khs2-parallel-coordination.md && grep -c "COORD-01-khs2-parallel-coordination" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - spec 文件存在且含:变更面三层限定、p04/p09 排序约束、"工作树干净" checklist 复制块、`git -C /data/workspace/kais-hermes-skills status --porcelain` 命令原文
    - ROADMAP.md 架构决策 #4 含指向 spec 的引用(`grep -c "COORD-01-khs2-parallel-coordination" .planning/ROADMAP.md` ≥ 1)
    - Task 1 的 verify-phase-51 S5 与此处断言同源,复跑 `npm run verify:phase-51` 仍 exit 0
  </acceptance_criteria>
  <done>COORD-01 双落地(规范文档 + ROADMAP 引用),checklist 可被后续涉 kmc 侧 plan 原文引用。</done>
</task>

<task type="auto">
  <name>Task 3: 全量回归 + 部署协调标注</name>
  <files>.planning/phases/51-canonical-write-path-coordination-guard/51-VALIDATION.md (status/nyquist_compliant 更新)</files>
  <read_first>51-VALIDATION.md (Full suite command 与 sign-off 清单), STATE.md 地雷 #5 (旧部署产物仍调 v1 save——deploy-canvas.sh 重跑为人工项)</read_first>
  <action>
1. 全量回归(51-VALIDATION Full suite):`cd packages/infinite-canvas && npm test && cd ../flowgraph-v3/ts && npm test && cd ../../.. && npx tsc --noEmit && npm run verify:phase-51`,另加 `cd packages/infinite-canvas && npm run test:e2e`(51-02 e2e 迁移套件)与 `npm run build`。全部 exit 0。
2. 更新 51-VALIDATION.md:frontmatter `nyquist_compliant: true`、Per-Task Verification Map status 全部标 ✅ green。
3. SUMMARY 中显式标注人工待办(地雷 #5):线上 SPA(data/web/infinite-canvas)与 src/routes/canvas/static 旧 bundle 在 v1 路由删除后保存会 404,需人工重跑 `scripts/deploy-canvas.sh` 重部署;保存失败 toast 视觉确认(51-VALIDATION Manual-Only)一并列为人工 UAT 项。不自动执行部署。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npm test 2>&1 | tail -2 && npm run test:e2e 2>&1 | tail -3 && npm run build 2>&1 | tail -2; cd ../flowgraph-v3/ts && npm test 2>&1 | tail -2; npx tsc --noEmit && npm run verify:phase-51 2>&1 | tail -5; grep -c "nyquist_compliant: true" .planning/phases/51-canonical-write-path-coordination-guard/51-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 双包 vitest + e2e + 根 tsc + verify:phase-51 + vite build 全部 exit 0
    - 51-VALIDATION.md frontmatter 含 `nyquist_compliant: true`,验证表无 pending 行
    - SUMMARY 含 deploy-canvas.sh 人工重跑与 toast 视觉确认两项人工待办的显式标注
  </acceptance_criteria>
  <done>Phase 51 全量验证绿,验证文档签核,人工部署/UAT 项显式交接。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify 脚本 → data/db2.sqlite | 契约门若直连生产库可污染 1600+ 行生产数据(49-01 历史教训) |
| COORD-01 文档 → 跨仓库工作纪律 | 仅文档,无代码面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-05-01 | Tampering | verify 集成组触碰生产 db2.sqlite | mitigate | verify-49 隔离 chdir 范式(mkdtemp + chdir 先于 import);acceptance 断言运行前后生产库 mtime 不变;chdir/import 行序 source 断言 |
| T-51-05-02 | Tampering | 契约门重实现被测逻辑致假绿 | mitigate | acceptance 断言脚本无 INSERT/UPDATE 直写(零重实现);集成组只调真实 canvasRelationalStore |
| T-51-05-03 | Repudiation | gate 恒绿不可信 | mitigate | forced-failure 自检组(50-02 先例),自检意外 PASS 则 exit 1 |
| T-51-05-SC | Tampering | package installs | accept | 零新包 |

**结论:验证基建 plan,最大风险是验证自身伤到生产数据,由隔离范式 + mtime 断言双重锁死。无 high 级威胁。**
</threat_model>

<verification>
- `npm run verify:phase-51` exit 0(S1-S5 + forced-failure 自检)
- 生产 data/db2.sqlite 运行前后 mtime 不变
- COORD-01 spec 存在 + ROADMAP 引用 + checklist 三要素
- 全量回归:双包 vitest + e2e + 双根 tsc + vite build 全绿
- 51-VALIDATION.md nyquist_compliant: true
</verification>

<success_criteria>
- 成功标准 1-4 的自动化部分由 verify-phase-51 单门收敛(grep 0 命中 / 删除不复活集成断言 / 死代码与依赖断言 / 双根编译绿)
- 成功标准 5 落地:COORD-01 冲突约束成文并含 plan 开工 checklist(工作树干净检查项),ROADMAP 引用闭环
- 人工待办(deploy-canvas.sh 重跑、toast 视觉确认)显式交接,不遗漏
</success_criteria>

<output>
Create `.planning/phases/51-canonical-write-path-coordination-guard/51-05-SUMMARY.md` when done. 记录:verify 各 section 通过证据、COORD-01 checklist 首用示范执行结果(本 plan 不碰 kmc 侧,N/A 说明)、人工待办清单(deploy + toast UAT)。
</output>
