---
phase: 51-canonical-write-path-coordination-guard
plan: 02
type: execute
wave: 1
# Task 3 e2e 复跑依赖 51-01 serializer 接入保存链(plan-checker WARNING 修复)
depends_on: ["51-01"]
files_modified:
  - packages/infinite-canvas/src/store/canvasStore.ts
  - packages/infinite-canvas/src/store/__tests__/canonicalWriteback.test.ts
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
  - packages/infinite-canvas/src/components/panel/MetaRenderer.tsx
  - packages/infinite-canvas/test/e2e/tests/phase35-storyboard-metadata.mjs
  - packages/infinite-canvas/test/e2e/mock-backend/server.mjs
autonomous: true
requirements: [WRITE-03]
must_haves:
  truths:
    - "store 新增 updateAssetMeta / applySocketNodeState / applySocketNodePreview 三个 canonical action,全部经 applyGraphTransform 写 store.graph"
    - "编辑后触发一次 applyGraphTransform(如 applyLayout),编辑值仍在——transform-survival 单测锁死"
    - "socket node:state 的 state 落 canonical(error→failed 归一,与 adapter 同一张表);progress 保持派生缓存 ephemeral,不落 canonical(裁定见下)"
    - "socket node:preview 的 thumbnailUrl 写 asset.media.thumbnail"
    - "MetaRenderer.setField 不再写 flat data[field];读侧 flat 覆盖层删除,直接读 meta[field]"
    - "phase35 e2e 迁移到 canonical 回写契约后全绿;mock backend 的 /api/canvas/save 已删除"
  artifacts:
    - path: "packages/infinite-canvas/src/store/canvasStore.ts"
      provides: "三个 canonical 回写 action"
      contains: "updateAssetMeta"
    - path: "packages/infinite-canvas/src/store/__tests__/canonicalWriteback.test.ts"
      provides: "三 action canonical 写入 + transform-survival vitest"
    - path: "packages/infinite-canvas/test/e2e/tests/phase35-storyboard-metadata.mjs"
      contains: "data.meta"
  key_links:
    - from: "packages/infinite-canvas/src/components/FlowCanvas.tsx"
      to: "packages/infinite-canvas/src/store/canvasStore.ts"
      via: "socket onNodeStateChange/onNodePreviewUpdate 回调改调 store action"
      pattern: "applySocketNodeState|applySocketNodePreview"
    - from: "packages/infinite-canvas/src/components/panel/MetaRenderer.tsx"
      to: "packages/infinite-canvas/src/store/canvasStore.ts"
      via: "MetadataEditor.setField 改调 updateAssetMeta"
      pattern: "updateAssetMeta"
---

<objective>
修复"派生缓存直改"类 bug 的写入方:MetadataEditor 镜头意图编辑与 socket `node:preview`/`node:state` 更新全部改经 store canonical action 回写 store.graph,派生 RF 缓存只由 graphToViewModel 重建、永不反向覆盖——从机制上消灭"applyGraphTransform 后编辑被冲掉"(成功标准 3)。phase35 e2e 契约随之迁移到新回写路径。

**明写裁定(对 CONTEXT "state/progress/thumbnailUrl 落 canonical" 的实证修正,research 地雷 #4):** V3 zod 全树 strict 且无 progress 槽位,progress 是瞬态运行时量——**progress 保持派生缓存 ephemeral,不落 canonical**;仅 state(归一后)与 thumbnailUrl 落 canonical。执行者不得把 progress 塞进 meta。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/51-canonical-write-path-coordination-guard/51-CONTEXT.md
@.planning/phases/51-canonical-write-path-coordination-guard/51-RESEARCH.md

<interfaces>
<!-- 提取自 51-RESEARCH.md Task 3/Task 5,已直读源码核实 -->

store 现状(packages/infinite-canvas/src/store/canvasStore.ts):
- `graph: FlowGraphV3 | null` 唯一真值源;`applyGraphTransform(fn)`(L315)→ setGraph → getViewModel memo 重建派生 RF 缓存
- 派生缓存永不反向覆盖 canonical(机制上无此通道);今天的 bug = 写入方只写派生缓存,下一次 transform 重建时编辑被冲掉
- L84 注释已声明该约定;`showToast` 既有

canonical 结构(packages/flowgraph-v3/ts/src/types.ts + zod.ts):
- 资产节点持 state / media.thumbnail / meta(判别联合);zod 全树 .strict()(唯一例外 GenerationParams catchall)
- 新 action 写入必须产出合法联合成员(stage 不可丢);storyboard 联合分支含 cameraMovement/framing/composition/pacing,字段级 patch 合法

接线点:
- FlowCanvas.tsx L208-225:socket 回调 onNodeStateChange(nodeId, state, progress?) / onNodePreviewUpdate(nodeId, thumbnailUrl),当前 setNodes 直改派生缓存
- MetaRenderer.tsx(packages/infinite-canvas/src/components/panel/) L138-175:MetadataEditor.setField 当前 setNodes(flat data[field]);读侧 `data[field] ?? meta[field]` flat 覆盖层

e2e 迁移清单(research Task 5 逐行表):packages/infinite-canvas/test/e2e/tests/phase35-storyboard-metadata.mjs
- L27-33 getField helper 读 flat data[field] → 删除/改读 data.meta(getMeta)
- L85/L96-98/L109 下拉改 framing/cameraMovement → getMeta 断言 data.meta.framing==='wide'、zoom_in → 清空后 undefined
- L124-126 STORYBOARD-07 保存往返 → 断言 saved payload 是 V2 形状(meta.version==='2'、节点带 branchId)+ flat data.cameraMovement==='zoom_in';建议加 reload 后 getMeta 真往返断言
- L63-99 下拉渲染/枚举不涉及写入路径,不动
- mock backend test/e2e/mock-backend/server.mjs 已有 /api/canvas/v2/save-v2(L183-193);迁移收尾删除 mock 的 /api/canvas/save 并 grep 全套 e2e 确认无其他用例打 v1 save
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: store 三个 canonical 回写 action + vitest(含 transform-survival)</name>
  <files>packages/infinite-canvas/src/store/canvasStore.ts, packages/infinite-canvas/src/store/__tests__/canonicalWriteback.test.ts (new)</files>
  <read_first>packages/infinite-canvas/src/store/canvasStore.ts (applyGraphTransform L315 上下文、approveNode L453 的 canonical 写法范式、state 归一表现状), packages/flowgraph-v3/ts/src/types.ts (asset 节点判别联合,meta 各分支字段), packages/flowgraph-v3/ts/src/zod.ts (strict 约束), packages/infinite-canvas/src/store/__tests__/selectWinner.test.ts (store 单测构造范式)</read_first>
  <action>
在 canvasStore 新增三个 action(命名为 Claude discretion,以下建议名沿 research):
1. `updateAssetMeta(nodeId, patch: Record<string, unknown>)` — MetaRenderer 专用:applyGraphTransform 内对目标资产节点的 `asset.meta` 做字段级 patch(patch 值 undefined/null/'' = 删除该字段,对应"未设置"清空语义);写入必须保持判别联合合法(stage 字段不动,只 patch 该 stage 分支允许的 key;非法 key 忽略并 console.warn,不 throw)。
2. `applySocketNodeState(nodeId, state: string, progress?: number)` — state 经与 adapter 同一张归一表(error→failed,未知值 console.warn 忽略)后落 canonical 节点 state;**progress 不落 canonical**——若调用方传 progress,走既有派生缓存 ephemeral 通道(保留现状 setNodes 直改或等价机制),注释写明"V3 strict 无 progress 槽位,瞬态量不持久化"(objective 裁定)。
3. `applySocketNodePreview(nodeId, thumbnailUrl: string)` — applyGraphTransform 内写 `asset.media.thumbnail`。
三个 action 在 graph === null 或节点不存在时静默早退(console.warn),不得 throw(socket 事件可在图加载前到达)。

新建 __tests__/canonicalWriteback.test.ts:
a. 三 action 各自断言 canonical graph 被更新(meta 字段 / state / media.thumbnail);
b. **transform-survival**:updateAssetMeta 写 framing='wide' 后再调一次无关 applyGraphTransform(如 applyLayout 或 identity 变换),断言 meta.framing 仍为 'wide'、派生 nodes 数据同步;
c. updateAssetMeta 清空语义:patch {cameraMovement: undefined} 后字段不存在;
d. applySocketNodeState('error') → canonical state==='failed';progress 传入后 canonical graph 序列化(JSON.stringify)不含 progress 值(ephemeral 断言);
e. 非法 meta key 被忽略不 throw;graph===null 时三 action 均不 throw。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx vitest run src/store/__tests__/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/infinite-canvas && npx vitest run src/store/__tests__/canonicalWriteback.test.ts` exit 0,5 断言组全绿
    - `grep -c 'updateAssetMeta\|applySocketNodeState\|applySocketNodePreview' packages/infinite-canvas/src/store/canvasStore.ts` ≥ 3
    - canvasStore.ts 中三 action 均经 applyGraphTransform 写 canonical(grep 各 action 函数体含 applyGraphTransform;progress 分支除外,其注释含 ephemeral)
    - 既有 `npx vitest run src/store/__tests__/selectWinner.test.ts` 无回归
  </acceptance_criteria>
  <done>三个 canonical action 落地,transform-survival 与 progress-ephemeral 裁定有测试锁死。</done>
</task>

<task type="auto">
  <name>Task 2: 接线——FlowCanvas socket 回调 + MetaRenderer 改 canonical 回写</name>
  <files>packages/infinite-canvas/src/components/FlowCanvas.tsx (L208-225 socket 回调区), packages/infinite-canvas/src/components/panel/MetaRenderer.tsx (L138-175 setField + 读侧覆盖层)</files>
  <read_first>packages/infinite-canvas/src/components/FlowCanvas.tsx (L200-230 socket 回调全文 + store hook 取用方式), packages/infinite-canvas/src/components/panel/MetaRenderer.tsx (L120-180 全文,setField 与读侧 `data[field] ?? meta[field]` 覆盖层), Task 1 的 canvasStore diff(新 action 签名)</read_first>
  <action>
1. FlowCanvas.tsx L208-225:onNodeStateChange 回调由 setNodes 直改改为 `applySocketNodeState(nodeId, state, progress)`(progress 仍随调用传入,由 action 内部走 ephemeral 通道);onNodePreviewUpdate 改为 `applySocketNodePreview(nodeId, thumbnailUrl)`。删除回调内不再需要的 setNodes 派生缓存直改逻辑。**本 plan 对 FlowCanvas.tsx 的改动仅限 socket 回调区(L200-230 一带)及其 import**——handleSave/handleOrchestrate 区归 51-01(W1 并行,提交时注意按代码区分离)。
2. MetaRenderer.tsx:MetadataEditor.setField 由 `setNodes(flat data[field])` 改为调 `updateAssetMeta(nodeId, { [field]: value })`(空值语义由 action 处理);读侧删除 `data[field] ?? meta[field]` 的 flat 覆盖层,直接读 `meta[field]`(即 asset.meta 经 graphToViewModel 注入 data.meta 的值)。下拉渲染/枚举选项逻辑(L63-99 对应区)不动。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx tsc -b --pretty 2>&1 | tail -3 && npm test 2>&1 | tail -5; grep -n 'updateAssetMeta' packages/infinite-canvas/src/components/panel/MetaRenderer.tsx | head -3; grep -n 'applySocketNodeState\|applySocketNodePreview' packages/infinite-canvas/src/components/FlowCanvas.tsx | head -4</automated>
  </verify>
  <acceptance_criteria>
    - MetaRenderer.tsx 含 `updateAssetMeta(` 调用;setField 路径不再出现对 flat `data[field]` 的写操作(该函数体内无 `data: {` 展开覆写节点 data 字段的 setNodes 调用)
    - FlowCanvas.tsx L200-230 区含 `applySocketNodeState(` 与 `applySocketNodePreview(`;两回调内不再有直改节点 state/thumbnailUrl 的 setNodes
    - FlowCanvas.tsx diff 不触及 handleSave/handleOrchestrate 代码区(L500-570,与 51-01 的文件划界)
    - `cd packages/infinite-canvas && npx tsc -b` exit 0;`npm test` 全绿
  </acceptance_criteria>
  <done>socket 与 MetaEditor 写入方全部改走 canonical action;派生缓存只读重建,编辑经 applyGraphTransform 后存活。</done>
</task>

<task type="auto">
  <name>Task 3: phase35 e2e 契约迁移 + mock backend v1 save 删除</name>
  <files>packages/infinite-canvas/test/e2e/tests/phase35-storyboard-metadata.mjs, packages/infinite-canvas/test/e2e/mock-backend/server.mjs</files>
  <read_first>packages/infinite-canvas/test/e2e/tests/phase35-storyboard-metadata.mjs (142 行全文), packages/infinite-canvas/test/e2e/mock-backend/server.mjs (L150-200,/api/canvas/save 与 /api/canvas/v2/save-v2 两个端点), 51-RESEARCH.md Task 5 逐行迁移表</read_first>
  <action>
按 research Task 5 逐行表迁移 phase35-storyboard-metadata.mjs:
1. L27-33 getField helper(读 flat data[field])删除,替换为 getMeta helper(读节点 data.meta);
2. L85/L96-98/L109 下拉写入断言改 getMeta:framing==='wide'、cameraMovement zoom_in → 清空后 undefined;
3. L124-126 STORYBOARD-07 保存往返:断言 saved payload 为 V2 形状(meta.version==='2'、节点带 branchId)、flat data.cameraMovement==='zoom_in'(序列化器摊平设计,reload 可读);并补 reload 后 getMeta 断言(真往返,最接近成功标准 3);
4. L63-99 下拉渲染/枚举断言不动;
5. mock-backend/server.mjs:删除 /api/canvas/save 端点(契约诚实),保留 /api/canvas/v2/save-v2;`grep -rn "canvas/save" packages/infinite-canvas/test/e2e/ | grep -v save-v2` 确认无其他用例打 v1 save(命中则一并迁移)。
注意:本任务依赖 51-01 的 serializeGraphToV2 已接入保存链路(STORYBOARD-07 走新序列化器);若 51-01 尚未合并,e2e 红灯属预期,合并后复跑转绿——两个 plan 均在本 phase W1,执行编排时 e2e 复跑以 51-01 落地后为准。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npm run test:e2e 2>&1 | tail -10; grep -rn "canvas/save" packages/infinite-canvas/test/e2e/ | grep -v save-v2 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/infinite-canvas && npm run test:e2e` exit 0(51-01 合并后),phase35 套件全绿
    - phase35-storyboard-metadata.mjs 不再含读 flat `data[field]` 的 getField helper;含 getMeta 断言与 V2 wire 形状断言(meta.version==='2')
    - `grep -rn "canvas/save" packages/infinite-canvas/test/e2e/ | grep -v save-v2` 0 命中(mock v1 端点已删,无用例残留)
    - mock-backend/server.mjs 仍含 `/api/canvas/v2/save-v2`
  </acceptance_criteria>
  <done>e2e 契约迁移到新 canonical 回写 + save-v2 路径,STORYBOARD-07 成为 WRITE-01/WRITE-03 的端到端覆盖。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| socket 事件 → canonical graph | 后端推送的 node:state/node:preview 进入唯一真值源,非法值可污染图 |
| 用户编辑(MetaEditor)→ canonical graph | 字段级 patch 需保持判别联合合法 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-02-01 | Tampering | 非法/未知 state 值写入 canonical | mitigate | action 内归一表(error→failed)+ 未知值 console.warn 忽略,单测锁死 |
| T-51-02-02 | Tampering | meta patch 破坏判别联合(zod strict) | mitigate | 非法 key 忽略不 throw;stage 字段不可经 patch 修改;单测覆盖 |
| T-51-02-03 | Tampering | progress 塞入 canonical 致 zod strict 违规 | mitigate | objective 明写裁定 progress ephemeral;单测断言 canonical 序列化不含 progress |
| T-51-02-04 | Information Disclosure | socket 事件在图加载前到达致 throw 崩 UI | mitigate | graph===null 静默早退,单测覆盖 |
| T-51-02-SC | Tampering | package installs | accept | 零新包 |

**结论:内部重构,无新增外部攻击面;写入合法性由 action 内校验 + 单测覆盖。无 high 级威胁。**
</threat_model>

<verification>
- `cd packages/infinite-canvas && npm test` 全绿(canonicalWriteback 新套件 + 既有无回归)
- `cd packages/infinite-canvas && npx tsc -b` exit 0
- `cd packages/infinite-canvas && npm run test:e2e` 全绿(51-01 合并后)
- MetaRenderer/FlowCanvas source-shape:setField 不写 flat、socket 回调走 store action
- mock backend 无 /api/canvas/save 残留
</verification>

<success_criteria>
- WRITE-03 落地:MetadataEditor 编辑与 socket node:preview/node:state 更新回写 canonical graph;触发 applyGraphTransform 后编辑仍在(transform-survival 单测 + e2e reload 往返双重锁死)
- progress-ephemeral 裁定成文(objective)+ 测试锁死,无 strict zod 违规风险
</success_criteria>

<output>
Create `.planning/phases/51-canonical-write-path-coordination-guard/51-02-SUMMARY.md` when done. 记录:三 action 最终命名、e2e 迁移的实际行变更、与 51-01 在 FlowCanvas.tsx 的代码区划界执行结果。
</output>
