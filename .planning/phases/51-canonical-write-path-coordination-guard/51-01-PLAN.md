---
phase: 51-canonical-write-path-coordination-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/infinite-canvas/src/v3/serialize.ts
  - packages/infinite-canvas/src/v3/__tests__/serialize.test.ts
  - packages/infinite-canvas/src/v3/adapter.ts
  - packages/infinite-canvas/src/v3/__tests__/adapter.test.ts
  - packages/infinite-canvas/src/services/canvasApi.ts
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
  - packages/infinite-canvas/src/components/CanvasContextMenu.tsx
  - src/routes/canvas/save.ts
  - src/router.ts
autonomous: true
requirements: [WRITE-01]
must_haves:
  truths:
    - "serializeGraphToV2 是 v3/serialize.ts 中的纯函数,输出通过服务端 FlowGraphV2Schema safeParse"
    - "data 袋重建合并 rawDataByNodeId——audio 节点 round-trip 后 shot_id/engine/duration_sec 不丢"
    - "rawDataByNodeId === null 时序列化器退化为纯 flattenMeta,不 throw"
    - "adapter normalizeNodeState 含 error→failed 分支,失败节点重载不再复活为 success"
    - "selectMode:'locked' 序列化为 'single' 并记 warning,不触发服务端 zod 400"
    - "saveCanvasGraph 指向 /canvas/v2/save-v2;FlowCanvas.handleSave/handleOrchestrate 与 CanvasContextMenu.handleBatchExecute 三处均改走 serializeGraphToV2"
    - "handleSave catch 分支调 showToast(err.message, 'error');graph===null 时 toast 早退"
    - "src/routes/canvas/save.ts 不存在,src/router.ts 无 route12 import/mount"
  artifacts:
    - path: "packages/infinite-canvas/src/v3/serialize.ts"
      provides: "canonical V3 → FlowGraphV2 正向序列化器(migrateV2toV3/buildMeta 逆变换)"
      contains: "serializeGraphToV2"
    - path: "packages/infinite-canvas/src/v3/__tests__/serialize.test.ts"
      provides: "round-trip vitest(adapt∘serialize 保 id 集/storyboard 字段/audio audioType + V2 schema safeParse)"
    - path: "packages/infinite-canvas/src/v3/adapter.ts"
      contains: "case 'error'"
  key_links:
    - from: "packages/infinite-canvas/src/services/canvasApi.ts"
      to: "src/routes/canvas/v2/save-v2.ts"
      via: "saveCanvasGraph POST /canvas/v2/save-v2"
      pattern: "/canvas/v2/save-v2"
    - from: "packages/infinite-canvas/src/components/FlowCanvas.tsx"
      to: "packages/infinite-canvas/src/v3/serialize.ts"
      via: "handleSave/handleOrchestrate 调 serializeGraphToV2(store.graph, rawDataByNodeId, viewport)"
      pattern: "serializeGraphToV2"
---

<objective>
新写 canonical V3 → FlowGraphV2 正向序列化器(本 phase 唯一的核心新造物),把画布保存从 legacy v1 `canvasToFlowGraph` 绕行一次性切换到既有 save-v2 端点(zod 校验 + 结构化参数强制 + graph:saved 广播),并随刀修复 adapter `normalizeNodeState` 缺 `error→failed` 映射的伴随 bug(失败节点重载复活为 success)。

输出:v3/serialize.ts + vitest round-trip 单测、adapter 修复 + 单测、3 处保存调用点接线、保存失败 showToast、v1 save 路由删除。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-canonical-write-path-coordination-guard/51-CONTEXT.md
@.planning/phases/51-canonical-write-path-coordination-guard/51-RESEARCH.md

<interfaces>
<!-- 提取自 51-RESEARCH.md Task 2 逆变换映射表(逐行推导自 migrate.ts §14 / buildMeta / adapter.ts),执行者直接取用 -->

服务端契约(src/routes/canvas/v2/save-v2.ts + src/types/flowgraph-v2-schema.ts):
- POST /api/canvas/v2/save-v2,body `{projectId, episodesId, graph}`
- FlowGraphV2Schema:meta.version 字面值 '2';nodes:FlowNodeV2Schema(id/type/branchId/phaseIndex/phaseName/position/size/data/state + 可选 reviewStatus/aiScore/isWinner/variantGroupId);links;branches(status/createdAt/updatedAt 必填);variantGroups.selectMode 枚举仅 single|multi
- 状态枚举:V2 = idle|pending|running|success|error|skipped;V3 = pending|running|success|failed(序列化器必须 failed→error)
- 结构化参数强制(src/lib/canvasAssetSchema.ts):仅 audio 类型有 schema,必填 shot_id + engine + duration_sec;其余类型放行

序列化器签名(建议,research 裁定):
```typescript
// packages/infinite-canvas/src/v3/serialize.ts — 纯函数,与 adapter.ts 同目录成对
// 对 @kais/flowgraph-v3 只许 `import type`(tsx 下类型擦除,根 scripts/verify-phase-51.ts 可直接 import 本文件)
export function serializeGraphToV2(
  graph: FlowGraphV3,
  rawDataByNodeId: Map<string, Record<string, unknown>> | null,
  viewport?: { x: number; y: number; zoom: number },
): FlowGraphV2Shape  // { meta: { version: '2', ... }, nodes, links, branches, variantGroups }
```

data 袋重建公式(关键):`data = { ...rawDataByNodeId?.get(id), ...flattenMeta(asset.meta), filePath, thumbnailUrl }`
- flattenMeta 必须摊平(非嵌套 data.meta)——reload 时 buildMeta 只读 flat 字段
- rawDataByNodeId 是 audio 必填字段与白名单外字段的唯一存活地,不合并 = 首存抹字段、后续全 400(地雷 #1)

映射要点(research Task 2 全表):stage script→'script'(content→data.prompt,补 hookType/hookIntensity/premise);storyboard→'storyboard' flat 补 shotId/shotType/durationS/cameraMovement/framing/composition/pacing;keyframe/global→'asset'(global 补 assetType,rawData.type 优先还原 scene_image);video/composite→'video'(composite 摊平 meta.edlRef);voice/foley/bgm→'audio' + data.audioType + shotId/emotion/speaker;mix→'audio' 缺省 audioType 有损记 warning;media.original/thumbnail→data.filePath/data.thumbnailUrl;failed→'error';reviewStatus/aiScore/curation=='selected'→顶层;event 节点 + role:'output' 边不落盘(同 graphToViewModel 折叠语义);link role→dataType,isExplore/isInactive 透传;branches shim {id,label:name,parentId,status:'active',forkReason:'',createdAt,updatedAt}(有损,与 adapter 现状同级);variantGroups selectMode 'locked'→'single'+warning(地雷 #3)

store 现状(packages/infinite-canvas/src/store/canvasStore.ts):`rawDataByNodeId: Map|null`(L46/287,adaptV2Graph 产出,fixture/V3 直通模式为 null);`applyGraphTransform`(L315);`showToast` 既有
调用点:FlowCanvas.tsx L44 import canvasToFlowGraph,L520 handleSave、L557 handleOrchestrate;CanvasContextMenu.tsx L7 import、L136 handleBatchExecute;canvasApi.saveCanvasGraph L322
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 正向序列化器 v3/serialize.ts + vitest round-trip 单测</name>
  <files>packages/infinite-canvas/src/v3/serialize.ts (new), packages/infinite-canvas/src/v3/__tests__/serialize.test.ts (new)</files>
  <read_first>packages/infinite-canvas/src/v3/adapter.ts (normalizeNode/normalizeLink/buildMeta 全量,逆变换的唯一依据), packages/flowgraph-v3/ts/src/migrate.ts (§14 映射 + buildMeta 各 stage 分支), packages/flowgraph-v3/ts/src/types.ts (canonical 联合类型), src/types/flowgraph-v2-schema.ts (FlowGraphV2Schema,单测 safeParse 断言用), packages/infinite-canvas/src/v3/__tests__/adapter.test.ts (vitest 风格范式)</read_first>
  <action>
1. 新建 packages/infinite-canvas/src/v3/serialize.ts:导出纯函数 serializeGraphToV2(graph, rawDataByNodeId, viewport?),按 <interfaces> 逆变换映射表实现。对 @kais/flowgraph-v3 一律 `import type`(类型在 tsx 下擦除,保证根 verify 脚本可 import)。data 袋重建公式 `{ ...rawDataByNodeId?.get(id), ...flattenMeta(asset.meta), filePath, thumbnailUrl }`;rawDataByNodeId === null 时退化为纯 flattenMeta 不得 throw(地雷 #6)。selectMode 'locked' → 'single' + warnings 数组收集(函数返回或出参携带 warnings,形式 Claude discretion,注释写明"服务端 zod 无 locked 槽位,映射 single 防整图 400")。mix stage → type 'audio' 缺省 audioType,记 warning(有损,research 裁定)。event 节点与 role:'output' 边不落盘(折叠语义同 graphToViewModel;evt_* 剔除语义与现行 canvasToFlowGraph 保持一致)。branches shim status:'active'/forkReason:''(有损,注释注明与 adapter 现状同级,不在本 phase 治本——地雷 #7)。link refType/sourceHandle 本就不在 V3 链路存活,不引入新损耗,注释注明(地雷 #8)。
2. 新建 __tests__/serialize.test.ts(vitest),断言组:
   a. round-trip 不变量:构造含 script/storyboard/asset(keyframe+global)/video/composite/audio(voice+foley) 各 stage 的 V3 图 + 配套 rawDataByNodeId,`adaptV2Graph(serializeGraphToV2(g, raw))` 保 node id 集一致、storyboard meta 字段(cameraMovement/framing/composition/pacing)round-trip 相等、audio audioType 保留;
   b. audio 必填参数存活:rawDataByNodeId 携带 shot_id/engine/duration_sec 的 audio 节点序列化后 data 含此三字段(地雷 #1 直接防线);
   c. V2 schema 合规:序列化输出对 FlowGraphV2Schema.safeParse 通过(从 src/types/flowgraph-v2-schema.ts import——若包间 import 受限,可复制最小 schema 断言关键字段,注释注明来源;优先直接 import);
   d. failed→error 状态映射;reviewStatus/aiScore/isWinner 落顶层;
   e. selectMode 'locked' → 'single' 且产生 warning;
   f. rawDataByNodeId === null 不 throw,输出仍 safeParse 通过;
   g. event 节点 + output 边不出现在输出 nodes/links。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx vitest run src/v3/__tests__/serialize.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/infinite-canvas && npx vitest run src/v3/__tests__/serialize.test.ts` exit 0,上述 7 断言组全绿
    - `grep -c 'serializeGraphToV2' packages/infinite-canvas/src/v3/serialize.ts` ≥ 1
    - `grep -n "from '@kais/flowgraph-v3'" packages/infinite-canvas/src/v3/serialize.ts` 命中的每一行均为 `import type`
    - serialize.ts 不含对 rawDataByNodeId 的非空断言(`grep -c 'rawDataByNodeId!' packages/infinite-canvas/src/v3/serialize.ts` = 0)
    - 单测含 audio 节点 round-trip 用例与 safeParse 断言
  </acceptance_criteria>
  <done>serializeGraphToV2 纯函数落地,7 组 round-trip/合规断言全绿,rawData 合并与 locked→single 两个 400 防线有测试锁死。</done>
</task>

<task type="auto">
  <name>Task 2: adapter normalizeNodeState 补 error→failed 伴随修复 + 单测</name>
  <files>packages/infinite-canvas/src/v3/adapter.ts (L73 normalizeNodeState), packages/infinite-canvas/src/v3/__tests__/adapter.test.ts</files>
  <read_first>packages/infinite-canvas/src/v3/adapter.ts (normalizeNodeState 现有分支结构 + warn 通道), packages/infinite-canvas/src/v3/__tests__/adapter.test.ts (既有用例风格)</read_first>
  <action>
在 normalizeNodeState 的 switch/映射中补 `case 'error': return 'failed'`(与 V3 枚举对齐;不再落 default→success+warning)。在 adapter.test.ts 追加用例:构造 V2 节点 state='error',adaptV2Graph 后节点 state==='failed' 且不产生"未知状态"warning;同时回归断言既有 success/running/pending 映射不变。本修复与 Task 1 序列化器 failed→error 互为往返,防止已保存的失败节点重载后复活为 success(地雷 #2)。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx vitest run src/v3/__tests__/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "case 'error'" packages/infinite-canvas/src/v3/adapter.ts` 命中 ≥ 1
    - `cd packages/infinite-canvas && npx vitest run src/v3/__tests__/adapter.test.ts` exit 0(含新 error→failed 用例 + 既有用例无回归)
    - adapter.test.ts 中存在断言 state==='failed' 的新用例
  </acceptance_criteria>
  <done>error→failed 归一落地并有测试锁死,失败节点保存-重载往返状态守恒。</done>
</task>

<task type="auto">
  <name>Task 3: 保存通道切换——canvasApi 端点 + 3 处调用点接线 + showToast + v1 路由删除</name>
  <files>packages/infinite-canvas/src/services/canvasApi.ts (saveCanvasGraph L322), packages/infinite-canvas/src/components/FlowCanvas.tsx (handleSave L515-527, handleOrchestrate L557), packages/infinite-canvas/src/components/CanvasContextMenu.tsx (handleBatchExecute L136 单行), src/routes/canvas/save.ts (delete), src/router.ts (route12 import/mount 删除)</files>
  <read_first>packages/infinite-canvas/src/services/canvasApi.ts (saveCanvasGraph 签名与参数类型), packages/infinite-canvas/src/components/FlowCanvas.tsx (L44 import、L500-570 handleSave/handleOrchestrate 全文、showToast 既有用法), packages/infinite-canvas/src/components/CanvasContextMenu.tsx (L1-20 import 区 + L130-140 handleBatchExecute), src/routes/canvas/save.ts (确认无被复用导出), src/router.ts (L15 route12 import + mount 行), src/routes/canvas/v2/save-v2.ts (body 形状 {projectId, episodesId, graph})</read_first>
  <action>
1. canvasApi.saveCanvasGraph:URL '/canvas/save' → '/canvas/v2/save-v2',graph 参数类型改为 serializeGraphToV2 的输出形状(FlowGraphV2 wire 形状);函数签名其余不变。
2. FlowCanvas.handleSave:删除 canvasToFlowGraph import 与调用,改从 store 取 `graph` 与 `rawDataByNodeId` 调 serializeGraphToV2(graph, rawDataByNodeId, viewport) 后 saveCanvasGraph;graph === null(加载完成前瞬态 / fixture 模式)时 showToast 提示并早退(地雷 #6,不再存在 flowDataMapper 兜底路径);catch 分支 console.error → `showToast(err.message, 'error')`(CONTEXT 锁定,复用既有 store 通知)。handleOrchestrate 同一序列化入口。
3. CanvasContextMenu.handleBatchExecute(L136):单行切换 canvasToFlowGraph(nodes, edges) → serializeGraphToV2(store graph/rawData)。**本 plan 对 CanvasContextMenu.tsx 的改动仅限 L136 调用与 L7 import 两行**——approve/reject/delete 重接与 legacy 类型清理归 51-03(W2 独占该文件),避免文件级冲突。
4. 删除 v1 路由前 grep 消费方(地雷 #9):`grep -rn "canvas/save" src/ scripts/ --include='*.ts' | grep -v save-v2` 与 `grep -rn "'/canvas/save'" packages/infinite-canvas/src src/` 确认仅剩被删除的 import;确认无服务端内部调用方后,删除 src/routes/canvas/save.ts 与 src/router.ts 的 route12 import/mount 两行(router 注释序号不重排,留空缺即可,与仓库惯例一致)。
5. 不动:flowDataMapper.ts 本体(51-04 删除)、kmc 侧任何文件、ReviewActionButtons。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx tsc -b --pretty 2>&1 | tail -3; npx tsc --noEmit 2>&1 | tail -3; grep -rn "canvasToFlowGraph" packages/infinite-canvas/src --include='*.ts*' | wc -l; grep -c "save-v2" packages/infinite-canvas/src/services/canvasApi.ts; ls src/routes/canvas/save.ts 2>&1; grep -c "routes/canvas/save\"" src/router.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "canvasToFlowGraph" packages/infinite-canvas/src --include='*.ts' --include='*.tsx'` 输出为空(0 命中——三处调用点 + import 全部清除)
    - packages/infinite-canvas/src/services/canvasApi.ts 含 `'/canvas/v2/save-v2'` 且不含 `'/canvas/save'`(精确串,排除 save-v2 子串误匹配用引号界定)
    - FlowCanvas.tsx handleSave catch 分支含 `showToast(`;含 graph === null 早退分支
    - CanvasContextMenu.tsx  diff 仅触及 import 行与 L136 调用行(行数变化 ≤ 4 行)
    - `ls src/routes/canvas/save.ts` 报不存在;`grep -c 'routes/canvas/save"' src/router.ts` = 0
    - `npx tsc --noEmit`(根)exit 0;`cd packages/infinite-canvas && npx tsc -b` exit 0
    - `cd packages/infinite-canvas && npm test` 全绿
  </acceptance_criteria>
  <done>保存全链路走 serializeGraphToV2 → save-v2;失败 toast 可见;v1 save 路由与 canvasToFlowGraph 引用清零;双根 tsc + 包内 vitest 绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 画布 SPA → POST /api/canvas/v2/save-v2 | 内部 localhost 工具,无认证面变化;本 plan 仅切换既有前端指向既有端点 |
| canonical graph → wire 序列化 | 数据变换错误可致生产图字段丢失(audio 必填参数) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01-01 | Tampering | serializeGraphToV2 data 袋重建 | mitigate | rawDataByNodeId 合并为强制公式,audio 必填三字段 round-trip 单测锁死(地雷 #1);V2 schema safeParse 断言进单测 |
| T-51-01-02 | Tampering | selectMode 'locked' zod 400 整图 | mitigate | 映射 'single' + warning,单测断言(地雷 #3) |
| T-51-01-03 | Tampering | 失败节点状态复活 | mitigate | adapter error→failed 伴随修复 + 单测(地雷 #2) |
| T-51-01-04 | Denial of Service | v1 路由删除后旧部署产物 404 | accept+note | 构建产物(src/routes/canvas/static、data/web)不在本 plan 重部署;phase 收尾由 51-05 VERIFICATION 标注人工重跑 deploy-canvas.sh(地雷 #5) |
| T-51-01-SC | Tampering | package installs | accept | 零新包,无 install 任务 |

**结论:内部重构,无新增外部攻击面;主要风险是数据完整性,全部由单测/grep 门覆盖。无 high 级威胁。**
</threat_model>

<verification>
- `cd packages/infinite-canvas && npm test` 全绿(serialize + adapter 新用例 + 既有无回归)
- `npx tsc --noEmit`(根)与 `cd packages/infinite-canvas && npx tsc -b` 双根 exit 0
- `grep -rn "canvasToFlowGraph" packages/infinite-canvas/src src/routes --include='*.ts*'` 0 命中(排除 static 构建产物)
- src/routes/canvas/save.ts 不存在;router.ts 无 route12
- saveCanvasGraph 指向 /canvas/v2/save-v2;handleSave catch 含 showToast
</verification>

<success_criteria>
- WRITE-01 主体落地:保存走 save-v2 持久化 canonical V3 graph,canvasToFlowGraph 在活代码中 0 引用;保存失败 toast 可见(source-shape 断言,视觉确认列入 51-VALIDATION Manual-Only)
- 地雷 #1/#2/#3 三道 400/复活防线均有自动化锁死
</success_criteria>

<output>
Create `.planning/phases/51-canonical-write-path-coordination-guard/51-01-SUMMARY.md` when done. 记录:序列化器取舍(mix 有损、branch shim)、v1 路由删除前 grep 消费方的实证结果。
</output>
