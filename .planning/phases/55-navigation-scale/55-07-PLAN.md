---
phase: 55-navigation-scale
plan: 07
type: execute
wave: 3
depends_on: ["55-04", "55-05"]
files_modified:
  - packages/infinite-canvas/test/e2e/tests/phase55-nav.mjs
  - packages/infinite-canvas/src/constants.ts
autonomous: true
requirements: [NAV-03, NAV-04, NAV-05]
must_haves:
  truths:
    - "playwright 冒烟覆盖本期导航主链路：`/` 打开导航器 → 场景分组结果 → Enter/点击聚焦跳转；搜索期间无节点被隐藏；新资产落点有界；列头聚焦后 zoom ≥ 0.6"
    - "全 phase 门禁绿：全量 vitest + verify:phase-55 契约 + 双根 tsc + dist build；LAYOUT.NEW_NODE_* 死常量清除"
  artifacts:
    - path: "packages/infinite-canvas/test/e2e/tests/phase55-nav.mjs"
      provides: "phase 55 e2e 冒烟（55-VALIDATION Wave 0 最后一个缺口）"
    - path: "packages/infinite-canvas/src/constants.ts"
      provides: "NEW_NODE_* 随机散布常量删除（引用已于 55-04 清零）"
  key_links:
    - from: "packages/infinite-canvas/test/e2e/tests/phase55-nav.mjs"
      to: "packages/infinite-canvas/test/e2e/helpers.mjs"
      via: "test/expect/loadCanvas/store 桥（window.__kaisCanvas）既有 helper"
      pattern: "helpers.mjs"
    - from: "packages/infinite-canvas/test/e2e/tests/phase55-nav.mjs"
      to: "packages/infinite-canvas/src/main.tsx"
      via: "window.__kaisCanvas.addNodeForTest / getViewCenter（55-04 交付的 testMode 门控桥——placement 用例驱动面）"
      pattern: "addNodeForTest"
    - from: "packages/infinite-canvas/test/e2e/tests/phase55-nav.mjs"
      to: "packages/infinite-canvas/src/components/canvas/SearchNavigator.tsx"
      via: "data-testid=search-navigator / role=dialog 断言"
      pattern: "search-navigator"
---

<objective>
Phase 55 收口：落地 55-VALIDATION Wave 0 的最后一个缺口（e2e phase55-nav.mjs 冒烟），跑全 phase 门禁（全量 vitest + verify:phase-55 契约 + 双根 tsc + build），并清除 LAYOUT.NEW_NODE_* 死常量（随机散布时代的最后残留）。

Purpose: 55-VALIDATION 的 pre-verify 冒烟契约要求 search jump / placement / lane focus 三链路有 e2e 证据后才能进入 /gsd:verify-work。
Output: phase55-nav.mjs + 门禁全绿记录 + 死常量清理。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/55-navigation-scale/55-RESEARCH.md
@.planning/phases/55-navigation-scale/55-VALIDATION.md
@.planning/phases/55-navigation-scale/55-04-SUMMARY.md
@.planning/phases/55-navigation-scale/55-05-SUMMARY.md

<interfaces>
<!-- e2e 基建 -->
packages/infinite-canvas/test/e2e/helpers.mjs（test/expect 封装、loadCanvas、nodeSelector、getCalls、switchToCanvasView；window.__kaisCanvas store 桥 getNodes 等 :37-54；loadCanvas :21-26 已带 testMode: '1' 并 waitForFunction(__kaisCanvas)——桥可用前提零额外装配）
packages/infinite-canvas/test/e2e/tests/phase52-regen.mjs（正式测试模板：helpers import、expect.poll、data-testid、mock 后端模式）
packages/infinite-canvas/canvas-real-screenshot.mjs（真实后端探针模板：localhost:10588、waitForSelector('.react-flow__node')）
playwright.config.mjs（baseURL localhost:9876、workers 1）——地雷 #10：e2e 跑 dist build，先 npm run build
<!-- 被测表面（前序 plan 产物） -->
data-testid: search-navigator / scene-shot-browser / branch-panel / phase-columns（既有）/ shot-tree（既有）
store 桥可用动作：getNodes、setSelectedNodeIds、addNodeForTest/getViewCenter（55-04 交付，testMode 门控——生产 bundle 无此面）；focusAssetNodeId 经 UI 交互触发
addNodeForTest 语义（55-04 Task 3.6）：位置决策 = node.position 有限 → 用之；否则 placeNewAsset(center)（getViewCenter() 换算——读 FlowCanvas 模块级 live viewport ref（getLiveViewport），FlowCanvas 挂载后即非 null，与 localStorage/meta.viewport 无关；store.viewport 镜像方案已被证伪废弃）→ addNodeFromSocket canonical 写回；不触发 setFocusAssetNodeId（视口不动，支撑有界断言）
UI-SPEC §1 键盘流（↑↓/Enter/Esc）与 §3 列头聚焦（恢复 ≥0.6）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e phase55-nav.mjs 导航冒烟</name>
  <files>packages/infinite-canvas/test/e2e/tests/phase55-nav.mjs</files>
  <read_first>
    - packages/infinite-canvas/test/e2e/tests/phase52-regen.mjs（全文——helpers 导入形态/mock 模式/expect.poll 纪律）
    - packages/infinite-canvas/test/e2e/helpers.mjs（可注入动作清单——store 桥能力边界；loadCanvas 已带 testMode: '1'）
    - packages/infinite-canvas/src/main.tsx:8-24（55-04 扩张后的桥面：addNodeForTest/getViewCenter 签名与语义）
    - packages/infinite-canvas/canvas-real-screenshot.mjs（真实后端探针：URL 参数 projectId/episodesId/testMode、等待节点出现的写法）
    - .planning/phases/55-navigation-scale/55-VALIDATION.md Wave 0 e2e 行（冒烟范围定义）
  </read_first>
  <action>
新建 packages/infinite-canvas/test/e2e/tests/phase55-nav.mjs（先 `npm run build` 再跑——e2e 用 dist；沿用 phase52-regen.mjs 的 import 与断言风格）：

1. 前置：加载含分镜资产的 canvas（优先 helpers 的 mock/fixture 通道；若分镜/泳道数据不足则走 canvas-real-screenshot.mjs 的真实后端模式 localhost:10588 + 真实 projectId/episodesId——脚本顶部以常量注明可切换，默认 mock）。helpers loadCanvas 自带 testMode: '1'，55-04 的桥面（addNodeForTest/getViewCenter）经 waitForFunction(__kaisCanvas) 就绪后可用。
2. 用例 search-navigator-open：window keydown 派发 '/'（非输入焦点态）→ [data-testid="search-navigator"]（role="dialog"）可见且搜索框自动聚焦；在画布某 input 聚焦时按 '/' 不打开（Pitfall 7 守卫）。
3. 用例 search-grouped-jump：输入能命中分镜的 query（如 shot_id 片段）→ 出现场景分组头（sticky 场景色点结构）；Enter → 选中节点变为目标（store 桥 getNodes/setSelectedNodeIds 或 selectedNode 断言）且画布 viewport zoom ≤ 1.5（fitView maxZoom 语义）；查询激活期间 [data-testid] 画布节点 hidden 数 === 0（断言 style.display 非 none / React Flow hidden 类缺失——「不再隐藏非命中」）。
4. 用例 new-asset-placement：经 55-04 交付的 testMode 门控桥驱动——先 expect.poll 至 `page.evaluate(() => window.__kaisCanvas.getViewCenter())` 非 null（live 桥就绪前置：FlowCanvas 已挂载、testMode effect 已把 getViewport 接入模块级 live ref——读的是 reactFlow 实例实时值而非 store 镜像，空 localStorage / fixture 无 meta.viewport 的 mock 环境同样成立；__kaisCanvas 对象在 FlowCanvas 挂载前即存在，一次性 evaluate 会竞态，必须 expect.poll）→ 以唯一新造 id 构造无 position 节点调 `window.__kaisCanvas.addNodeForTest(node)` → expect.poll store 桥 getNodes() 出现该 id → 断言节点 position 与再次 getViewCenter() 各轴距离 ≤ 64px（有界断言，成功标准 4 的 e2e 口径；两侧均为 live 实时值——不与任何陈旧镜像自洽；桥不触发 setFocusAssetNodeId，视口不动，测得的是放置决策本身）。
5. 用例 lane-focus-readable：点击 `[data-testid="phase-columns"] [aria-label*="聚焦本阶段"]`（CSS 属性选择器——直接匹配 DOM 属性、不经可访问性树，勿对该热区用 getByRole/getByLabel；与 55-05 的处置互补：55-05 删 SVG 根 aria-hidden 使热区 role/tabIndex/aria-label 语义成立且保留 aria-label，本用例定位器本就不依赖 a11y 树——两改互不冲突，改其一不应回退另一）→ expect.poll 至 getViewport().zoom ≥ 0.6（恢复下限）；再次点击另一列头 → fitView 动画后该列节点均在视口内（boundingBox 与 viewport 相交断言，取简实现）。
6. 收尾断言：Esc 关闭导航器；控制台不出现未捕获错误（page.on('pageerror') 收集为空）。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npm run build && npm run test:e2e -- phase55</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e -- phase55` 退出码 0，≥ 4 个用例（open/grouped-jump/placement/lane-focus）全绿。
    - 用例内含 hidden===0 断言与视口中心 ≤64px 有界断言（源断言：grep "64" 与 "hidden" 命中测试文件）。
    - `grep -c "addNodeForTest" packages/infinite-canvas/test/e2e/tests/phase55-nav.mjs` ≥ 1（placement 用例经 55-04 桥驱动，非临时 window 注入）。
    - 测试文件不依赖真实后端硬编码凭据；真实后端模式可经顶部常量切换（grep "10588" 命中）。
  </acceptance_criteria>
  <done>导航三链路（搜索跳转/落点/泳道聚焦）有 e2e 证据，55-VALIDATION Wave 0 清单闭合。</done>
</task>

<task type="auto">
  <name>Task 2: LAYOUT.NEW_NODE_* 死常量清除 + 全 phase 门禁</name>
  <files>packages/infinite-canvas/src/constants.ts</files>
  <read_first>
    - packages/infinite-canvas/src/constants.ts:78-110（LAYOUT.NEW_NODE_* 四键与 :82 注释 mentioning NEW_NODE）
    - 前置 grep 确认引用为零（见 action）
  </read_first>
  <action>
1. 先证死：`grep -rn "NEW_NODE" packages/infinite-canvas/src --include=*.ts --include=*.tsx | grep -v __tests__ | grep -v constants.ts` 必须零命中（55-04 已删 FlowCanvas 引用；若非零，回补删除后再继续）。
2. constants.ts：删除 LAYOUT 内 NEW_NODE_X_MIN/NEW_NODE_X_RANGE/NEW_NODE_Y_MIN/NEW_NODE_Y_RANGE 四键；:82 注释中「NEW_NODE 等交互偏移保留」措辞同步修正（改为指向 placeNewAsset 的有界落点）。
3. 全 phase 门禁（全部必须绿并在 SUMMARY 记录数值）：
   - `cd packages/infinite-canvas && npm test`（全量 vitest）
   - `cd packages/infinite-canvas && npx tsc -b --pretty`
   - `cd /data/workspace/kais-aigc-platform && npx tsc -b --pretty`（根仓——import-from-dir 跨界 import 编译）
   - `cd /data/workspace/kais-aigc-platform && npm run verify:phase-55`（契约）
   - `cd packages/infinite-canvas && npm run build && npm run test:e2e`（全量 e2e——含 phase55 与既有回归）
4. 红线终检（记入 SUMMARY）：useLod.ts 四常量原值；tokens.css LOD 三行未动；khs 仓 `git status --porcelain` 零改动。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npm test && npx tsc -b --pretty && cd /data/workspace/kais-aigc-platform && npx tsc -b --pretty && npm run verify:phase-55 && grep -c "NEW_NODE" packages/infinite-canvas/src/constants.ts; true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NEW_NODE" packages/infinite-canvas/src/constants.ts` === 0 且全 src 引用为零。
    - 五项门禁命令全部退出码 0（vitest / 双 tsc / verify:phase-55 / build+e2e）。
    - `git -C /data/workspace/kais-hermes-skills status --porcelain | wc -l` 与 phase 开工前一致（零 khs 改动）。
  </acceptance_criteria>
  <done>死常量清零、五门禁绿、红线终检记录——phase 55 达到 /gsd:verify-work 前置（UAT 项见 55-VALIDATION Manual-Only 表）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e 测试 → dev server / 真实后端 | 本机服务（9876/10588），只读断言 + store 桥白名单动作 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-07 | Tampering | e2e 真实后端模式误写生产库 | mitigate | 断言只读；placement 用例经 55-04 的 testMode 门控桥 addNodeForTest（生产 bundle 无此面，仅客户端 store 写回），不 POST 生产端点；真实模式仅读导航 |
| T-55-SC | Tampering | npm 安装 | accept | 零新增依赖（playwright/vitest 既有） |
</threat_model>

<verification>
- 五项门禁（Task 2 全列）+ 死常量 grep 零命中 + khs 零改动
- UAT 人工项（fitView 可读性 / 生产全量导入未映射为空 / 分支手感 / 93 镜性能）不在本 plan——55-VALIDATION Manual-Only 表由 verify 阶段领取
</verification>

<success_criteria>
- e2e 冒烟三链路绿（成功标准 3/4/5 的交互级证据）；Wave 0 五缺口（verify 脚本/registry 测试/placement 测试/search 测试/e2e）全部闭合。
- 全 phase 门禁绿 + 死常量清零 + 红线（LOD/token/khs）终检通过。
</success_criteria>

<output>
Create `.planning/phases/55-navigation-scale/55-07-SUMMARY.md` when done
</output>
