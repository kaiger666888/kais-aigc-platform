---
phase: 55-navigation-scale
plan: 03
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - packages/infinite-canvas/src/components/pipeline/model.ts
  - packages/infinite-canvas/src/constants.ts
  - packages/infinite-canvas/src/components/pipeline/__tests__/pipelineModel.test.ts
  - src/routes/canvas/v2/import-from-dir.ts
  - packages/infinite-canvas/src/v3/__tests__/adapter.test.ts
autonomous: true
requirements: [NAV-01]
must_haves:
  truths:
    - "代码库中 phase 词汇只有一个来源：PIPELINE_PHASES/PHASE_GROUPS/PHASE_DEFS 三张旧表全部删除，改为消费 phaseRegistry（D-04 反模式守卫，binding constraint 1）"
    - "未知 phaseIndex 节点落入「未映射」兜底条目并 console.warn（聚合一条/索引），不 throw（D-03）"
    - "import-from-dir 写入的 zone/summary/artifact 节点 phaseIndex 取自注册表 khs 编号，不再是 laneIndex+1（binding constraint 8）"
    - "一张覆盖 22 phaseIndex 的合成 V2 图经 adapter/派生后零「未映射」条目（成功标准 1 的 fixture 口径，A1：生产全量导入归 UAT）"
  artifacts:
    - path: "packages/infinite-canvas/src/components/pipeline/model.ts"
      provides: "PIPELINE_PHASES = PHASE_REGISTRY 重导出 + derivePipelineModels 未映射兜底"
    - path: "packages/infinite-canvas/src/constants.ts"
      provides: "PHASE_GROUPS 由注册表派生（字面量表删除）"
    - path: "src/routes/canvas/v2/import-from-dir.ts"
      provides: "后端 zone 表消费注册表（22 条 + khs phaseIndex 写入 + 词汇表增补）"
    - path: "packages/infinite-canvas/src/components/pipeline/__tests__/pipelineModel.test.ts"
      provides: "未映射兜底/22 条完整性单测"
  key_links:
    - from: "packages/infinite-canvas/src/components/pipeline/model.ts"
      to: "packages/infinite-canvas/src/constants/phaseRegistry.ts"
      via: "import { PHASE_REGISTRY } 并删除内联 19 条"
      pattern: "from '../../constants/phaseRegistry'"
    - from: "src/routes/canvas/v2/import-from-dir.ts"
      to: "packages/infinite-canvas/src/constants/phaseRegistry.ts"
      via: "相对路径跨界 import（schema/generated 先例）+ def.phaseIndex 写入三处写点"
      pattern: "PHASE_REGISTRY"
---

<objective>
NAV-01 消费方迁移：把 55-01 的 22-phase 注册表接成唯一词汇源——前端 model.ts/constants.ts 删旧表改消费（D-04），derivePipelineModels 的 extras 机制升级为「未映射」兜底 zone（D-03 fail-loud 不崩），后端 import-from-dir.ts 删 PHASE_DEFS 13 条旧表、phaseIndex 写点改 khs 编号（binding constraint 8），并以合成 fixture 断言「新导入零未映射」。

Purpose: 这是 zone 表对齐的实际落地步；三张表合一消除了 kap 侧最后一处 pre-W6 编号错位，Phase 54 高亮与 Phase 57 taxonomy 从此同源。
Output: 单源注册表消费链 + 未映射兜底 + 契约内导入断言。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/55-navigation-scale/55-CONTEXT.md
@.planning/phases/55-navigation-scale/55-RESEARCH.md
@.planning/phases/55-navigation-scale/55-PATTERNS.md
@.planning/phases/55-navigation-scale/55-01-SUMMARY.md

<interfaces>
<!-- 55-01 产物（本 plan 的依赖） -->
packages/infinite-canvas/src/constants/phaseRegistry.ts 导出:
  PHASE_REGISTRY: readonly PipelinePhaseDef[]（22 条；PipelinePhaseDef 含 sortKey/code/name/group/phaseIndex/sub?/khsPrefix/prefix/canvasType/assetType/label/unmapped?）
  PhaseGroup / DEREGISTERED_PHASE_PREFIXES / phaseByPrefix
<!-- 被删除/改造的三张旧表 -->
pipeline/model.ts:46-80 PIPELINE_PHASES 内联 19 条（删除对象）
constants.ts:319-331 PHASE_GROUPS 字面量表 1-18（删除对象；消费者 laneGeometry.ts:94 经 PHASE_GROUPS[index] ?? 'production' 自动适应，不需改）
src/routes/canvas/v2/import-from-dir.ts:88-102 PHASE_DEFS 13 条 + :104-107 PHASE_DEF_MAP + :110-127 FILE_TO_PHASE + :134-145 ASSET_DIR_TO_PHASE（删除/重建对象）
<!-- 兜底机制基座 -->
pipeline/model.ts:382-398 derivePipelineModels 的 extras 机制（未注册 phaseIndex 追加「阶段 {idx}」——升级为「未映射」命名条目 + warn）
model.ts:161-165 phaseIndexOf 的 Number.isFinite 守卫（保留不动）
v3/adapter.ts:380-408 buildPhaseCatalog（zone 节点 phaseIndex/phaseName 胜出——消费链自动适应）
<!-- 导入写点（phaseIndex=laneIndex+1 错位的三个位置） -->
import-from-dir.ts:627-649（zone 节点）、:657（summary）、:842（artifact）
UI-SPEC §Component Inventory 4 未映射 zone 呈现（--cv-stale 琥珀 + 列头「未映射 · {N} 节点」——视觉消费在 PhaseColumns，55-05 不重叠；本 plan 只出数据/命名）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: model.ts + constants.ts 删旧表改单源 + 未映射兜底</name>
  <files>packages/infinite-canvas/src/components/pipeline/model.ts, packages/infinite-canvas/src/constants.ts, packages/infinite-canvas/src/components/pipeline/__tests__/pipelineModel.test.ts</files>
  <read_first>
    - packages/infinite-canvas/src/components/pipeline/model.ts 全文（重点 :24-80 注册表与注释、:150-170 phaseIndexOf、:370-400 derivePipelineModels/extras）
    - packages/infinite-canvas/src/constants.ts:315-335（PhaseGroup/PHASE_GROUPS 原文）
    - packages/infinite-canvas/src/constants/phaseRegistry.ts（55-01 产物，消费对象）
    - packages/infinite-canvas/src/components/canvas/laneGeometry.ts:63-111（PHASE_GROUPS 消费方式——确认派生表类型兼容，不改此文件）
  </read_first>
  <action>
先写 packages/infinite-canvas/src/components/pipeline/__tests__/pipelineModel.test.ts（vitest，行为表）：
- derivePipelineModels 对含未注册 phaseIndex（如 99 与 13——新注册表无 13）节点的图：产出含一个「未映射」条目（断言 code/name 出现「未映射」），不 throw；同索引多次出现时条目仍只有一个。
- 全部节点 phaseIndex ∈ 注册表集合时：零「未映射」条目。
- console.warn 被调用且按索引聚合（vi.spyOn(console,'warn') 断言未知索引 99 只 warn 一条含 99 的消息）。
- PIPELINE_PHASES 长度 === 22 且 P09c/P12a/P12b/P11a0 在列（防止退回内联旧表）。
- PHASE_GROUPS 派生断言：PHASE_REGISTRY 每条 e => PHASE_GROUPS[e.phaseIndex] === e.group；PHASE_GROUPS[5] 与 PHASE_GROUPS[13] 为 undefined（注销 lane 不再映射——未注册索引走兜底）。

实现（RED→GREEN）：
1. model.ts：删除 :46-80 内联 PIPELINE_PHASES 数组，改为 `import { PHASE_REGISTRY } from '../../constants/phaseRegistry'` + `export const PIPELINE_PHASES: readonly PipelinePhaseDef[] = PHASE_REGISTRY`（保持既有导入方零改动）；PipelinePhaseDef 类型改为从 phaseRegistry re-export（本地 interface 删除或 `export type { PipelinePhaseDef } from '../../constants/phaseRegistry'`）；修正文件头与 :5/:40 的「17 阶段」陈旧注释为「22 phase 单一注册表（src/constants/phaseRegistry，khs _PHASE_INDEX_MAP 契约守护）」。
2. derivePipelineModels extras 升级（D-03）：未注册 phaseIndex 的追加条目改为 `{ sortKey: 1000 + idx, code: 'P?', name: '未映射', group: PHASE_GROUPS[idx] ?? 'post', phaseIndex: idx, unmapped: true }`；console.warn 聚合为每个未知索引一条（消息前缀 `[pipeline] 未映射 phaseIndex:`，含该索引节点计数；用模块级 Set 去重防止每帧重复 warn）。不 throw。
3. constants.ts：删除 :324-331 PHASE_GROUPS 字面量表，改为 `import { PHASE_REGISTRY } from './constants/phaseRegistry'` + 派生 `export const PHASE_GROUPS: Record<number, PhaseGroup> = Object.fromEntries(PHASE_REGISTRY.map((e) => [e.phaseIndex, e.group]))`；PhaseGroup 类型改为从 './constants/phaseRegistry' re-export（本地 type 定义删除）。注销索引 5/13 不再出现在派生表（消费方 laneGeometry 有 ?? 'production' 兜底；未注册索引节点本身已进「未映射」条目）。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx vitest run src/components/pipeline/__tests__/pipelineModel.test.ts src/constants/__tests__ && npm test && npx tsc -b --pretty</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sortKey: 1, code: 'P01'" packages/infinite-canvas/src/components/pipeline/model.ts` === 0 且 `grep -c "from '../../constants/phaseRegistry'" packages/infinite-canvas/src/components/pipeline/model.ts` ≥ 1（内联表已删、单源已接）。
    - `grep -c "10: 'production'" packages/infinite-canvas/src/constants.ts` === 0 且 `grep -c "Object.fromEntries(PHASE_REGISTRY.map" packages/infinite-canvas/src/constants.ts` === 1（PHASE_GROUPS 派生化）。
    - `grep -c "未映射" packages/infinite-canvas/src/components/pipeline/model.ts` ≥ 2（命名条目 + warn 消息）。
    - 全量 `npm test` 绿（canvasState.test.ts 等既有回归不破）。
  </acceptance_criteria>
  <done>前端 phase 词汇单源化（旧表删除），未知 phaseIndex fail-loud 不崩进入「未映射」条目。</done>
</task>

<task type="auto">
  <name>Task 2: import-from-dir.ts 后端 zone 表消费注册表 + phaseIndex 写点修正</name>
  <files>src/routes/canvas/v2/import-from-dir.ts</files>
  <read_first>
    - src/routes/canvas/v2/import-from-dir.ts 全文相关区：:1-27（import 区——注意既有 ../../../../schema/generated 跨界相对 import 先例）、:78-145（PhaseDef/PHASE_DEFS/PHASE_DEF_MAP/FILE_TO_PHASE/ASSET_DIR_TO_PHASE 三表）、:225-240（FILE_TO_PHASE 最长前缀排序处）、:620-670（zone/summary 写点）、:830-855（artifact 写点）
    - packages/infinite-canvas/src/constants/phaseRegistry.ts（消费对象：khsPrefix/prefix/canvasType/assetType/label/phaseIndex）
    - /data/workspace/kais-hermes-skills/plugins/kais_aigc/canvas_graph.py:741 ZONE_PHASES（lane 内顺序与 label 文案对照）
  </read_first>
  <action>
1. 删除 :81-107 的 PhaseDef 接口、PHASE_DEFS 13 条表与 PHASE_DEF_MAP；改为 `import { PHASE_REGISTRY } from "../../../../packages/infinite-canvas/src/constants/phaseRegistry"`（相对路径跨界，先例同 schema/generated；该模块零依赖故双 tsconfig 均可编译）。按需保留本地派生：`const PHASE_DEF_MAP = Object.fromEntries(PHASE_REGISTRY.map((p) => [p.prefix, p]))`（键为 lane prefix，p11a0 经 prefix='p11a' 折叠加 p11a 主条目——与 khs _PHASE_PREFIX_RE 折叠语义一致）。def.label/canvasType/assetType/phaseGroup 分别的取值改为 e.label/e.canvasType/e.assetType/e.group。lane 顺序（ZONE_X_STEP 布局几何）改由 PHASE_REGISTRY sortKey 排序驱动——布局坐标系不变，只是条目从 13 → 22。
2. phaseIndex 写点修正（binding constraint 8）：:639 zone 节点 `phaseIndex: laneIndex + 1` → `phaseIndex: def.phaseIndex`；:657 summary 与 :842 artifact 同理——三处一律取 PHASE_DEF_MAP 命中条目的 phaseIndex（khs 编号），绝不由数组下标推导。laneIndex 仅继续用于 baseX 布局。
3. FILE_TO_PHASE 增补（保持最长前缀匹配既有排序逻辑）：追加 p035/p09b/p09c/p10c/p11a0/p11a/p11b/p11c/p12a/p12b/p14/p15 各自的简单前缀条目（形如 { filePrefix: "p035", phasePrefix: "p035" }；p11a0 用 phasePrefix "p11a"（lane 折叠）；p10_voice/p11_video/p11_prompt 等既有具体条目保留在前缀表前部不动）。
4. ASSET_DIR_TO_PHASE 增补（保守）：P12_composite 与 output 由 p12 改指 p12a（时间线合成承接合成产物）；narration/audio/voice 保持 p10（语音域不动，避免误路由 p12b）；追加 { dirPrefix: "mix", phasePrefix: "p12b" } 与 { dirPrefix: "bgm", phasePrefix: "p12b" }；P11 既有条目保持。
5. 未命中任何前缀的文件走既有忽略路径不动；确保新表下 `npx tsc`（根仓）编译干净。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc -b --pretty && grep -n "phaseIndex: def.phaseIndex" src/routes/canvas/v2/import-from-dir.ts && grep -c "laneIndex + 1" src/routes/canvas/v2/import-from-dir.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const PHASE_DEFS" src/routes/canvas/v2/import-from-dir.ts` === 0（旧表删除）且 `grep -c "PHASE_REGISTRY" src/routes/canvas/v2/import-from-dir.ts` ≥ 2（import + 派生消费）。
    - `grep -c "laneIndex + 1" src/routes/canvas/v2/import-from-dir.ts` === 0（错位写点全清；laneIndex 仍可存在于 baseX 计算）。
    - `grep -c "p09c\|p12a\|p12b" src/routes/canvas/v2/import-from-dir.ts` ≥ 3（词汇表增补可见）。
    - 根仓 tsc 编译干净（跨界 import 生效）。
  </acceptance_criteria>
  <done>后端 zone 写入器 22-phase 化：phaseIndex 为 khs 编号，新导入数据的 zone/泳道归组与注册表一致。</done>
</task>

<task type="auto">
  <name>Task 3: adapter 导入断言——22 phaseIndex 合成图零未映射（成功标准 1 fixture 口径）</name>
  <files>packages/infinite-canvas/src/v3/__tests__/adapter.test.ts</files>
  <read_first>
    - packages/infinite-canvas/src/v3/__tests__/adapter.test.ts 全文（既有 fixture 构造手法与 -t phase 可命中的用例命名习惯）
    - packages/infinite-canvas/src/v3/adapter.ts:380-408（buildPhaseCatalog：zone phaseIndex/phaseName 胜出语义）
    - packages/infinite-canvas/src/components/pipeline/model.ts（Task 1 后的 derivePipelineModels——断言消费方）
  </read_first>
  <action>
在 adapter.test.ts 追加一个名字含 "phase" 的用例（保证 `npx vitest run src/v3/__tests__/adapter.test.ts -t phase` 直接命中）：
1. 构造合成 V2 图：对 PHASE_REGISTRY 全部 22 个条目，各生成 1 个 asset 节点（phaseIndex = e.phaseIndex）+ 1 个对应 zone 节点（type 'zone'，phaseIndex 同值、phaseName = e.label）；节点 id 唯一，构造手法复用该文件既有 helper。
2. 断言：经 adaptV2Graph → derivePipelineModels 消费链后，「未映射」（unmapped/name 含 未映射）条目数为 0；phaseCatalog 条目覆盖 22 条目各自的 phaseIndex 且 phaseName 与 e.label 一致（zone 胜出语义）。
3. 反向对照用例：同图追加一个 phaseIndex 99 的节点 → 「未映射」条目数 === 1（兜底真实承接，D-03 数据现实——存量 pre-W6 图走此路径不算失败，断言口径限定新导入，A3）。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx vitest run src/v3/__tests__/adapter.test.ts -t phase && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/v3/__tests__/adapter.test.ts -t phase` 全绿，用例内断言 22 覆盖 + 0 未映射 + 99 → 1 未映射。
    - 生产库全量 episode 导入的「未映射区为空」留待 UAT（55-VALIDATION Manual-Only 表 A1 行）——本 task 是其自动化 fixture 镜像。
  </acceptance_criteria>
  <done>成功标准 1 的可自动化口径闭合：新导入（合成全量 22 phase）零未映射，未知索引兜底可验。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 导入目录文件名/目录名 → phase 归组 | import-from-dir 扫描用户提供的目录树（既有端点内数据，无新路由） |
| 图数据 phaseIndex → 派生模型 | 不可信整数，可能越界/非有限 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-02 | Tampering | phaseIndex 污染（图数据注入超大/NaN index） | mitigate | D-03 未映射兜底条目 + 既有 phaseIndexOf Number.isFinite 守卫保留不动；Task 1 用例断言不 throw |
| T-55-03 | Tampering | 跨界 import 引入前端运行时依赖到后端 | mitigate | phaseRegistry 零外部 import（55-01 acceptance 已锁），根仓 tsc 编译门在 verify 内 |
| T-55-SC | Tampering | npm/pip 安装 | accept | 零新增依赖，无安装任务 |
</threat_model>

<verification>
- `cd packages/infinite-canvas && npm test && npx tsc -b --pretty`
- `cd /data/workspace/kais-aigc-platform && npx tsc -b --pretty && npm run verify:phase-55`（契约在消费迁移后仍绿）
- D-04 守卫 grep：`grep -rn "PIPELINE_PHASES:" packages/infinite-canvas/src | grep -v phaseRegistry` 无内联字面量残留；`grep -c "const PHASE_DEFS" src/routes/canvas/v2/import-from-dir.ts` === 0
</verification>

<success_criteria>
- 三张旧表删除、单源消费（D-04/binding constraint 1）；import-from-dir phaseIndex=khs 编号（constraint 8）。
- 未映射兜底 fail-loud 不崩（D-03）+ 合成 fixture 零未映射断言（A1/A3 口径）。
- 全量 vitest + 双根 tsc + verify:phase-55 三绿；Phase 54 泳道高亮消费链（laneGeometry→PHASE_GROUPS）类型兼容不回归。
</success_criteria>

<output>
Create `.planning/phases/55-navigation-scale/55-03-SUMMARY.md` when done
</output>
