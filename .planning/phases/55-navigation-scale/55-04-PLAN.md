---
phase: 55-navigation-scale
plan: 04
type: execute
wave: 2
depends_on: ["55-02"]
files_modified:
  - packages/infinite-canvas/src/utils/placeNewAsset.ts
  - packages/infinite-canvas/src/components/__tests__/placeNewAsset.test.ts
  - packages/infinite-canvas/src/components/canvas/SearchNavigator.tsx
  - packages/infinite-canvas/src/components/__tests__/searchNavigator.test.ts
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
  - packages/infinite-canvas/src/hooks/useCanvasSocket.ts
  - packages/infinite-canvas/src/store/canvasStore.ts
  - packages/infinite-canvas/src/v3/adapter.ts
  - packages/infinite-canvas/src/main.tsx
autonomous: true
requirements: [NAV-03, NAV-04]
must_haves:
  truths:
    - "按 `/` 打开搜索导航器（输入框聚焦时按 `/` 不误触——Pitfall 7），结果按场景分组呈现，点击/Enter 聚焦跳转对应节点"
    - "搜索期间不再隐藏任何非命中节点（hidden 过滤 useEffect 整段删除——Do-Not-Regress 3）"
    - "socket node:created 新资产不再随机散布：有服务端 position 用之，否则落在当前视口中心（UI-SPEC §6 偏移与网格对齐）；写回走 canonical store action（WRITE-03），不再 setNodes 直写"
  artifacts:
    - path: "packages/infinite-canvas/src/utils/placeNewAsset.ts"
      provides: "有界落点纯函数（视口中心/事件源旁两模式）"
      exports: ["placeNewAsset"]
    - path: "packages/infinite-canvas/src/components/canvas/SearchNavigator.tsx"
      provides: "搜索导航器浮层（UI-SPEC §1 契约实现 + 导出纯派生 deriveSearchResults）"
      exports: ["deriveSearchResults", "default"]
    - path: "packages/infinite-canvas/src/store/canvasStore.ts"
      provides: "addNodeFromSocket canonical 写回 action"
    - path: "packages/infinite-canvas/src/main.tsx"
      provides: "testMode 门控 e2e 桥扩张：__kaisCanvas.addNodeForTest / getViewCenter（55-07 消费；生产 bundle 桥面零变化——W2 裁决）"
  key_links:
    - from: "packages/infinite-canvas/src/components/canvas/SearchNavigator.tsx"
      to: "packages/infinite-canvas/src/store/canvasStore.ts"
      via: "setFocusAssetNodeId(id) 触发既有聚焦 effect（fitView 600/maxZoom 1.5/1.5s 清空——语义不改）"
      pattern: "setFocusAssetNodeId"
    - from: "packages/infinite-canvas/src/components/FlowCanvas.tsx"
      to: "packages/infinite-canvas/src/utils/placeNewAsset.ts"
      via: "onNewAsset handler 计算 position 后调 store.addNodeFromSocket"
      pattern: "placeNewAsset"
    - from: "packages/infinite-canvas/src/main.tsx"
      to: "packages/infinite-canvas/src/store/canvasStore.ts"
      via: "testMode 门控桥 addNodeForTest → addNodeFromSocket（同一 canonical 防线，不绕过）"
      pattern: "addNodeFromSocket"
---

<objective>
NAV-03 搜索导航器 + NAV-04 新资产落点，两者都落在 FlowCanvas 交互层所以合一个 plan：删除 Phase 45 隐藏式搜索过滤，新建 SearchNavigator（`/` 快捷键、场景分组结果列表、focusAssetNodeId 跳转）；重写 onNewAsset socket handler（payload {node} 适配 + 有界落点 + canonical 写回）。

Purpose: 93 镜规模下隐藏式搜索让画布「闪空」不可用；随机散布的新节点在整理布局前破坏空间语义。两者是本期签名交互面（UI-SPEC BINDING）。
Output: placeNewAsset 纯函数+测试、SearchNavigator+派生测试、FlowCanvas 搜索/落点重写、socket payload 修正、addNodeFromSocket store action、main.tsx testMode 桥扩张（addNodeForTest/getViewCenter——55-07 e2e 消费）。

NAV-04 discretion 裁决记录（CONTEXT 授予，checker 认可的 OR 语义决议）：落点优先级 = 服务端 position 有限即用（真相优先）→ 否则视口中心（placeNewAsset center 分支）；事件源旁落点（source+24/−16）以纯函数+单测完整交付且接线就绪，但本期无客户端 createNode 调用点可传事件源（canvasApi.createNode 无组件消费方——RESEARCH 已证），故 live 触发路径为「服务端 position → 视口中心」两档，源旁分支保留为已测代码路径（Task 3 action 4 含「grep 到调用方则接线」条件项）。断言口径 = 坐标与视口/源距离有界（Task 1 单测 + 55-07 e2e ≤64px）。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/55-navigation-scale/55-CONTEXT.md
@.planning/phases/55-navigation-scale/55-RESEARCH.md
@.planning/phases/55-navigation-scale/55-PATTERNS.md
@.planning/phases/55-navigation-scale/55-UI-SPEC.md
@.planning/phases/55-navigation-scale/55-02-SUMMARY.md

<interfaces>
<!-- 要删除的搜索过滤（FlowCanvas.tsx:608-633，Phase 45 TEXT-03） -->
useEffect → setTimeout 200ms → setNodes(nds => nds.map(n => ({...n, hidden: !matches})))，匹配 label/description/prompt；searchQuery state 于 :113，工具栏输入框于 :957 附近
<!-- 复用勿改的聚焦 effect（FlowCanvas.tsx:700-721） -->
focusAssetNodeId effect：target = nodes.find(id) → setSelectedNode+setDetailNode → 50ms 后 fitView({nodes:[{id}], duration:600, maxZoom:1.5}) → 1500ms setFocusAssetNodeId(null)；未找到 toast「该资产尚未放置在画布上」
store: setFocusAssetNodeId（canvasStore.ts:1016-1017）→ SearchNavigator 跳转唯一入口
<!-- `/` 键守卫先例（FlowCanvas.tsx:726-735 Escape handler） -->
window keydown + e.key 判断 + modal 开启早退（useVariantPickerStore.getState().open 等）——`/` 版本另需排除 e.target 为 input/textarea/contentEditable（Pitfall 7）
<!-- 要重写的 onNewAsset（FlowCanvas.tsx:217-229）与同源 canonical 先例（:208-213） -->
现 handler: setNodes append + LAYOUT.NEW_NODE_* 随机坐标（反模式：直写派生缓存 + 随机散布）
canonical 先例: applySocketNodeState/applySocketNodePreview = useCanvasStore selector + store action
canvasStore.ts:441 applyGraphTransform(fn: (g: FlowGraphV3) => FlowGraphV3)（graph 为 null 早退）
<!-- payload 错配（Pitfall 3） -->
server src/routes/canvas/v2/nodes.ts:90/151 broadcastToProject(projectId, "node:created", { node })（node 为 upsert 的 V2 FlowNodeV2）
client useCanvasSocket.ts:153 socket.on('node:created', (payload: { nodeId, data }) => ...) —— 解构 undefined；Q4 裁决：client 适配 {node}，后端零改动
<!-- testMode 桥现状（main.tsx:8-24，Task 3.6 扩张对象） -->
if (URLSearchParams(window.location.search).has('testMode')) 内挂载 window.__kaisCanvas = { setSelectedNodeIds, getSelectedNodeIds, getOrchestration, getNodes, getEdges, showToast, io }——生产 bundle 默认 noop/不挂载；e2e helpers loadCanvas 已带 testMode: '1' 且 waitForFunction(__kaisCanvas)
<!-- 场景分组口径（55-02 产物） -->
src/utils/sceneGrouping.ts: sceneNumOf / sceneColorOf / SCENE_COLORS
<!-- 视口先例 -->
FlowCanvas.tsx:349-355 getViewport（reactFlow.getViewport()）；reactFlow.screenToFlowPosition 可用（@xyflow/react 12）
canvasStore state viewport 运行时镜像（P17：{ x, y, zoom } | null）——getViewCenter 换算基座
UI-SPEC §1 SearchNavigator / §6 落点契约（BINDING）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: placeNewAsset 纯函数 + deriveSearchResults 纯派生（Wave 0 测试对）</name>
  <files>packages/infinite-canvas/src/utils/placeNewAsset.ts, packages/infinite-canvas/src/components/__tests__/placeNewAsset.test.ts, packages/infinite-canvas/src/components/canvas/SearchNavigator.tsx, packages/infinite-canvas/src/components/__tests__/searchNavigator.test.ts</files>
  <read_first>
    - packages/infinite-canvas/src/constants.ts:100-108（LAYOUT.NEW_NODE_* —— 被替换的反模式，仅读对照）
    - packages/infinite-canvas/src/utils/sceneGrouping.ts（55-02 产物，deriveSearchResults 消费）
    - packages/infinite-canvas/src/components/__tests__/StoryboardTimeline.shotKey.test.ts（纯函数 it.each 行为表范式）
    - .planning/phases/55-navigation-scale/55-UI-SPEC.md §1（索引范围：label/shot_id/prompt 摘要 + raw 白名单字段）与 §6（源旁 +24/−16 视口中心 8px 网格）
  </read_first>
  <action>
先写两份测试再实现（RED→GREEN）：

1. packages/infinite-canvas/src/utils/placeNewAsset.ts —— `export interface PlaceNewAssetOptions { sourcePosition?: { x: number; y: number } | null; viewportCenter: { x: number; y: number }; anchor?: 'source' | 'center' }` + `export function placeNewAsset(opts): { x: number; y: number }`：
   - anchor='source' 且 sourcePosition 有限：返回 source + (+24, −16)（右上偏移，UI-SPEC §6），坐标按 4px 网格对齐（Math.round(v/4)*4）。
   - 否则（anchor='center' 或 source 缺失/非有限）：返回 viewportCenter 按 8px 网格对齐。
   - 全分支对 NaN/Infinity 输入防御（非有限 → 视为缺失，走 center 分支）。
   placeNewAsset.test.ts 断言：source 分支 |pos − source| 各轴 ≤ 24/16 且两轴均为 4 的倍数；center 分支 |pos − center| 各轴 ≤ 4（8px 网格余数）且为 8 的倍数；sourcePosition 传 NaN 时走 center；有界常量 R 断言（与视口/源距离有界——成功标准 4 的纯函数口径）。

2. packages/infinite-canvas/src/components/canvas/SearchNavigator.tsx 本 task 先只落纯派生部分（组件壳与 UI 在 Task 2 完成）：`export interface SearchHit { nodeId: string; label: string; sub: string; sceneNum: number | null; kind: 'shot' | 'other' }` + `export function deriveSearchResults(nodes: Array<{ id: string; data?: Record<string, unknown> }>, query: string, rawDataByNodeId?: Record<string, Record<string, unknown>>): Array<{ sceneNum: number | null; title: string; hits: SearchHit[] }>`：
   - 空/纯空白 query 返回空数组（打开态骨架由组件层处理，不在此）。
   - 索引字段（A4 口径）：data.label、data.shot_id（或 raw.shot_id）、data.prompt、raw.video_prompt/ltx_prompt（经 rawDataByNodeId）、data.description——大小写不敏感子串。
   - 分镜类节点（data.shot_id 或 raw.shot_id 存在）按 sceneNumOf(shot_id) 分组，组按 sceneNum 升序；非分镜命中归入 sceneNum=null 的末组。
   - 不修改输入节点（无 hidden 写入——Do-Not-Regress 3 的纯函数面）；上限保护：命中超过 200 条时截断并在返回结构上带 truncated 标记。
   searchNavigator.test.ts 断言：多场景命中分组正确且升序；非 shot 节点进末组；query 命中 video_prompt（raw 穿透）；空 query → []；输入数组对象不被 mutate（深比较快照）；200+ 截断标记。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx vitest run src/components/__tests__/placeNewAsset.test.ts src/components/__tests__/searchNavigator.test.ts && npx tsc -b --pretty</automated>
  </verify>
  <acceptance_criteria>
    - 两测试文件全绿（placeNewAsset ≥ 6 用例、deriveSearchResults ≥ 6 用例）。
    - `grep -c "Math.random" packages/infinite-canvas/src/utils/placeNewAsset.ts` === 0（随机散布零残留于新函数）。
    - deriveSearchResults 不含 "hidden"（`grep -c "hidden" ...` === 0）。
  </acceptance_criteria>
  <done>NAV-04 落点契约与 NAV-03 结果派生以纯函数+单测锁定（55-VALIDATION Wave 0 两缺口闭合）。</done>
</task>

<task type="auto">
  <name>Task 2: SearchNavigator 组件 + FlowCanvas 接线（删 hidden 过滤 + `/` 快捷键）</name>
  <files>packages/infinite-canvas/src/components/canvas/SearchNavigator.tsx, packages/infinite-canvas/src/components/FlowCanvas.tsx</files>
  <read_first>
    - .planning/phases/55-navigation-scale/55-UI-SPEC.md §1（形态/结构/键盘/aria/文案——BINDING）与 §Copywriting Contract（空态与键位提示原文）
    - packages/infinite-canvas/src/components/FlowCanvas.tsx:600-633（要整段删除的 hidden 过滤 useEffect）、:700-735（聚焦 effect 与 Escape 守卫——`/` 监听照此形态）、:950-965（工具栏搜索输入框）
    - packages/infinite-canvas/src/components/canvas/ShotTree.tsx:153-169（浮层容器样式先例：--cv-bg-panel/--cv-shadow-pop/backdropFilter blur）与 :282-364（列表行/折叠节头模式）
    - packages/infinite-canvas/src/theme/tokens.css（.cv-search-input 既有类与 focus-visible 规则——不得删）
  </read_first>
  <action>
1. SearchNavigator.tsx 组件本体（data-testid="search-navigator"）：
   - Props { open: boolean; onClose: () => void; initialQuery?: string }。open 且无输入时显示全部分组骨架（deriveSearchResults 空结果时组件自行用全量 shot 节点按场景分组的骨架列表——直接复用 deriveSearchResults 以空格查询的退化路径或单独 derive，实现取简）。
   - 形态：top 15vh 居中，宽 min(560px, 90vw)，--cv-bg-panel + --cv-shadow-pop + backdropFilter blur(4px)；面板 max-height 60vh 内部滚动（93 镜规模约束）。`role="dialog"` + `aria-label="搜索导航器"`。
   - 输入框复用 .cv-search-input 类与既有样式；placeholder「搜索镜头 / 场景 / 节点…」尾缀 kbd 样式 `/`。
   - 结果列表按 deriveSearchResults 分组渲染：sticky 场景头（8px×8px 圆角色点 sceneColorOf + mono 场景号 + 计数，末组「其他资产」用 --cv-lane-label 灰点）；行高 ≥ 28px；行首色点、label 11px、sub 10px tertiary。
   - 键盘：↑↓ 跨组移动（跳过组头）、Enter 跳转、Esc 关闭（onClose）；激活行左侧 2px 实线 + rgba(237,238,241,0.10) 底（ShotTree active 同款）。
   - 点击/Enter → setFocusAssetNodeId(hit.nodeId)（既有 effect 语义不改）+ onClose()。
   - 空态：heading「未找到匹配项」body「试试场景号（如 S03）、景别（近景 / 特写）或运镜（推镜 / 摇镜）关键词」；底部键位提示条「↑↓ 选择 · Enter 跳转 · Esc 关闭」10px tertiary。
   - 客户端即时过滤（无 debounce；>100ms 视为实现缺陷）。
2. FlowCanvas.tsx：
   - 删除 :608-633 hidden 过滤 useEffect 整段及 searchQuery 与之的关联（searchQuery state 可保留为导航器初值或删除——以「工具栏输入框保留为入口」为准：输入框 onFocus/点击 → 打开导航器并把已输入文本作为 initialQuery；输入框不再有任何 setNodes(hidden) 路径）。
   - 新增本地 state searchNavOpen；`/` 全局 keydown useEffect（照 :726-735 Escape 形态）：e.key === '/' 且 e.target 不是 input/textarea/contentEditable（用 tagName/contentEditable 检查）且 variant picker 等 modal 未开 → preventDefault + 打开导航器；Esc 在导航器开着时关闭（导航器内部处理后 stopPropagation 或此处协调，取简）。
   - 渲染 <SearchNavigator open={searchNavOpen} ... /> 于画布视图层（viewMode === 'canvas' 时）。
3. 设计检查步（SUMMARY 记录）：对照 UI-SPEC §1 逐条（浮层形态/键盘流/aria/文案/60vh 上限/无 93 行平铺——按场景分组 sticky 头）+ §Typography/§Spacing（28px 行高下限、t3/t4 字号）+ Do-Not-Regress 2/3/5。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx tsc -b --pretty && npx vitest run src/components/__tests__ && grep -c "hidden: !matches" src/components/FlowCanvas.tsx; true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hidden:" packages/infinite-canvas/src/components/FlowCanvas.tsx` === 0（隐藏式搜索路径全删——Do-Not-Regress 3）。
    - `grep -c "role=\"dialog\"" packages/infinite-canvas/src/components/canvas/SearchNavigator.tsx` ≥ 1 且 `grep -c "aria-label=\"搜索导航器\"" ...` ≥ 1。
    - `grep -c "setFocusAssetNodeId" packages/infinite-canvas/src/components/canvas/SearchNavigator.tsx` ≥ 1。
    - `grep -cE "#[0-9A-Fa-f]{6}" packages/infinite-canvas/src/components/canvas/SearchNavigator.tsx` === 0（token-only，rgba 例外仅 UI-SPEC 指定的 0.10 激活底）。
    - 既有 vitest 套件全绿；FlowCanvas 中 focusAssetNodeId effect 区块（fitView duration 600/maxZoom 1.5/1500ms 清空）源码未被改动（diff 审）。
  </acceptance_criteria>
  <done>`/` 打开导航器 → 场景分组结果 → Enter/点击聚焦跳转；搜索期间画布节点零隐藏。</done>
</task>

<task type="auto">
  <name>Task 3: onNewAsset 重写——{node} payload 适配 + 有界落点 + canonical 写回 + testMode 桥</name>
  <files>packages/infinite-canvas/src/hooks/useCanvasSocket.ts, packages/infinite-canvas/src/store/canvasStore.ts, packages/infinite-canvas/src/v3/adapter.ts, packages/infinite-canvas/src/components/FlowCanvas.tsx, packages/infinite-canvas/src/main.tsx</files>
  <read_first>
    - packages/infinite-canvas/src/hooks/useCanvasSocket.ts:100-165（socket.on 注册区；node:created 于 :153-155）
    - src/routes/canvas/v2/nodes.ts:60-160（server broadcastToProject("node:created", { node }) 的 node 形状——FlowNodeV2 upsert 结果）
    - packages/infinite-canvas/src/store/canvasStore.ts:441-460（applyGraphTransform/loadGraphFromV2 的 rawDataByNodeId 设置手法——addNodeFromSocket 照此形状）
    - packages/infinite-canvas/src/v3/adapter.ts（adaptV2Graph 内 V2→V3 节点级转换逻辑——提炼或复用；loadGraphFromV2 整图路径不可用于单节点增量）
    - packages/infinite-canvas/src/components/FlowCanvas.tsx:217-229（被删除的 onNewAsset）与 :349-355（getViewport 先例）
    - packages/infinite-canvas/src/utils/placeNewAsset.ts（Task 1 产物）
    - packages/infinite-canvas/src/main.tsx:1-30（__kaisCanvas testMode 门控块 :8-24——第 6 步扩张对象；helpers.mjs loadCanvas 已带 testMode: '1'）
  </read_first>
  <action>
1. useCanvasSocket.ts：node:created 监听改为 payload { node }（server 形状，Q4 裁决——后端零改动）；回调签名 onNewAsset(node)（node 含 id/position/data 等 V2 字段；类型放宽为携带可选 position 的形状即可，不引 FlowGraphV2 类型依赖）。
2. adapter.ts：若不存在节点级转换则导出 `adaptV2Node(node): { v3Node, raw } | null`——从 adaptV2Graph 的单节点映射逻辑提炼（meta/raw 拆分、类型规整、zod 宽松校验，坏节点返回 null 不 throw——fail-loud 不崩同源哲学，warn 归调用方）。adaptV2Graph 改为内部复用该函数（行为不变，shotKey/adapter 既有测试护住）。
3. canvasStore.ts 新增 action `addNodeFromSocket(node, position)`：adaptV2Node 失败 → console.warn('[canvasStore] node:created 无法适配', node.id) 并早退；成功 → set(state => ({ rawDataByNodeId: { ...state.rawDataByNodeId, [id]: raw } })) + applyGraphTransform(g => ({ ...g, nodes: [...g.nodes, v3Node with position] }))。绝不动 setNodes/派生缓存（WRITE-03）。
4. FlowCanvas.tsx onNewAsset 重写：删除 LAYOUT.NEW_NODE_* 随机坐标与 setNodes append；新实现：
   - position 决策：payload node.position 有限 → 直接用（服务端真相）；否则 placeNewAsset({ sourcePosition: null, viewportCenter: reactFlow.screenToFlowPosition({ x: window.innerWidth/2, y: window.innerHeight/2 }), anchor: 'center' })。（事件源旁分支已在纯函数就绪；当前无客户端 createNode 调用点传源，canvasApi.createNode 存在但无组件调用方——若 grep 到调用方则在其调用前经 store 记 source 并传入，否则仅保留视口中心路径。）
   - 调 useCanvasStore.getState().addNodeFromSocket(node, position)；随后 setFocusAssetNodeId(node.id)（到达高亮 1.5s + fitView——UI-SPEC §6「到达后复用 --cv-glow-select 1.5s 提示」即既有聚焦高亮路径）。
   - 若节点已存在（重播/幂等）：addNodeFromSocket 内按 id 查重，已存在则只更新 rawData 并 warn 一条，不重复 append。
5. 验证编译与既有回归；LAYOUT.NEW_NODE_* 常量本体本 task 不删（constants.ts 归 55-03/55-07 文件所有权，避免并行冲突），仅 FlowCanvas 引用清零。
6. main.tsx testMode 桥扩张（W2 裁决——55-07 e2e 消费，生产面零变化）：在既有 `if (new URLSearchParams(...).has('testMode'))` 门控块内（:11-24）追加两个桥方法，绝不移出守卫：
   - `getViewCenter: () => ({ x, y } | null)`——读 useCanvasStore.getState().viewport 运行时镜像（P17）；镜像为 null 或 zoom ≤ 0 → null；否则按 screenToFlowPosition 等价数学换算 flow 坐标视口中心：x = v.x + window.innerWidth / (2 * v.zoom)，y = v.y + window.innerHeight / (2 * v.zoom)。
   - `addNodeForTest: (node) => boolean`——位置决策与 FlowCanvas onNewAsset 完全同一顺序（node.position 有限 → 用之；否则 placeNewAsset({ sourcePosition: null, viewportCenter: getViewCenter() ?? { x: 0, y: 0 }, anchor: 'center' })），随后调 useCanvasStore.getState().addNodeFromSocket(node, position) 并返回是否新增（幂等查重结果）。桥内不调 setFocusAssetNodeId——视口保持不动，支撑 55-07 的「新节点坐标与视口中心 ≤64px」有界断言（聚焦行为已由 search-jump 用例经 UI 交互覆盖）。防线不绕过：addNodeForTest 走 addNodeFromSocket 的同一 zod 校验 + id 查重 + warn 早退。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx tsc -b --pretty && npx vitest run src/v3/__tests__ src/components/__tests__ && grep -c "NEW_NODE" src/components/FlowCanvas.tsx; true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NEW_NODE" packages/infinite-canvas/src/components/FlowCanvas.tsx` === 0 且 `grep -c "Math.random" packages/infinite-canvas/src/components/FlowCanvas.tsx` === 0。
    - `grep -c "addNodeFromSocket" packages/infinite-canvas/src/store/canvasStore.ts` ≥ 2（接口声明 + 实现）且该实现内无 setNodes（`grep -A20 "addNodeFromSocket:" src/store/canvasStore.ts | grep -c "setNodes"` === 0）。
    - useCanvasSocket.ts node:created 分支消费 payload.node（`grep -c "payload.node\|payload?.node" src/hooks/useCanvasSocket.ts` ≥ 1）且不再解构 nodeId/data。
    - `grep -c "adaptV2Node" packages/infinite-canvas/src/v3/adapter.ts` ≥ 2（导出 + adaptV2Graph 复用）。
    - `grep -c "addNodeForTest" packages/infinite-canvas/src/main.tsx` ≥ 1 且新增桥方法位于既有 testMode 守卫块内（`grep -n "has('testMode')" src/main.tsx` 行号 < addNodeForTest 首现行号——生产 bundle __kaisCanvas 桥面零变化）；`grep -c "getViewCenter" src/main.tsx` ≥ 1。
    - adapter 既有测试全绿——节点级提炼零回归（条件子句：55-03 与本 plan 同 wave 无执行依赖——若 55-03 已先执行则含其 adapter.test.ts -t phase 用例；未执行时以 adapter.test.ts 既有用例为准，55-03 落地后由其自身 verify 覆盖）。
  </acceptance_criteria>
  <done>node:created 新节点：位置有界（服务端 position 或视口中心 8px 对齐）、canonical 写回、幂等、到达高亮；随机散布与派生缓存直写永久移除；testMode 门控桥（addNodeForTest/getViewCenter）就绪供 55-07 e2e 消费。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| socket node:created payload → store 写回 | 服务端广播数据进入客户端 canonical graph（既有鉴权房间，无新端点） |
| `/` 全局键盘监听 → 导航器 | DOM 事件面；输入框内按键不得劫持 |
| testMode 桥（main.tsx）→ store 写回 | 测试注入口；仅 ?testMode URL 参数存在时挂载，生产 bundle 无此面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-V5 | Input Validation(V5 marginal) | 搜索输入 | accept | 纯客户端过滤无注入面；`/` 监听排除 input/textarea/contentEditable 焦点（Pitfall 7，Task 2 实现项） |
| T-55-04 | Tampering | node:created payload 伪造/重播 | mitigate | addNodeFromSocket：adaptV2Node zod 宽松校验 + id 查重幂等 + 坏节点 warn 早退不 throw |
| T-55-02 | Tampering | payload.position 非有限值 | mitigate | placeNewAsset 非有限防御回退视口中心（Task 1 用例） |
| T-55-08 | Tampering | addNodeForTest 测试桥滥用（绕过 UI 向 canonical graph 注入伪造 node） | mitigate | 桥面困于既有 URLSearchParams testMode 守卫内（main.tsx:11——生产 bundle __kaisCanvas 不存在）；addNodeForTest 走 addNodeFromSocket 同一 zod 校验 + id 查重 + warn 早退，不绕过 canonical 写回防线（W2 安全注记） |
| T-55-SC | Tampering | npm 安装 | accept | 零新增依赖（cmdk 已被 RESEARCH Alternatives 表否决，自建列表） |
</threat_model>

<verification>
- `cd packages/infinite-canvas && npm test && npx tsc -b --pretty`
- 交互冒烟归 55-07 e2e（phase55-nav.mjs 覆盖 `/` 打开→跳转→落点——落点用例经本 plan 的 testMode 桥 addNodeForTest/getViewCenter）；UAT 视觉项归 55-VALIDATION Manual-Only
</verification>

<success_criteria>
- 成功标准 3：`/` 导航器 + 场景分组结果列表 + focusAssetNodeId 跳转；hidden 过滤路径源码零残留。
- 成功标准 4：新节点坐标有界（纯函数单测 + 幂等 + canonical 写回），随机散布源头删除。
- focusAssetNodeId effect 与 LOD 体系零改动（Do-Not-Regress 1/2）；全量 vitest 绿。
- testMode 桥扩张不改变生产 bundle 表面（守卫块内新增，55-07 e2e 可驱动落点链路）。
</success_criteria>

<output>
Create `.planning/phases/55-navigation-scale/55-04-SUMMARY.md` when done
</output>
