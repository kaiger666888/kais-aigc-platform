---
phase: 55-navigation-scale
plan: 06
type: execute
wave: 3
depends_on: ["55-04"]
files_modified:
  - packages/infinite-canvas/src/store/canvasStore.ts
  - packages/infinite-canvas/src/store/__tests__/selectBranchAsMain.test.ts
  - packages/infinite-canvas/src/components/BranchPanel.tsx
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
  - packages/infinite-canvas/src/components/canvas/icons.tsx
  - packages/infinite-canvas/src/hooks/useCanvasSocket.ts
autonomous: true
requirements: [NAV-06]
must_haves:
  truths:
    - "BranchPanel 重写后消费 store.branches 与 selectBranchAsMain；「升为主线」内联二次确认后执行，成功 toast 且刷新后仍为主线（REST PATCH 持久化）"
    - "selectBranchAsMain 为乐观更新 + REST 持久化 + 失败回滚（selectWinner 范式）；分支 status 真相经 V2 事件流（branch_upsert/branch:updated）合并进 store，不再被 toLegacyBranches 的硬编码 'active' 覆盖"
    - "多结局探索两段式：预览（非破坏、他支压暗 0.25 可复原）→ 升主线（确认态 3s）"
  artifacts:
    - path: "packages/infinite-canvas/src/components/BranchPanel.tsx"
      provides: "分支与结局侧栏浮层（UI-SPEC §5 契约实现）"
    - path: "packages/infinite-canvas/src/store/__tests__/selectBranchAsMain.test.ts"
      provides: "REST 逐分支调用 / 回滚 / 早退三用例"
    - path: "packages/infinite-canvas/src/store/canvasStore.ts"
      provides: "selectBranchAsMain 重写 + applyBranchUpsert status 合并"
  key_links:
    - from: "packages/infinite-canvas/src/store/canvasStore.ts"
      to: "packages/infinite-canvas/src/services/canvasApi.ts"
      via: "selectBranchAsMain 内 await canvasApi.updateBranch(projectId, episodesId, branchId, { status }) 逐分支"
      pattern: "updateBranch"
    - from: "packages/infinite-canvas/src/store/canvasStore.ts"
      to: "src/routes/canvas/v2/branches.ts"
      via: "PATCH 写 branch_upsert 事件 → 客户端 branch:updated/socket 事件回放合并 status（A5：不动 flowgraph-v3 schema）"
      pattern: "branch_upsert|applyBranchUpsert"
---

<objective>
NAV-06 分支 UI 接通：重写 BranchPanel（51-WRITE-04 已删旧版，git 7ec2e605 为档案）消费既有 branches store；把 selectBranchAsMain 从 store-only 升级为乐观更新 + REST PATCH 持久化 + 失败回滚（成功标准 5 后半）；分支 status 真相按 A5 裁决走 V2 事件流合并（不扩 flowgraph-v3 schema）。

Purpose: 多结局/多版本探索当前无 UI 且升级主线刷新即丢；REST 端点与 store 语义都已存在，本 plan 是接线与 UI 重建。
Output: store action 重写 + 单测、BranchPanel 组件、工具栏入口（新 UiIcon kind 'branch'）。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/55-navigation-scale/55-CONTEXT.md
@.planning/phases/55-navigation-scale/55-RESEARCH.md
@.planning/phases/55-navigation-scale/55-PATTERNS.md
@.planning/phases/55-navigation-scale/55-UI-SPEC.md
@.planning/phases/55-navigation-scale/55-04-SUMMARY.md

<interfaces>
<!-- store 现状（重写对象） -->
canvasStore.ts:937-953 selectBranchAsMain（store-only：updateBranch loop + toast，无 REST）；:955 起 archiveBranch（同形态，本 plan 仅在面板消费不改其持久化——超出升主线持久化的项不动）
canvasStore.ts:278-292 toLegacyBranches（V3→legacy，status 硬编码 'active'——Pitfall 4 有损 shim）
canvasStore.ts:429-432 setGraph：branches: graph.branches.length > 0 ? toLegacyBranches(graph.branches) : state.branches（REST 合并挂钩点，注释已预留）
canvasStore.ts:886-933 selectWinner（乐观+回滚范式：prevGraph 快照 → setGraph 乐观 → await REST → 失败 setGraph(prevGraph) + error toast → 成功才 success toast；:889-892 project 上下文早退守卫）
<!-- REST（全部既有，零后端改动） -->
src/routes/canvas/v2/branches.ts:81-130 PATCH /v2/canvas/branches/:branchId —— zod updates { label?, status: draft|active|paused|completed|archived|rejected, forkReason?, metadata? }；appendAndSync 写 { type: "branch_upsert", nodeId: branchId, payload } 事件 + broadcastToProject("branch:updated", { branch, changedFields })；:133-183 DELETE
services/canvasApi.ts:594-602 updateBranch(projectId, episodesId, branchId, updates: Partial<Pick<FlowBranch,'label'|'status'>>)
<!-- 事件回流通道（status 真相，A5） -->
useCanvasSocket.ts 既有 onBranchCreated 回调位 + socket 订阅/事件回放（subscribe since 增量补发）；branch:updated 广播与 branch_upsert 事件经此通道到达客户端
<!-- UI 档案与样式先例 -->
git 档案：git show 7ec2e605:packages/infinite-canvas/src/components/BranchPanel.tsx（235 行旧版——状态分区 memo/renderBranch/branchStats 结构参考）
ShotTree.tsx:153-169 侧栏浮层语法（--cv-bg-panel/backdropFilter/data-testid）；UI-SPEC §5 BranchPanel 契约 + §Copywriting（升为主线/确认文案/toast 原文）+ §Color（Accent 3/4 与 Destructive 用途限定）
icons.tsx UiIcon 既有 kind 列表（save/layout/fit/rocket/iterate/search/graph/film/image/assets/pipeline）——新 kind 'branch' 按同一几何风格内联追加
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: selectBranchAsMain 乐观+REST+回滚重写 + applyBranchUpsert status 合并 + 单测</name>
  <files>packages/infinite-canvas/src/store/canvasStore.ts, packages/infinite-canvas/src/store/__tests__/selectBranchAsMain.test.ts, packages/infinite-canvas/src/hooks/useCanvasSocket.ts</files>
  <read_first>
    - packages/infinite-canvas/src/store/canvasStore.ts:270-300（toLegacyBranches + FlowBranch 形状）、:420-460（setGraph/loadGraphFromV2）、:886-960（selectWinner 全文 + selectBranchAsMain/archiveBranch 现状）
    - packages/infinite-canvas/src/store/__tests__/selectWinner.test.ts:1-100（vi.mock canvasApi + fixture 工厂 + 断言形态——本测试直接复刻该结构）
    - packages/infinite-canvas/src/services/canvasApi.ts:583-605（updateBranch 签名）
    - packages/infinite-canvas/src/hooks/useCanvasSocket.ts:100-115（onBranchCreated 回调位——applyBranchUpsert 的接线消费点参考）
  </read_first>
  <action>
先写 packages/infinite-canvas/src/store/__tests__/selectBranchAsMain.test.ts（复刻 selectWinner.test.ts 结构：vi.mock('../../services/canvasApi') 至少 mock updateBranch；fixture 分支工厂——branches: [{id:'b-main',label:'主线A',status:'active'},{id:'b-x',label:'结局X',status:'paused'}]；store 置 projectId/episodesId 上下文）：

用例（三必 + 两守护）：
1. 升主线成功：selectBranchAsMain('b-x') → 乐观后 b-x.status==='active'、b-main.status==='archived'；canvasApi.updateBranch 被调 2 次（参数含 { status: 'active' } 与 { status: 'archived' }——仅对状态实际变化的分支发 PATCH，未变化分支不发）；await 后 toast 为成功文案（`已升主线: ${label}` 既有文案保留——UI-SPEC Copywriting「既有 store 文案保留」）。
2. REST 失败回滚：mockUpdateBranch 第 1 次 reject → 状态恢复原样（b-x 'paused'、b-main 'active'）+ toast「主线切换失败，已恢复原状，请重试」。
3. 上下文早退：store 无 projectId/episodesId 时调用 → updateBranch 零调用 + error toast，状态不变。
4. 分支不存在：selectBranchAsMain('nope') → toast「分支不存在」+ 零 REST 调用。
5. applyBranchUpsert：set({branches:[...]}含 b-main active) 后 applyBranchUpsert('b-main', { status: 'archived' }) → store.branches 中 b-main.status==='archived'；未知 id 时 warn 且不动状态。

实现（RED→GREEN）：
1. selectBranchAsMain 重写（selectWinner 范式）：早退守卫（project 上下文缺失）→ 分支存在守卫 → prevBranches = [...branches] 快照 → 乐观 set（目标 active、其余 active→archived）→ for 每个「状态变化」分支 await canvasApi.updateBranch(projectId, episodesId, id, { status }) → 任一失败：set 恢复 prevBranches + showToast('主线切换失败，已恢复原状，请重试', 'error') → 全成功才成功 toast。接口类型改 async（调用方为 UI onClick，兼容）。
2. 新增 action applyBranchUpsert(branchId: string, patch: Partial<Pick<FlowBranch,'label'|'status'>>)：按 id merge 进 state.branches（toLegacyBranches 覆盖后的运行时真相修正点，Pitfall 4 方案 b）；未知 id warn 早退不 throw。
3. status 真相合并接线（A5，最小面）：loadGraphFromV2 在 setGraph 之后不改 branches（V3 有损 shim 保持）；真实修正来自事件——useCanvasSocket 的 branch 相关回调位（onBranchCreated 既有）扩展消费 branch:updated / branch_upsert 回放 → 调 applyBranchUpsert。本 task 在 store 侧完成 action + 在 useCanvasSocket.ts 仅加一行回调透传（若该文件需改动，改动限于回调注册处，不碰 55-04 已改的 node:created 区）。FlowCanvas 回调 prop 传入 applyBranchUpsert 绑定。
4. archiveBranch 本期不改持久化（面板只消费其现有行为或不用）；DELETE 分支真实端点已存在，面板删除按钮如实现必须走真实 canvasApi.deleteBranch（禁止旧版 status:'rejected' 假删除——PATTERNS 明示的修坑点）。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx vitest run src/store/__tests__ && npx tsc -b --pretty</automated>
  </verify>
  <acceptance_criteria>
    - selectBranchAsMain.test.ts ≥ 5 用例全绿（成功/回滚/早退/不存在/applyBranchUpsert）。
    - `grep -A30 "selectBranchAsMain:" packages/infinite-canvas/src/store/canvasStore.ts | grep -c "updateBranch"` ≥ 1 且同区间无「仅 store 不发 REST」旧形态。
    - `grep -c "applyBranchUpsert" packages/infinite-canvas/src/store/canvasStore.ts` ≥ 2（接口 + 实现）。
    - 失败 toast 字符串与 UI-SPEC 原文一致：`主线切换失败，已恢复原状，请重试`（grep 命中）。
    - src/store/__tests__ 全目录绿（selectWinner 既有用例不回归）。
  </acceptance_criteria>
  <done>升主线 = 乐观上屏 + 逐分支 PATCH 持久化 + 失败整体回滚；status 真相可经 V2 事件流修正。</done>
</task>

<task type="auto">
  <name>Task 2: BranchPanel 重写 + 工具栏入口（含设计检查）</name>
  <files>packages/infinite-canvas/src/components/BranchPanel.tsx, packages/infinite-canvas/src/components/FlowCanvas.tsx, packages/infinite-canvas/src/components/canvas/icons.tsx</files>
  <read_first>
    - git show 7ec2e605:packages/infinite-canvas/src/components/BranchPanel.tsx（旧版 235 行全文——状态分区 memo/renderBranch/branchStats 结构参考；其 status:'rejected' 假删除不得复刻）
    - .planning/phases/55-navigation-scale/55-UI-SPEC.md §5（BINDING：侧栏浮层 360px、行结构、两段式交互、文案表）+ §Color（Accent 3 冷白主按钮 / Destructive 仅确认态与失败 toast / 压暗 --cv-dim-opacity 0.25）+ §State Matrix BranchPanel 行
    - packages/infinite-canvas/src/components/canvas/ShotTree.tsx:153-169（浮层容器/开合语法）与 FlowCanvas.tsx 中 ShotTree 的挂载方式（:1019 附近）
    - packages/infinite-canvas/src/components/canvas/icons.tsx（UiIcon kind 几何风格）
  </read_first>
  <action>
1. icons.tsx：新增 kind 'branch'（同一 stroke=currentColor 几何风格，14px 默认——二叉/分叉线条意象）。
2. 新建 packages/infinite-canvas/src/components/BranchPanel.tsx，data-testid="branch-panel"：
   - 侧栏浮层（ShotTree 同款 overlay 语法，宽 --cv-panel-w-min 360px），标题「分支与结局」，关闭按钮；内部列表 max-height 60vh 滚动。
   - 数据：useCanvasStore branches + selectBranchAsMain；行结构 = 分支名 + 父分支链（mono 10px `← {parent}`，parentBranchId 解析名）+ 节点计数（branchStats 同旧版 memo 手法）+ 状态徽章（仅主线行：冷白底深字「主线」，对应 status==='active'）。
   - 两段式交互（UI-SPEC §5）：
     a. 预览：行内「预览」动作 → 该分支节点保持常规样式、其余分支节点压暗 --cv-dim-opacity 0.25（经 setSelectedNodeIds/或组件层对 RF nodes 的 opacity 映射——取既有 store 能力实现，非破坏、再点或 Esc 复原；预览态不落库不持久化）。
     b. 升为主线：主按钮「升为主线」（Accent 冷白底 #EDEEF1 深字，Accent reserved-for 3）；点击进入内联确认态「确认升为主线？当前主线将归档」按钮 3s 内二次点击执行 selectBranchAsMain(branch.id)（await Task 1 的 async action，期间 disabled），超时复原；确认态文字用 Destructive #DD6A82（Color 表限定用途）。
   - 空态：heading「暂无分支」body「当前只有主线一条线路；管线产出多结局变体后可在此探索」（原文）。
   - 全交互元素 <button>（Do-Not-Regress 5）；行高 ≥ 28px。
3. FlowCanvas.tsx：工具栏（ToolbarButton 组，保存/整理/适配同排）新增 BranchPanel 开合按钮（UiIcon kind="branch" + 文案「分支」）+ 本地 state 控制浮层渲染（viewMode==='canvas' 时）。
4. 设计检查步（SUMMARY 记录）：UI-SPEC §5 + Copywriting 表逐条（升为主线/确认/toast/空态/标题原文）+ Color 限定（Accent 只出现在激活行/焦点环/主按钮/选中描边——本面板只有主按钮与主线徽章）+ 间距/字号 token-only。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx tsc -b --pretty && npx vitest run src/store/__tests__ src/components/__tests__ && grep -c "branch-panel" src/components/BranchPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "升为主线" packages/infinite-canvas/src/components/BranchPanel.tsx` ≥ 2 且确认文案 `确认升为主线？当前主线将归档` 原文命中。
    - `grep -c "selectBranchAsMain" packages/infinite-canvas/src/components/BranchPanel.tsx` ≥ 1。
    - `grep -c "rejected" packages/infinite-canvas/src/components/BranchPanel.tsx` === 0（旧版假删除不复刻）。
    - `grep -c "kind=\"branch\"\|'branch'" packages/infinite-canvas/src/components/canvas/icons.tsx` ≥ 1 且 FlowCanvas 工具栏含 BranchPanel 开合按钮。
    - `grep -cE "#[0-9A-Fa-f]{6}" packages/infinite-canvas/src/components/BranchPanel.tsx` === 0（token/常量引用，无内联新色值）。
    - tsc + 相关 vitest 全绿。
  </acceptance_criteria>
  <done>分支与结局面板可用：预览非破坏探索、升主线经确认后持久化（刷新不回弹——Pitfall 5 闭合），UI 全文按 UI-SPEC 契约。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BranchPanel → PATCH /v2/canvas/branches | 既有鉴权路由，写操作经 zod validateFields（status 枚举白名单） |
| socket branch 事件 → store 合并 | 服务端事件回流（既有房间鉴权），applyBranchUpsert 只 merge 已知 id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-06 | Elevation（客户端侧篡改面收窄） | selectBranchAsMain 批量状态翻转 | mitigate | 仅对实际变化分支发 PATCH；失败整体回滚不留半程态；服务端 zod status 枚举 + 404 分支不存在守卫既有 |
| T-55-06b | Tampering | 伪造 branch:updated 事件 | accept | 事件经既有 /ws/projects 房间鉴权通道；applyBranchUpsert 未知 id warn 早退 |
| T-55-SC | Tampering | npm 安装 | accept | 零新增依赖 |
</threat_model>

<verification>
- `cd packages/infinite-canvas && npm test && npx tsc -b --pretty`
- 刷新持久化（升主线 → F5 → 仍主线）与预览手感归 55-VALIDATION Manual-Only 表（UAT）
</verification>

<success_criteria>
- 成功标准 5 后半：BranchPanel 消费 branches store，selectBranchAsMain 可切换主线并持久化（REST PATCH + 乐观回滚单测锁定）。
- status 真相走 V2 事件流合并（A5），flowgraph-v3 schema 零改动；旧版假删除坑不复刻。
- UI 严格 UI-SPEC §5（两段式/文案/色彩限定），设计检查步已走并记录。
</success_criteria>

<output>
Create `.planning/phases/55-navigation-scale/55-06-SUMMARY.md` when done
</output>
