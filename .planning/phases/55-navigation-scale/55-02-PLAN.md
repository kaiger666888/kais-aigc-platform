---
phase: 55-navigation-scale
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/infinite-canvas/src/utils/sceneGrouping.ts
  - packages/infinite-canvas/src/utils/__tests__/sceneGrouping.test.ts
  - packages/infinite-canvas/src/components/StoryboardTimeline.tsx
  - packages/infinite-canvas/src/components/__tests__/StoryboardTimeline.shotKey.test.ts
  - packages/infinite-canvas/src/components/SceneShotBrowser.tsx
  - packages/infinite-canvas/src/store/canvasStore.ts
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
autonomous: true
requirements: [NAV-02]
must_haves:
  truths:
    - "用户可以从工具栏进入「分镜浏览」，按场景折叠展开，逐镜头看到 shot_id/景别/运镜/时长/画面提示(video_prompt)与引用角色/场景缩略图"
    - "场景分组全仓统一用 sceneNumOf 数字段口径（binding constraint 4），场景色带 4 色循环按 (场景号-1)%4"
    - "单击镜头卡即跳画布聚焦对应节点（复用 focusAssetNodeId）"
  artifacts:
    - path: "packages/infinite-canvas/src/utils/sceneGrouping.ts"
      provides: "sceneNumOf / sceneColorOf / SCENE_COLORS 共享口径（binding constraint 4 统一）"
      exports: ["sceneNumOf", "sceneColorOf", "SCENE_COLORS"]
    - path: "packages/infinite-canvas/src/components/SceneShotBrowser.tsx"
      provides: "场景→镜头两级浏览面板（UI-SPEC §2 契约的实现体）"
    - path: "packages/infinite-canvas/src/components/StoryboardTimeline.tsx"
      provides: "extractShots 增强：videoPrompt + referencedAssets 派生"
  key_links:
    - from: "packages/infinite-canvas/src/components/SceneShotBrowser.tsx"
      to: "packages/infinite-canvas/src/store/canvasStore.ts"
      via: "消费 extractShots(graph, rawDataByNodeId) 派生 + setFocusAssetNodeId 跳转"
      pattern: "setFocusAssetNodeId"
    - from: "packages/infinite-canvas/src/components/FlowCanvas.tsx"
      to: "packages/infinite-canvas/src/components/SceneShotBrowser.tsx"
      via: "ViewModeButton viewMode='scene_shots' 渲染分支"
      pattern: "scene_shots"
---

<objective>
NAV-02 分镜层级浏览：把 StoryboardTimeline 的 sceneNumOf/VT_SCENE_COLORS 口径抽成共享 util（binding constraint 4），增强 extractShots（补 video_prompt 与引用角色/场景缩略图缺口），并新建 SceneShotBrowser 独立面板（graph 派生数据，弃用 p10b board JSON——Pitfall 6），从工具栏「分镜浏览」进入。

Purpose: 93 镜规模下单级平铺不可用；场景→镜头两级 + 镜头卡信息密度是本期两大签名 UI 之一（UI-SPEC 已 6/6 批准，BINDING）。
Output: sceneGrouping util + 单测、extractShots 增强与测试断言、SceneShotBrowser 面板 + viewMode 接线。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/55-navigation-scale/55-CONTEXT.md
@.planning/phases/55-navigation-scale/55-RESEARCH.md
@.planning/phases/55-navigation-scale/55-PATTERNS.md
@.planning/phases/55-navigation-scale/55-UI-SPEC.md

<interfaces>
<!-- 数据派生基座（增强对象） -->
StoryboardTimeline.tsx:302-403 extractShots(graph, rawDataByNodeId) → StoryboardShot[]（已导出已测；Pass1 storyboard 节点收集 meta??raw 回退、shotKey 去重；Pass2 video/lastFrame 反查）
StoryboardTimeline.tsx:100-130 interface StoryboardShot { node; shotId; durationS; thumbnail; cameraMovement?; framing?; composition?; pacing?; promptText?; promptFacets?; videoUrl?; firstFrame?; lastFrame?; startFrameDesc?; endFrameDesc?; shotKey }
StoryboardTimeline.tsx:1763-1773 VT_SCENE_COLORS = [v3theme.modality.image, .video, .audio, .text]; sceneNumOf(shotId) = shotId.match(/s?0*(\d+)/i) → Number(m[1]) ?? 0; formatTotalDuration(sec) → MM:SS
StoryboardTimeline.tsx:2405-2406 场景色取色先例：sceneIdx = Math.max(0, sceneNumOf(shot.shotId)-1) % VT_SCENE_COLORS.length
<!-- 两级视觉先例（只借视觉，不借数据源——Pitfall 6） -->
storyboard/StoryboardBoard.tsx:60-223 ShotCard（16:9 缩略 + shot_id 角标、Chip 行、时长行、hover 展开）；:244-297 SceneSection（场景节折叠 + auto-fill 网格）
canvas/ShotTree.tsx:77-82 全局资产派生（scope='global' 节点回查——引用缩略图同源先例）
store/canvasStore.ts:36 export type ViewMode = 'canvas' | 'timeline' | 'assets' | 'pipeline' | 'storyboard_board'（+ setViewMode(mode)）
FlowCanvas.tsx:762-776 ViewModeButton 组；:822-843 viewMode 条件渲染分支
constants.ts: METADATA_LABELS（景别/运镜中文枚举）、RAW_FIELD_LABELS（video_prompt/ltx_prompt 标签既有）
v3/adapter.ts: rawDataByNodeId（白名单外富字段穿透，store 字段）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sceneGrouping 共享 util + StoryboardTimeline 口径迁移</name>
  <files>packages/infinite-canvas/src/utils/sceneGrouping.ts, packages/infinite-canvas/src/utils/__tests__/sceneGrouping.test.ts, packages/infinite-canvas/src/components/StoryboardTimeline.tsx</files>
  <read_first>
    - packages/infinite-canvas/src/components/StoryboardTimeline.tsx:1760-1780（VT_SCENE_COLORS/sceneNumOf/formatTotalDuration 原文——函数体逐字迁移）
    - packages/infinite-canvas/src/theme/catppuccin.ts（v3theme.modality 四色来源）
    - packages/infinite-canvas/src/components/canvas/ShotTree.tsx:38-42（scenePrefix 分隔符法——本 task 不改 ShotTree，其迁移在 55-05；先读懂两侧口径差异）
  </read_first>
  <action>
先写 packages/infinite-canvas/src/utils/__tests__/sceneGrouping.test.ts 行为表（it.each），再实现 packages/infinite-canvas/src/utils/sceneGrouping.ts：

1. `export const SCENE_COLORS = [v3theme.modality.image, v3theme.modality.video, v3theme.modality.audio, v3theme.modality.text] as const`（从 catppuccin.ts import v3theme——本 util 允许 import 仓内 theme，纯常量模块）。
2. `export function sceneNumOf(shotId: string): number`——函数体逐字照抄 StoryboardTimeline:1770-1773（/s?0*(\d+)/i 首个数字段；无匹配返回 0）。
3. `export function sceneColorOf(sceneNum: number): string`——SCENE_COLORS[Math.max(0, sceneNum - 1) % SCENE_COLORS.length]。
4. `export function formatTotalDuration(sec: number): string`——从 StoryboardTimeline:1776-1781 迁移（MM:SS，非有限/≤0 → '00:00'）。

单测断言（含边界）：sceneNumOf('S03_012')===3、('s1')===1、('shot_004')===4、('B02')===2、('无数字')===0、('S10')===10（两位数不被 0* 吃掉）；sceneColorOf(1)===SCENE_COLORS[0]、(5)===SCENE_COLORS[0]（循环）、(0)===SCENE_COLORS[0]（钳负）；formatTotalDuration(0)==='00:00'、(NaN)==='00:00'、(125)==='02:05'。

StoryboardTimeline.tsx 迁移：删除本地 VT_SCENE_COLORS/sceneNumOf/formatTotalDuration 定义，改为从 ../utils/sceneGrouping import；:2405-2406 的取色表达式改为 sceneColorOf(sceneNumOf(shot.shotId))；行为零变化（该文件其余 3000 行不动）。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx vitest run src/utils/__tests__/sceneGrouping.test.ts src/components/__tests__/StoryboardTimeline.shotKey.test.ts && npx tsc -b --pretty</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function sceneNumOf" packages/infinite-canvas/src/components/StoryboardTimeline.tsx` === 0（本地定义已删，改 import）。
    - `grep -n "from '../utils/sceneGrouping'" packages/infinite-canvas/src/components/StoryboardTimeline.tsx` 命中。
    - sceneGrouping.test.ts 用例数 ≥ 12 且全绿。
    - 既有 StoryboardTimeline.shotKey.test.ts 保持绿（口径迁移零回归）。
  </acceptance_criteria>
  <done>场景分组口径单源化（sceneNumOf 数字段法），StoryboardTimeline 消费共享 util，行为不变。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extractShots 增强——videoPrompt + 引用角色/场景缩略图</name>
  <files>packages/infinite-canvas/src/components/StoryboardTimeline.tsx, packages/infinite-canvas/src/components/__tests__/StoryboardTimeline.shotKey.test.ts</files>
  <read_first>
    - packages/infinite-canvas/src/components/StoryboardTimeline.tsx:100-130（StoryboardShot 接口）与 :302-403（extractShots Pass1/Pass2 全文——增强点插在 Pass2 之后，保持纯函数）
    - packages/infinite-canvas/src/components/canvas/ShotTree.tsx:60-106（deriveTree 的 global 资产派生：scope='global' 节点如何取名/副标/缩略——引用回查同源手法）
    - packages/infinite-canvas/src/constants.ts RAW_FIELD_LABELS（video_prompt/ltx_prompt 既有标签键名）
  </read_first>
  <action>
StoryboardShot 接口新增两个可选字段：
- `videoPrompt?: string`——取 raw.video_prompt ?? raw.ltx_prompt（rawDataByNodeId 白名单穿透字段，非空字符串才赋值）。
- `referencedAssets?: { characters: Array<{ name: string; thumbnail: string | null }>; scenes: Array<{ name: string; thumbnail: string | null }> }`

extractShots 增强（保持纯函数、签名不变）：
1. videoPrompt：Pass1 收集时从节点的 raw（meta ?? raw 回退同既有手法）读 video_prompt/ltx_prompt。
2. referencedAssets：先从 graph.nodes 收集 scope==='global' 的资产节点建两张名字索引（name→thumbnail；名字取节点 label/name 字段，缩略图字段与 ShotTree 全局栏同源）；再对每个 shot：
   - characters：shot 节点 meta/raw 的 characters（string[]）逐名回查角色索引；查到才进数组，查不到的保留名字、thumbnail 为 null。
   - scenes：由 shotId 的 sceneNumOf 对应场景（scene_id/scene 字段名优先，无则用「场景 N」名）回查场景索引；查不到则空数组。
3. 空结果不产生空对象：两数组皆空时 referencedAssets 为 undefined。

StoryboardTimeline.shotKey.test.ts 追加断言块（沿用既有 fixture 构造手法）：
- shot 节点 raw 含 video_prompt 时 videoPrompt 等于该值；仅含 ltx_prompt 时取 ltx_prompt；两者皆无时 undefined。
- graph 含 scope='global' 的 character 节点（名匹配 shot.characters[0]、带缩略图字段）时 referencedAssets.characters[0].thumbnail 等于该缩略图；无匹配时 thumbnail 为 null。
- 无 global 节点且 characters 为空时 referencedAssets === undefined。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx vitest run src/components/__tests__/StoryboardTimeline.shotKey.test.ts && npx tsc -b --pretty</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "videoPrompt" packages/infinite-canvas/src/components/StoryboardTimeline.tsx` ≥ 3（接口 + 派生 + 传递）。
    - `grep -c "referencedAssets" packages/infinite-canvas/src/components/StoryboardTimeline.tsx` ≥ 2。
    - 新增断言用例全绿且既有 shotKey 用例不回归。
    - extractShots 仍是纯函数：无 store/store hook 引用（`grep -c "useCanvasStore" ` 在 :302-403 区间为 0）。
  </acceptance_criteria>
  <done>镜头卡所需字段（video_prompt + 引用角色/场景缩略图）在数据层齐备，SceneShotBrowser 直接消费。</done>
</task>

<task type="auto">
  <name>Task 3: SceneShotBrowser 两级浏览面板 + viewMode/工具栏接线（含设计检查）</name>
  <files>packages/infinite-canvas/src/components/SceneShotBrowser.tsx, packages/infinite-canvas/src/store/canvasStore.ts, packages/infinite-canvas/src/components/FlowCanvas.tsx</files>
  <read_first>
    - .planning/phases/55-navigation-scale/55-UI-SPEC.md 全文（BINDING：§Signature Element 场景色带、§Component Inventory 2 SceneShotBrowser、§Copywriting Contract、§Spacing/Typography、§Do-Not-Regress）
    - packages/infinite-canvas/src/components/storyboard/StoryboardBoard.tsx:60-297（ShotCard/SceneSection 视觉模式——只复用视觉：16:9 缩略、Chip 行、hover 展开、折叠节头）
    - packages/infinite-canvas/src/components/canvas/ShotTree.tsx:282-364（CollapsibleSection/TreeItem 折叠交互先例）
    - packages/infinite-canvas/src/components/FlowCanvas.tsx:762-776（ViewModeButton 组）与 :822-843（viewMode 渲染分支）
    - packages/infinite-canvas/src/store/canvasStore.ts:36（ViewMode 联合类型）
    - packages/infinite-canvas/src/theme/tokens.css（--cv-* token 名单——禁止新造色值/字号/间距常量）
  </read_first>
  <action>
1. canvasStore.ts：ViewMode 联合类型追加 'scene_shots'（单行类型修改，不动默认值 'assets'）。
2. 新建 packages/infinite-canvas/src/components/SceneShotBrowser.tsx，data-testid="scene-shot-browser"，按 UI-SPEC §2 实现：
   - 数据：useCanvasStore 的 graph + rawDataByNodeId → extractShots → 按 sceneNumOf(shot.shotId) 分组升序；组内自然序。禁止 fetch /api/v1/storyboard（Pitfall 6——p10b 已注销数据源）。
   - 场景节头（可折叠，首场景 defaultOpen）：左侧 3px 竖色带（sceneColorOf 100%）；文案「场景 {N} · {X} 镜 · {MM:SS}」（formatTotalDuration 累计；场景名可得起则插入）；mono（--cv-font-mono）场景号 11px 场景色 + 计数 11px --cv-lane-label。折叠态保留计数与时长。
   - 镜头卡网格：repeat(auto-fill, minmax(200px, 1fr))。卡左缘 3px 场景色带；16:9 缩略图降级链 firstFrame → thumbnail → 底色 `${sceneColor}33`（无图占位）；shot_id（mono 11px）+ 时长 chip（4.5s 格式，fmtDuration/fallback 实现 fmtDuration(sec) = `${sec}s`）；景别·运镜两枚 chip（METADATA_LABELS 中文枚举，缺失不渲染 chip）。hover 展开（--cv-d-select 120ms）：画面提示 = videoPrompt ?? promptText 2 行截断（11px secondary，标签「画面提示」）；引用行 = referencedAssets 的角色/场景 24px 圆角缩略图横排 + 名称 10px（缩略图 null 时色点占位），标签「引用」。
   - 交互：单击卡 → setFocusAssetNodeId(shot.node.id) 并 setViewMode('canvas')（跳画布聚焦，复用既有 effect，语义不改——Do-Not-Regress 2）。全卡为 <button>（Do-Not-Regress 5 键盘焦点）。
   - 空态（无 storyboard 节点）：heading「本集无分镜数据」body「运行 P09 分镜拆解后，这里会按场景展示全部镜头」（UI-SPEC Copywriting 表原文）。
   - 动效/密度红线：93 镜禁止平铺——只按场景分组渲染；间距只用 xs/sm/md/lg 档与既有例外（28px 行高下限）；prefers-reduced-motion 走 tokens.css 既有降档。
3. FlowCanvas.tsx：ViewModeButton 组追加 viewMode='scene_shots' 按钮（UiIcon kind="film" size={13} + 文案「分镜浏览」）；:822-843 渲染分支追加 viewMode === 'scene_shots' 时渲染 <SceneShotBrowser />（与 storyboard_board 分支同款容器结构）。
4. 设计检查步（执行完必须走一遍并在 SUMMARY 记录）：对照 UI-SPEC §Signature Element（3px 色带/8px 点规则——本面板用 3px 竖带与卡片左缘带）、§Typography（t1-t4 + 400/600 两档权重 + mono 用于 shot_id）、§Color（场景维度 4 色只出现在场景分组表面；不得混入 phaseGroup 色）、§Do-Not-Regress 4/5/6。逐条核对 tokens 引用，确认零新造常量。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx tsc -b --pretty && npx vitest run src/components/__tests__ && grep -c "scene_shots" src/store/canvasStore.ts src/components/FlowCanvas.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "scene_shots" packages/infinite-canvas/src/store/canvasStore.ts` ≥ 1 且 `grep -c "分镜浏览" packages/infinite-canvas/src/components/FlowCanvas.tsx` ≥ 1。
    - SceneShotBrowser.tsx 不含 "fetch(" 与 "/api/v1/storyboard"（`grep -c "api/v1/storyboard" ...` === 0）。
    - `grep -c "setFocusAssetNodeId" packages/infinite-canvas/src/components/SceneShotBrowser.tsx` ≥ 1。
    - `grep -c "sceneColorOf\|SCENE_COLORS" packages/infinite-canvas/src/components/SceneShotBrowser.tsx` ≥ 1 且无十六进制色值字面量（`grep -cE "#[0-9A-Fa-f]{6}" ...` === 0）。
    - 组件内间距/字号值全部引用 --cv-* token 或 SCENE_COLORS 派生（源断言同上）。
    - npx tsc -b --pretty 双根干净；src/components/__tests__ 既有用例全绿。
  </acceptance_criteria>
  <done>工具栏「分镜浏览」进入两级面板：场景折叠节头（3px 色带 + 计数 + MM:SS）→ 镜头卡（shot_id/时长/景别/运镜常显，hover 出画面提示 + 引用缩略图），单击跳画布聚焦。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph/rawData（服务端数据） → 派生 UI | 纯前端只读派生，无新请求面 |
| 引用回查 | scope='global' 节点名 → 缩略图，客户端内存映射 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-02 | Tampering | referencedAssets 名字回查 | accept | 纯只读派生；名字不匹配时 thumbnail=null 降级，不产生请求拼接 |
| T-55-IH | Information Disclosure | 缩略图 URL 解析 | accept | 沿用仓内既有 resolveMediaUrl/toDisplayUrl 相对路径解析器，不新增 URL 拼接面 |
| T-55-SC | Tampering | npm 安装 | accept | 零新增依赖（RESEARCH 裁决），无安装任务 |
</threat_model>

<verification>
- `cd packages/infinite-canvas && npm test && npx tsc -b --pretty`
- 手动（执行期可选）: dev server 打开含 storyboard 数据的集 → 工具栏「分镜浏览」→ 折叠/展开/单击跳转（正式 UAT 归 55-VALIDATION Manual-Only 表，不在本 plan 设 checkpoint）
</verification>

<success_criteria>
- 场景→镜头两级浏览可用且镜头卡呈现成功标准 2 全部字段（shot_id/景别/运镜/时长/video_prompt/引用角色&场景缩略图）。
- 场景口径单源（sceneGrouping util + 单测）；StoryboardTimeline/后续 ShotTree/搜索导航器全部消费同一 sceneNumOf。
- UI 严格落 UI-SPEC（场景色带签名元素 + 审片中文文案 + token-only），设计检查步已走并记录。
</success_criteria>

<output>
Create `.planning/phases/55-navigation-scale/55-02-SUMMARY.md` when done
</output>
