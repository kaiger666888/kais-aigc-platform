---
phase: 55-navigation-scale
plan: 05
type: execute
wave: 2
depends_on: ["55-02"]
files_modified:
  - packages/infinite-canvas/src/hooks/useCanvasPersistence.ts
  - packages/infinite-canvas/src/hooks/__tests__/canvasState.test.ts
  - packages/infinite-canvas/src/components/canvas/PhaseColumns.tsx
  - packages/infinite-canvas/src/components/canvas/ShotTree.tsx
autonomous: true
requirements: [NAV-05]
must_haves:
  truths:
    - "点击 PhaseColumns 列头「聚焦本阶段」→ fitView 该列节点集合且恢复记忆 zoom（下限 0.6，keyFields 可读档）；默认 fitView 行为与 FITVIEW_MIN_ZOOM=0.4 完全不动"
    - "每泳道 zoom 记忆经 useCanvasPersistence patch 语义持久化（key 沿 kais:canvas:v1:p{pid}:e{eid}），刷新后可恢复"
    - "ShotTree 场景行可点击聚焦该场景节点集合（Q3 并列入口）；ShotTree 场景分组迁移到 sceneNumOf 统一口径（binding constraint 4）"
    - "useLod.ts 阈值/迟滞/FITVIEW_MIN_ZOOM 与 LodProvider 单订阅设计零改动，tokens.css LOD 镜像漂移本期不修（binding constraint 3 re-affirm）"
  artifacts:
    - path: "packages/infinite-canvas/src/hooks/useCanvasPersistence.ts"
      provides: "PersistedCanvasState.laneZoom?: Record<number, number> 扩展"
    - path: "packages/infinite-canvas/src/components/canvas/PhaseColumns.tsx"
      provides: "列头可点热区（聚焦本阶段 + zoom 记忆恢复）"
    - path: "packages/infinite-canvas/src/components/canvas/ShotTree.tsx"
      provides: "sceneNumOf 口径迁移 + 场景行聚焦"
  key_links:
    - from: "packages/infinite-canvas/src/components/canvas/PhaseColumns.tsx"
      to: "packages/infinite-canvas/src/hooks/useCanvasPersistence.ts"
      via: "saveCanvasState(key, { laneZoom }) / loadCanvasState 恢复"
      pattern: "laneZoom"
    - from: "packages/infinite-canvas/src/components/canvas/PhaseColumns.tsx"
      to: "packages/infinite-canvas/src/components/canvas/laneGeometry.ts"
      via: "computePhaseColumns 现有几何（不改数学）取列节点集"
      pattern: "computePhaseColumns"
---

<objective>
NAV-05 默认可读混合方案（Q3 裁决）：默认 fitView 行为完全不动；新增 PhaseColumns 列头「聚焦本阶段」交互（fitView 该列节点 + 恢复记忆 zoom，下限 0.6 L2 可读档）+ laneZoom 泳道缩放记忆（localStorage patch 语义）+ ShotTree 场景行聚焦并列入口；顺带完成 ShotTree scenePrefix→sceneNumOf 口径迁移（binding constraint 4 的最后一个使用点）。

Purpose: 34160px 超大图天然 fit-zoom ~0.05、FITVIEW_MIN_ZOOM=0.4 只保 L1——提下限会只见局部，故走「记忆 + 列聚焦」混合；八月盲区修复成果是不可回归红线。
Output: laneZoom 持久化 + 测试、PhaseColumns 交互层、ShotTree 迁移与场景聚焦。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/55-navigation-scale/55-CONTEXT.md
@.planning/phases/55-navigation-scale/55-RESEARCH.md
@.planning/phases/55-navigation-scale/55-PATTERNS.md
@.planning/phases/55-navigation-scale/55-UI-SPEC.md
@.planning/phases/55-navigation-scale/55-02-SUMMARY.md

<interfaces>
<!-- LOD 红线（不可回归，binding constraint 2） -->
src/hooks/useLod.ts:13-23 常量 LOD_L0_MAX=0.22 / LOD_L1_MAX=0.6 / LOD_HYSTERESIS=0.03 / FITVIEW_MIN_ZOOM=0.4；:33-53 resolveLodLevel 迟滞；:72-79 LodProvider 唯一 viewport 订阅者
src/theme/tokens.css:152-154 --cv-lod-l0-max: 0.35 / --cv-lod-l1-max: 0.8（已知镜像漂移——useLod.ts 权威，本期两侧都不修，binding constraint 3）
<!-- 持久化基座 -->
src/hooks/useCanvasPersistence.ts:18-69: interface PersistedCanvasState { viewport?; selectedNodeId?; expandedStacks?; expandedTexts? }; canvasStateKey(projectId, episodesId) = `kais:canvas:v1:p${pid}:e${eid}`; saveCanvasState(key, patch, storage) patch-merge 写 + 损坏 JSON 容错；恢复侧 viewport clamp 先例 :100-110
src/hooks/__tests__/canvasState.test.ts:7-31 既有 LOD/持久化断言 + :35-42 fakeStorage() harness——必须保持绿
<!-- PhaseColumns 现状 -->
src/components/canvas/PhaseColumns.tsx:30-45: SVG overlay data-testid="phase-columns"，pointerEvents:'none' 全层，transform 同步 useViewport()；列几何来自 laneGeometry.computePhaseColumns（median-x、PHASE_GROUPS[index] ?? 'production' :94）
laneGeometry.ts computePhaseColumns 返回列对象含 group/节点归属（几何数学零改动）
<!-- 跳转先例 -->
ShotTree.tsx:132-142 jumpTo：reactFlow.setCenter(cx, cy, { zoom: 1.0, duration: 600 })；FlowCanvas fitView({nodes, maxZoom, duration}) 先例
ShotTree.tsx:38-42 scenePrefix 分隔符法（迁移对象）；:60-106 deriveTree（≥2-distinct-prefix 规则 :92-94——迁移时改判据为 sceneNumOf 分组）
UI-SPEC §3 PhaseColumns 列头聚焦契约（hover chip「聚焦本阶段」/恢复 0.6 下限/仅列头热区可点不挡节点）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: laneZoom 泳道缩放记忆（PersistedCanvasState 扩展 + 回归守卫测试）</name>
  <files>packages/infinite-canvas/src/hooks/useCanvasPersistence.ts, packages/infinite-canvas/src/hooks/__tests__/canvasState.test.ts</files>
  <read_first>
    - packages/infinite-canvas/src/hooks/useCanvasPersistence.ts 全文（接口/saveCanvasState patch 语义/canvasStateKey/恢复 clamp）
    - packages/infinite-canvas/src/hooks/__tests__/canvasState.test.ts 全文（fakeStorage + 既有断言——只加不删）
    - packages/infinite-canvas/src/hooks/useLod.ts:13-23（常量名与值——回归守卫断言引用）
  </read_first>
  <action>
先加测试（RED）再实现（GREEN），全部走既有 fakeStorage 注入模式：

1. canvasState.test.ts 新增 describe('laneZoom 泳道缩放记忆')：
   - saveCanvasState(key, { laneZoom: { 14: 0.85 } }) 后 loadCanvasState(key).laneZoom[14] === 0.85。
   - patch 语义：先存 { selectedNodeId: 'a', laneZoom: { 14: 0.85 } } 再 patch { laneZoom: { 14: 0.9, 15: 0.6 } } → selectedNodeId 仍在、laneZoom 两键合并为 { 14: 0.9, 15: 0.6 }。
   - 无 laneZoom 的旧存档 load 后 laneZoom 为 undefined（向后兼容）。
   - 损坏 JSON 容错既有行为不回归（既有用例保持）。
2. 新增 describe('LOD 阈值回归守卫')：import { LOD_L0_MAX, LOD_L1_MAX, LOD_HYSTERESIS, FITVIEW_MIN_ZOOM } from '../../useLod'（若未导出则补 export——仅导出不改值）断言 0.22/0.6/0.03/0.4 四值精确相等（任何后续误改立即红）。
3. useCanvasPersistence.ts：PersistedCanvasState 增加 `laneZoom?: Record<number, number>`（phaseIndex → 记忆 zoom）；saveCanvasState/loadCanvasState 零额外逻辑（patch-merge 天然覆盖）；不新建独立 storage key（禁止项）。
4. tokens.css 不做任何修改（binding constraint 3 re-affirm：镜像漂移留给显式裁决，本 plan 明确不动两侧）。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx vitest run src/hooks/__tests__/canvasState.test.ts && git diff --stat src/theme/tokens.css | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 新增 laneZoom 用例 ≥ 3 全绿；LOD 守卫 4 断言全绿。
    - `git diff --stat packages/infinite-canvas/src/theme/tokens.css` 输出为空（零改动——constraint 3）。
    - `git diff packages/infinite-canvas/src/hooks/useLod.ts` 输出为空或仅含 export 关键字补齐（值零变化）。
    - canvasState.test.ts 既有用例（:7-31 断言）全部保持绿。
  </acceptance_criteria>
  <done>laneZoom 记忆就绪且 LOD 红线被测试钉死（阈值/迟滞/FITVIEW_MIN_ZOOM/tokens 镜像全部原样）。</done>
</task>

<task type="auto">
  <name>Task 2: PhaseColumns 列头聚焦 affordance（含设计检查）</name>
  <files>packages/infinite-canvas/src/components/canvas/PhaseColumns.tsx</files>
  <read_first>
    - packages/infinite-canvas/src/components/canvas/PhaseColumns.tsx 全文（SVG 层结构/viewport transform/列渲染）
    - packages/infinite-canvas/src/components/canvas/laneGeometry.ts:63-111（computePhaseColumns 返回形状——列对象如何携带节点 id 集合；几何数学不改）
    - packages/infinite-canvas/src/hooks/useCanvasPersistence.ts（Task 1 后：laneZoom 读写 API）
    - packages/infinite-canvas/src/components/canvas/ShotTree.tsx:132-142（jumpTo/setCenter 形态）
    - .planning/phases/55-navigation-scale/55-UI-SPEC.md §3 与 §State Matrix 列头聚焦行（BINDING：hover chip「聚焦本阶段」、恢复下限 0.6、仅列头热区、fitView 动画 600ms）
  </read_first>
  <action>
PhaseColumns.tsx 增强（SVG 本体 pointerEvents:'none' 保持——节点不可被挡）：

1. 每列列头区域（headerY 处既有标签带）增加独立交互热区：SVG 内 <g> 包裹的命中 rect，style pointerEvents:'auto' + cursor:'pointer' + role="button" + tabIndex={0} + aria-label=`聚焦本阶段 ${col.code 或标签}`；键盘 onKeyDown Enter/Space 触发同 click。热区几何只覆盖列头文字带（高度 ≤ 28px 视觉行），不覆盖列身。
2. hover/焦点态出 chip「聚焦本阶段」（tooltip 形态：title 元素或受控小标签，文案取 UI-Spec 原文「聚焦本阶段（恢复上次可读缩放）」）。
3. 聚焦动作 focusPhaseColumn(col)：
   - 收集该列节点 id 集合（computePhaseColumns 结果或列归属数据；空列直接 return）。
   - reactFlow.fitView({ nodes: ids.map(id => ({ id })), duration: 600, maxZoom: 1.5 })。
   - 记忆恢复：if (laneZoom[col.phaseIndex]) 动画结束后 setViewport 保持中心、zoom = clamp(laneZoom[col.phaseIndex], 0.6, 1.5)（下限 0.6 = L2 可读档，UI-SPEC §3）；无记忆时不强制改 zoom（fitView 结果即好）。实现可用 fitView 完成回调或 600ms 后读 getViewport 再 setViewport({..., zoom})，取简实现并在注释标注时序假设。
4. 记忆写入：reactFlow onMoveEnd 时机不可用于列维度——改为在 focusPhaseColumn 完成恢复后与用户手动缩放后（FlowCanvas 侧已存在的 viewport 持久化路径旁，本组件经 props/store 拿 projectId/episodesId）把「当前视口主导列」的 zoom 写 saveCanvasState(key, { laneZoom: { [phaseIndex]: zoom } })；主导列 = computePhaseColumns 列中位 x 距视口中心最近者。写入节流（≥1s 或仅在 moveEnd）。
5. PhaseColumns 与 FlowCanvas 间的 reactFlow 实例获取：按该组件现有 props/context 通道（若现无则经 useReactFlow() —— @xyflow/react 12 子组件可用，PhaseColumns 渲染于 ReactFlow 内）。projectId/episodesId 经 props 传入（FlowCanvas 调用点补传）。
6. 设计检查步（SUMMARY 记录）：UI-SPEC §3 逐条 + State Matrix「列头聚焦」三态（默认/hover chip/记忆恢复 600ms）+ Do-Not-Regress 4（Phase 54 同坐标系同 phaseGroup token——列头色仍用 v3theme.phaseGroup[col.group]）+ 交互行 28px 下限。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx tsc -b --pretty && npx vitest run src/hooks/__tests__ src/components/__tests__ && grep -c "pointerEvents: 'none'" src/components/canvas/PhaseColumns.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "聚焦本阶段" packages/infinite-canvas/src/components/canvas/PhaseColumns.tsx` ≥ 2（aria/tooltip + 实现）。
    - `grep -c "pointerEvents: 'auto'" packages/infinite-canvas/src/components/canvas/PhaseColumns.tsx` ≥ 1 且全层 pointerEvents:'none' 仍保留（热区局部反转模式）。
    - `grep -c "laneZoom" packages/infinite-canvas/src/components/canvas/PhaseColumns.tsx` ≥ 2（读恢复 + 写记忆）。
    - `grep -cE "0\.6" packages/infinite-canvas/src/components/canvas/PhaseColumns.tsx` ≥ 1（恢复下限常量，或具名常量 LOD 可读档 = LOD_L1_MAX 引用——优先引用 useLod 导出而非新字面量）。
    - `grep -c "FITVIEW_MIN_ZOOM" packages/infinite-canvas/src/components/FlowCanvas.tsx` ≥ 1 且其值仍为 0.4（默认 fitView 行为零改动，diff 审 FlowCanvas 适配按钮区仅新增 props 传递）。
    - 既有 hooks/components 测试全绿。
  </acceptance_criteria>
  <done>列头可点「聚焦本阶段」：fitView 列节点 + 记忆 zoom 恢复（≥0.6 可读档），记忆按集隔离持久化；默认 fitView 与 LOD 本体零改动。</done>
</task>

<task type="auto">
  <name>Task 3: ShotTree sceneNumOf 口径迁移 + 场景行聚焦（并列入口）</name>
  <files>packages/infinite-canvas/src/components/canvas/ShotTree.tsx</files>
  <read_first>
    - packages/infinite-canvas/src/components/canvas/ShotTree.tsx 全文（:38-42 scenePrefix、:60-106 deriveTree、:132-142 jumpTo、:274-364 树渲染/行高）
    - packages/infinite-canvas/src/utils/sceneGrouping.ts（55-02 产物：sceneNumOf/sceneColorOf）
    - .planning/phases/55-navigation-scale/55-UI-SPEC.md §Spacing（28px 行高下限）与 §Typography（t5 仅 ShotTree 既有分类头保留）
  </read_first>
  <action>
1. 口径迁移（binding constraint 4 最后一个使用点）：删除本地 scenePrefix 函数；deriveTree 的场景分组判据由「分隔符前缀 + ≥2-distinct-prefix 规则」改为 sceneNumOf(shotId) 数字段分组——场景键 = sceneNumOf(shotId)（0 归入平铺分支保留现有行为：无场景号的 shot 仍走 flatShots 平铺路径）；场景行显示「场景 {N}」（N 为数字，mono）。自然排序、globals 栏、单双击语义（单击选中溯源/双击详情）全部保持。
2. 场景行聚焦（Q3 并列入口）：场景行（节头）新增点击行为——不与单双击 shot 行冲突（节头本无点击）：点击场景节头 → 收集该场景全部 shot 节点 id → reactFlow.fitView({ nodes: ids.map(id => ({ id })), duration: 600, maxZoom: 1.0 })（jumpTo 同款时序），并给节头 hover 态提示（title「聚焦本场景」）。行高与既有 28px 折叠轨/交互行下限对齐。
3. 若场景行需要场景色点（与导航器/浏览器签名元素一致）：用 sceneColorOf(N) 8px 圆点置于行首——仅此一处着色（UI-SPEC 二维映射规则：ShotTree 属场景分组表面）。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx tsc -b --pretty && npx vitest run src/components/__tests__ && grep -c "scenePrefix" src/components/canvas/ShotTree.tsx; true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "scenePrefix" packages/infinite-canvas/src/components/canvas/ShotTree.tsx` === 0；`grep -c "sceneNumOf" ...` ≥ 1（import 共享口径）。
    - `grep -c "fitView" packages/infinite-canvas/src/components/canvas/ShotTree.tsx` ≥ 1（场景行聚焦动作）。
    - 单击选中/双击详情语义未被改动（源码 diff 审 :274 附近 hint 与 onClick 逻辑保持）。
    - 全量组件测试绿；tsc 干净。
  </acceptance_criteria>
  <done>场景分组口径全仓统一为 sceneNumOf；场景节头成为泳道聚焦之外的并列聚焦入口。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage laneZoom → 视口恢复 | 本机持久化数据回流 UI（同 key 既有 P17 信任模型） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-05 | Tampering | localStorage laneZoom 篡改/损坏 | mitigate | 复用 loadCanvasState 损坏 JSON 容错 + 恢复值 clamp [0.6, 1.5]（Task 2 实现），异常值不进入 setViewport |
| T-55-REG | Denial of Service | 逐帧 ×N 回归（缩放卡顿 ∝ 总节点数） | mitigate | 列头交互为离散点击；记忆写入节流 ≥1s；不新增任何 viewport 订阅者（LodProvider 单订阅保持——Do-Not-Regress 1） |
| T-55-SC | Tampering | npm 安装 | accept | 零新增依赖 |
</threat_model>

<verification>
- `cd packages/infinite-canvas && npm test && npx tsc -b --pretty`
- 红线 grep：useLod.ts 四常量值不变、tokens.css LOD 行不变（Task 1 acceptance 已含）
- 视觉可读性（真实 93 镜图 fitView 后非 LOD0 色块 + 列头点击恢复 L2）归 55-VALIDATION Manual-Only 表，不在本 plan 设 checkpoint
</verification>

<success_criteria>
- 成功标准 5 前半：「每泳道缩放记忆生效」——列头聚焦恢复记忆 zoom（下限 0.6）且持久化跨刷新；默认 fitView 行为与 LOD 修法零回归（测试钉死）。
- ShotTree 与全仓场景口径统一（constraint 4 收口）；Phase 54 泳道视觉不受影响（同坐标系同 token）。
</success_criteria>

<output>
Create `.planning/phases/55-navigation-scale/55-05-SUMMARY.md` when done
</output>
