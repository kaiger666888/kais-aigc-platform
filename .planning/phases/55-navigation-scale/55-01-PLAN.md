---
phase: 55-navigation-scale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/infinite-canvas/src/constants/phaseRegistry.ts
  - packages/infinite-canvas/src/constants/__tests__/phaseRegistry.test.ts
  - scripts/verify-phase-55.ts
  - package.json
autonomous: true
requirements: [NAV-01]
must_haves:
  truths:
    - "kap 侧存在唯一的 22-phase 注册表模块，覆盖 khs PHASE_REGISTRY 全部 22 个活跃 phase（含 p035/p09b/p09c/p10c/p11a0/p11a/p11b/p11c/p12a/p12b/p14/p15）"
    - "khs 改 _PHASE_INDEX_MAP / ZONE_PHASES / PHASE_REGISTRY 任一处时，verify-phase-55 契约测试退出码非 0"
    - "D-02: 泳道分组沿用既有 PHASE_GROUPS 框架，注册表 group 字段按创作阶段归组（research/story/production/post）；组归属以 khs ZONE_PHASES 的 group 为权威（契约断言 C 强制），与 Phase 54 泳道阻塞高亮同坐标系"
  artifacts:
    - path: "packages/infinite-canvas/src/constants/phaseRegistry.ts"
      provides: "22-phase 单一注册表（D-01 镜像 + D-04 单源），含前端 PipelinePhaseDef 字段 + 后端 import-from-dir 消费字段"
      exports: ["PHASE_REGISTRY", "PipelinePhaseDef", "PhaseGroup", "DEREGISTERED_PHASE_PREFIXES", "phaseByPrefix"]
    - path: "scripts/verify-phase-55.ts"
      provides: "D-01 契约测试：kap 注册表 ≡ khs 三真相源"
    - path: "packages/infinite-canvas/src/constants/__tests__/phaseRegistry.test.ts"
      provides: "注册表完整性单测（22 条 / 唯一性 / sub 折叠 / 成功标准 1 前缀清单）"
  key_links:
    - from: "scripts/verify-phase-55.ts"
      to: "/data/workspace/kais-hermes-skills/plugins/kais_aigc/canvas_sync.py"
      via: "TS regex 解析 _PHASE_INDEX_MAP（KAIS_HERMES_SKILLS_PATH 可覆盖，只读）"
      pattern: "_PHASE_INDEX_MAP"
    - from: "package.json"
      to: "scripts/verify-phase-55.ts"
      via: "npm script verify:phase-55"
      pattern: "verify:phase-55"
---

<objective>
建立 NAV-01 的骨架：新建 22-phase 单一注册表模块（D-01 khs 镜像 + D-04 单源），并配 verify-schema-drift 式契约测试（第三次复刻镜像+契约模式）。本 plan 只新增文件，不动任何消费方——消费方迁移（删旧表）在 55-03。

Purpose: khs W6 重编号后 kap 后端 zone 表仍是 13-phase 旧结构且 phaseIndex 系统性错位（binding constraint 8）；单一注册表是 zone 对齐、Phase 54 高亮、Phase 57 taxonomy 共同的词汇地基。
Output: phaseRegistry.ts（22 条）+ phaseRegistry.test.ts + scripts/verify-phase-55.ts + package.json verify:phase-55 注册。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/55-navigation-scale/55-CONTEXT.md
@.planning/phases/55-navigation-scale/55-RESEARCH.md
@.planning/phases/55-navigation-scale/55-PATTERNS.md

<interfaces>
<!-- 契约测试要解析的 khs 真相源（只读；本会话已验证内容） -->
khs canvas_sync.py:2318-2348 `_PHASE_INDEX_MAP`（dict，条目形如 "p09b": 10；注意 p11a0 不在其中）:
  p01=1 p02=2 p03=3 p04=4 p05=5(注销) p06=6 p07=7 p08=8 p09=9
  p035=3 p09b=10 p09c=10 p10=11 p10c=12 p10b=13(注销) p11=14(注销)
  p12=15(注销) p13=16 p11a=14 p11b=14 p11c=14 p12a=15 p12b=15 p14=17 p15=18
khs canvas_graph.py:741 `ZONE_PHASES`：有序 (prefix, label, group) 三元组列表，25 条含注销（p05/p10b/p11/p12 单体），无 p11a0 条目；lane 内顺序权威（p11a < p11b < p11c < p12a < p12b）。
khs skills/kais-movie-pipeline/pipeline/phases/__init__.py:198 `PHASE_REGISTRY: list[dict] = [`（每条含 "id": "pXX_name"；恰好 22 个活跃 id，含 p11a0_iframe_qc）。

<!-- 既有前端注册表（55-03 将删除其内联 19 条、改为消费本 plan 的注册表） -->
packages/infinite-canvas/src/components/pipeline/model.ts:24-80:
  interface PipelinePhaseDef { sortKey: number; code: string; name: string; group: PhaseGroup; phaseIndex: number; sub?: boolean }
  PIPELINE_PHASES（19 条，phaseIndex 已 W6 对齐：P01..P15，含 P03.5/P09b/P10c/P11a/P11b/P11c，缺 P09c/P12a/P12b/p11a0，P12 仍为单体条目）
packages/infinite-canvas/src/constants.ts:319-331: type PhaseGroup = 'research'|'story'|'production'|'post'; PHASE_GROUPS: Record<number, PhaseGroup>（1-18 字面量表）
<!-- 契约测试模式先例 -->
scripts/canvas/verify-schema-drift.ts（SIBLING_ROOT = process.env.KAIS_HERMES_SKILLS_PATH ?? "/data/workspace/kais-hermes-skills"；深度计数的 dict 块提取；解析到 0 条即 FAIL 的脆弱性纪律）
scripts/canvas/lib/verify-harness.ts（createHarness()：results/assert/summary/exit-code）
scripts/verify-phase-53.ts（根脚本自包含 assert + grepSource 范围纪律先例；package.json 已有 verify:phase-50/51/53）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 phaseRegistry.ts 单一注册表（22 phase，D-01 镜像 + D-04 单源）</name>
  <files>packages/infinite-canvas/src/constants/phaseRegistry.ts, packages/infinite-canvas/src/constants/__tests__/phaseRegistry.test.ts</files>
  <read_first>
    - packages/infinite-canvas/src/components/pipeline/model.ts（PipelinePhaseDef 形状 + 既有 19 条的 sortKey/code/name/group/phaseIndex/sub 逐值——新注册表前 19 条语义照抄，仅按下方 delta 增删）
    - packages/infinite-canvas/src/constants.ts:319-331（PhaseGroup 类型与 PHASE_GROUPS）
    - .planning/phases/55-navigation-scale/55-RESEARCH.md §NAV-01 真相源全量（khs 三表内容）
  </read_first>
  <action>
新建 packages/infinite-canvas/src/constants/phaseRegistry.ts。模块必须零外部 import（自包含常量模块，供前端与 src/routes 后端两个 tsconfig 根共同编译——后端以相对路径 ../../../../packages/infinite-canvas/src/constants/phaseRegistry 引用，先例：import-from-dir.ts 顶部对 ../../../../schema/generated/frontend-enum-normalizers 的跨界引用）。

导出内容：
1. `export type PhaseGroup = 'research' | 'story' | 'production' | 'post'`（在本模块定义；55-03 再让 constants.ts 改为从这里 re-export，本 plan 不动 constants.ts）。
2. `export interface PipelinePhaseDef`——在 model.ts 既有五字段（sortKey/code/name/group/phaseIndex/sub?）基础上追加后端消费字段：`khsPrefix: string`（契约键，如 'p09c'、'p11a0'）、`prefix: string`（lane/目录前缀，除 p11a0→'p11a' 外等于 khsPrefix）、`canvasType: string`、`assetType: string`、`label: string`（后端 zone 标签，取 khs ZONE_PHASES 文案）、`unmapped?: boolean`（预留给 55-03 的「未映射」兜底条目，注册表本体不用）。
3. `export const PHASE_REGISTRY: readonly PipelinePhaseDef[]`——恰好 22 条（下表为权威内容；前 19 条的 sortKey/code/name/group/phaseIndex/sub 逐值照抄 model.ts 现状，仅两处例外见注）：

khsPrefix/prefix → code, name, group, phaseIndex, sortKey, sub, canvasType/assetType, label：
- p01 → P01 选题/钩子 research 1 1 · script/topic 「P01 · 选题+钩子」
- p02 → P02 大纲 research 2 2 · script/outline
- p03 → P03 剧本审计 story 3 3 · script/script_phase
- p035 → P03.5 戏剧事件打磨 story 3 3.5 sub · script/script_phase
- p04 → P04 角色设计 story 4 4 · asset/character
- p06 → P06 时空剧本 production 6 5 · script/script_phase
- p07 → P07 场景图生成 production 7 6 · asset/scene
- p08 → P08 场景选择 production 8 7 sub · asset/scene
- p09 → P09 分镜拆解 production 9 8 · storyboard/storyboard
- p09b → P09b 镜头审计 production 10 9 sub · storyboard/storyboard
- p09c → P09c 分镜故事板 production 10 9.5 sub · storyboard/storyboard（新增，A2/P09b 先例）
- p10 → P10 语音合成 post 11 10 · audio/voice
- p10c → P10c 语音审计 post 12 11 sub · audio/voice
- p11a0（prefix=p11a）→ P11a0 条件帧审核 post 14 12.5 sub · video/video（新增，A2 裁决：advisory micro-gate 折叠进 P11a lane）
- p11a → P11a 片段预览 post 14 12 · video/video
- p11b → P11b 片段生成 post 14 13 · video/video
- p11c → P11c 视频质检 post 14 13.5 sub · video/video（例外 1：sortKey 由既有 11.5 改 13.5——对齐 ZONE_PHASES lane 内权威顺序 p11a<p11b<p11c<p12a<p12b，使契约断言 5 可全局成立；在代码注释记录该修正）
- p12a → P12a 时间线合成 post 15 14 · video/clip（新增，替换旧 P12 单体；不设 sub——P11a/P11b 共 lane 先例，phaseIndex 15 的资产需要宿主卡）
- p12b → P12b 音频合成 post 15 14.5 · audio/mix（新增，同上不设 sub）
- p13 → P13 交付 post 16 15 · video/delivery
- p14 → P14 质量审计 post 17 16 · script/script_phase
- p15 → P15 反馈 post 18 17 · script/script_phase

   D-02 归组裁决记录（checker INFO 合规注记）：CONTEXT 括号示例（p10c/p11*→production）为示意非锁定（D-02 明示「具体归组 planner 可调」）；本表 group 以 khs ZONE_PHASES 三元组 group 字段为权威（契约断言 C 强制）且与既有 PHASE_GROUPS 11-18 全 post 一致——p10c/p11a0/p11a/p11b/p11c/p12a/p12b/p14/p15 均归 post，非照抄括号示例。

4. `export const DEREGISTERED_PHASE_PREFIXES = ['p05', 'p10b', 'p11', 'p12'] as const`（khs 保留编号但已注销的单体 phase）。
5. `export const phaseByPrefix: Record<string, PipelinePhaseDef>`（prefix → 条目；p11a0 与 p11a 同 prefix 时后写覆盖需在注释声明：lane 级查找取 p11a 主条目，逐条目查找走 PHASE_REGISTRY 本体）。

新建 packages/infinite-canvas/src/constants/__tests__/phaseRegistry.test.ts（vitest，先写测试再定稿常量亦可，行为表用 it.each）：
- PHASE_REGISTRY 长度 === 22；code 与 khsPrefix 各自唯一。
- 成功标准 1 的前缀清单全存在：['p035','p09b','p09c','p10c','p11a','p11a0','p12a','p12b','p14','p15']。
- 注销前缀 p05/p10b/p11/p12 不在 khsPrefix 集合。
- p11a0：sub === true 且 phaseIndex === p11a 条目的 phaseIndex（14）；prefix === 'p11a'。
- p12a/p12b：sub 未设（undefined）且 phaseIndex === 15。
- 逐条 phaseIndex 期望表断言（嵌入 khs map 值：p035→3, p09b→10, p09c→10, p10→11, p10c→12, p11*→14, p12a/p12b→15, p13→16, p14→17, p15→18）。
- group 覆盖：research/story/production/post 四组各至少 2 条；p09c/p09b group === 'production'，p12a/p12b/p14/p15 group === 'post'。
- sortKey 升序排序后 ZONE_PHASES 可见顺序成立：p11a < p11b < p11c < p12a < p12b 且 p09 < p09b < p09c < p10。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx vitest run src/constants/__tests__/phaseRegistry.test.ts && npx tsc -b --pretty</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "khsPrefix" packages/infinite-canvas/src/constants/phaseRegistry.ts` ≥ 3（接口字段 + 22 条目使用）。
    - `npx vitest run src/constants/__tests__/phaseRegistry.test.ts` 全绿，含 `expect(PHASE_REGISTRY).toHaveLength(22)`。
    - phaseRegistry.ts 顶部无任何 `import` 语句（自包含；`grep -c "^import" ...` === 0）。
    - p11c sortKey 为 13.5（源断言：注册表内 `13.5` 伴随 code 'P11c'）。
  </acceptance_criteria>
  <done>22 条注册表 + 单测落地，双根 tsc 可编译（本 plan 后运行 package 侧 tsc 即可，后端引用在 55-03 接线）。</done>
</task>

<task type="auto">
  <name>Task 2: verify-phase-55.ts 契约测试（kap 注册表 ≡ khs 三真相源）+ npm 注册</name>
  <files>scripts/verify-phase-55.ts, package.json</files>
  <read_first>
    - scripts/canvas/verify-schema-drift.ts（全文 201 行——SIBLING_ROOT env、深度计数块提取、逐项 only-in-kap/only-in-khs diff、零漂移终断言、脆弱性即信号注释）
    - scripts/canvas/lib/verify-harness.ts（createHarness 结果收集/退出码模式）
    - scripts/verify-phase-53.ts:36-105（根脚本自包含 assert + mkdtemp/chdir 隔离 + grepSource 范围纪律）
    - packages/infinite-canvas/src/constants/phaseRegistry.ts（Task 1 产物，被守护对象）
    - /data/workspace/kais-hermes-skills/plugins/kais_aigc/canvas_sync.py:2318-2348（_PHASE_INDEX_MAP 原文形态）
    - /data/workspace/kais-hermes-skills/plugins/kais_aigc/canvas_graph.py:741-774（ZONE_PHASES 元组列表原文形态）
    - /data/workspace/kais-hermes-skills/skills/kais-movie-pipeline/pipeline/phases/__init__.py:198 起（PHASE_REGISTRY list[dict]，每条含 "id": "pXX_..."）
  </read_first>
  <action>
新建 scripts/verify-phase-55.ts（根仓脚本，npx tsx 运行；TS regex 解析，禁止 spawn python——T-55-01）。结构复刻 verify-schema-drift.ts：

1. 头部：SIBLING_ROOT = process.env.KAIS_HERMES_SKILLS_PATH ?? "/data/workspace/kais-hermes-skills"；三个解析目标路径常量（plugins/kais_aigc/canvas_sync.py、plugins/kais_aigc/canvas_graph.py、skills/kais-movie-pipeline/pipeline/phases/__init__.py）；createHarness()；保留 verify-schema-drift.ts:36-40 的「脆弱性即契约信号」注释（解析到 0 条即 FAIL，不得修复为更健壮的解析器）。
2. 三个解析器（每个先做文件存在门 + 解析条目数 > 0 门）：
   - parsePhaseIndexMap：match /^_PHASE_INDEX_MAP[^{]*\{/m 定位，深度计数花括号取 dict 体，条目正则 /"([^"]+)"\s*:\s*(\d+)/g → Map<string, number>。
   - parseZonePhases：定位 ZONE_PHASES 列表体（深度计数方括号），条目正则匹配三元组 "prefix", "label", "group" → 有序数组 {prefix,label,group}。
   - parsePhaseRegistry：定位 PHASE_REGISTRY: list[dict] = [ 块（深度计数），条目正则 /"id"\s*:\s*"([^"]+)"/g → id 列表；id → 前缀用 /^(p\d+[a-z0-9]*)/ 提取（p11a0_iframe_qc → p11a0、p035_dramatic_polish → p035）。
3. 断言组（每条独立 assert，结果进 harness）：
   - A 集合等价：khs PHASE_REGISTRY 前缀集合 === kap PHASE_REGISTRY 的 khsPrefix 集合（双向 diff 输出 only-in-kap / only-in-khs）。
   - B 编号：对每个 khsPrefix 存在于 _PHASE_INDEX_MAP 的条目，registry.phaseIndex === map 值；p11a0 不在 map 中（本会话已验证），单独断言 registry p11a0.phaseIndex === registry p11a.phaseIndex。
   - C 归组：对每个 lane prefix（p11a0 用 p11a）存在于 ZONE_PHASES 的条目，registry.group === zone group。
   - D 顺序：ZONE_PHASES 过滤掉 DEREGISTERED_PHASE_PREFIXES 后的活跃前缀序列，与 PHASE_REGISTRY 按 sortKey 升序的同前缀子序列逐一相等（lane 内顺序权威）。
   - E 注销：DEREGISTERED_PHASE_PREFIXES（p05/p10b/p11/p12）不在 registry khsPrefix 集合。
4. package.json scripts 追加 `"verify:phase-55": "npx tsx scripts/verify-phase-55.ts"`（与 verify:phase-53 相邻）。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsx scripts/verify-phase-55.ts && KAIS_HERMES_SKILLS_PATH=/tmp/nonexistent-khs npx tsx scripts/verify-phase-55.ts; [ $? -ne 0 ] || exit 1</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/verify-phase-55.ts` 退出码 0，输出含全部断言组通过汇总。
    - 环境变量指向不存在路径时退出码非 0（文件缺失门可失败——Phase 50「forced-failure sanity」传统）。
    - `grep -c "spawn\|execSync\|exec(" scripts/verify-phase-55.ts` === 0（无 khs 代码执行，T-55-01）。
    - `grep -n "verify:phase-55" package.json` 命中 1 行。
  </acceptance_criteria>
  <done>契约测试绿 + 失败路径可验 + npm 入口注册；khs 侧三真相源任一漂移即变红。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kap 契约脚本 → khs 仓库文件系统 | 契约测试读取兄弟仓 Python 源码（只读） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Tampering | scripts/verify-phase-55.ts 读 khs 路径 | mitigate | 只读 fs.readFile + TS regex 解析，无 spawn/execSync/exec（acceptance grep 断言）；路径经 KAIS_HERMES_SKILLS_PATH env 可覆盖 |
| T-55-02 | Tampering | phaseIndex 数据污染 | accept | 本 plan 仅建静态注册表；运行时防护（D-03 fallback + zod Number.isFinite）在 55-03 落地 |
| T-55-SC | Tampering | npm/pip 安装 | accept | 本 phase 零新增依赖（RESEARCH Package Legitimacy Audit 裁决），无安装任务即无 slopcheck 面 |
</threat_model>

<verification>
- `cd packages/infinite-canvas && npx vitest run src/constants/__tests__/phaseRegistry.test.ts && npx tsc -b --pretty`
- `cd /data/workspace/kais-aigc-platform && npm run verify:phase-55`
- khs 不被修改：`git -C /data/workspace/kais-hermes-skills status --porcelain` 无本 phase 产生的新变更
</verification>

<success_criteria>
- PHASE_REGISTRY 恰好 22 条，覆盖成功标准 1 全部缺失 phase 且 p11a0 按 A2 折叠（sub=true 共 phaseIndex 14）。
- 契约测试对 khs 三真相源（_PHASE_INDEX_MAP / ZONE_PHASES / PHASE_REGISTRY）双向 diff 全绿；坏路径退出非 0。
- 注册表零外部 import，双根可编译；旧表未动（删除归 55-03，本 plan 不产生两套词汇并存的终态）。
</success_criteria>

<output>
Create `.planning/phases/55-navigation-scale/55-01-SUMMARY.md` when done
</output>
