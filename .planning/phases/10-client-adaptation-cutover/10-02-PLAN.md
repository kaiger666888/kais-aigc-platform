---
phase: 10-client-adaptation-cutover
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/hermes-agent/Dockerfile
  - docker-compose.v9.yml
  - docs/hermes-migration.md
autonomous: true
requirements: [REPLACE-01, REPLACE-02, REPLACE-03]

must_haves:
  truths:
    - "hermes-agent Docker image builds successfully from docker/hermes-agent/Dockerfile"
    - "docker-compose.v9.yml includes hermes-agent service on :8080 with health check"
    - "hermes-agent container joins kais-net network and can be reached by other containers"
    - "Old hermes-worker-agent retirement is documented with stop/verify commands"
  artifacts:
    - path: "docker/hermes-agent/Dockerfile"
      provides: "Minimal Python 3.11 container for hermes-agent FastAPI service"
      contains: "uvicorn"
    - path: "docker-compose.v9.yml"
      provides: "hermes-agent service definition alongside existing services"
      contains: "hermes-agent"
    - path: "docs/hermes-migration.md"
      provides: "Migration guide from old hermes-worker-agent to new hermes-agent"
  key_links:
    - from: "docker-compose.v9.yml"
      to: "docker/hermes-agent/Dockerfile"
      via: "build context"
      pattern: "hermes-agent"
    - from: "hermes-agent container"
      to: "/app/data"
      via: "HERMES_HOME volume mount"
      pattern: "hermes-data"
---

<objective>
Create Dockerfile for hermes-agent, add it to docker-compose.v9.yml, and document retirement of the old hermes-worker-agent service.

Purpose: Deploys the new hermes-agent service as a Docker container (REPLACE-03), ensuring it listens on :8080 (REPLACE-01) and the old Node.js :3100 service is documented as retired (REPLACE-02). The hermes-agent is a CPU-only, stateless service -- no GPU needed.

Output: Dockerfile, updated docker-compose.v9.yml with hermes-agent service, migration documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-client-adaptation-cutover/10-CONTEXT.md
@.planning/phases/10-client-adaptation-cutover/10-RESEARCH.md

<interfaces>
<!-- From docker/hermes-agent/src/config.py -->
Settings class:
  port: int = 8080 (from PORT env var)
  host: str = "0.0.0.0" (from HOST env var)
  hermes_home: Path = ~/.hermes (from HERMES_HOME env var)
  llm_api_key: str = "" (from LLM_API_KEY env var)
  llm_base_url: str = "https://open.bigmodel.cn/api/paas/v4" (from LLM_BASE_URL env var)

<!-- From docker/hermes-agent/requirements.txt -->
  hermes-agent>=0.15.1,<0.17
  fastapi>=0.136.1,<1
  uvicorn>=0.46.0,<1
  pydantic>=2.13,<3

<!-- docker-compose.v9.yml patterns (all services follow) -->
  - container_name: kais-{name}
  - restart: unless-stopped
  - networks: [kais-net]
  - healthcheck with interval/timeout/retries/start_period
  - env_file: .env (for LLM keys)
  - Ports bound to 127.0.0.1 (not exposed externally)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dockerfile + add hermes-agent to docker-compose + write migration doc</name>
  <files>docker/hermes-agent/Dockerfile, docker-compose.v9.yml, docs/hermes-migration.md</files>
  <read_first>
    docker-compose.v9.yml
    docker/hermes-agent/requirements.txt
    docker/hermes-agent/src/config.py
    docker/hermes-agent/src/main.py
    docs/hermes-worker-agent.md
  </read_first>
  <action>
    Create docker/hermes-agent/Dockerfile following the minimal Python Dockerfile pattern from RESEARCH.md:
    - Base: python:3.11-slim
    - Use TUNA mirror for apt (consistent with gold-team Dockerfile pattern): sed -i 's|deb.debian.org|mirrors.tuna.tsinghua.edu.cn|g' /etc/apt/sources.list.d/debian.sources
    - COPY requirements.txt, pip install with TUNA PyPI mirror: pip install --no-cache-dir -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
    - COPY src/ ./src/ and scripts/ ./scripts/
    - ENV PORT=8080, EXPOSE 8080
    - CMD: uvicorn src.main:app --host 0.0.0.0 --port 8080
    - No GPU needed (CPU-only service)

    Modify docker-compose.v9.yml to add hermes-agent service (insert as service entry, maintain existing service numbering):
    - Service name: hermes-agent
    - build: context: ./docker/hermes-agent, dockerfile: Dockerfile
    - container_name: kais-hermes-agent
    - restart: unless-stopped
    - ports: "127.0.0.1:${HERMES_PORT:-8080}:8080" (host-only, consistent with gold-team)
    - env_file: .env
    - environment: PORT=8080, LLM_API_KEY from env, LLM_BASE_URL from env, HERMES_HOME=/app/data
    - volumes: hermes-data:/app/data (named volume for domain data persistence)
    - networks: [kais-net]
    - healthcheck: python3 -c urllib.request.urlopen('http://localhost:8080/v1/health'), interval 30s, timeout 10s, retries 3, start_period 30s
    - NO depends_on for movie-agent (hermes is optional, movie-agent starts independently per RESEARCH.md anti-patterns)
    - Add hermes-data named volume to bottom volumes section

    Create docs/hermes-migration.md documenting:
    - Old service: hermes-worker-agent (Node.js :3100) at ~/.openclaw/workspace/hermes-worker-agent/
    - New service: hermes-agent (Python FastAPI :8080) in Docker container kais-hermes-agent
    - How to verify old service is stopped: `ss -tlnp | grep 3100` should return nothing
    - How to stop old service if still running: `pm2 stop hermes-worker-agent` or `kill $(lsof -ti:3100)`
    - How to start new service: `docker compose -f docker-compose.v9.yml up -d hermes-agent`
    - How to verify new service: `curl http://localhost:8080/v1/health` returns {"status":"ok","engine":"hermes-agent"}
    - API migration: old 6-tool interface -> new 3-endpoint interface (decide/audit/register)
    - Note: hermes-adapter.js (LLM routing) unchanged -- it does not call the new hermes-agent service (CLIENT-03)
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && docker build -f docker/hermes-agent/Dockerfile -t kais-hermes-agent:test docker/hermes-agent/ 2>&1 | tail -5 && echo "BUILD_SUCCESS" || echo "BUILD_FAILED"</automated>
  </verify>
  <done>
    - Dockerfile at docker/hermes-agent/Dockerfile builds successfully
    - docker-compose.v9.yml has hermes-agent service with :8080, healthcheck, kais-net, named volume
    - No :3100 port references exist in docker-compose.v9.yml
    - docs/hermes-migration.md documents old service retirement and new service deployment
    - hermes-data volume declared in docker-compose.v9.yml volumes section
    - Docker image builds without errors
  </done>
  <acceptance_criteria>
    - docker/hermes-agent/Dockerfile exists and contains "python:3.11-slim" and "uvicorn"
    - Dockerfile uses TUNA mirror for both apt and pip
    - Dockerfile copies src/ and scripts/ directories
    - docker-compose.v9.yml contains "hermes-agent:" service key
    - hermes-agent ports maps to "127.0.0.1:${HERMES_PORT:-8080}:8080"
    - hermes-agent env_file is .env
    - hermes-agent environment includes HERMES_HOME=/app/data
    - hermes-agent volumes includes hermes-data:/app/data
    - hermes-agent healthcheck tests /v1/health endpoint
    - hermes-data appears in bottom volumes section of docker-compose.v9.yml
    - grep -c '3100' docker-compose.v9.yml returns 0 (no old port references)
    - docs/hermes-migration.md exists and contains "hermes-worker-agent" and ":8080"
    - Docker build command exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host:8080 -> hermes-agent | Port bound to 127.0.0.1 only. No external access. |
| kais-net -> hermes-agent | Other containers on bridge network can reach hermes-agent. Trusted internal zone. |
| hermes-agent -> LLM API | HTTPS outbound to LLM_BASE_URL. API key via env var. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Information Disclosure | LLM_API_KEY in container env | mitigate | Key in .env (gitignored), passed via env_file, not hardcoded in Dockerfile |
| T-10-04 | Tampering | hermes-agent Docker image | mitigate | Dockerfile builds from pinned requirements.txt, TUNA mirror reduces supply chain risk |
| T-10-05 | Information Disclosure | Port 8080 exposure | mitigate | Bound to 127.0.0.1 -- only host access, not externally reachable |
| T-10-SC | Tampering | pip installs | mitigate | All packages pre-verified in Phases 7-9. TUNA mirror for reproducibility. |
</threat_model>

<verification>
- Docker image builds: `docker build -f docker/hermes-agent/Dockerfile docker/hermes-agent/`
- docker-compose.v9.yml is valid YAML: `docker compose -f docker-compose.v9.yml config`
- No :3100 references: `grep -c 3100 docker-compose.v9.yml` returns 0
- Migration doc exists: `test -f docs/hermes-migration.md`
</verification>

<success_criteria>
- hermes-agent Dockerfile builds successfully with python:3.11-slim base
- docker-compose.v9.yml includes hermes-agent service listening on 127.0.0.1:8080
- hermes-agent has Docker healthcheck hitting GET /v1/health
- Named volume hermes-data provides persistent storage for domain data
- Old hermes-worker-agent retirement documented with stop/verify commands
- No old :3100 port references in docker-compose.v9.yml
</success_criteria>

<output>
Create `.planning/phases/10-client-adaptation-cutover/10-02-SUMMARY.md` when done
</output>
