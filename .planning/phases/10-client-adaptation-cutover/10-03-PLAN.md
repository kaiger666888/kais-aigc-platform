---
phase: 10-client-adaptation-cutover
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - docker/hermes-agent/tests/test_e2e.py
autonomous: true
requirements: [CLIENT-01, CLIENT-02, REPLACE-01, REPLACE-03]

must_haves:
  truths:
    - "hermes-agent Docker container starts and responds to GET /v1/health with status=ok"
    - "movie-pipeline domain is registered and GET /v1/domains returns it"
    - "POST /v1/decide for movie-pipeline task returns decision_id + recommendation + confidence"
    - "POST /v1/audit after decide returns recorded=true"
    - "hermes-client.js degradation works end-to-end when hermes-agent is stopped"
  artifacts:
    - path: "docker/hermes-agent/tests/test_e2e.py"
      provides: "End-to-end validation against real Docker container"
      exports: ["test_health", "test_decide_for_registered_task", "test_audit_after_decide"]
  key_links:
    - from: "test_e2e.py"
      to: "hermes-agent Docker container :8080"
      via: "httpx requests"
      pattern: "httpx\\.(get|post).*8080"
    - from: "test_e2e.py"
      to: "hermes-client.js"
      via: "subprocess node test"
      pattern: "hermes-client"
---

<objective>
End-to-end validation of the full hermes-agent stack: Docker container startup, domain registration, decide/audit cycle, and hermes-client.js degradation behavior.

Purpose: Proves the entire pipeline works from movie-agent through hermes-agent to the learning loop. This is the final verification that Phase 10 delivers on all success criteria before closing the phase.

Output: E2E test script that validates the complete hermes decision flow against a real Docker container.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-client-adaptation-cutover/10-CONTEXT.md
@.planning/phases/10-client-adaptation-cutover/10-RESEARCH.md
@.planning/phases/10-client-adaptation-cutover/10-01-SUMMARY.md
@.planning/phases/10-client-adaptation-cutover/10-02-SUMMARY.md

<interfaces>
<!-- From docker/hermes-agent/src/api/routes.py -->
POST /v1/decide -> DecideResponse
POST /v1/audit -> AuditResponse
GET /v1/health -> HealthResponse {status, engine, domains_count, domains}
GET /v1/domains -> list[str]
POST /v1/register -> RegisterResponse

<!-- From docker/hermes-agent/src/api/models.py -->
DecideResponse: {decision_id, recommendation, confidence, domain, task, timestamp}
AuditResponse: {recorded, auto_learn_triggered, decision_id}
HealthResponse: {status, engine, domains_count, domains}

<!-- From docker/hermes-agent/scripts/register_movie_pipeline.py -->
Registration script path: docker/hermes-agent/scripts/register_movie_pipeline.py
Domain: "movie-pipeline"
Tasks: [requirement, art-direction, character, scenario, voice, storyboard, scene, camera-preview, camera-final, post-production]

<!-- From docker/movie-agent/lib/hermes-client.js (created in 10-01) -->
Exports: decide(task, context), audit(decisionId, outcome, metrics)
HERMES_URL env var, HERMES_DOMAIN="movie-pipeline"
Degradation: returns {degraded: true, ...} on failure
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test script validating hermes-agent Docker container + hermes-client.js integration</name>
  <files>docker/hermes-agent/tests/test_e2e.py</files>
  <read_first>
    docker/hermes-agent/src/api/routes.py
    docker/hermes-agent/src/api/models.py
    docker/hermes-agent/scripts/register_movie_pipeline.py
    docker/hermes-agent/tests/conftest.py
    docker/hermes-agent/tests/test_routes.py
  </read_first>
  <action>
    Create docker/hermes-agent/tests/test_e2e.py as a pytest test module that validates against a REAL running hermes-agent Docker container (not TestClient mock). Tests connect to http://localhost:8080.

    Test functions to implement:

    1. test_health: GET /v1/health returns status_code 200, json has status="ok", engine="hermes-agent", domains_count >= 0

    2. test_register_movie_pipeline_if_needed: Call POST /v1/register with domain="movie-pipeline", description, and the 10 tasks from register_movie_pipeline.py TASKS list. Accept 201 (new) or 422 (already registered) -- idempotent. Then GET /v1/domains and assert "movie-pipeline" is in the list.

    3. test_decide_art_direction: POST /v1/decide with {domain: "movie-pipeline", task: "art-direction", context: {style: "anime"}}. Assert status 200, response has decision_id (non-empty string), recommendation (non-empty string), confidence (float >= 0), domain="movie-pipeline", task="art-direction". Timeout 30s (LLM call may be slow).

    4. test_audit_after_decide: First POST /v1/decide for task="scene" with empty context, capture decision_id. Then POST /v1/audit with {domain: "movie-pipeline", decision_id, outcome: "completed", metrics: {task: "scene", score: 8}}. Assert recorded=true.

    5. test_decide_all_tasks: Loop over a representative subset of tasks (art-direction, storyboard, camera-preview) and verify each returns valid DecideResponse with non-empty decision_id.

    6. test_client_degradation: Use subprocess to run node -e that imports hermes-client.js with HERMES_URL=http://localhost:19999 (dead port) and calls decide(). Parse stdout for "degraded" indicator. This tests real hermes-client.js degradation without needing Docker control. Assert subprocess exits 0 and output contains degradation marker.

    Use httpx for HTTP calls (already in requirements.txt). Set base URL from env var HERMES_URL defaulting to "http://localhost:8080". Set generous timeouts (30s for decide, 10s for others) to accommodate LLM latency.

    Use conftest.py pattern from existing tests for any shared fixtures.

    The test should be runnable via: cd docker/hermes-agent && python -m pytest tests/test_e2e.py -v
    Prerequisites: hermes-agent container running, movie-pipeline domain registered (test handles registration).
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/docker/hermes-agent && python -m pytest tests/test_e2e.py -v --co -q 2>&1 | grep -c "test_"</automated>
  </verify>
  <done>
    - test_e2e.py created with 5+ test functions covering health, registration, decide, audit, multi-task
    - Tests use httpx against real Docker container at HERMES_URL (default localhost:8080)
    - Registration is idempotent (accepts 201 or 422)
    - Decide tests allow 30s timeout for LLM latency
    - Existing test suite still passes: python -m pytest tests/ -x -q
  </done>
  <acceptance_criteria>
    - docker/hermes-agent/tests/test_e2e.py exists
    - File imports httpx and pytest
    - HERMES_URL defaults to "http://localhost:8080" via env var
    - test_health function exists and asserts status="ok"
    - test_register_movie_pipeline_if_needed function exists and handles idempotent registration
    - test_decide_art_direction function exists with 30s timeout
    - test_audit_after_decide function exists and asserts recorded=true
    - test_decide_all_tasks tests multiple tasks
    - `python -m pytest docker/hermes-agent/tests/test_e2e.py --co -q` lists all test functions
    - Existing test suite unbroken: `cd docker/hermes-agent && python -m pytest tests/ -x -q` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test_e2e.py -> hermes-agent | Test connects to localhost:8080. Assumes local Docker environment. |
| hermes-agent -> LLM API | LLM calls use API key from env. Test triggers real LLM calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Information Disclosure | LLM API key used during E2E test | accept | Test runs in developer environment, key not logged |
| T-10-SC | Tampering | pip installs | mitigate | All packages pre-verified. No new packages in this plan. |
</threat_model>

<verification>
- Test collection succeeds: `cd docker/hermes-agent && python -m pytest tests/test_e2e.py --co -q`
- Existing tests still pass: `cd docker/hermes-agent && python -m pytest tests/ -x -q`
- E2E tests can run against Docker container: `cd docker/hermes-agent && python -m pytest tests/test_e2e.py -v` (requires running container)
</verification>

<success_criteria>
- E2E test script covers health check, domain registration, decide, audit, and multi-task scenarios
- Tests are idempotent (registration handles already-registered domain)
- Tests use generous timeouts for LLM latency
- Existing test suite unbroken (97+ tests still pass)
- Full E2E flow: health -> register -> decide -> audit -> verify response shapes
</success_criteria>

<output>
Create `.planning/phases/10-client-adaptation-cutover/10-03-SUMMARY.md` when done
</output>
