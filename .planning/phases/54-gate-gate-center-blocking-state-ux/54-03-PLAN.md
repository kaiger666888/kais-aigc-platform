---
phase: 54-gate-gate-center-blocking-state-ux
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - kais-hermes-skills:plugins/kais_aigc/review_platform.py
  - kais-hermes-skills:plugins/review_gates/runner_hooks.py
  - kais-hermes-skills:plugins/review_gates/tests/test_poller_complete_state.py
autonomous: true
requirements: [GATE-03]
must_haves:
  truths:
    - "khs Path 2 poller 认 COMPLETE 为终态(并集保留 resolved/closed 兼容)——G1 缺口关闭,kmc 能读到平台决议"
    - "decision 来源优先 status.result.decision(approve/reject/waive→approve),无 decision 时保留旧 disposition 兜底"
    - "chosen 通道补 result.selected[0]→choose:v{N} 喂 _chosen_from_suggested——G3/G4 关闭,机器选片通道贯通"
    - "query_review_status 返回 dict 新增 result 键(metadata.review_result),旧四键不动(向后兼容)"
  artifacts:
    - path: "kais-hermes-skills:plugins/review_gates/runner_hooks.py"
      provides: "Path 2 终态词汇对齐 + decision 映射 + chosen 第三通道"
      contains: "COMPLETE"
    - path: "kais-hermes-skills:plugins/kais_aigc/review_platform.py"
      provides: "query_review_status 第五键 result"
      contains: "review_result"
    - path: "kais-hermes-skills:plugins/review_gates/tests/test_poller_complete_state.py"
      provides: "R2/R3 红先绿后回归网"
      contains: "test_poller_complete_state"
  key_links:
    - from: "khs:plugins/review_gates/runner_hooks.py Path 2"
      to: "khs:plugins/kais_aigc/review_platform.py query_review_status"
      via: "status dict 的 result 键"
      pattern: 'status\.get\("result"'
    - from: "khs:runner_hooks resolve_direct"
      to: "review-outcomes.json + PipelineState.phases[phase]"
      via: "RESEARCH §C.3 round-trip 链的 kmc 落档段(既有,零改动)"
      pattern: "resolve_direct"
---

<objective>
khs R2+R3(GATE-03 三腿之二/三,纯契约层):R2 = review_platform.py query_review_status 提取 metadata.review_result 为 result 键;R3 = runner_hooks.py Path 2 终态词汇对齐 COMPLETE + decision 优先映射(waive→approve)+ chosen 第三通道(result.selected)。红先(pytest)绿后。
Purpose: SC3"kmc 恢复/继续管线时消费到该决策"的消费端——49-D11 冻结到期的协议缺口在 khs 侧的关闭。
Output: 两个 plugin 契约文件最小修改(~26 行)+ 新测试文件;khs 全量 plugin 测试绿。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-CONTEXT.md
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-RESEARCH.md
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-PATTERNS.md
@.planning/specs/COORD-01-khs2-parallel-coordination.md

### COORD-01 开工 checklist(涉及 kmc 侧变更的 plan 必查)

- [ ] **kais-hermes-skills 工作树干净**:`git -C /data/workspace/kais-hermes-skills status --porcelain`
      输出为空——有未提交变更时先确认归属(可能是 khs2 v2.4 进行中的工作,踩上去即冲突)。
- [ ] **与上游同步确认**:kais-hermes-skills 已与上游同步(fetch + 确认本地不落后于
      khs2 v2.4 最新验收点),避免基于过期契约做映射变更。
- [ ] **变更面自查**:本 plan 的 kmc 侧改动是否只碰契约/映射三层(field-map / canvas_sync /
      manifest schema)?凡是碰 kmc 22 phases 内部算法的一律停手回议(见规范 ①)。
- [ ] **排序自查**:涉及 p04/p09 输出字段映射?确认 khs2 v2.4 Phase 25 已验收(见规范 ②)。

**Phase 53 D-04 收窄口径(本 plan 适用)**:工作树检查仅计**代码文件**(pipeline/phases/*.py + plugins/);episodes/ 运行态产物与 .pipeline-state.json 不计。**已知基线(RESEARCH §I.1,2026-08-21)**:p04_character_design.py 与 p09c_storyboard_board.py 两文件 M(khs2 v2.4 Phase 25 mid-flight)——与本 plan 变更面(runner_hooks.py / review_platform.py)**零文件交集**,确认归属后记录进 SUMMARY 即可继续。**严禁 git add -A**(只允许具名文件);本 phase 不做 git commit(orchestrator 在 checker 门后统一处理),SUMMARY 中列出精确改动文件清单。

**变更面合规说明**:runner_hooks.py 的 review_gates 契约缝(终态词汇/decision 提取)与 review_platform.py 的平台客户端提取 = 契约层;不碰 22-phase 内部算法;不涉 p04/p09 字段映射排序约束(排序自查 N/A)。

<interfaces>
runner_hooks.py 既有结构(实读锚点,PATTERNS 摘录):
- Path 2 终态判定 L649-651:`if isinstance(status, dict) and status.get("state") in {"resolved", "closed"}:`
- decision 映射 L652-656:`disposition = str(status.get("disposition", "APPROVED")).upper(); decision = {"APPROVED": "approve", "REJECTED": "reject"}.get(disposition, "contest")`
- chosen 通道 L661-664:`chosen = (status.get("chosen_variant_id") or _chosen_from_suggested(outcome.get("suggested_action")))`
- _chosen_from_suggested L612-615:choose:v{N} 解析器(**零改动**)
- poll_until_terminal L539:次要路径 `state in {"resolved", "closed"}`(顺手对齐)
review_platform.py:
- query_review_status L294-311:返回 {"review_id","state","disposition","version"};degrade envelope 原样返回
测试模板:test_runner_hooks.py L1-27(monkeypatch.setattr(runner_hooks, "_review_client", lambda: fake_client) MagicMock 注入)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 红先——test_poller_complete_state.py(mock client 返 COMPLETE + review_result)</name>
  <files>kais-hermes-skills:plugins/review_gates/tests/test_poller_complete_state.py</files>
  <read_first>
  - /data/workspace/kais-hermes-skills/plugins/review_gates/tests/test_runner_hooks.py(MagicMock 注入模式 L1-27 + tmp_path/monkeypatch.chdir 在案用法)
  - /data/workspace/kais-hermes-skills/plugins/review_gates/runner_hooks.py(Path 2 块 L637-678、poll_until_terminal L539、_chosen_from_suggested L612-615、resolve_direct 与 review-outcomes 写档 L240-256)
  - /data/workspace/kais-hermes-skills/plugins/kais_aigc/review_platform.py(query_review_status L294-311)
  </read_first>
  <behavior>
  - mock _review_client 返 {"review_id":7,"state":"COMPLETE","disposition":"HUMAN","version":3,"result":{"decision":"approve"}} → Path 2 以 decision="approve" resolve
  - 同上但 result.decision="reject" → resolve("reject")
  - 同上但 result.decision="waive" → resolve("approve")(waive→approve 语义映射,C.4 定案)
  - state="COMPLETE" 无 result 键 → 旧 disposition 兜底映射仍生效(APPROVED→approve)
  - state="resolved"/"closed" 仍为终态(并集兼容不回归)
  - result={"decision":"approve","selected":[2]} → chosen 解析出 choose:v2(经 _chosen_from_suggested)
  - query_review_status:fake _request 返 {"data":{"metadata":{"review_result":{"decision":"waive","reason":"x"}},...}} → 返回 dict 含 result 键等于该对象且旧四键齐备;degrade envelope 原样透传
  </behavior>
  <action>
  开工先执行上方 COORD-01 checklist(D-04 收窄口径),结果记 SUMMARY。随后新建 plugins/review_gates/tests/test_poller_complete_state.py,照 test_runner_hooks 的 monkeypatch/MagicMock/tmp_path 模式写上述七组用例;测试直接驱动被测函数(按实读签名选择 Path 2 的实际入口函数/轮询循环可注入的最小驱动方式),不发起真实 HTTP。运行确认全红(词汇未对齐前 COMPLETE 不是终态)。
  </action>
  <verify>
  <automated>cd /data/workspace/kais-hermes-skills && python3 -m pytest plugins/review_gates/tests/test_poller_complete_state.py -v</automated>
  </verify>
  <acceptance_criteria>
  - pytest 退出非零,失败集中在 COMPLETE 终态/result 键/selected 通道相关断言(红即证据)
  - 测试不修改被测源码;不 import 网络;文件头 docstring 注明 "54-03 R2/R3: COMPLETE vocabulary + review_result extraction"
  </acceptance_criteria>
  <done>R2/R3 行为契约为可执行红测试</done>
</task>

<task type="auto">
  <name>Task 2: 绿后——R2 result 键 + R3 词汇对齐/decision 映射/chosen 第三通道</name>
  <files>kais-hermes-skills:plugins/kais_aigc/review_platform.py, kais-hermes-skills:plugins/review_gates/runner_hooks.py</files>
  <read_first>
  - /data/workspace/kais-hermes-skills/plugins/kais_aigc/review_platform.py(query_review_status L294-311 全函数)
  - /data/workspace/kais-hermes-skills/plugins/review_gates/runner_hooks.py(L539、L637-678 Path 2 全块、L612-615)
  </read_first>
  <action>
  **R2(review_platform.py,~6 行)**:query_review_status 返回 dict 增加第五键 `"result": (data.get("metadata") or {}).get("review_result")`;旧四键(review_id/state/disposition/version)与 degrade envelope 原样返回逻辑零改动;docstring 补一句 result 键来源。
  **R3(runner_hooks.py,~20 行)**:
  1. L649-651 Path 2 终态集合 {"resolved","closed"} → {"COMPLETE","resolved","closed"}(并集保守兼容);L539 poll_until_terminal 的 {"resolved","closed"} 同步加 "COMPLETE"。
  2. decision 来源改为优先 result:`raw = status.get("result") or {}; decision = {"approve":"approve","reject":"reject","waive":"approve"}.get(str(raw.get("decision","")).lower())`——取到即用;无 decision 键时走既有 disposition 兜底映射(APPROVED→approve/REJECTED→reject/其他 contest,degrade envelope 场景)。
  3. chosen 通道 L661-664:在既有两路(chosen_variant_id / _chosen_from_suggested(suggested_action))之后补第三路——`sel = (status.get("result") or {}).get("selected") or []; chosen = chosen or (f"choose:v{sel[0]}" if sel else None)` 之后仍经 _chosen_from_suggested 解析成 variant 索引(解析器 L612-615 零改动;实现时可按实读代码结构调整喂入方式,保持"构造 choose:v{N} 字符串复用解析器"的语义)。
  4. 不改:resolve_direct、review-outcomes 写档、_gate_guard、runner.py、gates.yaml、任何 pipeline/phases/*。
  </action>
  <verify>
  <automated>cd /data/workspace/kais-hermes-skills && python3 -m pytest plugins/review_gates/tests plugins/kais_aigc/tests</automated>
  </verify>
  <acceptance_criteria>
  - python3 -m pytest plugins/review_gates/tests plugins/kais_aigc/tests 全绿(含 Task 1 七组用例与既有全量)
  - grep -n 'COMPLETE' plugins/review_gates/runner_hooks.py 在终态判定两处(L539/L649 邻域)各命中
  - grep -n '"result"' plugins/kais_aigc/review_platform.py 命中且旧四键仍在
  - git -C /data/workspace/kais-hermes-skills status --porcelain 相对基线(I.1)新增改动仅为 plugins/kais_aigc/review_platform.py、plugins/review_gates/runner_hooks.py、plugins/review_gates/tests/test_poller_complete_state.py(新增)三处;pipeline/phases/p04/p09c 的既有 M 状态未被动过
  </acceptance_criteria>
  <done>R2+R3 落地:平台 COMPLETE+decision → kmc resolve → review-outcomes/PipelineState 消费链贯通(与 54-02 部署的平台构成 SC3 后半链)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kmc poller → review-platform | 跨进程 HTTP(30s 轮询);平台响应是外部输入 |
| khs 工作树 | 与 khs2 v2.4 并行会话共享(共享文件系统=信任边界) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-03-01 | Tampering | 并行会话互踩(khs2 v2.4) | mitigate | COORD-01 checklist + D-04 收窄口径工作树检查;零文件交集基线在案;具名文件 add;SUMMARY 记录基线对比 |
| T-54-03-02 | Spoofing/Tampering | 平台返回的 decision 值 | mitigate | decision 枚举白名单映射(仅 approve/reject/waive 三值);未知值落 disposition 兜底/contest,不崩溃不错放大 |
| T-54-03-03 | Repudiation | 消费决策无档 | mitigate | review-outcomes.json 追加式落档 + 平台 audit(既有机器,零改动,测试覆盖 resolve 路径) |
| T-54-03-04 | Tampering | degrade envelope 误判 | mitigate | degrade envelope 原样透传逻辑不动;兜底映射仅对合成 disposition 生效(测试第五组用例锁定) |
| T-54-SC | Tampering | pip installs | accept | 零新增依赖;pytest 既有 |
</threat_model>

<verification>
- khs 两 plugin 目录 pytest 全绿(红→绿全程可追溯)
- 工作树 diff 严格限定三文件
- COORD-01 checklist 执行结果记录于 SUMMARY
</verification>

<success_criteria>
- G1/G2/G3/G4 四缺口在 khs 消费端关闭:kmc poller 能读到平台 COMPLETE 决议并 resolve
- 与 54-02(平台数据面)合起来,SC3 的"平台→kmc"半链成立
</success_criteria>

<output>
Create .planning/phases/54-gate-gate-center-blocking-state-ux/54-03-SUMMARY.md when done(记录:COORD-01 checklist 结果、基线对比、改动文件清单、pytest 输出)
</output>
