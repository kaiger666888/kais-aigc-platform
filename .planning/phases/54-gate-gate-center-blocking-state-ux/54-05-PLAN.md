---
phase: 54-gate-gate-center-blocking-state-ux
plan: 05
type: execute
wave: 2
depends_on: ["54-01", "54-02"]
files_modified:
  - src/lib/gateStateService.ts
  - src/routes/canvas/v2/gate-state.ts
  - src/routes/canvas/v2/gate-ops.ts
  - src/router.ts
  - scripts/verify-phase-54.ts
autonomous: false
requirements: [GATE-01, GATE-03]
must_haves:
  truths:
    - "kap 服务端 20s 轮询 review-platform(source=kais-movie-agent,翻页 fail-closed),diff(id+state+version) 后经 broadcastToProject 广播 gate:state——D-03 落地,前端不直连平台;运行时状态真值源 = review-platform REST GET /api/v1/reviews(envelope {data:{items,next_cursor,has_more}} 翻页),khs gates.yaml 是定义非状态——D-01"
    - "新会话经 GET /api/canvas/v2/gate-state 拿全量快照(四态折叠后展示模型 + degrade 标志);平台不可达时 degrade=true 保留旧快照,绝不折叠为全放行"
    - "POST /api/canvas/v2/gate-ops:zod 严格入参 + fail-closed 三维匹配(reviewId 必属当前 episode 候选集)+ 409=幂等成功(applied:false, cause:already-resolved)+ 成功后即时 re-poll"
    - "S-live 活体:kap gate-state 与直查平台列表按 §E 折叠表逐条一致;ep-ccport-test01 的 2 条 APPROVING review 呈 pending"
  artifacts:
    - path: "src/lib/gateStateService.ts"
      provides: "GateStateService 单例:lazy 启动 20s interval + pollOnce + pollNow + scope 注册表 + fold + blocking 推导 + diff 广播 + degrade"
      contains: "gate:state"
    - path: "src/routes/canvas/v2/gate-ops.ts"
      provides: "approve/reject/waive 三操作主通道(await 主操作,非 fire-and-forget)"
      contains: "already-resolved"
    - path: "src/routes/canvas/v2/gate-state.ts"
      provides: "GET 快照端点(miss/stale 触发即时拉取)"
      contains: "gate-state"
  key_links:
    - from: "src/lib/gateStateService.ts"
      to: "REVIEW_PLATFORM_URL /api/v1/reviews?source=kais-movie-agent"
      via: "依赖注入 fetch + MAX_LIST_PAGES=10 fail-closed 翻页(照抄 reviewBridge L139-177)"
      pattern: "kais-movie-agent"
    - from: "src/routes/canvas/v2/gate-ops.ts"
      to: "review-platform approve/reject/waive 端点"
      via: "await 主操作;409→200 applied:false;成功后 void pollNow().catch()"
      pattern: "pollNow"
    - from: "src/lib/gateStateService.ts"
      to: "src/utils/ws.ts broadcastToProject"
      via: "diff 后 broadcastToProject(projectId, 'gate:state', payload)"
      pattern: "broadcastToProject"
    - from: "src/router.ts"
      to: "两新路由"
      via: "route 编号顺延注册(执行时核对实际空位;53-07 未执行 plan 已占位 route168,预期 route169/route170)"
      pattern: "gate-ops"
must_haves_notes: "D-01(真值源=review-platform REST GET /api/v1/reviews)由本 plan 的轮询实现落地"
---

<objective>
kap 服务端主体:① GateStateService 轮询单例(20s,fail-closed 翻页,episode/phase 三维过滤,fold 四态,blocking 推导,diff 后 gate:state 广播,degrade 降级);② GET gate-state 快照端点 + POST gate-ops 操作端点(独立端点,不合流 select-winner——RESEARCH §H 定案);③ verify-phase-54 填充 S-poller/S-ops + 生产重启 + S-live 活体比对。
Purpose: GATE-01 的同步机制(D-03)+ GATE-03 的 kap 侧操作通道;SC1 的活体证明。
Output: 服务端三新文件 + router 注册 + verify 全节绿 + 生产 10588 活体验证。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-CONTEXT.md
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-RESEARCH.md(§D 轮询设计/§H 通道定案/§I.2 env/§Code Examples)
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-PATTERNS.md(gate-ops/gate-state/verify 骨架三节)
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-01-SUMMARY.md(gateCatalog/foldDisplayState/filterEpisodePhaseCandidates 产物)
@src/lib/reviewBridge.ts
@src/routes/canvas/v2/select-winner.ts

<interfaces>
GateStatePayload(与 54-04 前端逐字段一致,双侧钉死):
GateStateGate { gateId, phaseId, label, display: 'pending'|'approve'|'reject'|'waive'|'auto', reviewId?, updatedAt?, note? }
GateStatePayload { projectId, episodesId, fetchedAt, degrade, blocking: {gateId, reviewId, phaseId, label} | null, gates: GateStateGate[] }
GET gate-state HTTP 响应另加诊断字段 episodeRefs?: string[](socket 事件体不带)。

消费的 54-01 产物:src/lib/gateCatalog.ts(GATE_CATALOG/GATE_DISPLAY_NAMES/deriveGateId/LEGACY 别名表/foldDisplayState/EXPECTED_GATE_COUNT);src/lib/reviewBridge.ts(leadingPhaseToken/filterEpisodePhaseCandidates 已导出)。

episodeRef 解析设计(planner 裁定,RESEARCH 未覆盖的映射空白):kap 无 episode 名称实体(实查 ProjectEpisode={id,nodeCount},canvas meta 无名字),而平台 content_ref 的 episode segment 是 kmc 剧集名(活体 ep-ccport-test01)。resolveEpisodeRefs(scope) 三层:
1. 显式 override:GET gate-state 查询参数 episodeRef(测试/运维通道);
2. legacy 形态:ep{episodesId} 与 String(episodesId)(Phase 49 reviewBridge 既有候选形态);
3. 画布探针:listNodes(scope) 的节点 data(filePath/sourceId/原始 JSON 字符串)中正则提取 /episodes\/(ep-[^/"']+)/ 或 ^(ep-[A-Za-z0-9_-]+) 的最高频 token(kmc 工作目录命名),结果缓存于 scope 注册表。
三层合并为 Set;探针无命中时仅剩 legacy 形态——fail-closed(不猜、不错配)。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gateStateService.ts 轮询单例 + S-poller 单测节</name>
  <files>src/lib/gateStateService.ts, scripts/verify-phase-54.ts</files>
  <read_first>
  - src/lib/reviewBridge.ts(deps 注入 L74-79、baseUrl 解析 L120-121、翻页循环 L139-177、approve 409 语义 L237-240)
  - src/lib/gateCatalog.ts(54-01 产物:GATE_CATALOG/foldDisplayState/GATE_DISPLAY_NAMES/LEGACY 别名)
  - src/lib/comfyuiPoll.ts(poller 骨架先例 L29-52)
  - src/utils/ws.ts(broadcastToProject L13-23)+ src/socket/index.ts(房间 join L23-26)
  - src/lib/canvasRelationalStore.ts(listNodes(scope) 签名——episodeRef 画布探针数据源)
  - .planning/phases/54-gate-gate-center-blocking-state-ux/54-RESEARCH.md §D.1/§D.2
  </read_first>
  <behavior>
  - pollOnce:注入 fetchImpl 返回多页(has_more 翻页)→ 全量收集;第 MAX_LIST_PAGES(10)页仍 has_more → truncated → 该 scope degrade=true,不广播部分数据(fail-closed)
  - 过滤:content_ref episode segment ∈ resolveEpisodeRefs 集合 且 leadingPhaseToken(item.type) 等值匹配门 token(WR-01);item.type 等于 derivedGateId **或** 其 legacy 别名也命中
  - 折叠:每平台门取最新(review_id 最大)review → foldDisplayState;红线 3 门恒 display='auto';从未提交的门 display='pending' 无 reviewId
  - blocking:gates 中 display='pending' 且有 reviewId 的最大 reviewId 者 → {gateId,reviewId,phaseId,label};无则 null
  - diff:同一 scope 两次 poll 结果签名(per-gate display+reviewId+updatedAt+note)不变 → 不广播;变化或 degrade 切换 → broadcastToProject(projectId,'gate:state',payload)
  - 平台 fetch 异常 → degrade=true + 保留 last snapshot + fetchedAt 不更新;恢复后 degrade=false 并广播
  </behavior>
  <action>
  新建 src/lib/gateStateService.ts:
  1. deps 全注入 {baseUrl?, fetchImpl?, logger?, intervalMs?}(intervalMs 默认 20000,env GATE_POLL_INTERVAL_MS 可覆盖;baseUrl 解析照 reviewBridge L120-121 显式走 REVIEW_PLATFORM_URL + 去尾斜杠——P2:默认常量宿主不可达)。
  2. 拉取:GET /api/v1/reviews?source=kais-movie-agent&limit=100 + next_cursor 翻页,MAX_LIST_PAGES=10,超限 truncated→degrade(宁可漏过不可错批)。
  3. resolveEpisodeRefs(scope):按 context interfaces 三层实现(override 参数 / legacy 双形态 / listNodes 画布探针正则提取最高频 ep-* token,探针结果缓存);listNodes 经可注入 reader(deps.nodesReader?,默认真实现)以便测试注入。
  4. scope 注册表:Map key `${projectId}:${episodesId}` → {episodeRefs, lastPayload, lastSignature, lastAccessedAt};ensureScope(scope) lazy 启动全局 interval(首个 scope 注册时);30min 未访问的 scope 淘汰;pollNow(scope) 立即拉取并返回 payload。
  5. 组装 payload:16 门(GATE_CATALOG 管线序)→ GateStateGate[](label 来自 GATE_DISPLAY_NAMES;note 取 review_result.reason 截断 80 字符或 legacy/系统拦截标注文案键)+ blocking 推导 + degrade;foldDisplayState 在服务端完成(D-04:平台原始态不泄给前端)。
  6. 模块级单例导出 getGateStateService()(进程级,lazy);import 本模块**不**启动任何 timer(纯副作用零)。
  7. verify-phase-54.ts 填充 **S-poller** 节(替换 FILLED-BY-54-05 占位):注入 fake fetchImpl/nodesReader 覆盖 behavior 全部分支(翻页/truncation/diff-不广播/变化-广播/异常-degrade/红线 auto/legacy 别名命中);结果经捕获的 broadcast 钩子断言(deps.broadcast? 可注入,默认 broadcastToProject)。
  </action>
  <verify>
  <automated>npm run verify:phase-54 && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
  - npm run verify:phase-54:S-poller ≥8 条 PASS;S-catalog/S-fold 仍绿;S-live/S-ops 显示 SKIP: FILLED-BY-54-05(本 task 后仅 S-live/S-ops 剩)
  - grep -c "startsWith" src/lib/gateStateService.ts 输出 0
  - grep -n "REVIEW_PLATFORM_URL" src/lib/gateStateService.ts 命中;模块顶层无 setInterval 直呼(lazy 启动)
  </acceptance_criteria>
  <done>轮询/折叠/广播/降级四机制落地并被 S-poller 锁定</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gate-state + gate-ops 路由 + router 注册 + S-ops 节</name>
  <files>src/routes/canvas/v2/gate-state.ts, src/routes/canvas/v2/gate-ops.ts, src/router.ts, scripts/verify-phase-54.ts</files>
  <read_first>
  - src/routes/canvas/v2/select-winner.ts(imports+router L1-13、inline safeParse 400 L46-49、双 backstop L151-158、broadcast L160-166、500 catch L171-174)
  - src/routes/canvas/v2/health.ts(GET + success envelope + 503 模式 L13-41)
  - src/router.ts(L173 import 区 + L200 挂载区——route167 先例;**执行时核对实际最大编号**:53-07 未执行 plan 已占位 route168,预期本处 route169(gate-ops)/route170(gate-state),若已被占则顺延)
  - src/lib/responseFormat.ts(success/error)
  - src/lib/reviewBridge.ts(approve body 写法 L222-235 + 409 处理 L237-240)
  - .planning/phases/54-gate-gate-center-blocking-state-ux/54-RESEARCH.md §H + §Code Examples(gate-ops 骨架)
  </read_first>
  <behavior>
  - POST gate-ops 缺 reason 而 action=reject/waive → 400(zod refine)
  - reviewId 不属于当前 (projectId,episodesId) 候选集 → 422 error("review 不属于当前剧集")(fail-closed,绝不转发)
  - 平台返回 409 → 200 {applied:false, cause:"already-resolved"} + 触发 re-poll(P4 幂等成功语义,不当错误弹)
  - 平台 2xx → 200 {applied:true, gateId} + void pollNow(scope).catch(()=>{})
  - 平台其他非 2xx/网络异常 → 502 error("审核平台调用失败")
  - GET gate-state 返回 success({fetchedAt, blocking, gates, degrade, episodeRefs});stale(fetchedAt 距今超过 interval)时先 await pollNow(带超时保护)再回
  </behavior>
  <action>
  1. **gate-ops.ts**:照 select-winner 骨架。zod schema:{projectId: z.number(), episodesId: z.number(), reviewId: z.number().int(), action: z.enum(["approve","reject","waive"]), reason: z.string().min(1).max(500).optional(), selected: z.array(z.number().int()).max(20).optional()} + superRefine:action∈{reject,waive} 时 reason 必填。流程:①parse 失败 → 400 error("参数校验失败", issues)(select-winner L46-49 同法);②const scopeCandidates = await gateStateService.pollNow({projectId,episodesId})(新鲜候选)→ filterEpisodePhaseCandidates/候选 id 集内找不到 reviewId → 422;③按 action 调平台(服务端 fetch 经注入或直接 reviewBridge 风格 client:approve → POST /api/v1/reviews/{id}/approve body {comment?, result: selected?{selected}:undefined};reject/waive → POST /{id}/reject 或 /{id}/waive body {reason})——**await 主操作**,非 fire-and-forget;409 映射 applied:false;④void gateStateService.pollNow(scope).catch(()=>{})(即时 re-poll,fire-and-forget 唯一处);⑤回 success({applied, cause?, gateId})。
  2. **gate-state.ts**:GET "/",query projectId/episodesId(z.coerce.number())+ 可选 episodeRef;调 ensureScope+stale 判定 → 必要时 await pollNow;回 success(payload + episodeRefs 诊断字段);服务内部异常 → 500 error("gate 状态获取失败")。**不返回 503-on-degrade**——degrade 是数据字段(fail-closed 展示,不是请求失败)。
  3. **router.ts**:import 两路由并 app.use("/api/canvas/v2/gate-ops", routeN) / app.use("/api/canvas/v2/gate-state", routeN+1)(编号按执行时空位,预期 169/170)。
  4. verify-phase-54.ts 填充 **S-ops** 节:**spawn 子进程 dispatch**(49-01 在案模式——路由模块间接引 DB/单例,勿与 verify 主进程同进程):脚本内先起一个 node:http stub 平台(ephemeral port,模拟 GET /api/v1/reviews 列表 + approve/reject/waive 三端点,可编程返回 409 一次);再 spawn `npx tsx scripts/verify-phase-54.ts --s-ops-child`(env REVIEW_PLATFORM_URL 指向 stub)——child 分支挂 express+两路由+stub auth((req,res,next)=>next()),对 localhost 发真实 HTTP 断言 behavior 六条(400/422/409 幂等/2xx/502/stale-snapshot);child 打印 JSON 结果,父进程汇总。stub 平台同时断言收到的 approve body 形状(result 仅在 selected 提供时出现)。**执行者护栏**:stub 平台保持最小化(仅 reviews 列表 + approve/reject/waive 三端点的可编程返回),一切断言以 S-ops 子进程打印的 JSON 结果契约为准——spawn/端口脆弱时优先修 child 分支的 JSON 输出与退出码,不在父进程内联 HTTP 断言(spawn 脆弱性兜底,49-01 在案教训)。
  </action>
  <verify>
  <automated>npm run verify:phase-54 && npx tsc --noEmit && grep -n 'app.use("/api/canvas/v2/gate-ops"' src/router.ts && grep -n 'app.use("/api/canvas/v2/gate-state"' src/router.ts</automated>
  </verify>
  <acceptance_criteria>
  - npm run verify:phase-54:S-ops ≥6 条 PASS(含 409→applied:false 与 422 fail-closed);仅剩 S-live SKIP
  - router.ts 两挂载行存在且编号不与既有冲突(grep route1 段核对)
  - gate-ops.ts 含 "already-resolved" 字符串;平台调用为 await(非 void);仅 pollNow 一处 void+.catch
  </acceptance_criteria>
  <done>SC3 的 kap 操作通道 + SC1 的快照通道上线,S-ops 契约锁定</done>
</task>

<task type="auto">
  <name>Task 3: 生产重启 + S-live 活体比对(10588)</name>
  <files>scripts/verify-phase-54.ts</files>
  <read_first>
  - .env(REVIEW_PLATFORM_URL 已修,54-01 Task 2)
  - .planning/phases/54-gate-gate-center-blocking-state-ux/54-02-SUMMARY.md(平台已部署 R1)
  - .planning/phases/54-gate-gate-center-blocking-state-ux/54-RESEARCH.md §B.3(活体 3 条 review 现值)/§I.2
  </read_first>
  <action>
  1. 重启 kap 生产服务使 env+新路由生效:`sudo -n systemctl restart kais-aigc-platform`;若 sudo 无免密权限,**创建 human-action checkpoint 请用户执行重启**(提示词给出确切命令),重启完成后继续。验证:curl -sf http://localhost:10588/api/canvas/v2/gate-state?projectId=1\&episodesId=1 返回 200 envelope(任一合法 scope 即证明路由挂载;404/502 即失败)。
  2. 定位真实 scope:查询 db2.sqlite 的 canvas_graph_meta(或 listNodes)找到节点 filePath 含 ep-ccport-test01 的 (project_id, episodes_id);S-live 用该 scope。
  3. verify-phase-54.ts 填充 **S-live** 节:并发取得 a) GET localhost:10588/api/canvas/v2/gate-state?真实scope;b) 直查 localhost:8090/api/v1/reviews?source=kais-movie-agent;断言:gate-state gates[] 中 p11c-gate 与 p13-gate 均 display='pending'(两条 APPROVING 活体,§E 折叠)、blocking.gateId === 'p13-gate'(最大 reviewId=3;若活体数据已变,按"逐门与直查列表折叠结果一致"的通用断言为准并打印对照表)、smoke 类 review 不出现(source 过滤)、红线 3 门 display='auto'、ep-ccport-test01 ∈ episodeRefs。数据断言做成对照驱动(打印 live 对照表,防活体漂移误报)。
  4. SUMMARY 记录:重启命令、真实 scope、对照表、S-live PASS 证据。
  </action>
  <verify>
  <automated>npm run verify:phase-54</automated>
  </verify>
  <acceptance_criteria>
  - npm run verify:phase-54 全节 PASS(S-catalog/S-fold/S-forced-fail/S-poller/S-ops/S-live),无 SKIP 残留
  - 10588 上 gate-state 返回 200 且 gates 数组长度 16;degrade=false
  - S-live 对照表显示平台直查与 kap 折叠逐门一致
  </acceptance_criteria>
  <done>SC1 活体成立:平台侧状态变更 → kap 轮询 → 折叠呈现一致</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kap 服务端 → review-platform(localhost:8090) | 无认证内网服务;其响应(列表/decision)是外部输入 |
| 浏览器 → kap gate-ops(写操作) | 经 kap auth 中间件;对象级授权靠 fail-closed 三维匹配 |
| systemd 生产进程 | 重启操作需 root 权限(human-action 动态升级) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-05-01 | Elevation of Privilege / Tampering | 错批他集 review(wrong-approve) | mitigate | reviewId 必须 ∈ 当前 scope 候选集(episode segment 等值 + phase token 等值,WR-01);不命中 422 拒操作;S-ops 锁定 |
| T-54-05-02 | Tampering | 平台不可达 → 假"全放行" | mitigate | degrade=true + 旧快照保留,fail-closed 永不折叠为 approve;S-poller 异常分支锁定 |
| T-54-05-03 | Information Disclosure | 平台内部态泄露前端 | mitigate | 服务端 foldDisplayState 后才出 payload;POLICY_EVAL/disposition 不进 GateStatePayload(D-04) |
| T-54-05-04 | DoS | 轮询压垮平台 | mitigate | 20s 单请求(活体总量 3 条)+ MAX_LIST_PAGES=10 fail-closed + lazy 启动(空画布不空转) |
| T-54-05-05 | Spoofing | 409 误报为失败/成功歧义 | mitigate | 409 统一映射 applied:false, cause:"already-resolved"(幂等成功);S-ops 断言 |
| T-54-05-06 | Repudiation | 操作无追溯 | mitigate | 平台 audit trail(action=approve/reject/waive,54-02)+ payload 带 reviewId/updatedAt;gate-ops 日志含 [canvas:v2/gate-ops] 前缀 |
| T-54-SC | Tampering | npm installs | accept | 零新增依赖 |
</threat_model>

<verification>
- npm run verify:phase-54 全节 PASS(六节,无占位)
- npx tsc --noEmit 干净;10588 活体 gate-state 200
- router 两挂载编号无冲突
</verification>

<success_criteria>
- D-03 同步机制上线(20s 轮询 + diff + gate:state 广播 + 新会话快照);D-01 真值源唯一经 review-platform REST
- GATE-03 kap 侧通道上线(409 幂等 + fail-closed 匹配)
- SC1 活体证据(S-live 对照表)在 SUMMARY
</success_criteria>

<output>
Create .planning/phases/54-gate-gate-center-blocking-state-ux/54-05-SUMMARY.md when done
</output>
