---
phase: 54-gate-gate-center-blocking-state-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - kais-review-platform:app/api/v1/actions.py
  - kais-review-platform:app/models/schemas.py
  - kais-review-platform:tests/test_approve_reject.py
autonomous: true
requirements: [GATE-03]
must_haves:
  truths:
    - "approve 恒写 metadata_json.review_result.decision='approve'(无论 request.result 是否携带)——G2 缺口关闭的前半"
    - "reject 补写 metadata_json.review_result={decision:'reject', reason}(镜像 approve 原子写)——决策值首次落 review 记录"
    - "POST /api/v1/reviews/{id}/waive 端点存在:APPROVING→COMPLETE、reason 必填 1-500、review_result={decision:'waive',reason}——G5 缺口关闭"
    - "部署后活体 round-trip:submit→waive→GET 能读到 metadata.review_result.decision"
  artifacts:
    - path: "kais-review-platform:app/api/v1/actions.py"
      provides: "approve 恒写 decision / reject 补写 review_result / waive 端点(镜像 reject)"
      contains: "waive"
    - path: "kais-review-platform:app/models/schemas.py"
      provides: "WaiveRequest(reason Field(min_length=1, max_length=500))"
      contains: "WaiveRequest"
    - path: "kais-review-platform:tests/test_approve_reject.py"
      provides: "三操作 decision 持久化回归用例"
      contains: "waive"
  key_links:
    - from: "khs:plugins/kais_aigc/review_platform.py query_review_status"
      to: "kais-review-platform:GET /api/v1/reviews/{id} metadata.review_result"
      via: "R2(54-03)提取 result 键——本 plan 是该链路的数据生产端"
      pattern: "review_result"
    - from: "kap:src/routes/canvas/v2/gate-ops.ts(54-05)"
      to: "kais-review-platform:app/api/v1/actions.py"
      via: "approve/reject/waive 三端点(await 主操作)"
      pattern: "waive"
---

<objective>
review-platform R1(GATE-03 三腿之一):关闭"决策值不在 review 记录"协议缺口——approve 恒写 review_result.decision、reject 补写 review_result(含 reason)、新增 waive 端点;配 pytest + docker 重建部署 + 活体冒烟。
Purpose: SC3 round-trip 的第一环——画布审批操作的决策必须落在 kmc poller 可读的通道(GET metadata.review_result)。没有 R1,后续 R2/R3 无数据可消费。
Output: actions.py/schemas.py 最小变更(~25 行)+ 测试扩展 + 已部署活体平台。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-CONTEXT.md
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-RESEARCH.md
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-PATTERNS.md

**范围裁定(manager constraint 2)**:本 plan 是 54-CONTEXT.md Claude's-Discretion 条款"必要时 review-platform callback 字段…属契约层修改"授权的最小 R1 契约补齐(decision 字段持久化 + waive 端点 + 测试)。REQUIREMENTS.md Out-of-Scope 的"review-platform 消费侧改造"指 SC-4 chosen_variant_id 跨仓债,不含本补齐。若实现中发现需要超出此清单的改动(改 state_machine/worker/policy),**停止扩面**,在 SUMMARY 记录 deviation。

<interfaces>
actions.py 既有结构(实读锚点):
- approve_review L324-391:须 APPROVING 否则 409(L350-354);body {comment?, result?{selected,scores,feedback}};现仅 if request.result 才写 metadata_json.review_result(L359-374,extra_updates 原子通道);comment 只进 audit payload(L373)
- reject_review L399-458:reason 必填(RejectRequest min_length=1 max_length=500);reason 现只进 audit payload(L441),不写 metadata
- transition_state(db, review.id, ReviewState.APPROVING, ReviewState.COMPLETE, review.version, actor, action=..., payload=..., extra_updates={"metadata_json": metadata} 可选)
- 头注释 L1-9 端点清单(waive 须注册)
- _resolve_actor(review_id, token, client, redis) / StateConflictError→409 / InvalidTransitionError→409 / await db.refresh(review) / ApiResponse(data=_review_response(review)...)
- schemas.py L299-301 RejectRequest = WaiveRequest 的镜像模板
- tests/test_approve_reject.py L23-45 approving_review fixture(transition_state 两跳造 APPROVING)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 平台测试先行(红)——decision 持久化 + waive 端点用例</name>
  <files>kais-review-platform:tests/test_approve_reject.py</files>
  <read_first>
  - /data/workspace/kais-review-platform/tests/test_approve_reject.py(fixture 模式 L23-45 + 既有 approve/reject 用例全文)
  - /data/workspace/kais-review-platform/app/api/v1/actions.py(approve L324-391 / reject L399-458 全文)
  - /data/workspace/kais-review-platform/app/models/schemas.py(RejectRequest L299-301;ReviewResponse)
  </read_first>
  <behavior>
  - approve(不带 result)后 GET /api/v1/reviews/{id}:metadata.review_result.decision == "approve"
  - approve(带 result={selected:[1]})后:decision == "approve" 且 selected == [1](合并不丢)
  - reject 后:metadata.review_result == {decision:"reject", reason:&lt;传入&gt;}
  - POST /{id}/waive reason="..." → 200,GET 后 state == "COMPLETE" 且 review_result.decision == "waive"、reason 回读一致
  - waive 空 reason → 422;waive 于非 APPROVING(已 COMPLETE)→ 409
  - audit timeline 出现 action == "waive" 条目
  </behavior>
  <action>
  在 tests/test_approve_reject.py 追加测试(照既有 fixture/approving_review 的 transition_state 两跳模式与既有断言风格;不新建测试框架):覆盖上述 behavior 六条;waive 相关用例在实现前会 404/断言失败——**保持红**,不做任何让测试提前通过的 stub。
  </action>
  <verify>
  <automated>cd /data/workspace/kais-review-platform && python3 -m pytest tests/test_approve_reject.py -k "decision or waive" -x</automated>
  </verify>
  <acceptance_criteria>
  - 新用例运行结果:waive/decision 相关 case FAIL(红),既有用例不受影响(pytest 退出码非零仅由新 case 贡献)
  - 测试文件不修改任何被测源码
  </acceptance_criteria>
  <done>红测试落档,行为契约可执行</done>
</task>

<task type="auto">
  <name>Task 2: R1 实现——approve 恒写 / reject 补写 / waive 端点(绿)</name>
  <files>kais-review-platform:app/api/v1/actions.py, kais-review-platform:app/models/schemas.py</files>
  <read_first>
  - /data/workspace/kais-review-platform/app/api/v1/actions.py(Task 1 已读;改造点:approve L359-374 条件写、reject L399-458、头注释 L1-9)
  - /data/workspace/kais-review-platform/app/models/schemas.py(L299-301 RejectRequest 镜像位)
  </read_first>
  <action>
  1. **approve_review**:把 L359-374 的 `if request.result:` 条件构造改为恒构造——metadata = review.metadata_json or {};metadata["review_result"] = {"decision": "approve", ...(request.result.model_dump() if request.result else {})};extra_updates 原子通道写法保持不变(仅当构造成功即恒传)。audit payload comment 行为不变。
  2. **reject_review**:在 transition_state 调用处补 extra_updates={"metadata_json": {**(review.metadata_json or {}), "review_result": {"decision": "reject", "reason": request.reason}}}(镜像 approve 的原子写;保留既有 payload={"reason": request.reason} audit 通道)。
  3. **新增 waive_review**:POST /{review_id}/waive,整体镜像 reject_review——同 _resolve_actor/404/409 守卫(APPROVING 前置)/StateConflict/InvalidTransition 处理/db.refresh/ApiResponse 返回;差异仅 action="waive" 与 review_result={"decision":"waive","reason":request.reason}。
  4. **schemas.py**:新增 class WaiveRequest(BaseModel): reason: str = Field(min_length=1, max_length=500)(紧邻 RejectRequest,docstring 注明 waive 请求契约)。
  5. **头注释端点清单**注册 "POST /api/v1/reviews/{review_id}/waive -- Waive a review"(L1-9 清单格式)。
  6. 不动:state_machine、worker/tasks.py、policies、batch 端点、audit stats L178 的 action.in_(["approve","reject"])(waive 属 additive,统计不破坏)。
  </action>
  <verify>
  <automated>cd /data/workspace/kais-review-platform && python3 -m pytest tests/test_approve_reject.py && python3 -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
  - python3 -m pytest tests/test_approve_reject.py 全绿(含 Task 1 六条新 case)
  - 平台全量 pytest tests/ 通过(无既有回归)
  - grep '"/{review_id}/waive"' app/api/v1/actions.py 命中;头注释含 waive 行;schemas.py 含 "class WaiveRequest"
  - git -C /data/workspace/kais-review-platform status --porcelain 仅显示 actions.py/schemas.py/tests/test_approve_reject.py 三文件改动
  </acceptance_criteria>
  <done>R1 三变更落地,平台仓全绿</done>
</task>

<task type="auto">
  <name>Task 3: docker 重建部署 + 活体 round-trip 冒烟</name>
  <files>kais-review-platform:app/api/v1/actions.py(部署,无新改动)</files>
  <read_first>
  - /data/workspace/kais-review-platform/ 根目录 docker-compose*.yml / deploy 脚本(确认服务名 review-api 与重建命令)
  - /data/workspace/kais-review-platform/app/api/v1/reviews.py L73-206(submit 契约:ReviewCreateRequest 必填字段、POST /api/v1/reviews/ 尾斜杠)
  - .planning/phases/54-gate-gate-center-blocking-state-ux/54-RESEARCH.md §I.3(部署走既有 review-api rebuild)
  </read_first>
  <action>
  1. 按仓内既有部署方式重建并重启 review-api 容器(docker compose build + up -d,或仓内 deploy 脚本;以实读 compose 文件为准);重建后 curl http://localhost:8090/api/v1/health 确认 {"status":"ok"}。
  2. 数据完好性:GET /api/v1/reviews?limit=100 确认存量 3 条(id=1/2/3)仍在且状态不变(APPROVING×2 + smoke_test)。
  3. **活体 round-trip 冒烟**(precedent:id=1 就是 smoke_test,允许冒烟数据):POST http://localhost:8090/api/v1/reviews/(**带尾斜杠**)按 ReviewCreateRequest 必填字段最小提交——source="kap-phase54-smoke"、type="smoke-gate-54"、content_ref="ep-phase54-smoke/p13_delivery"、其余必填字段照 schema 实读补齐;路由应到 APPROVING(HUMAN)。随后 POST /api/v1/reviews/{newId}/waive body {"reason":"phase54 deploy smoke"};断言:响应 200,GET /api/v1/reviews/{newId} 返回 state=COMPLETE 且 metadata.review_result.decision="waive"、reason 回读一致。
  4. 冒烟 review 保留在平台(source=kap-phase54-smoke,不会被 kais-movie-agent 过滤器命中,不污染 gate 面板);结果(新 review id + 三段 curl 输出摘录)记入 SUMMARY。
  </action>
  <verify>
  <automated>curl -sf http://localhost:8090/api/v1/health | grep -o '"status":"ok"' && curl -sf http://localhost:8090/api/v1/reviews?limit=100 | grep -c '"review_id"' </automated>
  </verify>
  <acceptance_criteria>
  - review-api 容器 healthy;health ok;存量 review 数 ≥4(3 存量 + 1 冒烟)
  - 冒烟 round-trip:GET 冒烟 review 显示 COMPLETE + metadata.review_result.decision == "waive"(curl 输出在 SUMMARY 为证)
  </acceptance_criteria>
  <done>R1 上线,kmc poller(54-03)与 kap gate-ops(54-05)有了可消费/可调用的决策数据面</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器/kap → review-platform REST | 平台 auth 已摘除(get_current_client 无条件 "api"),网络位置即边界(内网 localhost:8090) |
| review-platform 容器重建窗口 | 部署期间平台短暂不可用;kmc poller 30s 周期会自然重试 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-02-01 | Repudiation | 决策持久化 | mitigate | decision 落 review 记录 + audit trail 双写(approve/reject/waive action 均入 audit timeline);测试断言 GET 可回读 |
| T-54-02-02 | Tampering | waive 端点(新决策通道) | mitigate | 镜像 reject 的 APPROVING 前置 409 + reason 必填 1-500;无批量 waive |
| T-54-02-03 | Spoofing | 平台无认证面 | accept(既有) | 内网拓扑边界;本期不扩认证(超范围);kap 侧另有 fail-closed 三维匹配防错操作对象 |
| T-54-02-04 | DoS | 容器重建 | accept | 单实例测试环境,存量 review 3 条;重建窗口 <1min,poller 自愈 |
| T-54-SC | Tampering | pip installs | accept | 零新增依赖;pytest/uvicorn 既有环境 |
</threat_model>

<verification>
- 平台仓 pytest 全绿(含六条新 decision/waive 用例)
- 活体:waive round-trip curl 三段证据
- 存量数据完好(3 条 review 状态不变)
</verification>

<success_criteria>
- G2/G5 缺口关闭:approve 恒写 decision、reject 写 review_result、waive 端点可用且已部署
- khs query_review_status(54-03 R2)从此有 result 键可提取
</success_criteria>

<output>
Create .planning/phases/54-gate-gate-center-blocking-state-ux/54-02-SUMMARY.md when done(记录:变更 diff 摘要、部署命令、冒烟 review id、curl 证据)
</output>
