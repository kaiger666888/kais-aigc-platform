---
phase: 54-gate-gate-center-blocking-state-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/gateCatalog.ts
  - src/lib/reviewBridge.ts
  - scripts/verify-phase-54.ts
  - package.json
  - .env
autonomous: true
requirements: [GATE-01]
must_haves:
  truths:
    - "kap 侧存在 16 gate 定义快照,与 khs gates.yaml 逐字段一致(khs 改 gates.yaml 时 verify:phase-54 S-catalog 变红)——D-02"
    - "foldDisplayState 按 RESEARCH §E 全表把平台 (state, disposition, review_result) 折叠为 pending/approve/reject/waive 四态——D-04"
    - "生产 .env 配置 REVIEW_PLATFORM_URL 且宿主直连 review-platform 活体可达(502 断点消除)——RESEARCH §I.2 P0 前置"
    - "reviewBridge 的 episode/phase 三维匹配纯函数被导出,供后续 gateStateService 与 gate-ops 共用(WR-01 等值匹配,非前缀)"
  artifacts:
    - path: "src/lib/gateCatalog.ts"
      provides: "D-02 快照 zod + deriveGateId 移植 + legacy 别名表 + 16 计数 + foldDisplayState + U-06 显示名表"
      contains: "foldDisplayState"
    - path: "scripts/verify-phase-54.ts"
      provides: "S-catalog/S-fold/S-forced-fail 契约门 + S-live/S-ops/S-poller 占位链"
      contains: "S-catalog"
    - path: "src/lib/reviewBridge.ts"
      provides: "导出 leadingPhaseToken + 新增 filterEpisodePhaseCandidates 纯函数"
      contains: "filterEpisodePhaseCandidates"
  key_links:
    - from: "scripts/verify-phase-54.ts"
      to: "kais-hermes-skills:plugins/review_gates/gates.yaml"
      via: "js-yaml 解析 + KAIS_HERMES_SKILLS_PATH 约定直读 sibling 仓"
      pattern: "KAIS_HERMES_SKILLS_PATH"
    - from: "src/lib/gateCatalog.ts"
      to: "src/lib/reviewBridge.ts"
      via: "无 import 依赖(平行纯函数);foldDisplayState 枚举值以平台 app/models/schemas.py L11-37 为准"
      pattern: "foldDisplayState"
---

<objective>
Gate 中心的契约地基:① D-02 的 16 gate 定义快照(src/lib/gateCatalog.ts,zod + derive 规则移植 + legacy 别名 + 16 计数)+ D-04 的四态折叠纯函数 foldDisplayState;② reviewBridge 三维匹配纯函数导出(后续轮询/gate-ops 共用);③ verify-phase-54 契约门骨架(S-catalog/S-fold/S-forced-fail)+ npm script;④ P0 环境断点修复(.env REVIEW_PLATFORM_URL)。
Purpose: GATE-01 的状态模型接入以契约为地基;49-D11 协议缺口(SC3)与环境断点(502)都阻塞在此计划的产物上。
Output: gateCatalog.ts、verify-phase-54.ts、.env 修复、reviewBridge 纯函数导出。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-CONTEXT.md
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-RESEARCH.md
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-PATTERNS.md

khs 侧文件本 plan **只读不写**(gates.yaml/gate_config.py 是契约对读对象;变更面检查 N/A,但仍建议在 SUMMARY 记录 khs 工作树观测结果作基线)。

<interfaces>
<!-- 平台 review 记录关键字段(review-platform app/models/schemas.py L11-37 活体审计) -->
- state: "PENDING" | "POLICY_EVAL" | "APPROVING" | "COMPLETE"(COMPLETE 为终态,空迁移集)
- disposition(路由值,非决策): "AUTO" | "HUMAN" | "AI_AUDIT" | "BLOCK"
- metadata.review_result: { decision?: "approve"|"reject"|"waive", reason?: string, selected?: number[], scores?, feedback? }(R1 落地前仅 approve-with-result 才有)

khs derive 规则(gate_config.py):
- _PHASE_PREFIX_RE = ^(p\d+[a-z0-9]*);_REDLINE_SUFFIX_RE = _redline_[a-z_]+$
- derive_gate_id: strip redline 后缀 → 取完整 sub-phase token + "-gate"(p11a0_iframe_qc → p11a0-gate)
- _LEGACY_GATE_ID_TO_PHASE_ID 8 个别名(topic-gate/outline-gate/script-gate/render-gate/delivery-gate/p11-gate→p11b_final_render/3 个 redline detector 名)
- _EXPECTED_GATE_COUNT = 16;红线 3 键(p13_delivery_redline_*)派生 p13-gate 但从不 submit_review

UI-SPEC U-06 显示名表(固化进 catalog):p01-gate 选题定向 / p02-gate 大纲 / p03-gate 剧本审核 / p04-gate 角色设计 / p06-gate 时空剧本 / p07-gate 场景生成 / p09c-gate 分镜板 / p10c-gate 配音审听 / p11a0-gate 条件帧门 / p11a-gate 预览片段 / p11b-gate 最终渲染 / p11c-gate 镜头质检 / p13-gate 成片交付 / 红线×3 情绪红线·无冷开场·完整性红线
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gateCatalog.ts 快照契约 + foldDisplayState + reviewBridge 纯函数导出</name>
  <files>src/lib/gateCatalog.ts, src/lib/reviewBridge.ts</files>
  <read_first>
  - /data/workspace/kais-hermes-skills/plugins/review_gates/gates.yaml(331 行,16 entry 现值——快照的唯一权威源)
  - /data/workspace/kais-hermes-skills/plugins/review_gates/gate_config.py(L91-158 derive 规则 + legacy 别名;L74/L275-278 16 计数断言)
  - src/lib/canvasAssetSchema.ts(平行声明头注释模式 L10-15/L181-182——gateCatalog 头注释照此指向 khs 权威源)
  - src/lib/reviewBridge.ts(L92-106 leadingPhaseToken 纯函数;L196-208 三维 fail-closed 匹配内联实现)
  - .planning/phases/54-gate-gate-center-blocking-state-ux/54-RESEARCH.md §A.1(16 门表)§E(折叠全表)§Code Examples(foldDisplayState 签名)
  - .planning/phases/54-gate-gate-center-blocking-state-ux/54-UI-SPEC.md(U-06 显示名表)
  </read_first>
  <behavior>
  - deriveGateId("p11a0_iframe_qc") === "p11a0-gate"(完整 sub-phase token,非 p11-gate)
  - deriveGateId("p13_delivery_redline_emotion") === "p13-gate" 且 isRedline === true
  - foldDisplayState 全表(§E 9 行):非 COMPLETE 一律 "pending";COMPLETE+decision reject/waive/approve 各归位;COMPLETE+BLOCK 无 decision → "reject";COMPLETE+AUTO 或 HUMAN 无 decision → "approve"(legacy 兼容读法)
  - foldDisplayState("APPROVING","HUMAN",null) === "pending"(主路径:人工门停在 APPROVING)
  - filterEpisodePhaseCandidates 对 episode segment 与 leading phase token 做**等值**匹配;ep-abc 与 ep-abcd 互不匹配(WR-01)
  </behavior>
  <action>
  新建 src/lib/gateCatalog.ts(放置位置裁定:src/lib/——服务端折叠 + verify 脚本直读,P7 纪律;PATTERNS 已裁定此矛盾):
  1. 头 docblock 照 canvasAssetSchema 模式显式指向 khs 权威源:plugins/review_gates/gates.yaml(version 2)+ gate_config.py 的 derive 规则,注明"D-02 平行声明零漂移,khs 改动时 verify:phase-54 S-catalog 变红"。
  2. zod 快照:GATE_ENTRY_SCHEMA(phaseId=yaml key、derivedGateId、mode=blocking|webhook、isRedline boolean;reviewer_role/timeout_sec 等其余 yaml 字段按实读 gates.yaml 镜像为 optional)。GATE_CATALOG = 16 个 entry 按管线序 P01→P13 排列(含 p13 三条红线子键,isRedline=true)。
  3. 移植 derive 规则:PHASE_PREFIX_RE 与 khs _PHASE_PREFIX_RE 同源(^(p\d+[a-z0-9]*)),REDLINE_SUFFIX_RE(_redline_[a-z_]+$),deriveGateId(phaseId) 先 strip redline 后缀再取完整 token 拼接 "-gate";LEGACY_GATE_ID_TO_PHASE_ID 8 条别名表照 gate_config.py L136-158 抄录;EXPECTED_GATE_COUNT=16 常量。
  4. GATE_DISPLAY_NAMES(U-06 全表,红线 3 键显示名:情绪红线/无冷开场/完整性红线)+ GATE_PIPELINE_ORDER(P01→P13 索引)导出。
  5. foldDisplayState(state: string, disposition: string | null, result: { decision?: string } | null) 纯函数,逻辑严格按 RESEARCH §E 全表与 §Code Examples 骨架(非 COMPLETE → pending;decision 优先;BLOCK→reject;AUTO/legacy→approve)。
  修改 src/lib/reviewBridge.ts(最小侵入,不改任何既有行为):
  6. leadingPhaseToken 加 export(若已导出则跳过)。
  7. 新增导出纯函数 filterEpisodePhaseCandidates(items, episodeRefs: Set&lt;string&gt;, phaseToken: string):把 L196-208 内联匹配逻辑提取为可复用函数——item.type 与 phaseToken 经 leadingPhaseToken 后**等值**比较、content_ref 按最后一个 "/" 切 episode segment 与 phase segment、episode segment 必须 ∈ episodeRefs、phase segment 的 leading token 等值匹配;任何字段非 string 返回 false。原内联处改为调用该函数(行为不变)。
  8. 禁止任何 startsWith/前缀匹配(WR-01);禁止 import @/utils barrel(P7——verify 脚本将直接 import 本文件)。
  </action>
  <verify>
  <automated>npx tsc --noEmit && grep -c "foldDisplayState\|deriveGateId\|GATE_DISPLAY_NAMES" src/lib/gateCatalog.ts && grep -c "export function filterEpisodePhaseCandidates" src/lib/reviewBridge.ts</automated>
  </verify>
  <acceptance_criteria>
  - npx tsc --noEmit exit 0
  - src/lib/gateCatalog.ts 导出 foldDisplayState / deriveGateId / GATE_CATALOG / GATE_DISPLAY_NAMES / EXPECTED_GATE_COUNT;GATE_CATALOG.length === 16(红线 3 条 isRedline=true)
  - grep -c "startsWith" src/lib/gateCatalog.ts 输出 0(等值匹配铁律)
  - src/lib/reviewBridge.ts 含 "export function filterEpisodePhaseCandidates";原 resolveOpenReviewForSelection 行为不变(该函数其余代码零改动)
  </acceptance_criteria>
  <done>gateCatalog 快照+折叠纯函数落地,reviewBridge 纯函数导出,tsc 干净</done>
</task>

<task type="auto">
  <name>Task 2: P0 环境修复——.env REVIEW_PLATFORM_URL + 宿主直连活体验证</name>
  <files>.env</files>
  <read_first>
  - .env(现值:只有 REVIEW_PORT=8090,无 REVIEW_PLATFORM_URL)
  - .planning/phases/54-gate-gate-center-blocking-state-ux/54-RESEARCH.md §I.2(502 复现证据与修复路径)、§B.2(端点面)
  </read_first>
  <action>
  1. .env 增加(或修正)一行 REVIEW_PLATFORM_URL=http://localhost:8090(宿主 systemd 进程同机直连 review-nginx,不经 Tailscale;REVIEW_PORT=8090 保留不动)。代码内默认常量 http://review-platform:8090 不改——env 覆盖即修复。
  2. 活体验证(无需重启 kap,直接打平台):curl http://localhost:8090/api/v1/health 期望 {"status":"ok",...};curl "http://localhost:8090/api/v1/reviews?limit=3" 期望 envelope {data:{items,next_cursor,has_more}},当前活体含 id=1/2/3(smoke_test、p11c-gate、p13-gate)。
  3. 在 SUMMARY 记录:kap 服务进程读取 env 于启动时,故 src/router 挂载后的生效重启安排在 54-05 Task 3(S-live 之前);本 task 只修配置 + 直连验证。
  </action>
  <verify>
  <automated>grep -n "^REVIEW_PLATFORM_URL=http://localhost:8090$" .env && curl -sf http://localhost:8090/api/v1/health | grep -o '"status":"ok"' && curl -sf "http://localhost:8090/api/v1/reviews?limit=3" | grep -o '"has_more"'</automated>
  </verify>
  <acceptance_criteria>
  - .env 含 REVIEW_PLATFORM_URL=http://localhost:8090(唯一一行,无重复键)
  - 平台 health 返回 status ok;reviews 列表 envelope 可解析(items 数组存在)
  </acceptance_criteria>
  <done>502 断点在生产配置层消除,活体直连证据记录在案</done>
</task>

<task type="auto">
  <name>Task 3: verify-phase-54.ts 骨架(S-catalog/S-fold/S-forced-fail)+ npm script 注册</name>
  <files>scripts/verify-phase-54.ts, package.json</files>
  <read_first>
  - scripts/verify-phase-51.ts(骨架:assert helper + mkdtemp 隔离 + S-section 头注释契约 L1-83)
  - scripts/verify-manifest-contract.ts(sibling 仓读取约定 KAIS_HERMES_SKILLS_PATH L27-32)
  - package.json(L43-44 verify:phase-50/51 注册先例)
  - /data/workspace/kais-hermes-skills/plugins/review_gates/gates.yaml(Task 1 已读,契约断言对象)
  - .planning/phases/54-gate-gate-center-blocking-state-ux/54-RESEARCH.md §A.1/§E(断言表来源)
  </read_first>
  <action>
  新建 scripts/verify-phase-54.ts,照 verify-phase-51 S-section 模式,头注释声明各 S 节断言面:
  1. **S-catalog**:用 js-yaml(根 package.json 既有 ^4.2.0 依赖)解析 `${KAIS_HERMES_SKILLS_PATH ?? "/data/workspace/kais-hermes-skills"}/plugins/review_gates/gates.yaml`;断言:a) yaml entry 计数 === EXPECTED_GATE_COUNT === 16;b) 快照每个 phaseId 在 yaml 中存在且 mode(含 p11b webhook)一致;c) deriveGateId(phaseId) round-trip 对 16 键逐一成立(红线键 strip 后派生 p13-gate);d) 3 条红线键 isRedline=true 且标记 platformInvisible(从不 submit_review → 静态「自动扫描」);e) legacy 别名抽检:p11-gate → p11b_final_render、topic-gate → p01_hook_topic、delivery-gate → p13_delivery。纯 TS 直读,**不 spawn pytest**(P7:脚本只 import gateCatalog + js-yaml,不碰 @/utils)。
  2. **S-fold**:枚举 RESEARCH §E 全表 9 分支逐一断言 foldDisplayState(PENDING/POLICY_EVAL/APPROVING→pending;COMPLETE×decision approve/reject/waive;COMPLETE+BLOCK→reject;COMPLETE+AUTO→approve;COMPLETE+HUMAN 无 decision→approve legacy)。
  3. **S-forced-fail 自检**(证明门能红):对**内存中**解析出的 yaml 对象做变异(删一个 entry / 改一个 mode),重跑 S-catalog 的 diff 逻辑,断言变异被检出(报 mismatch)——绝不写 khs 文件。
  4. **占位链**:S-live / S-ops / S-poller 三节声明为 TODO 占位,输出显式 "SKIP: FILLED-BY-54-05" 行(53-04 占位链先例)。
  5. package.json scripts 注册 "verify:phase-54": "npx tsx scripts/verify-phase-54.ts"(紧邻 verify:phase-51)。
  </action>
  <verify>
  <automated>npm run verify:phase-54</automated>
  </verify>
  <acceptance_criteria>
  - npm run verify:phase-54 exit 0;输出含 S-catalog(≥6 条 PASS,含 "16")、S-fold(9 分支 PASS)、forced-fail 自检 PASS
  - 输出含三行 "SKIP: FILLED-BY-54-05"(S-live/S-ops/S-poller 占位)
  - package.json 含 "verify:phase-54" 键;脚本 grep -c "@/" 输出 0(自包含纪律)
  - 强制红验证:临时(仅在 shell 管道中)将脚本对 gates.yaml 的路径指向不存在的文件再跑 → 非零退出(在 SUMMARY 记录,不改脚本)
  </acceptance_criteria>
  <done>D-02 零漂移契约门生效:khs gates.yaml 任何变更即红;D-04 折叠表有永久回归网</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kap verify 脚本 → khs 文件系统 | 契约测试只读 sibling 仓 gates.yaml;路径来自 KAIS_HERMES_SKILLS_PATH env(可被篡改指向假 yaml) |
| kap → review-platform(localhost:8090) | 内网无认证服务(auth 已摘除);网络位置即边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01-01 | Tampering | S-catalog 契约测试 | mitigate | 路径仅覆盖自用;断言 16 计数+逐字段 diff,任何 gates.yaml 漂移(含被误改)即红 |
| T-54-01-02 | Information Disclosure | REVIEW_PLATFORM_URL | accept | localhost 直连属内网拓扑,不新增对外暴露;浏览器侧仍不直连(D-03) |
| T-54-01-03 | Tampering | foldDisplayState legacy 分支 | mitigate | legacy 无 decision 的 COMPLETE 读作 approve 仅显示层;S-fold 锁表防回归,note 标注 legacy(UI 层文案) |
| T-54-SC | Tampering | npm/pip installs | accept | 本 plan 零新增依赖(js-yaml/zod 均既有);无 [ASSUMED]/[SUS] 包,无安装步骤 |
</threat_model>

<verification>
- npx tsc --noEmit 干净
- npm run verify:phase-54:S-catalog+S-fold+S-forced-fail 全 PASS,占位链显式 SKIP
- curl localhost:8090 平台活体 OK(env 修复证据)
</verification>

<success_criteria>
- D-02 快照+契约测试落地:khs gates.yaml 篡改场景下 verify:phase-54 可红(forced-fail 自检证明)
- D-04 折叠全表(§E 9 分支)有自动化断言
- 生产 502 断点在配置层消除
</success_criteria>

<output>
Create .planning/phases/54-gate-gate-center-blocking-state-ux/54-01-SUMMARY.md when done
</output>
