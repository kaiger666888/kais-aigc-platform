---
phase: 54-gate-gate-center-blocking-state-ux
plan: 07
type: execute
wave: 4
depends_on: ["54-06"]
files_modified:
  - packages/infinite-canvas/src/components/gate/GateCenterBlock.tsx
  - packages/infinite-canvas/src/components/gate/__tests__/GateCenterBlock.test.tsx
  - packages/infinite-canvas/src/components/gate/GateCenterPanel.tsx
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
  - .planning/phases/54-gate-gate-center-blocking-state-ux/54-VALIDATION.md
autonomous: true
requirements: [GATE-02, GATE-03]
must_haves:
  truths:
    - "Gate 中心面板(420px dock + 可内嵌 Block)展示 16 门四态清单 + 阻塞门 review 富字段 + 降级横幅——GATE-01 浏览面 + GATE-02 操作面"
    - "面板内 放行(单点击)/驳回/豁免(必填理由+二次确认)→ gateOps → 成功行级翻转、409 幂等 toast、失败回滚——GATE-03 前端闭环,全程无 telegram/CLI"
    - "工具栏常驻「Gate 中心」入口(pending 数徽章,0 时不显示)+ {gate.open && <GateCenterPanel />} 挂载 + Esc 关闭"
    - "verify:phase-54 全节绿 + 包内测试绿 + 54-VALIDATION.md 回填(每 SC 的采样点映射到真实 task,nyquist_compliant: true)"
  artifacts:
    - path: "packages/infinite-canvas/src/components/gate/GateCenterBlock.tsx"
      provides: "16 门清单/阻塞展开卡/动作条/理由对话框/降级横幅——无 dock 依赖可被 G15 内嵌(D-13 seam)"
      contains: "GateCenterBlock"
    - path: "packages/infinite-canvas/src/components/gate/GateCenterPanel.tsx"
      provides: "420px 右 dock 包装(头部 40px/Esc/theme.shadow.pop)"
      contains: "GateCenterPanel"
  key_links:
    - from: "GateCenterBlock 动作条"
      to: "canvasApi.gateOps"
      via: "行级 处理中… → await gateOps → 乐观翻转/回滚;409 → applied:false toast"
      pattern: "gateOps"
    - from: "packages/infinite-canvas/src/components/FlowCanvas.tsx"
      to: "GateCenterPanel"
      via: "工具栏 ToolbarButton 入口 + overlay 区 {gate.open && <GateCenterPanel />}(iteration.panelOpen 先例)"
      pattern: "GateCenterPanel"
    - from: "GateCenterBlock"
      to: "useGateStore(snapshot/degrade/setOpen)"
      via: "selector 逐字段订阅(IterationPanel L56-67 模式)"
      pattern: "useGateStore"
---

<objective>
GATE-02 操作面 + GATE-03 前端闭环 + phase 收口:① GateCenterBlock(16 门工作台内容块,D-13 自建带 seam)+ GateCenterPanel(420px dock);② 三操作闭环(放行单点击;驳回/豁免必填理由+组件内二次确认;409 幂等;失败回滚);③ FlowCanvas 工具栏常驻入口 + 挂载;④ 条件任务:53 的 G15 面板已交付则接互嵌 seam;⑤ 全量 verify 收口 + 54-VALIDATION.md 回填。
Purpose: SC2 后半(gate 面板列出待决策项)+ SC3 的画布端终点;phase 验收材料齐备。
Output: 两组件 + 接线 + verify/VALIDATION 收口。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-UI-SPEC.md(C-3/C-4/Copywriting/按钮规范/Anti-Patterns——binding)
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-RESEARCH.md(§C.3 round-trip 叙事/§F.2 面板内容)
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-PATTERNS.md(面板/store selector 节)
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-VALIDATION.md(回填对象)
@.planning/phases/53-variant-contract-picker-upgrade/53-07-PLAN.md(只读——G15TriagePanel/g15Bridge 契约,条件任务判据)
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-05-SUMMARY.md
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-06-SUMMARY.md

<interfaces>
消费的既有契约:
- useGateStore(54-04):snapshot(GateStatePayload:16 门 gates[] + blocking + degrade + fetchedAt)、setOpen
- canvasApi.gateOps(54-04):(projectId, episodesId, reviewId, action, reason?, selected?) → {applied, cause?};409 已被服务端映射为 applied:false, cause:"already-resolved"
- useCanvasStore(54-06 摘):showToast、projectId/episodesId
- GateStateGate.note:驳回/豁免理由摘要(服务端截断 80 字);display 五值

UI-SPEC 钉死的设计决定(U-02/U-05/U-07 等,设计检查步对照):
- Block/Panel 双导出 = D-13 互嵌 seam;420px 宽 + minWidth 360(--cv-panel-w-min);--cv-bg-panel 底 + theme.shadow.pop
- 按钮三态:放行=theme.button.primary;驳回=theme.button.danger;豁免=ghost 底+signal.locked 描边文字
- 非阻塞 pending 行 = signal.pending 灰点(U-07);金色只给当前阻塞行
- copy 全部审片 vernacular(UI-SPEC Copywriting 表逐字:等你决策/放行/驳回/豁免/自动扫描/管线畅通/状态源不可达…/该门已在别处处理…/处理中…)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GateCenterBlock——16 门清单 + 阻塞展开卡 + 动作条 + 理由对话框 + 降级横幅</name>
  <files>packages/infinite-canvas/src/components/gate/GateCenterBlock.tsx, packages/infinite-canvas/src/components/gate/__tests__/GateCenterBlock.test.tsx</files>
  <read_first>
  - packages/infinite-canvas/src/components/IterationPanel.tsx(store selector 逐字段订阅 L56-67 + 工具栏面板形态)
  - packages/infinite-canvas/src/store/gateStore.ts + src/services/canvasApi.ts(gateOps 签名)
  - packages/infinite-canvas/src/theme/catppuccin.ts(signal.*/button.*/chrome.errorBar L75-80/L194/L204-208)
  - packages/infinite-canvas/src/theme/tokens.css(--cv-panel-pad/--cv-panel-section-gap/--cv-fs-t1..t4/--cv-line-panel/--cv-bg-card/--cv-d-panel/--cv-d-select)
  - .planning/phases/54-gate-gate-center-blocking-state-ux/54-UI-SPEC.md 全文(C-3/C-4 结构逐项 + Copywriting 表 + Anti-Patterns)
  - packages/infinite-canvas/src/components/nodes/__tests__/AssetCardNode.playBadge.test.tsx(组件渲染测试范式:文件级 @vitest-environment jsdom + react-dom/client createRoot + React 19 act;真实 zustand store + 定向 vi.mock)
  </read_first>
  <behavior>
  - mock canvasApi.gateOps 返回 {applied:true} → 点击 [放行] 后对应门行 data-testid="gate-state-{gateId}" 乐观翻转为预期终态(approve),「处理中…」瞬时态消失
  - gateOps 返回 {applied:false, cause:"already-resolved"} → 行刷新为快照实际态 + 出现「该门已在别处处理」toast(P4 幂等成功,非报错)
  - gateOps 抛异常/reject → 行回滚到操作前状态 + 错误 toast,无中间残留态
  </behavior>
  <action>
  按 UI-SPEC C-3 结构自上而下实现 GateCenterBlock(纯内容块,**无 dock 外壳依赖**——useCanvasStore 取 projectId/episodesId/showToast,useGateStore 取 snapshot/degrade):
  1. 快照行:episode 标识 + fetchedAt(var(--cv-font-mono) 10px tertiary + tabular-nums);正常连接静默。
  2. 16 门清单(gates[] 按 GATE_CATALOG 管线序,分组头 P01→P13 11px/600 phaseGroupLabel):行高 36px、padding "8px 16px"、行间发丝线 var(--cv-line-panel);行 = 10px 状态点 + 门名 12px + 状态标签 11px/600 + phase token mono 10px 右对齐。四态色(U-08):pending 阻塞行金点呼吸+左侧 2px 金竖条、非阻塞 pending 行 v3theme.signal.pending 灰点(U-07);approve=v3theme.signal.approved;reject=v3theme.signal.rejected;waive=v3theme.signal.locked;auto=laneLabel 静态文字徽章「自动扫描」。终态行整体 text.secondary。data-testid="gate-row-{gateId}"/"gate-state-{gateId}"。
  3. 阻塞门展开卡(当前阻塞行下方):--cv-bg-card 圆角 8 padding 16;review 富字段只读键值表(best_fails/dims/verify_summary——来自服务端 gates[].note 与后续扩展字段,值 mono 10px 长值截断+title 全文);note 区显示驳回/豁免理由;legacy/BLOCK/AUTO note 文案按 UI-SPEC 逐字。
  4. 动作条(sticky 底部 padding 16 gap 8):[放行][驳回][豁免] 三按钮高 32px;**放行单点击直接执行(U-05 无二次确认)**;驳回/豁免 → 理由对话框(C-4:组件内 state confirming:'reject'|'waive'|null,标题 = Destructive confirmation copy 逐字,textarea min-height 80 圆角 6 底 --cv-bg-card、必填 min1 max500,按钮 [取消] ghost / [确认驳回] danger / [确认豁免] locked ghost,Esc 取消)。行级「处理中…」瞬时态,禁全屏 loading。
  5. 操作流:await gateOps(...)——applied:true → 行乐观翻转预期态 + toast;applied:false cause already-resolved → 行刷新为实际态 + 「该门已在别处处理(如 telegram),状态已刷新。」toast(P4 幂等);异常 → 回滚行态 + toast。
  6. 降级横幅:degrade=true 时显示 chrome.errorBar 底(#2A1620 via theme token)+玫边模式 + Error state copy 逐字(含 fetchedAt 相对时间)+ [重试] 按钮调 fetchGateState。data-testid="gate-degrade-banner"/"gate-reason-dialog"。
  7. 空态(无阻塞且全终态):Empty state 文案逐字(管线畅通/16 道门都无待决策项…)。
  8. 禁:原生 confirm()/alert();新 hex;字重 700;暴露 POLICY_EVAL/disposition/review_id 裸值(review_id 只进 mono 标注)。
  9. **测试先行(红→绿)**:先建 src/components/gate/__tests__/GateCenterBlock.test.tsx 跑红再实现——照 AssetCardNode.playBadge.test.tsx 范式(文件级 `// @vitest-environment jsdom` 注释头 + react-dom/client createRoot + React 19 act + IS_REACT_ACT_ENVIRONMENT;真实 gateStore setState 种 snapshot,仅 vi.mock canvasApi 的 gateOps/fetchGateState 两个函数)覆盖 behavior 三条;**零新增依赖**(不引入 @testing-library,jsdom/react-dom 既有)。
  </action>
  <verify>
  <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm test -- GateCenterBlock && npx tsc --noEmit && grep -c "gate-reason-dialog\|gate-degrade-banner\|gate-row-" src/components/gate/GateCenterBlock.tsx && ! grep -nE "#[0-9A-Fa-f]{6}|confirm\(" src/components/gate/GateCenterBlock.tsx</automated>
  </verify>
  <acceptance_criteria>
  - npm test -- GateCenterBlock 全绿(≥3 用例:乐观翻转 / 409 already-resolved 幂等 toast / 异常回滚)
  - tsc 干净;data-testid 三族齐备(row/state/reason-dialog/degrade-banner)
  - copy 逐字命中:grep "等你决策\|放行\|驳回\|豁免\|自动扫描\|管线畅通\|该门已在别处处理\|处理中" 全部 ≥1
  - grep -c "confirm(" 输出 0(无原生 confirm);无硬编码 hex;组件不 import dock/window 定位逻辑(可内嵌性)
  </acceptance_criteria>
  <done>16 门工作台内容块 + 三操作闭环 + 降级路径完整,文案/颜色全走契约</done>
</task>

<task type="auto">
  <name>Task 2: GateCenterPanel dock + FlowCanvas 入口/挂载 + 条件 G15 seam</name>
  <files>packages/infinite-canvas/src/components/gate/GateCenterPanel.tsx, packages/infinite-canvas/src/components/FlowCanvas.tsx</files>
  <read_first>
  - packages/infinite-canvas/src/components/FlowCanvas.tsx(工具栏按钮区/ToolbarButton helper、overlay 挂载 L1009 iteration.panelOpen 先例)
  - packages/infinite-canvas/src/components/gate/GateCenterBlock.tsx(Task 1 产物)
  - /data/workspace/kais-aigc-platform/packages/infinite-canvas/src/components/g15/ 目录(**执行时探测**存在性——53-07 交付判据)
  - .planning/phases/53-variant-contract-picker-upgrade/53-07-PLAN.md(只读:G15TriagePanel 契约,sewm 接法参考)
  - .planning/phases/54-gate-gate-center-blocking-state-ux/54-UI-SPEC.md C-3 落位/挂载节
  </read_first>
  <action>
  1. **GateCenterPanel.tsx**:右侧 420px dock(width 420, minWidth 360),背景 var(--cv-bg-panel),浮起 theme.shadow.pop;头部 40px:「Gate 中心」14px/600 + ✕ 关闭(gateStore.setOpen(false));Esc 关闭;开合动效 --cv-d-panel(240ms);body = &lt;GateCenterBlock /&gt;。data-testid="gate-center-panel"。
  2. **FlowCanvas.tsx**:工具栏常驻入口按钮「Gate 中心」(既有 ToolbarButton helper;badge 数 = pending 门数(display==='pending' 且有 reviewId),0 时不显示徽章——Anti-Pattern 6);overlay 区挂 {gate.open && &lt;GateCenterPanel />}(iteration.panelOpen 同法;gate.open 经 useGateStore selector)。
  3. **条件任务(D-13 后续,非阻塞)**:若 packages/infinite-canvas/src/components/g15/G15TriagePanel.tsx 已存在(53 已交付)→ 在 G15 面板预留位嵌入 &lt;GateCenterBlock /&gt;(或按 53-07 实际 seam 形态接入),并在 SUMMARY 记录接法;**不存在 → 跳过并记录**「自建 seam 已备(双导出),53 落地后接入零返工」。自建路线不得依赖 53 交付。
  4. 设计自检步(SUMMARY 记录):对照 Anti-Patterns 八条逐条勾选;重点:①无红溅(玫仅状态点/标签/驳回按钮/横幅边)②无节点角标占用③无新 hex/节拍④无原生 confirm⑤无平台内部态⑥无空态占位噪音⑦无 zone hack(54-06 已保)⑧无全屏 loading。
  </action>
  <verify>
  <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm test && npx tsc --noEmit && grep -c "GateCenterPanel\|Gate 中心" src/components/FlowCanvas.tsx</automated>
  </verify>
  <acceptance_criteria>
  - FlowCanvas.tsx 含工具栏入口与 {gate.open && <GateCenterPanel />} 挂载;npm test 全绿
  - GateCenterPanel 含 width 420/minWidth 360/Esc 处理/gate-center-panel testid
  - 条件任务结论(g15 目录存在性探测结果 + 接法或跳过)写入 SUMMARY
  </acceptance_criteria>
  <done>GATE-02 面板层 + GATE-03 前端终点落地;D-13 seam 双向就绪</done>
</task>

<task type="auto">
  <name>Task 3: 全量收口——verify 全节 + 回填 54-VALIDATION.md</name>
  <files>.planning/phases/54-gate-gate-center-blocking-state-ux/54-VALIDATION.md</files>
  <read_first>
  - .planning/phases/54-gate-gate-center-blocking-state-ux/54-VALIDATION.md(Per-Task Verification Map 现表 + Sign-Off 区)
  - scripts/verify-phase-54.ts(全节现状)
  </read_first>
  <action>
  1. 跑全量:npm run verify:phase-54(六节全 PASS,无 SKIP);cd packages/infinite-canvas && npm test;npx tsc --noEmit(kap 根 + 包内双根);khs python3 -m pytest plugins/review_gates/tests plugins/kais_aigc/tests(确认 54-03 仍绿);活体 curl GET localhost:10588/api/canvas/v2/gate-state?真实scope(54-05 SUMMARY 中的 scope)200。
  2. 回填 54-VALIDATION.md:核对 Per-Task Verification Map 的 Task ID 列(已按 54-01..07 PLAN 预落位:S-catalog/S-fold/S-forced-fail/env→54-01、S-poller/S-ops/S-live→54-05、khs pytest→54-03、平台 pytest→54-02、gateStore/useCanvasSocket 单测→54-04、GateCenterBlock 操作流单测→54-07;服务端 blocking 推导随 S-poller 归 54-05)无误并补齐遗漏;Wave 0 四项打勾;frontmatter 改 nyquist_compliant: true、wave_0_complete: true;Status 列按实际绿红更新;Manual-Only 三行保留并指向 HUMAN-UAT。
  3. SUMMARY 附 verify 全节输出 + 测试计数,声明 phase 可进入 /gsd:verify-work 前置状态(活体 SC3 全链手工签收留给 HUMAN-UAT:存量 2 条 APPROVING review 用新面板真实放行一次 = 天然用例,RESEARCH Open Question 2)。
  </action>
  <verify>
  <automated>npm run verify:phase-54 && cd packages/infinite-canvas && npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
  - verify:phase-54 输出无 SKIP/FAIL;khs 两 plugin 目录 pytest 退出 0
  - 54-VALIDATION.md frontmatter 含 nyquist_compliant: true 与 wave_0_complete: true;表格 Task ID 列无 "54-NN" 占位残留
  </acceptance_criteria>
  <done>phase 验证矩阵闭环,材料就绪待 checker/HUMAN-UAT</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户 → gate-ops 写操作(UI 入口) | 破坏性操作(驳回触发 kmc rollback/retry;豁免跳过质量门) |
| 平台响应 → UI 呈现 | 409/失败路径的呈现语义直接影响操作员决策 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-07-01 | Tampering | 误触破坏性操作 | mitigate | 驳回/豁免必填理由(1-500)+ 组件内二次确认对话框;放行虽单点击但其语义可逆性低风险(U-05 定案);无批量 UI |
| T-54-07-02 | Tampering | 409 双通道竞速(telegram 先点)误判失败 | mitigate | applied:false, cause:"already-resolved" → 幂等成功 toast + 行刷新(P4);S-ops 锁定 |
| T-54-07-03 | Information Disclosure | review 富字段上屏 | mitigate | 只读键值表 + 长值截断;review_id 只进 mono 标注;不暴露 disposition/POLICY_EVAL |
| T-54-07-04 | Tampering | 平台不可达时误导操作员 | mitigate | 降级横幅 fail-closed 文案(不误判为已放行)+ [重试];操作按钮在 degrade 期仍可用但结果如实呈现 |
| T-54-SC | Tampering | npm installs | accept | 零新增依赖 |
</threat_model>

<verification>
- verify:phase-54 六节全 PASS;包内 npm test + 双根 tsc 绿;khs pytest 绿
- 10588 活体 gate-state 200
- 54-VALIDATION.md 回填完成且 nyquist_compliant: true
</verification>

<success_criteria>
- GATE-02/GATE-03 前端终点:面板内三操作闭环 + 幂等/失败/降级全路径
- phase 验证矩阵(自动化 + 手工清单)齐备,HUMAN-UAT 材料(SC3 全链 + 阻塞态视觉签收)就绪
</success_criteria>

<output>
Create .planning/phases/54-gate-gate-center-blocking-state-ux/54-07-SUMMARY.md when done
</output>
