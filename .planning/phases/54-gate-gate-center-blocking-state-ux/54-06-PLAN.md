---
phase: 54-gate-gate-center-blocking-state-ux
plan: 06
type: execute
wave: 3
depends_on: ["54-04", "54-05"]
files_modified:
  - packages/infinite-canvas/src/components/canvas/GateTodoChip.tsx
  - packages/infinite-canvas/src/components/canvas/PhaseColumns.tsx
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
  - packages/infinite-canvas/src/store/gateStore.ts
  - packages/infinite-canvas/src/store/__tests__/gateStore.test.ts
autonomous: true
requirements: [GATE-02]
must_haves:
  truths:
    - "管线阻塞时 topbar 出现「等你决策 · {门名}」chip(金色呼吸点,一处发光);无阻塞时 chip 完全不渲染(无 0 徽章空壳)——SC2 待办通知入口"
    - "点击 chip 跳焦到阻塞门代表节点(g-{gate_id} → n-{phase_id} → 该 phase 首资产,三级解析)并打开 gate 面板入口——新会话打开画布即可定位当前阻塞门(SC2)"
    - "阻塞 phase 列获得唯一双层金描边呼吸(签名元素);其余列渲染零变化——画布内定位"
    - "新会话载入:loadCanvas 并行 fetch gate-state(不阻塞首帧),gate:state socket 增量经 onGateState 入 store(P6 浅比较在 54-04 已备)"
  artifacts:
    - path: "packages/infinite-canvas/src/components/canvas/GateTodoChip.tsx"
      provides: "C-1 topbar 待办 chip(26px/圆角 7px/金边/呼吸点/blocking==null return null)"
      contains: "gate-todo-chip"
    - path: "packages/infinite-canvas/src/components/canvas/PhaseColumns.tsx"
      provides: "可选 blockingPhaseIndex prop + 仅阻塞列双层描边/竖带 0.08/前缀金"
      contains: "blockingPhaseIndex"
    - path: "packages/infinite-canvas/src/store/gateStore.ts"
      provides: "resolveRepresentativeNodeId(blocking, nodes) 纯函数(g-/n-/phaseName token 等值三级)"
      contains: "resolveRepresentativeNodeId"
  key_links:
    - from: "packages/infinite-canvas/src/components/FlowCanvas.tsx"
      to: "packages/infinite-canvas/src/store/gateStore.ts"
      via: "onGateState 回调(scope 守卫 projectId/episodesId)→ gateStore.apply;blocking → derive blockingPhaseIndex"
      pattern: "onGateState"
    - from: "packages/infinite-canvas/src/components/FlowCanvas.tsx"
      to: "canvasApi.fetchGateState"
      via: "loadCanvas 流程并行 void fetch(不 await 首帧)"
      pattern: "fetchGateState"
    - from: "GateTodoChip"
      to: "canvasStore.setFocusAssetNodeId + gateStore.setOpen(true)"
      via: "代表节点解析 → 跳焦 + 开面板"
      pattern: "setFocusAssetNodeId"
---

<objective>
GATE-02 画布阻塞态呈现三层中的两层:topbar 待办 chip(入口)+ PhaseColumns 阻塞列高亮(画布内定位)+ FlowCanvas 全部接线(socket 消费/新会话快照/blockingPhaseIndex 传递)。签名元素 = 当前阻塞门的单一醒目指示(chip 呼吸点 + 阻塞列描边同源同拍,全画布恒一处发光);反模式 = 到处红。
Purpose: SC2 前半——"新会话打开画布即可定位当前阻塞门"。
Output: chip 组件、列叠加层、代表节点解析纯函数、FlowCanvas 接线 + 设计自检。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-UI-SPEC.md(设计宪法/C-1/C-2/Motion Contract/Anti-Patterns——**binding 设计契约**)
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-RESEARCH.md(§F 呈现设计)
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-PATTERNS.md(PhaseColumns/GateTodoChip/chip 落位节)
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-04-SUMMARY.md(gateStore 前置产物)

<interfaces>
消费的既有契约:
- gateStore(54-04):{snapshot, degrade, open, setOpen, apply} + GateStatePayload(blocking:{gateId,reviewId,phaseId,label}|null)
- canvasApi(54-04):fetchGateState(projectId, episodesId) → GateStatePayload|null
- useCanvasSocket(54-04):onGateState?: (payload: GateStatePayload) => void(scope 守卫照 FlowCanvas onVariantSelected L265-285 模板)
- canvasStore:setFocusAssetNodeId(L1054)、节点含 phaseIndex/phaseName(import-from-dir PHASE_DEFS 产)

设计 token(UI-SPEC 钉死,设计检查步逐条对照):
- 金 = v3theme.signal.running(#E0B665);灰 pending = v3theme.signal.pending(#9A9FA8);chip 底 --cv-bg-overlay;呼吸 0.35↔0.7 / 2.4s(--cv-e-inout,1.2s 拍组合);进场 2×400ms(--cv-d-stale-pulse);退场 220ms(--cv-d-unhighlight);reduced-motion 静态 strokeOpacity 0.7
- 列:竖带 opacity 0.04→0.08;描边底层 4px/0.16 常伴 + 上层 1.5px(--cv-edge-causal-w)/0.35↔0.7;只 animate strokeOpacity/opacity;禁 SVG filter;pointer-events:none
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolveRepresentativeNodeId 纯函数 + PhaseColumns 阻塞列叠加层</name>
  <files>packages/infinite-canvas/src/store/gateStore.ts, packages/infinite-canvas/src/store/__tests__/gateStore.test.ts, packages/infinite-canvas/src/components/canvas/PhaseColumns.tsx</files>
  <read_first>
  - packages/infinite-canvas/src/components/canvas/PhaseColumns.tsx(全文:列渲染循环 L47-82、groupColor/label/prefix 既有逻辑)
  - packages/infinite-canvas/src/theme/tokens.css(L164-191 动效节拍 + prefers-reduced-motion 降级块)
  - packages/infinite-canvas/src/theme/catppuccin.ts(L75-80 signal.*)
  - packages/infinite-canvas/src/store/gateStore.ts(54-04 产物)
  - .planning/phases/54-gate-gate-center-blocking-state-ux/54-UI-SPEC.md C-2 + Motion Contract + Anti-Patterns 1/3/7
  </read_first>
  <behavior>
  - resolveRepresentativeNodeId:节点集中存在 id==="g-p13-gate" → 取之;否则 id==="n-p13_delivery" → 取之;否则首个 leadingToken(node.phaseName)===leadingToken("p13-gate") 的节点(token 等值,非前缀——p1 与 p11a0 不互撞);三级皆无 → null
  - blockingPhaseIndex=null 时 PhaseColumns 渲染输出与改造前逐字节等价(默认零变化)
  - 阻塞列:竖带 fill=v3theme.signal.running 且 opacity=0.08;左边界双层描边(4px/0.16 + 1.5px/0.35↔0.7);P0X 前缀 fill=金、名称色 laneLabel;其余列零改动
  </behavior>
  <action>
  1. gateStore.ts 追加导出纯函数 resolveRepresentativeNodeId(blocking: GateStatePayload["blocking"], nodes: Array&lt;{id: string; phaseName?: string; phaseIndex?: number}&gt;): string | null——按 behavior 三级解析;内嵌 8 行 leadingToken 正则(/^p\d+[a-z0-9]*/)做**等值**比较。同步在 gateStore.test.ts 补三级解析 + token 等值(p1 ≠ p11a0)用例。
  2. PhaseColumns.tsx:props 增可选 blockingPhaseIndex?: number | null(默认 null);仅 col.index === blockingPhaseIndex 的列走叠加分支:竖带 fill 换 v3theme.signal.running、opacity "0.08";左边界 line 升级为两条 line(底层 strokeWidth 4 + strokeOpacity 0.16;上层 strokeWidth 1.5 + strokeOpacity CSS/SMIL 呼吸 0.35↔0.7,周期 2.4s ease-in-out,进场 2×400ms 脉冲);该列 P0X 前缀 text fill 换 signal.running、名称 text fill 提到 v3theme.laneLabel。实现约束:只 animate strokeOpacity/opacity(不触发布局);禁 SVG filter;pointer-events:none 保持;prefers-reduced-motion 时静态 strokeOpacity 0.7(读 tokens.css 既有降级块,不新写全局规则)。
  </action>
  <verify>
  <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm test -- gateStore && npx tsc --noEmit && ! grep -n "filter" src/components/canvas/PhaseColumns.tsx</automated>
  </verify>
  <acceptance_criteria>
  - npm test -- gateStore 全绿(新增 ≥4 用例:g-/n-/phaseName 级、token 等值反例、null)
  - PhaseColumns.tsx 含 "blockingPhaseIndex";grep -c "0.08" ≥1;grep -cE "strokeWidth.*4|strokeWidth=\{4\}" ≥1;无 "filter=" SVG 滤镜
  - grep -c "startsWith" gateStore.ts 输出 0
  </acceptance_criteria>
  <done>代表节点解析 + 阻塞列签名发光落地(列由节点 median 投影派生,天然兼容 Phase 55 zone 重构——无一次性 zone hack)</done>
</task>

<task type="auto">
  <name>Task 2: GateTodoChip + FlowCanvas 接线(socket 消费/新会话快照/chip 落位/列传参)</name>
  <files>packages/infinite-canvas/src/components/canvas/GateTodoChip.tsx, packages/infinite-canvas/src/components/FlowCanvas.tsx</files>
  <read_first>
  - packages/infinite-canvas/src/components/FlowCanvas.tsx(topbar 右簇 L791-797、PhaseColumns 挂载 L868、onVariantSelected scope 守卫 L265-285、loadCanvasGraph 流程 L300 邻域、视图切换机制)
  - packages/infinite-canvas/src/store/gateStore.ts(apply/setOpen/resolveRepresentativeNodeId)
  - packages/infinite-canvas/src/services/canvasApi.ts(fetchGateState)
  - .planning/phases/54-gate-gate-center-blocking-state-ux/54-UI-SPEC.md C-1(chip 规格全项)+ Copywriting Contract(chip 文案/门名)
  </read_first>
  <action>
  1. **GateTodoChip.tsx**(新组件,UI-SPEC C-1 逐项):订阅 useGateStore((s)=>s.snapshot?.blocking ?? null);blocking === null → return null(空态不占顶栏)。形态:高 26px 圆角 7px、底 var(--cv-bg-overlay)、边 1px v3theme.signal.running(alpha 0.55)、padding "4px 12px";内容 = 10px 金色呼吸圆点(opacity 0.5↔1.0,与列描边同拍 2.4s)+ 文案 `等你决策 · ${blocking.label}`——「等你决策」四字 600 金色(--cv-fs-t2 12px),门名 text.secondary。点击:①resolveRepresentativeNodeId(blocking, useCanvasStore.getState().nodes)→ 命中则 setFocusAssetNodeId(nodeId) 且当前非画布视图先切回 canvas 视图;②useGateStore.getState().setOpen(true);③三级皆无 → 只开面板不跳焦不报错。data-testid="gate-todo-chip";reduced-motion 点常亮。
  2. **FlowCanvas.tsx** 四处接线:
     a. topbar 右簇(L791-797 的 gap:8 容器)连接点指示左侧插入 &lt;GateTodoChip /&gt;;
     b. useCanvasSocket 消费处增 onGateState 回调:照 onVariantSelected 模板做 scope 守卫(payload.projectId !== projectId || payload.episodesId !== episodesId → return)后 useGateStore.getState().apply(payload);
     c. loadCanvas 流程:并行 void fetchGateState(projectId, episodesId).then((p)=>p \&\& apply(p)).catch(()=>{})——**不 await、不阻塞画布首帧**;
     d. PhaseColumns 挂载处传 blockingPhaseIndex:由 gateStore snapshot.blocking + useCanvasStore nodes 经 resolveRepresentativeNodeId 解析出代表节点 → 其 phaseIndex ?? null(useGateStore 订阅 selector 计算,勿在 render 内每帧扫描)。
  3. 设计自检步(结果写入 SUMMARY):对照 UI-SPEC Anti-Patterns 逐条——①玫色只出现在本次新增面的 0 处(chip/列只用金);②无 gate 状态上节点角标;③无新 hex(组件内 grep 不得出现 # 开头色值,全部走 v3theme/var);④chip 在 blocking==null 时不渲染;⑤同屏发光恒 1(chip 点与列描边同源同拍)。
  </action>
  <verify>
  <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm test && npx tsc --noEmit && ! grep -nE "#[0-9A-Fa-f]{6}" src/components/canvas/GateTodoChip.tsx</automated>
  </verify>
  <acceptance_criteria>
  - npm test 全绿(既有不回归)+ tsc 干净
  - GateTodoChip.tsx 含 "gate-todo-chip"、"等你决策"、return null 分支;无硬编码 hex
  - FlowCanvas.tsx 含 onGateState、fetchGateState、blockingPhaseIndex、&lt;GateTodoChip 四处接线(grep 各 ≥1)
  - SUMMARY 含设计自检五条勾选结果
  </acceptance_criteria>
  <done>SC2 前半闭环:新会话开画布 → 快照拉取 → chip 即现 → 一次点击定位阻塞门 + 列发光</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate:state socket payload → 前端渲染 | 服务端产物;scope 守卫(projectId/episodesId)防跨项目串扰 |
| 用户点击 chip | 只读定位 + 开面板,无写操作面(写操作在 54-07 面板) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-06-01 | Tampering | 跨项目 payload 串扰 | mitigate | onGateState scope 守卫(payload.projectId/episodesId 与当前会话比对,onVariantSelected 同法) |
| T-54-06-02 | DoS(渲染) | gate:state 频繁广播重渲 | mitigate | 54-04 apply 浅比较 + selector 订阅(不整 store 订阅)+ 列叠加纯展示 pointer-events:none |
| T-54-06-03 | Information Disclosure | 平台内部态上 UI | mitigate | payload 已是折叠后展示模型(D-04);chip/列文案只用审片 vernacular |
| T-54-SC | Tampering | npm installs | accept | 零新增依赖 |
</threat_model>

<verification>
- 包内 npm test 全绿 + npx tsc --noEmit
- grep 四处接线 + 组件规格锚点(testid/copy/return null)
- 设计自检五条(UI-SPEC Anti-Patterns)记录于 SUMMARY
</verification>

<success_criteria>
- GATE-02 的入口层与画布内定位层落地;签名元素单一发光纪律经自检确认
- 代表节点三级解析有单测锁定(g-/n-/phaseName token 等值)
</success_criteria>

<output>
Create .planning/phases/54-gate-gate-center-blocking-state-ux/54-06-SUMMARY.md when done
</output>
