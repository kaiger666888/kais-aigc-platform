---
phase: 54-gate-gate-center-blocking-state-ux
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/infinite-canvas/src/store/gateStore.ts
  - packages/infinite-canvas/src/store/__tests__/gateStore.test.ts
  - packages/infinite-canvas/src/hooks/useCanvasSocket.ts
  - packages/infinite-canvas/src/hooks/__tests__/useCanvasSocket.test.ts
  - packages/infinite-canvas/src/services/canvasApi.ts
autonomous: true
requirements: [GATE-02]
must_haves:
  truths:
    - "gate:state socket 事件经 useCanvasSocket 的 onGateState 回调到达消费方(注册/转发/无回调安全/卸载四件套测试)——D-03 前端侧"
    - "gateStore 独立存在(P5 正交轴:不碰节点 reviewStatus/canvasStore 字段),apply 做载荷级浅比较防重渲(P6)"
    - "canvasApi 提供 fetchGateState(GET 快照,失败返回 null 不抛)与 gateOps(POST 操作)——前端不直连 review-platform(D-03)"
  artifacts:
    - path: "packages/infinite-canvas/src/store/gateStore.ts"
      provides: "zustand 独立 store:{snapshot, degrade, open, setOpen, apply}"
      contains: "useGateStore"
    - path: "packages/infinite-canvas/src/hooks/useCanvasSocket.ts"
      provides: "GateStatePayload 接口 + onGateState 回调(三处插入) + socket.on('gate:state')"
      contains: "gate:state"
    - path: "packages/infinite-canvas/src/services/canvasApi.ts"
      provides: "fetchGateState + gateOps API client"
      contains: "gateOps"
  key_links:
    - from: "packages/infinite-canvas/src/hooks/useCanvasSocket.ts"
      to: "kap 服务端 broadcastToProject(projectId,'gate:state',payload)"
      via: "既有 /ws/projects socket 房间(54-05 服务端发射;本 plan 只定义消费契约)"
      pattern: "gate:state"
    - from: "packages/infinite-canvas/src/services/canvasApi.ts"
      to: "GET /api/canvas/v2/gate-state + POST /api/canvas/v2/gate-ops"
      via: "既有 apiCall(4xx business/5xx 重试分类,勿另写 fetch)"
      pattern: "/canvas/v2/gate"
---

<objective>
前端地基:gateStore(独立 zustand store)+ useCanvasSocket 的 gate:state 事件消费 + canvasApi 的 gateOps/fetchGateState 客户端。与 54-05(服务端)平行开发——契约(payload shape)在本 plan 与 54-05 双侧钉死一致。
Purpose: GATE-02 的数据层;chip/列高亮/面板(54-06/07)全部消费这三个构件。
Output: 三构件 + vitest 单测(gateStore apply 去重/blocking 选择 + socket 四件套)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-CONTEXT.md
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-RESEARCH.md(§D.2 payload shape)
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-UI-SPEC.md(C-5 store 契约 + State & Interaction Contract)
@.planning/phases/54-gate-gate-center-blocking-state-ux/54-PATTERNS.md(useCanvasSocket/canvasApi/gateStore 三节——实现锚点)

<interfaces>
<!-- GateStatePayload —— 本 plan 与 54-05(服务端发射)双侧钉死、逐字段一致;D-04:平台原始态(POLICY_EVAL/disposition)不进 payload -->
GateStateGate { gateId: string; phaseId: string; label: string; display: 'pending'|'approve'|'reject'|'waive'|'auto'; reviewId?: number; updatedAt?: string; note?: string }
GateStatePayload { projectId: number; episodesId: number; fetchedAt: number; degrade: boolean; blocking: { gateId: string; reviewId: number; phaseId: string; label: string } | null; gates: GateStateGate[] }
(HTTP GET /api/canvas/v2/gate-state 响应 = 上列 payload 外加诊断字段 episodeRefs?: string[];socket 事件体 = payload 精确形状)

参考既有实现(实读):
- useCanvasSocket.ts:VariantSelectedPayload L45-51 / options 回调 L66-67 / callbacksRef L103+L108 / socket.on 注册 L203-205(三处同步加,漏一处回调永不开火)
- canvasStore.ts:zustand create L1 / focusAssetNodeId L1053-1055 / 面板开合 L1090-1092
- canvasApi.ts:selectVariantWinner L463-475(apiCall POST 先例)/ fetchCanvasHealth L508-525(GET 失败返回 null 先例)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gateStore + apply 去重/blocking 单测</name>
  <files>packages/infinite-canvas/src/store/gateStore.ts, packages/infinite-canvas/src/store/__tests__/gateStore.test.ts</files>
  <read_first>
  - packages/infinite-canvas/src/store/canvasStore.ts(L1 create 导入形态;L1053-1055 focus 字段;L1090-1092 面板开合字段——形态先例,**不得并入 canvasStore**)
  - .planning/phases/54-gate-gate-center-blocking-state-ux/54-UI-SPEC.md C-5(store 契约)与 State & Interaction Contract(P6 浅比较)
  - packages/infinite-canvas 既有任一 store/hook 测试文件(vitest 风格)
  </read_first>
  <behavior>
  - apply(payload):gates 数组与 fetchedAt/degrade/blocking 浅比较逐项相等时**不**触发 setState(subscribe listener 不再被调用);任一变化才更新
  - apply 后 state.snapshot === payload;degrade=true 的 payload 也照常应用(绝不折叠为全放行)
  - setOpen(true/false) 切换 open
  - 初始 state:snapshot=null, degrade=false, open=false
  </behavior>
  <action>
  新建 gateStore.ts:import { create } from 'zustand';导出 GateStateGate/GateStatePayload 接口(按上方 interfaces 精确形状);store 字段 {snapshot, degrade, open, setOpen, apply}——apply 内做载荷级 memo 比较(gates 数组元素浅比较 + fetchedAt/degrade/blocking 比较)后才 set(P6 广播风暴防线)。文件内**零** canvasStore import、**零** reviewStatus 字段引用(P5 正交轴铁律,gate 是管线轴非资产轴)。单测文件覆盖 behavior 四条(vitest,subscribe 计数法验证去重)。
  </action>
  <verify>
  <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm test -- gateStore && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
  - npm test -- gateStore 全绿(≥4 用例,含去重用例)
  - grep -c "reviewStatus" src/store/gateStore.ts 输出 0;grep -c "canvasStore" src/store/gateStore.ts 输出 0
  - 导出 useGateStore + 类型;interface 含 display 五值字面量联合
  </acceptance_criteria>
  <done>独立 gate store 落地,去重防线有测试锁定</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useCanvasSocket gate:state 四件套 + canvasApi gateOps/fetchGateState</name>
  <files>packages/infinite-canvas/src/hooks/useCanvasSocket.ts, packages/infinite-canvas/src/hooks/__tests__/useCanvasSocket.test.ts, packages/infinite-canvas/src/services/canvasApi.ts</files>
  <read_first>
  - packages/infinite-canvas/src/hooks/useCanvasSocket.ts(全文:VariantSelectedPayload L45-51、options L66-67、callbacksRef L103/L108、socket.on L203-205——三处插入点)
  - packages/infinite-canvas/src/hooks/__tests__/useCanvasSocket.test.ts(variant:selected describe 四件套 L53-117)
  - packages/infinite-canvas/src/services/canvasApi.ts(selectVariantWinner L463-475、fetchCanvasHealth L508-525、apiCall 分类 L71-105)
  </read_first>
  <behavior>
  - socket 'gate:state' 事件到达 → onGateState 回调收到原样 payload
  - 挂载即注册 handler;未提供 onGateState 回调时不炸(可选链);卸载断开
  - fetchGateState 失败 → 返回 null 不抛(fetchCanvasHealth 先例)
  - gateOps 走 apiCall POST,路径 /canvas/v2/gate-ops,body {projectId, episodesId, reviewId, action, reason?, selected?}
  </behavior>
  <action>
  1. useCanvasSocket.ts:新增 export interface GateStatePayload(与 54-04 Task 1 / 54-05 plan 中 interfaces 块逐字段一致);options 接口、callbacksRef 对象、socket.on('gate:state', ...) 三处同步新增 onGateState?: (payload: GateStatePayload) => void(注释注明 "Phase 54 D-03: gate 中心状态推送,scope 守卫由消费方负责")。
  2. 测试文件:照 variant:selected describe 结构新增 gate:state describe 四件套(挂载注册/事件转发/无回调安全/卸载断开,mock socket 先例)。
  3. canvasApi.ts:新增 fetchGateState(projectId, episodesId, cancelToken?) → apiCall GET '/canvas/v2/gate-state' query projectId/episodesId,失败返回 null;gateOps(projectId, episodesId, reviewId, action: 'approve'|'reject'|'waive', reason?: string, selected?: number[]) → apiCall POST '/canvas/v2/gate-ops',返回 {applied: boolean, cause?: string}。JSDoc 写明端点契约(409 → applied:false cause:"already-resolved" 的幂等语义,P4)。
  4. 全程不得出现 review-platform URL/域名(D-03 前端不直连):grep 校验。
  </action>
  <verify>
  <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm test -- useCanvasSocket && npx tsc --noEmit && ! grep -rn "review-platform\|8090" src/hooks/useCanvasSocket.ts src/services/canvasApi.ts</automated>
  </verify>
  <acceptance_criteria>
  - npm test -- useCanvasSocket 全绿(新增 gate:state 四件套 + 既有不回归)
  - grep -c "gate:state" src/hooks/useCanvasSocket.ts ≥ 2(注册+类型);三处插入点齐备(grep onGateState 命中 3 行)
  - canvasApi.ts 导出 fetchGateState 与 gateOps;两函数体仅经 apiCall,无裸 fetch
  </acceptance_criteria>
  <done>前端三构件(gateStore/socket/api)就绪,54-06/07 可纯消费</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 服务端 socket 事件 → 前端 store | gate:state payload 是服务端产物;客户端做 scope(projectId/episodesId)过滤的责任在消费方 |
| 前端 → kap 域端点 | gate-ops 是写操作入口;前端不直连 review-platform(D-03 拓扑隐藏) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-04-01 | Information Disclosure | 平台拓扑暴露 | mitigate | 前端仅经 kap 域 canvasApi;本 plan grep 断言零 review-platform URL;不新增 route77 使用 |
| T-54-04-02 | DoS(渲染层) | gate:state 高频推送 | mitigate | apply 载荷级浅比较(P6)+ 服务端 diff 后才广播(54-05)双防线;测试锁定去重 |
| T-54-04-03 | Tampering | payload 与服务端形状漂移 | mitigate | 接口在 54-04/54-05 双侧逐字段钉死;54-05 S-ops 断言服务端发射字段名 |
| T-54-SC | Tampering | npm installs | accept | 零新增依赖(zustand 既有) |
</threat_model>

<verification>
- cd packages/infinite-canvas && npm test(gateStore + useCanvasSocket 全绿,既有不回归)
- npx tsc --noEmit(包内)干净
- grep:零 review-platform 引用、零 canvasStore 交叉、三处 onGateState 插入点
</verification>

<success_criteria>
- GATE-02 数据层三构件就绪且测试锁定;payload 契约与 54-05 服务端零漂移
</success_criteria>

<output>
Create .planning/phases/54-gate-gate-center-blocking-state-ux/54-04-SUMMARY.md when done
</output>
