# Phase 54: Gate 中心 (Gate Center + Blocking-State UX) - Pattern Map

**Mapped:** 2026-08-21
**Files analyzed:** 22(三仓:kap 17 / khs 2+测试 / review-platform 3)
**Analogs found:** 20 / 22(2 项无整文件先例,已给最接近构件组合)

> 所有 excerpt 均为实读代码,带 `file:line` 引用。跨仓绝对路径:
> kap=`/data/workspace/kais-aigc-platform`,khs=`/data/workspace/kais-hermes-skills`,平台=`/data/workspace/kais-review-platform`。

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `src/lib/gateCatalog.ts` (NEW) | config/contract(zod 快照+纯函数) | transform | `src/lib/canvasAssetSchema.ts` + `src/lib/reviewBridge.ts` L92-106 | exact(平行声明模式) |
| `src/lib/gateStateService.ts` (NEW) | service(轮询单例) | polling + event-driven | `src/lib/reviewBridge.ts`(翻页/过滤)+ `src/lib/comfyuiPoll.ts`(poller) | role-match(常驻 interval 无先例) |
| `src/routes/canvas/v2/gate-ops.ts` (NEW) | route | request-response | `src/routes/canvas/v2/select-winner.ts` | exact |
| `src/routes/canvas/v2/gate-state.ts` (NEW) | route(GET 快照) | request-response | `src/routes/canvas/v2/health.ts` | exact |
| `src/router.ts` (MOD) | route 注册 | — | 自身 route167 先例(L173/L200) | exact |
| `package.json` (MOD) | config(npm script) | — | `verify:phase-51`(L44) | exact |
| `packages/infinite-canvas/src/hooks/useCanvasSocket.ts` (MOD) | hook | event-driven | 自身 variant:selected 块(L45-51/L203-205) | exact |
| `packages/infinite-canvas/src/hooks/__tests__/useCanvasSocket.test.ts` (MOD) | test | event-driven | 自身 variant:selected describe(L53+) | exact |
| `packages/infinite-canvas/src/services/canvasApi.ts` (MOD) | service(前端 API client) | request-response | 自身 `selectVariantWinner`(L463-475) | exact |
| `packages/infinite-canvas/src/store/gateStore.ts` (NEW) | store(zustand) | event-driven | `src/store/canvasStore.ts`(create + focusAssetNodeId) | role-match(独立小 store 无先例) |
| `packages/infinite-canvas/src/components/gate/GateCenterPanel.tsx` + `GateCenterBlock.tsx` (NEW) | component(面板) | request-response + event-driven | `src/components/IterationPanel.tsx` | exact(dock 面板同构) |
| `packages/infinite-canvas/src/components/canvas/GateTodoChip.tsx` (NEW) | component(topbar chip) | event-driven | FlowCanvas topbar 簇(L779-797) | role-match(无独立 chip 组件) |
| `packages/infinite-canvas/src/components/canvas/PhaseColumns.tsx` (MOD) | component(叠加层) | transform | 自身列渲染循环(L47-82) | exact |
| `packages/infinite-canvas/src/components/FlowCanvas.tsx` (MOD) | component(接线) | event-driven | 自身 onVariantSelected(L265-285)+ 挂载点(L868/L1009) | exact |
| `scripts/verify-phase-54.ts` (NEW) | test(契约门) | batch | `scripts/verify-phase-51.ts` + `verify-manifest-contract.ts` | exact |
| `.env` REVIEW_PLATFORM_URL (MOD) | config | — | 无(RESEARCH I.2 硬前置) | 无先例(纯配置) |
| khs `plugins/review_gates/runner_hooks.py` (MOD, R3) | service(poller 消费端) | polling | 自身 Path 2 块(L637-678) | exact |
| khs `plugins/kais_aigc/review_platform.py` (MOD, R2) | service(HTTP client) | request-response | 自身 `query_review_status`(L294-311) | exact |
| khs `plugins/review_gates/tests/test_poller_complete_state.py` (NEW) | test | polling | 同目录 `test_runner_hooks.py`(MagicMock 注入) | exact |
| 平台 `app/api/v1/actions.py` (MOD, R1) | controller | request-response | 自身 `reject_review`(L399-458)+ approve 原子写(L356-375) | exact |
| 平台 `app/models/schemas.py` (MOD, WaiveRequest) | model | — | 自身 `RejectRequest`(L299-301) | exact |
| 平台 `tests/test_approve_reject.py` (MOD, 扩展) | test | request-response | 自身 fixture 模式(L23-45) | exact |

---

## Pattern Assignments

### `src/lib/gateCatalog.ts`(config/contract,transform)

**Analog:** `src/lib/canvasAssetSchema.ts`(D-02 平行声明零漂移模式的本仓先例)+ `src/lib/reviewBridge.ts` 的两个纯函数。

**平行声明头注释模式**(canvasAssetSchema.ts L10-15——头注释显式指向 khs 权威源,gateCatalog 必须同法指向 `plugins/review_gates/gates.yaml` + `gate_config.py`):

```typescript
 * Schema keys mirror the manifest params written by pipeline phases
 * (see _manifest.py → write_manifest → params dict).
 *
 * Per-type optional fields are augmented from
 * ``schema/pipeline-field-map.yaml`` via ``frontend-zod-extensions.ts``
 * (generated by ``python schema/generate_mappings.py``).
```

**gate_id 派生纯函数模式**(reviewBridge.ts L98-106——`leadingPhaseToken` 正是 derive 规则的 kap 侧形态;gateCatalog 移植 khs `_PHASE_PREFIX_RE` 时照此写法,WR-01 前缀碰撞防线):

```typescript
function leadingPhaseToken(value: string): string | null {
  const m = /^p\d+/.exec(value.trim().toLowerCase());
  return m === null ? null : m[0];
}
```

**折叠纯函数**(foldDisplayState)同文件,签名按 RESEARCH §Code Examples;枚举值以平台 `app/models/schemas.py` L11-37 为准。注意:`review-gate.ts` 路由文件名相近但**是 legacy 文件态审核,勿参考**。

**放置位置提示(planner 裁定点):** RESEARCH §A.2 写 `src/lib/gateCatalog.ts`(与 canvasAssetSchema/reviewBridge 同位,服务端 gateStateService/gate-ops/verify 脚本直接 import;D-04 折叠发生在服务端,前端只收 display 态),§Validation Wave 0 又写 `packages/infinite-canvas/src/lib/`(该目录现不存在,且 packages 侧无 js-yaml 依赖)。两处矛盾——按消费方分布(服务端折叠 + verify 脚本直读源文件 P7 纪律),`src/lib/` 更自洽;若 planner 选 packages 侧,须把 js-yaml 加进 workspace 包并在 verify 脚本里跨包 import。

---

### `src/lib/gateStateService.ts`(service,polling + event-driven)

**Analog:** `src/lib/reviewBridge.ts`(翻页 + 客户端过滤 + 依赖注入)+ `src/lib/comfyuiPoll.ts`(poller 骨架)+ `src/utils/ws.ts`(广播)。

**依赖全注入模式**(reviewBridge.ts L74-79——模块不 import utils barrel,脚本/测试可独立驱动):

```typescript
export interface ReviewBridgeDeps {
  baseUrl?: string;               // default process.env.REVIEW_PLATFORM_URL || "http://review-platform:8090"
  fetchImpl?: typeof fetch;       // test injection
  logger?: { info: (...a: unknown[]) => void; warn: (...a: unknown[]) => void };
  timeoutMs?: number;             // default 5000
}
```

baseUrl 解析(L120-121):`(deps.baseUrl ?? process.env.REVIEW_PLATFORM_URL ?? "http://review-platform:8090").replace(/\/+$/, "")`——gateStateService 必须同法显式走 `REVIEW_PLATFORM_URL`(P2/I.2:默认值宿主不可解析,是本期 P0 env 前置)。

**fail-closed 翻页循环**(reviewBridge.ts L139-177,MAX_LIST_PAGES=10 上限照抄):

```typescript
const MAX_LIST_PAGES = 10;
const items: ReviewListItem[] = [];
let cursor: string | null = null;
let truncated = false;
for (let page = 0; page < MAX_LIST_PAGES; page++) {
  const qs = new URLSearchParams({ source: "kais-movie-agent", status: "APPROVING", limit: "100" });
  if (cursor !== null) qs.set("cursor", cursor);
  const listResp = await fetchImpl(`${baseUrl}/api/v1/reviews?${qs.toString()}`, {
    signal: AbortSignal.timeout(timeoutMs),
  });
  ...
  if (body?.data?.has_more !== true) break;
  ...
}
if (truncated) { logger.warn(...); return; }   // 宁可漏过不可错批
```

**三维 fail-closed 候选过滤**(reviewBridge.ts L197-208,gate 轮询与 gate-ops 共用;建议按 RESEARCH §D.1 提取 L92-106 两纯函数到共享模块):

```typescript
const episodeIds = new Set<string>([`ep${params.episodesId}`, String(params.episodesId)]);
const candidates = items.filter((item) => {
  if (typeof item.type !== "string" || typeof item.content_ref !== "string") return false;
  if (leadingPhaseToken(item.type) !== phaseToken) return false;
  const ref = item.content_ref;
  const slash = ref.lastIndexOf("/");
  if (slash < 0) return false;
  const episodeSegment = ref.slice(0, slash);
  const phaseSegment = ref.slice(slash + 1);
  if (!episodeIds.has(episodeSegment)) return false;
  return leadingPhaseToken(phaseSegment) === phaseToken;
});
```

**常驻 interval 无先例的替代锚:** comfyuiPoll.ts L29-52 是"有界 while + setTimeout 轮询"(per-request,非 resident service)。gateStateService 的 `setInterval` 20s + lazy 启动 + diff(`review.id + state + version`)为新增形态;diff 后广播用 `broadcastToProject`(见 Shared Patterns)。降级语义:连续失败 → `degrade: true` 快照保留(UI-SPEC State Contract),绝不折叠为全放行。

---

### `src/routes/canvas/v2/gate-ops.ts`(route,request-response)

**Analog:** `src/routes/canvas/v2/select-winner.ts`(zod 严格入参 + responseFormat + 广播 + 桥挂载)。

**Imports + zod 模式**(select-winner.ts L1-13, L37-41):

```typescript
import express from "express";
import { z } from "zod";
import { success, error } from "@/lib/responseFormat";
import { broadcastToProject } from "@/utils/ws";
...
const selectWinnerSchema = z.object({
  projectId: z.number(),
  episodesId: z.number(),
  winnerNodeId: z.string().min(1).max(128), // T-49-01: no oversized ids into the DB
});
```

gate-ops schema:`action: z.enum(["approve","reject","waive"])`、`reviewId: z.number().int()`、`reason: z.string().min(1).max(500)`(对齐平台 RejectRequest)、`selected: z.array(z.number()).optional()`。入参校验失败返回 `res.status(400).send(error("参数校验失败", parse.error.issues))`(L46-49 同法)。

**与 select-winner 的关键差异(RESEARCH §H 定案):** 平台调用是**主操作非 fire-and-forget**——`await` 后结果回面板(成功才翻四态);409 → `200 {applied:false, cause:"already-resolved"}`(P4 幂等成功语义)。fire-and-forget 的只有操作后的即时 re-poll:

```typescript
// select-winner.ts L151-158 的双 backstop 形态(gate-ops 仅用于 re-poll 触发):
void resolveOpenReviewForSelection({ ... }).catch(() => {});
// gate-ops 对应: void gateStateService.pollNow(projectId).catch(() => {});
```

**成功后广播**(select-winner.ts L160-166;gate-ops 可交给 pollNow 的 diff 广播,或直接发 `gate:state`):

```typescript
broadcastToProject(projectId, "variant:selected", {
  projectId, episodesId, groupId, winnerNodeId, timestamp: Date.now(),
});
```

**顶层错误处理**(select-winner.ts L171-174):`console.error("[canvas:v2/gate-ops] ...", err); return res.status(500).send(error("..."))`。

**approve 载荷写法**(reviewBridge.ts L222-235——comment 人类可读 + result.selected 机器通道):

```typescript
body: JSON.stringify({
  comment: `choose:v${params.variantIndex} (canvas group ... winner ..., project ...)`,
  result: { selected: [params.variantIndex] },
}),
```

---

### `src/routes/canvas/v2/gate-state.ts`(route GET,request-response)

**Analog:** `src/routes/canvas/v2/health.ts` L13-41(GET + try/catch + success envelope):

```typescript
router.get("/", async (_req, res) => {
  const timestamp = Date.now();
  try {
    const scopes = await getAllScopes();
    return res.status(200).send(success({ timestamp, canvas: { ... } }));
  } catch (err) {
    console.error("[v2/canvas/health] 探活失败:", err);
    return res.status(503).send(error("画布服务不可用"));
  }
});
```

gate-state:query 参数 `projectId&episodesId`,从 GateStateService 缓存即时回,miss 触发一次即时拉取;响应 `success({ fetchedAt, blocking, gates, degrade })`(四态折叠后的展示模型,平台原始态不泄给前端)。

**路由注册**(src/router.ts L173 import + L200 挂载——route167 先例,新路由顺延 route168/route169):

```typescript
import route167 from "./routes/canvas/v2/select-winner";   // L173
...
app.use("/api/canvas/v2/variant-groups", route167);         // L200
```

---

### `scripts/verify-phase-54.ts` + `package.json`(test/contract,batch)

**Analog:** `scripts/verify-phase-51.ts`(S-section 聚合门 + mkdtemp 隔离)+ `scripts/verify-manifest-contract.ts`(跨仓 sibling 读取)。

**S-section 头注释契约**(verify-phase-51.ts L1-56——每个 S 节先在头注释声明断言面;54 建议 S-catalog(js-yaml 解析 khs gates.yaml 逐字段 diff + derive round-trip + 16 计数)/S-fold(foldDisplayState 全表)/S-ops(gate-ops spawn 子进程)/S-live(活体比对))。

**assert helper + 隔离骨架**(verify-phase-51.ts L62-83——mkdtemp+chdir 必须在 dynamic import 之前;54 的 S-catalog 只读 YAML 纯函数,若不碰 @/utils 可省 chdir,但沿用骨架无害):

```typescript
interface TestResult { name: string; pass: boolean; detail?: string; }
const results: TestResult[] = [];
function assert(cond: boolean, name: string, detail?: string): void {
  results.push({ name, pass: cond, detail });
  console.log(`  ${cond ? "PASS" : "FAIL"}: ${name}${detail ? " — " + detail : ""}`);
}
const REPO_ROOT = path.resolve(__dirname, "..");
const ISOLATION_DIR = fs.mkdtempSync(path.join(os.tmpdir(), "verify-phase-51-"));
fs.copyFileSync(path.join(REPO_ROOT, "package.json"), path.join(ISOLATION_DIR, "package.json"));
process.chdir(ISOLATION_DIR);
```

**跨仓 sibling 读取约定**(verify-manifest-contract.ts L27-32——契约测试直读 khs 文件;54 用 js-yaml 解析 gates.yaml 替代 spawn pytest,`js-yaml@^4.2.0` 已在根 package.json L74):

```typescript
const SIBLING_ROOT = process.env.KAIS_HERMES_SKILLS_PATH ?? "/data/workspace/kais-hermes-skills";
const TEST_FILES = [ "skills/kais-movie-pipeline/tests/test_manifest_schema.py", ... ];
```

**npm script 注册**(package.json L44 先例):`"verify:phase-54": "npx tsx scripts/verify-phase-54.ts"`。

**P7 纪律:** 脚本自包含——直接 `import` gateCatalog/foldDisplayState 源文件 + js-yaml,不碰 @/utils barrel(tsx 卡死陷阱在案)。

---

### `packages/infinite-canvas/src/hooks/useCanvasSocket.ts`(MOD,hook,event-driven)

**Analog:** 自身 variant:selected 全链(Phase 49 WR-08 加事件的逐字节模板)。

**Payload 接口声明**(L45-51):

```typescript
export interface VariantSelectedPayload {
  projectId: number
  episodesId: number
  groupId: string
  winnerNodeId: string
  timestamp: number
}
```

→ 同法新增 `GateStatePayload`(RESEARCH §D.2 shape,含 `blocking`/`gates[]`/`fetchedAt`;`display ∈ pending|approve|reject|waive|auto`)。

**Options + 回调 ref 两处同步添加**(L66-67 与 L103/L108 的 callbacksRef 对象——三处都要加 `onGateState`,漏一处回调永不开火):

```typescript
    /** Phase 49 (WR-08): 他端选定了变体组 winner — 消费方负责回显守卫。 */
    onVariantSelected?: (payload: VariantSelectedPayload) => void,
```

**socket.on 注册**(L203-205 插入点,命名风格 `gate:state` 与 `variant:selected` 同构):

```typescript
    socket.on('variant:selected', (payload: VariantSelectedPayload) => {
      callbacksRef.current.onVariantSelected?.(payload)
    })
```

**消费端 scope 守卫**(FlowCanvas.tsx L265-285——onGateState 的 projectId/episodesId 过滤照此写;注意 gate:state 客户端还须 payload 级 memo 浅比较,P6 广播风暴防线):

```typescript
    onVariantSelected: (payload) => {
      if (!projectId || episodesId == null) return
      if (payload.projectId !== projectId || payload.episodesId !== episodesId) return
      const st = useCanvasStore.getState()
      ...
    },
```

**测试扩展**(hooks/__tests__/useCanvasSocket.test.ts L53-117——describe + mock socket + `挂载即注册 handler` / `事件到达 → 原样转发` / `未提供回调时不炸` / `卸载断开` 四件套,gate:state 照抄一套)。

---

### `packages/infinite-canvas/src/services/canvasApi.ts`(MOD,service client,request-response)

**Analog:** 自身 `selectVariantWinner`(L463-475——JSDoc 写明端点契约 + 错误语义 + 调用方回滚责任):

```typescript
export async function selectVariantWinner(
  projectId: number, episodesId: number, groupId: string, winnerNodeId: string,
  cancelToken?: CancelToken,
): Promise<void> {
  await apiCall<void>(
    `/canvas/v2/variant-groups/${encodeURIComponent(groupId)}/select-winner`,
    { projectId, episodesId, winnerNodeId },
    { cancelToken },
  )
}
```

→ 新增 `gateOps(projectId, episodesId, reviewId, action, reason?, selected?)` 走 `apiCall('/canvas/v2/gate-ops', ...)`;GET 快照参考 `fetchCanvasHealth`(L508-525,GET + 失败返回 null 不抛)。核心 `apiCall` 已内建 4xx business/5xx 重试分类(L96-105)与逐 attempt 超时(L71-72)——**勿另写 fetch**。

---

### `packages/infinite-canvas/src/store/gateStore.ts`(NEW,store,event-driven)

**Analog:** `src/store/canvasStore.ts`(zustand `create` + 焦点/面板字段形态)。

**create 导入与形态**(canvasStore.ts L1;独立文件,不并入 canvasStore——P5 正交轴铁律):

```typescript
import { create } from 'zustand'
```

**焦点字段先例**(canvasStore.ts L1053-1055——chip 跳焦直接用既有 setter,不新建):

```typescript
  // 【资产↔画布交叉联动】焦点资产节点 ID（画布定位 + 高亮）
  focusAssetNodeId: null,
  setFocusAssetNodeId: (id) => set({ focusAssetNodeId: id }),
```

**面板开合字段先例**(canvasStore.ts L1090-1092——gateStore 的 `open/setOpen` 同法):

```typescript
  setIterationPanelOpen: (open) => set((state) => ({
    iteration: { ...state.iteration, panelOpen: open },
  })),
```

gateStore shape(UI-SPEC C-5):`{ snapshot: GateStatePayload | null, degrade: boolean, open, setOpen(v), apply(payload) }`;`apply` 内做 gates 数组浅比较后才 setState(P6)。

---

### `packages/infinite-canvas/src/components/gate/GateCenterPanel.tsx` + `GateCenterBlock.tsx`(NEW,component)

**Analog:** `src/components/IterationPanel.tsx`(工具栏触发的 overlay 面板 + store selector 逐字段订阅 + canvasApi 调用)。

**Store selector 模式**(IterationPanel.tsx L56-67——不取整个 store,逐字段订阅):

```typescript
  const projectId = useCanvasStore((s) => s.projectId)
  const episodesId = useCanvasStore((s) => s.episodesId)
  const iteration = useCanvasStore((s) => s.iteration)
  ...
  const showToast = useCanvasStore((s) => s.showToast)
```

**挂载点先例**(FlowCanvas.tsx L1009——`{iteration.panelOpen && <IterationPanel />}`;gate 面板同法 `{gate.open && <GateCenterPanel />}`,L868 PhaseColumns 挂载点则是 blockingPhaseIndex 的传参处)。

**样式纪律:** 一切颜色读 `v3theme.*`/`theme.*`(UI-SPEC token 消费规则);四态色 = `v3theme.signal.{running,approved,rejected,locked}`(catppuccin.ts L75-80),按钮 = `theme.button.{primary,danger,ghost}`(L204-208),浮起 = `theme.shadow.pop`(L214)。双导出 Block/Panel = D-13 互嵌 seam(53 未交付,g15/ 目录确认不存在)。

---

### `packages/infinite-canvas/src/components/canvas/GateTodoChip.tsx`(NEW,component)

**Analog:** FlowCanvas topbar 右簇(FlowCanvas.tsx L791-797——chip 的落位容器与 inline-style 词汇):

```tsx
        <div style={{ display: 'flex', gap: 8, alignItems: 'center' }}>
          {!fixtureMode && (
            <span style={{ color: connected ? theme.status.connected : theme.status.disconnected, fontSize: 11 }}>
              {connected ? '● 已连接' : '○ 未连接'}
            </span>
          )}
        </div>
```

chip 插入该 `gap:8` 容器内连接点左侧;`blocking == null` 时整个组件 return null(UI-SPEC C-1)。点击行为 = `setFocusAssetNodeId(代表节点)` + 打开面板;代表节点解析序 `g-{gate_id} → n-{phase_id} → phaseIndex 首资产`。

---

### `packages/infinite-canvas/src/components/canvas/PhaseColumns.tsx`(MOD,component,transform)

**Analog:** 自身列渲染循环(L47-82)——加可选 `blockingPhaseIndex?: number | null` prop 后仅阻塞列分支处理:

```tsx
      {phaseColumns.map((col) => {
        const groupColor = v3theme.phaseGroup[col.group] ?? v3theme.laneLabel
        const label = col.name.replace(/^P\d{2}\s*[·\-]?\s*/u, '') || col.name
        const prefix = `P${String(col.index).padStart(2, '0')}`
        return (
          <g key={`phase-${col.index}`}>
            <rect x={col.x0} y={0} width={Math.max(0, col.x1 - col.x0)} height={canvasBottom}
                  fill={groupColor} opacity={0.04} />
            <line x1={col.x0} y1={0} x2={col.x0} y2={canvasBottom}
                  stroke="rgba(255,255,255,0.05)" strokeWidth={1} />
```

改造点(UI-SPEC C-2):阻塞列竖带 `fill=v3theme.signal.running`、`opacity 0.04→0.08`;边界线换双层金描边(底层 4px/0.16 常伴 + 上层 1.5px/0.35↔0.7 呼吸);只 animate `strokeOpacity`/`opacity`,禁 SVG filter,`pointer-events:none` 保持。动效节拍读 tokens.css L164-180(`--cv-e-inout`/`--cv-d-stale-pulse` 400ms/`--cv-d-unhighlight` 220ms/`--cv-d-running-spin` 1.2s);`prefers-reduced-motion` 降级块在 tokens.css L188-191 已有,勿新写。

---

### `packages/infinite-canvas/src/components/FlowCanvas.tsx`(MOD,接线)

四处改点全部有同文件先例:①topbar chip(L791-797 簇);②`onGateState` 回调(L265-285 onVariantSelected 的 scope 守卫模板);③PhaseColumns 传 `blockingPhaseIndex`(L868);④面板挂载(L1009)。新会话全量快照:loadCanvas 流程并行 fetch gate-state,不阻塞首帧(参考 L250-263 onGraphSaved 的 scope 判断写法)。

---

### khs `plugins/review_gates/runner_hooks.py`(MOD R3,service,polling)

**Analog:** 自身 Path 2 块——改点即锚点。

**终态词汇 + decision 映射**(L649-656——G1 缺口所在;R3 改 `{"resolved","closed"}` → 并集加 `{"COMPLETE"}`,decision 优先 `status.get("result",{}).get("decision")`):

```python
            if isinstance(status, dict) and status.get("state") in {
                "resolved", "closed",
            }:
                disposition = str(status.get("disposition", "APPROVED")).upper()
                decision = {
                    "APPROVED": "approve",
                    "REJECTED": "reject",
                }.get(disposition, "contest")
```

**chosen 通道补一路**(L661-664——`result.selected[0]` 构造 `choose:v{N}` 喂 `_chosen_from_suggested`,解析器 L612-615 零改动):

```python
                chosen = (
                    status.get("chosen_variant_id")
                    or _chosen_from_suggested(outcome.get("suggested_action"))
                )
```

**次要路径同步**(L539 `poll_until_terminal` 的 `state in {"resolved", "closed"}` 同样对齐 COMPLETE)。

**测试先例**(同目录 `test_runner_hooks.py` L1-27——`monkeypatch.setattr(runner_hooks, "_review_client", lambda: fake_client)` 的 MagicMock 注入是新测试文件 `test_poller_complete_state.py` 的直接模板;`tmp_path + monkeypatch.chdir` 模式在案)。

---

### khs `plugins/kais_aigc/review_platform.py`(MOD R2,service client,request-response)

**Analog:** 自身 `query_review_status`(L294-311——R2 = 返回 dict 增加 `"result": (data.get("metadata") or {}).get("review_result")`,旧四键不动):

```python
    def query_review_status(self, review_id: Any) -> dict[str, Any]:
        """Query review status via ``GET /api/v1/reviews/{review_id}``.

        Mirrors Node.js ``queryReviewStatus``. Returns
        ``{"review_id", "state", "disposition", "version"}`` extracted from the
        ``data`` envelope. On degrade returns the degrade envelope unchanged.
        """
        result = self._request("GET", f"/api/v1/reviews/{review_id}")
        if isinstance(result, dict) and result.get("degraded"):
            return result

        data = (result or {}).get("data", {}) if isinstance(result, dict) else {}
        return {
            "review_id": data.get("review_id", data.get("id")),
            "state": data.get("state"),
            "disposition": data.get("disposition"),
            "version": data.get("version"),
        }
```

同文件 L281-283 尾斜杠注释是平台 POST URL 的在案坑(`POST /api/v1/reviews/` 必须带斜杠)。

---

### 平台 `app/api/v1/actions.py`(MOD R1,controller,request-response)

**Analog:** 自身 approve/reject——waive 整体镜像 reject。

**头注释端点清单**(L1-9——新 waive 端点须注册进此清单):

```python
"""Review approve/reject action endpoints.

POST /api/v1/reviews/{review_id}/approve -- Approve a review (REV-04)
POST /api/v1/reviews/{review_id}/reject  -- Reject a review (REV-05)
POST /api/v1/reviews/{review_id}/token   -- Generate one-time review token (DEBT-01)
POST /api/v1/reviews/batch/approve       -- Batch approve multiple reviews
POST /api/v1/reviews/batch/reject        -- Batch reject multiple reviews
```

**approve 的原子写(R1-1 改造点:L359-362 条件写 → 恒写 decision)**:

```python
    metadata = None
    if request.result:
        metadata = review.metadata_json or {}
        metadata["review_result"] = request.result.model_dump()

    try:
        await transition_state(
            db, review.id, ReviewState.APPROVING, ReviewState.COMPLETE, review.version, actor,
            action="approve",
            payload={"comment": request.comment},
            extra_updates={"metadata_json": metadata} if metadata else None,
        )
```

**reject 的完整形态(waive 的镜像模板,L399-458;R1-2 = 补 `extra_updates={"metadata_json": {"decision":"reject","reason":request.reason}}`)**:

```python
@router.post("/{review_id}/reject", response_model=ApiResponse[ReviewResponse])
async def reject_review(
    review_id: int, request: RejectRequest, token: str | None = Query(None),
    db: AsyncSession = Depends(get_db), client: str = Depends(get_current_client),
    redis=Depends(get_redis),
):
    actor = await _resolve_actor(review_id, token, client, redis)
    review = await db.get(Review, review_id)
    if review is None:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=f"Review {review_id} not found")
    if review.state != ReviewState.APPROVING.value:
        raise HTTPException(status_code=status.HTTP_409_CONFLICT,
            detail=f"Review is not in APPROVING state, current state: {review.state}")
    try:
        await transition_state(
            db, review.id, ReviewState.APPROVING, ReviewState.COMPLETE, review.version, actor,
            action="reject",
            payload={"reason": request.reason},
        )
    except StateConflictError:
        raise HTTPException(status_code=status.HTTP_409_CONFLICT, detail="State conflict: ...")
    except InvalidTransitionError:
        raise HTTPException(status_code=status.HTTP_409_CONFLICT, detail="Invalid state transition")
    await db.refresh(review)
    return ApiResponse(data=_review_response(review).model_dump(), meta={"request_id": _request_id()})
```

**WaiveRequest schema**(app/models/schemas.py L299-301 RejectRequest 镜像):

```python
class RejectRequest(BaseModel):
    """V1 legacy reject request — kept for backward compat with actions.py."""
    reason: str = Field(min_length=1, max_length=500)
```

**平台测试先例**(tests/test_approve_reject.py L23-45——`approving_review` fixture 经 `transition_state` 两跳造 APPROVING 态;waive 用例同法扩展)。

---

## Shared Patterns

### 1. broadcastToProject(socket 广播)
**Source:** `src/utils/ws.ts` L13-23;房间 join 在 `src/socket/index.ts` L23-26。
**Apply to:** gateStateService diff 后 + gate-ops 成功后。

```typescript
export function broadcastToProject(projectId: string | number, event: string, data: any) {
  if (!_io) return;
  _io.of("/ws/projects").to(`project:${projectId}`).emit(event, data);
}
```

事件名 `gate:state`,payload 带 `episodesId` 由客户端过滤(variant:selected 同法)。客户端在 useCanvasSocket 加 `socket.on('gate:state', ...)`。

### 2. fail-closed 三维匹配 + 翻页(防错批)
**Source:** `src/lib/reviewBridge.ts` L139-208。
**Apply to:** gateStateService 轮询过滤、gate-ops 的 reviewId 归属校验。WR-01 教训:等值比较 leading phase token,绝不用 `startsWith`。

### 3. zod + responseFormat 端点骨架
**Source:** `src/routes/canvas/v2/select-winner.ts` L1-49 + L171-174。
**Apply to:** gate-ops、gate-state 两个新路由;`success()/error()` 来自 `@/lib/responseFormat`;router.ts 顺延编号注册。

### 4. 409 = 幂等成功语义
**Source:** `src/lib/reviewBridge.ts` L237-240(`已被别处 resolve(409),跳过` warn)+ 平台 actions.py L350-354(APPROVING 前置)。
**Apply to:** gate-ops 全部三操作——409 → `200 {applied:false, cause:"already-resolved"}` + 触发 re-poll,P4 禁当错误弹。

### 5. 平台出站调用三纪律
**Source:** reviewBridge.ts L120-121(env baseUrl + 去尾斜杠)、review_platform.py L281-283(POST 尾斜杠)、RESEARCH I.2(REVIEW_PLATFORM_URL 未配置,默认值不可达)。
**Apply to:** gateStateService、gate-ops、verify 脚本 S-live 节。env 修复是硬前置。

### 6. zustand 独立 store + selector 订阅
**Source:** canvasStore.ts L1/L1053-1055 + IterationPanel.tsx L56-67。
**Apply to:** gateStore 及其三个消费面(chip/PhaseColumns/Panel);绝不写节点 reviewStatus 字段(P5 正交轴)。

### 7. verify 脚本 S-section + sibling 仓读取
**Source:** verify-phase-51.ts L62-83 + verify-manifest-contract.ts L27-32。
**Apply to:** scripts/verify-phase-54.ts;`KAIS_HERMES_SKILLS_PATH` 约定复用;纯 TS + js-yaml,不 spawn pytest。

### 8. khs 测试 MagicMock 注入
**Source:** khs `plugins/review_gates/tests/test_runner_hooks.py` L1-27。
**Apply to:** test_poller_complete_state.py(`monkeypatch.setattr(runner_hooks, "_review_client", lambda: fake_client)`;mock client 返 COMPLETE + review_result)。

---

## No Analog Found

| File | Role | Data Flow | Reason |
|------|------|-----------|--------|
| `src/lib/gateStateService.ts` 常驻 interval 部分 | service | polling | kap 无 resident 轮询单例先例(comfyuiPoll 是 per-request 有界 while);生命周期管理(lazy 启动/单例)须按 RESEARCH §D.1 新建,构件(翻页/过滤/广播)全有先例 |
| `GateTodoChip` 视觉形态 | component | event-driven | 无独立 topbar chip 组件先例;按 UI-SPEC C-1 从 FlowCanvas topbar inline-style 词汇(L791-797)+ tokens.css 档位新造;26px 高/圆角 7px 对齐顶栏控件族 |
| `.env` REVIEW_PLATFORM_URL | config | — | 纯环境接线,无代码先例;修法 = `.env` 增 `REVIEW_PLATFORM_URL=http://localhost:8090` + restart |

---

## Metadata

**Analog search scope:** kap src/{lib,routes/canvas/v2,utils,socket,router.ts}、packages/infinite-canvas/src/{hooks,store,services,components,theme}、scripts/verify-*、khs plugins/{review_gates,kais_aigc}、review-platform app/{api/v1,models}
**Files scanned:** 26(全读 14 + 定向读 12)
**关键否定结论:** ①`components/g15/` 不存在(53 未交付,U-02 条件分支成立);②`scripts/verify-phase-53.ts` 不存在(最新 verify 模板 = verify-phase-51.ts);③`src/routes/canvas/v2/review-gate.ts` 是 legacy 文件态审核(kais-movie-agent 死链),不是 gate-ops 参考物;④gateCatalog 放置位置 RESEARCH 内部有两处矛盾表述,planner 须显式裁定(见 Pattern Assignments 首节提示)。
**Pattern extraction date:** 2026-08-21
