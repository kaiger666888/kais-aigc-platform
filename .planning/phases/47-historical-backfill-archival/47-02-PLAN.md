---
phase: 47-historical-backfill-archival
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - scripts/oneoffs/backfill-asset-descriptions.py  # MOVED here from scripts/; + deprecation header
  - scripts/oneoffs/README.md  # NEW — one-off script convention
  - scripts/verify-phase-47-backfill.ts  # NEW — archival + post-run verify
  - package.json  # + verify:phase-47-backfill entry
autonomous: true
requirements: [BACKFILL-02, BACKFILL-03]

must_haves:
  truths:
    - "scripts/backfill-asset-descriptions.py no longer exists at its original path (moved to scripts/oneoffs/)"
    - "scripts/oneoffs/backfill-asset-descriptions.py has a deprecation header prepended explaining it's archived + why"
    - "scripts/oneoffs/README.md documents the one-off script convention: when to add here, when NOT to run scripts from here"
    - "scripts/verify-phase-47-backfill.ts exists with 4 assertions: script moved, deprecation header present, post-apply empty-shell count still low, Phase 46 contract still green"
    - "package.json registers verify:phase-47-backfill"
    - "MANUAL SAMPLING CHECKLIST section in 47-VERIFICATION.md is filled in by the operator after visual inspection of 10 nodes each from P04/P07/P08"
  artifacts:
    - path: "scripts/oneoffs/backfill-asset-descriptions.py"
      provides: "Archived backfill script with deprecation header"
      contains: "[DEPRECATED"
    - path: "scripts/oneoffs/README.md"
      provides: "One-off script convention documentation"
      contains: "one-off"
    - path: "scripts/verify-phase-47-backfill.ts"
      provides: "Phase 47 archival + post-run verifier"
      contains: "verify-phase-47"
  key_links:
    - from: "scripts/verify-phase-47-backfill.ts"
      to: "scripts/oneoffs/backfill-asset-descriptions.py"
      via: "static file existence + deprecation header check"
      pattern: "scripts/oneoffs/backfill-asset-descriptions"
    - from: "scripts/verify-phase-47-backfill.ts"
      to: "scripts/backfill-asset-descriptions.py"
      via: "negative assertion — original path must be ABSENT (moved, not copied)"
      pattern: "scripts/backfill-asset-descriptions"
---

<objective>
Phase 47 Wave 2 — archive the backfill script + manual sampling sign-off.
Depends on Plan 47-01 (the apply must have run).

Three deliverables:

1. **Archive the script.** Move `scripts/backfill-asset-descriptions.py`
   to `scripts/oneoffs/backfill-asset-descriptions.py` with a deprecation
   header. The original path no longer exists (move, not copy) so future
   operators can't accidentally re-run it via muscle memory.

2. **Document the one-off convention.** `scripts/oneoffs/README.md`
   explains: when to add a script here, when NOT to run scripts from
   here, the audit trail requirements (snapshot + log + verify).

3. **Phase 47 verifier.** `scripts/verify-phase-47-backfill.ts` asserts
   the archival is correct + the post-apply DB state is still healthy +
   the Phase 46 contract gate is still green. 4 assertions.

Plus the manual sampling checklist (BACKFILL-02) is filled in by the
operator in `47-VERIFICATION.md` after visual inspection of 10 nodes
each from P04 / P07 / P08.

Output: 4 file changes. No DB mutations in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-historical-backfill-archival/47-CONTEXT.md
@.planning/phases/47-historical-backfill-archival/47-01-PLAN.md

<interfaces>
<!-- The script being archived + the verifier pattern. -->

From scripts/backfill-asset-descriptions.py:1-14 — current docstring (will be preserved; deprecation header is PREPENDED):
```python
"""
backfill-asset-descriptions.py — one-shot repair for empty asset node details.
...
"""
```

From scripts/verify-phase-46-e2e.ts (Phase 46 — verifier skeleton with env-gate pattern):
- Standard TestResult + assert + main + finish structure
- Static file reads via fs.readFileSync
- Sibling-repo path via env var

From .planning/phases/47-historical-backfill-archival/47-CONTEXT.md decision D5 — deprecation header:
```
[DEPRECATED — Phase 47 archive, 2026-07-16]
This script was a one-shot repair for the 2026-07-12 empty-shell
asset nodes. Phase 42 hardened the source-side contract; Phase 44
hardened the receiver-side import; re-running this on data that now
satisfies the contract is unnecessary and risks overwriting newer
descriptions with synthesized ones.

Kept for audit/reproducibility. Do NOT add to cron. Do NOT run
against production data without explicit approval.
```

From packages/infinite-canvas/src/components/FlowCanvas.tsx — Phase 45 search input:
- Operators can type "P04" / "P07" / "P08" to narrow visible nodes for manual sampling
- The filter is visibility-only; doesn't mutate data
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move backfill script to scripts/oneoffs/ + add deprecation header</name>
  <files>scripts/oneoffs/backfill-asset-descriptions.py, scripts/backfill-asset-descriptions.py</files>

  <read_first>
    - scripts/backfill-asset-descriptions.py (full file — the script being archived)
    - .planning/phases/47-historical-backfill-archival/47-CONTEXT.md decision D5 (deprecation header text)
  </read_first>

  <behavior>
    - `scripts/backfill-asset-descriptions.py` no longer exists at the original path (moved, not copied)
    - `scripts/oneoffs/backfill-asset-descriptions.py` exists with identical logic + a prepended deprecation header
    - The deprecation header is the FIRST thing in the file (before the existing docstring)
    - The script remains executable (shebang preserved; only the header is new)
  </behavior>

  <action>
    1. Create `scripts/oneoffs/` directory if it doesn't exist: `mkdir -p scripts/oneoffs`

    2. Move via git mv (preserves history):
       ```bash
       git mv scripts/backfill-asset-descriptions.py scripts/oneoffs/backfill-asset-descriptions.py
       ```

    3. Prepend the deprecation header to the moved file. The header becomes the new module docstring's leading comment block; the existing docstring follows it. Use the Edit tool to insert the header at the top.

    4. Verify the script still runs (header doesn't break Python parsing):
       ```bash
       python3 scripts/oneoffs/backfill-asset-descriptions.py --help
       ```
       Should print argparse help without syntax errors.

    Do NOT modify any logic in the script — it's archived as-is for reproducibility.
    Do NOT leave a copy at the original path (move, not copy).
  </action>

  <verify>
    <automated>! test -f scripts/backfill-asset-descriptions.py && test -f scripts/oneoffs/backfill-asset-descriptions.py && head -2 scripts/oneoffs/backfill-asset-descriptions.py | grep -q "\[DEPRECATED" && python3 scripts/oneoffs/backfill-asset-descriptions.py --help >/dev/null 2>&1</automated>
  </verify>

  <done>
    - Original path `scripts/backfill-asset-descriptions.py` does NOT exist
    - New path `scripts/oneoffs/backfill-asset-descriptions.py` exists
    - First 2 lines contain `[DEPRECATED`
    - `python3 scripts/oneoffs/backfill-asset-descriptions.py --help` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/oneoffs/README.md documenting the convention</name>
  <files>scripts/oneoffs/README.md</files>

  <read_first>
    - .planning/phases/47-historical-backfill-archival/47-CONTEXT.md decision D5 (deprecation header pattern)
    - .planning/phases/47-historical-backfill-archival/47-01-PLAN.md (audit trail requirements)
  </read_first>

  <behavior>
    - `scripts/oneoffs/README.md` exists
    - Documents: (a) what one-off scripts are, (b) when to add a script here, (c) when NOT to run scripts from here, (d) audit trail requirements (snapshot + log + verify), (e) list of current one-offs (just the backfill script after Phase 47)
    - Markdown-formatted; reads as developer documentation
  </behavior>

  <action>
    Create `scripts/oneoffs/README.md` with these sections:

    ```markdown
    # scripts/oneoffs/ — archived one-shot scripts

    Scripts in this directory are **archived one-off repairs**, not
    maintained infrastructure. They're kept for audit and reproducibility.

    ## When to add a script here

    A script belongs here when ALL of these are true:
    - It was written to repair a specific historical data condition
    - The underlying bug that produced the bad data has been fixed at the source
    - Re-running the script on data that now satisfies the contract is
      unnecessary or harmful
    - The script has a clear "DO NOT run on production without approval"
      semantic

    ## When NOT to run scripts from here

    - As part of CI / cron / startup hooks
- On production data without explicit approval + DB backup
    - On data that already satisfies the contract the script was
      designed to repair

    ## Audit trail requirements

    Every archived one-off script MUST have accompanying artifacts in
    `.planning/phases/<NN-...>/`:
    - `baseline-snapshot.txt` — pre-apply dry-run output
    - `apply-log.txt` — apply run output
    - `post-run-verify.txt` — post-apply verification
    - Pre-apply DB backup at `data/db2-backup-pre-phase-NN.sqlite`

    ## Current one-offs

    | Script | Archived | Reason | Phase |
    |--------|----------|--------|-------|
    | `backfill-asset-descriptions.py` | 2026-07-16 | Repaired 417 empty-shell asset nodes; Phase 42 hardened source-side contract; Phase 44 hardened receiver-side import; re-running would overwrite newer descriptions | Phase 47 |
    ```

    Commit alongside Task 1.
  </action>

  <verify>
    <automated>test -f scripts/oneoffs/README.md && grep -q "DO NOT run" scripts/oneoffs/README.md && grep -q "Audit trail" scripts/oneoffs/README.md && grep -q "backfill-asset-descriptions.py" scripts/oneoffs/README.md</automated>
  </verify>

  <done>
    - README.md exists at scripts/oneoffs/
    - Contains the 4 required sections (when to add, when NOT to run, audit trail, current list)
    - Lists the backfill script in the table
  </done>
</task>

<task type="auto">
  <name>Task 3: Create scripts/verify-phase-47-backfill.ts (archival + post-run verifier)</name>
  <files>scripts/verify-phase-47-backfill.ts, package.json</files>

  <read_first>
    - scripts/verify-phase-46-e2e.ts (Phase 46 — env-gate pattern + TestResult/assert skeleton)
    - scripts/verify-phase-45.ts (Phase 45 — substring-presence verifier pattern)
    - .planning/phases/47-historical-backfill-archival/47-CONTEXT.md decision D3 (post-run verify checks)
  </read_first>

  <behavior>
    - Script shebang `#!/usr/bin/env tsx`
    - 4 assertions:
      1. `scripts/oneoffs/backfill-asset-descriptions.py` exists (archival)
      2. `scripts/backfill-asset-descriptions.py` does NOT exist (move not copy)
      3. The archived file's first 200 chars include `[DEPRECATED` (deprecation header present)
      4. `npm run verify:phase-46-contracts` exit 0 (Phase 46 contract still green after backfill)
    - Optional 5th assertion: `data/db2-backup-pre-phase-47.sqlite` exists (audit trail)
    - Exit 0 on all PASS; exit 1 on any FAIL
    - Does NOT spawn docker; safe-tier
    - package.json registers `verify:phase-47-backfill` immediately after `verify:phase-46-e2e`
  </behavior>

  <action>
    Create scripts/verify-phase-47-backfill.ts. Structure mirrors Phase 46 verify scripts:

    1. Shebang + fs+path+child_process imports + TestResult/assert helpers
    2. REPO_ROOT + read() helper
    3. In main():
       - Check `scripts/oneoffs/backfill-asset-descriptions.py` exists → PASS/FAIL "archived at oneoffs/"
       - Check `scripts/backfill-asset-descriptions.py` does NOT exist → PASS/FAIL "original path absent"
       - Read first 200 chars of archived file; assert contains `[DEPRECATED` → PASS/FAIL "deprecation header"
       - spawnSync `npm run verify:phase-46-contracts` from REPO_ROOT; assert exit 0 → PASS/FAIL "Phase 46 contract gate green"
       - Optional: check `data/db2-backup-pre-phase-47.sqlite` exists → PASS/FAIL "DB backup retained"
    4. Print summary + exit

    Edit package.json: add `"verify:phase-47-backfill": "npx tsx scripts/verify-phase-47-backfill.ts"` immediately after `verify:phase-46-e2e`.

    Run `npm run verify-phase-47-backfill` to confirm 5/5 (or 4/4) assertions pass.
  </action>

  <verify>
    <automated>test -f scripts/verify-phase-47-backfill.ts && grep -c "verify:phase-47-backfill" package.json | grep -qE '^1$' && npm run verify:phase-47-backfill 2>&1 | tail -10 && echo "EXIT=$?"</automated>
  </verify>

  <done>
    - Script exists at scripts/verify-phase-47-backfill.ts
    - 4-5 assertions, all PASS
    - package.json entry present
    - Exit 0 on green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem move (script archival) | git mv preserves history; original path no longer exists; no data loss |
| Verify script → filesystem + spawnSync npm | Read-only checks + bounded subprocess (same trust level as Phase 46 verify scripts) |
| Operator → manual sampling checklist | Human-only sign-off; no code path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-07 | Tampering | Future operator re-runs archived script against current data | mitigate | (a) Move out of scripts/ root removes muscle-memory trigger; (b) DEPRECATED header explains why not; (c) README documents the rule |
| T-47-08 | Repudiation | Archival committed without audit trail | mitigate | baseline-snapshot.txt + apply-log.txt + post-run-verify.txt from Plan 47-01 form the audit trail; Plan 47-02 README documents where to find them |
| T-47-09 | Tampering | Verify script false-passes on broken contract | mitigate | T-47-09 spawns Phase 46 verify as a subprocess; if Phase 46 fails, Phase 47 verify fails too |
| T-47-SC | Tampering | npm installs | accept | Plan 47-02 introduces zero new packages |

Security net effect: Plan 47-02 is a file-organization + verifier phase. The "move not copy" + DEPRECATED header combination prevents accidental re-run. The verifier's spawn of Phase 46 ensures contract drift catches future regressions.
</threat_model>

<verification>
1. `scripts/backfill-asset-descriptions.py` does NOT exist
2. `scripts/oneoffs/backfill-asset-descriptions.py` exists with `[DEPRECATED` header
3. `scripts/oneoffs/README.md` exists with 4 required sections
4. `scripts/verify-phase-47-backfill.ts` exists with 4-5 assertions
5. `npm run verify:phase-47-backfill` exits 0
6. `npm run verify:phase-46-contracts` still exits 0 (no regression)
7. Operator has filled in the MANUAL SAMPLING CHECKLIST section in 47-VERIFICATION.md (10 nodes each from P04 / P07 / P08) — autonomous: false sub-step
</verification>

<success_criteria>
- BACKFILL-02: manual sampling confirmed via 47-VERIFICATION.md checklist — operator verified 10 nodes each from P04 / P07 / P08 show meaningful description text in NodeDetailPanel (not just label)
- BACKFILL-03: backfill script moved to scripts/oneoffs/ with deprecation header; README documents the convention; verify-phase-47-backfill confirms archival + Phase 46 contract green
- v2.0 milestone complete — all 6 phases shipped, all 24+ requirements met
</success_criteria>

<output>
Create `.planning/phases/47-historical-backfill-archival/47-02-SUMMARY.md` when done.
</output>
