---
phase: 17-workflow-builder-expansion
plan: 03
type: execute
wave: 1
depends_on: ["17-01"]
files_modified:
  - docker/gold-team/src/v6/executor.py
  - docker/gold-team/src/v6/engine/router.py
  - docker/gold-team/tests/test_executor_routing.py
  - docker/gold-team/tests/conftest.py
autonomous: true
requirements:
  - WFB-04
  - WFB-05

must_haves:
  truths:
    - "Submitting IMAGE_TO_3D with params.extra.engine='trellis' routes to build_trellis_image_to_3d_workflow instead of hunyuan3d-local"
    - "Submitting IMAGE_TO_3D with params.extra.mode='flux_trellis' routes to build_flux_trellis_full_workflow"
    - "Submitting IMAGE_TO_3D without extra params still routes to hunyuan3d-local as before (no regression)"
    - "TRELLIS tasks bypass the DEDICATED_ENGINES lookup and go to comfyui-primary"
  artifacts:
    - path: "docker/gold-team/src/v6/executor.py"
      provides: "TRELLIS routing branch in IMAGE_TO_3D handler"
      contains: "trellis"
    - path: "docker/gold-team/src/v6/engine/router.py"
      provides: "Conditional DEDICATED_ENGINES bypass for TRELLIS"
      contains: "trellis"
    - path: "docker/gold-team/tests/test_executor_routing.py"
      provides: "Unit tests for TRELLIS routing"
      contains: "test_image_to_3d_trellis_routing"
  key_links:
    - from: "docker/gold-team/src/v6/executor.py"
      to: "docker/gold-team/src/v6/engines/workflow_builder.py"
      via: "import build_trellis_image_to_3d_workflow, build_flux_trellis_full_workflow"
      pattern: "build_trellis_image_to_3d_workflow"
    - from: "docker/gold-team/src/v6/executor.py"
      to: "docker/gold-team/src/v6/engine/router.py"
      via: "DEDICATED_ENGINES bypass"
      pattern: "DEDICATED_ENGINES"
---

<objective>
Add routing for TRELLIS workflows (WFB-04, WFB-05) so that IMAGE_TO_3D tasks with params.extra.engine="trellis" or params.extra.mode="flux_trellis" route to the TRELLIS ComfyUI workflow builders instead of the default hunyuan3d-local dedicated engine.

Purpose: The TRELLIS builders already exist but have no routing entries. The DEDICATED_ENGINES map unconditionally sends IMAGE_TO_3D to hunyuan3d-local, so TRELLIS tasks need a bypass mechanism.
Output: Modified executor.py with TRELLIS routing, modified router.py with conditional bypass, and passing routing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/phase-17/17-CONTEXT.md
@.planning/phases/phase-17/17-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From docker/gold-team/src/v6/executor.py lines 106-176:
- _execute_task(task: GenerationTask) method
- task.params dict contains user params; task.params.get("extra", {}) for params.extra
- Line 155: `elif task.type == TaskType.IMAGE_TO_3D:` routes to build_hunyuan3d_workflow
- Line 508-521: _resolve_engine checks DEDICATED_ENGINES first at line 516

From docker/gold-team/src/v6/engine/router.py lines 19-28:
- DEDICATED_ENGINES maps TaskType.IMAGE_TO_3D to "hunyuan3d-local"
- _pick_local_engine_id at line 163 checks DEDICATED_ENGINES first

From docker/gold-team/src/v6/engines/workflow_builder.py:
- build_trellis_image_to_3d_workflow(image_name, resolution, steps, cfg_scale, shape_guidance, ...) -> dict[str, Any]
- build_flux_trellis_full_workflow(prompt, negative_prompt, flux_steps, flux_cfg, width, height, ...) -> dict[str, Any]

From docker/gold-team/src/v6/models/task.py:
- TaskType.IMAGE_TO_3D = "image_to_3d"
- GenerationTask has params: dict[str, Any]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TRELLIS routing to executor and router (WFB-04, WFB-05)</name>
  <files>docker/gold-team/src/v6/executor.py, docker/gold-team/src/v6/engine/router.py, docker/gold-team/tests/test_executor_routing.py, docker/gold-team/tests/conftest.py</files>

  <read_first>
  - docker/gold-team/src/v6/executor.py (full file, especially lines 106-176 for _execute_task routing and lines 508-521 for _resolve_engine)
  - docker/gold-team/src/v6/engine/router.py (full file, especially DEDICATED_ENGINES dict at lines 19-28 and _pick_local_engine_id at lines 163-170)
  - docker/gold-team/src/v6/engines/workflow_builder.py (build_trellis_image_to_3d_workflow signature at line 464, build_flux_trellis_full_workflow signature at line 587)
  - docker/gold-team/tests/conftest.py (existing fixtures from Plan 17-01)
  - docker/gold-team/src/v6/models/task.py (GenerationTask model, TaskType enum)
  - .planning/phases/phase-17/17-CONTEXT.md (locked decisions: TRELLIS routes via IMAGE_TO_3D + params.extra.engine="trellis", flux_trellis via params.extra.mode="flux_trellis")
  </read_first>

  <behavior>
  - Test 1: test_image_to_3d_trellis_routing -- Create a GenerationTask with type=IMAGE_TO_3D and params.extra.engine="trellis", params.input_image="test.png". The executor routing logic must select build_trellis_image_to_3d_workflow and produce a workflow dict containing a node with class_type "TRELLISImageTo3D".
  - Test 2: test_image_to_3d_flux_trellis_routing -- Create a GenerationTask with type=IMAGE_TO_3D and params.extra.mode="flux_trellis", params.prompt="a 3d object". The executor routing must select build_flux_trellis_full_workflow and produce a dict containing a node with class_type "TRELLISImageTo3D".
  - Test 3: test_image_to_3d_default_hunyuan3d -- Create a GenerationTask with type=IMAGE_TO_3D and no params.extra. The routing must fall through to the existing build_hunyuan3d_workflow handler (no change from current behavior).
  - Test 4: test_trellis_bypasses_dedicated_engine -- When IMAGE_TO_3D + params.extra.engine="trellis", the task must be routed to comfyui-primary, NOT to hunyuan3d-local. Verify the _resolve_engine or routing logic returns a comfyui engine, not the dedicated hunyuan3d engine.
  </behavior>

  <action>
  The executor routing for IMAGE_TO_3D currently (lines 155-176) unconditionally builds a hunyuan3d_workflow. Modify this branch to check params.extra BEFORE building hunyuan3d:

  In executor.py, replace the `elif task.type == TaskType.IMAGE_TO_3D:` block (lines 155-176) with a version that:
  1. Extracts `extra_engine = task.params.get("extra", {}).get("engine", "")` and `extra_mode = task.params.get("extra", {}).get("mode", "")`
  2. If `extra_mode == "flux_trellis"`: call build_flux_trellis_full_workflow with prompt, negative_prompt, flux_steps, flux_cfg, width, height, trellis_resolution, trellis_steps, trellis_cfg, shape_guidance, texture_resolution, pbr_channels, output_format, seed from task.params
  3. Elif `extra_engine == "trellis"`: call build_trellis_image_to_3d_workflow with input_image (from task.params.get("input_image") or task.params.get("image")), resolution, steps, cfg_scale, shape_guidance, texture_resolution, pbr_channels, remove_background, output_format, seed, filename_prefix
  4. Else: keep existing hunyuan3d_workflow code unchanged

  For the DEDICATED_ENGINES bypass in router.py:
  Modify `_pick_local_engine_id` (lines 163-170) to check if the task is IMAGE_TO_3D with params.extra containing trellis/flux_trellis, and return "comfyui-primary" instead of the dedicated engine. This requires access to task.params -- update the method signature to accept task: GenerationTask (it already receives the task in the route() method).

  Specifically in router.py:
  1. Change `_pick_local_engine_id(self, task: GenerationTask) -> str:` (already receives task)
  2. Add a check BEFORE the DEDICATED_ENGINES lookup: if task.type == TaskType.IMAGE_TO_3D and task.params.get("extra", {}).get("engine") == "trellis" or task.params.get("extra", {}).get("mode") == "flux_trellis": return "comfyui-primary"
  3. This ensures TRELLIS tasks go to comfyui-primary while hunyuan3d tasks still go to hunyuan3d-local

  Also add VRAM estimates for TRELLIS workflows to VRAM_ESTIMATES dict:
  - No new TaskType needed (TRELLIS uses IMAGE_TO_3D), but the VRAM estimate for IMAGE_TO_3D (10.0 GB) is reasonable for both hunyuan3d and TRELLIS on the primary GPU.

  For testing:
  Since the routing logic is embedded in the _execute_task async method, the tests should directly test the routing branch logic. Create a helper function or test the routing branch in isolation by mocking the store and testing the workflow selection logic.

  Create docker/gold-team/tests/test_executor_routing.py with the 4 tests listed in the behavior block. The tests should:
  1. Import the necessary types and workflow builders
  2. Create mock GenerationTask objects with appropriate params
  3. Test the routing branch selection logic (which builder gets called)
  4. Verify the output workflow dict contains the expected node class_types

  Update conftest.py if needed to add fixtures for mock task objects.
  </action>

  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/docker/gold-team && python -m pytest tests/test_executor_routing.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>

  <done>
    - executor.py IMAGE_TO_3D branch checks params.extra.engine and params.extra.mode for TRELLIS routing
    - router.py _pick_local_engine_id returns comfyui-primary for TRELLIS tasks
    - build_trellis_image_to_3d_workflow is called when params.extra.engine="trellis"
    - build_flux_trellis_full_workflow is called when params.extra.mode="flux_trellis"
    - Default IMAGE_TO_3D routing to hunyuan3d-local is preserved
    - 4 routing tests pass in test_executor_routing.py
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Task params -> executor routing | params.extra values determine which workflow builder is selected |
| Executor -> ComfyUI engine | Workflow dict is constructed by builder code; user cannot inject arbitrary nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | params.extra.engine/mode values | accept | Values only select from hardcoded builder functions; no injection possible |
| T-17-05 | Tampering | TRELLIS routing bypass | accept | Bypass is deterministic based on params.extra; only trellis/flux_trellis values trigger bypass |
| T-17-SC | Tampering | pytest execution | accept | Test-only, no production risk |
</threat_model>

<verification>
```bash
cd /data/workspace/kais-aigc-platform/docker/gold-team && python -m pytest tests/test_executor_routing.py tests/test_workflow_builder.py -v
```
All routing tests pass. All existing builder tests still pass.
</verification>

<success_criteria>
- IMAGE_TO_3D + params.extra.engine="trellis" routes to build_trellis_image_to_3d_workflow
- IMAGE_TO_3D + params.extra.mode="flux_trellis" routes to build_flux_trellis_full_workflow
- IMAGE_TO_3D without extra params still routes to hunyuan3d-local
- TRELLIS tasks bypass DEDICATED_ENGINES and go to comfyui-primary
- 4 new routing tests pass, 0 regressions
</success_criteria>

<output>
Create `.planning/phases/phase-17/17-03-SUMMARY.md` when done
</output>
