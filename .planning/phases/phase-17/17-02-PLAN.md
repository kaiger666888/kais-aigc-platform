---
phase: 17-workflow-builder-expansion
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - docker/gold-team/src/v6/engines/workflow_builder.py
  - docker/gold-team/tests/test_workflow_builder.py
autonomous: true
requirements:
  - WFB-06
  - WFB-07

must_haves:
  truths:
    - "build_lipsync_workflow returns a ComfyUI workflow dict with VHS_LoadVideo, LoadAudio, LatentSyncNode, and VHS_VideoCombine nodes"
    - "build_frame_interpolate_workflow returns a ComfyUI workflow dict with VHS_LoadVideo, RIFE VFI, and VHS_VideoCombine nodes"
    - "Both builders accept video_input param and produce valid numbered-node dict structure"
    - "build_lipsync_workflow accepts audio_input param and passes it to LoadAudio node"
    - "build_frame_interpolate_workflow accepts interpolation_factor param and maps it to RIFE multiplier"
  artifacts:
    - path: "docker/gold-team/src/v6/engines/workflow_builder.py"
      provides: "Lip sync and frame interpolation workflow builders"
      contains: "build_lipsync_workflow"
      min_lines: 1
    - path: "docker/gold-team/tests/test_workflow_builder.py"
      provides: "Unit tests for new builders"
      contains: "test_build_lipsync_workflow"
  key_links:
    - from: "docker/gold-team/tests/test_workflow_builder.py"
      to: "docker/gold-team/src/v6/engines/workflow_builder.py"
      via: "import"
      pattern: "from src\\.v6\\.engines\\.workflow_builder import build_lipsync_workflow"
---

<objective>
Implement the two NEW workflow builder functions: build_lipsync_workflow (WFB-06) for LatentSync lip synchronization and build_frame_interpolate_workflow (WFB-07) for RIFE frame interpolation. Both follow the established ComfyUI numbered-node dict pattern.

Purpose: These are the only two builders that do not yet exist. All other builders (WFB-01 through WFB-05) are already implemented.
Output: Two new functions in workflow_builder.py with passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/phase-17/17-CONTEXT.md
@.planning/phases/phase-17/17-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From docker/gold-team/src/v6/engines/work_builder.py (existing pattern):
Every ComfyUI builder function:
  1. Accepts a seed param (int | None = None), generates random seed if None
  2. Returns dict[str, Any] with string-numbered keys
  3. Each node has "class_type" and "inputs"
  4. Node references use ["node_id", output_index] format

LatentSyncNode (from research):
  class_type: "LatentSyncNode"
  inputs: images (IMAGE tensor), audio (AUDIO tensor), seed (INT), lips_expression (FLOAT 1.0-3.0), inference_steps (INT 1-999)
  RETURN_TYPES: ("IMAGE", "AUDIO")

RIFE VFI (from research):
  class_type: "RIFE VFI"
  inputs: images (IMAGE tensor), ckpt_name (string), multiplier (INT)
  RETURN_TYPES: ("IMAGE",)

VHS_VideoCombine (from existing build_wan21_i2v_dual_stage_workflow):
  class_type: "VHS_VideoCombine"
  inputs: images, frame_rate, loop_count, filename_prefix, format, pingpong, save_output

VHS_LoadVideo:
  class_type: "VHS_LoadVideo"
  inputs: video, force_rate, custom_width, custom_height, frame_start, frame_end
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement build_lipsync_workflow (WFB-06)</name>
  <files>docker/gold-team/src/v6/engines/workflow_builder.py, docker/gold-team/tests/test_workflow_builder.py</files>

  <read_first>
  - docker/gold-team/src/v6/engines/workflow_builder.py (existing patterns, especially build_wan21_i2v_dual_stage_workflow for VHS_VideoCombine usage at lines 1234-1245)
  - docker/gold-team/tests/test_workflow_builder.py (existing tests from Plan 17-01)
  - .planning/phases/phase-17/17-CONTEXT.md (locked decisions: audio_input accepts file path/URL, output format MP4)
  - .planning/phases/phase-17/17-RESEARCH.md (LatentSync node API details, pitfall on tensor types)
  </read_first>

  <behavior>
  - Test 1: build_lipsync_workflow returns dict with exactly 4 top-level keys (nodes "1" through "4")
  - Test 2: Node "1" has class_type "VHS_LoadVideo" with video="test_video.mp4"
  - Test 3: Node "2" has class_type "LoadAudio" with audio="test_audio.wav"
  - Test 4: Node "3" has class_type "LatentSyncNode" linking to node "1" output 0 (images) and node "2" output 0 (audio)
  - Test 5: Node "4" has class_type "VHS_VideoCombine" linking to node "3" output 0, with format "video/h264-mp4", frame_rate=25
  - Test 6: Seed is deterministic when provided (seed=42 -> LatentSyncNode seed=42)
  - Test 7: Custom params (lips_expression=2.0, inference_steps=30) are passed through to LatentSyncNode
  - Test 8: Path traversal rejection: video_input containing ".." raises ValueError
  - Test 9: Path traversal rejection: audio_input containing ".." raises ValueError
  </behavior>

  <action>
  Add `build_lipsync_workflow` function at the end of workflow_builder.py (before the final return of build_image_refine_workflow, or appended after it). Follow the established pattern from build_wan21_i2v_dual_stage_workflow.

  Function signature per CONTEXT.md locked decisions:
  - video_input: str (required) -- video filename in ComfyUI input/ directory
  - audio_input: str (required) -- audio filename or URL (per D-01: accept both file path and URL)
  - seed: int | None = None
  - lips_expression: float = 1.5 (range 1.0-3.0 per LatentSync spec)
  - inference_steps: int = 20
  - output_fps: int = 25 (LatSync standard, per RESEARCH pitfall 6)
  - filename_prefix: str = "lipsync"

  Workflow node structure (4 nodes):
  - Node "1": VHS_LoadVideo -- loads video_input, extracts frames as IMAGE batch. force_rate=0, custom_width=-1, custom_height=-1, frame_start=0, frame_end=-1
  - Node "2": LoadAudio -- loads audio_input
  - Node "3": LatentSyncNode -- inputs: images=["1",0], audio=["2",0], seed, lips_expression, inference_steps
  - Node "4": VHS_VideoCombine -- inputs: images=["3",0], frame_rate=output_fps, loop_count=0, filename_prefix, format="video/h264-mp4", pingpong=False, save_output=True

  Input validation (per RESEARCH security domain T-17-01):
  - Reject video_input containing ".." or starting with "/" -- raise ValueError with descriptive message
  - Reject audio_input containing ".." or starting with "/" -- raise ValueError with descriptive message
  - Note: URL-based audio_input (starting with "http://" or "https://") is allowed per CONTEXT.md locked decision

  Update the module docstring at the top of workflow_builder.py to include the two new builders in the "Supports:" list.

  Write all tests listed in the behavior block. Add them to the existing test_workflow_builder.py file.
  </action>

  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/docker/gold-team && python -m pytest tests/test_workflow_builder.py::test_build_lipsync_workflow -v --tb=short 2>&1 && python -m pytest tests/test_workflow_builder.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>

  <done>
    - build_lipsync_workflow function exists in workflow_builder.py with 4-node structure
    - VHS_LoadVideo + LoadAudio + LatentSyncNode + VHS_VideoCombine pipeline
    - Path traversal validation on video_input and audio_input
    - All 9 behavior tests pass
    - Existing tests from Plan 17-01 still pass (no regressions)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement build_frame_interpolate_workflow (WFB-07)</name>
  <files>docker/gold-team/src/v6/engines/workflow_builder.py, docker/gold-team/tests/test_workflow_builder.py</files>

  <read_first>
  - docker/gold-team/src/v6/engines/workflow_builder.py (the build_lipsync_workflow just added, plus existing patterns)
  - docker/gold-team/tests/test_workflow_builder.py (existing tests including lipsync tests from Task 1)
  - .planning/phases/phase-17/17-CONTEXT.md (locked decisions: interpolation_factor 2x/4x/8x, video file input)
  - .planning/phases/phase-17/17-RESEARCH.md (RIFE VFI node details, multiplier semantics, pitfall on multiplier vs factor)
  </read_first>

  <behavior>
  - Test 1: build_frame_interpolate_workflow returns dict with exactly 3 top-level keys (nodes "1" through "3")
  - Test 2: Node "1" has class_type "VHS_LoadVideo" with video="test_video.mp4"
  - Test 3: Node "2" has class_type "RIFE VFI" linking to node "1" output 0
  - Test 4: Node "3" has class_type "VHS_VideoCombine" linking to node "2" output 0, with format "video/h264-mp4"
  - Test 5: Default interpolation_factor=2 maps to RIFE multiplier=1 (interpolation_factor - 1)
  - Test 6: interpolation_factor=4 maps to multiplier=3
  - Test 7: interpolation_factor=8 maps to multiplier=7
  - Test 8: Custom ckpt_name="rife49.pth" is passed to RIFE VFI node
  - Test 9: Custom output_fps=60 is passed to VHS_VideoCombine
  - Test 10: Path traversal rejection: video_input containing ".." raises ValueError
  </behavior>

  <action>
  Add `build_frame_interpolate_workflow` function in workflow_builder.py, placed right after build_lipsync_workflow.

  Function signature per CONTEXT.md locked decisions:
  - video_input: str (required) -- video filename in ComfyUI input/ directory
  - interpolation_factor: int = 2 (valid: 2, 4, 8 -- per CONTEXT.md locked decision)
  - ckpt_name: str = "rife49.pth" (RIFE model checkpoint filename)
  - output_fps: int | None = None (auto: input FPS * interpolation_factor if None)
  - seed: int | None = None (kept for API consistency, though RIFE VFI does not use seed)
  - filename_prefix: str = "frame_interp"

  RIFE multiplier mapping per RESEARCH (pitfall 2):
  - multiplier = interpolation_factor - 1
  - So 2x -> multiplier 1, 4x -> multiplier 3, 8x -> multiplier 7

  Workflow node structure (3 nodes):
  - Node "1": VHS_LoadVideo -- loads video_input, force_rate=0, custom_width=-1, custom_height=-1, frame_start=0, frame_end=-1
  - Node "2": RIFE VFI -- inputs: images=["1",0], ckpt_name, multiplier=(interpolation_factor - 1)
  - Node "3": VHS_VideoCombine -- inputs: images=["2",0], frame_rate=(output_fps or 30 as default), loop_count=0, filename_prefix, format="video/h264-mp4", pingpong=False, save_output=True

  Input validation:
  - Reject video_input containing ".." or starting with "/" -- raise ValueError

  Update the module docstring at the top of workflow_builder.py to include build_frame_interpolate_workflow.

  Write all tests listed in the behavior block. Add them to test_workflow_builder.py.
  </action>

  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/docker/gold-team && python -m pytest tests/test_workflow_builder.py::test_build_frame_interpolate_workflow -v --tb=short 2>&1 && python -m pytest tests/test_workflow_builder.py -v --tb=short 2>&1 | tail -25</automated>
  </verify>

  <done>
    - build_frame_interpolate_workflow function exists in workflow_builder.py with 3-node structure
    - VHS_LoadVideo + RIFE VFI + VHS_VideoCombine pipeline
    - Correct multiplier mapping (factor - 1)
    - Path traversal validation on video_input
    - All 10 behavior tests pass
    - All existing tests (Plans 17-01 + Task 1) still pass (no regressions)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User params -> workflow_builder | video_input and audio_input filenames must be sanitized |
| workflow_builder -> ComfyUI | Node graph is hardcoded; user params fill data fields only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | build_lipsync_workflow video_input/audio_input | mitigate | Validate filenames reject ".." and absolute paths |
| T-17-02 | Tampering | build_frame_interpolate_workflow video_input | mitigate | Validate filenames reject ".." and absolute paths |
| T-17-03 | Elevation | Workflow builder class_type values | accept | All class_types hardcoded in builder code; user cannot inject arbitrary nodes |
| T-17-SC | Tampering | pytest execution | accept | Test-only, no production risk |
</threat_model>

<verification>
```bash
cd /data/workspace/kais-aigc-platform/docker/gold-team && python -m pytest tests/test_workflow_builder.py -v
```
All tests pass including new lipsync and frame_interpolate tests plus existing builder tests from Plan 17-01.
</verification>

<success_criteria>
- build_lipsync_workflow produces valid 4-node ComfyUI graph with LatentSyncNode
- build_frame_interpolate_workflow produces valid 3-node ComfyUI graph with RIFE VFI
- Path traversal validation blocks ".." and absolute paths in video_input/audio_input
- All unit tests pass with zero regressions
</success_criteria>

<output>
Create `.planning/phases/phase-17/17-02-SUMMARY.md` when done
</output>
