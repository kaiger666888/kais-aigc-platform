---
phase: 17-workflow-builder-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/gold-team/tests/__init__.py
  - docker/gold-team/tests/test_workflow_builder.py
  - docker/gold-team/tests/conftest.py
autonomous: true
requirements:
  - WFB-01
  - WFB-02
  - WFB-03

must_haves:
  truths:
    - "build_flux_dev_workflow returns a valid ComfyUI workflow dict with KSampler and UNETLoader nodes"
    - "build_flux_ipadapter_workflow returns a valid workflow with IPAdapterFluxLoader and ApplyIPAdapterFlux nodes"
    - "build_hunyuan3d_workflow returns a valid subprocess parameter dict with input_image and output_path keys"
    - "All three existing builders produce output dicts with the correct class_type values"
  artifacts:
    - path: "docker/gold-team/tests/__init__.py"
      provides: "Test package marker"
    - path: "docker/gold-team/tests/conftest.py"
      provides: "Shared test fixtures for workflow builder tests"
    - path: "docker/gold-team/tests/test_workflow_builder.py"
      provides: "Unit tests for existing workflow builders"
      contains: "test_build_flux_dev_workflow"
      exports:
        - "test_build_flux_dev_workflow"
        - "test_build_flux_ipadapter_workflow"
        - "test_build_hunyuan3d_workflow"
  key_links:
    - from: "docker/gold-team/tests/test_workflow_builder.py"
      to: "docker/gold-team/src/v6/engines/workflow_builder.py"
      via: "import"
      pattern: "from src\\.v6\\.engines\\.workflow_builder import"
---

<objective>
Create test infrastructure for workflow builders and verify that the three already-existing builders (WFB-01, WFB-02, WFB-03) produce valid ComfyUI workflow dicts.

Purpose: Establish a test harness that all subsequent plans will extend. Verify existing builders work correctly before adding new ones.
Output: tests/ directory with passing unit tests for build_flux_dev_workflow, build_flux_ipadapter_workflow, and build_hunyuan3d_workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-17/17-CONTEXT.md
@.planning/phases/phase-17/17-RESEARCH.md
@.planning/phases/phase-17/17-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From docker/gold-team/src/v6/engines/workflow_builder.py:
- build_flux_dev_workflow(prompt, negative_prompt, width, height, steps, cfg_scale, seed, filename_prefix, unet_name, weight_dtype, clip_name1, clip_name2, clip_type, vae_name) -> dict[str, Any]
- build_flux_ipadapter_workflow(prompt, reference_image, negative_prompt, width, height, steps, cfg_scale, weight, start_percent, end_percent, seed, ...) -> dict[str, Any]
- build_hunyuan3d_workflow(input_image, output_path, model, device, steps, seed, model_dir, task_id) -> dict[str, Any]

All ComfyUI builders return dict[str, Any] with string-numbered keys mapping to node dicts.
Each node dict has "class_type" (str) and "inputs" (dict).
Subprocess builders (hunyuan3d, tts) return flat param dicts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test infrastructure and verify existing workflow builders</name>
  <files>docker/gold-team/tests/__init__.py, docker/gold-team/tests/conftest.py, docker/gold-team/tests/test_workflow_builder.py</files>

  <read_first>
  - docker/gold-team/src/v6/engines/workflow_builder.py (all builder functions, return types, node structure)
  - docker/gold-team/requirements.txt (verify pytest availability)
  </read_first>

  <action>
  1. Create the test package directory structure:
     - `docker/gold-team/tests/__init__.py` (empty file)
     - `docker/gold-team/tests/conftest.py` with shared fixtures

  2. In `docker/gold-team/tests/conftest.py`, define:
     - A `sys.path` fixture that adds `docker/gold-team` to Python path so `from src.v6.engines.workflow_builder import ...` works
     - A `sample_seed` fixture returning 42

  3. In `docker/gold-team/tests/test_workflow_builder.py`, create the following test functions:

     **test_build_flux_dev_workflow** (covers WFB-01):
     - Call `build_flux_dev_workflow(prompt="test prompt", seed=42)`
     - Assert result is a dict with string keys
     - Assert node "1" has class_type "UNETLoader"
     - Assert node "4" has class_type "CLIPTextEncode" with text "test prompt"
     - Assert node "6" has class_type "KSampler" with seed=42, steps=28, cfg=3.5
     - Assert node "8" has class_type "SaveImage"
     - Assert node "7" has class_type "VAEDecode" linking to node "6" output and node "3" output
     - Assert node "5" has class_type "EmptySD3LatentImage" with width=1024, height=1024

     **test_build_flux_ipadapter_workflow** (covers WFB-02):
     - Call `build_flux_ipadapter_workflow(prompt="test", reference_image="ref.png", seed=42)`
     - Assert node with class_type "IPAdapterFluxLoader" exists (node "10")
     - Assert node with class_type "LoadImage" has image="ref.png" (node "11")
     - Assert node with class_type "ApplyIPAdapterFlux" exists (node "12") linking to LoadImage and IPAdapterFluxLoader
     - Assert KSampler (node "6") uses the IPAdapter-modified model from node "12" (model input = ["12", 0])

     **test_build_hunyuan3d_workflow** (covers WFB-03):
     - Call `build_hunyuan3d_workflow(input_image="/path/to/img.png", task_id="test-123")`
     - Assert result is a flat dict (NOT a numbered-node ComfyUI graph)
     - Assert "input_image" key exists with value "/path/to/img.png"
     - Assert "output_path" key exists (auto-generated under KAIS_OUTPUT_ROOT)
     - Assert "model" key has default value "full"
     - Assert "steps" key has default value 50

     **test_build_flux_dev_workflow_custom_params**:
     - Call with width=512, height=768, steps=15, cfg_scale=7.0, seed=99
     - Assert KSampler has steps=15, cfg=7.0
     - Assert EmptySD3LatentImage has width=512, height=768

     **test_build_flux_ipadapter_workflow_custom_weight**:
     - Call with weight=0.5, start_percent=0.2, end_percent=0.6
     - Assert ApplyIPAdapterFlux has weight=0.5, start_percent=0.2, end_percent=0.6

  4. Verify all tests pass by running pytest from the `docker/gold-team` directory with PYTHONPATH set.
  </action>

  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/docker/gold-team && python -m pytest tests/test_workflow_builder.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>

  <done>
    - tests/test_workflow_builder.py exists with 5+ test functions covering WFB-01, WFB-02, WFB-03
    - All tests pass: build_flux_dev_workflow, build_flux_ipadapter_workflow, build_hunyuan3d_workflow verified
    - Test infrastructure (conftest.py, __init__.py) in place for subsequent plans to extend
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code -> workflow_builder | Test imports and exercises builder functions; no external trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-SC | Tampering | pytest execution | accept | Test-only, no production risk |
</threat_model>

<verification>
```bash
cd /data/workspace/kais-aigc-platform/docker/gold-team && python -m pytest tests/test_workflow_builder.py -v
```
All 5+ tests pass, covering flux_dev, flux_ipadapter, and hunyuan3d builders.
</verification>

<success_criteria>
- 3 test functions verify the 3 existing builders produce correct node structures
- 2 additional tests verify custom parameter passthrough
- All tests pass
- Test scaffold ready for Plans 02-04 to extend
</success_criteria>

<output>
Create `.planning/phases/phase-17/17-01-SUMMARY.md` when done
</output>
