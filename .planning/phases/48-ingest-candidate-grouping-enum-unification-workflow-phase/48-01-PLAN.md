---
phase: 48-ingest-candidate-grouping-enum-unification-workflow-phase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/assetTypes.ts
  - src/lib/candidateGrouping.ts
  - scripts/fixtures/phase48-p11-manifest.fixture.json
  - scripts/verify-phase-48.ts
  - package.json
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03, PHASE-01]

must_haves:
  truths:
    - "normalizeAssetType('role')==='character', normalizeAssetType('tool')==='prop', canonical values pass through, unknown values return null"
    - "expandTypesForQuery('character') returns ['character','role'] so registry search can still find legacy-vocabulary rows"
    - "planGroups on the P11 manifest fixture yields per-shot first/last groups; primary = selected_*_variant when set, else first member in all_*_frames order"
    - "planGroups on naming-convention inputs groups *_v{N} variants; primary = canonical no-suffix file when present, else lowest variant number"
    - "deriveWorkflowPhase extracts normalized lowercase p{NN} from phase field or path segment; returns null (never guesses) when underivable"
    - "npm run verify:phase-48 exits 0 with all Part-1 assertions passing"
  artifacts:
    - path: "src/lib/assetTypes.ts"
      provides: "assetType 单一真值源 (canonical enum + legacy aliases + normalize/expand)"
      exports: ["CANONICAL_ASSET_TYPES", "LEGACY_ASSET_TYPE_ALIASES", "INGEST_INPUT_ASSET_TYPES", "normalizeAssetType", "expandTypesForQuery"]
    - path: "src/lib/candidateGrouping.ts"
      provides: "候选建组纯函数 (manifest 通道 + 命名通道 + primary 解析 + workflow_phase 推导)"
      exports: ["planGroups", "parseVariantName", "deriveWorkflowPhase"]
    - path: "scripts/fixtures/phase48-p11-manifest.fixture.json"
      provides: "kmc iframe-manifest 形状的 fixture (Phase 50 GUARD-01 契约测试复用)"
      contains: "selected_first_variant"
    - path: "scripts/verify-phase-48.ts"
      provides: "Phase 48 验证脚本 Part 1 (纯函数动态断言)"
  key_links:
    - from: "scripts/verify-phase-48.ts"
      to: "src/lib/assetTypes.ts"
      via: "dynamic import + behavior assertions"
      pattern: "normalizeAssetType"
    - from: "scripts/verify-phase-48.ts"
      to: "src/lib/candidateGrouping.ts"
      via: "dynamic import + fixture-driven grouping assertions"
      pattern: "planGroups"
    - from: "package.json"
      to: "scripts/verify-phase-48.ts"
      via: "npm script verify:phase-48"
      pattern: "verify:phase-48"
---

<objective>
Create the contract layer Phase 48 everything else builds on: the assetType single truth source (D-06/D-07, INGEST-03) and the pure candidate-grouping functions (manifest channel D-02, naming channel D-02, priority D-03, primary resolution D-04, state policy D-05, workflow_phase derivation D-08) — plus the kmc-shape fixture and the first part of `scripts/verify-phase-48.ts` that proves them.

Purpose: Plan 48-02 (route rewrite + registry compat) and Phase 50 (backfill + GUARD-01 contract test) both consume these exact exports. Locking the contracts first prevents scavenger-hunt execution and guarantees the backfill script reuses the same grouping function as online ingest (CONTEXT.md Claude's Discretion recommendation).

Output: 4 new files (`src/lib/assetTypes.ts`, `src/lib/candidateGrouping.ts`, `scripts/fixtures/phase48-p11-manifest.fixture.json`, `scripts/verify-phase-48.ts`) + one `package.json` script line.
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-ingest-candidate-grouping-enum-unification-workflow-phase/48-CONTEXT.md

Ground truth — real kmc iframe-manifest entry (ep-shencongshenyuan-ep01, 15 entries total; `selected_*_variant` observed as null on unselected shots, 1-based index into `all_*_frames` when set; paths are workdir-RELATIVE):

```json
{
  "shot_id": "S01_B01",
  "iframe_mode": "paired",
  "variants": 3,
  "first_frame_path": "assets/P11/iframes_S01_B01/first_frame_v1.png",
  "all_first_frames": [
    "assets/P11/iframes_S01_B01/first_frame_v1.png",
    "assets/P11/iframes_S01_B01/first_frame_v2.png",
    "assets/P11/iframes_S01_B01/first_frame_v3.png"
  ],
  "all_last_frames": [
    "assets/P11/iframes_S01_B01/last_frame_v1.png",
    "assets/P11/iframes_S01_B01/last_frame_v2.png",
    "assets/P11/iframes_S01_B01/last_frame_v3.png"
  ],
  "selected_first_variant": null,
  "selected_last_variant": null,
  "first_frame_prompt": "…",
  "last_frame_prompt": "…"
}
```

Naming-convention channel ground truth (turnaround_sheets dir listing): `base_turnaround_chengyu_v1.png`, `base_turnaround_chengyu_v2.png`, `base_turnaround_chengyu_v3.png` coexist with canonical no-suffix `base_turnaround_chengyu.png` (same for luyanzhou, guhongyuan, …). Legacy oddity observed: `base_turnaround_guhongyuan.1.png` — parseVariantName must treat it as non-variant (standalone), not crash.

Repo conventions:
- verify scripts: `scripts/verify-phase-*.ts` with `assert(cond, name, detail)` + PASS/FAIL console lines + `process.exit(1)` on any FAIL (see `scripts/verify-phase-41.ts` / `verify-phase-45.ts`), run via `npx tsx`, registered in package.json as `verify:phase-N`.
- TS path alias `@/` maps to `src/` (routes import `@/lib/...`, `@/utils`).
- v1.6 pattern: truth source = delete duplicates, do not wrap (STATE.md Decisions).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/lib/assetTypes.ts — assetType 单一真值源 (INGEST-03 contract)</name>
  <files>src/lib/assetTypes.ts</files>
  <read_first>
  - src/routes/v1/assets-registry/index.ts (lines 18-35 createSchema enum, lines 98-142 /search handler — the primary consumer whose vocabulary wins per D-06)
  - src/routes/v1/pipeline/ingest/images.ts (line 18 — the legacy vocabulary being eliminated)
  - src/lib/canvasAssetSchema.ts (lib file conventions: header comment style, export style)
  </read_first>
  <behavior>
  - normalizeAssetType("role") → "character"; normalizeAssetType("tool") → "prop"; normalizeAssetType("scene") → "scene"; normalizeAssetType("character") → "character"; normalizeAssetType("keyframe") → null; normalizeAssetType("") → null
  - expandTypesForQuery("character") → ["character", "role"]; expandTypesForQuery("prop") → ["prop", "tool"]; expandTypesForQuery("scene") → ["scene"]; expandTypesForQuery("delivery") → ["delivery"]; expandTypesForQuery("bogus") → ["bogus"] (passthrough, no crash)
  - CANONICAL_ASSET_TYPES contains exactly the 11 registry values, no more, no less
  </behavior>
  <action>
  Create a pure data+function module (no zod, no DB, no fs imports) implementing decision D-06/D-07 (INGEST-03). Exports, exactly:
  (1) `CANONICAL_ASSET_TYPES` — a readonly string tuple copied value-for-value from the registry enum at src/routes/v1/assets-registry/index.ts:21: character, scene, prop, clip, voice, video, storyboard, script_phase, outline, topic, delivery. Plus a `CanonicalAssetType` type derived from it via `typeof [number]`.
  (2) `LEGACY_ASSET_TYPE_ALIASES` — a Record mapping the two legacy ingest words per D-06: role → character, tool → prop. Nothing else.
  (3) `INGEST_INPUT_ASSET_TYPES` — readonly tuple = canonical values followed by the legacy alias keys (13 total); this is the zod-enum source for the rewritten ingest route in Plan 48-02 (input accepts old words, output is always canonical).
  (4) `normalizeAssetType(input: string): CanonicalAssetType | null` — returns the canonical value for canonical input (identity), maps legacy alias keys to their targets, returns null for anything unknown. Never throws.
  (5) `expandTypesForQuery(canonical: string): string[]` — returns the input plus every legacy alias key whose target equals the input; single-element array when the input has no aliases or is not canonical (passthrough so callers can use it unconditionally in a whereIn).
  Include a header comment stating this module is the single truth source for o_assets.type vocabulary per Phase 48 INGEST-03 and that assets-registry/index.ts and pipeline/ingest/images.ts must import from here instead of declaring inline enums.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "import('./src/lib/assetTypes.ts').catch(()=>process.exit(1))" 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
  - File src/lib/assetTypes.ts exists and exports exactly: CANONICAL_ASSET_TYPES, CanonicalAssetType, LEGACY_ASSET_TYPE_ALIASES, INGEST_INPUT_ASSET_TYPES, normalizeAssetType, expandTypesForQuery
  - Source assertion: file contains neither `import u from` nor `from "fs"` nor `from "knex"` (pure module)
  - Source assertion: the string `script_phase` appears in the CANONICAL tuple; the strings `role` and `tool` appear only in the alias mapping / input-types tuple contexts
  - Behavior assertions in Task 3's verify script all pass (normalize + expand cases from the behavior block)
  - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>assetTypes.ts compiles, exports the 5 symbols + type, and all normalize/expand behaviors listed above are exercised green in scripts/verify-phase-48.ts (Task 3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/lib/candidateGrouping.ts — 建组/primary/workflow_phase 纯函数 (INGEST-01/02 + PHASE-01 contracts)</name>
  <files>src/lib/candidateGrouping.ts</files>
  <read_first>
  - src/routes/v1/assets-registry/index.ts (lines 27, 277-294: assetsId semantics "Primary→Secondary 引用", variants endpoint queries where a.assetsId = integer id — the group shape being reused)
  - packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx (lines 8-15 三态模型; lines 236-287 getGroupKey — the UI-side consumer that groups independently by characterId/meta.subtype; lines 782-805 handleSelect)
  - scripts/canvas/register_turnaround_b2.py (the manual three-layer write shape the new path must equal: characterId + meta.subtype='turnaround_sheet' + workflow_phase='p04_turnaround')
  - /data/workspace/kais-hermes-skills/skills/kais-movie-pipeline/episodes/ep-shencongshenyuan-ep01/assets/P11/iframe-manifest.json (real manifest — 1-based selected_*_variant, relative paths)
  - src/lib/initDB.ts lines 447-478 (o_assets DDL — assetsId integer, isPrimaryView boolean default false, state default 'active', workflow_phase column)
  </read_first>
  <behavior>
  - parseVariantName("…/base_turnaround_chengyu_v2.png") → { base: "base_turnaround_chengyu", variant: 2 }; parseVariantName("…/base_turnaround_chengyu.png") → null; parseVariantName("…/base_turnaround_guhongyuan.1.png") → null (legacy oddity, treated as standalone)
  - planGroups manifest channel, fixture entry shot S01_B01 (selected_first_variant=2, selected_last_variant=null): group "shot:S01_B01:first" has 3 members, primary = basename first_frame_v2.png; group "shot:S01_B01:last" has 3 members, primary = basename last_frame_v1.png (fallback = first entry of all_last_frames)
  - planGroups manifest channel, partial batch (only v1+v3 of all_first_frames present in images): group members = the 2 present paths; if selected index points at an absent path, primary falls back to first present member in all_*_frames order
  - planGroups naming channel with canonical present ([base_turnaround_chengyu.png, _v1, _v2, _v3]): one group, primary = canonical, characterId = "chengyu"; without canonical ([scene_S07_v1.png, scene_S07_v2.png]): primary = _v1 (lowest variant number)
  - planGroups priority (D-03): an image claimed by a manifest group is never re-claimed by the naming channel
  - Unmatched image → listed in ungroupedFilePaths (standalone, 维持现状, no error)
  - deriveWorkflowPhase("p11", any) → "p11"; ("P04", …) → "p04"; (undefined, "/oss/1785508691757/p04/turnaround_sheets/base_turnaround_chengyu_v1.png") → "p04"; (undefined, "assets/P11/iframes_S01_B01/first_frame_v1.png") → "p11"; ("pipeline", "/oss/manual/hero.png") → null; ("4", any) → null (no p prefix — never guess)
  </behavior>
  <action>
  Create a pure module (no DB/fs/network imports — functions take data in, return plans out) exporting exactly the types and functions below. This is the grouping engine shared by online ingest (Plan 48-02) and the Phase 50 backfill script.

  Types: `IngestImageInput` (filePath: string; assetName: string; assetType?: string; prompt?: string; description?: string; characterId?: string; viewAngle?: string; subtype?: string; meta?: Record<string, unknown>), `ManifestFrameEntry` (shot_id: string; all_first_frames?: string[]; all_last_frames?: string[]; selected_first_variant?: number | null; selected_last_variant?: number | null; first_frame_prompt?: string; last_frame_prompt?: string), `AssetGroupPlan` (groupKey: string; primaryFilePath: string; memberFilePaths: string[]; characterId?: string; metaSubtype?: string; source: "manifest" | "naming"), and `GroupPlanResult` (groups: AssetGroupPlan[]; ungroupedFilePaths: string[]).

  (1) `parseVariantName(filePath: string): { base: string; variant: number } | null` — take the basename, strip the final extension, match trailing `_v{digits}` (regex on the whole base, e.g. ^(.*)_v(\d+)$). Return null when there is no `_v{N}` suffix. Non-numeric suffixes (`.1` legacy files) must not match.

  (2) `planGroups(images: IngestImageInput[], manifests?: ManifestFrameEntry[]): GroupPlanResult` — implement D-02/D-03/D-04/D-05:
  - Build a basename → image index (first occurrence wins; manifest paths are workdir-relative while ingest filePaths are OSS-style, so manifest↔batch matching is by basename).
  - Manifest channel FIRST (priority D-03): for each entry, for each of the two frame lists all_first_frames / all_last_frames: members = list paths whose basename is present in the batch; skip lists with fewer than 1 present member. groupKey format `shot:{shot_id}:first` / `shot:{shot_id}:last` — precisely the literal `shot:` + shot_id + `:first` or `:last` (first and last frames of one shot are TWO different groups, matching getGroupKey's keyframe split). Primary resolution per D-04: if selected_first_variant (resp. selected_last_variant) is an integer ≥ 1 and ≤ list length AND the path at that 1-based index is present in the batch, that path is primary; otherwise primary = the first present member in the list's original order (which yields v1 on unselected shots). Record source "manifest".
  - Naming channel for images not claimed by the manifest: for each image whose parseVariantName returns a match, group by base. If a batch image exists whose extension-stripped basename equals base exactly (canonical no-suffix file), that canonical image is primary; otherwise the member with the lowest variant number is primary (D-04 fallback). groupKey format `name:{base}`. Turnaround characterId extraction (CONTEXT specifics "characterId 仍应尽量写入"): if base matches trailing turnaround_([a-z0-9]+) (e.g. base_turnaround_chengyu, final_turnaround_x), set the group's characterId to the capture and metaSubtype to "turnaround_sheet" (mirrors register_turnaround_b2.py meta shape). Take characterId also from any member's input characterId if present (input wins over name-derived). Record source "naming".
  - Everything else → ungroupedFilePaths (D-03 "无分组维持现状，不抛错").
  - Deterministic output: groups sorted by groupKey; memberFilePaths in their natural list/variant order.

  (3) `deriveWorkflowPhase(phase: string | undefined, filePath: string): string | null` — per D-08, never guess: first try the phase string: match p followed by 1-2 digits at a word boundary, case-insensitive (e.g. "p11", "P04", "p04_turnaround" → 11/04); if no match, try each path segment of filePath against the exact segment pattern p{1-2 digits} (case-insensitive; catches both `/oss/{proj}/p04/…` and kmc-relative `assets/P11/…`). Normalize to lowercase `p` + zero-padded 2 digits ("p04", "p11"). Return null when neither source yields a value. Callers (Plan 48-02) are responsible for the null → logger.warn part of D-08; document that in the JSDoc.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
  - File exports exactly: IngestImageInput, ManifestFrameEntry, AssetGroupPlan, GroupPlanResult, parseVariantName, planGroups, deriveWorkflowPhase
  - Source assertion: no `import u from "@/utils"`, no `from "fs"`, no `from "knex"` in the file (pure module — the DB-writing service lives in Plan 48-02's ingestAssets.ts)
  - Source assertion: the module contains the groupKey literal formats `:first` and `:last` and `name:` and the state policy is NOT implemented here (no 'eliminated'/'archived' writer — D-05 says ingest writes active only, enforced at the service layer)
  - Every behavior in the behavior block passes as a dynamic assertion in scripts/verify-phase-48.ts (Task 3)
  - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>candidateGrouping.ts compiles, is pure, and all behavior-block cases (manifest channel incl. selected-variant + partial batch + fallback, naming channel with/without canonical, priority, standalone passthrough, workflow_phase derivation incl. null cases) are proven green by npm run verify:phase-48.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: kmc-shape fixture + scripts/verify-phase-48.ts Part 1 + package.json registration</name>
  <files>scripts/fixtures/phase48-p11-manifest.fixture.json, scripts/verify-phase-48.ts, package.json</files>
  <read_first>
  - scripts/verify-phase-41.ts (full — the assertion/assert + PASS/FAIL + exit-code + summary convention to replicate)
  - scripts/verify-phase-45.ts (header comment style + section organization convention)
  - /data/workspace/kais-hermes-skills/skills/kais-movie-pipeline/episodes/ep-shencongshenyuan-ep01/assets/P11/iframe-manifest.json (fixture must mirror this shape; prompts may be shortened)
  - package.json (scripts block — registration convention verify:phase-N)
  - src/lib/assetTypes.ts and src/lib/candidateGrouping.ts (created in Tasks 1-2 — the modules under test; import their real exports, do not re-implement)
  </read_first>
  <behavior>
  - npm run verify:phase-48 exits 0 when all assertions pass; exits 1 and prints each FAIL line when any assertion fails
  - All assertions import the REAL modules from src/lib (dynamic `await import` per verify-phase-41 convention) — no re-implemented copies
  - Fixture round-trip: JSON.parse of the fixture file yields ManifestFrameEntry-shaped objects consumable directly by planGroups
  </behavior>
  <action>
  (a) Create `scripts/fixtures/phase48-p11-manifest.fixture.json` — an array of 2 entries mirroring the real kmc manifest field set (shot_id, iframe_mode, variants, first_frame_path, last_frame_path, all_first_frames, all_last_frames, selected_first_variant, selected_last_variant, first_frame_prompt, last_frame_prompt; prompts may be one-liners). Entry 1: shot_id "S01_B01", 3 first + 3 last frame paths under `assets/P11/iframes_S01_B01/`, selected_first_variant 2, selected_last_variant null. Entry 2: shot_id "S02_B01", 2 first + 2 last frame paths under `assets/P11/iframes_S02_B01/`, selected_first_variant null, selected_last_variant 2. This fixture is intentionally Phase-50-reusable (GUARD-01 contract test will re-ingest it).

  (b) Create `scripts/verify-phase-48.ts` following the verify-phase-41 script skeleton (TestResult/assert helper, REPO_ROOT, console PASS/FAIL per assertion, final "X/Y PASSED" summary, process.exit(1) on any failure). Header comment maps sections to INGEST-01..03 / PHASE-01. Part 1 (this task) contains, via dynamic import of ../src/lib/assetTypes and ../src/lib/candidateGrouping:
  - INGEST-03 unit section: every normalizeAssetType and expandTypesForQuery case from Task 1's behavior block.
  - parseVariantName section: _v2 match, canonical null, `base_turnaround_guhongyuan.1.png` null.
  - INGEST-01/02 grouping section: load the fixture file, build an images array whose filePaths are OSS-style (`/oss/1785508691757/p11/iframes_S01_B01/first_frame_v1.png` etc. — basenames match the fixture's relative paths) and call planGroups; assert the 4 manifest groups with exact groupKeys shot:S01_B01:first / shot:S01_B01:last / shot:S02_B01:first / shot:S02_B01:last, their member counts (3/3/2/2), and their primaries (v2 / v1 / v1 / v2 per the selected/fallback rules). Also run a partial-batch variant (drop v2 of S01 first frames from images) and assert 2 members with primary = first present member.
  - Naming-channel section: images for base_turnaround_chengyu canonical + v1..v3 (paths under `/oss/1785508691757/p04/turnaround_sheets/`) → one group, primary = canonical, characterId "chengyu", metaSubtype "turnaround_sheet"; scene_S07_v1 + scene_S07_v2 (no canonical, paths under `/oss/manual/`) → primary = v1.
  - Priority section: with manifests present, a naming-channel-eligible manifest member is claimed by the manifest group only.
  - Standalone section: `/oss/manual/hero.png` appears in ungroupedFilePaths.
  - PHASE-01 section: all deriveWorkflowPhase cases from Task 2's behavior block.
  Leave a clearly marked "Part 2 (Plan 48-02): ingest behavior on temp sqlite" placeholder comment at the bottom for Plan 48-02 to extend.

  (c) Register in package.json scripts: `"verify:phase-48": "npx tsx scripts/verify-phase-48.ts"` (place next to the other verify:phase-* entries).
  </action>
  <verify>
    <automated>npm run verify:phase-48 && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
  - `npm run verify:phase-48` prints per-assertion PASS lines and a summary, exit code 0
  - `grep -c "verify:phase-48" package.json` returns ≥ 1
  - scripts/verify-phase-48.ts contains `await import("../src/lib/candidateGrouping")` (or equivalent dynamic import) — imports real modules, and contains the Part-2 placeholder marker string `Part 2`
  - Fixture file parses as JSON and contains `"selected_first_variant": 2` and `"selected_last_variant": null`
  - Forcing a failure (e.g. temporarily asserting a wrong primary) makes the script exit 1 — verify once during development, then restore correct assertions
  </acceptance_criteria>
  <done>verify:phase-48 registered and green end-to-end: enum truth-source behaviors, both grouping channels (manifest + naming) with primary resolution incl. selected-variant and fallback, priority, standalone passthrough, and workflow_phase derivation all asserted against the real lib modules and the kmc-shape fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed in this plan) | Pure contract layer — no HTTP handler, no DB, no fs writes in the lib modules. The verify script only reads repo files. Input validation of untrusted HTTP bodies lands in Plan 48-02's route schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | DoS | planGroups on adversarial input | accept | Pure function, no IO; array-size caps are enforced at the route zod layer in Plan 48-02 (images ≤ 200, manifests ≤ 100, frames ≤ 20) — noted here so the boundary is explicit |
| T-48-02 | Tampering | fixture file | accept | Test-only artifact, never shipped or executed at runtime |
| T-48-SC | Tampering | npm/pip installs | accept (none) | No package installs in this plan — tsx, knex, better-sqlite3, zod all already in devDependencies |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (root project)
- `npm run verify:phase-48` exits 0 with all Part-1 assertions PASS
- `git diff --stat` shows only the 5 files in files_modified
</verification>

<success_criteria>
- assetType truth source exists with the exact 6 exports; legacy maps role→character, tool→prop; unknown → null (no silent passthrough on write path)
- Grouping engine handles both kmc channels with correct primary resolution (selected > v1/canonical fallback) and never re-claims manifest members for the naming channel
- deriveWorkflowPhase never guesses — null when underivable
- Fixture mirrors real kmc manifest shape (reusable by Phase 50 GUARD-01)
- Verify script imports real modules (non-tautological) and is registered in package.json
</success_criteria>

<output>
Create `.planning/phases/48-ingest-candidate-grouping-enum-unification-workflow-phase/48-01-SUMMARY.md` when done
</output>
