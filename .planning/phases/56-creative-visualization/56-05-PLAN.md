---
phase: 56-creative-visualization
plan: 05
type: execute
wave: 3
depends_on: ["56-01", "56-02", "56-04"]
files_modified:
  - src/lib/g15Bridge.ts
  - src/routes/canvas/v2/g15-ops.ts
  - packages/infinite-canvas/src/services/canvasApi.ts
  - packages/infinite-canvas/src/components/g16/voiceAuditStore.ts
  - packages/infinite-canvas/src/components/g16/useVoiceKeyboard.ts
  - packages/infinite-canvas/src/components/g16/__tests__/voiceAuditStore.test.ts
  - packages/infinite-canvas/src/components/g16/__tests__/useVoiceKeyboard.test.ts
  - packages/infinite-canvas/src/components/g16/G16VoiceWorkbench.tsx
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
  - packages/infinite-canvas/src/components/gate/GateCenterPanel.tsx
autonomous: true
requirements: [VIZ-03]
must_haves:
  truths:
    - "G15 操作桥 gate 目标参数化(D-11):route zod 加 gate 可选字段(白名单正则 ^p\\d+[a-z0-9]*-gate$,缺省 'p11c-gate')→ dispatchG15Op({...,gate}) → reviewBridge approve 目标;G15 既有调用零改动(缺省=现行为);writeback 队列旧行无 gate 字段天然回放正确"
    - "voiceAuditStore(g15TriageStore 同构):VoiceAuditSource seam(loadClips)+ fixture 对齐 p10c 真实 clips 形状({id,shot_id,path,transcript,verdict PASS/WARN/FAIL,similarity,reason,dims})+ 真实源=graph 内 voice-audit 节点 raw 袋防御式派生 + selected/rowState(waived)乐观回滚 + currentIndex 连播游标;无 requeue(G16 只有豁免)"
    - "工作台形态(D-09):TheaterShell 审核变体——左条目列表(min(380px,30vw) 内滚,行 28px+:勾选+shot_id mono+说话人+similarity mono+verdict 徽章)+ 右双轨区 + 底部 sticky 动作条(已选 N/全选/清空/重听/连播 toggle/批量豁免);列表主导"
    - "双轨(D-10,签名元素):上轨波形 canvas 72px(audioPeaks 真峰/伪兜底)+ 下轨转写分句(transcriptAlign 等时对齐;当前句 primary+左 2px accent,其余 secondary;点句按比例 seek)+ 一条共享光标贯穿两轨(rAF ~15fps 镜像,wallTransport UI 手法);近似对齐注记常显(10px tertiary)"
    - "连播+键盘(D-12):播完 onended 自动下一条(跳过已豁免)/可暂停;空格 播停 / → 下一条 / ← 上一条 / Esc 关(useWallKeyboard 范式新 hook;数字键/Enter 不占用)"
    - "批量豁免走 g15Ops(action:'waive', gate:'p10c-gate')(D-11);乐观 rowState→失败回滚+toast(G15 文法);豁免轻操作免二次确认(53-07 裁定沿用)"
    - "入口 = GateCenterPanel p10c-gate 行动作『打开听审工作台』(唯一入口;NodeDetailPanel 不动——56-04 同 wave 并行改该文件,避免冲突,语音审计节点详情入口列为 non-goal 记入 SUMMARY);FlowCanvas 挂载"
  artifacts:
    - path: "src/lib/g15Bridge.ts"
      provides: "dispatchG15Op gate 可选参数(缺省 p11c-gate)——G16 复用 waive 通道"
      contains: "gate"
    - path: "packages/infinite-canvas/src/components/g16/voiceAuditStore.ts"
      provides: "听审数据源 seam + fixture + 行状态机 + 连播游标"
      contains: "useVoiceAuditStore"
    - path: "packages/infinite-canvas/src/components/g16/G16VoiceWorkbench.tsx"
      provides: "全屏听审工作台(TheaterShell 消费;列表+双轨+动作条+键盘)"
      contains: "G16VoiceWorkbench"
  key_links:
    - from: "packages/infinite-canvas/src/components/g16/G16VoiceWorkbench.tsx"
      to: "packages/infinite-canvas/src/services/canvasApi.ts"
      via: "批量豁免 → g15Ops(projectId, episodesId, 'waive', shotIds, 'p10c-gate') 乐观+回滚+toast"
      pattern: "g15Ops"
    - from: "packages/infinite-canvas/src/components/g16/G16VoiceWorkbench.tsx"
      to: "packages/infinite-canvas/src/utils/audioPeaks.ts"
      via: "选中条目 → resolvePeaks(url) → canvas 波形"
      pattern: "resolvePeaks"
---

<objective>
落 VIZ-03 G16 配音听审工作台全链:① g15Bridge/route/canvasApi 三层 gate 参数化(D-11 复用 waive 通道,目标 p10c-gate);② voiceAuditStore(g15TriageStore 同构 + 连播游标)+ useVoiceKeyboard;③ G16VoiceWorkbench 全屏工作台(列表主导 + 波形×转写双轨签名元素 + 批量豁免);④ GateCenterPanel/详情面板双入口 + FlowCanvas 挂载。

完成后「一批待审配音可在工作台内全部处理完毕且状态回写正确」(ROADMAP 成功标准 3)成立。

输出:3 个修改 + 4 个新文件(2 测试)+ 2 个入口修改。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/56-creative-visualization/56-CONTEXT.md
@.planning/phases/56-creative-visualization/56-RESEARCH.md
@.planning/phases/56-creative-visualization/56-PATTERNS.md
@.planning/phases/56-creative-visualization/56-UI-SPEC.md

**COORD-01:本 plan 触 root 后端三文件(g15Bridge.ts/g15-ops.ts/canvasApi.ts)但全部是 kap 侧既有桥的参数扩展,零 khs 文件。执行者不得修改 files_modified 之外的文件。**

**Wave 裁定:本 plan 在 Wave 3(非与 56-04 并行)——FlowCanvas.tsx 与 56-04 双击路由同文件,串行消解并行编辑冲突;56-01/56-02 的数据与引擎前置不受影响。**

<design_decisions>
<!-- 前端设计纪律——UI-SPEC §5 全规格;签名元素 = 时间轴对齐双轨,其余安静(反 dashboard) -->

**Token:**
- 壳:TheaterShell(56-02);标题『配音听审 · {名}』+ 头部计数『待审 {pending} · 共 {total}』(11px secondary)
- verdict 徽章(行内 chip):PASS `v3theme.signal.approved`/FAIL `signal.rejected`/WARN `signal.running`,弱底 rgba 同色 0.12(G15TriagePanel categoryStyle L19-26 同款拼法)+ verdictLabel 中文(通过/留意/不过)
- 当前播放行:左 2px 实线 `--cv-select` + `rgba(237,238,241,0.10)` 弱底(ShotTree active 同款,UI-SPEC Accent ①)
- 已豁免行:整行 opacity 0.45 + 『已豁免』approved 色标(G15 L213-216 同款)
- 波形:已播 `v3theme.modality.audio` 1.0 / 未播 0.35;共享光标 1.5px `v3theme.signal.select`;句行当前句 text.primary + 左 2px select,其余 text.secondary
- 动作条:theaterBtnStyle;连播 toggle 激活态 select 描边(UI-SPEC Accent ④);批量豁免默认样式(G15 同款,冷白不落批量动作)

**布局:** 左列表 `min(380px, 30vw)` border-right;右双轨区 padding 12(剧场节奏)+ 内 flex column(波形 72px / 光标贯穿容器 / 句列表 flex:1 内滚);底部动作条 sticky bg.panel + border-top,高 ≥44px。
**文案:** UI-SPEC Copywriting 表逐字(配音听审/批量豁免/重听/连播/连播中/上一条/下一条/已豁免/toast 两式/空态两式/近似对齐注)。
**z/焦点:** TheaterShell zIndex 40;动作条按钮全 `<button>`;`:focus-visible` 既有规则。
</design_decisions>

<interfaces>
g15Bridge.ts 现状:L66 `dispatchG15Op({projectId, episodesId, action, shotIds})`,L93 注释「G15 = p11c-gate」;reviewBridge 调用位在函数体内(grep approve)。扩展:`gate?: string` 参数对象成员,内部 gate 传递到 review 载荷;**默认值 'p11c-gate' 字面缺省**。

g15-ops.ts 现状:L30 `action: z.enum(["waive","requeue"])`,body L23 注释,L68 解构,L72 dispatchG15Op,L80 writeback payload。扩展:schema 加 `gate: z.string().regex(/^p\d+[a-z0-9]*-gate$/).optional()`(gateCatalog.ts:53-58 deriveGateId 词汇同源正则);解构透传;writeback payload 带 gate。

canvasApi.ts 现状:L483-495 g15Ops(projectId, episodesId, action, shotIds)——加尾参 `gate?: string` 进 body。

voice-audit 数据形状(56-RESEARCH §G16):节点 raw 袋顶层 {phase, findings[], clips?, critical_count, warning_count, total_clips, pass};clips[] = {id, shot_id, path, transcript, verdict, similarity, reason, dims}(p10c_voice_audit.py:588-598)。防御式解析键序 ['clips','findings'](findings 兜底仅当其元素含 shot_id+verdict;否则空+warn)。

G15TriagePanel 平移件(结构参照,不 import):乐观回滚 runBatch L72-86 / sticky 动作条 L131-171 / 行组件 L177-243 / Esc L60-65。

useWallKeyboard 范式:useVoiceKeyboard(enabled, handlers, {})——键位 空格/→/←/Esc 四键;handlers ref 转发;enabled 门控;cleanup。

audioPeaks/transcriptAlign(56-02 产物):resolvePeaks(url) / splitSentences+evenAlign+sentenceAt;波形绘制 canvas + devicePixelRatio;伪波形注记『伪波形』10px tertiary。

连播引擎:单 `<audio>` 元素(audio src = resolveMediaUrl(clip.path));onended → next Pending(未豁免)条目 autoplay(**首次连播须由用户手势启动**——toggle 点击/空格即手势;链内后续 play() 属手势链,autoplay 策略满足,56-RESEARCH Pitfall 6);onTimeUpdate 更新 currentTime state(或 rAF 镜像——取 onTimeUpdate 简单,渲染节流由 React 批处理承担,**终裁 onTimeUpdate**);跳豁免行的推进函数纯逻辑进 store(nextPending(index))。

GateCenterPanel 扩展点:gate 行动作区(G15 内嵌同位);p10c-gate 判定 gateStore 词汇(gateCatalog 'p10c-gate');按钮 onClick → useVoiceAuditStore.setOpen(true)。**NodeDetailPanel 明确不动**(56-04 同 wave 并行修改该文件——同文件并行编辑是 wave 冲突源;语音审计节点详情面板按钮为 non-goal,记入 SUMMARY 待后续 phase 或 quick task)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: g15 桥三层 gate 参数化(root)+ 契约自查</name>
  <files>src/lib/g15Bridge.ts, src/routes/canvas/v2/g15-ops.ts, packages/infinite-canvas/src/services/canvasApi.ts</files>
  <read_first>src/lib/g15Bridge.ts (全文——dispatchG15Op 签名/reviewBridge 调用位/writeback 入队), src/routes/canvas/v2/g15-ops.ts (全文——zod schema/解构/writeback payload), packages/infinite-canvas/src/services/canvasApi.ts (L483-495 g15Ops), src/lib/gateCatalog.ts (L53-58 deriveGateId 正则词汇——gate 白名单同源)</read_first>
  <action>
  1. g15Bridge.ts:dispatchG15Op 参数对象加 `gate?: string`;函数内 `const gateId = gate ?? 'p11c-gate'` 并传递到 review 桥载荷(具体字段名以函数体内既有 review 调用形状为准——approve 目标处);文件头注释补 G16 消费注记(D-11:同一 bridge action,不同 gate 目标)。
  2. g15-ops.ts:schema 加 `gate: z.string().regex(/^p\d+[a-z0-9]*-gate$/).optional()`(注释:白名单式——任意 gate 字符串不接受,Tampering 防线);解构 + 透传 dispatchG15Op;writeback 入队 payload 带 gate(旧行无字段回放=缺省,注释钉)。
  3. canvasApi.ts:g15Ops 加尾参 `gate?: string` 进 body。
  4. G15 既有调用点零改动(grep g15Ops 消费方确认只加可选参数)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npm run lint 2>&1 | tail -3; grep -n "gate" src/routes/canvas/v2/g15-ops.ts | head -5</automated>
  </verify>
  <acceptance_criteria>
    - 三文件 gate 参数齐(route zod 白名单正则字面在);G15TriagePanel.tsx git diff 为空(既有消费零改动)
    - `grep -c "p11c-gate" src/lib/g15Bridge.ts` ≥ 2(缺省 + 注释)
    - `npm run lint`(root tsc --noEmit)exit 0
  </acceptance_criteria>
  <done>waive 通道一桥两用:G15 行为不变,G16 经 gate:'p10c-gate' 复用全链(事务/队列/重放)。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: voiceAuditStore + useVoiceKeyboard + vitest</name>
  <files>packages/infinite-canvas/src/components/g16/voiceAuditStore.ts, packages/infinite-canvas/src/components/g16/useVoiceKeyboard.ts, packages/infinite-canvas/src/components/g16/__tests__/voiceAuditStore.test.ts, packages/infinite-canvas/src/components/g16/__tests__/useVoiceKeyboard.test.ts</files>
  <read_first>packages/infinite-canvas/src/components/g15/g15TriageStore.ts (全文——seam/fixture/Set 状态机/markRows/unmarkRows 同构母本), packages/infinite-canvas/src/components/variants/useWallKeyboard.ts (全文——hook 范式), .planning/phases/56-creative-visualization/56-RESEARCH.md (§G16 记录形状 + §Pitfalls 3/4/6)</read_first>
  <behavior>
  - Test: fixture 源 loadClips → rows 按 p10c 形状齐字段(shot_id/transcript/verdict/similarity);verdict 域外值(如 'ok')过滤
  - Test: 真实源 seam:graph 含 voice-audit 节点(raw clips 3 条)→ deriveClips 得 3 条;raw 无可识别列表 → 空数组 + console.warn 一次,不 throw;graph null → 空
  - Test: nextPending(当前 0,rows 含已豁免 1) → 跳过豁免指向下一条;末尾无可审 → 返回 null(连播终止)
  - Test: markWaived(ids) 乐观 + unwaive 回滚(Set/rowState 状态机与 G15 同断言风格)
  - Test: useVoiceKeyboard:空格→onTogglePlay 且 preventDefault;→ onNext;← onPrev;Esc onClose;'1'/'Enter' 不触发任何 handler(键位不占用断言);enabled=false 零触发;卸载移除 listener
  </behavior>
  <action>
  1. voiceAuditStore.ts:zustand;`VoiceClip {id, shotId, path, transcript, verdict:'pass'|'warn'|'fail', similarity?:number, reason?, speaker?, dims?}`(verdict 归一小写走 scoreVocabulary 思想——内部 toVerdict 守卫);`VoiceAuditSource {loadClips(): Promise<VoiceClip[]>}` + fixture(形状对齐 p10c,含 PASS/WARN/FAIL/低相似/缺 transcript 多态样本 ≥5 条);真实源 `graphVoiceAuditSource(graph, raw)`(防御式:键序 clips/findings;str/num/isRecord 守卫);state {open, rows, selected:Set, rowState, currentIndex, autoPlay, source} + actions(setOpen 懒 load/setSource/load/toggle/selectAll/clear/markWaived/unmark/setCurrentIndex/setAutoPlay/nextPending)。
  2. useVoiceKeyboard.ts:useWallKeyboard 范式,handlers {onTogglePlay, onNext, onPrev, onClose};键位 空格(preventDefault)/ArrowRight/ArrowLeft/Escape;**不注册**数字与 Enter。
  3. 两测试文件照 <behavior>(fixture 断言/graph 派生/连播推进/乐观回滚/键盘六组)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/components/g16/__tests__/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - vitest exit 0(store 四组 + 键盘六组)
    - voiceAuditStore.ts 含 `'voice-audit'`、`'clips'`、`nextPending`;不含 `'requeue'`(G16 无重渲语义)
    - useVoiceKeyboard.ts 不含 `'1' && '9'` 数字键分支(grep `>= '1'` 0 命中)
    - `npx tsc -b` exit 0
  </acceptance_criteria>
  <done>听审数据/状态机/连播推进/键盘流四件全测——工作台 UI 只剩组装。</done>
</task>

<task type="auto">
  <name>Task 3: G16VoiceWorkbench 工作台 + 双入口 + FlowCanvas 挂载</name>
  <files>packages/infinite-canvas/src/components/g16/G16VoiceWorkbench.tsx, packages/infinite-canvas/src/components/FlowCanvas.tsx, packages/infinite-canvas/src/components/gate/GateCenterPanel.tsx</files>
  <read_first>packages/infinite-canvas/src/components/g15/G15TriagePanel.tsx (全文——列表行/sticky 动作条/乐观/toast/Row 组件,布局母本), packages/infinite-canvas/src/components/theater/TheaterShell.tsx (56-02), packages/infinite-canvas/src/components/g16/voiceAuditStore.ts + useVoiceKeyboard.ts (Task 2 产物), packages/infinite-canvas/src/utils/audioPeaks.ts + transcriptAlign.ts (56-02), packages/infinite-canvas/src/utils/mediaUrl.ts (resolveMediaUrl), packages/infinite-canvas/src/components/gate/GateCenterPanel.tsx (gate 行动作区结构)</read_first>
  <action>
  1. G16VoiceWorkbench.tsx:
     - 开态 useVoiceAuditStore;TheaterShell(title『配音听审 · {projectId/episodesId 可取名}』+ subtitle 计数;onClose=close;headerExtra:连播 toggle + 重听)。
     - 左列表:行 = 勾选 checkbox + shot_id mono + 说话人(clip.speaker ?? '—')+ similarity mono(Math.round(sim*100))+ verdict chip(三色弱底)+ 已豁免标;onClick 行 = 选中该条(currentIndex + 懒 resolvePeaks);当前播放行 accent 处理;空态卡(UI-SPEC 词表)。
     - 右双轨:容器 ref 内 canvas 波形(72px,devicePixelRatio;resolvePeaks 异步返回后重绘;kind pseudo 时注记『伪波形』)+ 句列表(splitSentences(transcript)→evenAlign(durationSec——audio duration onLoadedMetadata 后重排)+ 行 onClick seek 比例)+ 共享光标 absolute 线贯穿(位置 = currentTime/duration × 容器宽)+ 近似对齐注记常显(10px tertiary,双轨区右上)。
     - 音频引擎:单 `<audio hidden ref>` src=resolveMediaUrl(clip.path);onended → autoPlay 且 store.nextPending!=null → 换源续播,否则停;onTimeUpdate setCurrentTime;播放/暂停/重听(重听=seek 0+play)。
     - 底部动作条:已选 N / 全选 / 清空 / 重听 / 连播 toggle / 批量豁免;runWaive 乐观 markWaived → g15Ops(projectId, episodesId, 'waive', shotIds, 'p10c-gate') → toast 两式(G15 文法)。
     - useVoiceKeyboard 接线(空格=togglePlay、→=nextPending 选中、←=prev、Esc=close);Esc 与 TheaterShell 背板/✕ 三通道。
  2. FlowCanvas.tsx:`<G16VoiceWorkbench />` 挂 VariantWall/G15 同级 overlay 区。
  3. GateCenterPanel.tsx:p10c-gate 行动作区加『打开听审工作台』按钮(theaterBtnStyle 弱化版/既有行按钮风格)→ useVoiceAuditStore.getState().setOpen(true)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx tsc -b 2>&1 | tail -3 && npm test 2>&1 | tail -6 && grep -c "G16VoiceWorkbench" src/components/FlowCanvas.tsx src/components/gate/GateCenterPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - FlowCanvas 挂载 ≥1;GateCenterPanel 含『打开听审工作台』与 p10c 词汇
    - G16VoiceWorkbench.tsx 含 `TheaterShell`、`resolvePeaks`、`evenAlign`、`sentenceAt`、`'p10c-gate'`、`配音听审`、`批量豁免`、`已豁免`、`连播`
    - hex 扫描该文件 0 裸命中(口径:剥除 `var(--cv-…, #fallback)` 段后 grep——fallback 写法是 house style 允许)
    - `npm test` 全绿 + `npx tsc -b` exit 0;G15TriagePanel.tsx git diff 为空
  </acceptance_criteria>
  <done>VIZ-03 端到端:听审列表→双轨对照→逐条/连播试听→批量豁免回写;G16 在画布内闭环(替代 indextts25 测试页凑合)。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 工作台 → POST /api/canvas/v2/g15-ops(gate=p10c-gate) | 豁免指令回写 kmc gate(经桥+重放队列) |
| voice-audit raw 袋(khs 同步) → 工作台渲染 | 产线结构化数据,防御式消费 |
| 音频媒体 fetch/decode | 产线 wav,只读 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-05-01 | Elevation | gate 参数注入(任意 gate 越权豁免他门) | mitigate | route zod 白名单正则 ^p\d+[a-z0-9]*-gate$(gateCatalog 同源词汇);桥内缺省 + 不做任何通配;测试覆盖非法值 400(zod 拒) |
| T-56-05-02 | Tampering | clips raw 畸形(缺 verdict/负 similarity) | mitigate | toVerdict 域过滤 + num 守卫钳制;域外行丢弃(store 测试断言) |
| T-56-05-03 | DoS | 连播无限循环(单条 loop?) | mitigate | onended 推进只向前;nextPending null 即停;audio 不设 loop |
| T-56-05-04 | Repudiation | 豁免无痕迹 | accept | 桥侧既有 review-outcomes/approve-with-comment 审计(53-07 建成,不重复) |
| T-56-05-05 | DoS | 波形解码重复触发 | mitigate | resolvePeaks 模块缓存(56-02);选中才懒解码 |
| T-56-05-SC | Tampering | package installs | accept | 零新包 |

**结论:唯一跨信任边界写操作是豁免回写,白名单+既有事务桥+重放队列三层兜底;无 high 级未缓解威胁。**
</threat_model>

<verification>
- `npx vitest run src/components/g16/__tests__/` exit 0
- `npm test` 全绿 + 双侧 tsc 过(root + package)
- G15TriagePanel.tsx / g15 既有测试 git diff 与结果零回归
- 听审走查(双轨光标跟手/连播/豁免回写真机)归 HUMAN-UAT;真机一次 p10c 豁免回写核对入 VERIFICATION
</verification>

<success_criteria>
- ROADMAP 成功标准 3 全量成立:波形+转写对照(qwen-ear verdict)+ 逐条试听 + 批量豁免 + 状态回写正确(桥白名单/乐观回滚/重放兼容)
- D-09/D-10/D-11/D-12 落地;UI-SPEC §5 规格 + Do-Not-Regress 7/8/12 守住
</success_criteria>

<output>
Create `.planning/phases/56-creative-visualization/56-05-SUMMARY.md` when done. 记录:gate 白名单最终正则、NodeDetailPanel 入口 non-goal 决定、onTimeUpdate vs rAF 终裁理由、fixture 样本分布、真机豁免回写验证结果(如已跑)。
</output>
