---
phase: 56-creative-visualization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/infinite-canvas/src/components/theater/TheaterShell.tsx
  - packages/infinite-canvas/src/utils/audioPeaks.ts
  - packages/infinite-canvas/src/utils/__tests__/audioPeaks.test.ts
  - packages/infinite-canvas/src/utils/transcriptAlign.ts
  - packages/infinite-canvas/src/utils/__tests__/transcriptAlign.test.ts
autonomous: true
requirements: [VIZ-02, VIZ-03]
must_haves:
  truths:
    - "TheaterShell = 剧场家族最小公共壳:fixed inset-0 zIndex:40 + chrome.lightboxOverlay + blur(2px) + 背板点击关 + 头栏(bg.panel+border.default+标题+控件位)+ btnStyle/closeBtnStyle 词汇——全部从 VariantWall L285-339/L519-531 抽出,不新造样式"
    - "53 VariantWall 零改动(不迁移到 Shell——Do-Not-Regress 6;Shell 文件头注释标注未来收编点)"
    - "audioPeaks.computePeaks:注入 AudioContext 构造器,decodeAudioData → 双声道合并 → 分桶峰值(每桶 max|RMS|);pseudoPeaks = AssetCardNode.pseudoWaveform 原样平移(FNV 哈希);resolvePeaks 编排 fetch→compute→缓存→失败退 pseudo,never-throws"
    - "transcriptAlign.splitSentences(。!?！？\n 切分保标点,空→['(无转写)']) + evenAlign(按字符数加权等时分布) + sentenceAt(二分查找)——三个纯函数"
    - "零 React import(audioPeaks/transcriptAlign 照 wallTransport 纯模块纪律);零新依赖(否决 wavesurfer——UI-SPEC Registry Safety)"
  artifacts:
    - path: "packages/infinite-canvas/src/components/theater/TheaterShell.tsx"
      provides: "TheaterShell 组件({title, subtitle?, onClose, headerExtra?, children})——56-04 组视图与 56-05 G16 共同消费"
      contains: "TheaterShell"
    - path: "packages/infinite-canvas/src/utils/audioPeaks.ts"
      provides: "computePeaks/pseudoPeaks/resolvePeaks 纯模块(注入式可单测)+ peaksCache"
      contains: "resolvePeaks"
    - path: "packages/infinite-canvas/src/utils/transcriptAlign.ts"
      provides: "splitSentences/evenAlign/sentenceAt 纯函数"
      contains: "evenAlign"
  key_links:
    - from: "packages/infinite-canvas/src/components/theater/TheaterShell.tsx"
      to: "packages/infinite-canvas/src/theme/catppuccin.ts"
      via: "theme.chrome.lightboxOverlay / theme.bg.panel / theme.border.default / theme.text.*——Shell 唯一取色源"
      pattern: "lightboxOverlay"
---

<objective>
建剧场家族与音频引擎的地基三件:① `TheaterShell.tsx` 最小公共壳(56-04 组视图剧场与 56-05 G16 工作台的共同容器,从 VariantWall 抽出而墙本体不动);② `audioPeaks.ts` 波形引擎(decodeAudioData 真实峰 + 伪波形兜底 + 缓存,注入式可单测——G16 双轨与音色试听完整播放器共用);③ `transcriptAlign.ts` 转写分句等时对齐三纯函数(G16 下轨)。

本 plan 无挂载、无业务接线(消费者在 Wave 2);三个模块全部有单测。

输出:1 个新组件 + 2 个新纯模块 + 2 个测试文件。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/56-creative-visualization/56-CONTEXT.md
@.planning/phases/56-creative-visualization/56-RESEARCH.md
@.planning/phases/56-creative-visualization/56-PATTERNS.md
@.planning/phases/56-creative-visualization/56-UI-SPEC.md

**COORD-01:本 plan 零 khs 文件、零 root 后端文件。执行者不得修改 files_modified 之外的文件(尤其不得动 VariantWall.tsx)。**

<design_decisions>
<!-- 前端设计纪律(/frontend-design):Shell 是「家族语法固化」不是新设计——视觉决策已在 UI-SPEC 定死 -->

**TheaterShell token(全部既有,禁新造):**
- 壳底 `theme.chrome.lightboxOverlay`(catppuccin.ts L196)+ `backdropFilter: 'blur(2px)'`
- 头栏 `theme.bg.panel` + `borderBottom: 1px solid theme.border.default`;头栏上下 padding **10px**、左右 16px(剧场家族 grandfathered 节奏,UI-SPEC Spacing 例外条款)
- 标题 `theme.text.primary` 13px/700(剧场头栏既有字级);副标 `theme.text.secondary` 11px;ID 类 `--cv-font-mono`
- 控件按钮 btnStyle/closeBtnStyle 从 VariantWall L519-531 原样抄出(export 供消费者复用)
- 头栏装饰 emoji 位保留(可选 prop icon?——消费者传 🎞/🎤 等家族词汇)

**布局契约:** Shell = flex column;头栏固定;children 区 `flex:1 minHeight:0`(消费者全权布局);Esc 关闭**不由 Shell 挂**(各消费者键盘 hook 统一处理,避免双 listener)——Shell 只承接背板点击关与 ✕。

**波形视觉(G16/音色共用,消费者实现 canvas 绘制,本 plan 只供数据):** 已播/未播由消费者按 currentTime 分割;peaks 数组 0-1 归一(模块保证 max 桶=1 上界钳制)。
</design_decisions>

<interfaces>
VariantWall 壳段(抽出源,只读):L285-294 壳 div(fixed/inset 0/zIndex 40/lightboxOverlay/blur/背板 onClick `e.target===e.currentTarget` 判定)、L296-339 头栏结构(左 icon+标题+副标+chip / 右控件+✕)、L519-531 btnStyle/closeBtnStyle。

wallTransport 纯模块纪律(照抄):文件头 docblock 说明设计、零 React import、依赖经参数注入(HTMLVideoElementLike 先例→AudioContextLike)、`tickForTest` 式测试出口(computePeaks 接受已解码 Float32Array 的重载供 node 环境测)。

pseudoWaveform 源:AssetCardNode.tsx:488-503(FNV-1a 哈希 + xorshift 三拍 + 0.15-1.0 域)——**原样平移**(改 seed 参数化),AssetCardNode 本体不动(WaveformCover 继续用自家副本,注记未来收编,不在本 plan)。

p10c transcript 现状:无时间戳(56-RESEARCH §G16)→ evenAlign 是**近似对齐**,消费者须展示 UI-SPEC Copywriting 的近似注记——本模块头注释同步声明,防后人误当精确对齐用。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TheaterShell.tsx 剧场家族公共壳</name>
  <files>packages/infinite-canvas/src/components/theater/TheaterShell.tsx</files>
  <read_first>packages/infinite-canvas/src/components/variants/VariantWall.tsx (L285-339 壳+头栏段、L519-531 按钮样式——抽出源,逐行对照), packages/infinite-canvas/src/theme/catppuccin.ts (theme.chrome/bg/border/text 段)</read_first>
  <action>
  1. 新建 `components/theater/TheaterShell.tsx`(新目录 theater/):props `{ title: string; subtitle?: React.ReactNode; icon?: string; onClose: () => void; headerExtra?: React.ReactNode; children: React.ReactNode }`。
  2. 结构照 VariantWall 抽出:壳 div(data-testid="theater-shell",fixed inset-0 zIndex:40,lightboxOverlay+blur,flex column,onClick 背板判定关)+ 头栏(左:icon span(text.primary)+title(13/700)+subtitle;右:headerExtra 槽 + ✕ closeBtnStyle)+ children 区(flex:1 minHeight:0)。
  3. `export function theaterBtnStyle(active: boolean)` 与 `export const theaterCloseBtnStyle`——VariantWall L519-531 同值抄出(消费者统一从 Shell import,不各自复写)。
  4. 文件头注释:①53 VariantWall 不迁移(Do-Not-Regress;未来 ≥3 消费者稳定后收编);②Esc 由消费者键盘 hook 承担的理由;③剧场家族 grandfathered 节奏(10px 头栏上下)指向 UI-SPEC。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx tsc -b 2>&1 | tail -3 && grep -c "lightboxOverlay" src/components/theater/TheaterShell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TheaterShell.tsx 含 `lightboxOverlay`、`backdropFilter`、`e.target === e.currentTarget`(背板关判定)与 export 的 theaterBtnStyle/theaterCloseBtnStyle
    - `git diff --stat packages/infinite-canvas/src/components/variants/VariantWall.tsx` 为空(墙零改动)
    - `npx tsc -b` exit 0
  </acceptance_criteria>
  <done>剧场家族公共壳就位:56-04/56-05 有统一容器,家族视觉语言一处固化。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: audioPeaks.ts 波形引擎 + vitest</name>
  <files>packages/infinite-canvas/src/utils/audioPeaks.ts, packages/infinite-canvas/src/utils/__tests__/audioPeaks.test.ts</files>
  <read_first>packages/infinite-canvas/src/components/variants/wallTransport.ts (纯模块+注入接口+never-throws 纪律全文——本模块的直接范本), packages/infinite-canvas/src/components/nodes/AssetCardNode.tsx (L488-503 pseudoWaveform——平移源), packages/infinite-canvas/src/utils/mediaUrl.ts (L89-101 resolveMediaUrl——resolvePeaks 的 url 入参约定)</read_first>
  <behavior>
  - Test: computePeaksFromSamples(正弦 Float32Array 采样, buckets=24) → 长度 24、每值 0-1、峰值桶≈1(容差 0.05)
  - Test: 双声道合并(左右差异 data)→ 峰值取两声道 max(传入 [L,R] 数组形态或 AudioBufferLike)
  - Test: pseudoPeaks('seed-x', 24) 与 AssetCardNode 同 seed 确定性:同 seed 两次调用结果逐值相等;值域 [0.15,1.0]
  - Test: resolvePeaks:注入 fake fetch+fake AudioContext 成功 → kind:'real' + peaks;fetch reject → kind:'pseudo' + pseudoPeaks 同 seed 结果;decode throw → kind:'pseudo'
  - Test: 缓存:同 url 第二次 resolvePeaks 不再调 fake fetch(计数不增)
  - Test: 空/短音频(samples < buckets)→ 每桶至少 1 样本,不 throw;全零采样 → 全 0 数组(不除零)
  </behavior>
  <action>
  1. 新建 `utils/audioPeaks.ts`:零 React import。导出:
     - `interface AudioBufferLike { numberOfChannels: number; length: number; getChannelData(ch: number): Float32Array }`(真实 AudioBuffer 天然满足——wallTransport HTMLVideoElementLike 同款)
     - `computePeaksFromSamples(channels: Float32Array[], buckets: number): number[]`(分桶取 max(|sample|),末尾整体归一:全数除以全局 max(0 保护);buckets<=0 → [])
     - `pseudoPeaks(seed: string, buckets: number): number[]`(FNV-1a + xorshift,0.15 + 0.85 域——AssetCardNode 伪波形同算法)
     - `resolvePeaks(url: string, opts?: { buckets?: number; fetchImpl?: typeof fetch; decode?: (buf: ArrayBuffer) => Promise<AudioBufferLike> }): Promise<{ kind: 'real' | 'pseudo'; peaks: number[] }>`:默认 buckets 96(G16 波形密度);模块级 `Map<string, {kind,peaks}>` 缓存(容量上界 128,超界清最旧——简单 FIFO);任何异常(网络/解码/类型)→ pseudo,never-throws;真实环境 decode 缺省走 lazy `new AudioContext()`(仅在 browser 且未注入时)。
  2. 文件头注释:否决 wavesurfer 依据(UI-SPEC Registry Safety);decodeAudioData 的 browser 兼容注记;peaks 归一上界契约(max=1)。
  3. 测试照 <behavior> 六组(fake 注入,node 环境零 AudioContext 依赖)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/utils/__tests__/audioPeaks.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - vitest exit 0(六组:真峰/双声道/伪波形确定性/resolve 三态/缓存/边界)
    - `grep -c "from 'react'" packages/infinite-canvas/src/utils/audioPeaks.ts` = 0 且 `grep -c "wavesurfer\|peaks.js" packages/infinite-canvas/package.json` = 0(零新依赖)
    - audioPeaks.ts 含 `AudioBufferLike` 与 `never-throws` 注释字样
    - `npx tsc -b` exit 0
  </acceptance_criteria>
  <done>波形数据引擎就位:G16 双轨与音色完整播放器同源消费,真实峰/伪兜底/缓存行为全有测试。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: transcriptAlign.ts 分句等时对齐 + vitest</name>
  <files>packages/infinite-canvas/src/utils/transcriptAlign.ts, packages/infinite-canvas/src/utils/__tests__/transcriptAlign.test.ts</files>
  <read_first>packages/infinite-canvas/src/components/StoryboardTimeline.tsx (grep 定位 shotKey/frame 纯函数段——纯字符串/结构化转换纪律参照), packages/infinite-canvas/src/store/g15/g15TriageStore.ts (classifyG15 纯特征→结构化输出先例)</read_first>
  <behavior>
  - Test: splitSentences('你好。今天天气如何?走吧!') = ['你好。','今天天气如何?','走吧!'](标点保留在句尾)
  - Test: splitSentences('第一句\n第二句') 换行切分;splitSentences('') / splitSentences(undefined as any) → ['(无转写)'] 兜底
  - Test: evenAlign(['a。','bb。','c。'], 9) → 三段互不重叠、首段 start=0、末段 end=9、按字符数加权(bb 段时长 > a 段)
  - Test: evenAlign(单句, 4.5) = [{start:0,end:4.5}];duration<=0 → end=0(不产生 NaN)
  - Test: sentenceAt(align, t) 命中正确段;t=0 → 第 0 段;t>end → 末段;t<0 → 第 0 段(钳制)
  </behavior>
  <action>
  1. 新建 `utils/transcriptAlign.ts`:零 React import;三个导出纯函数:
     - `splitSentences(text: string | null | undefined): string[]`——正则按 。!?!?\n 切分保标点;空串/全空白 → `['(无转写)']`;过滤空白段。
     - `evenAlign(sentences: string[], durationSec: number): Array<{ start: number; end: number; text: string }>`——权重 = max(1, 句 charCount);总权重归一到 duration;duration<=0 → 全段 end=start=0。
     - `sentenceAt(align, t): number`——线性/二分扫描返回 index(空数组 -1;越界钳制首末)。
  2. 文件头注释:**近似对齐契约**——p10c transcript 无时间戳(56-RESEARCH §G16),等时分布是展示近似;消费者必须展示 UI-SPEC 的「分句按等时近似对齐」注记;未来 khs 若透传时间戳,在此模块加 timed 分支,消费者无感。
  3. 测试照 <behavior> 五组。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/utils/__tests__/transcriptAlign.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - vitest exit 0(五组断言齐,含 NaN 防护)
    - `grep -c "from 'react'" packages/infinite-canvas/src/utils/transcriptAlign.ts` = 0
    - transcriptAlign.ts 含字面 `(无转写)` 与 `近似` 注释
    - `npx tsc -b` exit 0
  </acceptance_criteria>
  <done>G16 下轨的分句/对齐/查找三函数就位,近似语义在模块与测试双锁定。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 音频文件 URL(fetch 对象) → peaks 数据 | 产线媒体文件,只读取字节 |
| Shell children | 各消费者渲染内容(信任域内组件) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-02-01 | DoS | 大文件 decodeAudioData 阻塞主线程 | mitigate | 仅消费者选中条目时懒解码(消费侧契约,Shell/引擎头注释声明);peaks 缓存避免重复 decode;单文件数秒 wav 实测量级可接受 |
| T-56-02-02 | DoS | 缓存无界增长 | mitigate | Map 容量上界 128 FIFO(测试覆盖行为由实现注释+code review 保证,不设专项断言) |
| T-56-02-03 | Tampering | 跨源音频 URL | accept | resolveMediaUrl 上游已限(/oss/ 与 /local-file 白名单代理);引擎只消费调用方传入的已解析 URL |
| T-56-02-04 | DoS | AudioContext 实例泄漏(browser) | mitigate | 模块级单例 AudioContext 复用(不每调用新建);node 环境不创建 |
| T-56-02-SC | Tampering | package installs | accept | 零新包 |

**结论:纯引擎+壳;媒体读取只经既有白名单通道;无 high 级未缓解威胁。**
</threat_model>

<verification>
- `cd packages/infinite-canvas && npx vitest run src/utils/__tests__/audioPeaks.test.ts src/utils/__tests__/transcriptAlign.test.ts` exit 0
- `npm test` 全绿 + `npx tsc -b` exit 0
- `git diff --stat` 确认 VariantWall.tsx / AssetCardNode.tsx 零改动
</verification>

<success_criteria>
- 剧场家族壳 + 波形/分句引擎三件落地,56-04/56-05 可并行开工(Wave 2 解锁)
- 零新依赖、VariantWall 零改动、纯模块纪律(React import 0)全达标
</success_criteria>

<output>
Create `.planning/phases/56-creative-visualization/56-02-SUMMARY.md` when done. 记录:TheaterShell props 最终签名、audioPeaks 缓存策略与 buckets 默认值裁定、pseudoWaveform 平移注记(AssetCardNode 收编留待未来)。
</output>
