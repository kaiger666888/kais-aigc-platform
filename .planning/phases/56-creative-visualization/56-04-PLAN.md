---
phase: 56-creative-visualization
plan: 04
type: execute
wave: 2
depends_on: ["56-01", "56-02"]
files_modified:
  - packages/infinite-canvas/src/components/theater/theaterStore.ts
  - packages/infinite-canvas/src/components/theater/groupMembership.ts
  - packages/infinite-canvas/src/components/theater/__tests__/groupMembership.test.ts
  - packages/infinite-canvas/src/components/theater/TurnaroundView.tsx
  - packages/infinite-canvas/src/components/theater/SceneGallery.tsx
  - packages/infinite-canvas/src/components/theater/VoiceProfileBoard.tsx
  - packages/infinite-canvas/src/components/theater/GroupViewTheater.tsx
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
  - packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx
autonomous: true
requirements: [VIZ-02]
must_haves:
  truths:
    - "组视图 = 全屏组视图剧场(TheaterShell 消费,53 变体墙同族):双击 character/scene/voice_profile 类资产节点(含变体组节点)开剧场;其余节点双击仍开详情面板(REGEN-04 零回归);剧场内『节点详情』按钮开右面板保闭环(D-05 + planner 终裁)"
    - "组员推导数据驱动(D-05 前置):变体组 → graph.variantGroups;character/scene → raw characterId/scene_id/label-base 同族(global 域);voice → voice_profile + 同 characterId voice_print——groupMembership 纯函数 + 单测,识别不了显示『未找到同族资产』空态"
    - "turnaround 布局(D-06):2×2 网格(buildTurnaroundGrid 语义平移:byAngle + 空格占位)+ 中央悬浮 chip(参考图 48px + 角色名 + 服装套系 + 一致性分 mono 透传展示——无字段不渲染,不计算 D-16)+ 同步缩放(四格共享 scale 1.0-4.0,wheel 任一格缩全格、双击复位、头栏 ＋/－/复位 + 当前倍率)"
    - "场景画廊(D-07):主图 contain 大图 + 当前视角 chip + 底部 64px 缩略行(横滚/选中 select 描边);视角集数据驱动(p07 views dict 有啥列啥,中文 viewLabel 回退)——不硬编码四视角"
    - "音色试听(D-08):左列声纹卡 mini ▶(点即播再点停,不进面板;同时至多一条在播)+ 右侧完整播放器(audioPeaks 波形 canvas 72px + 时长 + 播放/暂停 + 可拖光标;DialogueRow 同构)"
    - "媒体 URL 一律 resolveMediaUrl;图片 404 兜底 = 剧场家族 emoji 占位(可复用 healThumb,不改其本体)"
  artifacts:
    - path: "packages/infinite-canvas/src/components/theater/theaterStore.ts"
      provides: "组视图开关态 {kind:'turnaround'|'scene'|'voice', anchorId} | null + open/close(variantPickerStore 协议同款)"
      contains: "useTheaterStore"
    - path: "packages/infinite-canvas/src/components/theater/groupMembership.ts"
      provides: "theaterTargetOf(node, graph, raw) 判定 + deriveGroupMembers 纯函数"
      contains: "theaterTargetOf"
    - path: "packages/infinite-canvas/src/components/theater/GroupViewTheater.tsx"
      provides: "三布局容器(TheaterShell 消费 + 组解析 useMemo + Esc/背板关 + 节点详情按钮)"
      contains: "GroupViewTheater"
  key_links:
    - from: "packages/infinite-canvas/src/components/FlowCanvas.tsx"
      to: "packages/infinite-canvas/src/components/theater/theaterStore.ts"
      via: "onNodeDoubleClick 前置分支:theaterTargetOf 命中 → useTheaterStore.open + return;未命中原路径零改动"
      pattern: "theaterTargetOf"
    - from: "packages/infinite-canvas/src/components/theater/GroupViewTheater.tsx"
      to: "packages/infinite-canvas/src/components/theater/TheaterShell.tsx"
      via: "56-02 公共壳消费"
      pattern: "TheaterShell"
---

<objective>
落 VIZ-02 全屏组视图剧场:theaterStore 开关态 + groupMembership 纯推导 + 三布局组件(TurnaroundView 2×2 同步缩放 / SceneGallery 主图+缩略行 / VoiceProfileBoard mini+完整播放器)+ GroupViewTheater 容器 + FlowCanvas 双击路由改道 + NodeDetailPanel「组视图」次入口。

消费 56-02 的 TheaterShell 与 audioPeaks。完成后「角色 turnaround 四视图同屏对比 / 场景多视角画廊 / voice_profile 试听内嵌」三条成功标准成立。

输出:6 个新文件(含 1 测试)+ 2 个修改文件。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/56-creative-visualization/56-CONTEXT.md
@.planning/phases/56-creative-visualization/56-RESEARCH.md
@.planning/phases/56-creative-visualization/56-PATTERNS.md
@.planning/phases/56-creative-visualization/56-UI-SPEC.md

**COORD-01:本 plan 零 khs 文件、零 root 后端文件。VariantWall.tsx / healThumb.ts / useLod.ts 禁改。**

<design_decisions>
<!-- 前端设计纪律——UI-SPEC §4 全规格,签名元素 = 同步缩放,其余安静 -->

**Token:**
- 壳/头栏/按钮:TheaterShell(56-02)自带;标题 `{名称} · 组视图 · {N} 视图` / `{名称} · 音色试听 · {N} 条声纹`
- 网格卡:`theme.bg.card` + `theme.border.default`;选中/聚焦描边 `v3theme.signal.select`(1.5px)
- 中央 chip:`--cv-bg-panel` 90% 不透明(background rgba 拼 theme.bg.panel?——**用 theme.bg.panel 原值 + opacity 0.9 两层 div**,不手拼 rgba 新值)+ border.default;一致性分 `getScoreColor` + mono
- 视角/角度标签:`v3theme.modalityWeak.image` 弱底 + text.secondary(53 墙 chip 同款);视角名 viewLabel 中文
- 缩略行选中卡:select 描边;未选中 border.default;占位 emoji 🖼(家族词汇)

**同步缩放(签名元素)规格:**
- scale 单一 state,clamp [1.0, 4.0],步进 0.1;wheel `e.deltaY < 0 ? +0.1 : -0.1`(preventDefault;React onWheel passive 问题→用 ref + addEventListener non-passive 或 onWheel + { passive: false } 依仓内先例,核 FlowCanvas wheel 用法)
- 每格 `transform: scale(s)`;hover 格 transformOrigin 跟光标(`{x}% {y}%` 由 onMouseMove 算),其余格 `50% 50%`;transition:wheel 时无(跟手),按钮/复位 `120ms --cv-d-select` 同值
- 头栏控件:「同步缩放 ＋」「－」「复位」(theaterBtnStyle)+ 当前倍率 mono `{s.toFixed(1)}×`

**布局:**
- turnaround 网格:`grid-template-columns: repeat(2, minmax(280px, 1fr))` gap 12;中央 chip absolute 居中(pointer-events:auto 仅 chip——可点开参考图大图?**终裁:chip 纯展示**,降低范围)
- 场景画廊:主图区 flex:1 contain + 缩略行高 64px 横滚(卡 88×64)
- 音色:左列 `min(320px, 26vw)` 卡列 + 右完整播放器 flex:1
- 空态:居中卡 `--cv-bg-panel` + border + 32px 内距,文案『未找到同族资产』+『该节点没有可同屏对比的视图/声纹』

**文案(UI-SPEC Copywriting):**『打开组视图剧场』(tooltip)/『节点详情』/『同步缩放 ＋/－/复位』/『一致性 {N}』(tooltip『turnaround-ssim 透传,平台不计算』)/『声纹 {characterId}』/『{MM:SS}』/空态词表。
</design_decisions>

<interfaces>
TheaterShell(56-02 产物):props `{title, subtitle?, icon?, onClose, headerExtra?, children}` + theaterBtnStyle/theaterCloseBtnStyle。

FlowCanvas 双击位:L547-553 onNodeDoubleClick——**前置插入**:`const t = theaterTargetOf(node, graph, rawMap); if (t) { useTheaterStore.getState().open(t); return }`;原 navSkip/navHistory/setSelectedNode/setDetailNode 链保持在其后。挂载 `<GroupViewTheater />` 于 L1111 `<VariantWall />` 同级。node 参数是 RF node(data 袋)——theaterTargetOf 接受 {id, data} 形状即可(不依赖 V3 类型)。

groupMembership 词汇源:assetManagerData.ts L462-491(metaSub voice_profile/voice_print 判别 + characterId)+ L1383/1430 中文/emoji;变体组 variantOps.ts findVariantGroupForNode(L13)。turnaround 视角序(四宫格):['face_cu'?]——CharacterWardrobe buildTurnaroundGrid L86-92 的 byAngle 映射(近身面部/正面/侧面/背面,face_cu 优先)照抄其槽位语义:槽序 [正面 face_cu 优先, 侧面, 背面, 3/4 侧],空槽占位。

TurnaroundView 数据源:资产 raw 袋 views: string[](canvasAssetSchema asset 可选字段)/turnaround_path/单图 filePath——四视图缺 crops 时:整图(turnaround_path/filePath)占 1 格 + 其余空格占位(56-RESEARCH A-2 兜底)。

VoiceProfileBoard 播放先例:DialoguePanel.tsx L146-215(audio ref/progress 0-1/onSeek 比例点击 L167-177/fmt MM:SS L179-184);mini toggle = WaveformCover L452-462(new Audio + onended);波形 = audioPeaks.resolvePeaks + canvas 绘制(72px 高,devicePixelRatio 缩放;已播 audio 橙全不透明/未播 0.35;光标 1.5px #EDEEF1 via theme.select)。

healThumb(可选复用):variants/healThumb.ts createThumbHealer——组视图 img onError 走同三段;**不改其本体**,直接 import 消费。

Esc:GroupViewTheater 自挂 window keydown Esc(简单 useEffect,无数字键需求——不引 useWallKeyboard,该 hook 键位表不合)。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: theaterStore + groupMembership 纯推导 + vitest</name>
  <files>packages/infinite-canvas/src/components/theater/theaterStore.ts, packages/infinite-canvas/src/components/theater/groupMembership.ts, packages/infinite-canvas/src/components/theater/__tests__/groupMembership.test.ts</files>
  <read_first>packages/infinite-canvas/src/components/variants/variantPickerStore.ts (L13-28 开关态协议同款), packages/infinite-canvas/src/store/variantOps.ts (findVariantGroupForNode L13 + getVariantMemberNodes L27-34), packages/infinite-canvas/src/components/assetManager/assetManagerData.ts (L348-371 metaSub 词汇 + L462-491 voice 双形态判别 + characterId 字段——只读参照不 import), .planning/phases/56-creative-visualization/56-RESEARCH.md (§组视图数据)</read_first>
  <behavior>
  - Test: theaterTargetOf——raw assetType='character' → {kind:'turnaround', anchorId};'scene' → scene;metaSub 'voice_profile' → voice;变体组节点(groupId 归属) → {kind:'turnaround', anchorId}(组节点走变体组分支,锚=组或 winner)
  - Test: theaterTargetOf——video/audio/storyboard/普通 shot 资产 → null(双击原语义)
  - Test: deriveGroupMembers('turnaround')——同 characterId 的 global 域资产集;变体组 → variantNodeIds 成员
  - Test: deriveGroupMembers('scene')——同 scene_id 资产集(views dict/多图任一形态)
  - Test: deriveGroupMembers('voice')——voice_profile 锚 + 同 characterId voice_print 集
  - Test: 孤节点(无同族) → 空数组;graph null → 空数组不 throw
  </behavior>
  <action>
  1. theaterStore.ts:zustand `create`(variantPickerStore 同款):`group: { kind: TheaterKind; anchorId: string } | null` + `open(g)` + `close()`。
  2. groupMembership.ts:手写 interface(不 import root/assetManagerData——P8 纪律,metaSub 词汇手写镜像);`theaterTargetOf(node: {id; data?: Record<string,unknown>}, graph, raw): TheaterTarget | null`(判定序:变体组归属 > assetType character/scene > metaSub voice_profile;守卫 str());`deriveGroupMembers(kind, anchorId, graph, raw): MemberAsset[]`(MemberAsset = {nodeId, label, filePath?, thumbnailUrl?, views?, viewAngle?, characterId?, metaSub?}——从 V3 asset + raw 袋提取,num/str 守卫照 VariantWall L94-99)。
  3. 四宫格槽位序函数 `turnaroundSlots(members)`(buildTurnaroundGrid 语义平移:face_cu 优先正面槽,次 side/back/three_quarter,空槽占位条目)。
  4. 测试照 <behavior> 七组(fixture graph 工厂内联)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/components/theater/__tests__/groupMembership.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - vitest exit 0(七组断言)
    - `grep -c "from 'react'" packages/infinite-canvas/src/components/theater/groupMembership.ts` = 0(纯模块;store 文件允许 zustand)
    - groupMembership.ts 含 `'voice_profile'`、`'character'`、`'scene'`、`variantGroups`(三链齐)
    - `npx tsc -b` exit 0
  </acceptance_criteria>
  <done>组视图的「谁能开剧场/开哪个布局/组员是谁」三问成纯函数,数据驱动 + 空态兜底有测。</done>
</task>

<task type="auto">
  <name>Task 2: TurnaroundView(2×2 同步缩放)+ SceneGallery + VoiceProfileBoard 三布局</name>
  <files>packages/infinite-canvas/src/components/theater/TurnaroundView.tsx, packages/infinite-canvas/src/components/theater/SceneGallery.tsx, packages/infinite-canvas/src/components/theater/VoiceProfileBoard.tsx</files>
  <read_first>packages/infinite-canvas/src/components/assetManager/CharacterWardrobe.tsx (L86-92 buildTurnaroundGrid + L546 turnaroundGrid 消费——四宫格语义), packages/infinite-canvas/src/components/variants/VariantWall.tsx (L342-401 N-up 格/L404-449 胶片条/ThumbOrPlaceholder L500-517 emoji 占位), packages/infinite-canvas/src/components/assetManager/DialoguePanel.tsx (L146-215 完整播放器), packages/infinite-canvas/src/utils/audioPeaks.ts (56-02 产物——resolvePeaks), packages/infinite-canvas/src/utils/scoreVocabulary.ts (viewLabel), packages/infinite-canvas/src/components/theater/TheaterShell.tsx (56-02 产物)</read_first>
  <action>
  1. TurnaroundView.tsx:props `{members, center: {refUrl?, name, costumeName?, consistency?}}`——2×2 grid;格 = 图(resolveMediaUrl(filePath/thumbnailUrl),onError healThumb 可选消费→emoji 🖼 占位)+ 底部角度标签(viewLabel,modalityWeak.image 弱底);同步缩放按 <design_decisions> 规格(scale state + wheel + origin + 头栏控件经 headerExtra 由 GroupViewTheater 传 callback——**终裁:scale state 提在 GroupViewTheater,TurnaroundView 受控** `{scale, onScaleChange}`,控件渲染在剧场头栏);中央 chip(参考图 48px + 名称 + 套系 + 一致性分 consistency!=null 才渲染 + tooltip 透传注)。
  2. SceneGallery.tsx:props `{views: Array<{key, label, url}>}`(GroupViewTheater 从 members 的 views dict/多图聚合,key 数据驱动)——主图区(当前 url contain + 右上视角 chip)+ 缩略行(88×64 卡:img/占位 + viewLabel(key) + 选中 select 描边;onClick 切主图,120ms 淡切 opacity);单视图时缩略行不渲染。
  3. VoiceProfileBoard.tsx:props `{profiles: MemberAsset[]}`——左列卡(角色名 + characterId mono + 时长 + mini ▶:new Audio(url) toggle,播放中 ⏸;点击新卡先停旧卡——模块级单 audio 引用);右侧完整播放器(audio ref + progress + onSeek 比例点击 + MM:SS + 波形 canvas 72px:resolvePeaks(url) → useEffect 绘制,currentTime 驱动已播分割 + 1.5px 光标线;devicePixelRatio 适配;kind==='pseudo' 时角落 10px 注记『伪波形』)。
  4. 三组件全部 inline style + theme token;禁新 hex(grep 自检);`<button>` 承接一切交互。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx tsc -b 2>&1 | tail -3 && for f in src/components/theater/TurnaroundView.tsx src/components/theater/SceneGallery.tsx src/components/theater/VoiceProfileBoard.tsx; do sed 's/var([^)]*)//g' "$f" | grep -nE "#[0-9A-Fa-f]{6}" && echo "FAIL $f"; done; echo hex-scan-done</automated>
  </verify>
  <acceptance_criteria>
    - 三文件 tsc 过;hex 扫描三文件 0 命中——**口径:先剥除 `var(--cv-…, #fallback)` 整段后 grep**(仓内 NodeBadges 等既有 `var(--cv-bg-overlay, #1E2128)` fallback 写法是允许的 house style,不算新造色值;裸 hex 才 FAIL)
    - TurnaroundView 含 `scale`、`clamp`、`transformOrigin`、`4`(上界字面);SceneGallery 含 `viewLabel`;VoiceProfileBoard 含 `resolvePeaks`、`new Audio`
    - 三文件均含 `resolveMediaUrl`
    - `npx tsc -b` exit 0
  </acceptance_criteria>
  <done>三布局组件就位:同步缩放签名元素、数据驱动视角画廊、两级试听全部按 UI-SPEC 规格。</done>
</task>

<task type="auto">
  <name>Task 3: GroupViewTheater 容器 + FlowCanvas 双击路由 + NodeDetailPanel 入口</name>
  <files>packages/infinite-canvas/src/components/theater/GroupViewTheater.tsx, packages/infinite-canvas/src/components/FlowCanvas.tsx, packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx</files>
  <read_first>packages/infinite-canvas/src/components/theater/TheaterShell.tsx (56-02), packages/infinite-canvas/src/components/theater/groupMembership.ts (Task 1 产物), packages/infinite-canvas/src/components/variants/VariantWall.tsx (L52-76 组解析 useMemo 段——结构范本), packages/infinite-canvas/src/components/FlowCanvas.tsx (L547-553 onNodeDoubleClick + L1111 挂载区 + graph/raw store selector 用法), packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx (头栏按钮区——52-03 重生成按钮同位)</read_first>
  <action>
  1. GroupViewTheater.tsx:`useTheaterStore` 开态 → 组解析 useMemo(deriveGroupMembers;空 → TheaterShell 内空态卡『未找到同族资产』)→ 按 kind 分发 TurnaroundView/SceneGallery/VoiceProfileBoard;头栏 title 词表 + headerExtra(turnaround:同步缩放控件——scale state 在此;全部:「节点详情」按钮 → setDetailNode(锚节点 RF node 查 store.nodes)后**不关剧场**(面板与剧场并存,zIndex:panel 45? 核 NodeDetailPanel z 值——若低于剧场 40 则在 SUMMARY 注记提升为 45,最小改)。
  2. Esc:useEffect window keydown Esc → close(开态门控 + cleanup)。
  3. FlowCanvas.tsx:onNodeDoubleClick 前置 theaterTargetOf 分支(≤6 行;graph/raw 经 useCanvasStore.getState() 取——避免重渲;判定失败静默走原路径);挂载 `<GroupViewTheater />` VariantWall 同级。
  4. NodeDetailPanel.tsx:头栏加「组视图」按钮(theaterTargetOf(detailNode) 命中才渲染;onClick → useTheaterStore.open)——次入口闭环。
  5. 手动冒烟指令写入文件头注释(npm run dev + 双击角色节点步骤)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx tsc -b 2>&1 | tail -3 && npm test 2>&1 | tail -6 && grep -c "theaterTargetOf" src/components/FlowCanvas.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<GroupViewTheater" FlowCanvas.tsx` ≥ 1 且 onNodeDoubleClick 段含 `theaterTargetOf` + `return`(命中改道)
    - 原双击链(navHistory.push/setSelectedNode/setDetailNode)在分支后完整保留(diff 核对)
    - NodeDetailPanel 含 `组视图` 按钮与 `theaterTargetOf` 条件
    - GroupViewTheater 含 `TheaterShell`、`Escape`、`节点详情`
    - `npm test` 全绿 + `npx tsc -b` exit 0(既有 FlowCanvas 相关测试无回归)
  </acceptance_criteria>
  <done>VIZ-02 端到端:双击改道 + 三布局剧场 + 详情互链;REGEN-04 双击语义对非组资产零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| khs 同步的 views/turnaround_path/声纹 URL → 剧场渲染 | 产线媒体路径,展示层消费 |
| wheel 事件 → 画布缩放冲突 | 剧场内 wheel preventDefault 与画布 zoom 互斥(剧场 open 时画布在其下层) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-04-01 | DoS | 同步缩放高频 setState 重渲 4 格 | mitigate | scale state 单点 + 图 transform 纯 CSS(不重排);wheel 步进 0.1 粒度天然节流;transform 无 transition(wheel 态) |
| T-56-04-02 | Tampering | views/URL 注入(拼接 XSS) | mitigate | React 文本节点 + URL 只进 img src 且经 resolveMediaUrl 白名单;无 dangerouslySetInnerHTML |
| T-56-04-03 | DoS | 组员推导 O(N) 每渲染 | mitigate | useMemo graph/raw 引用门控(与 56-03 同纪律) |
| T-56-04-04 | DoS | 多声纹同时播放 | mitigate | 模块级单 audio 引用,新卡播放先停旧卡(设计内建) |
| T-56-04-05 | Tampering | 双击改道误伤非组资产 | mitigate | theaterTargetOf 判定序 + 防御守卫;测试覆盖 null 路径(七组之两组) |
| T-56-04-SC | Tampering | package installs | accept | 零新包 |

**结论:纯展示剧场;最敏感点是双击改道回归面,由判定纯函数测试 + FlowCanvas diff 约束兜住。无 high 级未缓解威胁。**
</threat_model>

<verification>
- `npx vitest run src/components/theater/__tests__/groupMembership.test.ts` exit 0
- `npm test` 全绿 + `npx tsc -b` exit 0
- VariantWall.tsx / healThumb.ts / useLod.ts git diff 为空
- 同步缩放手感/剧场观感归 HUMAN-UAT
</verification>

<success_criteria>
- ROADMAP 成功标准 2 全量成立:turnaround 四视图同屏对比(同步缩放)/场景画廊多视角/voice_profile 内嵌试听
- D-05/D-06/D-07/D-08/D-16 落地;UI-SPEC §4 规格 + Do-Not-Regress 5/6/9/11 守住
</success_criteria>

<output>
Create `.planning/phases/56-creative-visualization/56-04-SUMMARY.md` when done. 记录:双击改道插入行数与位置、NodeDetailPanel z 值裁定、scale 状态归属终裁、空态命中场景(存量数据 A-2 兜底观察)。
</output>
