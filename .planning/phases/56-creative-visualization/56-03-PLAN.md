---
phase: 56-creative-visualization
plan: 03
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - packages/infinite-canvas/src/components/canvas/icons.tsx
  - packages/infinite-canvas/src/components/badges/NodeBadges.tsx
  - packages/infinite-canvas/src/components/badges/ScorePopover.tsx
  - packages/infinite-canvas/src/components/nodes/AssetCardNode.tsx
  - packages/infinite-canvas/src/components/canvas/slots.ts
  - packages/infinite-canvas/src/components/__tests__/scorePopover.test.tsx
autonomous: true
requirements: [VIZ-01]
must_haves:
  truths:
    - "verdict 角标:左下 verdict 带——stale 三角贴角位不动,verdict 环排其右(left: off + tri + 4px,同带横排 gap 4px;无 stale 时同位稳定不跳);眼在前耳在后;直径 10px(badge.dot),glyph eye/ear 8px text.primary,底 --cv-bg-overlay"
    - "verdict 三态形色双编码(D-02):PASS=signal.approved 1.5px 实线环 / FAIL=signal.rejected 实线环+外扩 2px 同色 40% 静态光环 / WARN=signal.running 虚线环(dashed);judge 身份不走颜色(dataviz 单通道纪律)"
    - "LOD 联动(D-04):lod===0 全不渲染(NodeBadges 既有早返天然满足);L1/L2 均渲染;刷新纯 store 派生(deriveQcVerdicts memo),socket scored/同步到达自动重渲,零轮询"
    - "hover mini-雷达 popover:悬停资产卡 ≥250ms 且 aiScore.dimensions≥3 → 上方居中 8px 锚距(越界翻下)浮层;ScoreRadar size=128 零修改直用 + 维度行(中文 dimLabel + getScoreColor 色点 + mono 数值,≤8 行截断)+ overall 大字头;pointer-events:none;100ms 消失防抖;全局单例;LOD≥1 才触发"
    - "tooltip 维度分明细:角标 native title『眼审 通过/耳审 不过/…』(UI-SPEC Copywriting 词表);popover 头『AI 评分 · {N}』"
    - "四角产权制不回归:左上策展/右上执行+score/右下 review 语义与位置零改动;verdict 是左下带新增成员"
  artifacts:
    - path: "packages/infinite-canvas/src/components/canvas/icons.tsx"
      provides: "eye/ear 两个 SVG icon kind(stroke=currentColor,size 8-12 适配,同一几何风格)"
      contains: "eye"
    - path: "packages/infinite-canvas/src/components/badges/NodeBadges.tsx"
      provides: "左下 verdict 带渲染(verdicts prop 注入,环三态 + title 词表)"
      contains: "verdict"
    - path: "packages/infinite-canvas/src/components/badges/ScorePopover.tsx"
      provides: "hover mini-雷达浮层组件(ScoreRadar 128 复用 + 维度行 + overall 头)"
      contains: "ScorePopover"
  key_links:
    - from: "packages/infinite-canvas/src/components/nodes/AssetCardNode.tsx"
      to: "packages/infinite-canvas/src/store/qcVerdict.ts"
      via: "useMemo(deriveQcVerdicts(graph, rawDataByNodeId)) → NodeBadges verdicts prop + ScorePopover"
      pattern: "deriveQcVerdicts"
    - from: "packages/infinite-canvas/src/components/badges/ScorePopover.tsx"
      to: "packages/infinite-canvas/src/components/panel/ScoreRadar.tsx"
      via: "<ScoreRadar aiScore={aiScore} size={128} /> 原组件直用(零修改)"
      pattern: "size={128}"
---

<objective>
落 VIZ-01 的两件 UI:① verdict 眼/耳角标(NodeBadges 左下带扩展 + icons.tsx eye/ear kind + slots verdicts 通道);② hover mini-雷达 popover(ScorePopover 新组件,ScoreRadar 128px 直用,AssetCardNode hover 250ms 触发)。

消费 56-01 的 deriveQcVerdicts/scoreVocabulary/applySocketScored 数据链——「分数更新后角标实时刷新」在本 plan 成端到端事实。

输出:2 个修改组件 + 1 新组件 + icons/slots 扩展 + 1 测试文件。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/56-creative-visualization/56-CONTEXT.md
@.planning/phases/56-creative-visualization/56-RESEARCH.md
@.planning/phases/56-creative-visualization/56-PATTERNS.md
@.planning/phases/56-creative-visualization/56-UI-SPEC.md

**COORD-01:本 plan 零 khs 文件、零 root 后端文件。ScoreRadar.tsx 禁改(UI-SPEC Do-Not-Regress 3)。**

<design_decisions>
<!-- 前端设计纪律(/frontend-design)——UI-SPEC §1/§2 逐条落地,执行者不再决策 -->

**Token(全既有,禁新 hex):**
- PASS 环 `v3theme.signal.approved` / FAIL 环 `v3theme.signal.rejected`(+光环 `rgba` 同色 0.4 用 theme 语义色拼 opacity——照 NodeBadges 既有 modalityWeak 用法,不新造常量)/ WARN 环 `v3theme.signal.running`
- glyph 前景 `theme.text.primary`;环底 `var(--cv-bg-overlay, #1E2128)`(既有角标同款)
- popover:`--cv-bg-panel` 底 + `--cv-border-subtle` + `--cv-shadow-pop` + blur(4px);维度行色点 `getScoreColor(dim)`;数值 mono

**几何:**
- 环:svg 10×10 circle r=4 stroke-width 1.5;FAIL 光环 = 第二 circle r=5.5 stroke 同色 strokeOpacity 0.4(静态,不动画);WARN strokeDasharray '2 1.5'
- verdict 带起点 `left: off + badge.tri + 4`(off=-6/tri=14 → 12px;与 stale 共存时 stale 占 0-14px,verdict 12px 起**微重叠 2px 属允许**(两标记视觉成组),planner 终裁:改为 `off + tri + 2` 消除重叠——**执行按 +2**);眼耳互排 gap 4px
- popover 宽 ~232px(128 雷达 + 96 维度列);维度行 11px/1.4;截断行「…」10px tertiary

**文案(UI-SPEC Copywriting 词表,逐字):** title『眼审 通过』『眼审 不过』『眼审 留意』『耳审 通过』『耳审 不过』『耳审 留意』;popover 头『AI 评分 · {Math.round(overall*100)}」+『/ 100』;不足 3 维不触发 popover(退角标 title)。

**交互:** popover 触发 250ms/消失 100ms(AssetCardNode hoverTimer 视频先例改参);卡 onMouseLeave 清 timer;popover absolute 定位于卡内(卡根 position:relative 已成立);**pointerEvents:'none' 全层**(不抢画布交互/不挡拖拽)。
</design_decisions>

<interfaces>
NodeBadgesProps 扩展:slots.ts L14-24 interface 加 `verdicts?: QcVerdict[]`(import type from store/qcVerdict——type-only import 不违 P8);NodeBadges.tsx 在 stale 段(L71-80)与 review 段(L82-96)之间插 verdict 段;`if (!curationBadge && …)` 空判(L98)加 verdicts 非空条件。

AssetCardNode 接线位:卡根 div(找 `data-testid` 或根容器 onMouseEnter 既有 hover 区);hoverTimer 模式 L261-263(视频 200ms)——新独立 timer state(250/100ms),不与视频 timer 共用;Badges 渲染位 L651(`<Badges … />` 加 verdicts prop);graph/raw 取数 `useCanvasStore` graph + rawDataByNodeId(selector memo,graph 引用变才重算——93 镜审计节点稀疏,代价可忽略,56-RESEARCH Pitfall 4)。

icons.tsx 扩展:AssetTypeIcon L101-162 的 SvgIcon 包装同款——新增 `EyeIcon`/`EarIcon`(或 kind 并入既有导出风格,依文件实际结构;size prop 默认 14→本处传 8)。眼=杏仁形+瞳孔圆;耳=半圆弧+内螺旋;stroke=currentColor fill none strokeWidth 1.2——与既有 kind 同几何密度。

ScoreRadar 直用:`<ScoreRadar aiScore={aiScore} size={128} />`(props L19-23 现成 size);其 <3 维自返 null 的行为由调用方前置判断(dimensions≥3 才开 popover)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: icons.tsx eye/ear + slots verdicts 通道</name>
  <files>packages/infinite-canvas/src/components/canvas/icons.tsx, packages/infinite-canvas/src/components/canvas/slots.ts</files>
  <read_first>packages/infinite-canvas/src/components/canvas/icons.tsx (全文——SvgIcon 包装/既有 kind 几何风格/export 形态), packages/infinite-canvas/src/components/canvas/slots.ts (L14-24 NodeBadgesProps + L29 registerNodeBadgesRenderer)</read_first>
  <action>
  1. icons.tsx:按文件既有风格新增 `EyeIcon` 与 `EarIcon`(props `{size?: number; color?: string}` 同既有 icon;stroke=currentColor strokeWidth 1.2 fill none);眼:ellipse 轮廓 + circle 瞳孔;耳:path 弧线内卷;尺寸 viewBox 16×16 内构,渲染 size 缩放。
  2. slots.ts:NodeBadgesProps 加 `verdicts?: ReadonlyArray<{ judge: 'eye'|'ear'; verdict: 'pass'|'warn'|'fail' }>`(内联类型,不 import qcVerdict 模块——slots 是 B 层稳定接口,手写形状避免 A→B 依赖;qcVerdict 的 QcVerdict 结构性兼容)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx tsc -b 2>&1 | tail -3 && grep -c "EyeIcon\|EarIcon" src/components/canvas/icons.tsx</automated>
  </verify>
  <acceptance_criteria>
    - icons.tsx 导出 EyeIcon/EarIcon 且 `grep -c 'fill="none"' 该新增段` ≥ 1(线构不实心)
    - slots.ts NodeBadgesProps 含 `verdicts?`
    - `npx tsc -b` exit 0
  </acceptance_criteria>
  <done>眼/耳 glyph 与 verdicts 通道就位,B 层接口零破坏(可选 prop)。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NodeBadges 左下 verdict 带 + vitest(既有测试文件若存则扩)</name>
  <files>packages/infinite-canvas/src/components/badges/NodeBadges.tsx, packages/infinite-canvas/src/components/__tests__/scorePopover.test.tsx</files>
  <read_first>packages/infinite-canvas/src/components/badges/NodeBadges.tsx (全文——四段结构/空判 L98/dotStyle), packages/infinite-canvas/src/store/__tests__/qcVerdict.test.ts (56-01 产物——verdicts 形状), .planning/phases/56-creative-visualization/56-UI-SPEC.md (§1 规格 + Copywriting 词表)</read_first>
  <behavior>
  - Test: verdicts=[{judge:'ear',verdict:'fail'}] → 渲染 1 个 verdict 角标,title 含『耳审 不过』
  - Test: 眼+耳共存 → 两枚横排,眼在前(DOM 序断言)
  - Test: stale 与 verdict 共存 → 两者都在(stale svg + verdict 环),verdict 定位含 tri 偏移(style left 断言或 data 属性)
  - Test: verdicts 空数组/undefined → 零 verdict 节点渲染;lod===0 → 整体 null(既有行为不回归)
  - Test: 三态环样式——FAIL 含 strokeOpacity 0.4 光环 circle;WARN 含 strokeDasharray;PASS 单 circle
  </behavior>
  <action>
  1. NodeBadges.tsx:stale 段后插 verdict 段——`verdicts?.map((v, i) => …)`:容器 span 绝对定位 `bottom: off, left: off + badge.tri + 2, display:flex, gap:4`;每枚 = 10×10 svg(外环按三态 + FAIL 光环第二 circle)+ 内嵌 8px EyeIcon/EarIcon(color theme.text.primary)+ 底 `var(--cv-bg-overlay)`(svg 内先画 opacity 底 circle fill)。title = 词表拼接(verdictLabel 不 import utils?——可 import:badges→utils 无环;用 scoreVocabulary.verdictLabel 得『通过/留意/不过』,拼『眼审 』『耳审 』前缀)。
  2. 空判链 `if (!curationBadge && !execBadge && !staleBadge && !reviewBadge && !hasVerdicts) return null` 补 hasVerdicts。
  3. 既有四角渲染代码零改动(只插入,不重排)。
  4. 测试并入 scorePopover.test.tsx(或如仓内已有 NodeBadges 测试则扩之;render 用既有测试基建——查 components/__tests__ 目录风格,react 元素浅断言即可:直接调用组件函数或 @testing-library/react 依仓内先例)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/components/__tests__/scorePopover.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - vitest exit 0(五组断言)
    - NodeBadges.tsx 新增段含 `signal.approved`、`signal.rejected`、`signal.running`、`strokeDasharray`、`眼审`(词表前缀)
    - `grep -n "title=" packages/infinite-canvas/src/components/badges/NodeBadges.tsx | wc -l` ≥ 5(既有 4 + verdict)
    - 既有段(curation/exec/stale/review)diff 仅缩进级无语义变更
    - `npx tsc -b` exit 0
  </acceptance_criteria>
  <done>左下 verdict 带落地:三态形色双编码、共存规则、LOD 门控全部有测;四角产权制零回归。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ScorePopover + AssetCardNode hover 接线</name>
  <files>packages/infinite-canvas/src/components/badges/ScorePopover.tsx, packages/infinite-canvas/src/components/nodes/AssetCardNode.tsx, packages/infinite-canvas/src/components/__tests__/scorePopover.test.tsx</files>
  <read_first>packages/infinite-canvas/src/components/panel/ScoreRadar.tsx (全文——size prop/≥3 维自返 null 行为), packages/infinite-canvas/src/components/nodes/AssetCardNode.tsx (L261-263/294-298 hoverTimer 模式、L615-660 组件体 graph/raw 取数与 Badges 消费位、卡根 div 定位), packages/infinite-canvas/src/components/canvas/SearchNavigator.tsx (浮层 token 语法:panel 底/shadow-pop/blur), packages/infinite-canvas/src/utils/scoreVocabulary.ts (56-01 产物——dimLabel)</read_first>
  <behavior>
  - Test: ScorePopover 收 {overall:0.82, dimensions:{a:.9,b:.8,c:.7,d:.6}} → 含 data-testid="score-popover"、雷达容器、4 行维度行、头『AI 评分 · 82』
  - Test: dimensions 9 维 → 渲染 8 行 + 截断行『…』
  - Test: 维度行中文——dimensions key 'drama' → 行文本含 dimLabel('drama') 中文
  - Test: popover 容器 style 含 pointerEvents:'none'
  - Test: AssetCardNode 侧(集成,可选 mock socket):aiScore 更新(props/store 变)→ 重渲含新 overall(实时刷新链的组件侧断言)
  </behavior>
  <action>
  1. 新建 ScorePopover.tsx:`({ aiScore }: { aiScore: AIScore })` 纯展示——容器 absolute `left:'50%', transform:'translateX(-50%)', bottom:'calc(100% + 8px)'`(锚卡上方;AssetCardNode 依卡在视口位置翻转方向的计算复杂度不值——**终裁:只上方**,卡贴顶时 popover 被视口裁的概率低(可拖画布),SUMMARY 注记该简化);`pointerEvents:'none'` 全层。
  2. 结构:头(『AI 评分 · 』+ mono 大字 Math.round(overall*100) + 『/ 100』secondary)→ 主体横排:左 ScoreRadar size 128、右维度列(行 = 色点(getScoreColor) 6px + dimLabel(name) 11px + mono 值 Math.round(v*100);≤8 行 + 溢出『…』行)。
  3. AssetCardNode:组件体加 popover state(`radarOpen`)与 enter/leave timer(250/100ms);onEnter 条件 `lod >= 1 && dimsCount(aiScore) >= 3`;卡根 div 挂 onMouseEnter/onMouseLeave(不与视频 hoverTimer 共 state);条件渲染 `<ScorePopover/>`(卡根需 position:relative——已成立则零改,核之)。
  4. verdicts 注入:组件体 `const qcVerdicts = useMemo(() => deriveQcVerdicts(graph, rawDataByNodeId).get(id) ?? [], [graph, rawDataByNodeId, id])`(graph/raw 经 useCanvasStore selector);`<Badges … verdicts={qcVerdicts} />`。
  5. 测试并入 scorePopover.test.tsx(Task 2 同文件)——Task 3 增 5 组。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/components/__tests__/scorePopover.test.tsx 2>&1 | tail -5 && npx tsc -b 2>&1 | tail -3 && npm test 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - vitest exit 0(合并 10 组断言)
    - ScorePopover.tsx 含 `size={128}`、`pointerEvents: 'none'`、`dimLabel`;hex 扫描 0 裸命中(口径:剥除 `var(--cv-…, #fallback)` 段后 grep——fallback 写法是 house style 允许)
    - AssetCardNode.tsx 含 `deriveQcVerdicts` 与 `ScorePopover`;hover 常量 250/100 字面在
    - ScoreRadar.tsx `git diff` 为空(零修改红线)
    - `npm test` 全绿 + `npx tsc -b` exit 0
  </acceptance_criteria>
  <done>VIZ-01 成端到端:socket scored → aiScore canonical → 雷达/角标实时刷;审计节点 → 眼/耳角标;hover 即看雷达不占常驻面积(D-01/02/03/04 全落地)。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 审计 raw 袋/aiScore → 角标与 popover 渲染 | 产线数值,纯展示(React 文本节点,无 innerHTML) |
| hover 事件 → 画布拖拽 | popover 不挡画布交互 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-03-01 | DoS | 93 镜 × hover popover 频繁挂卸 | mitigate | 全局单例(hover 天然)+ 250ms 延迟 + pointerEvents none;不进 DOM 直到触发 |
| T-56-03-02 | Tampering | dimension key 超长/换行注入 | mitigate | dimLabel 未知回退原样但经 React 文本节点;行内 overflow hidden + ellipsis(样式层) |
| T-56-03-03 | DoS | deriveQcVerdicts 每渲染重算 | mitigate | useMemo graph/raw 引用门控(56-RESEARCH Pitfall 4);审计节点稀疏 |
| T-56-03-04 | Repudiation | verdict 无审计痕迹 | accept | 展示层只派生不写;真值在审计节点/gate(54 期) |
| T-56-03-SC | Tampering | package installs | accept | 零新包 |

**结论:纯展示层;无 high 级未缓解威胁。**
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/scorePopover.test.tsx` exit 0
- `npm test` 全绿 + `npx tsc -b` exit 0
- ScoreRadar.tsx / VariantWall.tsx git diff 为空
- 真机走查(角标三态观感/popover 跟手)归 HUMAN-UAT
</verification>

<success_criteria>
- ROADMAP 成功标准 1 的 UI 半边成立:雷达呈现(面板既有 + hover popover 新增)+ verdict 角标直贴资产 + 实时刷新
- UI-SPEC §1/§2 全规格落地;Do-Not-Regress 2/3/11 守住
</success_criteria>

<output>
Create `.planning/phases/56-creative-visualization/56-03-SUMMARY.md` when done. 记录:verdict 带与 stale 的偏移终值(+2 裁定)、popover 只上方简化的理由、icons kind 命名、测试覆盖清单。
</output>
