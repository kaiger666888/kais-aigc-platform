---
phase: 56-creative-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/infinite-canvas/src/hooks/useCanvasSocket.ts
  - packages/infinite-canvas/src/store/canvasStore.ts
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
  - packages/infinite-canvas/src/utils/scoreVocabulary.ts
  - packages/infinite-canvas/src/utils/__tests__/scoreVocabulary.test.ts
  - packages/infinite-canvas/src/store/qcVerdict.ts
  - packages/infinite-canvas/src/store/__tests__/qcVerdict.test.ts
  - packages/infinite-canvas/src/store/__tests__/socketScored.test.ts
autonomous: true
requirements: [VIZ-01]
must_haves:
  truths:
    - "socket 'node:state' state==='scored' payload(含 aiScore)不再死信:useCanvasSocket 识别并经新回调 onNodeScored 转发;payload 类型加 aiScore 槽位(D-03/56-RESEARCH Gap 1)"
    - "canvasStore.applySocketScored(nodeId, aiScore) 走 applyGraphTransform 只写 kind==='asset' 节点的 aiScore——不触碰 state、不触碰 stale(52-01 语义红线:评分到达≠新事实产出)"
    - "normalizeSocketNodeState 既有归一表零改动('scored' 不入表——入表会把评分错映射成执行态并污染 stale 清除规则)"
    - "scoreVocabulary:维度/视角/verdict 三张中文映射表 + normalizeScore(v,scale) 量纲归一(unit|percent);未知 key 原样返回(fail-soft,khs 改维度不炸前端,D-14/D-15)"
    - "qcVerdict.deriveQcVerdicts(graph, rawDataByNodeId):审计节点(voice-audit/video-qc/preview-qc)raw 袋防御式解析 → per-clip verdict 按 shot_id join 资产节点 → Map<nodeId,{judge:'eye'|'ear',verdict:'pass'|'warn'|'fail'}>;识别失败=空 Map + console.warn,永不 throw(D-13 单一派生链)"
    - "资产 raw 袋若有 qc_verdict/verdict 直读字段则优先(shortcut 演进位,khs 未来直写无缝)"
  artifacts:
    - path: "packages/infinite-canvas/src/utils/scoreVocabulary.ts"
      provides: "DIM_LABELS/VIEW_LABELS/VERDICT_LABELS 三张映射 + normalizeScore 量纲归一 + dimLabel/viewLabel/verdictLabel 查询函数(未知回退原 key)"
      contains: "normalizeScore"
    - path: "packages/infinite-canvas/src/store/qcVerdict.ts"
      provides: "deriveQcVerdicts 纯 selector(审计节点×资产 join)+ QcVerdict 手写 interface"
      contains: "deriveQcVerdicts"
    - path: "packages/infinite-canvas/src/store/canvasStore.ts"
      provides: "applySocketScored canonical 写(applyGraphTransform 写 asset.aiScore)"
      contains: "applySocketScored"
  key_links:
    - from: "packages/infinite-canvas/src/hooks/useCanvasSocket.ts"
      to: "packages/infinite-canvas/src/store/canvasStore.ts"
      via: "socket.on('node:state') state==='scored' → onNodeScored 回调 → FlowCanvas 既有 socket 接线处调 store.applySocketScored"
      pattern: "applySocketScored"
    - from: "packages/infinite-canvas/src/store/qcVerdict.ts"
      to: "packages/infinite-canvas/src/store/canvasStore.ts"
      via: "消费 useCanvasStore.getState().graph + rawDataByNodeId 纯派生(不在 store 内持久化,消费侧 memo)"
      pattern: "deriveQcVerdicts"
---

<objective>
打通 VIZ-01 的数据地基三件:① socket scored 死信修复(useCanvasSocket 识别 state==='scored'+aiScore payload → canvasStore.applySocketScored canonical 写 asset.aiScore——56-RESEARCH Critical Gap 1 的唯一修复点);② scoreVocabulary 中文映射模块(D-15 契约伴生映射,p03 五维/p14 八维/视角 key/verdict 三态 + 量纲归一);③ qcVerdict 派生 selector(D-13 单一真值链:审计节点 → shot_id join → per-asset 眼/耳 verdict)。

本 plan 无 UI(角标/popover 归 56-03);全部纯逻辑 + store 接线 + 三份单测。执行后「分数更新后角标实时刷新」的数据前提成立。

输出:2 个修改文件 + 2 个新纯模块 + 3 个测试文件。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/56-creative-visualization/56-CONTEXT.md
@.planning/phases/56-creative-visualization/56-RESEARCH.md
@.planning/phases/56-creative-visualization/56-PATTERNS.md
@.planning/phases/56-creative-visualization/56-UI-SPEC.md

**COORD-01:本 plan 零 khs 文件。执行者不得修改 files_modified 之外的文件。**

<design_decisions>
纯逻辑 plan——UI-SPEC 的 token/布局决策不适用;纪律:零 React import(scoreVocabulary/qcVerdict 照 variantOps 纯模块)、never-throws(防御式解析)、P8 不 import root schema(手写 interface,候选 zod 只做语义参照不 import)。
</design_decisions>

<interfaces>
useCanvasSocket.ts 现状:L152 `socket.on('node:state', (payload: {nodeId; state: NodeState; progress?}) => callbacksRef.current.onNodeStateChange(...))`;callbacksRef 转 L108-119——onNodeScored 加入 options/callbacksRef 两处(与 onGateState L75 同款 optional 回调)。

canvasStore 写范式:L739-757 applySocketNodePreview(graph 空守卫→节点存在守卫→kind==='asset' 守卫→applyGraphTransform 写单字段);L137 CanvasState 接口 applySocketNodeState 同排加 applySocketScored。aiScore 持久化:canvas_nodes.aiScore 独立列经 store 往返不丢(types/canvas.ts:310 注释),applyGraphTransform 后既有 save 链路天然落库——本 plan 不加显式 save 调用(socket 写回与 node:state 同策略)。

FlowCanvas 接线位:useCanvasSocket 消费处(与 onNodeStateChange 同排)加 onNodeScored: (nodeId, aiScore) => useCanvasStore.getState().applySocketScored(nodeId, aiScore)——若接线须改 FlowCanvas.tsx,允许最小插入(≤3 行),在 SUMMARY 注记。

qcVerdict 审计节点识别词汇:pipeline/model.ts:589-591 idIncludes 'video-qc' 先例;voice-audit 同 lane(phaseIndex 11 p10c 域)。防御式守卫 isRecord/str/num 照 candidateEnvelope.ts:174-179。

审计 raw 形状(56-RESEARCH §G16):voice-audit 顶层 {phase, findings[], clips?, critical_count, warning_count, total_clips, pass};clips[] 记录 {id, shot_id, path, transcript, verdict(PASS/WARN/FAIL), similarity, reason, dims}(p10c_voice_audit.py:588-598)。video-qc/preview-qc 形状各异 → 只识别「有 per-item 列表且 item 含 shot_id+verdict」的通用探测(列表键候选:clips/per_shot/shots/items/variants),识别不了返回空——fail-soft 是契约而非缺陷。

量纲:candidateScoreSchema.scale 'unit'|'percent' 语义(candidateEnvelope.ts:52-60);p14 八维 0-100(HOOK 等)——normalizeScore(78,'percent')=0.78,'unit' 原样钳制 0-1。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scoreVocabulary.ts 中文映射 + 量纲归一 + vitest</name>
  <files>packages/infinite-canvas/src/utils/scoreVocabulary.ts, packages/infinite-canvas/src/utils/__tests__/scoreVocabulary.test.ts</files>
  <read_first>packages/infinite-canvas/src/components/g15/g15TriageStore.ts (L55-74 classifyG15——root 契约特征表包内手写镜像的 P8 纪律模范), .planning/phases/56-creative-visualization/56-RESEARCH.md (§雷达 维度清单 + §Critical Gap 2), packages/infinite-canvas/src/components/StoryboardTimeline.tsx (METADATA_LABELS 中文枚举表 grep 定位——中文标签表先例)</read_first>
  <behavior>
  - Test: dimLabel('drama')='戏剧性' 等 p03 五维(drama/rhythm/character/reversal_depth/social_resonance)与 p14 八维(hook_quality/narrative_design/shot_breakdown/scene_planning/character_consistency/audio_voice/visual_rendering + 整体项)全覆盖
  - Test: dimLabel('unknown_dim_2077') 原样返回 'unknown_dim_2077'(未知回退,不 throw)
  - Test: viewLabel('front')='正面' / ('angle_left')='左侧' / ('back'与'rear'均可)='背面' / ('top_down'与'top-down')='俯视' / ('three_quarter')='3/4 侧';未知 key 原样
  - Test: verdictLabel('PASS')='通过' / ('WARN')='留意' / ('FAIL')='不过';小写 'pass' 亦命中;未知原样
  - Test: normalizeScore(0.82,'unit')=0.82;normalizeScore(78,'percent')=0.78;normalizeScore(120,'percent')=1(钳制);normalizeScore(-3,'unit')=0;normalizeScore(NaN, 任意)=0
  </behavior>
  <action>
  1. 新建 scoreVocabulary.ts:零 React import;三张 `Readonly<Record<string,string>>` 表 + 三个查询函数(dimLabel/viewLabel/verdictLabel——内部 key 规范化:verdict 走 `String(v).toUpperCase()` 后查表)+ `normalizeScore(v: unknown, scale?: 'unit'|'percent'): number`(typeof 守卫→percent /100→clamp 0-1;scale 缺省按 'unit')。
  2. 表内容以 56-RESEARCH §雷达清单为准手写;文件头注释标注真值源(khs p03_script_audit.py / p14_quality_audit.py / p07_scene_generation.py)+ 「khs 改维度不炸前端(未知回退)」契约 + verify:phase-56 S-vocabulary 契约组锚点。
  3. 测试照 <behavior> 六组;中文期望值逐一列(非快照)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/utils/__tests__/scoreVocabulary.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - vitest exit 0,六组断言齐
    - `grep -c "from 'react'\|from \"react\"" packages/infinite-canvas/src/utils/scoreVocabulary.ts` = 0
    - scoreVocabulary.ts 含字面 `reversal_depth` 与 `hook_quality`(两代维度集都在)与 `'percent'`(量纲分支)
    - `npx tsc -b` exit 0
  </acceptance_criteria>
  <done>维度/视角/verdict 中文映射 + 量纲归一成模块,未知回退有测试锁死——D-14/D-15 的前端侧契约落地。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: socket scored 死信修复 — useCanvasSocket + canvasStore.applySocketScored + vitest</name>
  <files>packages/infinite-canvas/src/hooks/useCanvasSocket.ts, packages/infinite-canvas/src/store/canvasStore.ts, packages/infinite-canvas/src/components/FlowCanvas.tsx, packages/infinite-canvas/src/store/__tests__/socketScored.test.ts</files>
  <read_first>packages/infinite-canvas/src/store/canvasStore.ts (L396-413 normalizeSocketNodeState——禁改;L687-757 applySocketNodeState/applySocketNodePreview 全段——applySocketScored 照 preview 段写), packages/infinite-canvas/src/hooks/useCanvasSocket.ts (L58-79 options + L108-119 callbacksRef + L152-159 node:state handler), packages/infinite-canvas/src/store/__tests__/ (既有 store 测试的 fixture graph 工厂风格,selectWinner.test.ts 若在)</read_first>
  <behavior>
  - Test: applySocketScored('n1', {overall:0.82, dimensions:{a:0.9,b:0.7,c:0.6}}) 后 graph 中 n1 的 aiScore 等于该对象,且 n1.state 不变、n1.stale 不变(有 stale 的节点评分后 stale 仍在)
  - Test: graph 为空 → warn 不 throw;节点不存在 → warn 不 throw;kind!=='asset'(event 节点) → warn 不 throw 且 graph 零变更
  - Test: 归一表不认 'scored':normalizeSocketNodeState('scored') === null(锁死——防后人顺手加进归一表)
  - Test: 量纲:applySocketScored 收 {overall:78} 百分制脏数据时钳制为 1(0-1 域)——在 applySocketScored 内经 normalizeScore(overall,'percent'>100 钳制)处理:overall>1 视为 percent 归一
  </behavior>
  <action>
  1. useCanvasSocket.ts:UseCanvasSocketOptions 加 `onNodeScored?: (nodeId: string, aiScore: unknown) => void`;callbacksRef 两处同步;node:state handler 改为先判 `payload.state === 'scored'`(payload 类型扩 `aiScore?: unknown`)→ onNodeScored 转发并 return,否则走既有 onNodeStateChange。**不改既有任何 handler 语义**。
  2. canvasStore.ts:CanvasState 加 `applySocketScored: (nodeId: string, aiScore: unknown) => void`;实现照 applySocketNodePreview 骨架:graph 空守卫/节点存在守卫/kind==='asset' 守卫(各 warn+return)→ 防御式解析 aiScore(isRecord + overall 经 normalizeScore(>1 按 percent) + dimensions Record<string,number> 过滤)→ applyGraphTransform 写 `aiScore: {overall, dimensions}`(dimensions 全非法时省略字段)。
  3. FlowCanvas 接线:在 useCanvasSocket 消费处(既有 onNodeStateChange 接线同排,grep 定位)加 `onNodeScored: (nodeId, aiScore) => useCanvasStore.getState().applySocketScored(nodeId, aiScore)`(≤3 行插入)。
  4. socketScored.test.ts:fixture graph(1 asset + 1 event 节点)驱动上述四组;normalizeSocketNodeState 锁死断言 import 归一函数直接测。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/store/__tests__/socketScored.test.ts 2>&1 | tail -5 && npx tsc -b 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - vitest exit 0(四组:canonical 写+state/stale 不动/三守卫/归一锁死)
    - `grep -n "applySocketScored" packages/infinite-canvas/src/store/canvasStore.ts | wc -l` ≥ 2(接口+实现)
    - `grep -c "'scored'" packages/infinite-canvas/src/store/canvasStore.ts` 中 normalizeSocketNodeState 函数体内 0 命中(grep -A 18 'function normalizeSocketNodeState' 段内无 scored)
    - useCanvasSocket.ts 含 `onNodeScored`(≥3 处:类型/ref 两处/handler)
    - `npx tsc -b` exit 0;`npm test` 全绿(既有 socket/store 测试无回归)
  </acceptance_criteria>
  <done>scored 事件从死信变 canonical 写:分数到达 → asset.aiScore 更新 → 派生渲染自动刷(D-03「实时刷新」数据链成立),52-01 stale 语义零扰动。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: qcVerdict.ts 派生 selector + vitest</name>
  <files>packages/infinite-canvas/src/store/qcVerdict.ts, packages/infinite-canvas/src/store/__tests__/qcVerdict.test.ts</files>
  <read_first>packages/infinite-canvas/src/store/variantOps.ts (纯函数图派生纪律:graph 入参/自足返回/无 React), packages/infinite-canvas/src/components/pipeline/model.ts (L589-591 video-qc match 词汇——只读参照,不 import), packages/infinite-canvas/src/components/variants/VariantWall.tsx (L79-120 candidates useMemo——raw 袋字段防御式提取 num/str 写法照抄), .planning/phases/56-creative-visualization/56-RESEARCH.md (§Critical Gap 2 全节)</read_first>
  <behavior>
  - Test: voice-audit 节点(raw 袋含 clips[{shot_id:'shot_012', verdict:'FAIL', ...}]) + 资产节点(raw shot_id='shot_012') → Map 得 {judge:'ear', verdict:'fail'}
  - Test: video-qc/preview-qc 节点(per_shot/variants 列表) → judge:'eye';PASS/WARN/FAIL → pass/warn/fail 小写映射
  - Test: 同一资产眼+耳共存 → Map 值为两判官数组或聚合结构(接口定为 verdicts: QcVerdict[]——每个 {judge, verdict})
  - Test: raw 袋无任何可识别列表 → 空 Map + console.warn(mock 断言调用一次),不 throw
  - Test: 资产 raw 袋自带 qc_verdict:'fail' → shortcut 直读优先(不走 join)
  - Test: graph 为空 / rawDataByNodeId 为 null → 空 Map 不 throw
  </behavior>
  <action>
  1. 新建 qcVerdict.ts:手写 `interface QcVerdict { judge: 'eye'|'ear'; verdict: 'pass'|'warn'|'fail' }`;导出 `deriveQcVerdicts(graph: GraphV3 | null, raw: Map<string, Record<string,unknown>> | null): Map<string, QcVerdict[]>`。
  2. 审计节点识别:script 节点 id 或 raw 袋含 'voice-audit'/'video-qc'/'preview-qc'(idIncludes 词汇)→ judge 映射 ear/eye/eye;per-item 列表探测键序 ['clips','per_shot','shots','items','variants'],item 需含 shot_id(string)+verdict(string)才算命中。
  3. join:资产节点 raw 袋 shot_id(d.shot_id ?? d.shotId)相等即挂 verdict;shortcut:资产 raw 袋 qc_verdict/verdict 直读(值域 PASS/WARN/FAIL/pass/warn/fail,其他忽略)。
  4. 纯模块零 React;所有提取走 str/num/isRecord 守卫;未知形状 console.warn 一次聚合(每审计节点至多一条)。
  5. 测试照 <behavior> 六组(fixture graph 工厂内联构造)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/store/__tests__/qcVerdict.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - vitest exit 0,六组断言齐
    - `grep -c "from 'react'" packages/infinite-canvas/src/store/qcVerdict.ts` = 0
    - qcVerdict.ts 含字面 `'voice-audit'`、`'video-qc'`、`'preview-qc'` 与 `throw` 0 命中
    - `npx tsc -b` exit 0
  </acceptance_criteria>
  <done>眼/耳 verdict 的 store 派生链成单一真值源:审计节点一处写入,角标(56-03)与 G16 工作台(56-05)同源消费。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| socket scored payload → store | 服务端广播的 aiScore(数值域不保证),展示层数值 |
| 审计节点 raw 袋(khs 同步) → 派生 selector | 产线写入的结构化数据,防御式消费 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01-01 | Tampering | scored payload aiScore 恶意/畸形(NaN/Infinity/负值/超1) | mitigate | applySocketScored 防御式解析:typeof+isFinite+clamp 0-1;dimensions 逐值过滤;socketScored.test 钳制断言 |
| T-56-01-02 | DoS | 高频 scored 事件打爆 applyGraphTransform | accept | 与既有 node:state 同频域(评分事件稀疏,单节点级别);applyGraphTransform 已是批量优化路径;不加节流(过度设计) |
| T-56-01-03 | Tampering | raw 袋原型污染(__proto__ 注入 key) | mitigate | isRecord 守卫(Array.isArray 排除已在)+ 不以 raw key 动态建对象属性;只做查表读 |
| T-56-01-04 | Information | console.warn 泄漏内部路径 | accept | 既有 applySocketNodeState 同款 warn 风格;只打 nodeId 不打 payload 全文 |
| T-56-01-SC | Tampering | package installs | accept | 零新包 |

**结论:纯数据链修复,输入全部防御式钳制;无 high 级未缓解威胁。**
</threat_model>

<verification>
- `cd packages/infinite-canvas && npx vitest run src/utils/__tests__/scoreVocabulary.test.ts src/store/__tests__/socketScored.test.ts src/store/__tests__/qcVerdict.test.ts` exit 0
- `npm test` 全绿 + `npx tsc -b` exit 0
- normalizeSocketNodeState 归一表 'scored' 0 命中(锁死断言在测内)
</verification>

<success_criteria>
- VIZ-01 的两条数据链(scored→aiScore canonical;审计节点→verdict 派生)全部打通且有测试锁死
- D-03/D-13/D-14/D-15 的前端侧契约落地;khs 改维度/改 verdict 词汇不炸(fail-soft 有测)
- 52-01 stale 语义零扰动(state/stale 不动有测)
</success_criteria>

<output>
Create `.planning/phases/56-creative-visualization/56-01-SUMMARY.md` when done. 记录:FlowCanvas 接线插入位置与行数、qcVerdict 的 item 列表探测键最终集、shortcut 字段名裁定、normalizeScore 的 >1 视为 percent 裁定。
</output>


