---
phase: 56-creative-visualization
plan: 06
type: execute
wave: 4
depends_on: ["56-01", "56-02", "56-03", "56-04", "56-05"]
files_modified:
  - scripts/verify-phase-56.ts
  - package.json
  - .planning/ROADMAP.md
  - packages/infinite-canvas/test/e2e/tests/phase56-viz.mjs
autonomous: true
requirements: [VIZ-01, VIZ-02, VIZ-03]
must_haves:
  truths:
    - "verify:phase-56 契约断言(section 化,verify-phase-53/55 同范式):S-socket-scored(死信修复链断言)/S-vocabulary(khs 维度词表镜像零漂移——正则读 khs python,55-01 手法)/S-badge(角标渲染词汇)/S-theater(TheaterShell 家族语法)/S-g16(桥 gate 白名单 + clips 解析)/S-token(新组件零新增 hex)/S-lod(LOD 本体零改动红线)"
    - "e2e 冒烟(phase55-nav.mjs 同范式):画布加载 → scored mock → 角标/雷达出现 → 双击角色节点开剧场 → G16 工作台开/连播/豁免 mock 回路;5 组断言 × headless"
    - "GUARD 收尾传统:verify:phase-56 注册进 package.json;断言失败 exit 非 0"
  artifacts:
    - path: "scripts/verify-phase-56.ts"
      provides: "phase 56 全契约断言(7 section,失败即红)"
      contains: "verify-phase-56"
    - path: "packages/infinite-canvas/test/e2e/tests/phase56-viz.mjs"
      provides: "VIZ 三面 e2e 冒烟(headless)"
      contains: "phase56"
  key_links:
    - from: "package.json"
      to: "scripts/verify-phase-56.ts"
      via: "npm run verify:phase-56"
      pattern: "verify:phase-56"
---

<objective>
Phase 56 收口三件:① `scripts/verify-phase-56.ts` 契约断言(7 section,把 56 的关键行为钉死——scored 修复链/词表镜像/角标词汇/剧场语法/G16 桥白名单/零新 hex/LOD 红线);② `phase56-viz.mjs` e2e 冒烟;③ package.json 注册 + ROADMAP Plans 行更新。

这是本 phase 的 GUARD 门——milestone 末 phase 汇总守护(ROADMAP 架构决策 7)的消费件。

输出:2 个新文件 + package.json 一行 + ROADMAP 文案更新(.planning 文档改动随本 plan 提交)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/56-creative-visualization/56-CONTEXT.md
@.planning/phases/56-creative-visualization/56-RESEARCH.md
@.planning/phases/56-creative-visualization/56-PATTERNS.md
@.planning/phases/56-creative-visualization/56-UI-SPEC.md

**COORD-01:verify 脚本**只读** khs python 文件做词表对照(regex 断言),零 khs 写入。**

<design_decisions>
verify 脚本纪律(verify-phase-53/55 同款):真实模块 import(不复制代码断字符串)、section 化输出(pass/fail 计数 + 失败列表)、process.exit 码;khs 侧文件路径经 env `KHS_ROOT` 缺省 `/data/workspace/kais-hermes-skills`。
</design_decisions>

<interfaces>
verify-phase-53.ts / verify-phase-55.ts(root-script 聚合范式:section 函数 + 汇总 + exit);package.json L46-47 注册位。khs 词表源:p03_script_audit.py(4 维 + reversal/social)/p14_quality_audit.py(L1022-1028 八维 JSON 模板)/p07_scene_generation.py(L450 views)——正则提取与 scoreVocabulary 表比对。

e2e 范式:test/e2e/tests/phase55-nav.mjs(headless 探针 + 断言 × N + mock 桥);testMode 桥(55-04 建)可注入 scored 假事件/socket mock——先读该文件确认既有注入手法再写。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/verify-phase-56.ts 七 section 契约断言 + 注册</name>
  <files>scripts/verify-phase-56.ts, package.json, .planning/ROADMAP.md</files>
  <read_first>scripts/verify-phase-53.ts (全文——section 范式/汇总/exit), scripts/verify-phase-55.ts (khs 文件正则对照手法 S-catalog), packages/infinite-canvas/src/utils/scoreVocabulary.ts + store/qcVerdict.ts + store/canvasStore.ts (56-01 产物——真实模块 import 断言), packages/infinite-canvas/src/components/badges/NodeBadges.tsx + theater/TheaterShell.tsx + g16/G16VoiceWorkbench.tsx (56-03/04/05 产物)</read_first>
  <action>
  1. 新建 scripts/verify-phase-56.ts(tsx 直跑;真实模块 import):
     - **S-socket-scored**:canvasStore 导出含 applySocketScored;normalizeSocketNodeState('scored')===null(锁死);useCanvasSocket 源码含 onNodeScored。
     - **S-vocabulary**:scoreVocabulary 含 p03 五维 + p14 八维 key;KHS_ROOT 下 p03/p14 python 正则提取维度 token 对照包内表——khs 新增维度未镜像时 FAIL(零漂移治理;**khs 删除/改名维度时 WARN 不 FAIL**——未知回退语义本来就容删除,注释钉)。
     - **S-badge**:NodeBadges 源码含 signal.approved/rejected/running 三态 + 『眼审』『耳审』词表;slots.ts NodeBadgesProps 含 verdicts。
     - **S-theater**:theater/TheaterShell.tsx 含 lightboxOverlay/theaterBtnStyle;VariantWall.tsx git 不动(文件内容含 53 特征注释);FlowCanvas 含 theaterTargetOf 分支与 GroupViewTheater/G16VoiceWorkbench 挂载。
     - **S-g16**:g15-ops.ts 含 gate 白名单正则字面;g15Bridge 含 'p11c-gate' 缺省;voiceAuditStore 含 'voice-audit'/'clips'/nextPending 且无 'requeue'。
     - **S-token**:56 新文件(theater/*, g16/*, badges/ScorePopover.tsx)hex 扫描——**口径:先剥除 `var(--cv-…, #fallback)` 整段再 grep `#[0-9A-Fa-f]{6}`,裸 hex 0 命中**(仓内 `var(--cv-bg-overlay, #1E2128)` fallback 是 house style 允许;UI-SPEC 红线 12 机械化)。
     - **S-lod**:useLod.ts 的 LOD_L0_MAX/LOD_L1_MAX/LOD_HYSTERESIS/FITVIEW_MIN_ZOOM 数值字面 0.22/0.6/0.03/0.4 仍在(本体零改动红线)。
  2. package.json scripts 加 `"verify:phase-56": "npx tsx scripts/verify-phase-56.ts"`(L47 后)。
  3. ROADMAP.md Phase 56 `**Plans**: TBD` 行更新为 6 plan 清单(01 scored 链/02 壳与引擎/03 VIZ-01/04 VIZ-02/05 VIZ-03/06 verify 收口)。
  </action>
  <verify>
    <automated>npm run verify:phase-56 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:phase-56` exit 0,七 section 全 pass(输出含各 section 名与计数)
    - 人为临时改 useLod.ts 阈值 → S-lod 变红(验证后复原;执行者自测一次,复原后 git diff useLod.ts 为空)
    - package.json 注册行在;ROADMAP Phase 56 无 TBD
  </acceptance_criteria>
  <done>phase 56 的行为契约成门:scored 链/词表/角标/剧场/桥/零新色/LOD 红线全部机械断言。</done>
</task>

<task type="auto">
  <name>Task 2: phase56-viz.mjs e2e 冒烟</name>
  <files>packages/infinite-canvas/test/e2e/tests/phase56-viz.mjs</files>
  <read_first>packages/infinite-canvas/test/e2e/tests/phase55-nav.mjs (全文——headless 探针/断言结构/testMode 桥), packages/infinite-canvas/test/e2e/ (runner/README 确认执行方式)</read_first>
  <action>
  1. 新建 phase56-viz.mjs(phase55 同范式,5 组断言 × 探针重试):
     ① 画布加载 + L1 缩放:mock/fixture 资产带 aiScore.dimensions ≥3 → hover 卡 → `[data-testid="score-popover"]` 出现(或组件挂载证据);
     ② verdict 角标:graph 含 voice-audit 节点 + 同 shot_id 资产 → 卡上 verdict 环 title 含『耳审』;
     ③ 组视图:双击 character 资产 → theater-shell 出现 + 2×2 网格(或场景画廊);
     ④ G16:open 工作台(store 直驱或入口点击)→ 列表行 ≥1 + 双轨(canvas + 句行)在;
     ⑤ 豁免 mock:勾选 → 批量豁免 → mock g15-ops 200 → 行出现『已豁免』。
  2. 数据注入沿用 55 e2e 的 testMode/fixture 手法(该文件确认;不新造注入机制)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && node test/e2e/tests/phase56-viz.mjs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - e2e exit 0,5/5 断言过(输出逐条)
    - 不新增依赖;探针超时与重试参数沿用 55 范式
  </acceptance_criteria>
  <done>VIZ 三面端到端冒烟就位,后续回归有一键探针。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify 脚本读 KHS_ROOT python 文件 | 只读 regex 提取,零写入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-06-01 | Tampering | KHS_ROOT env 指向恶意路径(读任意文件) | accept | 只读 + 只 regex 提取维度 token;无执行/无写;脚本仓库内运行语境 |
| T-56-06-SC | Tampering | package installs | accept | 零新包 |

**结论:纯断言脚本,无攻击面增量。**
</threat_model>

<verification>
- `npm run verify:phase-56` exit 0
- `node packages/infinite-canvas/test/e2e/tests/phase56-viz.mjs` exit 0
- `cd packages/infinite-canvas && npm test` 全绿 + `npx tsc -b` exit 0(全 phase 终态)
</verification>

<success_criteria>
- Phase 56 GUARD 门就位:七 section 契约 + e2e 冒烟 + ROADMAP 收口
- milestone 末 phase 汇总守护(架构决策 7)可消费 verify:phase-56
</success_criteria>

<output>
Create `.planning/phases/56-creative-visualization/56-06-SUMMARY.md` when done. 记录:七 section 断言数、S-token 白名单裁定(如有)、e2e 五断言的注入手法、ROADMAP 更新 diff 摘要。
</output>
