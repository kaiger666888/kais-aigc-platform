---
phase: 19-integration-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/gold-team/tests/test_regression_verification.py
autonomous: true
requirements: [FIX-03, ENG-04, CLN-01, CLN-02]

must_haves:
  truths:
    - "ACE-Step engine is registered and routes MUSIC tasks correctly"
    - "Cloud engines (Kling/Jimeng/Seedance) are registered with correct backend_type"
    - "Cloud fallback path works when ComfyUI engine is unavailable"
    - "No movie-agent references exist in any source or config file"
  artifacts:
    - path: "docker/gold-team/tests/test_regression_verification.py"
      provides: "Regression verification tests for Phase 15-18 features"
      exports: ["TestACEStepRegression", "TestCloudFallback", "TestMovieAgentRemoval"]
  key_links:
    - from: "test_regression_verification.py"
      to: "src/v6/engines/acestep.py"
      via: "ACEStepEngine registration and MUSIC routing"
      pattern: "ACEStep|acestep"
    - from: "test_regression_verification.py"
      to: "src/v6/engines/cloud_base.py"
      via: "CloudEngine fallback path"
      pattern: "CloudEngine|cloud_base"
---

<objective>
Regression verification for ACE-Step music generation, cloud engine fallback, and movie-agent removal.

Purpose: Proves that Phase 15 fixes (ACE-Step permissions, movie-agent removal) and Phase 18 engine registration (cloud engines) remain intact. These are the critical "don't break what was already working" checks.
Output: Single test file covering ACE-Step, cloud fallback, and movie-agent absence regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From src/v6/engines/acestep.py:
```python
_TASK_TYPE_MAP = {"music": "text2music", ...}
# supported_types includes: "audio_generate", "music_generation", "music", "music_cover"
```

From src/v6/engines/cloud_base.py:
```python
class CloudEngine(BaseEngine):
    _supported_types: list[str] = []
    def _build_request(self, task_type, prompt, width, height, workflow, params) -> dict: ...
```

From src/v6/engines/cloud_jimeng.py:
```python
class JimengCloudEngine(CloudEngine):
    _supported_types = ["image_draw", "image_refine", "video_final"]
```

From src/v6/engines/cloud_kling.py:
```python
class KlingCloudEngine(CloudEngine):
    _supported_types = ["video_final", "video_preview", "image_draw"]
```

From src/v6/engines/cloud_seedance.py:
```python
class SeedanceCloudEngine(CloudEngine):
    _supported_types = ["video_final", "video_preview"]
```
</interfaces>

@docker/gold-team/tests/test_engine_registration.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ACE-Step, cloud fallback, and movie-agent regression tests</name>
  <files>docker/gold-team/tests/test_regression_verification.py</files>
  <behavior>
    - Test 1: ACEStepEngine can be instantiated and has backend_type == BackendType.DOCKER
    - Test 2: ACEStepEngine supported_types includes "music"
    - Test 3: ACEStepEngine _TASK_TYPE_MAP maps "music" to "text2music"
    - Test 4: CloudEngine subclasses (Kling/Jimeng/Seedance) each have backend_type == BackendType.CLOUD
    - Test 5: KlingCloudEngine._supported_types includes "video_final"
    - Test 6: JimengCloudEngine._supported_types includes "image_draw"
    - Test 7: When no ComfyUI engine is registered, a fallback cloud engine handles VIDEO_FINAL
    - Test 8: No file under docker/gold-team/src/ contains "movie-agent" or "movie_agent" (excluding __pycache__)
    - Test 9: No docker-compose*.yml file under docker/ contains "movie-agent" service definition
    - Test 10: No Python import statement references movie_agent or movie-agent module
  </behavior>
  <action>
    Create test_regression_verification.py with three test classes:

    **TestACEStepRegression:**
    - Import ACEStepEngine from src.v6.engines.acestep (or its docker_polling equivalent). If ACEStepEngine is the DockerPollingAPIEngine config wrapper, import that instead.
    - Instantiate and verify backend_type == BackendType.DOCKER.
    - Verify supported_types contains "music".
    - Verify _TASK_TYPE_MAP maps "music" -> "text2music".

    **TestCloudFallback:**
    - Import CloudEngine, KlingCloudEngine, JimengCloudEngine, SeedanceCloudEngine from src.v6.engines.
    - Verify each cloud engine has backend_type == BackendType.CLOUD.
    - Verify each cloud engine's _supported_types list contains the expected task types.
    - For fallback test: create a TaskExecutor with NO ComfyUI engine registered, but register a MockEngine or KlingCloudEngine mock. Submit a VIDEO_FINAL task and verify it resolves to the cloud engine. This proves the fallback path exists when ComfyUI is down.

    **TestMovieAgentRemoval:**
    - Use subprocess to run `grep -r "movie.agent\|movie_agent" docker/gold-team/src/ --include="*.py" --include="*.yaml" --include="*.yml" --include="*.json"` and assert exit code is 1 (no matches). Exclude __pycache__ directories.
    - Run `grep -r "movie-agent" docker/gold-team/docker-compose*.yml` and assert exit code is 1.
    - Run `grep -r "from movie_agent\|import movie_agent" docker/gold-team/src/ --include="*.py"` and assert exit code is 1.
    - These are grep-based assertions using subprocess.run in the test. Use helper function to make assertions readable.

    Follow the existing import patterns: import from src.v6.engines.*, src.v6.engines.base for BackendType, src.v6.executor for TaskExecutor. Use conftest.py autouse fixture for sys.path.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/docker/gold-team && python -m pytest tests/test_regression_verification.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>ACE-Step regression: engine has correct backend_type (DOCKER), supported_types includes music, task type mapping is correct. Cloud fallback: all cloud engines classified as CLOUD backend type, VIDEO_FINAL resolves to cloud engine when ComfyUI is absent. Movie-agent: zero references in source code, Docker Compose, or imports. All tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test -> Source filesystem | Grep-based movie-agent checks scan source files |
| Test -> Executor | Mock engine registration for cloud fallback test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering | Grep subprocess commands | mitigate | Hardcoded grep patterns, no user input |
| T-19-04 | Spoofing | Mock cloud engine pretending to be real | accept | Tests intentionally use mocks to verify routing logic |
| T-19-SC | Tampering | pytest/pip installs | mitigate | Standard test dependencies only |
</threat_model>

<verification>
All existing tests + new tests pass:
```bash
cd /data/workspace/kais-aigc-platform/docker/gold-team && python -m pytest tests/ -v --tb=short 2>&1 | tail -5
```
</verification>

<success_criteria>
- ACE-Step engine regression verified (backend_type, supported_types, task mapping)
- Cloud fallback works when ComfyUI is unavailable
- Zero movie-agent references in codebase
- All existing 74 tests remain green
</success_criteria>

<output>
Create `.planning/phases/phase-19/19-02-SUMMARY.md` when done
</output>
