# Phase 19.1: Close v1.3 gaps: live runtime verification + ACEStepEngine fix + REQUIREMENTS.md reconciliation - Context

**Gathered:** 2026-06-13
**Status:** Ready for planning

<domain>
## Phase Boundary

Close three v1.3 gaps identified after Phase 19 completion:
1. Live runtime verification — spin up gold-team with mock ComfyUI, verify `/health`, `/engines`, task submission via actual HTTP (not just unit tests)
2. ACEStepEngine dual-mode fix — engine.start() currently always launches subprocess, but in Docker it should connect to external `kais-acestep` container
3. REQUIREMENTS.md reconciliation — update requirement statuses to reflect actual completion state

This phase does NOT add new features — it validates existing work at runtime and fixes a known fragility in ACEStepEngine.

</domain>

<decisions>
## Implementation Decisions

### Live Runtime Verification
- Smoke test gold-team API only — spin up gold-team with mock ComfyUI, verify `/health`, `/engines`, task submission endpoint
- Mock/no-GPU mode: test API routing and response structure without actual model inference
- Python smoke test script (`tests/test_live_smoke.py`) using `httpx` against running container
- Verify against docker-compose.v9.yml specifically

### ACEStepEngine Fix
- Detect external container: if `ACESTEP_API_HOST` is not localhost/127.0.0.1, skip subprocess and connect directly to external API
- Health-check the external URL when external mode detected, set `_ready=True`
- Add unit test that verifies `start()` skips subprocess when host is external; test for localhost fallback
- Skip directory check when in external mode — engine doesn't need local files

### REQUIREMENTS.md Reconciliation
- Mark CLN-01/02/03 as COMPLETE — Phase 19 regression tests prove zero movie-agent references
- Mark FIX-01/02 as COMPLETE after ACEStepEngine fix; FIX-03 already verified by Phase 19
- Leave MERGE-01/02/03/04 as PENDING — formal merge process never executed, functionality covered by Phases 17-19
- Keep Phases 15/16 in ROADMAP as historical reference, note requirements satisfied by later phases

### Claude's Discretion
Implementation details for the ACEStepEngine dual-mode detection, smoke test structure, and REQUIREMENTS.md format changes are at Claude's discretion.

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- `docker/gold-team/tests/test_regression_verification.py` — 14 regression tests including ACE-Step tests
- `docker/gold-team/tests/test_integration_pipeline.py` — pipeline chain tests
- `docker/gold-team/tests/test_tasktype_coverage.py` — TaskType routing coverage
- `docker-compose.test.yml` — existing test compose configuration
- `docker-compose.v9.yml` — production compose with `kais-acestep` service (profile: ace)

### Established Patterns
- `ACEStepEngine` in `docker/gold-team/src/v6/engines/acestep.py` — 328 lines, subprocess-based, connects via httpx
- `ACESTEP_API_HOST` env var controls target host — defaults to `127.0.0.1`, set to `kais-acestep` in Docker
- `BaseEngine` has `backend_type` property (returns `MOCK` default) — ACEStepEngine doesn't override it
- 101 existing unit tests all pass (test suite: `cd docker/gold-team && python3 -m pytest tests/ -v`)

### Integration Points
- `ACEStepEngine.start()` — launches subprocess, health-checks `self._base_url` (http://{ACESTEP_HOST}:{ACESTEP_PORT})
- `docker-compose.v9.yml` gold-team env: `ACESTEP_API_HOST: kais-acestep`, `ACESTEP_API_PORT: 8001`
- `kais-acestep` container: standalone REST API on port 8001, Docker profile `ace`, GPU 1 (RTX 3090)
- `REQUIREMENTS.md` traceability table maps 26 requirements to phases

</code_context>

<specifics>
## Specific Ideas

- ACEStepEngine should check if `ACESTEP_API_HOST` resolves to a non-local address — if so, skip subprocess launch and just health-check the external URL
- Live smoke test should verify: `/health` returns 200, `/engines` returns engine list with correct backend_types, task submission accepts valid payload and returns job ID
- REQUIREMENTS.md traceability should add a "Satisfied By" column noting which later phase covered the work

</specifics>

<deferred>
## Deferred Ideas

- MERGE-01/02/03/04 requirements remain PENDING — formal diff report and merge process not executed, but all functionality is present
- Full stack live testing with real ComfyUI + GPU — deferred to operational testing phase
- Phase 15/16 formal execution — kept in roadmap as historical reference, not re-executed

</deferred>
