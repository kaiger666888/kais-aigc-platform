---
phase: 59-narrow-trigger-stale-cascade
plan: "03"
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - packages/infinite-canvas/src/services/canvasApi.ts
  - packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx
  - packages/infinite-canvas/src/components/eventParams/EventParamsPopover.tsx
  - packages/infinite-canvas/src/hooks/useCanvasSocket.ts
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
autonomous: true
requirements: [STALE-01, STALE-02]
must_haves:
  truths:
    - "面板「重生成」请求体携带 regenSource:'panel-regen';事件芯片换 seed 请求体携带 regenSource:'reroll-seed'"
    - "socket node:updated(stale 载荷)到达后画布角标实时出现,无需 reload(FLAG-1 Option A)"
    - "非 stale 载荷的 node:updated 被静默忽略——不开面板、不弹 toast、不改选中(UI-SPEC §5 严格契约)"
    - "orchestrate/CanvasContextMenu 执行请求不含 regenSource(既有调用方零改动)"
  artifacts:
    - path: "packages/infinite-canvas/src/hooks/useCanvasSocket.ts"
      provides: "node:updated 订阅三件套(options/callbacksRef/socket.on)"
      contains: "node:updated"
    - path: "packages/infinite-canvas/src/components/FlowCanvas.tsx"
      provides: "onNodeUpdated → triggerStaleCascade 接线"
      contains: "triggerStaleCascade"
  key_links:
    - from: "packages/infinite-canvas/src/components/FlowCanvas.tsx"
      to: "packages/infinite-canvas/src/hooks/useStale.ts"
      via: "triggerStaleCascade([triggerAssetId])"
      pattern: "triggerStaleCascade"
    - from: "packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx"
      to: "packages/infinite-canvas/src/services/canvasApi.ts"
      via: "executeNode extra.regenSource"
      pattern: "panel-regen"
---

<objective>
客户端窄通道(全 phase 唯一客户端接线,近零而非零——G1 缺口):两条窄路径发射 regenSource 身份标识;useCanvasSocket 新增 node:updated 订阅,FlowCanvas 把 stale 载荷接到既有 triggerStaleCascade(幂等收敛,复用 markStaleDownstream 纯函数),角标/脉动/StaleSection/重跑出口全部零视觉零逻辑改动复用。

Purpose: D-01「客户端复用 socket 级联链」的前提补齐——该链今天不存在(Pitfall 1 / UI-SPEC FLAG-1);autonomous 裁定采 Option A(实时角标,SC1/SC2 e2e 无需 reload)。
Output: 5 个文件的近零改动:1 个类型扩展、2 个一字段发射、1 个订阅三件套、1 个回调接线。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/59-narrow-trigger-stale-cascade/59-CONTEXT.md
@.planning/phases/59-narrow-trigger-stale-cascade/59-RESEARCH.md
@.planning/phases/59-narrow-trigger-stale-cascade/59-PATTERNS.md
@.planning/phases/59-narrow-trigger-stale-cascade/59-UI-SPEC.md

<interfaces>
<!-- 既有真值,执行者直接消费 -->

From packages/infinite-canvas/src/services/canvasApi.ts L372-388(52-02 契约):
- executeNode(projectId, episodesId, nodeId, nodeType, extra?: { prompt?: string; seed?: number; params?: Record<string, unknown> }, cancelToken?) — body 经 `...extra` 平铺透传,加字段即达

From packages/infinite-canvas/src/hooks/useStale.ts L48-60(零改动复用):
- export function triggerStaleCascade(changedAssetIds: string[]): void — 内部 store.markStaleDownstream + useStalePulse.addPulse

From packages/infinite-canvas/src/hooks/useCanvasSocket.ts:
- 订阅三件套范式:onVariantSelected/onGateState(options 接口 L58-81 可选回调声明 + callbacksRef 字面量 L111-122 两处同步 + socket.on 注册 L226-234)
- 坏形状守卫先例:node:created L171-178(node != null && typeof node === 'object' 才转发)
- L157 附近注释:絶不让事件流入 normalizeSocketNodeState(FLAG-3 红线)

From packages/infinite-canvas/src/components/FlowCanvas.tsx:
- useCanvasSocket 调用点 L267-269;onGateState/onVariantSelected 接线范式 L349-376

服务端 wire 契约(59-02 产出):node:updated payload = { node: FlowNodeV2 行, changedFields: ["data.stale"] };node.data.stale = { since: number, triggerAssetId: string, triggerEventId: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: regenSource 发射——canvasApi 类型 + 两个窄触发点</name>
  <files>packages/infinite-canvas/src/services/canvasApi.ts, packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx, packages/infinite-canvas/src/components/eventParams/EventParamsPopover.tsx</files>
  <read_first>
  - packages/infinite-canvas/src/services/canvasApi.ts(被改文件:L372-388 executeNode 52-02 注释段 + L642-649 updateCanvasNode 的「前端 socket 当前未消费该事件」互证注释)
  - packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx(被改文件:L720-740 handleRegenerate;组件头注释地雷 #4 裁定)
  - packages/infinite-canvas/src/components/eventParams/EventParamsPopover.tsx(被改文件:L62-94 handleRerollSeed,L86-87 新 seed 只在请求体的注释)
  - packages/infinite-canvas/src/components/CanvasContextMenu.tsx(L57 既有 executeNode 调用——确认不传 extra,零改动)
  </read_first>
  <action>
  1. canvasApi.ts executeNode 的 extra 类型加 `regenSource?: 'panel-regen' | 'reroll-seed'`,注释在 52-02 段后续写 59 段:服务端在任务成功且携带此标识时标记下游 stale(59-02);orchestrate/ContextMenu 永不携带;展开逻辑零改动(...extra 已透传)。
  2. NodeDetailPanel.tsx handleRegenerate 的 executeNode extra 对象加 `regenSource: 'panel-regen'`(prompt/params 保持不变)。
  3. EventParamsPopover.tsx handleRerollSeed 的 executeNode extra 对象加 `regenSource: 'reroll-seed'`。
  4. canvasApi.ts L642-649 updateCanvasNode doc 注释同步:前端已消费 node:updated(仅 stale 载荷,59-03);乐观更新语义不变。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
  - grep -q "regenSource" packages/infinite-canvas/src/services/canvasApi.ts 且类型为两值字面量联合
  - grep -q "'panel-regen'" packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx
  - grep -q "'reroll-seed'" packages/infinite-canvas/src/components/eventParams/EventParamsPopover.tsx
  - grep -c "regenSource" packages/infinite-canvas/src/components/CanvasContextMenu.tsx 输出 0(既有调用方零改动)
  - npx tsc -b 退出 0
  </acceptance_criteria>
  <done>两条窄路径的身份标识上线;既有 executeNode 调用方(CanvasContextMenu)与 orchestrate 客户端链零改动——SC3 客户端侧结构性保证。</done>
</task>

<task type="auto">
  <name>Task 2: node:updated 订阅 + FlowCanvas 接线(FLAG-1 Option A / FLAG-3 红线)</name>
  <files>packages/infinite-canvas/src/hooks/useCanvasSocket.ts, packages/infinite-canvas/src/components/FlowCanvas.tsx</files>
  <read_first>
  - packages/infinite-canvas/src/hooks/useCanvasSocket.ts(被改文件:options 接口 L58-81、callbacksRef L111-122 两处字面量、socket.on 注册区 L136-244 含 variant:selected L226-234 与 node:created 守卫 L171-178、L157 normalizeSocketNodeState 警告注释)
  - packages/infinite-canvas/src/components/FlowCanvas.tsx(被改文件:useCanvasSocket 调用 L267-269、onGateState/onVariantSelected 接线范式 L349-376)
  - packages/infinite-canvas/src/hooks/useStale.ts(triggerStaleCascade L48-60 本体——零改动复用对象)
  - 59-UI-SPEC.md §5(interaction contract)与 FLAG-3(订阅位置与 normalizeSocketNodeState 红线)
  </read_first>
  <action>
  1. useCanvasSocket.ts 三件套(照 onGateState/onVariantSelected 范式逐处):
     - options 接口加 `onNodeUpdated?: (payload: { node: Record<string, unknown>; changedFields?: string[] }) => void`,doc 注释标注 Phase 59 (D-01):服务端 stale 标记广播;消费方负责形状守卫与级联派发
     - callbacksRef 两处字面量清单同步加 onNodeUpdated(防重连丢回调)
     - socket.on 注册区(variant:selected 旁)加 `socket.on('node:updated', (payload) => { if (payload?.node != null && typeof payload.node === 'object') callbacksRef.current.onNodeUpdated?.(payload) })`——**独立注册,绝不路由进 normalizeSocketNodeState 或任何执行态映射**(FLAG-3 / 52-01 stale 保留红线:stale 载荷若被误映射为执行态,error 时会错清 stale)
  2. FlowCanvas.tsx useCanvasSocket 调用处加 onNodeUpdated 回调(照 onGateState 结构):
     - 轻校验:node 为对象且 node.data?.stale 形状合法(since 为 number、triggerAssetId 为 string)才继续;否则静默 return(非 stale 载荷的 node:updated——如 nodes.ts PATCH——一律忽略:UI-SPEC §5 严格契约,不开面板/不改选中/不弹 toast)
     - 合法则 `triggerStaleCascade([String(node.data.stale.triggerAssetId)])`(import 自 '../hooks/useStale')——内部即 store.markStaleDownstream 纯函数重算 + 脉动,与服务端真相幂等收敛(divergence impossible by construction)
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx tsc -b && npm run build</automated>
  </verify>
  <acceptance_criteria>
  - grep -q "socket.on('node:updated'" packages/infinite-canvas/src/hooks/useCanvasSocket.ts
  - grep -c "onNodeUpdated" packages/infinite-canvas/src/hooks/useCanvasSocket.ts ≥ 4(接口声明 + 两处 callbacksRef + 注册转发)
  - grep -q "triggerStaleCascade" packages/infinite-canvas/src/components/FlowCanvas.tsx 且新回调内无 showToast/setSelected/normalizeSocketNodeState 调用(读源码断言)
  - stale 形状非法时静默 return(读源码断言:校验失败分支无任何 store 写入)
  - npx tsc -b 与 npm run build 退出 0(build 产 dist 为 59-04 e2e 前置)
  </acceptance_criteria>
  <done>G1 缺口闭合:服务端 node:updated → 客户端 triggerStaleCascade 实时链就位;角标/脉动/StaleSection/useStaleRerun 零改动消费;非 stale 载荷零副作用。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| socket 广播 → 客户端 store | node:updated payload 跨端注入画布状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-07 | Tampering | node:updated 消费(useCanvasSocket/FlowCanvas) | mitigate | 轻校验 stale 三字段形状(since number + triggerAssetId string),畸形静默忽略;严格契约限定副作用=仅 stale 级联,无面板/选中/toast 面 |
| T-59-08 | Tampering / DoS | 误入 normalizeSocketNodeState(FLAG-3) | mitigate | 独立订阅注册 + 59-04 e2e SC4 与 error 路径回归(phase52-stale-panel)守 52-01 stale 保留语义 |
| T-59-SC | Tampering | package installs | accept (N/A) | 零安装 |
</threat_model>

<verification>
- `cd packages/infinite-canvas && npx tsc -b && npm run build` 退出 0
- 行为级验证(角标实时出现/SC1/SC2/非 stale 忽略)在 59-04 e2e 落地(mock 回放 node:updated 契约事件)
- 既有 vitest 套件回归:cd packages/infinite-canvas && npx vitest run(无新单测——纯接线,e2e 为验收面)
</verification>

<success_criteria>
- 两条窄路径请求体携带 regenSource;ContextMenu/orchestrate 客户端链零改动
- node:updated → triggerStaleCascade 实时链接通且副作用面严格限定(UI-SPEC §5)
- 全部既有 stale 视觉词汇零改动(UI-SPEC 锁定:NodeBadges/StaleSection/useStaleRerun/pulse)
</success_criteria>

<output>
Create `.planning/phases/59-narrow-trigger-stale-cascade/59-03-SUMMARY.md` when done
</output>
