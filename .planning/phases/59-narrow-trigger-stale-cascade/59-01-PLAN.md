---
phase: 59-narrow-trigger-stale-cascade
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/canvas/v2/import-from-dir.ts
  - src/routes/canvas/_engine.ts
  - src/routes/canvas/_simulate.ts
  - scripts/verify-phase-59.ts
  - package.json
autonomous: true
requirements: [STALE-01, STALE-02]
must_haves:
  truths:
    - "pollEngineTask 对引擎活体形状(outputs.image = /mnt/agents/output/...)返回 /oss/ 开头的 outputUrl,不再恒 null"
    - "submitEngineTask 提交体 params.ref_images(键名,非 reference_images)且值为引擎可见宿主路径"
    - "image_* 任务提交体携带 model_preference:'cloud'"
    - "引擎调用失败时 simulateExecution rethrow,不再降级 simulateOnly 假成功"
    - "成功产物 data.filePath 落库 + node:preview 广播 /oss/ web 路径(reload 可见)"
  artifacts:
    - path: "scripts/verify-phase-59.ts"
      provides: "本 phase 聚合门骨架(S1 路径翻译 + S2 fake 引擎)"
      contains: "verify:phase-59"
    - path: "src/routes/canvas/_engine.ts"
      provides: "ossToEnginePath 入向翻译 + outputs.* 读取 + ref_images/model_preference 提交"
      exports: ["ossToEnginePath", "submitEngineTask", "pollEngineTask"]
    - path: "src/routes/canvas/_simulate.ts"
      provides: "readNode 关系表化 + rethrow + V3 Stage 映射 + overrides + filePath 落库"
  key_links:
    - from: "src/routes/canvas/_engine.ts"
      to: "src/routes/canvas/v2/import-from-dir.ts"
      via: "fsToOssUrl / ossToEnginePath import"
      pattern: "fsToOssUrl"
    - from: "src/routes/canvas/_simulate.ts"
      to: "src/lib/canvasRelationalStore.ts"
      via: "listNodes / upsertNode"
      pattern: "canvasRelationalStore"
---

<objective>
execute 链修真 A(D-06 断点①②④ + ③ 的模拟器侧 + REGEN-02 seed 通道 + Stage 映射)——级联语义依赖「成功」信号为真,本 plan 把引擎适配层与模拟器的数据通道全部对齐真实引擎契约(:8002 活体实证形状),并立起 verify-phase-59 聚合门骨架。

Purpose: D-07 修真优先于接线。不修假成功/恒 null outputUrl,STALE-01/02 的「成功后级联」在引擎故障时是假的(SC5 / ROADMAP 成功标准 5)。
Output: 路径翻译双向函数、_engine 契约对齐、_simulate 真化、scripts/verify-phase-59.ts 骨架 + package.json 注册。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/59-narrow-trigger-stale-cascade/59-CONTEXT.md
@.planning/phases/59-narrow-trigger-stale-cascade/59-RESEARCH.md
@.planning/phases/59-narrow-trigger-stale-cascade/59-PATTERNS.md

<interfaces>
<!-- 执行者直接使用,无需再探索代码库 -->

From src/routes/canvas/_engine.ts (既有,本 plan 修改):
- export type TaskType = "image_draw" | "image_refine" | "image_draw_ipadapter" | "image_pulid" | "video_final" | "video_preview" | "tts" | "tts_zh" | "tts_en" | "tts_bilingual" | "music" | "sfx" | "upscale" | "face_restore" | "controlnet_depth"
- export interface EngineTaskSubmitInput { taskType; prompt; projectId; episodesId; nodeId; referenceImages?: string[]; metadata?: Record<string, unknown>; callbackUrl?: string }
- export interface EngineTaskResult { taskId; status; outputUrl: string | null; raw }
- baseUrl() 运行时读 process.env.GOLD_TEAM_URL → fake 引擎可直接在 verify 进程内 set env 后直调(无需 spawn)

From src/routes/canvas/v2/import-from-dir.ts L194-212 (fsToOssUrl 现体,模块私有):
- 分支序:/oss/ 透传 → ossDir 字面量 "/data/workspace/kais-aigc-platform/data/oss" → _workdirToOss 映射 → http(s) 透传 → null
- /mnt/agents/output 输入今天每分支穿透返回 null(Pitfall 2)

From src/lib/canvasRelationalStore.ts:
- export async function upsertNode(scope, node: FlowNodeV2): Promise<void>  // L91,data 列整列覆盖
- export async function listNodes(scope): Promise<FlowNodeV2[]>  // L162
- scope 形如 { projectId, episodesId }

引擎活体契约(GET :8002/api/v1/tasks/{id},两轮活体实证 + docker/gold-team/src/v6/models/task.py:90-127):
- completed: { status, outputs: { video, thumbnail, audio, image }, metadata: { seed } } — image/thumbnail = "/mnt/agents/output/jimeng_<id>/output.png"
- failed: { status: "failed", error: "Generation timed out" }
- 提交侧 cloud 直通键: prompt / ratio / model_version / ref_images(executor.py:703-717);seed 不在直通表(本地引擎读 params.seed,完成时 metadata.seed)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 双向路径翻译 + verify-phase-59 聚合门骨架</name>
  <files>src/routes/canvas/v2/import-from-dir.ts, src/routes/canvas/_engine.ts, scripts/verify-phase-59.ts, package.json</files>
  <read_first>
  - src/routes/canvas/v2/import-from-dir.ts(被改文件:L175-215 fsToOssUrl 现体 + setWorkdirToOss export-for-verify 注释先例)
  - src/routes/canvas/_engine.ts(被改文件:ossToEnginePath 落点)
  - scripts/verify-phase-58.ts(骨架复刻真值源:TestResult/assert/read/exists/runCmd + WR-01 注释/shadowAssert forced-failure/退出码 0-1-2;L40-43 相对 import flowgraph-v3 先例)
  - src/utils/replaceUrl.ts(L14-21 防穿越 normalize 先例)
  - package.json(scripts L43-51 verify:phase-5x 注册序列)
  </read_first>
  <action>
  1. import-from-dir.ts:`function fsToOssUrl` 改 `export function fsToOssUrl`;在 `/oss/` 透传检查之后插入纯字符串分支——输入 startsWith `/mnt/agents/output/` 时返回 `/oss/` + 去前缀余量(如 `/mnt/agents/output/jimeng_x/output.png` → `/oss/jimeng_x/output.png`)。不依赖 `_workdirToOss` 全局态;http(s) 透传与既有分支全部保持。函数 doc 注释补一行 59-01 断点②动机(引擎 outputs 容器路径翻译,app.ts:74-87 /oss 静态链已能服务)。
  2. _engine.ts 新增并 export `ossToEnginePath(input: string): string | null`(文件头补 node:fs / node:path import 与 `import { fsToOssUrl } from "./v2/import-from-dir"`):
     - 空/非 string → null
     - startsWith `/oss/` → rel = 去前缀;normalized = path.posix.normalize(rel);normalized 为 `..` 或 startsWith `../` → null(防穿越,Security Domain 路径穿越缓解);然后双根探测 fs.existsSync:先 `/mnt/agents/output/` + normalized,再 `/data/workspace/kais-aigc-platform/data/oss/` + normalized(与 fsToOssUrl 的 ossDir 同字面量,引擎容器 Mounts 实证只挂这两个根,Pitfall 3);命中者返回,均不存在 → null
     - 其余输入(宿主绝对路径如 `/data/workspace/kais-hermes-skills/...png`、http(s) URL)原样返回(已是引擎可见形态,kmc 活体先例)
  3. 新建 scripts/verify-phase-59.ts:复刻 verify-phase-58.ts 全骨架(assert 记录 / read / exists / runCmd 带 WR-01 教训注释不经 shell 管道 maxBuffer 16MB / shadowAssert forced-failure / try-catch main + 退出码 0=全绿 1=有红 2=异常)。S1 段按 verify-phase-58.ts L40-43 同款相对路径直接 import 两个函数,断言:
     - fsToOssUrl("/mnt/agents/output/jimeng_x/output.png") === "/oss/jimeng_x/output.png"
     - fsToOssUrl("/oss/a.png") === "/oss/a.png";fsToOssUrl("https://cdn.example/x.png") 原样返回
     - ossToEnginePath("/oss/../../etc/passwd") === null;ossToEnginePath("/oss/a/../../b") === null
     - 用例内 mkdirSync(data/oss/__v59_probe, recursive) + 写临时 t.png:ossToOssPath 断言 ossToEnginePath("/oss/__v59_probe/t.png") === 仓库绝对路径 data/oss/__v59_probe/t.png;finally 清理目录
     - ossToEnginePath("/data/workspace/kais-hermes-skills/x.png") 原样返回
     本任务先落 S1;S2+ 段位注释占位(后续任务填)。
  4. package.json scripts:verify:phase-58 之后加 `"verify:phase-59": "npx tsx scripts/verify-phase-59.ts",`。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npm run verify:phase-59</automated>
  </verify>
  <acceptance_criteria>
  - npm run verify:phase-59 退出码 0,S1 断言全 PASS
  - grep -q "export function fsToOssUrl" src/routes/canvas/v2/import-from-dir.ts
  - grep -q "/mnt/agents/output" src/routes/canvas/v2/import-from-dir.ts
  - grep -q "export function ossToEnginePath" src/routes/canvas/_engine.ts 且 grep -q "posix.normalize" src/routes/canvas/_engine.ts
  - grep -q "verify:phase-59" package.json
  </acceptance_criteria>
  <done>双向翻译函数就位且被 S1 单测锁定;/oss/ 前缀含 `..` 的输入返回 null(穿越拒绝);聚合门骨架可跑、forced-failure 框架在场。</done>
</task>

<task type="auto">
  <name>Task 2: _engine.ts 引擎契约对齐——断点①(outputs.*)②(/oss 翻译)④(ref_images)+ model_preference</name>
  <files>src/routes/canvas/_engine.ts, scripts/verify-phase-59.ts</files>
  <read_first>
  - src/routes/canvas/_engine.ts(被改文件:L63-105 submitEngineTask payload、L130-146 poll completed/failed 分支)
  - 59-RESEARCH.md「Code Examples」节(引擎活体 JSON 两轮实证形状)与 Pattern 3(修正读法)
  - scripts/verify-phase-54.ts L284-328(内联 http stub + 随机端口 + 请求体捕获骨架)
  - scripts/verify-phase-59.ts(Task 1 产物,S2 段落点)
  </read_first>
  <action>
  1. pollEngineTask completed 分支(L132-140)改读引擎真实形状:取 `raw.outputs` 对象,containerPath 依序 `out.image ?? out.video ?? out.audio ?? out.thumbnail`,旧键 `raw.output_url ?? raw.outputUrl` 保留兜底(无害);outputUrl = containerPath 非 null 时 `fsToOssUrl(String(containerPath))`(断点②;/mnt/agents/output 分支生效,http(s) 分支覆盖 cloud 引擎 CDN 直链形态)。
  2. submitEngineTask(L66-82):referenceImages 先经 `ossToEnginePath` 映射并 filter 掉 null;payload params 中键名 `reference_images` 改为 `ref_images`(引擎 v6 cloud 直通表键名,断点④),仅非空数组时展开;在 `...input.metadata` 展开之后追加 image 任务的路由政策:taskType 以 "image" 开头时平铺 `model_preference: "cloud"`——附注释钉 A3 裁定(平台政策 2026-08-19:t2i 5.0 / i2i 4.6 白名单走 :8002 gateway cloud-jimeng;已知边界:cloud 路径 dreamina CLI 不接受 seed,seed 只落 metadata.seed,确定性重放仅本地 ComfyUI 路径成立——VERIFICATION 如实记录,不阻塞)。
  3. seed 无需改签名:调用方经 input.metadata 平铺即达 params.seed(59-02 接线);在 metadata 展开行加一句注释点明该通道(REGEN-02)。
  4. verify-phase-59.ts 增 S2 段:进程内 http.createServer 起 fake 引擎 listen 127.0.0.1:0(verify-phase-54.ts L284-328 骨架),设 `process.env.GOLD_TEAM_URL` 指向它(baseUrl() 调用时读取,直调无需 spawn):
     - POST /api/v1/tasks 捕获请求体入数组,返回 202 与 task_id
     - GET /api/v1/tasks/:id 两模式:completed 返回活体形状(outputs.image 与 outputs.thumbnail 均为 `/mnt/agents/output/jimeng_T6384/output.png`,metadata.seed 42);failed 返回 `{ status: "failed", error: "Generation timed out" }`
     断言:
     - completed 模式 pollEngineTask 返回 outputUrl === "/oss/jimeng_T6384/output.png"(断点①②行为)
     - failed 模式 pollEngineTask 抛错(try/catch 捕获后断言 instanceOf Error)
     - submitEngineTask 传 taskType "image_draw" + referenceImages ["/oss/__v59_probe/t.png"](复用 Task 1 临时文件,不存在则重建)+ metadata { seed: 12345 }:捕获体 params.ref_images 为含宿主路径的数组、params 无 reference_images 键、params.model_preference === "cloud"、params.seed === 12345
     - submitEngineTask 传 taskType "video_final":捕获体 params 无 model_preference 键
     - 段末 delete process.env.GOLD_TEAM_URL 并 server.close(不污染后续段)
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc --noEmit && npm run verify:phase-59</automated>
  </verify>
  <acceptance_criteria>
  - verify:phase-59 S1+S2 全 PASS 退出 0
  - grep -q "ref_images" src/routes/canvas/_engine.ts 且 payload 中对象键 reference_images 消灭(grep -n "reference_images" 仅允许命中注释或无)
  - grep -q "model_preference" src/routes/canvas/_engine.ts
  - npx tsc --noEmit 退出 0(storyboardPreview 等既有调用方签名未变,零破坏)
  </acceptance_criteria>
  <done>引擎适配层对活体契约全对齐:poll 读 outputs.*、产物 URL 为 /oss/ web 路径、提交体 ref_images + 宿主路径 + image 任务 model_preference cloud + seed 通道打通——S2 fake 引擎三模式锁定。</done>
</task>

<task type="auto">
  <name>Task 3: _simulate.ts 真化——readNode 关系表 / rethrow / V3 Stage 映射 / overrides / filePath 落库</name>
  <files>src/routes/canvas/_simulate.ts</files>
  <read_first>
  - src/routes/canvas/_simulate.ts(被改文件:全文 152 行,L18-24 映射表 / L33-53 readNode / L86-148 simulateExecution / L144-147 假成功 catch)
  - src/lib/canvasRelationalStore.ts(listNodes L162 / upsertNode L91 签名与 scope 形状)
  - src/routes/canvas/v2/nodes.ts L180-213(listNodes find → merge → upsertNode 服务端先例)
  - 59-RESEARCH.md「V3 Stage → 引擎 TaskType 映射证据表」+ Pitfall 5/6
  </read_first>
  <action>
  1. readNode(L33-53)整体替换为关系表读法:import { listNodes } from "@/lib/canvasRelationalStore",`const rows = await listNodes({ projectId, episodesId }); const node = rows.find((n) => n.id === nodeId) ?? null;` 返回完整行(供成功块 upsert 复用)。o_agentWorkData blob 查询删除(v2 项目 readNode 恒 null → 恒 simulateOnly 的根因,D-06③)。旧行为注释:legacy-blob-only 项目此后落 simulateOnly 兜底(它们本就无关系表真值)。
  2. NODE_TYPE_TO_TASK_TYPE(L18-24)扩表,照既有字面量风格增五行:global 与 keyframe 均映射 "image_draw"、voice 映射 "tts"、foley 映射 "sfx"、bgm 映射 "music"(Pitfall 6:global 是面板重生成主力资产;storyboard 保持 "image_draw"——ref 化的 image_draw_ipadapter 是 storyboardPreview 专用路径,A4 钉最小实现不解析上游参考)。script 保持短路;mix/composite 有意不进表——在未命中分支加 console.log warn(含 nodeType 值,注明 A2 裁定:引擎无混音/合成 TaskType,维持 simulateOnly 守批量路径稳定)。
  3. simulateExecution 签名扩第 4 参 `overrides?: { prompt?: string; seed?: number; params?: Record<string, unknown>; nodeType?: string }`(可选,既有调用方 orchestrate 不传零影响)。nodeType 解析改为 overrides.nodeType 优先(请求体 V3 Stage 是权威),store node.type 剥 skill 前缀逻辑保留为兜底(Pitfall 6);effective prompt = overrides?.prompt ?? extractPrompt(node ?? {});submitEngineTask metadata 改为 `{ nodeType, originalNodeId, ...(overrides?.seed != null ? { seed: overrides.seed } : {}), ...(overrides?.params ?? {}) }`。
  4. 引擎调用 catch 块(L144-147)去降级:console.error 保留(消息去掉「降级模拟」字样)后 `throw err` rethrow——execute.ts L72-73 / orchestrate.ts L102-108 既有 error 广播接管(D-06③ 断点③)。**唯一合法保留的 simulateOnly 降级**是 GOLD_TEAM_URL 未配置分支(L100-104)与无 prompt 分支(L107-110),原样不动。
  5. 成功块 node:preview 广播后追加产物落库(A1 钉:输出 outputUrl 非 null 时,把 readNode 拿到的行 data 合并 `filePath = result.outputUrl`(/oss/ web 路径,与 import 链 filePath 语义一致)→ upsertNode({ projectId, episodesId }, { ...node, data }));外裹 try/catch console.error——落库失败不得把成功翻成 error。
  6. 文件头注释补 59-01 段:行为变化声明(引擎失败现为真 error 广播;orchestrate 共享本函数,其执行载荷随之变真——Pitfall 5 裁定:orchestrate.ts 自身目标筛选读法本 phase 不动,SC3「零变化」限定为 stale 级联零触发 + 无 regen 通道结构保证)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc --noEmit && npm run verify:phase-59</automated>
  </verify>
  <acceptance_criteria>
  - grep -c "o_agentWorkData" src/routes/canvas/_simulate.ts 输出 0
  - 映射表字面量含 global / keyframe / voice / foley / bgm 五键(grep 各命中)
  - 引擎调用 catch 块含 throw,且不再含 "return simulateOnly"(grep -A3 "引擎调用失败" 验证)
  - grep -q "listNodes" 与 "upsertNode" src/routes/canvas/_simulate.ts(readNode 关系表化 + filePath 落库)
  - tsc --noEmit 退出 0;verify:phase-59 S1/S2 保持全绿(行为级 spawn 断言在 59-02 S3 落)
  </acceptance_criteria>
  <done>模拟器只在水到渠成时成功:关系表真值、V3 Stage 全映射(除 mix/composite 显式 warn)、引擎错误上抛不吞、成功产物 filePath 落库 reload 可见、overrides(prompt/seed/params/nodeType)通道就绪等待 59-02 接线。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 引擎 :8002 → kap 服务端 | outputs.* 字符串是不可信外部输入,直灌前端 URL |
| /oss/ web 路径 → 引擎宿主路径(入向翻译) | 客户端可影响 referenceImages 值,穿越可读任意宿主文件 |
| kap 服务端 → 引擎 :8002(出向) | 提交体经内网 REST,既有信任模型 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Information Disclosure / Tampering | ossToEnginePath(/oss/→宿主) | mitigate | path.posix.normalize + 拒绝 `..` 上溯 + fs.existsSync 双根白名单探测(/mnt/agents/output 与 data/oss),非白名单根不可达(replaceUrl.ts L14-21 先例) |
| T-59-02 | Tampering / XSS | pollEngineTask outputUrl | mitigate | 翻译只接受 /mnt/agents/output 前缀或 http(s) 透传,其余 null;不做 HTML 拼接,前端仅作 img src(React 转义面) |
| T-59-03 | Spoofing | model_preference 常量 | accept | 服务端常量非用户输入;路由政策为平台既定(A3) |
| T-59-SC | Tampering | npm/pip/cargo installs | accept (N/A) | 本 phase 零安装(RESEARCH Package Legitimacy Audit:N/A),无 [ASSUMED]/[SUS] 包 |
</threat_model>

<verification>
- `npm run verify:phase-59`:S1(双向翻译含穿越拒绝)+ S2(fake 引擎 completed/failed/提交体捕获三模式)全 PASS
- `npx tsc --noEmit` 根仓零错(storyboardPreview 等 _engine 既有调用方零破坏)
- 行为级 spawn 断言(③ rethrow 后 error 广播 / seed 到引擎 / filePath reload)在 59-02 S3 落地——本 plan 的静态 + 单元/适配层证据先行
</verification>

<success_criteria>
- 断点①②④+seed 通道+映射缺口全部修复且有自动化证据;断点③模拟器侧(去假成功)完成
- orchestrate.ts 零改动(本 plan 不触碰该文件)
- A1(filePath 落库)/A3(model_preference cloud)/A2(mix|composite simulateOnly+warn)/A4(不解析上游参考)四项钉死裁决全部体现在代码注释中
</success_criteria>

<output>
Create `.planning/phases/59-narrow-trigger-stale-cascade/59-01-SUMMARY.md` when done
</output>
