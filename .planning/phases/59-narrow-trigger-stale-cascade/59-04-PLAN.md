---
phase: 59-narrow-trigger-stale-cascade
plan: "04"
type: execute
wave: 3
depends_on: ["59-02", "59-03"]
files_modified:
  - packages/infinite-canvas/test/e2e/tests/phase59-stale-cascade.mjs
  - packages/infinite-canvas/test/e2e/mock-backend/server.mjs
  - packages/infinite-canvas/test/e2e/probe-59-real.mjs
  - scripts/verify-phase-59.ts
  - .planning/phases/59-narrow-trigger-stale-cascade/59-VALIDATION.md
autonomous: true
requirements: [STALE-01, STALE-02, STALE-03]
must_haves:
  truths:
    - "SC1:mock 下面板 regen 成功后下游节点 svg[aria-label=\"stale\"] 实时出现,无需 reload;getGraph/getMockState 的 stale.triggerAssetId 指向触发资产"
    - "SC2:事件芯片换 seed 成功后同样实时角标;getCalls 断言 body.regenSource='reroll-seed' 且 body.params.seed 为数字"
    - "SC3:orchestrate 与无标记 execute 成功后角标计数不变(负向)"
    - "SC4:stale 节点经既有出口重跑 → node:state success 后角标 toHaveCount(0);无关节点不受波及"
    - "真机 :10588 零足迹探针:窄路径 regen 后 canvas_nodes 出现 data.stale,SC3 真机负向(无标记 execute)零新增,finally 恢复净足迹 0"
  artifacts:
    - path: "packages/infinite-canvas/test/e2e/tests/phase59-stale-cascade.mjs"
      provides: "SC1-SC4 e2e(mock 回放 node:updated 契约)"
    - path: "packages/infinite-canvas/test/e2e/probe-59-real.mjs"
      provides: ":10588 零足迹真机探针(捕获-改-断言-恢复)"
    - path: "scripts/verify-phase-59.ts"
      provides: "终局聚合门(补客户端静态断言 + 全命令门 + forced-failure)"
  key_links:
    - from: "packages/infinite-canvas/test/e2e/mock-backend/server.mjs"
      to: "socket /ws/projects"
      via: "execute mock 对 regenSource 请求回放 node:updated"
      pattern: "regenSource"
    - from: "packages/infinite-canvas/test/e2e/probe-59-real.mjs"
      to: ":10588 /api/canvas/execute + load-v2"
      via: "regenSource POST → 轮询 data.stale → 恢复"
      pattern: "regenSource"
---

<objective>
端到端契约与守护收口:mock-backend 按服务端契约回放 node:updated,phase59-stale-cascade.mjs 锁 SC1-SC4(含 STALE-03 负向);probe-59-real.mjs 真机零足迹实证级联与 SC3 真机负向;verify-phase-59.ts 补客户端静态断言与全命令门,phase 全量回归(phase52 三件套 + phase58)。

Purpose: 三个 requirement 的用户可见证据(角标可见/一键重跑/批量零波及)+ 真机链路实证(SC5);守护传统延续(ROADMAP 架构决策 6)。
Output: 1 个新 e2e + mock 扩展 + 1 个真机探针 + 终局聚合门 + VALIDATION.md 状态更新。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/59-narrow-trigger-stale-cascade/59-CONTEXT.md
@.planning/phases/59-narrow-trigger-stale-cascade/59-RESEARCH.md
@.planning/phases/59-narrow-trigger-stale-cascade/59-PATTERNS.md
@.planning/phases/59-narrow-trigger-stale-cascade/59-UI-SPEC.md
@.planning/phases/59-narrow-trigger-stale-cascade/59-01-SUMMARY.md
@.planning/phases/59-narrow-trigger-stale-cascade/59-02-SUMMARY.md
@.planning/phases/59-narrow-trigger-stale-cascade/59-03-SUMMARY.md

<interfaces>
<!-- 既有测试基建 + 59-02/03 产物,执行者直接消费 -->

mock-backend(server.mjs):
- POST /api/canvas/execute L358-366(logCall 记完整 body;setTimeout 30ms 后广播 node:state success)
- POST /api/canvas/orchestrate L255(mock 一键成片,广播 orchestrate:start/progress/done + 各节点 node:state)
- POST /__mock/emit L380-384(手动广播兜底)
- mock canvas state 经 /__mock/state 读写;resetMock 复位

e2e helpers(helpers.mjs):test.extend / resetMock / loadCanvas / switchToCanvasView / setSelectedNodeIds / nodeSelector / getCalls / getMockState;另有 window.__kaisCanvas.getGraph() 画布桥

e2e 断言范式先例:
- phase52-regen.mjs(getCalls body 断言 + /__mock/state 轮询;L15-16 dist 纪律注释必须复刻进新文件头)
- phase58-recipe.mjs(save-v2 fixture 注入范式——58-03:每用例 POST 写 fixture graph 再 reload)
- phase52-stale-panel.mjs(stale 角标/stale-rerun-btn 断言先例,SC4 复用)

UI-SPEC §8 选择器契约(禁新增 testid):svg[aria-label="stale"] / [data-testid="stale-rerun-btn"] / [data-testid="detail-panel"] / window.__kaisCanvas.getGraph();脉动不断言(decorative,flake-bait)

probe 先例:probe-58-real.mjs(部署纪律头 L15-23 + SKIP 条款 + stripUpdatedAt/firstDiff 工具 L53-82 + 捕获-改-断言-恢复骨架 L85-97)

服务端契约(59-02):node:updated payload { node, changedFields: ["data.stale"] };data.stale = { since: number, triggerAssetId: string, triggerEventId: string }(evt_ 前缀确定性 id)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: mock regenSource 回放 + phase59-stale-cascade.mjs(SC1-SC4)</name>
  <files>packages/infinite-canvas/test/e2e/mock-backend/server.mjs, packages/infinite-canvas/test/e2e/tests/phase59-stale-cascade.mjs</files>
  <read_first>
  - packages/infinite-canvas/test/e2e/mock-backend/server.mjs(被改文件:execute mock L358-366、orchestrate mock L255 起、/__mock/emit L380、mock state 结构)
  - packages/infinite-canvas/test/e2e/tests/phase52-regen.mjs(getCalls/状态轮询两大观测范式 + 文件头 dist 纪律注释)
  - packages/infinite-canvas/test/e2e/tests/phase58-recipe.mjs(save-v2 fixture 注入范式)
  - packages/infinite-canvas/test/e2e/tests/phase52-stale-panel.mjs(stale 断言与 rerun 出口先例)
  - packages/infinite-canvas/test/e2e/helpers.mjs(可用 helper 全集)
  - 59-UI-SPEC.md §5/§7/§8(mock 回放契约形状 + 负向契约 + 选择器锁定)
  </read_first>
  <action>
  1. server.mjs execute mock 扩展(镜像 59-02 服务端契约,52-02 注释风格续 59 段):body 含 regenSource 时,在既有 node:state success 广播**之前**,从 mock canvas state 沿 links 自 nodeId 做下游 BFS(目标限资产节点;data.curation === 'locked' 视作终点跳过;mock 图未建模 isInactive/sequence 边则自然全走——mock 是契约回放非语义重实现,语义在 59-02 服务端断言),对每个命中节点写入 data.stale = { since: Date.now(), triggerAssetId: nodeId, triggerEventId: "evt_" + 中间事件 id(按 mock 图实际事件节点合成,无则 "evt_" + nodeId) },同步更新 mock state 并逐节点 broadcastToProject('node:updated', { node: 更新后节点, changedFields: ['data.stale'] });**body 无 regenSource 时行为与今天完全一致**(SC3 mock 侧负向前提)。
  2. 新建 phase59-stale-cascade.mjs:文件头复刻 phase52-regen L15-16 dist 纪律注释(e2e 跑 build 产物,运行前必须 npm run build);fixture 用 save-v2 注入(58-03 范式):触发资产节点(带 prompt/stage)+ 因果事件 + 下游资产 + link,每用例 resetMock 后注入再 loadCanvas。用例(命名含组词供 -g 过滤):
     - test "panel regen cascades stale to downstream (SC1)":打开触发资产详情面板(复用 phase52 面板交互路径)触发重生成 → expect.poll getCalls 找 /api/canvas/execute body.regenSource === 'panel-regen' → 断言下游节点 svg[aria-label="stale"] 出现(**不 reload**,FLAG-1 Option A 验收)→ getGraph() 下游 stale.triggerAssetId === 触发资产 id;触发节点自身无角标
     - test "seed reroll cascades stale and passes seed (SC2)":事件芯片 popover 触发换 seed → body.regenSource === 'reroll-seed' 且 body.params.seed 为数字 → 下游角标实时出现
     - test "orchestrate does not cascade (SC3-negative)":记录角标计数 → page.request POST /api/canvas/orchestrate(mock L255)等 orchestrate:done → 角标计数不变;再直接 page.request POST /api/canvas/execute **不带 regenSource**(ContextMenu 形状)等 success → 计数仍不变;getMockState 复核零 data.stale 新写
     - test "rerun clears badge via existing exit (SC4)":SC1 前置产生角标后,click [data-testid="stale-rerun-btn"](面板出口)→ 等 orchestrate 子集与 node:state success → 角标 toHaveCount(0);另一用例用 svg[aria-label="stale"] click 出口同断言;无下游关系的节点全程无角标
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm run build && npx playwright test test/e2e/tests/phase59-stale-cascade.mjs</automated>
  </verify>
  <acceptance_criteria>
  - 四用例全绿(-g panel / reroll / orchestrate / rerun-clears 各自可过滤命中)
  - SC1/SC2 断言中无 page.reload()(实时性验收)
  - mock execute 对无 regenSource 请求零 node:updated stale 回放(grep mock 代码:回放逻辑在 regenSource 条件分支内)
  - 无新增 data-testid(UI-SPEC §8;只用既有四选择器)
  </acceptance_criteria>
  <done>STALE-01/02/03 与 SC4 的用户可见行为在 mock 双端(服务端 59-02 dispatch + 客户端 e2e)全部有自动化证据。</done>
</task>

<task type="auto">
  <name>Task 2: probe-59-real.mjs——:10588 零足迹真机探针 + SC3 真机负向</name>
  <files>packages/infinite-canvas/test/e2e/probe-59-real.mjs</files>
  <read_first>
  - packages/infinite-canvas/test/e2e/probe-58-real.mjs(全文 200 行:部署纪律头/SKIP 条款/stripUpdatedAt/firstDiff/捕获-恢复骨架)
  - packages/infinite-canvas/test/e2e/probe-52-real.mjs(loadV2/saveV2 HTTP 形状先例,如有更贴近的 canvas 项目选择逻辑以此为准)
  - scripts/verify-phase-59.ts(59-02 产物——探针与聚合门互不依赖,但 SUMMARY 需互引)
  </read_first>
  <action>
  新建 probe-59-real.mjs,复刻 probe-58-real 骨架:
  1. 文件头部署纪律注释原样复刻(build → deploy-canvas.sh → build:server → :10588 kill+setsid nohup 重启序列);:10588 不可达或重启失败 → 打印 SKIP 理由并 process.exit 非零(RESEARCH Environment Availability fallback 条款——探针延后不阻塞 verify 门,SUMMARY 必须记录)。
  2. 捕获:loadV2 选真项目/真 episodes,找 type/stage 命中映射表的触发节点(优先 asset/storyboard/global)**且存在 ≥1 下游节点**者;originalGraph 快照。
  3. 操作段一(级联实证):POST /api/canvas/execute { projectId, episodesId, nodeId, nodeType, prompt: 节点现有 prompt, regenSource: "panel-regen" } → 轮询 load-v2(≤180s,2s 间隔)直到任一下游节点 data.stale 出现 → 断言 stale.triggerAssetId === 触发节点 id、三字段齐全;同时记录触发节点 data.filePath 是否落库(引擎路径被走到时应有——A1 真机证据;GOLD_TEAM_URL 未配置的环境落 simulateOnly,探针打印实际路径并记录)。
  4. 操作段二(SC3 真机负向):另选一无下游映射节点(或同节点二次)POST execute **不带 regenSource** → 等待完成 → 断言 data.stale 新增行数为 0。
  5. finally:saveV2 恢复 originalGraph;stripUpdatedAt 深比对净足迹 = 0(firstDiff 输出首个差异点);注意引擎侧产生的任务文件(:8002 output)非画布足迹,如实记录任务 id 即可。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && node --check packages/infinite-canvas/test/e2e/probe-59-real.mjs</automated>
  </verify>
  <acceptance_criteria>
  - node --check 语法通过;文件含 SKIP 条款与部署纪律头(grep "SKIP" 与 "deploy-canvas")
  - 恢复逻辑在 finally 且有 stripUpdatedAt 深比对(grep 命中)
  - 真机执行输出(probe 运行):段一出现 triggerAssetId 断言 PASS、段二零新增 PASS、净足迹 0(运行结果记入 SUMMARY;:10588 不可达时记 SKIP)
  </acceptance_criteria>
  <done>真机链路(窄路径 regen → 引擎任务 → 服务端标记 → canvas_nodes data.stale)零足迹实证;SC3 真机负向(无标记 execute)实证。</done>
</task>

<task type="auto">
  <name>Task 3: verify 终局聚合门 + 全量回归 + VALIDATION 收口</name>
  <files>scripts/verify-phase-59.ts, .planning/phases/59-narrow-trigger-stale-cascade/59-VALIDATION.md</files>
  <read_first>
  - scripts/verify-phase-59.ts(59-01/59-02 累计产物:S1-S4 段与 runCmd 骨架)
  - scripts/verify-phase-58.ts(命令门序列 L222-230 与 forced-failure L233-264 完整范式)
  - .planning/phases/59-narrow-trigger-stale-cascade/59-VALIDATION.md(Per-Task Verification Map 待更新行)
  </read_first>
  <action>
  1. verify-phase-59.ts 补客户端静态断言段(S5):read() 检查——useCanvasSocket.ts 含 socket.on('node:updated';FlowCanvas.tsx 含 onNodeUpdated 与 triggerStaleCascade;canvasApi.ts 含 regenSource 类型;NodeDetailPanel.tsx 含 'panel-regen'、EventParamsPopover.tsx 含 'reroll-seed';useCanvasSocket.ts 的 node:updated 转发不在 normalizeSocketNodeState 调用链内(读注册块上下文断言独立 handler)。
  2. 命令门序列(照 58 版):根仓 tsc --noEmit / infinite-canvas tsc -b / flowgraph-v3 tsc --noEmit / 双包 vitest run(flowgraph-v3 全量含 stale.test.ts——D-03 语义基线回归)。
  3. forced-failure 补一项:shadow 断言「orchestrate.ts 含 markStaleDownstream」必须 FAIL。
  4. 全量回归(任务内执行,结果记 SUMMARY):cd packages/infinite-canvas && npm run build && npx playwright test test/e2e/tests/phase59-stale-cascade.mjs test/e2e/tests/phase52-regen.mjs test/e2e/tests/phase52-reroll.mjs test/e2e/tests/phase52-stale-panel.mjs test/e2e/tests/phase58-recipe.mjs(级联改动共享 stale 面,phase52 三件套 + 58 回归是 ROADMAP 相位门要求)。
  5. 更新 59-VALIDATION.md:Per-Task Verification Map 各行 Status 按实际结果置 ✅/❌/⚠(flaky 需注明原因),frontmatter nyquist_compliant: true;Wave 0 五项勾选指向已完成产物。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npm run verify:phase-59 && cd packages/infinite-canvas && npx playwright test test/e2e/tests/phase59-stale-cascade.mjs test/e2e/tests/phase52-regen.mjs test/e2e/tests/phase52-reroll.mjs test/e2e/tests/phase52-stale-panel.mjs test/e2e/tests/phase58-recipe.mjs</automated>
  </verify>
  <acceptance_criteria>
  - npm run verify:phase-59 退出 0:S1-S5 + 命令门 + forced-failure 全绿
  - 全量 e2e 五文件全绿(phase52 三件套零回归证明 52-01 stale 保留红线未破)
  - 59-VALIDATION.md 无 {planner fills} 残留(grep -c "planner fills" 输出 0)
  </acceptance_criteria>
  <done>phase 级守护收口:聚合门可红可绿自证、全量回归绿、验证地图与实物一致——/gsd:verify-work 就绪。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mock 回放 → e2e 断言 | mock 契约必须镜像服务端真实形状,否则 e2e 绿不代表真机绿 |
| probe → 真机 :10588 + 真项目 | 探针写真实 canvas_nodes,必须恢复 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-09 | Tampering | mock node:updated 回放形状 | mitigate | payload 严格镜像 59-02 契约({node, changedFields:['data.stale']} + 三字段);probe 在 :10588 断言真实形状闭环 |
| T-59-10 | Tampering / Integrity | probe 真机足迹 | mitigate | finally saveV2 恢复 + stripUpdatedAt 深比对净足迹 0;SKIP 条款防误跑半配置环境 |
| T-59-SC | Tampering | package installs | accept (N/A) | 零安装(沿用 59-01 审计结论) |
</threat_model>

<verification>
- `npm run verify:phase-59` 全绿(S1-S5 + 命令门 + forced-failure)
- phase59 + phase52 三件套 + phase58 e2e 全绿
- probe-59-real 运行记录(SUMMARY:级联实证/SC3 真机负向/净足迹 0,或 SKIP 理由)
</verification>

<success_criteria>
- STALE-01/02:角标实时可见(mock e2e + 真机探针双证据);STALE-03:三负向(mock e2e + 服务端 dispatch + 真机)全绿
- SC4 经既有出口消除;SC5 断点修复行为级断言(59-02)+ 真机 filePath 证据(本 plan 探针)
- VALIDATION.md 与实物一致,phase 验收材料齐备
</success_criteria>

<output>
Create `.planning/phases/59-narrow-trigger-stale-cascade/59-04-SUMMARY.md` when done
</output>
