---
phase: 59-narrow-trigger-stale-cascade
plan: "02"
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - src/routes/canvas/_stale.ts
  - src/routes/canvas/execute.ts
  - src/routes/canvas/orchestrate.ts
  - scripts/verify-phase-59.ts
  - scripts/verify-59-dispatch.ts
autonomous: true
requirements: [STALE-01, STALE-02, STALE-03]
must_haves:
  truths:
    - "带 regenSource 的 execute 成功后,canvas_nodes 下游资产出现 data.stale 三字段,且逐节点广播 node:updated {node, changedFields:['data.stale']}"
    - "引擎失败 → node:state error 广播,零 stale 写(D-02)"
    - "无 regenSource 的 execute 与 orchestrate → 零 stale 写(SC3 架构性保证)"
    - "body.params.seed 经 overrides 到达引擎提交体 params.seed(REGEN-02 行为级)"
    - "reload(loadFullGraph → migrate)读回服务端所写 stale(D-05 落库保真)"
    - "orchestrate 目标筛选改读关系表(loadFullGraph),谓词逐字不变,零级联结构——SC4 真机链路(重跑下游→orchestrate 子集)不再因 blob 空恒 404"
  artifacts:
    - path: "src/routes/canvas/_stale.ts"
      provides: "markStaleAndBroadcast(pid, eid, changedAssetId) 服务端标记+落库+广播"
      exports: ["markStaleAndBroadcast"]
    - path: "scripts/verify-59-dispatch.ts"
      provides: "spawn 子进程 dispatch 手 harness(隔离 sqlite + socket.io 事件捕获)"
    - path: "src/routes/canvas/execute.ts"
      provides: "regenSource zod 枚举 + overrides 透传 + 成功块标记调用"
      contains: "regenSource"
    - path: "src/routes/canvas/orchestrate.ts"
      provides: "目标筛选 legacy blob → loadFullGraph 关系表读(谓词冻结,零级联结构)"
      contains: "loadFullGraph"
  key_links:
    - from: "src/routes/canvas/execute.ts"
      to: "src/routes/canvas/_stale.ts"
      via: "setImmediate 成功块内条件调用"
      pattern: "markStaleAndBroadcast"
    - from: "src/routes/canvas/_stale.ts"
      to: "packages/flowgraph-v3/ts/src/stale.ts"
      via: "相对深链 import(禁 index.ts)"
      pattern: "markStaleDownstream"
    - from: "src/routes/canvas/_stale.ts"
      to: "src/lib/canvasRelationalStore.ts"
      via: "loadFullGraph/listNodes/upsertNode"
      pattern: "upsertNode"
    - from: "src/routes/canvas/orchestrate.ts"
      to: "src/lib/canvasRelationalStore.ts"
      via: "loadFullGraph 关系表读(替代 o_agentWorkData blob,谓词不变)"
      pattern: "loadFullGraph"
---

<objective>
服务端级联接线(D-01/D-02/D-03/D-05 + SC3 负向锁死)——_stale.ts markStaleAndBroadcast(纯函数级联 + 落库 + node:updated 广播),execute.ts 携 regenSource 契约与 seed 透传并在成功判定处接线;orchestrate.ts 目标筛选换关系表读(谓词冻结)。以 spawn dispatch 手 harness 完成 D-02/SC3/seed/reload 四组行为级断言。

Purpose: STALE-01/02 的服务端半边;STALE-03 的架构性保证(orchestrate 不经 executeNode-extra 通道)用负向断言锁死;orchestrate 关系表化打通 SC4 真机链路(52-02 stale-success 谓词在 save-v2 项目上真正生效)。
Output: _stale.ts 新服务端接缝、execute.ts 契约扩展、orchestrate.ts 数据源真化、verify S3/S4 行为段 + verify-59-dispatch.ts harness。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/59-narrow-trigger-stale-cascade/59-CONTEXT.md
@.planning/phases/59-narrow-trigger-stale-cascade/59-RESEARCH.md
@.planning/phases/59-narrow-trigger-stale-cascade/59-PATTERNS.md
@.planning/phases/59-narrow-trigger-stale-cascade/59-01-SUMMARY.md

<planner_calls>
裁定记录(SUMMARY 需如实复述):
1. **任务序 = 接口先行**:_stale.ts(Task 1)先于 execute.ts(Task 2)落地——Task 2 的 import { markStaleAndBroadcast } from "./_stale" 在模块缺失时 tsc 必红,先建被消费方。
2. **Open Question 3 采纳(orchestrate 关系表化,orchestrator 裁决)**:orchestrate.ts 目标筛选 L34-47 的 o_agentWorkData blob 读取换 loadFullGraph 关系表读,谓词(L58-59 success/cached 且无 stale 才跳过)逐字冻结。理由:(a) save-v2 只写关系表,v2 项目 blob 恒空 → orchestrate 现状对现代项目恒 404,SC4 的「重跑下游(useStaleRerun → orchestrate 子集)」真机链路死路;(b) 52-02 stale-success 谓词只有在真数据源上才生效;(c) 本 plan 的 S4-orchestrate 负向断言必须非空洞——orchestrate 真执行了目标(有 success 广播)且零 stale,而不是 404 空转的假绿。SC3「零变化」限定为其本义:stale 级联零触发 + 无 regen 通道结构保证(静态负向锁这个);数据源真化对 orchestrate 执行载荷/失败可见性的改善是 59-01 断点③的自然外溢,如实记录进 SUMMARY/VERIFICATION。
3. **结构冻结**:orchestrate.ts 除数据源读取段外行为面冻结——不得出现 markStaleDownstream import、不得消费 regenSource;broadcastToProject node:state 循环(L95-109)不动。
4. **A3 语义如实记录**:model_preference 已由 59-01 在 _engine 提交层钉 cloud(image_* 任务);S3 捕获断言复核。cloud-jimeng 无 seed 参数——seed 只落 metadata.seed,换 seed 重跑语义靠非确定性达成,不承诺确定性重放(SUMMARY 与 59-VERIFICATION 照此措辞,禁止伪装确定性)。
</planner_calls>

<interfaces>
<!-- 59-01 产物 + 既有真值,执行者直接消费 -->

From 59-01(_simulate.ts 新签名):
- simulateExecution(projectId, nodeId, episodesId = 0, overrides?: { prompt?: string; seed?: number; params?: Record<string, unknown>; nodeType?: string })
- metadata 组包: { nodeType, originalNodeId, ...(overrides?.seed != null ? { seed: overrides.seed } : {}), ...(overrides?.params ?? {}) };成功产物 node:preview + data.filePath 落库

From packages/flowgraph-v3/ts/src/stale.ts:
- export function markStaleDownstream(graph: FlowGraphV3, changedAssetIds: string[], now: number): FlowGraphV3
- export function getDownstreamIds(graph: FlowGraphV3, nodeId: string): string[]
- 语义(宪法 §13,零改动复用):传递闭包;sequence 边/isInactive 置灰边不传播;curation:'locked' 是传播终点;环防御;最早 since 保留

From packages/flowgraph-v3/ts/src/migrate.ts:
- export function migrateV2toV3(graph) → { graph } — 事件 id 确定性合成 `evt_${n.id}`(L523),与客户端 adaptV2Graph 同规则 → triggerEventId 跨端一致

From src/lib/canvasRelationalStore.ts:
- loadFullGraph(scope) → FlowGraphV2 | null({meta.version:'2', nodes, links, branches, variantGroups};全空返回 null)
- listNodes(scope) → FlowNodeV2[];upsertNode(scope, node) — data 列整列覆盖
- scope 形如 { projectId: number, episodesId: number }

From src/utils/ws.ts:
- setIo(io: Server);broadcastToProject(projectId, event, data) — /ws/projects 命名空间 room `project:{id}`
- 根仓 package.json 已有 socket.io 与 socket.io-client ^4.8.3(dispatch 子进程可建 Server+真客户端捕获广播)

From src/routes/canvas/orchestrate.ts(修改对象):
- L34-47 o_agentWorkData blob 读取(替换目标);L58-59 目标筛选谓词(逐字冻结):success/cached 且【无】stale 才跳过;L95-109 node:state success/error 广播循环(不动);body schema: { projectId, episodesId, nodeIds? }

node:updated wire 契约(v2/nodes.ts L210-213 先例):{ node, changedFields }
data.stale wire 三字段(v3/serialize.ts L276-281):{ since, triggerAssetId, triggerEventId } — 缺一 migrate 还原为 null
DB 注入机制:src/utils/getPath.ts 以 process.cwd()/data 为基 → spawn 子进程 cwd=mkdtemp 临时目录即得隔离空库 db2.sqlite(db.ts 自动建)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: _stale.ts——markStaleAndBroadcast 服务端标记接缝(D-01/D-03/D-04/D-05)</name>
  <files>src/routes/canvas/_stale.ts</files>
  <read_first>
  - packages/flowgraph-v3/ts/src/stale.ts(级联语义真值源全文——宪法 §3/§13 注释即契约)
  - packages/flowgraph-v3/ts/src/migrate.ts(L523 evt_ 合成;migrate runtime 依赖仅 integrity.js+recipe.js)
  - src/lib/canvasRelationalStore.ts(loadFullGraph L987 / listNodes L162 / upsertNode L91)
  - src/routes/canvas/v2/nodes.ts L180-213(落库+广播序列先例:先 upsert 后 broadcastToProject("node:updated"))
  - 59-RESEARCH.md Pattern 2(本函数骨架全文)+ Anti-Patterns(禁 import index.ts)
  - src/routes/canvas/v2/import-from-dir.ts L81(跨包相对深链运行时 import 先例)
  </read_first>
  <action>
  新建 src/routes/canvas/_stale.ts,文件头块注释照 _engine.ts/_simulate.ts 下划线模块范式(Phase 59 + D-01/D-03/D-04/D-05 决策编号 + 配置说明)。具名导出 `markStaleAndBroadcast(projectId: number, episodesId: number, changedAssetId: string): Promise<void>`:
  1. import 深链(**禁 index.ts**——会拉包内 zod 3.23.8 与根仓 4.3.6 冲突,Anti-Pattern #2):`import { markStaleDownstream } from "../../../packages/flowgraph-v3/ts/src/stale";` 与 `import { migrateV2toV3 } from "../../../packages/flowgraph-v3/ts/src/migrate";`(src/routes/canvas/ → 3 级上溯到仓根);`import { loadFullGraph, listNodes, upsertNode } from "@/lib/canvasRelationalStore";`;broadcastToProject from "@/utils/ws"。
  2. 函数体按 RESEARCH Pattern 2:`const v2 = await loadFullGraph({ projectId, episodesId }); if (!v2) return;` → `const { graph: v3 } = migrateV2toV3(v2 as any)`(事件 id 确定性 evt_<nodeId>,客户端同款)→ `const next = markStaleDownstream(v3, [changedAssetId], Date.now())`。
  3. diff 只取增量:prevById = v3 节点 Map;newlyStale = next.nodes 中 kind === "asset" 且 stale != null 且 prev.stale == null 者(**只写新增,绝不覆盖既有 since**——重复触达保最早时间戳)。
  4. 逐节点落库+广播(顺序:先落库后广播,Pitfall 4):`const existing = await listNodes(scope)` 找同 id 行(找不到 skip);`data = { ...(row.data ?? {}) }` 合并 `data.stale = { since, triggerAssetId, triggerEventId }`(三字段整包,serialize.ts:276-281 同款 wire 形状);`await upsertNode(scope, { ...row, data })` → `broadcastToProject(projectId, "node:updated", { node: { ...row, data }, changedFields: ["data.stale"] })`。
  5. 错误上抛(由 execute.ts 标记位 try/catch 接)——本模块不做静默吞错。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
  - grep -q "flowgraph-v3/ts/src/stale" 与 "flowgraph-v3/ts/src/migrate" src/routes/canvas/_stale.ts,且 grep -c "ts/src/index" 输出 0(禁 index 深链)
  - grep -q "changedFields: \[\"data.stale\"\]" src/routes/canvas/_stale.ts(wire 契约)
  - grep -q "triggerEventId" src/routes/canvas/_stale.ts(三字段整包)
  - 落库在广播之前(读源码顺序断言:upsertNode 调用行号 < 对应 broadcastToProject 行号)
  - tsc --noEmit 退出 0
  </acceptance_criteria>
  <done>唯一新增服务端接缝就位:纯函数级联(宪法 §13 语义单点复用)+ 增量落库 + 既有格式 node:updated 广播;错误上抛不吞。</done>
</task>

<task type="auto">
  <name>Task 2: execute.ts 契约接线(regenSource + overrides 透传 + 成功块标记)+ orchestrate.ts 关系表读</name>
  <files>src/routes/canvas/execute.ts, src/routes/canvas/orchestrate.ts</files>
  <read_first>
  - src/routes/canvas/execute.ts(被改文件:全文 85 行;L24-28 params zod 注释范式、L31 解构丢弃点、L68-75 setImmediate 成功/失败广播缝)
  - src/routes/canvas/orchestrate.ts(被改文件:L34-47 blob 读取段、L58-59 谓词逐字冻结、L95-109 广播循环不动)
  - src/routes/canvas/_simulate.ts(59-01 修订后现状:overrides 通道与 metadata 组包——透传形状消费端)
  - src/middleware/middleware.ts L8-27(safeParse 不 strip——regenSource 漏声明不 400,Pitfall 8)
  - 59-RESEARCH.md Pattern 1/4 + Pitfall 5(orchestrate 数据源裁决)
  </read_first>
  <action>
  1. execute.ts zod schema(L24-28 params 声明之后)加 `regenSource: z.enum(["panel-regen", "reroll-seed"]).optional()`,注释照 52-02 params 段风格续写 59 段:regen 身份标识(Pattern 4);validateFields 不 strip 未知键,此声明为契约诚实 + 类型提示 + zod 白名单枚举(Security V5,客户端可伪造信号,仅作标记依据绝不当权限依据);orchestrate/CanvasContextMenu 永不携带 → SC3 零波及是架构性保证而非行为过滤(D-01)。
  2. L31 解构改为 `const { projectId, episodesId, nodeId, nodeType, prompt, branchId, params, regenSource } = req.body;`(seed 不再被丢弃——REGEN-02);同步更新 52-02 注释块(params 自本 phase 起被 handler 消费)。
  3. setImmediate 块(L68-75)改:simulateExecution 调用传入 overrides `{ prompt, seed: typeof params?.seed === "number" ? params.seed : undefined, params, nodeType: effectiveType }`;成功路径在 `await simulateExecution(...)` 之后、success 广播之前插标记——`if (regenSource) { try { await markStaleAndBroadcast(projectId, episodesId, nodeId); } catch (e) { console.error("[canvas:execute] stale 标记失败:", e); } }`(标记自身失败不把成功翻成 error);catch 路径(引擎失败)**不进任何标记**(D-02 结构性)。文件头 import 块加 `import { markStaleAndBroadcast } from "./_stale";`。
  4. orchestrate.ts:L34-47 blob 查询 + JSON.parse 替换为 `import { loadFullGraph } from "@/lib/canvasRelationalStore"` + `const graph = await loadFullGraph({ projectId: Number(projectId), episodesId: Number(episodesId) })`;null → 维持原 404 文案「画布数据不存在,请先保存」;`allNodes = graph.nodes`;后续 filter/谓词(L58-59)/拓扑排序/广播循环全部逐字不动。**结构冻结**:本文件不得出现 markStaleDownstream import 与 regenSource 消费(本 plan Task 3 + 59-04 静态负向锁)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
  - grep -q "regenSource: z.enum" src/routes/canvas/execute.ts
  - grep -q "markStaleAndBroadcast" src/routes/canvas/execute.ts 且仅出现在 setImmediate 成功分支(catch 分支无标记调用)
  - grep -q "params, regenSource" src/routes/canvas/execute.ts(解构不再丢弃 seed 通道)
  - grep -c "o_agentWorkData" src/routes/canvas/orchestrate.ts 输出 0;grep -q "loadFullGraph" src/routes/canvas/orchestrate.ts;谓词行 `n.data != null && n.data.stale != null` 逐字保留
  - grep -c "markStaleDownstream\|_stale\|regenSource" src/routes/canvas/orchestrate.ts 输出 0(零级联结构)
  - tsc --noEmit 退出 0
  </acceptance_criteria>
  <done>execute 路由层完成 regen 契约(窄路径身份标识进 zod、seed/params/nodeType 透传、成功块条件标记、失败路径结构性无标记);orchestrate 数据源统一关系表,stale-success 谓词真正生效,级联结构零引入。</done>
</task>

<task type="auto">
  <name>Task 3: verify S3/S4——spawn dispatch 行为断言(D-02/SC3 负向三件套/seed/reload 保真)</name>
  <files>scripts/verify-phase-59.ts, scripts/verify-59-dispatch.ts</files>
  <read_first>
  - scripts/verify-phase-54.ts L284-328(内联 http stub + spawn 注入 env 骨架)
  - scripts/verify-phase-49-bridge.ts(http mock + 断言组织)与 STATE.md 49-01 教训(端点 dispatch 必须 spawn 子进程——app-db knex 池不落共享进程)
  - scripts/verify-phase-59.ts(59-01 产物,S3/S4 段落点与 fake 引擎复用)
  - src/utils/getPath.ts(cwd 基目录机制)+ src/utils/db.ts(db2.sqlite 自动建)
  - src/utils/ws.ts(setIo/命名空间)
  - src/routes/canvas/orchestrate.ts(dispatch 模式所需的 body schema)
  - src/lib/canvasRelationalStore.ts(图 seed 原语:meta/node/link 写入函数名以现场为准)
  - packages/flowgraph-v3/ts/src/migrate.ts L440-540(V2 节点/边如何合成 evt_ 事件与因果边——fixture 的 input/output link role 用法)
  </read_first>
  <action>
  1. 新建 scripts/verify-59-dispatch.ts(短命子进程脚本,tsx 直跑):
     - 入参 env:DISPATCH_MODE ∈ {cascade, engine-fail, no-marker, orchestrate};GOLD_TEAM_URL 由父进程注入
     - cwd 由父进程 spawn 指向 mkdtemp 临时目录(getPath 以 cwd/data 为基 → 隔离空库自动建)
     - 启 socket.io Server listen 127.0.0.1:0 → setIo(server);`server.of("/ws/projects").on("connection", s => s.join("project:" + s.handshake.query.projectId))`;再以 socket.io-client 连自身并带 query {projectId: 990059},收集事件到数组
     - seed fixture 图(经 canvasRelationalStore 原语写入,函数名读现场):trigger 资产 id "trig-1" type "asset"(data 含 prompt)、事件节点 id "evt-node-1"、下游资产 id "down-1"、input/output 两条因果 link(角色用法照 migrate.ts 因果边规则,seed 后可先 migrateV2toV3 + getDownstreamIds 自检形状,不对修 fixture 不改产品代码)+ meta
     - dispatch:import execute 路由 default handler,以 mock req { body } / res { status: () => ({ send: () => {} }) } 直调(49-01 dispatch 范式);cascade/engine-fail 模式 body 带 { projectId: 990059, episodesId: 1, nodeId: "trig-1", nodeType: "asset", prompt: "v59 probe", regenSource: "panel-regen", params: { seed: 777 } };no-marker 模式去掉 regenSource/params;orchestrate 模式改调 orchestrate handler,body { projectId: 990059, episodesId: 1, nodeIds: ["down-1"] }(blob 从未写入——关系表是唯一数据源,数据源真化的行为级证明)
     - dispatch 后轮询收集事件至收到 node:state success/error 或 15s 超时;随后 loadFullGraph 读回;stdout 打印单行 JSON { events, staleRows: [{id, stale}], httpStatus };process.exit 强退不等 knex pool(49-01 教训注释)
  2. verify-phase-59.ts 增 S3/S4 段:fake 引擎常驻本进程(复用 S2 server,completed/failed 模式可切换,POST 捕获体累积);逐模式 spawnSync `npx tsx scripts/verify-59-dispatch.ts` { cwd: 临时目录, env: { ...process.env, GOLD_TEAM_URL: stubUrl, DISPATCH_MODE }, timeout 120s};断言:
     - S3-cascade(completed):events 含 node:state success、含 ≥1 条 node:updated 且 changedFields 为 ["data.stale"]、其 node.data.stale.triggerAssetId === "trig-1";staleRows 含 down-1 且 stale 三字段齐全(**同一断言即 D-05 reload 保真**——loadFullGraph 是 load-v2 数据源);fake 引擎捕获体 params.seed === 777(REGEN-02 行为级)
     - S3-engine-fail(failed):events 含 node:state error、无 success、无 node:updated、staleRows 为空(D-02 负向:失败零标记)
     - S4-no-marker(completed):events 含 success、零 node:updated、staleRows 为空(负向 #1:ContextMenu 路径)
     - S4-orchestrate(completed):**非空洞负向**——httpStatus 200 且 events 含 down-1 的 node:state success(证明 orchestrate 真执行了关系表目标,而非 404 空转)且 staleRows 为空(负向 #2:SC3)
     - 静态断言:execute.ts 含 regenSource: z.enum;orchestrate.ts 含 loadFullGraph 且不含 markStaleDownstream/"./_stale"/regenSource 且不含 o_agentWorkData;_stale.ts 不含 ts/src/index
     - forced-failure(shadowAssert):「no-marker 模式 staleRows.length > 0」必须 FAIL、「engine-fail 模式出现 success 事件」必须 FAIL、「orchestrate 模式 httpStatus 404」必须 FAIL——证明门能红
  3. 全段跑完清理临时目录。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npm run verify:phase-59</automated>
  </verify>
  <acceptance_criteria>
  - verify:phase-59 退出 0,S1-S4 全 PASS 含 forced-failure 组
  - S3-cascade 同时覆盖:success + node:updated 契约形状 + DB stale 三字段 + 引擎体 seed === 777
  - 三条负向(engine-fail 零标记 / no-marker 零标记 / orchestrate 真执行且零标记)全部有断言且 PASS;orchestrate 负向含 httpStatus 200 + 目标 success 的非空洞前提
  - scripts/verify-59-dispatch.ts 含 process.exit 强退与 knex 池教训注释
  </acceptance_criteria>
  <done>D-01/D-02/SC3/REGEN-02/D-05 全部有行为级自动化证据;orchestrate 数据源真化经 dispatch 行为级证明(blob 从未写入仍 200 执行);级联语义与客户端收敛一致的前提(evt_ 确定性 id + 同一纯函数)在 dispatch fixture 上实证。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端 → execute 路由 | regenSource/params 均客户端可伪造字段 |
| 服务端标记 → 所有连接客户端 | node:updated 广播进各端 store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-04 | Spoofing / Elevation of Privilege | regenSource(execute.ts) | mitigate | zod enum 白名单两值;仅作标记信号绝不当权限依据(RESEARCH Security V4);最坏语义=多显示一个 stale 角标,重跑仍走既有鉴权通道 |
| T-59-05 | Tampering | node:updated 广播 payload | accept | broadcastToProject room 按 projectId 隔离的既有信任模型不变;消费侧(59-03)轻校验 stale 形状,畸形静默忽略;增量写不覆盖既有标记 |
| T-59-06 | Repudiation | 假成功掩盖引擎故障 | mitigate | D-02 结构性(catch 不进标记)+ S3-engine-fail 负向断言(零 stale + error 广播)锁死 |
| T-59-07 | Tampering | orchestrate 数据源切换 | mitigate | 谓词逐字冻结 + 静态断言(无 markStaleDownstream/_stale/regenSource)+ S4-orchestrate 非空洞行为负向;执行载荷变化仅是 59-01 断点③真化的自然外溢,如实记录 |
| T-59-SC | Tampering | package installs | accept (N/A) | 零安装(沿用 59-01 审计结论) |
</threat_model>

<verification>
- `npm run verify:phase-59`:S1/S2(59-01)+ S3/S4(本 plan)全 PASS,forced-failure 自检红绿可证
- `npx tsc --noEmit` 退出 0
- 静态:`grep -c "o_agentWorkData" src/routes/canvas/orchestrate.ts` 输出 0;orchestrate 无 markStaleDownstream/regenSource;execute 标记调用仅在成功分支
</verification>

<success_criteria>
- 带标记的窄路径成功 → 下游 DB stale + node:updated 契约广播,行为级断言绿
- 失败/无标记/orchestrate 三负向全绿(STALE-03 架构性保证锁死,orchestrate 负向非空洞)
- seed 行为级到达引擎体;reload 保真(同一 loadFullGraph 断言)实证
- orchestrate 关系表化:SC4 真机链路(重跑下游→orchestrate 子集)打通,52-02 stale-success 谓词生效
</success_criteria>

<output>
Create `.planning/phases/59-narrow-trigger-stale-cascade/59-02-SUMMARY.md` when done
</output>
