---
phase: 60-post-save-panel-persistence
plan: "03"
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - packages/infinite-canvas/src/store/canvasStore.ts
  - packages/infinite-canvas/src/store/__tests__/reloadAnchor.test.ts
autonomous: true
requirements: [PANEL-02]
must_haves:
  truths:
    - "他端保存触发的整图 reload 后,detailNode 与 selectedNode 按 id 重锚到同一节点,panelWidth/tab 内部态不丢(PANEL-02/D-02/D-07)"
    - "重锚找不到同 id 节点时,面板与选中同时诚实收起并 console.warn 一次,无模糊匹配(D-03)"
    - "save→reload roundtrip 零 id 漂移有可重复门(真机 --strict 或 in-memory vitest 锁,按诊断分支二选一或双备)"
  artifacts:
    - path: "packages/infinite-canvas/src/store/canvasStore.ts"
      provides: "setGraph 锚丢失 console.warn(D-03 验收钩子)"
      contains: "[panel-persist]"
    - path: "packages/infinite-canvas/src/store/__tests__/reloadAnchor.test.ts"
      provides: "D-03/D-07 对称锁 + warn 行为锁 + roundtrip in-memory 锁"
  key_links:
    - from: "canvasStore setGraph"
      to: "vm.rfNodes.find(n => n.id === …) ?? null"
      via: "id 重锚语义(L442-447 保持不动,warn 为纯增量)"
      pattern: "rfNodes\\.find"
    - from: "scripts/diagnose-60-roundtrip.ts --strict"
      to: "60-03 Task 2 分支裁定"
      via: "60-DIAGNOSIS.md Fix branch A/B 行"
      pattern: "Fix branch"
---

<objective>
PANEL-02 的 reload 侧收口: D-03 锚丢失诚实收起(+console.warn 验收钩子)+ D-07 selected/detail
对称锁 + 按 60-01 诊断裁定的证据驱动修复分支(A: setGraph 语义已对,仅锁不修 / B: 在诊断点名层
修 id 漂移)。D-02 的对齐基准就是 setGraph L442-447 既有语义——本 plan 不重写它,只补 warn 与锁。

Purpose: 「重载恢复的锚定与保存前语义等价」从 store 假说变为被锁死的行为;锚丢失场景诚实可验收。
Output: setGraph warn 增量 + 扩充的 reloadAnchor 永久锁套件 + (分支 B 时)诊断点名层的 id 稳定性修复。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-post-save-panel-persistence/60-CONTEXT.md
@.planning/phases/60-post-save-panel-persistence/60-RESEARCH.md
@.planning/phases/60-post-save-panel-persistence/60-UI-SPEC.md
@.planning/phases/60-post-save-panel-persistence/60-DIAGNOSIS.md
@.planning/phases/60-post-save-panel-persistence/60-01-SUMMARY.md

<interfaces>
<!-- 修改点与锁目标(已提取) -->

canvasStore.ts setGraph L424-449 现状(保持语义,只加 warn):
  selectedNode: state.selectedNode ? vm.rfNodes.find((n) => n.id === state.selectedNode!.id) ?? null : null
  detailNode:   state.detailNode   ? vm.rfNodes.find((n) => n.id === state.detailNode!.id)   ?? null : null

NodeDetailPanel.tsx(锁目标的行为依据,不修改):
  L67 useEffect(() => { setTab('detail') }, [node?.id])  → 同 id 重锚不重置 tab(PANEL-02 断言基础)
  L40-44 panelWidth useState(() => Math.max(400, 480))   → unmount 才会丢宽度(无重置即未 unmount)
  L103 data-testid="detail-panel"(phase35 契约,禁改)

诊断消费契约: 60-DIAGNOSIS.md 最终裁定节
  「Pinned cause: …」「Fix branch: A / B(@文件+原因)」 —— Task 2 的唯一分支选择器

roundtrip in-memory 锁链(分支 A/B 共用):
  adaptV2Graph(wire) → serializeGraphToV2(adapted.graph, adapted.rawDataByNodeId) → adaptV2Graph(wire2)
  断言两次 adapted 的节点 id 集全等(纯函数级,不依赖 :10588)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-03 锚丢失 warn + D-07 对称锁</name>
  <files>packages/infinite-canvas/src/store/canvasStore.ts, packages/infinite-canvas/src/store/__tests__/reloadAnchor.test.ts</files>
  <read_first>
  - packages/infinite-canvas/src/store/canvasStore.ts L418-493(setGraph 全段——warn 插入点)
  - packages/infinite-canvas/src/store/__tests__/reloadAnchor.test.ts(60-01 Task 2 产物——本任务在其上扩充)
  - 60-UI-SPEC.md §1(stay-mounted 契约)与 §2(anchor-loss 契约)与 §7(D-03 验收钩子=console 捕获)
  </read_first>
  <action>
  (1) canvasStore.ts setGraph: 在 set() 更新器内,对 selectedNode 与 detailNode 各自的重锚计算处——当 state 中该锚非 null 且 vm.rfNodes.find 按 id 未命中(即将置 null)时,console.warn 一次,文案按 UI-SPEC 默认串: '[panel-persist] 锚点丢失: ' + 丢失节点id + ' 在重载图中未找到,面板已收起'。防刷屏守卫: 仅在「非 null → null」转移时发(锚本来就是 null 的 setGraph 不发)——实现上即比较 state.X 非 null 与 find 结果为 undefined 的合取,天然只在转移时命中,无需额外状态。
  (2) 重锚语义(L442-447)与其余 setGraph 行为零改动——D-02 对齐目标就是现状,warn 是纯增量;不加 assetKey/模糊匹配(D-03 明令禁止)。
  (3) reloadAnchor.test.ts 扩充四个用例(在 60-01 三 case 基础上):
      warn-on-miss: vi.spyOn(console, 'warn') mock 后,setGraph(g3)(锚定的 down-1 缺席)→ expect warn 被调且调用串含 '[panel-persist]' 与 'down-1';
      symmetric-collapse(D-07 锁): 同一次 setGraph(g3) 后 selectedNode 与 detailNode 同时为 null(已在 60-01 collapse case,补显式断言两值同时 null—— together or not at all);
      no-warn-on-hit: setGraph(g2)(id 命中)→ warn 零调用;
      no-warn-spam: 连续两次 setGraph(g3)(同一缺失锚)→ warn 恰一次(第二次不再发——非 null→null 转移已消费)。
      spy 用 afterEach restore。
  </4) UI-SPEC §2 红线复核: 无 user-facing toast/dialog/placeholder 加入本路径——warn 仅 dev-console(UI-SPEC 明示豁免 copy 审查,但其存在即 D-03 验收钩子)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc --noEmit && cd packages/infinite-canvas && npx vitest run src/store/__tests__/reloadAnchor.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - vitest 7 case 全绿(60-01 三 case + 本任务四 case);
  - grep -c "\[panel-persist\]" canvasStore.ts ≥ 1(两处锚各一或合并实现至少一处);
  - git diff canvasStore.ts 仅含 warn 相关增量(setGraph 重锚行/派生行零变化);
  - NodeDetailPanel.tsx / FlowCanvas.tsx 零改动。
  </acceptance_criteria>
  <done>锚丢失诚实收起有 warn 钩子;selected/detail 同生共死被 vitest 锁死;D-02 既有语义未被扰动。</done>
</task>

<task type="auto">
  <name>Task 2: 证据驱动修复分支(A 锁定 / B 定层修)</name>
  <files>packages/infinite-canvas/src/store/__tests__/reloadAnchor.test.ts, packages/infinite-canvas/src/v3/serialize.ts, packages/infinite-canvas/src/v3/adapter.ts, src/lib/canvasRelationalStore.ts</files>
  <read_first>
  - .planning/phases/60-post-save-panel-persistence/60-DIAGNOSIS.md 最终裁定节(Fix branch 行——本任务唯一分支选择器)
  - Branch B 时: 仅诊断点名的那一个层文件(层1→src/lib/canvasRelationalStore.ts;层2/层3→packages/infinite-canvas/src/v3/serialize.ts 或 adapter.ts,以裁定注明的派生源为准)
  - .planning/phases/60-post-save-panel-persistence/60-01-SUMMARY.md(证据摘录)
  </read_first>
  <action>
  读 60-DIAGNOSIS.md「Fix branch」行,二选一执行(files_modified 中 serialize/adapter/canvasRelationalStore 仅在 Branch B 实际触及时修改,Branch A 保持零生产改动):

  【Branch A —— 裁定为 A(setGraph 语义已对 + roundtrip 零漂移或 SKIP-条件裁定)】
  (a) 零生产代码修改。
  (b) reloadAnchor.test.ts 增 in-memory roundtrip 锁 case: 取 60-01 测试同款 wire fixture → adaptedA = adaptV2Graph(wire) → wire2 = serializeGraphToV2(adaptedA.graph, adaptedA.rawDataByNodeId, undefined) → adaptedB = adaptV2Graph(wire2) → expect adaptedA/adaptedB 节点 id 集合全等(含 evt_ 子集单列断言)。纯函数级,CI 安全,不依赖 :10588。
  (c) 重跑 npx tsx scripts/diagnose-60-roundtrip.ts --strict 确认 exit 0(:10588 可达时);SKIP 环境下以 (b) 为绑定门。
  (d) 在 60-DIAGNOSIS.md 追加「Branch A confirmation」段: 日期 + 两次门结果。SUMMARY 明确写「D-02 由既有 setGraph 语义满足,证据锚定」。

  【Branch B —— 裁定为 B(诊断点名漂移层)】
  (a) 只修诊断点名的那一层:
      层1(V2 重排)→ canvasRelationalStore: 修 rowToNode/upsertNode 的 id 透传(保存序与回读序一致性),禁止重排/重生成 id;
      层2/层3(V3 派生)→ serialize.ts 或 adapter.ts 的 evt_/vm id 派生源: 使派生对称(单一来源函数供序列化剔除与迁移重合成两端共用——禁止两处各写一份派生式),保证 save→reload 后同资产合成同 evt_ id。
  (b) 修复不得改变 FlowGraphV2 wire 形状的其他消费方契约(kmc pipeline 消费同一 wire)——只动 id 稳定性,不动字段集。
  (c) 重跑 scripts/diagnose-60-roundtrip.ts --strict → 必须 exit 0(三层差集全空 + 恢复全等);若 :10588 不可达(exit 2),以 (d) in-memory 锁为绑定门并在 SUMMARY 记录真机复验延后至 60-05 probe。
  (d) reloadAnchor.test.ts 增同款 in-memory roundtrip 锁 case(与 Branch A (b) 完全相同)。

  两分支共同收口: 60-DIAGNOSIS.md 追加执行结果段(分支字母 + 门结果 + 改动文件清单)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsx scripts/diagnose-60-roundtrip.ts --strict; echo "diag=$?"; cd packages/infinite-canvas && npx vitest run src/store/__tests__/reloadAnchor.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - diagnose --strict exit 0(零漂移)或 exit 2(SKIP 有理由输出,此时 vitest roundtrip 锁必须绿);
  - vitest 8 case 全绿(7 + roundtrip 锁);
  - Branch A: serialize.ts/adapter.ts/canvasRelationalStore.ts 三文件 diff 为零;
  - Branch B: diff 仅落在诊断点名文件,且修复后该层在 DIAGNOSIS 中的原差集计数归零;
  - 60-DIAGNOSIS.md 含执行结果段。
  </acceptance_criteria>
  <done>roundtrip id 稳定性有可重复门(A: 既有事实被锁 / B: 漂移被修并被锁);PANEL-02 锚等价的 id 前提成立。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reload wire→store | setGraph 重锚消费 reload 后的派生模型;锚丢失走诚实收起路径(无 trust 提升) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04 | Tampering | Branch B 修 id 派生/透传 | mitigate | 修复仅限 id 稳定性,wire 字段集不变(kmc pipeline 同契约消费);修复后 diagnose --strict 三层零差集 + 恢复全等双门;in-memory vitest 锁防回归 |
| T-60-05 | DoS(弱) | console.warn 路径 | mitigate | 转移守卫(非 null→null 才发)+ no-warn-spam vitest 锁——防高频 setGraph 下的日志洪水 |
| T-60-SC | Tampering | npm installs | accept | 零新依赖 |
</threat_model>

<verification>
- npx tsc --noEmit(根)干净;
- npx vitest run src/store/__tests__/reloadAnchor.test.ts 全量绿(含 D-03/D-07/roundtrip 锁);
- scripts/diagnose-60-roundtrip.ts --strict exit 0(或 SKIP 记录在案);
- Branch A 时三候选生产文件 diff 为零;Branch B 时 diff 精确限于点名文件。
</verification>

<success_criteria>
- 诊断先于修复的裁定被如实执行(分支与 DIAGNOSIS 一致,未擅自在错误层动刀);
- D-03 诚实收起(收起+warn+无模糊匹配)与 D-07 对称在 store 层锁死;
- PANEL-02 的「同一资产/同一事件锚」有了 id 稳定性这一物理前提的可重复证据。
</success_criteria>

<output>
Create `.planning/phases/60-post-save-panel-persistence/60-03-SUMMARY.md` when done。
SUMMARY 必须写明: 实际执行的分支字母 + 证据(诊断输出摘录) + Branch B 时的改动文件与漂移归零对照。
</output>
