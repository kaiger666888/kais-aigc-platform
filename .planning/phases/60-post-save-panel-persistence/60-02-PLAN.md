---
phase: 60-post-save-panel-persistence
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/canvas/v2/save-v2.ts
  - packages/infinite-canvas/test/e2e/mock-backend/server.mjs
  - packages/infinite-canvas/src/services/clientTabId.ts
  - packages/infinite-canvas/src/services/canvasApi.ts
  - packages/infinite-canvas/src/hooks/useCanvasSocket.ts
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
  - packages/infinite-canvas/test/e2e/tests/phase59-stale-cascade.mjs
autonomous: true
requirements: [PANEL-01]
must_haves:
  truths:
    - "客户端自保存返回 200 后,graph:saved 自回声不再触发 loadCanvas reload,也不再弹「Pipeline 同步了新数据」toast(D-01/D-05)"
    - "他端(kmc pipeline / 其他 tab,不携带 savedBy)保存仍 toast+reload——既有协作行为零回归"
    - "所有六处客户端保存路径(handleSave/handleOrchestrate/ContextMenu/canvasStore 两处/useStaleRerun)自动携带 tabId 身份——rerun 保存天然覆盖(Pitfall 5)"
    - "mock 与真机 save-v2 广播同形(savedBy 透传),mock 旋钮 suppressGraphSaved 退役且 59 SC4 经自然跳过保持绿(FLAG-4)"
    - "自回声分支保留 health 轮询基线重置(FLAG-1: 跳 reload 不跳基线)"
  artifacts:
    - path: "src/routes/canvas/v2/save-v2.ts"
      provides: "savedBy zod 可选字段 + broadcast 回显"
      contains: "savedBy"
    - path: "packages/infinite-canvas/src/services/clientTabId.ts"
      provides: "getClientTabId() 页面级 tabId 单例"
      exports: ["getClientTabId"]
    - path: "packages/infinite-canvas/src/services/canvasApi.ts"
      provides: "saveCanvasGraph body 自动附 savedBy"
    - path: "packages/infinite-canvas/src/components/FlowCanvas.tsx"
      provides: "onGraphSaved 自回声判定 + FLAG-1 基线重置次序"
    - path: "packages/infinite-canvas/src/hooks/useCanvasSocket.ts"
      provides: "graph:saved payload 类型 + 订阅类型加 savedBy?"
    - path: "packages/infinite-canvas/test/e2e/mock-backend/server.mjs"
      provides: "savedBy 透传 + logCall 观测 + 旋钮删除"
  key_links:
    - from: "packages/infinite-canvas/src/services/canvasApi.ts saveCanvasGraph"
      to: "src/routes/canvas/v2/save-v2.ts"
      via: "POST body savedBy 字段"
      pattern: "savedBy"
    - from: "src/routes/canvas/v2/save-v2.ts broadcastToProject"
      to: "packages/infinite-canvas/src/hooks/useCanvasSocket.ts graph:saved"
      via: "payload.savedBy 回显"
      pattern: "savedBy"
    - from: "FlowCanvas onGraphSaved"
      to: "clientTabId getClientTabId"
      via: "payload.savedBy === getClientTabId() 判定,命中则早退于 toast/loadCanvas 之前(但晚于基线重置)"
      pattern: "getClientTabId"
---

<objective>
落地 D-01 自保存自回声跳过机制(autonomous_decisions 已裁决: savedBy tabId 回显,时间窗方案禁用——
RESEARCH F-1 实证 broadcast 先于 HTTP 响应)。wire 契约三件套(server zod → broadcast → socket 类型
→ 消费方守卫,59 CR-02 同型)一次打通,顺带销掉 SC4 竞态根因(59 Known Issue #1)并退役 mock 旋钮。

Purpose: PANEL-01 的机制面——真机保存 200 后面板不因自回声整图重载而收起;rerun 角标复活竞态
的 reload 侧根因同时消失(D-08 前置)。
Output: 双端同形的 savedBy 契约 + 客户端跳过分支 + 59 SC4 用例改走真实回声路径。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-post-save-panel-persistence/60-CONTEXT.md
@.planning/phases/60-post-save-panel-persistence/60-RESEARCH.md
@.planning/phases/60-post-save-panel-persistence/60-PATTERNS.md

<interfaces>
<!-- 现场契约(已提取;两个 task 的修改点全部在此) -->

save-v2.ts L25-31 现状:
  router.post("/", validateFields({ projectId: z.number(), episodesId: z.number(), graph: z.any() }), ...)
  L71: broadcastToProject(projectId, "graph:saved", { projectId, episodesId, timestamp: Date.now() });
  L72: return res.status(200).send(success());   // broadcast 先于响应(F-1/Pitfall 1)

FlowCanvas.tsx onGraphSaved 现状(L328-342,scope 守卫后):
  showToast('Pipeline 同步了新数据,正在刷新画布…', 'info')   // L337
  lastEventCountRef.current = null                            // L339 基线重置
  loadCanvas(projectId, episodesId)                           // L340

canvasApi.ts saveCanvasGraph(L331-338):
  await apiCall<void>('/canvas/v2/save-v2', { projectId, episodesId, graph }, { cancelToken })
  // apiCall(path, body, options) —— body 原样 POST

saveCanvasGraph 全部六个调用方(全部=自保存,内部附 tabId 即全覆盖):
  FlowCanvas.tsx L686(handleSave) L728(handleOrchestrate) · CanvasContextMenu.tsx L122 ·
  canvasStore.ts L676/L952 附近两处 canonical 回写 · useStaleRerun.ts L59(rerun 先存再跑——Pitfall 5)

useCanvasSocket.ts:
  L73 onGraphSaved?: (payload: { projectId: number; episodesId: number; timestamp: number }) => void
  L234 socket.on('graph:saved', (payload: {...同形}) => callbacksRef.current.onGraphSaved?.(payload))

mock server.mjs save-v2 handler(L184-193):
  state.canvas 替换 → logCall({projectId, episodesId, nodeCount})(当前不记 savedBy——要加)
  → suppressGraphSaved 旋钮(L136/L181-183/L189/L445 共四处)→ 5ms setTimeout broadcast
  health mock L203: eventCount = save-v2 调用计数(FLAG-1 依据)

phase59-stale-cascade.mjs SC4: L316-324 依赖 suppressGraphSaved 旋钮(本 plan Task 2 移除并改写)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 服务端 savedBy 契约 + mock 镜像(旋钮暂留)</name>
  <files>src/routes/canvas/v2/save-v2.ts, packages/infinite-canvas/test/e2e/mock-backend/server.mjs</files>
  <read_first>
  - src/routes/canvas/v2/save-v2.ts(全文 81 行)
  - packages/infinite-canvas/test/e2e/mock-backend/server.mjs L123-197(state/config/save-v2 handler)与 L439-460(__mock 端点)
  - 60-RESEARCH.md F-3/F-6 节 + Security Domain 节
  </read_first>
  <action>
  (1) save-v2.ts: validateFields 对象增加 savedBy: z.string().max(64).optional()(V5 输入验证,T-60-02 缓解——白名单+长度限制,不落库只回显);handler 解构增加 savedBy;L71 broadcast payload 改为 { projectId, episodesId, timestamp: Date.now(), ...(savedBy != null ? { savedBy } : {}) }——kmc pipeline 等既有调用方不传 → 广播形状与今日完全一致(向后兼容,PATTERNS Shared Pattern 2),透传时多一个 savedBy 键。
  (2) mock server.mjs save-v2 handler: 解构增加 savedBy(req.body.savedBy);broadcast payload 同款条件展开加 savedBy;logCall 的 body 记录增加 savedBy: req.body?.savedBy ?? null(e2e 观测面——60-04 用例 1 断言客户端真的发了身份,以及负向对照可从 calls 读回 tabId)。5ms setTimeout 广播时机保持不变(F-6: 客户端判定靠 savedBy 非时序,mock/真机时机差异无害)。
  (3) 本任务不触碰 suppressGraphSaved 四处——留待 Task 2 与客户端跳过分支同 commit 退役,保证任务边界处 59 e2e 仍绿。
  禁止事项: 不给 savedBy 加任何鉴权/校验语义(该字段不是权限依据,RESEARCH F-3 威胁评估定级 Informational);不动 broadcast 时机(先于 res.send 的顺序保持——这正是 savedBy 方案的存在理由)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc --noEmit && node --check packages/infinite-canvas/test/e2e/mock-backend/server.mjs && grep -c "savedBy" src/routes/canvas/v2/save-v2.ts && grep -c "savedBy" packages/infinite-canvas/test/e2e/mock-backend/server.mjs && grep -c "suppressGraphSaved" packages/infinite-canvas/test/e2e/mock-backend/server.mjs</automated>
  </verify>
  <acceptance_criteria>
  - tsc 干净;server.mjs 语法检查通过;
  - save-v2.ts grep savedBy ≥ 2(zod 行 + broadcast 行);server.mjs grep savedBy ≥ 2(透传 + logCall);
  - suppressGraphSaved 计数保持 4(本任务不动旋钮——Task 2 前的红线);
  - 广播对无 savedBy 请求的形状与改造前逐键一致(条件展开保证,代码评审确认)。
  </acceptance_criteria>
  <done>双端 save-v2 契约同形落位: 客户端带身份保存 → 广播回显身份;不带 → 广播形状不变。</done>
</task>

<task type="auto">
  <name>Task 2: 客户端 tabId + 自回声跳过分支 + 旋钮退役 + 59 SC4 自然通过</name>
  <files>packages/infinite-canvas/src/services/clientTabId.ts, packages/infinite-canvas/src/services/canvasApi.ts, packages/infinite-canvas/src/hooks/useCanvasSocket.ts, packages/infinite-canvas/src/components/FlowCanvas.tsx, packages/infinite-canvas/test/e2e/mock-backend/server.mjs, packages/infinite-canvas/test/e2e/tests/phase59-stale-cascade.mjs</files>
  <read_first>
  - packages/infinite-canvas/src/services/canvasApi.ts L53-80(apiCall)与 L324-338(saveCanvasGraph)
  - packages/infinite-canvas/src/components/FlowCanvas.tsx L265-267(lastEventCountRef 声明)与 L328-342(onGraphSaved 现块)与 L795-823(health-poll 段——只读,FLAG-2 禁改)
  - packages/infinite-canvas/src/hooks/useCanvasSocket.ts L73 与 L233-236(graph:saved 类型与订阅)
  - packages/infinite-canvas/src/hooks/useStaleRerun.ts L55-62(rerun 保存路径——验证天然覆盖,不改)
  - packages/infinite-canvas/test/e2e/mock-backend/server.mjs L131-139/L179-193/L439-448(旋钮四处)
  - packages/infinite-canvas/test/e2e/tests/phase59-stale-cascade.mjs L313-330(SC4 旋钮用法)
  </read_first>
  <action>
  (1) 新建 packages/infinite-canvas/src/services/clientTabId.ts: export function getClientTabId(): string —— 模块级 lazy 单例,首次调用生成 `tab_` + crypto.randomUUID().slice(0,8),之后恒返回同值;crypto.randomUUID 不可用环境(旧 jsdom)回退 Math.random 十六进制 8 位。页面加载期身份即可(回声比对发生在同一页面实例内,sessionStorage 无必要——F-3)。头注释写明 D-01 语义与「时间窗方案为何被否」一句(broadcast 先于 HTTP 响应,F-1)。
  (2) canvasApi.ts saveCanvasGraph: apiCall body 改为 { projectId, episodesId, graph, savedBy: getClientTabId() }。签名不变——六个调用方(handleSave/handleOrchestrate/ContextMenu/canvasStore 两处/useStaleRerun)零改动自动带身份,rerun 保存天然覆盖(Pitfall 5 根治)。顶部 import getClientTabId。
  (3) useCanvasSocket.ts: L73 回调类型与 L234 订阅 payload 类型均加 savedBy?: string。
  (4) FlowCanvas.tsx onGraphSaved 重排(scope 守卫保持最前): 命中 scope 后依次——第一句 lastEventCountRef.current = null(无条件,FLAG-1: mock health eventCount 把每次 save-v2 计为事件,自回声分支丢基线重置会在 ≤30s 冒假「检测到 pipeline 远端更新」toast,违 D-05);第二句 const selfEcho = typeof payload.savedBy === 'string' && payload.savedBy === getClientTabId(),if (selfEcho) return(D-01 跳 reload,D-05 静默——无 toast);第三句 showToast('Pipeline 同步了新数据,正在刷新画布…', 'info');第四句 loadCanvas(projectId, episodesId)。顶部 import getClientTabId。
  (5) health-poll 段(L795-823)一行不动(FLAG-2: 真机 health 无 eventCount 是 pre-existing quirk,禁顺手修字段映射——修了会在本 phase 激活第二条 reload 通道)。
  (6) mock server.mjs 旋钮退役: 删 state.config 默认键 suppressGraphSaved(L136)、59-04 注释块(L181-183)、handler 内 guard(L189)、/__mock/reset 的 config 重建行中的该键(L445)。
  (7) phase59-stale-cascade.mjs SC4 改造: 删除 L324 __mock/config suppressGraphSaved POST;L316-323 注释块改写为 D-08 语义(rerun 保存经 saveCanvasGraph 自动携带 savedBy → 自回声被客户端跳过 → reload 侧竞态根因消失,59 Known Issue #1 销案,本用例改走真实回声路径)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc --noEmit && cd packages/infinite-canvas && npx vitest run src/store/__tests__/canonicalWriteback.test.ts src/store/__tests__/deleteNode.test.ts && npm run build && npx playwright test test/e2e/tests/phase59-stale-cascade.mjs -g "SC4" && cd /data/workspace/kais-aigc-platform && ! grep -rn "suppressGraphSaved" packages/infinite-canvas/test/ && echo KNOB-GONE</automated>
  </verify>
  <acceptance_criteria>
  - 根 tsc 干净;两个 store vitest(payload mock 断言)仍绿——签名未变,mock 不受 body 内部构造影响;
  - build + 59 SC4 e2e 绿(FLAG-4: 旋钮删除后经 D-01 自然跳过通过,证明 rerun 保存路径真的带了 savedBy);
  - 全仓 grep suppressGraphSaved = 0;
  - FlowCanvas onGraphSaved 块内: lastEventCountRef 重置行先于 savedBy 判定早退行(60-05 S2 静态锁将机器锁死此次序);
  - health-poll 段 diff 为零(FLAG-2)。
  </acceptance_criteria>
  <done>自保存静默且不 reload;他端保存行为不变;SC4 竞态的 reload 侧根因消失且既有 e2e 证据在手。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→save-v2 | savedBy 为客户端可伪造的自声明身份(非鉴权依据),跨此边界进入广播 |
| save-v2→所有同项目客户端 | broadcast payload 携带 savedBy 回显,接收端以此决定是否跳过 reload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-02 | Spoofing/Repudiation | savedBy 字段 | accept | RESEARCH Security Domain 预定级 Informational: 最坏情形=冒充他端 tabId 使该端跳过一次 reload,影响仅 UI 短暂陈旧(health-poll/手动刷新可恢复),无数据完整性影响;缓解= zod z.string().max(64).optional() 白名单长度限制 + echo-only 不落库;文档化接受(UI-SPEC §Copywriting 与本表即记录),不加鉴权(字段语义上不是权限依据) |
| T-60-03 | Information Disclosure | broadcast 携带 8 位随机 tabId | accept | 非 PII 随机值;socket room 本就按 project:{id} 隔离,收听者仅同项目成员 |
| T-60-SC | Tampering | npm installs | accept | 零新依赖;crypto.randomUUID 为平台 API 非 npm 包 |
</threat_model>

<verification>
- npx tsc --noEmit(根)干净;
- packages vitest canonicalWriteback + deleteNode 绿(签名不变的回归证据);
- build + playwright phase59-stale-cascade.mjs -g SC4 绿(FLAG-4);
- grep suppressGraphSaved 全仓 0。
</verification>

<success_criteria>
- D-01/D-04/D-05 机制面完成: 客户端判定(非 mock 旋钮),mock/真机同形契约;
- FLAG-1(基线重置保留)与 FLAG-2(health 映射禁改)双红线在 diff 中可核;
- Pitfall 5(rerun 保存覆盖)由 canvasApi 单点附加自动满足——六调用方零改动;
- PANEL-01 的行为断言面就绪,待 60-04 e2e 四用例收口。
</success_criteria>

<output>
Create `.planning/phases/60-post-save-panel-persistence/60-02-SUMMARY.md` when done.
SUMMARY 记录: onGraphSaved 最终块序(基线重置→早退→toast→reload)、SC4 自然通过证据、
kmc pipeline 无 savedBy 广播形状不变的兼容性说明。
</output>
