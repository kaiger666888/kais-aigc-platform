---
phase: 60-post-save-panel-persistence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/diagnose-60-roundtrip.ts
  - packages/infinite-canvas/src/store/__tests__/reloadAnchor.test.ts
  - .planning/phases/60-post-save-panel-persistence/60-DIAGNOSIS.md
autonomous: true
requirements: [PANEL-02]
must_haves:
  truths:
    - "真机 save→reload roundtrip 的节点 id 漂移被实证测量(V2 层/V3 层/evt_* 合成三层各自有数),而非猜测"
    - "store 侧 setGraph 重锚语义(survive/collapse)有 vitest 证据,loading 卸载假说有静态结论"
    - "60-DIAGNOSIS.md 钉死收起根因并给出 A/B 修复分支,是 60-03 的分支选择器"
    - ":10588 上的诊断净足迹为零(原图回存 deep-equal 全等),或 :10588 不可达时如实 SKIP"
  artifacts:
    - path: "scripts/diagnose-60-roundtrip.ts"
      provides: "真机 roundtrip id-diff 探针(--strict 门模式 / exit 2 SKIP)"
    - path: "packages/infinite-canvas/src/store/__tests__/reloadAnchor.test.ts"
      provides: "store 重锚 survive/collapse 对称 vitest(60-03 扩为永久锁)"
    - path: ".planning/phases/60-post-save-panel-persistence/60-DIAGNOSIS.md"
      provides: "根因裁定 + 修复分支(A/B)证据文档"
  key_links:
    - from: "scripts/diagnose-60-roundtrip.ts"
      to: "packages/infinite-canvas/src/v3/adapter.ts"
      via: "adaptV2Graph 导入(V2→V3 适配)"
      pattern: "adaptV2Graph"
    - from: "scripts/diagnose-60-roundtrip.ts"
      to: "packages/infinite-canvas/src/v3/serialize.ts"
      via: "serializeGraphToV2 导入(V3→V2 wire)"
      pattern: "serializeGraphToV2"
    - from: ".planning/phases/60-post-save-panel-persistence/60-DIAGNOSIS.md"
      to: "60-03-PLAN.md Task 2"
      via: "Fix-branch A/B 裁定行"
      pattern: "Fix branch"
---

<objective>
诊断先行(RESEARCH F-2/Pitfall 2 强制): 在动任何 store/loading 修复之前,用真机 :10588 上的
save→reload roundtrip id-diff 探针 + store 层重锚 vitest 探针,实证钉死「真机保存后面板收起」
的根因层——RESEARCH F-2 列出的候选①(vm/视图模型 id 派生往返不对称)②(loading 卸载闪断)
③(其他)。产出 60-DIAGNOSIS.md,其 A/B 分支裁定直接驱动 60-03 的修复任务。

Purpose: 避免在错误层动刀——setGraph L445-446 已按 id 重锚,若收起根因是 wire/服务端 id 漂移,
store 层「对齐」是无效修(FLAG-3 已预警 D-02 前提可能已满足)。
Output: 诊断探针脚本(可重复运行,60-05 dispatch 段复用)+ store 重锚测试基底 + 根因裁定文档。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-post-save-panel-persistence/60-CONTEXT.md
@.planning/phases/60-post-save-panel-persistence/60-RESEARCH.md
@.planning/phases/60-post-save-panel-persistence/60-UI-SPEC.md

<interfaces>
<!-- 探针需要的纯函数契约(已从源码提取,executor 直接用,无需再探索) -->

From packages/infinite-canvas/src/v3/adapter.ts:518:
  export function adaptV2Graph(raw: unknown): AdaptResult
  // AdaptResult = { graph: V3Graph, warnings: string[], source, rawDataByNodeId, phaseCatalog }
  // (形状见 canvasStore.loadGraphFromV2 L461 的解构)

From packages/infinite-canvas/src/v3/serialize.ts:217:
  export function serializeGraphToV2(graph, rawDataByNodeId, viewport?): FlowGraphV2 wire 形状

From packages/infinite-canvas/test/e2e/probe-59-real.mjs(范式源,直接照搬):
  SCOPES = [[2, 1], [2001, 1]] + MIGRATE_SUPPORTED 集合守卫
  stripUpdatedAt / deepEqual / firstDiff —— 剔 meta.updatedAt/lastEventId 的深比对
  捕获-恢复 finally 范式(原图回存 + 轮询 load-v2 复核全等)

真机端点: POST /api/canvas/v2/load-v2 {projectId, episodesId} → {code:200, data: FlowGraphV2}
          POST /api/canvas/v2/save-v2 {projectId, episodesId, graph} → 200
  (save-v2 对 body 未知字段 zod strip——本 phase 前的旧 server 加 savedBy 也安全,不依赖部署)

store 契约(探针 2 驱动对象):
  useCanvasStore.getState().setGraph(graph, warnings?) —— canvasStore.ts L424-449
    selectedNode/detailNode 重锚: vm.rfNodes.find(n => n.id === state.X!.id) ?? null (L442-447)
  setDetailNode / setSelectedNode setter(store 内既有)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 真机 roundtrip id-diff 探针 + Prong 1 证据</name>
  <files>scripts/diagnose-60-roundtrip.ts, .planning/phases/60-post-save-panel-persistence/60-DIAGNOSIS.md</files>
  <read_first>
  - packages/infinite-canvas/src/v3/adapter.ts(至少 L1-60 类型 + L518 adaptV2Graph 签名段)
  - packages/infinite-canvas/src/v3/serialize.ts(L217-260 serializeGraphToV2 签名段)
  - packages/infinite-canvas/test/e2e/probe-59-real.mjs(全文——SCOPES/MIGRATE_SUPPORTED/stripUpdatedAt/deepEqual/firstDiff/恢复范式逐行照搬)
  - src/lib/canvasRelationalStore.ts(只读参考:upsertNode/saveFullGraph 的 id 透传——印证 F-2 判断,不修改)
  </read_first>
  <action>
  创建 scripts/diagnose-60-roundtrip.ts(独立 tsx 脚本,verify-phase-59.ts 头注释风格,零依赖新包)。结构:
  (a) scope 选择:照搬 probe-59-real 的 SCOPES=[[2,1],[2001,1]] 遍历 + load-v2 200 检查 + MIGRATE_SUPPORTED 全图校验;全部不可达 → 打印 SKIP 理由 exit 2。
  (b) roundtrip 复现客户端全链:loadA = loadV2(pid,eid).data → adaptedA = adaptV2Graph(loadA) → wire = serializeGraphToV2(adaptedA.graph, adaptedA.rawDataByNodeId, undefined) → POST save-v2 {projectId, episodesId, graph: wire} → loadC = loadV2(pid,eid).data → adaptedC = adaptV2Graph(loadC)。
  (c) 三层 id diff,逐层 PASS/FAIL + 精确计数输出:
      层1 V2(服务端重组稳定性): loadA.nodes 的 id 集合 vs loadC.nodes 的 id 集合,双向差集;
      层2 V3(完整客户端往返): adaptedA.graph.nodes id 集 vs adaptedC.graph.nodes id 集,双向差集;
      层3 evt_*(事件重合成确定性): adaptedA/adaptedC 中以 evt_ 前缀的 id 子集单列比对;
      锚点抽检: 取 adaptedA 第一个 kind==='asset'(或 V2 侧第一个非 evt 节点)id,断言在 adaptedC 同 id 存在——这是 detailNode 重锚的最小情形。
  (d) exit 契约: --strict 时任一层差集非空 → exit 1;:10588 不可达 → exit 2;零漂移 → exit 0。默认(无 flag)只打印报告 exit 0。
  (e) finally 零足迹: saveV2(pid, eid, loadA 原图 verbatim) → ≤15s 轮询 load-v2,stripUpdatedAt 深比对全等;不等 → firstDiff 输出 + exit 1(footprint 失败即探针失败)。
  (f) 运行方式: 仓库根 npx tsx scripts/diagnose-60-roundtrip.ts --strict。若 @kais/flowgraph-v3 在 tsx 下解析失败(workspace alias),以 --tsconfig 显式指 packages/infinite-canvas/tsconfig.json 重跑(49-01/59-02 子进程教训,STATE 决策在案)。
  (g) 运行一次,把逐层结果原样记录进 60-DIAGNOSIS.md「Prong 1: 真机 roundtrip id diff」节(含三层计数 + exit code + 恢复核对结论)。
  禁止事项: 不修改任何 src/ 生产代码;不写 assetKey 模糊匹配;探针中途异常也必须走 finally 恢复。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc --noEmit && npx tsx scripts/diagnose-60-roundtrip.ts --strict; echo "exit=$?" (exit ∈ {0,2}; :10588 可达时必须 0 并含「恢复…全等」行)</automated>
  </verify>
  <acceptance_criteria>
  - scripts/diagnose-60-roundtrip.ts 存在且 tsc 干净;
  - 实跑一次: exit 0(零漂移,三层差集全为空 + 恢复全等)或 exit 2(SKIP 理由打印,:10588 不可达);
  - grep -c "stripUpdatedAt\|deepEqual" 脚本 ≥ 2(恢复复核逻辑在场);
  - 60-DIAGNOSIS.md 存在且含「Prong 1」节: 三层各自计数 + exit code + 恢复核对结论,数字与脚本输出一致。
  </acceptance_criteria>
  <done>真机 roundtrip 三层 id 漂移被实测并记录;诊断净足迹为零(或环境不可达已如实 SKIP);Prong 1 证据落文档。</done>
</task>

<task type="auto">
  <name>Task 2: store 重锚探针 + loading 卸载静态裁定 → 60-DIAGNOSIS 最终裁定</name>
  <files>packages/infinite-canvas/src/store/__tests__/reloadAnchor.test.ts, .planning/phases/60-post-save-panel-persistence/60-DIAGNOSIS.md</files>
  <read_first>
  - packages/infinite-canvas/src/store/canvasStore.ts L418-493(setGraph/loadGraphFromV2/loadInitialGraph 全段)
  - packages/infinite-canvas/src/components/FlowCanvas.tsx L183(loading 选择器)/L265-267(lastEventCountRef)/L328-342(onGraphSaved)/L415-462(loadCanvas)/L956(loading 门)/面板渲染两处(timeline 视图与 canvas 视图的 NodeDetailPanel node={detailNode},约 L1048 与 L1265——当前代码,60-02 不动这两处)
  - packages/infinite-canvas/src/store/__tests__/canonicalWriteback.test.ts(store 驱动 vitest 范式: 直接 useCanvasStore.getState() 调 action)
  - packages/infinite-canvas/src/v3/adapter.ts(为测试构造最小 V2 fixture 用 adaptV2Graph 生成合法 V3 graph)
  </read_first>
  <action>
  创建 packages/infinite-canvas/src/store/__tests__/reloadAnchor.test.ts(vitest,store 直驱,不渲染 React):
  (a) fixture: 3 节点最小 V2 图(沿用 phase59 cascadeFixtureGraph 的 trig-1/mid-1/down-1 三节点裁剪,links 两条 image 边),adaptV2Graph 生成 V3 graph g1;同构但节点对象全新(如重跑 adaptV2Graph(structuredClone(wire)))得 g2;删除 down-1 后重适配得 g3。
  (b) Case survive(D-02/D-07 正向): setGraph(g1) → setDetailNode(store.nodes 中 trig-1) + setSelectedNode(同) → setGraph(g2) → expect detailNode.id === 'trig-1' 且 selectedNode.id === 'trig-1' 且引用是 g2 派生模型的新节点(detailNode === g2 后 setGraph 的 nodes 中该 id 项,非旧引用)。
  (c) Case collapse(D-03/D-07 对称负向): 在 g2 锚定 down-1 后 setGraph(g3)(down-1 不存在)→ expect detailNode === null 且 selectedNode === null(同一次 setGraph 内对称收起)。
  (d) Case other-anchor-untouched: 锚定 trig-1 时 setGraph(g3)(down-1 没了,trig-1 在)→ trig-1 保持。
  (e) 原子性注记: setGraph 是单次同步 set 调用,无中间 null 窗口——在测试注释里写明「原子 by construction」,不加异步断言。
  静态裁定(Prong 2,写入 DIAGNOSIS): grep/实读 FlowCanvas 两处 NodeDetailPanel 渲染点是否被 loading 条件包裹(结论应为否);L956 的 if (loading && !hasData) 仅首载生效(hasData 置 true 后 reload 期间不再挂 loading 屏);loadInitialGraph L477-493 全程不清空 graph/detailNode。据此给出候选②(loading 卸载闪断)的结构性裁定: 成立/不成立 + 行级依据。
  最终裁定(60-DIAGNOSIS.md 收尾节,60-03 的分支选择器,必须逐字含):
  「Pinned cause: <①id 漂移@层X / ②loading 卸载 / ③其他@…>」
  「Fix branch: A(setGraph 语义已对,零生产修复,仅锁)/ B(修 @<精确文件+原因>)」
  裁定规则: Prong1 零漂移 + Prong2 候选②不成立 → Branch A;Prong1 任一层非零漂移 → Branch B 并点名层(层1→canvasRelationalStore,层2/层3→v3/serialize.ts 或 v3/adapter.ts 的派生源);Prong1 SKIP(:10588 不可达)→ 以 Prong2 + 代码级分析出条件裁定并注明证据等级 MEDIUM,Branch B 时点名嫌疑层且 60-03 必须带 in-memory roundtrip 锁。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/store/__tests__/reloadAnchor.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - vitest 3 case 全绿(survive/collapse/other-anchor-untouched);
  - 60-DIAGNOSIS.md 含「Prong 2」节(候选②裁定 + 行级依据)与最终裁定节,「Pinned cause」与「Fix branch」两行逐字在场;
  - grep -l "Pinned cause" .planning/phases/60-post-save-panel-persistence/60-DIAGNOSIS.md 命中;
  - 本任务零 src/ 生产代码改动(测试文件与文档除外)。
  </acceptance_criteria>
  <done>store 重锚语义有 vitest 证据;loading 假说有静态裁定;60-DIAGNOSIS.md 给出 60-03 可直接消费的 A/B 分支裁定。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe→:10588 生产 | 诊断探针对生产库执行真实 save-v2 写(读改读),必须在 finally 恢复 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01a | Tampering | :10588 canvas_nodes 数据 | mitigate | 捕获-恢复 finally + stripUpdatedAt deep-equal 全等断言;footprint 漂移 → exit 1 探针自判失败(probe-59-real 同款纪律) |
| T-60-01b | DoS(弱) | 探针选 scope | accept | 只选 load-v2 已 200 + MIGRATE_SUPPORTED 全图通过的 scope;保存内容为该 scope 原图 roundtrip,不引入新节点 |
| T-60-SC | Tampering | npm installs | accept | 本 plan 零新依赖(RESEARCH: 全仓内既有设施);tsx/vitest 均既有 devDep |
</threat_model>

<verification>
- npx tsc --noEmit(根)干净;
- npx tsx scripts/diagnose-60-roundtrip.ts --strict → exit 0(或 2 + SKIP 理由);
- cd packages/infinite-canvas && npx vitest run src/store/__tests__/reloadAnchor.test.ts 全绿;
- 60-DIAGNOSIS.md 三节齐全(Prong 1 / Prong 2 / 最终裁定)。
</verification>

<success_criteria>
- 诊断先于修复的次序成立: 本 plan 未改任何生产代码,60-03 消费其裁定;
- PANEL-02 的「语义等价」实现口径(id 全链稳定性)从假说变为实测数字;
- :10588 净足迹零(或环境不可达如实记录,不假绿)。
</success_criteria>

<output>
Create `.planning/phases/60-post-save-panel-persistence/60-01-SUMMARY.md` when done.
诊断结果(Pinned cause / Fix branch 两行)必须原样摘录进 SUMMARY 顶部——60-03 executor 首先读它。
</output>
