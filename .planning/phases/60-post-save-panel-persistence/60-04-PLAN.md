---
phase: 60-post-save-panel-persistence
plan: "04"
type: execute
wave: 3
depends_on: ["60-02", "60-03"]
files_modified:
  - packages/infinite-canvas/src/main.tsx
  - packages/infinite-canvas/test/e2e/tests/phase60-panel-persist.mjs
autonomous: true
requirements: [PANEL-01, PANEL-02]
must_haves:
  truths:
    - "mock 环境: 客户端点「保存」→ save-v2 200 → 回声到达后面板仍打开、同标题、无任何 reload toast、无 load-v2 后续请求(self-save + silent,PANEL-01/D-05)"
    - "mock 环境: 他端保存(无 savedBy)→ toast + reload → 面板重锚同 id、标题随新真相刷新、tab 与宽度不重置、选中对称保持(other-client + symmetry,PANEL-02/D-06/D-07)"
    - "mock 环境: 重载图中锚 id 消失 → 面板收起 + console.warn(anchor-miss,D-03)"
    - "mock 环境: rerun 重跑后 stale 角标归零且采样窗口内保持零——无 suppressGraphSaved 旋钮(no-revival,D-08/SC4 销案)"
    - "phase52 三件套 + phase59 全部回归绿(D-12)"
  artifacts:
    - path: "packages/infinite-canvas/test/e2e/tests/phase60-panel-persist.mjs"
      provides: "D-09 四用例 e2e(标题含 self-save/silent/other-client/symmetry/anchor-miss/no-revival 六个 -g 组词)"
    - path: "packages/infinite-canvas/src/main.tsx"
      provides: "bridge getDetailNode 只读访问器(e2e id 真值断言面)"
      contains: "getDetailNode"
  key_links:
    - from: "phase60-panel-persist.mjs"
      to: "[data-testid=\"detail-panel\"]"
      via: "phase35 契约选择器(UI-SPEC §7 禁新增 testid)"
      pattern: "detail-panel"
    - from: "phase60-panel-persist.mjs"
      to: "window.__kaisCanvas.getDetailNode()/getGraph()"
      via: "锚 id 真值断言"
      pattern: "__kaisCanvas"
    - from: "phase60-panel-persist.mjs toast 断言"
      to: "ToastContainer DOM(useToast.tsx 固定右下容器,3s 自灭)"
      via: "MutationObserver 页内 spy(捕获 <3s 瞬态)"
      pattern: "__toastLog"
---

<objective>
D-09 验证面: 新建 phase60-panel-persist.mjs 四用例,把 PANEL-01/PANEL-02/D-03/D-05/D-07/D-08 全部
变成 mock 环境的可重复 e2e 断言(mock 断言即真机行为——D-04 契约对齐后跳过逻辑在客户端实现);
随后跑 D-12 全量回归(phase52 三件套 + phase59 全部 + phase60)。

Purpose: 三个 phase Success Criteria 的行为级证据全部在此收口;SC4 竞态销案(D-08)在此定案。
Output: 四用例 e2e + getDetailNode 桥访问器 + 全量回归报告。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-post-save-panel-persistence/60-CONTEXT.md
@.planning/phases/60-post-save-panel-persistence/60-RESEARCH.md
@.planning/phases/60-post-save-panel-persistence/60-UI-SPEC.md
@.planning/phases/60-post-save-panel-persistence/60-02-SUMMARY.md
@.planning/phases/60-post-save-panel-persistence/60-03-SUMMARY.md

<interfaces>
<!-- e2e 基建契约(已提取) -->

helpers.mjs: test(自动 __mock/reset)· loadCanvas(page, opts)(goto /?projectId=1&episodesId=1&testMode=1
  + 双次导航 + switchToCanvasView「画布」按钮 + 等 __kaisCanvas)· nodeSelector(id)=.react-flow__node[data-id]
  · getCalls/getMockState · setSelectedNodeIds(bridge)· switchToCanvasView

phase59-stale-cascade.mjs 可复用范式(同目录直接参读):
  cascadeFixtureGraph()/injectCascadeFixture(page)(save-v2 注入 + page.reload)
  openDetailPanel(page, nodeId)(dblclick + detail-panel + prompt-section)
  staleBadge(page, nodeId)(svg[aria-label="stale"])· panelRegen(page)(prompt-regenerate 点击)
  lastCall(page, path)(getCalls 过滤)

UI 断言面(UI-SPEC §7 selector 契约,禁新增 testid):
  [data-testid="detail-panel"] 面板容器(NodeDetailPanel.tsx:103)
  面板标题 data.label ?? phaseName(锚同一性代理)
  「保存中…」/「保存」保存按钮文本(FlowCanvas.tsx:1136,toolbar,locator 'button:has-text("保存")')
  toast 文案两条精确串:「Pipeline 同步了新数据,正在刷新画布…」/「检测到 pipeline 远端更新,正在刷新画布…」
    (ToastContainer=固定右下 div 容器,3s 自灭——瞬态捕获须用页内 MutationObserver spy,不能只靠事后 locator)
  svg[aria-label="stale"] 计数(D-08)
  window.__kaisCanvas.getGraph()/getNodes()/getSelectedNodeIds()(bridge;本 plan 增 getDetailNode)

tab/宽度不重置的语义依据: NodeDetailPanel L67 tab 仅在 node?.id 变化时重置;L40 panelWidth useState
  初值 480(无持久化)——unmount 才丢。同 id 重锚 → 双双保持 = 未 unmount 的行为证明。

他端保存模拟: page.request.post('/api/canvas/v2/save-v2', { data: { projectId, episodesId, graph } })
  ——不带 savedBy → 广播无 savedBy → 客户端按他端处理(60-02 契约)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: getDetailNode 桥 + phase60 四用例 e2e</name>
  <files>packages/infinite-canvas/src/main.tsx, packages/infinite-canvas/test/e2e/tests/phase60-panel-persist.mjs</files>
  <read_first>
  - packages/infinite-canvas/src/main.tsx L27-60(testMode bridge 挂载段——getDetailNode 插入点,56-06 seam 先例)
  - packages/infinite-canvas/test/e2e/helpers.mjs(全文)
  - packages/infinite-canvas/test/e2e/tests/phase59-stale-cascade.mjs(fixture/注入/开面板/角标/regen 范式)
  - packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx L36-145(panelWidth/tab/reset effect/TabButton)
  - packages/infinite-canvas/src/hooks/useToast.tsx(ToastContainer DOM 形状——spy 设计依据)
  - 60-UI-SPEC.md §1-§8(全部行为契约与选择器红线)
  </read_first>
  <action>
  (1) main.tsx bridge 增一行: getDetailNode: () => useCanvasStore.getState().detailNode —— 只读 seam,56-06 openG16 同型;testMode 门内,零运行时影响。
  (2) 新建 test/e2e/tests/phase60-panel-persist.mjs。头部注释: D-09 四用例地图 + 地雷 #10(serve dist,先 npm run build)+ mock 断言即真机行为(D-04 依据: 跳过逻辑客户端实现)。
  (3) 页内 helper installToastSpy(page): page.evaluate 安装 MutationObserver 监 document.body,凡文本命中两条 toast 精确串之一即 push 进 window.__toastLog(数组);返回读取函数。瞬态 toast(3s 自灭)由此可靠捕获,替代事后 locator 竞态。
  (4) fixture: 本地裁剪 cascadeFixtureGraph 三节点版(trig-1/mid-1/down-1 + 两条 image 链;unrel-1 仅 no-revival 用例需要时取完整五节点版),注入路径照搬 injectCascadeFixture。
  (5) 四用例(标题内嵌 -g 组词,覆盖 VALIDATION 六个 tag):
      Test 1 「self-save keeps panel open, silent (PANEL-01, D-05)」:
        loadCanvas → 注入 → openDetailPanel(trig-1) → 记标题/getDetailNode().id → installToastSpy →
        click button:has-text("保存")(精确「保存」;saving 态按钮文本会变「保存中…」,等待期可断言其出现一次再回归)→
        expect.poll getCalls 最后一条 /api/canvas/v2/save-v2 的 body.savedBy 匹配 /^tab_/(客户端身份上 wire 证据)→
        记录此刻 load-v2 调用计数 → 等 900ms(5ms 回声 + 余量)→
        断言: detail-panel 可见;标题不变;getDetailNode().id === 'trig-1';window.__toastLog 为空(两条 toast 串零命中——silent);load-v2 计数不变(未 reload——最硬信号);保存按钮文本回到「保存」。
      Test 2 「other-client save reloads and re-anchors, symmetry preserved (PANEL-02, D-06, D-07)」:
        openDetailPanel(trig-1) → 点 TabButton「🔄 迭代」(tab 离开 detail) → 记 detail-panel boundingBox().width →
        setSelectedNodeIds([trig-1])(D-07 选中对称) → installToastSpy →
        page.request.post save-v2(不带 savedBy),graph = fixture 改造版: trig-1 保留但 data.label 改「他端改名」,另加节点 added-1 →
        断言: __toastLog 命中「Pipeline 同步了新数据…」(reload toast 只属他端)→ 等 reload 落地(expect.poll getDetailNode()?.id === 'trig-1' 且 getNodes 含 added-1)→
        断言: detail-panel 可见;标题 === 「他端改名」(D-06: 同 id 锚 + 内容随新真相刷新,非冻结快照);「🔄 迭代」tab 仍 active(tab 未重置=未 unmount);boundingBox().width 与记录差 ≤2px;getSelectedNodeIds() 仍含 'trig-1'(对称保持)。
      Test 3 「anchor-miss collapses honestly (D-03)」:
        openDetailPanel(down-1) → page.on('console') 收集 msg.text() →
        page.request.post save-v2(不带 savedBy),graph = 删除 down-1 后的 fixture → 等 reload 落地 →
        断言: '[data-testid="detail-panel"]' count === 0(真收起,无占位);捕获 console 中至少一条含 '[panel-persist]' 且含 'down-1';无任何模糊重锚(面板未漂到别的节点——count 0 即证明)。
      Test 4 「no-revival after rerun (D-08, SC4 closure)」:
        完整五节点 cascade fixture → panelRegen(trig-1) → 三下游角标可见 → openDetailPanel(down-1) →
        click [data-testid="stale-rerun-btn"] → 等 staleBadge(down-1) count 0 →
        采样窗: 每 300ms 读 '.react-flow__node svg[aria-label="stale"]' 全画布计数,持续 2500ms → 全程 === 0(无复活;全程无 suppressGraphSaved——旋钮已退役,回声活性由 D-01 自然跳过消化)。
  (6) 断言纪律: 不新增任何 data-testid(UI-SPEC §7);脉动/装饰性不断言(59-UI-SPEC §2 flake-bait 沿袭);宽度断言容差 ±2px。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm run build && npx playwright test test/e2e/tests/phase60-panel-persist.mjs</automated>
  </verify>
  <acceptance_criteria>
  - 四用例全绿(playwright exit 0);
  - 六个 -g 组词各自命中: -g "self-save" / -g "silent" / -g "other-client" / -g "symmetry" / -g "anchor-miss" / -g "no-revival" 均至少匹配一个用例(标题命名验证);
  - grep -c "data-testid" phase60-panel-persist.mjs 中新建 testid 为零(仅引用既有 detail-panel/stale-rerun-btn/prompt-regenerate/prompt-section);
  - main.tsx diff 仅 getDetailNode 一行增量。
  </acceptance_criteria>
  <done>PANEL-01/PANEL-02/D-03/D-05/D-06/D-07/D-08 全部有 mock e2e 行为证据;D-09 四用例地图落地。</done>
</task>

<task type="auto">
  <name>Task 2: D-12 全量回归(phase52 三件套 + phase59 全部 + phase60)</name>
  <files>packages/infinite-canvas/test/e2e/tests/phase60-panel-persist.mjs</files>
  <read_first>
  - 本 plan Task 1 产物(确保 phase60 文件在场)
  - .planning/STATE.md 决策段(59-02 dispatch/spawn 教训——本任务只跑 playwright,无 spawn 陷阱)
  </read_first>
  <action>
  单次 playwright 调用串行跑五文件(config workers=1 既有): phase52-regen.mjs, phase52-reroll.mjs,
  phase52-stale-panel.mjs, phase59-stale-cascade.mjs, phase60-panel-persist.mjs。
  加根仓 npx tsc --noEmit 收口。
  若个别用例红: 先隔离重跑该文件一次判定 flake(STATE 中 phase55-nav 负载噪音先例);复现性红 = 真回归,
  修 60-02/60-03 引入面直到绿——禁止改断言迁就;真 flake 记录进 SUMMARY(文件/用例/隔离重跑证据)。
  特别关注: phase59 SC4 已在 60-02 转自然路径,SC1-SC3 的实时性断言(无 page.reload 前提)不应受
  savedBy 改动影响——若 SC1/SC2 意外红,优先怀疑 execute/regen 链被误触,而非本 phase 面。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc --noEmit && cd packages/infinite-canvas && npm run build && npx playwright test test/e2e/tests/phase52-regen.mjs test/e2e/tests/phase52-reroll.mjs test/e2e/tests/phase52-stale-panel.mjs test/e2e/tests/phase59-stale-cascade.mjs test/e2e/tests/phase60-panel-persist.mjs</automated>
  </verify>
  <acceptance_criteria>
  - 五文件单次串行全绿(exit 0);
  - 或: 唯一红项经隔离重跑判 flake 且有记录(复现性红不允许存在——必须修至绿);
  - tsc 干净;SUMMARY 附五文件通过用例计数。
  </acceptance_criteria>
  <done>D-12 回归面绿: stale 面共享 reload 链的 52/59 历史行为零回归,phase60 新面全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e→mock backend | /__mock/* 与 page.request 直写仅存在于 :9876 mock;真机零接触 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-06 | Tampering | toast spy 页内注入 | accept | MutationObserver 只读 DOM 文本,零 store 写;仅 testMode 会话使用 |
| T-60-07 | Repudiation | e2e 假绿风险(断言不充分) | mitigate | 每用例含机制证据(savedBy 上 wire / load-v2 计数不变 / console.warn 捕获),非仅 UI 观感;60-05 verify 门再聚合一次 |
| T-60-SC | Tampering | npm installs | accept | 零新依赖(playwright/vitest 既有) |
</threat_model>

<verification>
- build + phase60 四用例绿(六个 -g 组词可独立过滤);
- 五文件回归串行绿;
- 根 tsc 干净。
</verification>

<success_criteria>
- Phase 60 三条 SC 中 SC1/SC2 的 mock 侧行为证据齐备;SC3 的 mock=真机等价性由「跳过逻辑在客户端 +
  mock 回声活性保留」结构保证,真机侧由 60-05 probe 收口;
- D-08 SC4 销案有 no-revival 采样窗证据;
- D-12 回归面零红。
</success_criteria>

<output>
Create `.planning/phases/60-post-save-panel-persistence/60-04-SUMMARY.md` when done。
SUMMARY 附: 四用例断言清单(逐 SC 映射) + 五文件回归计数 + flake 记录(如有)。
</output>
