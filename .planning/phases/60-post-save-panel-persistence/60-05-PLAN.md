---
phase: 60-post-save-panel-persistence
plan: "05"
type: execute
wave: 4
depends_on: ["60-04"]
files_modified:
  - scripts/verify-phase-60.ts
  - package.json
  - packages/infinite-canvas/test/e2e/probe-60-real.mjs
autonomous: true
requirements: [PANEL-01, PANEL-02]
must_haves:
  truths:
    - "npm run verify:phase-60 一键聚合门全绿: 静态锁(含 FLAG-1 次序/FLAG-2 负向/FLAG-4 退役/D-03 warn)+ 行为门(vitest/e2e)+ dispatch 门(roundtrip --strict)+ forced-failure 自检(门能红)"
    - "真机 :10588 实证: save-v2 带 savedBy → 广播回显同值;不带 → 广播无 savedBy(服务端契约)"
    - "真机浏览器实证: 点「保存」→ 200 + 回声后面板保持、标题不变、无 toast、save 后零 load-v2 请求(PANEL-01 真机收口,D-10)"
    - "探针净足迹零: 原图回存 deep-equal 全等(剔 meta.updatedAt/lastEventId)"
  artifacts:
    - path: "scripts/verify-phase-60.ts"
      provides: "D-11 聚合门(S/B/D/F 四段,exit 0/1/2 契约)"
    - path: "package.json"
      provides: "verify:phase-60 注册"
      contains: "verify:phase-60"
    - path: "packages/infinite-canvas/test/e2e/probe-60-real.mjs"
      provides: "D-10 零足迹真机探针(协议段 + 真浏览器段)"
  key_links:
    - from: "verify-phase-60 S2"
      to: "FlowCanvas.tsx onGraphSaved"
      via: "次序锁: 基线重置行 index < savedBy 早退行 index(FLAG-1)"
      pattern: "lastEventCountRef"
    - from: "verify-phase-60 D 段"
      to: "scripts/diagnose-60-roundtrip.ts"
      via: "spawn --strict(exit 2 → WARN+记录,exit 1 → FAIL)"
      pattern: "--strict"
    - from: "probe-60-real.mjs 浏览器段"
      to: ":10588 已部署 dist"
      via: "playwright.chromium + ?testMode=1(bridge) + page.on('response') load-v2 计数"
      pattern: "testMode"
---

<objective>
Phase 60 收口双件: (1) verify:phase-60 聚合门(D-11,沿 verify-phase-59 骨架: 静态锁 + 行为门 +
dispatch + forced-failure 自检),把 FLAG-1/FLAG-2/FLAG-4 三条执行器红线转成机器锁; (2) probe-60-real
零足迹真机探针(D-10,协议段验 savedBy 回显契约 + 真浏览器段验面板保持),完成 PANEL-01 的真机级证据。

Purpose: SC3(mock/真机一致)的真机侧半边 + 全部机器可锁红线的最终防线。
Output: 聚合门脚本 + npm 注册 + 真机探针及运行记录。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-post-save-panel-persistence/60-RESEARCH.md
@.planning/phases/60-post-save-panel-persistence/60-UI-SPEC.md
@.planning/phases/60-post-save-panel-persistence/60-04-SUMMARY.md

<interfaces>
<!-- 骨架范式与锁定目标(已提取) -->

verify-phase-59.ts 骨架(逐段照搬): assert(cond,name,detail)/results 收集/read(rel)/exists(rel)/
  runCmd(name,cwdRel,cmd) + main() 末尾失败计数 → process.exit(0 全绿/1 任一失败/2 crash);
  行为门 spawn 子进程(49-01 教训: 不与父进程共享 knex/事件循环)。

package.json L51-52 既有: "verify:phase-58"/"verify:phase-59" 条目——"verify:phase-60" 紧随注册。

probe-59-real.mjs 范式(全文参读): BASE=:10588 · SCOPES=[[2,1],[2001,1]] + MIGRATE_SUPPORTED 守卫 ·
  stripUpdatedAt/deepEqual/firstDiff · io('/ws/projects', query projectId) 直收 · finally 原图回存 +
  ≤15s 轮询复核全等 · 部署前置头注释(build → deploy-canvas.sh → build:server → setsid nohup restart)·
  SKIP 条款(:10588 不可达 → 退出非零 + 延后不阻塞 verify 门,SUMMARY 记录)。

锁定目标(60-02/60-03 产物的 grep 锚):
  save-v2.ts: zod savedBy 行 + broadcast 条件展开
  FlowCanvas.tsx onGraphSaved: lastEventCountRef.current = null 行必须先于 savedBy 判定早退行(FLAG-1)
  FlowCanvas.tsx health-poll: scope.eventCount 读取保持原样(FLAG-2 正向)
  src/routes/canvas/v2/health.ts: 不得出现 eventCount(FLAG-2 负向——禁激活第二 reload 通道)
  mock server.mjs: suppressGraphSaved 全仓零命中(FLAG-4)
  canvasStore.ts: '[panel-persist]' warn 在场(D-03)
  clientTabId.ts + canvasApi saveCanvasGraph body savedBy(D-01 链)
  useCanvasSocket.ts: graph:saved payload 类型含 savedBy?(wire 类型链)

真机探针浏览器段: playwright 为包内既有 devDep——import { chromium } from 'playwright' 直接
  chromium.launch() 新实例(不走 playwright.config 的 :9876 baseURL,直连 http://localhost:10588)。
  页面参数 ?projectId&episodesId&testMode=1(bridge 挂载前提,helpers.loadCanvas 同型)。
  保存按钮: 'button:has-text("保存")';面板: [data-testid="detail-panel"];标题: 面板 header 文本。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: verify-phase-60 聚合门 + npm 注册</name>
  <files>scripts/verify-phase-60.ts, package.json</files>
  <read_first>
  - scripts/verify-phase-59.ts(全文——骨架/断言/自检/exit 契约逐段复用)
  - package.json L45-60(scripts 注册区)
  - 60-02/60-03 产物中被锁文件的现状(save-v2.ts / FlowCanvas.tsx onGraphSaved 段 / canvasStore.ts warn 段 / clientTabId.ts / useCanvasSocket.ts graph:saved 段 / mock server.mjs / src/routes/canvas/v2/health.ts)——锁的 grep 锚必须与实际文本一致
  </read_first>
  <action>
  (1) scripts/verify-phase-60.ts(verify-phase-59.ts 同骨架: assert/read/exists/runCmd/results/exit 0-1-2):
  S 静态锁段:
    S1 save-v2.ts 含 zod savedBy 行(z.string().max(64).optional)且 broadcast 行含 savedBy 条件展开;
    S2 FLAG-1 次序锁——提取 FlowCanvas.tsx onGraphSaved 块文本,断言 lastEventCountRef.current = null 行号 < savedBy 比较早退行号,且早退行引用 getClientTabId,toast+loadCanvas 在早退之后(他端分支);
    S3 FLAG-2 双向锁——health.ts 不含 'eventCount'(负向: 执行器没顺手修映射)且 FlowCanvas.tsx health-poll 段仍读 scope.eventCount(正向: 轮询逻辑原样);
    S4 FLAG-4——packages/infinite-canvas/test 目录 suppressGraphSaved 零命中;
    S5 D-01 链——clientTabId.ts 存在且 canvasApi.ts saveCanvasGraph body 含 savedBy: getClientTabId();
    S6 D-03——canvasStore.ts 含 '[panel-persist]' 且 setGraph 重锚行 rfNodes.find 语义保持(正则锚 n.id === 与 ?? null 共存);
    S7 useCanvasSocket.ts graph:saved payload 类型含 savedBy?。
  B 行为门段(spawn 子进程): 根 npx tsc --noEmit;cd packages/infinite-canvas && npx vitest run
    src/store/__tests__/reloadAnchor.test.ts;npm run build && npx playwright test
    test/e2e/tests/phase60-panel-persist.mjs(四用例整文件)。
  D dispatch 段: spawn npx tsx scripts/diagnose-60-roundtrip.ts --strict(仓库根)——exit 0 → PASS;
    exit 2(:10588 SKIP)→ 计 WARN 不计 FAIL(输出要求 SUMMARY 记录补验);exit 1 → FAIL。
  F forced-failure 自检段(门能红证明): 对 S2/S3/S4 三锁,以内存变异样本各跑一次同一检查函数——
    S2 变异样本=onGraphSaved 文本把基线重置挪到早退之后(必须判 false);S3 变异样本=health.ts 文本
    插入 eventCount(必须判 false);S4 变异样本=server.mjs 文本插入 suppressGraphSaved(必须判 false)。
    任一变异样本被检查判 true(锁失效却放行)→ 整门 exit 1。不修改任何真实文件——样本为脚本内字符串。
  (2) package.json scripts 增 "verify:phase-60": "npx tsx scripts/verify-phase-60.ts"(紧随 verify:phase-59;仅注册一次)。
  (3) 运行 npm run verify:phase-60 至全绿(行为门依赖 60-04 已 build 的 dist;必要时先 rebuild)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npm run verify:phase-60</automated>
  </verify>
  <acceptance_criteria>
  - npm run verify:phase-60 exit 0;输出含 S1-S7/B 三项/D 段结论与 F 段三组 must-fail 行为;
  - D 段 exit 2 场景被计为 WARN 且提示 SUMMARY 记录(不假绿不硬红);
  - package.json grep verify:phase-60 恰 1 处;
  - F 段对三个变异样本全部判 false(证明锁非恒真)。
  </acceptance_criteria>
  <done>D-11 聚合门落地: 三条 FLAG 红线与 D-01/D-03 链路全部机器锁死,且锁本身被证明可以失败。</done>
</task>

<task type="auto">
  <name>Task 2: probe-60-real 零足迹真机探针 + 部署运行</name>
  <files>packages/infinite-canvas/test/e2e/probe-60-real.mjs</files>
  <read_first>
  - packages/infinite-canvas/test/e2e/probe-59-real.mjs(全文——部署头/SCOPES 守卫/stripUpdatedAt/deepEqual/firstDiff/恢复范式逐行照搬)
  - packages/infinite-canvas/test/e2e/helpers.mjs loadCanvas(URL 参数形状——testMode=1)
  - 60-04-SUMMARY(mock 侧行为基线——真机断言对齐它)
  </read_first>
  <action>
  (1) 部署前置(头注释写明,本任务实际执行): cd packages/infinite-canvas && npm run build → 仓库根
  bash scripts/deploy-canvas.sh(自带备份)→ npm run build:server → kill 旧 pid 后 NODE_ENV=production
  PORT=10588 setsid nohup node data/serve/app.js 重启(probe-59 头部同款序列)。:10588 不可达且重启
  失败 → 打印 SKIP 理由 exit 非零(RESEARCH fallback 条款——延后不阻塞 verify 门,SUMMARY 记录)。
  (2) 新建 test/e2e/probe-60-real.mjs(node 直跑,不进 playwright test runner):
  段一(协议段,无浏览器): probe-59 式选 scope(SCOPES+MIGRATE_SUPPORTED 守卫)→ 捕获原图 originalGraph →
  io 连 /ws/projects room → saveV2(原图, savedBy: 'tab_probe60') → 等 graph:saved 广播(≤5s)→
  note 断言 payload.savedBy === 'tab_probe60'(真机回显契约)→ saveV2(原图, 无 savedBy)→ 等广播 →
  note 断言 payload 无 savedBy 键(他端形状,kmc pipeline 兼容面)。
  段二(真浏览器段,PANEL-01 真机): import { chromium } from 'playwright' → launch() → goto
  :10588/?projectId=<pid>&episodesId=<eid>&testMode=1 → 等 .react-flow__node → 页内装 toast spy
  (MutationObserver 同 60-04 实现,记 window.__toastLog)→ 记 load-v2 响应计数(page.on('response')
  过滤 /api/canvas/v2/load-v2)→ dblclick 一个资产节点 → detail-panel 可见 + 记标题 + bridge
  getDetailNode().id → click 'button:has-text("保存")' → waitForResponse **/api/canvas/v2/save-v2
  断言 200 且其 postData JSON 的 savedBy 为 /^tab_/ 开头字符串(真机客户端身份上 wire)→ 等 1500ms
  (回声)→ note 断言: detail-panel 仍可见;标题不变;getDetailNode().id 不变;__toastLog 为空
  (两条 toast 串零命中);load-v2 响应计数与保存前相等(零 reload——PANEL-01 决定性真机信号)。
  finally(零足迹): 关浏览器与 socket → saveV2(pid, eid, originalGraph) 原图回存 → ≤15s 轮询 load-v2
  stripUpdatedAt deepEqual 全等 + firstDiff 诊断输出(footprint 失败 → note FAIL → exitCode 1)。
  (3) 实际运行 node test/e2e/probe-60-real.mjs(cwd packages/infinite-canvas);PASS 行与 SKIP/FAIL
  如实记入 SUMMARY。VALIDATION.md Manual-Only 行(人眼 panelWidth/滚动连续性)标注待 UAT。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && node test/e2e/probe-60-real.mjs</automated>
  </verify>
  <acceptance_criteria>
  - 探针 exit 0: 段一两条回显断言 PASS + 段二五条面板断言 PASS + 恢复 deep-equal PASS(净足迹 0);
  - 或 SKIP(部署不可行): 理由输出 + SUMMARY 记录延后补跑命令(node test/e2e/probe-60-real.mjs);
  - 探针运行后 :10588 图内容与捕获原图全等(剔 meta 簿记字段);
  - 头注释含完整部署序列与 SKIP 条款。
  </acceptance_criteria>
  <done>D-10 完成: PANEL-01 在真机有面板保持 + 静默 + 零 reload 的端到端证据;savedBy 服务端契约真机实证;净足迹零。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe→:10588 生产(写+浏览器) | 探针对生产库真实 save-v2 写入并驱动已部署前端;必须在 finally 恢复 |
| verify 门→全仓源码 | 静态锁 grep 生产源码;forced-failure 仅作用于脚本内字符串样本,不写真实文件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-08 | Tampering | probe 写 :10588 | mitigate | 捕获-恢复 finally + stripUpdatedAt deep-equal 全等硬断言;两次探针性保存均回存原图内容;scope 守卫沿用 probe-59 |
| T-60-09 | Repudiation | verify 门假绿(锁恒真) | mitigate | F 段 forced-failure: 三个变异样本必须使对应锁判 false,否则整门红——锁的可失败性被机器证明 |
| T-60-10 | DoS(弱) | 真浏览器段点击生产 UI | accept | 单次保存点击 + 只读断言;保存内容为当前已部署真相的序列化往返,无破坏性操作 |
| T-60-SC | Tampering | npm installs | accept | 零新依赖;playwright/chromium 为包内既有 devDep |
</threat_model>

<verification>
- npm run verify:phase-60 → exit 0(S/B/D/F 四段全绿,D 段允许 WARN-SKIP);
- node test/e2e/probe-60-real.mjs → exit 0(或 SKIP 记录在案);
- :10588 恢复全等(探针自身输出「恢复(净足迹) PASS」)。
</verification>

<success_criteria>
- Phase 60 三条 SC 全闭合: SC1 真机侧由探针浏览器段收口;SC2 由 60-03 锁 + 60-04 e2e;SC3 mock=真机
  等价由「客户端跳过 + 双端同形契约 + 探针协议段」三方证据钉死;
- D-08/D-10/D-11/D-12 全部落地;三条 FLAG 红线机器锁死且被证明可失败;
- 唯一 Manual-Only 项(人眼 panelWidth/滚动连续性)已标注待 UAT,不阻塞 verify 门。
</success_criteria>

<output>
Create `.planning/phases/60-post-save-panel-persistence/60-05-SUMMARY.md` when done。
SUMMARY 记录: verify 门四段结论、探针逐 note 结果(或 SKIP 理由与补跑命令)、VALIDATION.md 状态同步。
</output>
