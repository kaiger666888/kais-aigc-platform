---
phase: 16-v6-code-merge
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - docker/gold-team/src/v6/engine_pool.py
  - docker/gold-team/src/v6/gpu_monitor.py
  - docker/gold-team/src/v6/middleware/__init__.py
  - docker/gold-team/src/v6/middleware/gpu_guard.py
  - docker/gold-team/src/v6/engines/acestep.py
  - docker/gold-team/src/v6/engines/hunyuan3d.py
  - docker/gold-team/src/v6/engines/hunyuan3d_mv.py
  - docker/gold-team/src/v6/engines/workflows/__init__.py
  - docker/gold-team/src/v6/engines/workflows/ipadapter_flux.py
  - docker/gold-team/src/v6/routes/v1/__init__.py
  - docker/gold-team/src/v6/routes/v1/gpu.py
  - docker/gold-team/src/v6/config/engine_registry.py
  - docker/gold-team/src/v6/config/engine_schema.py
  - docker/gold-team/src/v6/config/gpu_config.py
  - docker/gold-team/src/v6/docker/container_manager.py
  - docker/gold-team/src/v6/engine/router.py
  - docker/gold-team/src/v6/engines/cloud_base.py
  - docker/gold-team/src/v6/engines/cloud_seedance.py
  - docker/gold-team/src/v6/engines/comfyui.py
  - docker/gold-team/src/v6/engines/docker_base.py
  - docker/gold-team/src/v6/engines/docker_cli.py
  - docker/gold-team/src/v6/engines/docker_polling.py
  - docker/gold-team/src/v6/engines/__init__.py
  - docker/gold-team/src/v6/engines/tts.py
  - docker/gold-team/src/v6/engines/workflow_builder.py
  - docker/gold-team/src/v6/executor.py
  - docker/gold-team/src/v6/main.py
  - docker/gold-team/src/v6/models/task.py
  - docker/gold-team/src/v6/routers/engines.py
  - docker/gold-team/src/v6/engines/joycaption.py
  - docker/gold-team/src/v6/engines/tts_http.py
autonomous: true
requirements: [MERGE-02]

must_haves:
  truths:
    - "All new research files exist in the deploy repo and are byte-identical to research originals"
    - "All modified files use the research version while preserving deploy-only additions"
    - "Deploy-only files (joycaption.py, tts_http.py) are untouched"
    - "main.py merges research engine registrations with deploy-specific TTS subprocess and JoyCaption"
    - "engines/__init__.py exports both research and deploy engine classes"
    - "Python imports resolve correctly (no broken import chains)"
  artifacts:
    - path: "docker/gold-team/src/v6/engine_pool.py"
      provides: "GPU engine lifecycle pool"
      contains: "class EnginePool"
    - path: "docker/gold-team/src/v6/engines/hunyuan3d.py"
      provides: "Hunyuan3D-2 subprocess engine"
      contains: "class Hunyuan3DEngine"
    - path: "docker/gold-team/src/v6/engines/hunyuan3d_mv.py"
      provides: "Hunyuan3D-2mv multiview subprocess engine"
      contains: "class Hunyuan3DMvEngine"
    - path: "docker/gold-team/src/v6/gpu_monitor.py"
      provides: "GPU VRAM monitor"
      contains: "def get_gpu_vram_usage"
    - path: "docker/gold-team/src/v6/middleware/gpu_guard.py"
      provides: "API middleware for GPU VRAM guard"
      contains: "class GPUGuardMiddleware"
    - path: "docker/gold-team/src/v6/main.py"
      provides: "FastAPI entry point with all engines registered"
      contains: "JoyCaption"
  key_links:
    - from: "docker/gold-team/src/v6/main.py"
      to: "engine_pool.py"
      via: "from src.v6.engine_pool import EnginePool"
      pattern: "from src\\.v6\\.engine_pool"
    - from: "docker/gold-team/src/v6/main.py"
      to: "engines/hunyuan3d.py"
      via: "from src.v6.engines.hunyuan3d import Hunyuan3DEngine"
      pattern: "from src\\.v6\\.engines\\.hunyuan3d"
    - from: "docker/gold-team/src/v6/main.py"
      to: "middleware/gpu_guard.py"
      via: "from src.v6.middleware.gpu_guard import GPUGuardMiddleware"
      pattern: "GPUGuardMiddleware"
---

<objective>
Merge all research repo engine code into the deploy repo.

Purpose: The deploy repo must encompass all research features -- Hunyuan3D-2mv, ACE-Step, EnginePool, GPU monitoring, dual ComfyUI routing, new TaskTypes, and updated workflow builders -- while preserving deploy-only additions (JoyCaption, TripleTrackTTSEngine, tts_unified_server).
Output: Deploy repo with all research features merged, deploy-only features intact, no broken imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-16/16-CONTEXT.md
@.planning/phases/phase-16/16-MANIFEST.md

<interfaces>
<!-- Key types the executor needs to understand for the merge -->

From src/v6/models/task.py (research version adds these TaskTypes):
```python
class TaskType(str, Enum):
    TTS_ZH = "tts_zh"
    TTS_EN = "tts_en"
    TTS_BILINGUAL = "tts_bilingual"
    IMAGE_PULID = "image_pulid"
    IMAGE_DRAW_IPADAPTER = "image_draw_ipadapter"
    CONTROLNET_DEPTH = "controlnet_depth"
    WAN_I2V = "wan_i2v"
    IMAGE_TO_3D_MV = "image_to_3d_mv"
    # ... existing types remain
```

From src/v6/engines/base.py (unchanged between repos):
```python
class BaseEngine(ABC):
    async def submit(self, workflow, params=None) -> str
    async def poll(self, engine_job_id) -> dict
    async def get_output(self, engine_job_id) -> dict
    async def cancel(self, engine_job_id) -> bool
    async def health(self) -> dict
```

From src/v6/engines/__init__.py (research version exports):
```python
from src.v6.engines.tts import TTSTracker, TTSTrack
from src.v6.engines.hunyuan3d import Hunyuan3DEngine
```

From src/v6/engines/__init__.py (deploy version exports):
```python
from src.v6.engines.tts import TTSEngine
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy new research files to deploy repo and overwrite modified files with research versions</name>
  <files>
    docker/gold-team/src/v6/engine_pool.py,
    docker/gold-team/src/v6/gpu_monitor.py,
    docker/gold-team/src/v6/middleware/__init__.py,
    docker/gold-team/src/v6/middleware/gpu_guard.py,
    docker/gold-team/src/v6/engines/acestep.py,
    docker/gold-team/src/v6/engines/hunyuan3d.py,
    docker/gold-team/src/v6/engines/hunyuan3d_mv.py,
    docker/gold-team/src/v6/engines/workflows/__init__.py,
    docker/gold-team/src/v6/engines/workflows/ipadapter_flux.py,
    docker/gold-team/src/v6/routes/v1/gpu.py,
    docker/gold-team/src/v6/config/engine_registry.py,
    docker/gold-team/src/v6/config/engine_schema.py,
    docker/gold-team/src/v6/config/gpu_config.py,
    docker/gold-team/src/v6/docker/container_manager.py,
    docker/gold-team/src/v6/engines/cloud_base.py,
    docker/gold-team/src/v6/engines/cloud_seedance.py,
    docker/gold-team/src/v6/engines/comfyui.py,
    docker/gold-team/src/v6/engines/docker_base.py,
    docker/gold-team/src/v6/engines/docker_cli.py,
    docker/gold-team/src/v6/engines/docker_polling.py,
    docker/gold-team/src/v6/engine/router.py,
    docker/gold-team/src/v6/engines/tts.py,
    docker/gold-team/src/v6/engines/workflow_builder.py,
    docker/gold-team/src/v6/executor.py,
    docker/gold-team/src/v6/models/task.py,
    docker/gold-team/src/v6/routers/engines.py
  </files>
  <action>
**Phase A: Create new directories and copy new files (straight copy from research).**

Research repo path: /home/kai/workspace/kais-gold-team/src/v6/
Deploy repo path: /data/workspace/kais-aigc-platform/docker/gold-team/src/v6/

Create these directories in deploy repo if they do not exist:
- docker/gold-team/src/v6/middleware/
- docker/gold-team/src/v6/engines/workflows/
- docker/gold-team/src/v6/routes/v1/

Copy these files byte-for-byte from research to deploy:
1. engine_pool.py (new -- engine lifecycle pool)
2. gpu_monitor.py (new -- GPU VRAM monitor)
3. middleware/__init__.py (new -- middleware package)
4. middleware/gpu_guard.py (new -- GPU guard middleware)
5. engines/acestep.py (new -- ACE-Step internal engine)
6. engines/hunyuan3d.py (new -- Hunyuan3D-2 engine)
7. engines/hunyuan3d_mv.py (new -- Hunyuan3D-2mv multiview engine)
8. engines/workflows/__init__.py (new -- workflows package)
9. engines/workflows/ipadapter_flux.py (new -- IP-Adapter workflow builder)
10. routes/v1/gpu.py (new -- GPU management API)

Also create routes/v1/__init__.py if it does not exist (empty file for Python package).

**Phase B: Overwrite modified files with research versions (straight copy).**

Copy these files from research to deploy, replacing the deploy version:
1. config/engine_registry.py (adds gpu_device field)
2. config/engine_schema.py (adds gpu_device to schema)
3. config/gpu_config.py (dual-GPU docs)
4. docker/container_manager.py (dynamic GPU flag)
5. engine/router.py (dual ComfyUI, dedicated engine mappings, new TaskTypes)
6. engines/cloud_base.py (adds name/engine_id properties)
7. engines/cloud_seedance.py (base URL change)
8. engines/comfyui.py (multi-instance profiles, engine_id parameter)
9. engines/docker_base.py (dynamic GPU, stop/cleanup)
10. engines/docker_cli.py (dynamic GPU flag)
11. engines/docker_polling.py (ACE-Step parameter profiles)
12. engines/tts.py (TTSTracker replaces TTSEngine)
13. engines/workflow_builder.py (research version)
14. executor.py (new TaskTypes, Hunyuan3D auto-build, FLUX workflows, TTS track routing)
15. models/task.py (new TaskTypes)
16. routers/engines.py (research version)

**Phase C: DO NOT TOUCH these deploy-only files:**
- engines/joycaption.py -- leave completely untouched
- engines/tts_http.py -- leave completely untouched

**Phase D: Merge engines/__init__.py.**

The research version exports TTSTracker, TTSTrack, Hunyuan3DEngine. The deploy version exports TTSEngine. The merged file must export ALL of them:
- TTSTracker, TTSTrack (from research tts.py)
- Hunyuan3DEngine (from new engines/hunyuan3d.py)
- TTSEngine (from deploy-only tts.py -- still used by tts_http.py)
- All other existing exports (base classes, etc.)

Read the research engines/__init__.py, add the deploy-only exports (TTSEngine from tts module). The research version already includes TTSTracker, TTSTrack, Hunyuan3DEngine.

**Phase E: Copy missing scripts from research repo.**

Research scripts path: /home/kai/workspace/kais-gold-team/scripts/
Deploy scripts path: /data/workspace/kais-aigc-platform/docker/gold-team/scripts/

Copy these scripts from research to deploy (they are referenced by Hunyuan3D engines and TTS tracker):
- hunyuan3d_infer.py
- hunyuan3d_mv_infer.py
- flux_generate.py
- tts_infer.py
- tts_manager.py
- tts_chatterbox_server.py
- tts_cosyvoice_server.py
- tts_gpt_sovits_direct.py
- tts_gpt_sovits_server.py
- download_trellis_models.sh

Do NOT overwrite tts_unified_server.py (deploy-only).

After all copies, verify that the deploy repo has NO missing Python imports by running:
```bash
cd /data/workspace/kais-aigc-platform && python3 -c "import ast; ast.parse(open('docker/gold-team/src/v6/main.py').read())" && echo "main.py syntax OK"
```
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && python3 -c "import ast; ast.parse(open('docker/gold-team/src/v6/main.py').read()); ast.parse(open('docker/gold-team/src/v6/executor.py').read()); ast.parse(open('docker/gold-team/src/v6/engine/router.py').read()); print('All critical files parse OK')" && test -f docker/gold-team/src/v6/engine_pool.py && test -f docker/gold-team/src/v6/engines/hunyuan3d.py && test -f docker/gold-team/src/v6/engines/hunyuan3d_mv.py && test -f docker/gold-team/src/v6/gpu_monitor.py && test -f docker/gold-team/src/v6/middleware/gpu_guard.py && test -f docker/gold-team/src/v6/routes/v1/gpu.py && test -f docker/gold-team/src/v6/engines/acestep.py && test -f docker/gold-team/src/v6/engines/workflows/ipadapter_flux.py && test -f docker/gold-team/src/v6/engines/joycaption.py && test -f docker/gold-team/src/v6/engines/tts_http.py && echo "All expected files present"</automated>
  </verify>
  <done>
    All new research files copied to deploy repo. All modified files overwritten with research version. Deploy-only files (joycaption.py, tts_http.py) preserved intact. engines/__init__.py merged to export both research and deploy engine classes. Missing scripts copied. Python syntax check passes on main.py, executor.py, router.py.
  </done>
</task>

<task type="auto">
  <name>Task 2: Merge main.py to combine research engine registrations with deploy-specific features</name>
  <files>docker/gold-team/src/v6/main.py</files>
  <action>
The main.py from the research repo is the source of truth BUT the deploy repo has critical features that must be preserved. Create a merged main.py that includes BOTH.

**Take from research main.py:**
1. Import EnginePool, get_engine_pool from src.v6.engine_pool
2. Import TTSTracker from src.v6.engines.tts
3. Import ACEStepEngine from src.v6.engines.acestep (commented out, same as research)
4. Import Hunyuan3DEngine from src.v6.engines.hunyuan3d
5. Import Hunyuan3DMvEngine from src.v6.engines.hunyuan3d_mv
6. Import get_gpu_vram_usage from src.v6.gpu_monitor
7. Import GPUGuardMiddleware from src.v6.middleware.gpu_guard
8. Import gpu_router from src.v6.routes.v1.gpu
9. Dual ComfyUI env vars (COMFYUI_PRIMARY_HOST/PORT, COMFYUI_AUX_HOST/PORT)
10. TTSTracker registration (replaces TTSEngine registration)
11. Hunyuan3D engine registration
12. Hunyuan3D-2mv engine registration
13. Dual ComfyUI mode (primary + auxiliary) with engine_id parameter
14. EnginePool registration for local Docker engines from engines/*.yaml
15. GPU guard middleware registration
16. gpu_router registration on app
17. EnginePool unload_all on shutdown

**Preserve from deploy main.py:**
1. Import TTSEngine from src.v6.engines.tts (legacy fallback)
2. Import TripleTrackTTSEngine from src.v6.engines.tts_http
3. TTS unified server subprocess management (TTS_UNIFIED_SCRIPT, TTS_UNIFIED_ENABLED, TTS_UNIFIED_PORT)
4. TTS unified subprocess startup in lifespan (create_subprocess_exec)
5. TripleTrackTTSEngine registration
6. Legacy TTSEngine registration as fallback
7. JoyCaption engine registration
8. TTS unified subprocess termination on shutdown

**The merged lifespan ordering should be:**
1. Mock engine
2. TTS unified subprocess startup (deploy)
3. TTSTracker engine registration (research -- 3-track tracker)
4. TripleTrackTTSEngine registration (deploy -- HTTP engine)
5. Legacy TTSEngine fallback registration (deploy)
6. ACE-Step engine (research -- commented out, disabled)
7. Hunyuan3D-2 engine (research)
8. Hunyuan3D-2mv engine (research)
9. ComfyUI engines -- dual mode with engine_id (research)
10. JoyCaption engine (deploy)
11. Cloud engines (shared)
12. Executor start
13. EnginePool registration from engines/*.yaml (research)
14. Local pool fallback (shared)

**Router registration (merged):**
- health.router, tasks.router, engines.router, events.router (both)
- gpu_router (research)

**Middleware (research):**
- app.add_middleware(GPUGuardMiddleware)

**Shutdown (merged):**
- executor.stop()
- local_pool.stop()
- EnginePool.unload_all() (research)
- TTS unified subprocess termination (deploy)

Write the complete merged main.py. It should be syntactically valid Python with no unresolved imports. All imports reference files that exist in the deploy repo after Task 1 copy.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && python3 -c "import ast; ast.parse(open('docker/gold-team/src/v6/main.py').read()); print('main.py syntax OK')" && grep -c "JoyCaption\|TripleTrackTTSEngine\|TTSTracker\|Hunyuan3DEngine\|EnginePool\|GPUGuardMiddleware\|gpu_router\|tts_unified_process" docker/gold-team/src/v6/main.py | grep -qv "^0$"</automated>
  </verify>
  <done>
    main.py merges research engine registrations (TTSTracker, Hunyuan3D, Hunyuan3D-2mv, dual ComfyUI, EnginePool, GPUGuardMiddleware, GPU routes) with deploy-specific features (TripleTrackTTSEngine, JoyCaption, TTS unified subprocess). Python syntax valid. All referenced imports exist in deploy repo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Research repo (read-only source) | Source of truth for engine code |
| Deploy repo (write target) | Production codebase being updated |
| Deploy-only code (preserve) | joycaption.py, tts_http.py must not be lost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering | File copy operations | mitigate | Copy from absolute research path, verify with diff after copy |
| T-16-04 | Tampering | Deploy-only file preservation | mitigate | Explicit list of files that MUST NOT be overwritten; verify existence after merge |
| T-16-05 | Information Disclosure | cloud_seedance.py base URL | accept | Internal service URL, not externally accessible |
| T-16-SC | Tampering | File system operations | mitigate | All paths verified before write, no glob-based destructive operations |
</threat_model>

<verification>
1. All new files from research exist in deploy repo (engine_pool.py, gpu_monitor.py, middleware/*, engines/acestep.py, engines/hunyuan3d*.py, engines/workflows/*, routes/v1/gpu.py)
2. All modified files overwritten with research version
3. Deploy-only files (joycaption.py, tts_http.py) untouched -- byte-identical to pre-merge state
4. engines/__init__.py exports both research and deploy engine classes
5. main.py merges both feature sets, passes Python syntax check
6. Key identifiers present in main.py: TTSTracker, TripleTrackTTSEngine, JoyCaption, Hunyuan3DEngine, Hunyuan3DMvEngine, EnginePool, GPUGuardMiddleware
7. All Python files parse without syntax errors
</verification>

<success_criteria>
- Deploy repo contains all 10 new research files
- All 19 modified files use research version
- Deploy-only files (joycaption.py, tts_http.py) preserved intact
- main.py merges research + deploy features, syntax-valid
- engines/__init__.py exports all engine classes
- No broken Python imports in merged codebase
- Research scripts copied to deploy scripts/ directory
</success_criteria>

<output>
Create `.planning/phases/phase-16/16-02-SUMMARY.md` when done
</output>
