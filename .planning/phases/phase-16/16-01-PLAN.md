---
phase: 16-v6-code-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/phase-16/16-MANIFEST.md
autonomous: true
requirements: [MERGE-01]

must_haves:
  truths:
    - "Every file that differs between research and deploy repos is enumerated in the manifest"
    - "New files, modified files, and deploy-only files are clearly categorized"
    - "Each modified file has a diff summary describing what changed"
  artifacts:
    - path: ".planning/phases/phase-16/16-MANIFEST.md"
      provides: "Complete change manifest with categories and diff summaries"
      contains: "## New Files"
  key_links:
    - from: "16-MANIFEST.md"
      to: "Plan 16-02"
      via: "file list drives copy/merge actions"
      pattern: "research.*deploy"
---

<objective>
Generate a comprehensive diff report and change manifest comparing the research repo (kais-gold-team/src/v6/) with the deploy repo (docker/gold-team/src/v6/).

Purpose: Before merging, we need a definitive catalog of every file change. This manifest becomes the checklist for Plan 16-02 (merge execution) and Plan 16-03 (regression verification).
Output: 16-MANIFEST.md containing categorized file lists with diff summaries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-16/16-CONTEXT.md

Research repo: /home/kai/workspace/kais-gold-team/src/v6/
Deploy repo: docker/gold-team/src/v6/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate full diff report and change manifest</name>
  <files>.planning/phases/phase-16/16-MANIFEST.md</files>
  <action>
Run a comprehensive diff between the research repo (kais-gold-team/src/v6/) and the deploy repo (docker/gold-team/src/v6/). Produce 16-MANIFEST.md with three sections:

**Section 1: New Files (research-only, to be copied)**
List every file that exists in the research repo but not in the deploy repo. Include line count and a one-line description of purpose. These files will be straight-copied in Plan 16-02.

New files to expect:
- engine_pool.py (GPU engine lifecycle manager)
- engines/acestep.py (ACE-Step internal subprocess music engine)
- engines/hunyuan3d.py (Hunyuan3D-2 image-to-3D subprocess engine)
- engines/hunyuan3d_mv.py (Hunyuan3D-2mv multiview image-to-3D subprocess engine)
- engines/workflows/__init__.py (workflow builders package init)
- engines/workflows/ipadapter_flux.py (IP-Adapter + Flux workflow builder)
- gpu_monitor.py (GPU VRAM monitor with nvidia-smi queries)
- middleware/__init__.py (middleware package init)
- middleware/gpu_guard.py (GPU VRAM guard middleware for API requests)
- routes/v1/gpu.py (GPU status and management API endpoints)

**Section 2: Modified Files (exist in both, content differs)**
For each file, include: file path, diff line count, and a summary of what the research version adds/changes vs the deploy version. Key modified files to cover:
- config/engine_registry.py (adds gpu_device field)
- config/engine_schema.py (adds gpu_device to schema)
- config/gpu_config.py (dual-GPU configuration docs)
- docker/container_manager.py (dynamic GPU device flag)
- engine/router.py (dual ComfyUI routing, dedicated engine mappings, light task types, new TaskTypes)
- engines/cloud_base.py (adds name/engine_id properties)
- engines/cloud_seedance.py (base URL change)
- engines/comfyui.py (multi-instance profiles, error extraction helper, engine_id parameter)
- engines/docker_base.py (dynamic GPU flag, stop/cleanup method)
- engines/docker_cli.py (dynamic GPU flag)
- engines/docker_polling.py (ACE-Step parameter profiles system)
- engines/__init__.py (exports TTSTracker/TTSTrack, Hunyuan3DEngine instead of TTSEngine)
- engines/tts.py (740-line TTSTracker vs 271-line TTSEngine -- different TTS architecture)
- engines/workflow_builder.py (1448 lines vs 1646 lines -- different workflow sets)
- executor.py (new TaskTypes, Hunyuan3D workflow auto-build, FLUX workflows, TTS track routing)
- main.py (completely different engine startup: EnginePool, GPUGuard, dual ComfyUI, Hunyuan3D, TTSTracker)
- models/task.py (new TaskTypes: TTS_ZH, TTS_EN, TTS_BILINGUAL, IMAGE_PULID, etc.)
- routers/engines.py (likely API endpoint additions for new engines)

**Section 3: Deploy-Only Files (must be preserved)**
Files that exist in the deploy repo but NOT in the research repo. These must NOT be deleted during merge:
- engines/joycaption.py (image captioning via ComfyUI)
- engines/tts_http.py (Triple-Track TTS HTTP engine)
- scripts/tts_unified_server.py (unified TTS subprocess server)

Also check for any scripts/ directory differences: research has 11 scripts (hunyuan3d_infer.py, hunyuan3d_mv_infer.py, flux_generate.py, etc.) while deploy has only 1 (tts_unified_server.py). List the scripts that need to be copied.

For each section, include a total file count. At the top of the manifest, include a summary table:

| Category | Count | Action |
|----------|-------|--------|
| New files | N | Copy from research |
| Modified files | N | Overwrite with research version, preserving deploy-only additions |
| Deploy-only files | N | Preserve unchanged |
| Scripts to copy | N | Copy from research scripts/ |
  </action>
  <verify>
    <automated>test -f .planning/phases/phase-16/16-MANIFEST.md && grep -c "^| " .planning/phases/phase-16/16-MANIFEST.md | grep -qv "^0$"</automated>
  </verify>
  <done>
    16-MANIFEST.md exists with three categorized sections (New, Modified, Deploy-only), summary table at top, and every file from the diff analysis accounted for. Total counts match actual diff results.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Research repo (read-only) | External code source, not modified during this phase |
| Deploy repo (write target) | Production codebase, merge destination |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | File copy operations | mitigate | Manifest checksums verified before and after copy |
| T-16-02 | Information Disclosure | Diff output may contain config URLs | accept | Manifest is internal planning artifact, not deployed |
| T-16-SC | Tampering | File system operations | mitigate | All operations through verified paths, no shell injection vectors |
</threat_model>

<verification>
1. 16-MANIFEST.md exists and is non-empty
2. All three sections present: New Files, Modified Files, Deploy-only Files
3. Summary table at top with counts matching section contents
4. Every file from diff analysis accounted for (no gaps)
</verification>

<success_criteria>
- Change manifest enumerates every file difference between research and deploy repos
- New, modified, and deploy-only files are clearly separated
- Diff summaries describe what each change introduces
- Manifest ready to drive Plan 16-02 merge actions
</success_criteria>

<output>
Create `.planning/phases/phase-16/16-01-SUMMARY.md` when done
</output>
