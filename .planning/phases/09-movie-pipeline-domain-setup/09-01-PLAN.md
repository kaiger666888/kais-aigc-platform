---
phase: 09-movie-pipeline-domain-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/hermes-agent/scripts/register_movie_pipeline.py
  - docker/hermes-agent/tests/test_movie_pipeline_domain.py
autonomous: true
requirements: [MOVIE-01, MOVIE-03, MOVIE-04]

must_haves:
  truths:
    - "movie-pipeline domain is registered with exactly 10 tasks"
    - "SOUL.md defines the movie-pipeline domain identity as a film production decision advisor"
    - "Seed memory contains structured parameter defaults for soul-visual, video-gen, and voice tasks"
    - "GET /v1/domains returns movie-pipeline in the domain list"
    - "GET /v1/domains/movie-pipeline/skills returns the skills list (empty or populated depending on Plan 02)"
  artifacts:
    - path: "docker/hermes-agent/scripts/register_movie_pipeline.py"
      provides: "Registration script for movie-pipeline domain"
      contains: "DomainRegistry"
    - path: "docker/hermes-agent/tests/test_movie_pipeline_domain.py"
      provides: "Verification tests for MOVIE-01, MOVIE-03, MOVIE-04"
      contains: "test_register_movie_pipeline"
  key_links:
    - from: "scripts/register_movie_pipeline.py"
      to: "src/core/domain_registry.py"
      via: "import DomainRegistry, call register()"
      pattern: "DomainRegistry"
    - from: "scripts/register_movie_pipeline.py"
      to: "src/core/domain_memory.py"
      via: "write audit_history.json seed data"
      pattern: "audit_history"
---

<objective>
Create the movie-pipeline domain registration script that registers the domain with DomainRegistry, writes SOUL.md defining the domain identity, and injects HERMES_DEFAULTS parameter defaults as seed memory into audit_history.json.

Purpose: Establish the movie-pipeline domain as the first real domain in the hermes-agent system, with structured seed data that future decide() calls can leverage.
Output: register_movie_pipeline.py script + verification tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-movie-pipeline-domain-setup/09-CONTEXT.md
@.planning/phases/09-movie-pipeline-domain-setup/09-RESEARCH.md

<interfaces>
<!-- Key interfaces the executor needs. Extracted from codebase. -->

From docker/hermes-agent/src/core/domain_registry.py:
```python
class DomainRegistry:
    def __init__(self, base_dir: Path | None = None) -> None:
        # Default: Path.home() / ".hermes" / "domains"

    def register(
        self,
        domain: str,
        description: str,
        tasks: list[str],
        skills_manifest: dict[str, Any],
    ) -> dict[str, Any]:
        # Creates skills/, memory/, SOUL.md (empty if not exists)
        # Updates registry.json

    def get_skills(self, domain: str) -> list[str]:
        # Returns sorted .md stems from skills/ dir

    def domain_exists(self, domain: str) -> bool:
        # Check if domain is in registry.json
```

From docker/hermes-agent/src/core/domain_memory.py:
```python
class DomainMemory:
    def __init__(self, memory_dir: Path) -> None:
        self.history_path = memory_dir / "audit_history.json"

    def append_record(self, task: str, record: dict[str, Any]) -> None:
        # Appends record, prunes, recomputes EWMA

    def get_confidence(self, task: str) -> float:
        # Returns EWMA confidence for task

# audit_history.json format:
# {task_name: {"records": [...], "ewma_confidence": 0.0}}
# Each record: {"decision_id": str, "outcome": str, "metrics": {...}, "timestamp": str}
# MIN_AUDITS_FOR_CONFIDENCE = 3 -- seed with 1 record per task means confidence stays 0.0
```

From docker/hermes-agent/tests/conftest.py:
```python
# Fixtures: tmp_hermes_dir, registry (DomainRegistry with tmp dir),
# mock_agent, agent_factory, decision_engine, client (TestClient with DI overrides)
# SYS_PATH_ENTRY = str(Path(__file__).resolve().parent.parent)
```

From docker/hermes-agent/src/core/agent_factory.py:
```python
# AgentFactory reads SOUL.md from domain_dir / "SOUL.md"
# Passes as ephemeral_system_prompt to AIAgent
# AIAgent created fresh per call, no caching
```

10 pipeline tasks (from pipeline.js PHASES, excluding quality-gate per CONTEXT.md):
["requirement", "art-direction", "character", "scenario", "voice", "storyboard", "scene", "camera-preview", "camera-final", "post-production"]

Seed memory tasks (from CONTEXT.md specific ideas):
- soul-visual: FLUX image generation params (steps, guidance_scale, sampler, scheduler, width, height, denoise, seed)
- video-gen: Wan2.2 video generation params (width, height, num_frames, fps, cfg, shift, total_steps)
- voice: CosyVoice3 TTS params (text, voice, speed)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create register_movie_pipeline.py script</name>
  <files>docker/hermes-agent/scripts/register_movie_pipeline.py</files>
  <read_first>
    - docker/hermes-agent/src/core/domain_registry.py (DomainRegistry.register() signature and behavior)
    - docker/hermes-agent/src/core/domain_memory.py (audit_history.json format)
    - docker/hermes-agent/src/config.py (Settings.hermes_home default path)
    - data/skills/ (verify 13 root .md files exist for later migration)
  </read_first>
  <behavior>
    - Script imports DomainRegistry from src.core.domain_registry
    - Script registers movie-pipeline domain with exactly 10 tasks: requirement, art-direction, character, scenario, voice, storyboard, scene, camera-preview, camera-final, post-production
    - Script writes SOUL.md to the domain directory (only if file does not already exist)
    - Script writes seed audit_history.json with parameter defaults for soul-visual, video-gen, and voice tasks
    - Script has idempotent re-run behavior (SOUL.md not overwritten, registry updated, memory seed preserved or appended)
  </behavior>
  <action>
    Create docker/hermes-agent/scripts/ directory (it does not exist yet). Then create register_movie_pipeline.py with these steps:

    1. PROJECT_ROOT detection: use Path(__file__).resolve().parent.parent.parent.parent to get the workspace root. Import sys and insert the hermes-agent src/ directory into sys.path (Path(__file__).resolve().parent.parent / "src").

    2. Define TASKS list with exactly 10 strings: "requirement", "art-direction", "character", "scenario", "voice", "storyboard", "scene", "camera-preview", "camera-final", "post-production". Per CONTEXT.md specific ideas and research task mapping.

    3. Define SKILLS_MANIFEST dict with two entries: "art_styles" -> {"count": 11, "path": "data/skills/art_skills/"} and "story_types" -> {"count": 11, "path": "data/skills/story_skills/"}. Per CONTEXT.md decision D-01 that art/story skills are manifest references only.

    4. Call DomainRegistry.register() with domain="movie-pipeline", description="AI short film production pipeline - intelligent decision engine", tasks=TASKS, skills_manifest=SKILLS_MANIFEST. Use base_dir from Settings.hermes_home / "domains" (default ~/.hermes/domains).

    5. Write SOUL.md to the domain directory ONLY if it does not already exist (check with path.exists()). SOUL.md content per CONTEXT.md decision: bilingual (Chinese primary, English terms), persona as movie-pipeline decision advisor with capabilities across script, art, cinematography, post-production. Include: role definition, core capabilities (FLUX/Wan2.2/CosyVoice parameter recommendation, historical optimization, quality/speed/resource balance), decision philosophy (character consistency first, multiple creative options, experience-based defaults that improve with audit data), and domain line "movie-pipeline". Use the template from 09-RESEARCH.md Code Examples section as a starting point.

    6. Write seed memory to memory/audit_history.json. Structure: dict keyed by task name, each value has "records" list and "ewma_confidence": 0.0. Seed records for three tasks:
      - "soul-visual": one seed record with decision_id "seed-flux-001", outcome "seed", metrics containing task "soul-visual", score 7, and params.flux dict with steps:20, guidance_scale:3.5, sampler:"euler", scheduler:"normal", width:1024, height:1024, denoise:1.0, seed:-1
      - "video-gen": one seed record with decision_id "seed-wan-001", outcome "seed", metrics containing task "video-gen", score 7, and params.wan dict with width:832, height:480, num_frames:81, fps:16, cfg:3.5, shift:5.0, total_steps:20
      - "voice": one seed record with decision_id "seed-tts-001", outcome "seed", metrics containing task "voice", score 7, and params.tts dict with voice:"default", speed:1.0
      All records get timestamp "2026-06-06T00:00:00+00:00". Per CONTEXT.md decision on structured JSON by task, and research Pitfall 5 that EWMA stays 0.0 with only 1 record (expected, not a bug).

    7. Print summary: registered tasks, SOUL.md status, seed memory tasks.

    Do NOT copy skill files in this task (that is Plan 02). Do NOT import httpx or call HTTP endpoints (use direct DomainRegistry import per research recommendation). Do NOT overwrite existing SOUL.md on re-run.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/docker/hermes-agent && python3 -m pytest tests/test_movie_pipeline_domain.py -x -q -k "test_register_movie_pipeline or test_soul_md or test_seed_memory" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - register_movie_pipeline.py exists in docker/hermes-agent/scripts/
    - Script defines exactly 10 tasks matching pipeline.js PHASES (excluding quality-gate)
    - Script writes SOUL.md only if it does not already exist
    - Script writes seed audit_history.json with soul-visual, video-gen, voice task defaults
    - Script uses DomainRegistry import directly (not HTTP)
    - Verification tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create verification tests for domain registration, SOUL.md, and seed memory</name>
  <files>docker/hermes-agent/tests/test_movie_pipeline_domain.py</files>
  <read_first>
    - docker/hermes-agent/tests/conftest.py (fixture patterns: tmp_hermes_dir, registry, client)
    - docker/hermes-agent/tests/test_integration.py (TestFullFlow pattern for register->decide->audit)
    - docker/hermes-agent/src/core/domain_registry.py (get_skills, domain_exists, register)
    - docker/hermes-agent/src/core/domain_memory.py (get_confidence, _load_history)
  </read_first>
  <behavior>
    - test_register_movie_pipeline: register movie-pipeline via registry.register(), assert domain_exists("movie-pipeline") is True, assert get("movie-pipeline") returns dict with exactly 10 tasks
    - test_soul_md_exists: after registration, assert SOUL.md file exists in domain directory
    - test_soul_md_content: read SOUL.md, assert it contains "movie-pipeline" and decision advisor identity keywords
    - test_seed_memory_content: write seed audit_history.json to memory dir, load with DomainMemory._load_history(), assert soul-visual, video-gen, voice tasks have records with correct params
    - test_seed_memory_confidence_zero: after seeding, DomainMemory.get_confidence() returns 0.0 for all seeded tasks (only 1 record < MIN_AUDITS_FOR_CONFIDENCE=3)
    - test_api_domain_registered: register via client.post("/v1/register"), assert client.get("/v1/domains") contains "movie-pipeline"
  </behavior>
  <action>
    Create docker/hermes-agent/tests/test_movie_pipeline_domain.py with test class TestMoviePipelineDomain. Use the same conftest.py fixtures (tmp_hermes_dir, registry, client) as existing tests.

    Define TASKS constant matching the 10 tasks from CONTEXT.md: requirement, art-direction, character, scenario, voice, storyboard, scene, camera-preview, camera-final, post-production.

    Write these test methods:

    1. test_register_movie_pipeline: Call registry.register() with domain="movie-pipeline", description, tasks=TASKS, skills_manifest={}. Assert registry.domain_exists("movie-pipeline") is True. Assert registry.get("movie-pipeline")["tasks"] has length 10. Assert registry.get("movie-pipeline")["tasks"] equals TASKS list.

    2. test_soul_md_exists: After registration, check that tmp_hermes_dir / "domains" / "movie-pipeline" / "SOUL.md" exists. Note: DomainRegistry.register() creates empty SOUL.md -- this test verifies the file exists.

    3. test_soul_md_content: This test simulates what the registration script does AFTER register() -- writes the actual SOUL.md content. Write the SOUL.md content (the movie-pipeline decision advisor identity) to the SOUL.md path. Then read it back and assert: contains "movie-pipeline", contains Chinese text (decision advisor identity), contains at least one of "FLUX" or "Wan" or "CosyVoice".

    4. test_seed_memory_content: Create a DomainMemory instance pointed at the domain memory dir. Build the seed audit_history.json data with soul-visual, video-gen, voice records matching the exact structure from the registration script. Write it directly to memory_dir / "audit_history.json". Then call domain_memory._load_history() and assert: "soul-visual" key exists, its records[0]["metrics"]["params"]["flux"]["steps"] == 20, "video-gen" key exists, records[0]["metrics"]["params"]["wan"]["width"] == 832, "voice" key exists, records[0]["metrics"]["params"]["tts"]["voice"] == "default".

    5. test_seed_memory_confidence_zero: After seeding, call domain_memory.get_confidence("soul-visual"), assert it equals 0.0. This verifies the documented behavior that 1 seed record does not trigger EWMA (needs MIN_AUDITS_FOR_CONFIDENCE=3).

    6. test_api_domain_registered: Use the client fixture. POST /v1/register with domain="movie-pipeline", tasks=TASKS. Assert status 201. GET /v1/domains. Assert "movie-pipeline" in response list. GET /v1/domains/movie-pipeline/skills. Assert response status 200 and "skills" key in response JSON.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/docker/hermes-agent && python3 -m pytest tests/test_movie_pipeline_domain.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - test_movie_pipeline_domain.py exists with 6 test methods
    - All 6 tests pass (register, soul exists, soul content, seed memory content, confidence zero, API registration)
    - Tests use conftest.py fixtures (no test-specific fixture duplication)
    - Tests verify MOVIE-01 (10 tasks), MOVIE-03 (seed memory), MOVIE-04 (SOUL.md identity)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script -> filesystem | Registration script writes to ~/.hermes/domains/ (local filesystem, no user input) |
| Test -> temp filesystem | Tests use tmp_path fixture, no production filesystem impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Domain name in register() | mitigate | DomainRegistry._validate_domain_name() regex blocks path traversal and special chars |
| T-09-02 | Tampering | SOUL.md file write | accept | Local script with hardcoded content, no user input path |
| T-09-03 | Tampering | Seed memory file write | accept | Local script with hardcoded JSON, no user input |
| T-09-SC | Tampering | pip installs | mitigate | No new packages installed in this phase |
</threat_model>

<verification>
1. Registration script runs without error: `cd /data/workspace/kais-aigc-platform/docker/hermes-agent && python3 scripts/register_movie_pipeline.py`
2. All domain tests pass: `cd /data/workspace/kais-aigc-platform/docker/hermes-agent && python3 -m pytest tests/test_movie_pipeline_domain.py -v`
3. Existing tests still pass: `cd /data/workspace/kais-aigc-platform/docker/hermes-agent && python3 -m pytest tests/ -x -q`
</verification>

<success_criteria>
- movie-pipeline domain registered with exactly 10 tasks (requirement through post-production)
- SOUL.md defines movie-pipeline decision advisor identity with Chinese text and engine references
- Seed memory in audit_history.json contains FLUX/Wan/CosyVoice default params for 3 tasks
- EWMA confidence is 0.0 for all seeded tasks (expected: only 1 record < MIN_AUDITS_FOR_CONFIDENCE)
- All 6 verification tests pass
- Existing 91 tests still pass (no regressions)
</success_criteria>

<output>
Create `.planning/phases/09-movie-pipeline-domain-setup/09-01-SUMMARY.md` when done
</output>
