---
phase: 09-movie-pipeline-domain-setup
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - docker/hermes-agent/scripts/register_movie_pipeline.py
  - docker/hermes-agent/tests/test_movie_pipeline_domain.py
autonomous: true
requirements: [MOVIE-02]

must_haves:
  truths:
    - "GET /v1/domains/movie-pipeline/skills returns exactly 14 skill names"
    - "All 14 skill .md files exist in the domain skills directory"
    - "13 root-level skills and 1 production_skills skill are present"
    - "storyboard_table_techniques is NOT in the skills list (only storyboard_prompt_techniques)"
  artifacts:
    - path: "docker/hermes-agent/scripts/register_movie_pipeline.py"
      provides: "Updated registration script with skill file migration"
      contains: "shutil.copy2"
    - path: "docker/hermes-agent/tests/test_movie_pipeline_domain.py"
      provides: "Updated tests with skill count and name verification"
      contains: "test_skills_count"
  key_links:
    - from: "scripts/register_movie_pipeline.py"
      to: "data/skills/"
      via: "shutil.copy2 for 13 root .md files"
      pattern: "shutil\\.copy2"
    - from: "scripts/register_movie_pipeline.py"
      to: "data/skills/production_skills/"
      via: "shutil.copy2 for storyboard_prompt_techniques.md"
      pattern: "production_skills"
---

<objective>
Add skill file migration to the registration script (13 root .md files + 1 from production_skills) and verify that GET /v1/domains/movie-pipeline/skills returns exactly 14 skills. Also add a decide() test verifying the domain is registered and returns a valid response for the soul-visual task.

Purpose: Complete MOVIE-02 by migrating 14 expert knowledge files into the hermes domain skills directory.
Output: Updated registration script with skill migration + verification tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-movie-pipeline-domain-setup/09-CONTEXT.md
@.planning/phases/09-movie-pipeline-domain-setup/09-RESEARCH.md
@.planning/phases/09-movie-pipeline-domain-setup/09-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output + existing codebase -->

From docker/hermes-agent/src/core/domain_registry.py:
```python
def get_skills(self, domain: str) -> list[str]:
    """Return skill names (filenames without .md extension) for a domain."""
    skills_dir = self.base_dir / domain / "skills"
    if not skills_dir.is_dir():
        return []
    return sorted(p.stem for p in skills_dir.iterdir() if p.is_file() and p.suffix == ".md")
```

14 expert skill files (from RESEARCH.md Expert Skill Inventory):

13 root-level files from data/skills/:
- production_agent_decision.md
- production_agent_supervision.md
- production_execution_derive_assets.md
- production_execution_director_plan.md
- production_execution_generate_assets.md
- production_execution_storyboard_gen.md
- production_execution_storyboard_panel.md
- production_execution_storyboard_table.md
- script_agent_decision.md
- script_agent_supervision.md
- script_execution_adaptation.md
- script_execution_script.md
- script_execution_skeleton.md

1 from data/skills/production_skills/:
- storyboard_prompt_techniques.md

EXCLUDED: storyboard_table_techniques.md (CONTEXT.md specifies only storyboard_prompt_techniques as the 14th)

From docker/hermes-agent/src/core/decision_engine.py:
```python
def decide(self, domain: str, task: str, context: dict[str, Any]) -> dict[str, Any]:
    # Returns: {"decision_id", "recommendation", "confidence", "domain", "task", "timestamp"}
    # Confidence is EWMA-based from DomainMemory; 0.0 for new tasks
    # Skills are NOT injected into the prompt (confirmed by research)
    # SOUL.md IS injected as ephemeral_system_prompt
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add skill file migration to registration script and verify 14 skills</name>
  <files>docker/hermes-agent/scripts/register_movie_pipeline.py, docker/hermes-agent/tests/test_movie_pipeline_domain.py</files>
  <read_first>
    - docker/hermes-agent/scripts/register_movie_pipeline.py (Plan 01 output -- current state of registration script)
    - docker/hermes-agent/tests/test_movie_pipeline_domain.py (Plan 01 output -- current state of tests)
    - data/skills/ (verify 13 root .md files exist)
    - data/skills/production_skills/storyboard_prompt_techniques.md (verify 14th skill file exists)
    - docker/hermes-agent/src/core/domain_registry.py (get_skills() method)
  </read_first>
  <behavior>
    - test_skills_count_14: After running full registration (including skill copy), registry.get_skills("movie-pipeline") returns list of length 14
    - test_skills_names: The 14 skill names match the expected list (all 13 root names + storyboard_prompt_techniques, no storyboard_table_techniques)
    - test_skills_api: Client GET /v1/domains/movie-pipeline/skills returns 14 skills in JSON response
    - test_decide_soul_visual: POST /v1/decide with domain movie-pipeline, task soul-visual returns 200 with decision_id, recommendation, confidence fields (confidence 0.0 expected for seeded task)
    - test_skill_files_exist_on_disk: All 14 .md files physically exist in the domain skills directory
  </behavior>
  <action>
    Update docker/hermes-agent/scripts/register_movie_pipeline.py to add skill file migration:

    1. Define SKILL_FILES list with 14 entries: the 13 root-level filenames (production_agent_decision.md through script_execution_skeleton.md) + storyboard_prompt_techniques.md. Use a constant list, not glob, to ensure exactly 14 files.

    2. After the DomainRegistry.register() call (from Plan 01), add skill copy logic: iterate over the first 13 filenames in SKILL_FILES, use shutil.copy2 to copy each from SKILLS_SOURCE (PROJECT_ROOT / "data" / "skills") to domain_skills_dir (domain dir / "skills"). For the 14th file (storyboard_prompt_techniques.md), copy from SKILLS_SOURCE / "production_skills" / "storyboard_prompt_techniques.md" to domain_skills_dir. Use shutil.copy2 to preserve metadata.

    3. Add logging/print for each copied skill file and a summary count at the end.

    4. Do NOT copy storyboard_table_techniques.md (CONTEXT.md decision explicitly lists only storyboard_prompt_techniques as the 14th).

    5. Ensure the script still handles re-runs gracefully: shutil.copy2 overwrites existing files, which is safe for idempotent re-runs.

    Update docker/hermes-agent/tests/test_movie_pipeline_domain.py to add these test methods:

    6. test_skills_count_14: Register the domain, then simulate skill copying by copying the 14 .md files from the actual data/skills/ directory to tmp_hermes_dir / "domains" / "movie-pipeline" / "skills/". Use the same file list as the script. Then call registry.get_skills("movie-pipeline") and assert len(skills) == 14.

    7. test_skills_names: Same setup as above. Assert that the returned skills list sorted matches the expected 14 names sorted: production_agent_decision, production_agent_supervision, production_execution_derive_assets, production_execution_director_plan, production_execution_generate_assets, production_execution_storyboard_gen, production_execution_storyboard_panel, production_execution_storyboard_table, script_agent_decision, script_agent_supervision, script_execution_adaptation, script_execution_script, script_execution_skeleton, storyboard_prompt_techniques.

    8. test_skills_api: Register domain via client POST, copy skill files to the domain skills dir (under tmp_hermes_dir), then GET /v1/domains/movie-pipeline/skills. Assert status 200, response["domain"] == "movie-pipeline", len(response["skills"]) == 14.

    9. test_decide_soul_visual: Register domain via client POST (no skill copy needed for this test). POST /v1/decide with domain="movie-pipeline", task="soul-visual", context={"action": "generate_character_image"}. Assert status 200, response has "decision_id", "recommendation", "confidence" keys. Assert confidence == 0.0 (expected: seed data does not affect EWMA with only 1 record). Assert response["domain"] == "movie-pipeline". Assert response["task"] == "soul-visual". Per the planning context, skills are NOT injected into decide() prompts -- this test verifies the domain is registered and decide returns a valid response, NOT that skills influence the response content.

    10. test_skill_files_exist_on_disk: Register domain, copy skill files. For each of the 14 filenames, assert (tmp_hermes_dir / "domains" / "movie-pipeline" / "skills" / filename).exists() is True.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/docker/hermes-agent && python3 -m pytest tests/test_movie_pipeline_domain.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - register_movie_pipeline.py copies 14 skill .md files to domain skills directory
    - 13 files from data/skills/ root + 1 from data/skills/production_skills/
    - storyboard_table_techniques.md is NOT copied (only storyboard_prompt_techniques)
    - All 5 new test methods pass (skills_count, skills_names, skills_api, decide_soul_visual, skill_files_exist)
    - Existing tests from Plan 01 still pass
    - Total test count in file: 11 (6 from Plan 01 + 5 from Plan 02)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script -> data/skills/ | Reading source skill files from project repo (trusted source) |
| Script -> ~/.hermes/domains/ | Writing to domain skills directory (local filesystem) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | Skill file copy paths | accept | Script uses hardcoded source paths from project repo, no user input |
| T-09-05 | Information Disclosure | Skill content in domain dir | accept | Expert knowledge files are project-internal, not sensitive |
| T-09-SC | Tampering | pip installs | mitigate | No new packages installed in this phase |
</threat_model>

<verification>
1. All movie-pipeline domain tests pass: `cd /data/workspace/kais-aigc-platform/docker/hermes-agent && python3 -m pytest tests/test_movie_pipeline_domain.py -v`
2. Full test suite passes (no regressions): `cd /data/workspace/kais-aigc-platform/docker/hermes-agent && python3 -m pytest tests/ -x -q`
3. Skills count assertion: the test verifies exactly 14 skills returned by get_skills()
4. Skill names match expected list (no storyboard_table_techniques)
</verification>

<success_criteria>
- GET /v1/domains/movie-pipeline/skills returns exactly 14 skill names
- All 14 .md files exist in ~/.hermes/domains/movie-pipeline/skills/
- 13 root-level skills + 1 production_skills skill present
- storyboard_table_techniques NOT included (only storyboard_prompt_techniques)
- decide() for soul-visual returns valid response with decision_id, recommendation, confidence=0.0
- All 11 tests in test_movie_pipeline_domain.py pass
- Existing 91 tests in the full suite still pass (no regressions)
</success_criteria>

<output>
Create `.planning/phases/09-movie-pipeline-domain-setup/09-02-SUMMARY.md` when done
</output>
