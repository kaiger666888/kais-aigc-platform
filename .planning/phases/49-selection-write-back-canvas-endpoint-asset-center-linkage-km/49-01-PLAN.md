---
phase: 49
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/canvasRelationalStore.ts
  - src/routes/canvas/v2/select-winner.ts
  - src/router.ts
  - scripts/verify-phase-49.ts
  - scripts/verify-phase-49-bridge.ts
  - scripts/verify-phase-49-linkage.ts
  - package.json
autonomous: true
requirements: [SELECT-01]
must_haves:
  truths:
    - "POST /api/canvas/v2/variant-groups/:groupId/select-winner persists canvas_variant_groups.winner_node_id + per-member canvas_nodes.is_winner in ONE transaction; a fresh load-v2 still returns the winner (selection no longer lives only in frontend memory or a whole-graph save-v2 flush)"
    - "group 不存在 → 404; winnerNodeId 不在组内 → 409; select_mode != 'single' → 409; 重复 select 同一 winner → 200 no-op (D-01/D-03)"
    - "winner 节点映射到 o_assets 时，同组资产 isPrimaryView 同步置换（旧 primary 置 false），失败只 warn 不回滚 canvas 主写入 (D-07)"
  artifacts:
    - path: "src/lib/canvasRelationalStore.ts"
      provides: "selectWinnerInGroup(trxDb, scope, groupId, winnerNodeId) 事务化选定 + syncAssetPrimaryForWinner o_assets 置换"
      contains: "selectWinnerInGroup"
    - path: "src/routes/canvas/v2/select-winner.ts"
      provides: "POST /:groupId/select-winner 端点（zod 校验 + 404/409/200 语义 + websocket 广播）"
      contains: "select-winner"
    - path: "scripts/verify-phase-49.ts"
      provides: "temp :memory: sqlite 行为断言（事务/幂等/错误语义/D-07）"
  key_links:
    - from: "src/router.ts"
      to: "src/routes/canvas/v2/select-winner.ts"
      via: "app.use('/api/canvas/v2/variant-groups', routeNN)"
      pattern: "variant-groups"
    - from: "src/routes/canvas/v2/select-winner.ts"
      to: "src/lib/canvasRelationalStore.ts"
      via: "selectWinnerInGroup + syncAssetPrimaryForWinner 调用"
      pattern: "selectWinnerInGroup"
---

<objective>
SELECT-01 落地：画布层 select-winner 后端端点。事务化写 `canvas_variant_groups.winner_node_id` + 组内节点 `canvas_nodes.is_winner`（D-01），幂等重选 no-op（D-03），复用 canvasRelationalStore 持久化风格、经 src/router.ts 既有挂载点注册（D-02），并在端点内顺带完成 canvas→o_assets 的 isPrimaryView 置换（D-07 反向联动）。

Purpose: 这是 Phase 49 的地基——前端接线 (49-04)、资产中心联动 (49-03)、kmc 桥接 (49-02) 全部挂在这个端点/函数上。
Output: 可用的 select-winner 端点 + 可注入 db 的事务化 store 函数 + verify:phase-49 行为门 + 49-02/49-03 的 verify 脚本 scaffold 与 npm 注册（消除 wave-2 三计划对 package.json 的同波次文件冲突）。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-selection-write-back-canvas-endpoint-asset-center-linkage-km/49-CONTEXT.md

<verified_facts>
规划期已完成的代码核实（执行者直接采信，不必重查）：

1. **D-06/D-07 映射机制（CONTEXT 标记"必须先核实"项——已核实）**：`src/routes/canvas/v2/sync-assets.ts:69` 建 canvas node id = `a-oasset-${o_assets.id}`；`:96` 写 `data.oAssetId = asset.id`；`:95` 同步时 `data.state = isPrimaryView ? "selected" : "candidate"`（仅同步瞬间，非持续量）。o_assets → canvas node 正查：`canvas_nodes.id === "a-oasset-" + assetId`（同一 project_id 下）；canvas node → o_assets 反查：`data.oAssetId`（首选）或 id 前缀 `a-oasset-` 解析。**sync-assets 建的节点不带 `variant_group_id`**——组关系只在用户于 UI 分组并 save-v2 后存在；"映射不到组 → 静默跳过" 是常态路径而非异常。
2. **表结构**：`src/lib/initDB.ts:1259-1287` canvas_nodes（is_winner:1276 boolean default false, variant_group_id:1280, data:1272 text JSON）；`:1332-1347` canvas_variant_groups（variant_node_ids:1339 text JSON array, winner_node_id:1340, select_mode:1341 default 'single'）；主键均为 (id, project_id, episodes_id)。
3. **round-trip 闭环（SC-1 依赖）**：`canvasRelationalStore.ts:353` listVariantGroups 把 winner_node_id → winnerNodeId；`:176` listNodes 把 is_winner → isWinner；`packages/infinite-canvas/src/v3/adapter.ts:279-286` 以组级 winnerNodeId 为权威覆写节点级 isWinner，`:739` 映射到 RF 视图 isWinner——本端点写库后 load-v2 → 前端即见。
4. **路由族惯例**：`src/routes/canvas/v2/` 每文件 export default 一个 express.Router；`src/router.ts:17-29` import route14..route27，`:189-201` app.use 挂载；本端点取下一个空闲编号（route28 起，执行者以 router.ts 实际末尾为准）。body 传 projectId/episodesId 是族内惯例（sync-assets.ts:26-33）。
5. **D-07 loop 预防**：本端点对 o_assets 的置换必须**直接 knex 写库**（本 plan 的 syncAssetPrimaryForWinner），严禁 HTTP 自调 /api/v1/assets-registry/:id PATCH（历史上子进程 HTTP 自调用死锁过事件循环——STATE.md hermes-driven-iteration 条目），也严禁调用 49-03 的 applyRegistrySelectionToCanvas（否则 registry hook ↔ canvas 端点互触发成环）。49-03 的 hook 只挂在 registry PATCH 路由上，本端点不走该路由 → 无环。
6. **responseFormat/middleware**：`success/error` from `@/lib/responseFormat`；`validateFields` from `@/middleware/middleware`（sync-assets.ts:1-10 用法）；websocket 广播 `broadcastToProject(projectId, event, payload)` from `@/utils/ws`（review/approve.ts:79 惯例）。
7. **verify 模式**：`scripts/verify-phase-48.ts` 的 :memory: better-sqlite3 knex + initDB 镜像 DDL + 动态 import 真实模块 + assert 计数模式；ts 脚本走 `npx tsx`，package.json 注册 npm run 条目。
</verified_facts>

<interfaces>
本 plan 创建、wave-2 三计划消费的契约（执行者必须按此形状实现）：

src/lib/canvasRelationalStore.ts 新增（db-参数化，沿用 48-02 "db handle as parameter" 决策——verify/后续计划注入自己的 knex）:

```typescript
export interface SelectWinnerResult {
  status: "updated" | "idempotent" | "not_found" | "not_in_group" | "multi_mode";
  groupId: string;
  winnerNodeId: string;
  /** 1-based，winner 在 group.variantNodeIds 内的位置；仅 status=updated/idempotent 时有意义，否则 0 */
  variantIndex: number;
  /** winner 节点 phase_name（如 "p11_first_last_frames"）；桥接 phase token 用；可 null */
  winnerPhaseName: string | null;
  /** winner 节点映射的 o_assets.id（data.oAssetId 或 a-oasset- 前缀解析）；映射不到为 null */
  winnerOAssetId: number | null;
  /** 组内全部成员的 oAssetId 集合（含 winner，若映射）；供 D-07 置换 */
  memberOAssetIds: number[];
  /** D-07 置换影响的 o_assets id 列表（本端点内已处理） */
  swappedAssetIds: number[];
}

export async function selectWinnerInGroup(
  trxDb: any,                 // knex 实例或事务句柄（验证脚本注入 :memory: 实例）
  scope: { projectId: number; episodesId: number },
  groupId: string,
  winnerNodeId: string,
): Promise<SelectWinnerResult>;

/** D-07：canvas 选定成功后置换 o_assets isPrimaryView。独立 try/catch 调用，失败只 warn。 */
export async function syncAssetPrimaryForWinner(
  trxDb: any,
  projectId: number,
  winnerOAssetId: number,
  memberOAssetIds: number[],
): Promise<number[]>;  // 返回被置换(改过 isPrimaryView)的资产 id
```

端点契约（49-04 canvasApi 与 verify 都按此调用）:

```
POST /api/canvas/v2/variant-groups/:groupId/select-winner
body: { projectId: number, episodesId: number, winnerNodeId: string }   (zod 全必填)
→ 200 success({ groupId, winnerNodeId, applied: true|false })   applied=false 即幂等 no-op
→ 404 group 不存在 | 409 winnerNodeId 不在组内 / select_mode != 'single'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: canvasRelationalStore 事务化 selectWinnerInGroup + D-07 syncAssetPrimaryForWinner</name>
  <files>src/lib/canvasRelationalStore.ts</files>
  <read_first>
  - src/lib/canvasRelationalStore.ts（全文——upsertNode :85-141 的 raw-SQL 参数化风格、saveFullGraph :453 的事务模式、Scope 接口 :23-26）
  - src/lib/initDB.ts:1259-1347（canvas 表 DDL）
  - src/routes/canvas/v2/sync-assets.ts:60-100（oAssetId / a-oasset- 前缀映射事实）
  </read_first>
  <behavior>
  - not_found：scope+groupId 查不到 canvas_variant_groups 行 → status "not_found"，不写任何库
  - multi_mode：select_mode != 'single' → status "multi_mode"，不写库（specifics：对 multi 组拒绝选定而非错误写单一 winner）
  - not_in_group：winnerNodeId 不在组 variant_node_ids（JSON 解析后）→ status "not_in_group"，不写库
  - idempotent：winner_node_id 已等于 winnerNodeId → status "idempotent"，零写入（D-03，不抛 409）
  - updated：单事务内 ① UPDATE canvas_variant_groups SET winner_node_id=?, updated_at=? ② 组内每个成员节点 UPDATE canvas_nodes SET is_winner=(id===winnerNodeId), updated_at=? WHERE project_id/episodes_id scope AND id IN 成员——事务内两个写全部成功或全部回滚
  - 成员节点行缺失（悬空 variant_node_ids 项）不阻断：is_winner 只写实际存在的行
  - data JSON 列不动：winner 真值是 winner_node_id/is_winner 两列（adapter :279 以组级 winnerNodeId 为权威），避免整 blob 重写踩并发
  - winnerPhaseName/winnerOAssetId/memberOAssetIds 从成员行 data JSON 提取（data.oAssetId；无则 id.startsWith("a-oasset-") 时 parseInt 前缀后数字）
  - syncAssetPrimaryForWinner：winnerOAssetId 为 null → 返回 []；否则 UPDATE o_assets SET isPrimaryView=1 WHERE id=winnerOAssetId AND projectId；旧 primary 置 false：UPDATE o_assets SET isPrimaryView=0 WHERE isPrimaryView=1 AND projectId AND id != winnerOAssetId AND (assetsId = winnerRow.assetsId OR id = winnerRow.assetsId OR id IN memberOAssetIds)——只动同组候选，绝不碰跨组资产
  </behavior>
  <action>
在 canvasRelationalStore.ts 的 Variant Group CRUD 区（listVariantGroups 之后、Meta 区之前）追加两个导出函数，签名严格按 <interfaces>。selectWinnerInGroup 内部：先读组行（trxDb("canvas_variant_groups").where(scope+id).first()），再按 behavior 顺序短路四种非 updated 状态；updated 路径包 trxDb.transaction(async trx => ...)，两步 UPDATE 均走 trx，用参数化 builder（禁止字符串拼 SQL）。成员读取：trx("canvas_nodes").where(scope).whereIn("id", variantNodeIds).select("id","data","phase_name")。注意 knex 3.2.5 typings 无 andWhereIn（STATE.md 既有决策）——链式 .whereIn()。syncAssetPrimaryForWinner 是独立函数（不在 selectWinnerInGroup 的事务内——D-07 要求失败不回滚 canvas 主写入），内部自己开事务或顺序两 UPDATE 均可。o_assets 列名：isPrimaryView/assetsId/projectId（registry index.ts:203-217 既有用法）。文件头部注释区补一段：选定真值 = winner_node_id 列 + is_winner 列，data blob 有意不写。
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "export async function selectWinnerInGroup" src/lib/canvasRelationalStore.ts</automated>
  </verify>
  <acceptance_criteria>
- tsc exit 0
- grep "selectWinnerInGroup\|syncAssetPrimaryForWinner" src/lib/canvasRelationalStore.ts 各 ≥1 且带 export
- 源码不含字符串拼接 SQL（UPDATE ... SET 走 knex builder 或 db.raw 带参占位）
- selectWinnerInGroup 不 import @/utils（db 由参数注入）；idempotent 分支无任何 UPDATE 语句执行（代码路径可见）
  </acceptance_criteria>
  <done>两个函数落地、类型过、可被路由与 verify 脚本以注入 db 方式调用</done>
</task>

<task type="auto">
  <name>Task 2: select-winner.ts 路由 + router.ts 挂载 + D-07 钩子</name>
  <files>src/routes/canvas/v2/select-winner.ts, src/router.ts</files>
  <read_first>
  - src/routes/canvas/v2/sync-assets.ts（族内路由文件样板：zod safeParse + success/error + 错误处理）
  - src/routes/canvas/review/approve.ts（broadcastToProject 广播惯例 :79）
  - src/router.ts:17-29, :189-201（import 编号 + app.use 挂载点）
  - src/lib/canvasRelationalStore.ts（Task 1 产出的两个函数签名）
  </read_first>
  <action>
新建 src/routes/canvas/v2/select-winner.ts：express.Router，router.post("/:groupId/select-winner", ...)。zod body schema：projectId number / episodesId number / winnerNodeId string（minLength 1, maxLength 128——防超长串进库）。safeParse 失败 → 400 error("参数校验失败", issues)。取 req.params.groupId。调 selectWinnerInGroup(db, {projectId, episodesId}, groupId, winnerNodeId)（db from "@/utils/db"，同文件族惯例）：status not_found → 404 error("变体组不存在")；not_in_group → 409 error("winnerNodeId 不在组内")；multi_mode → 409 error("仅 single 组支持选定")；idempotent → 直接 200 success({groupId, winnerNodeId, applied:false})（不走 D-07/桥接——重选同一 winner 无新信息）；updated → 先 200 语义准备：若 winnerOAssetId 非 null 则 try { await syncAssetPrimaryForWinner(db, projectId, winnerOAssetId, memberOAssetIds) } catch → console.warn("[select-winner] o_assets isPrimaryView 置换失败(不回滚 canvas):", err)。最后 broadcastToProject(projectId, "variant:selected", { projectId, episodesId, groupId, winnerNodeId, timestamp: Date.now() }) 并 200 success({groupId, winnerNodeId, applied:true})。文件头 JSDoc 注明本端点即 docs/canvas-next-steps.md:428-545 Phase 3.2 selectWinner 的落地、D-01..D-03/D-07 决策来源。src/router.ts：import route28（或实际下一个空闲编号）from "./routes/canvas/v2/select-winner"，在 sync-assets 挂载行后追加 app.use("/api/canvas/v2/variant-groups", route28)。**预留桥接缝**：在 updated 成功路径注释标记 `// [49-02] review bridge hook mounts here (fire-and-forget)`——49-02 将在此插入调用，本 plan 不实现桥接。
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -n "variant-groups" src/router.ts && grep -c "select-winner" src/routes/canvas/v2/select-winner.ts</automated>
  </verify>
  <acceptance_criteria>
- `curl -s -X POST http://localhost:3000/api/canvas/v2/variant-groups/nonexistent/select-winner -H 'Content-Type: application/json' -d '{"projectId":1,"episodesId":1,"winnerNodeId":"x"}'` 返回 404（若 dev server 未跑则以 verify-phase-49 的 handler 级断言为准）
- router.ts 有 app.use("/api/canvas/v2/variant-groups", ...) 且 import 的文件存在
- 路由文件内：syncAssetPrimaryForWinner 调用被 try/catch 包裹且 catch 只 warn 不 rethrow、不改变 200 响应
- 端点不调用 /api/v1/assets-registry（grep assets-registry 该文件 = 0 处）——D-07 loop 预防
- 幂等分支（applied:false）不触发 syncAssetPrimaryForWinner 也不广播（代码路径可见）
  </acceptance_criteria>
  <done>端点挂载完成、四类错误语义 + 幂等 + D-07 钩子就位，桥接缝已标注</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: verify-phase-49.ts 行为门 + wave-2 verify scaffold 与 npm 注册</name>
  <files>scripts/verify-phase-49.ts, scripts/verify-phase-49-bridge.ts, scripts/verify-phase-49-linkage.ts, package.json</files>
  <read_first>
  - scripts/verify-phase-48.ts:1-110（:memory: knex + DDL 镜像 + assert 计数 harness 全样）
  - src/lib/initDB.ts:1259-1347（DDL 镜像来源）
  - src/lib/canvasRelationalStore.ts（被测函数）
  - package.json:29-38（verify:phase-* 注册格式）
  </read_first>
  <behavior>
  verify-phase-49.ts（temp :memory: better-sqlite3 knex，动态 import 真实 src/lib/canvasRelationalStore）：
  - fixture：2 组（g1 single 3 成员 / g2 select_mode='multi' 2 成员）+ g1 一成员行缺失（悬空 id）；成员 data 带 oAssetId 模拟 a-oasset- 节点 + 同步镜像的 o_assets 行（1 组 3 资产：1 primary + 2 member 共 assetsId）
  - 断言 updated：selectWinnerInGroup(db, scope, "g1", 成员B) → status updated、winner_node_id=B、成员 A/B/C is_winner=false/true/false、variantIndex=2、winnerPhaseName 提取正确
  - 断言 idempotent：再次选 B → status idempotent 且 DB 值无变化（updated_at 不变）
  - 断言换选：改选成员 C → A/B/C = false/false/true
  - 断言 not_found / not_in_group（选组外节点 id）/ multi_mode（g2）
  - 断言悬空成员不炸：g1 的缺失成员 id 不在 DB，updated 仍成功
  - 断言 D-07：syncAssetPrimaryForWinner(db, projectId, 成员B资产id, [A,B,C资产id]) → B isPrimaryView=1，旧 primary 置 0，同组另一无关项目资产（不同 projectId）不被触碰
  - 断言 D-07 无映射：winnerOAssetId=null 路径 → 返回 []
  - 断言事务性：临时 monkeypatch 事务内第二个 UPDATE 抛错 → 组表 winner_node_id 保持旧值（无半写）
  scaffold 脚本 ×2：verify-phase-49-bridge.ts / verify-phase-49-linkage.ts —— 仅 console.log("SKIP: scaffold — 实际断言由 49-02/49-03 落地替换本文件") + process.exit(0)
  </behavior>
  <action>
按 48 脚本模式写 scripts/verify-phase-49.ts：knex better-sqlite3 :memory:，createTable 镜像 canvas_nodes/canvas_variant_groups/o_assets（o_assets 最小列：id 自增, projectId, assetsId 可空 int, isPrimaryView boolean default 0）——绝不碰生产 db2.sqlite。行数控制在 ~250 行内，assert 命名含需求号（如 "SELECT-01 idempotent no-op"）。末尾统计 PASS/FAIL，任何 FAIL exit 1。package.json scripts 区（verify:phase-48 行后）追加三条："verify:phase-49"、"verify:phase-49-bridge"、"verify:phase-49-linkage"（格式 npx tsx scripts/...）。写两个 scaffold 脚本。**给 49-02/49-03 的交接**：它们整体替换自己的 scaffold 文件、不动 package.json（避免 wave-2 三计划 package.json 同波次冲突——本任务一次性注册的原因）。
  </action>
  <verify>
    <automated>npm run verify:phase-49 && npm run verify:phase-49-bridge && npm run verify:phase-49-linkage && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
- npm run verify:phase-49 exit 0，全断言 PASS（预计 ≥18 条），输出含 FAIL 汇总为 0
- verify 脚本 grep '"db2.sqlite"' = 0（:memory: only）
- package.json 三条 npm script 存在（grep verify:phase-49 计 3）
- 两个 scaffold exit 0 且打印 SKIP 标记
- 强制失败 sanity：临时改一条断言期望值 → exit 1 → 改回（SUMMARY 记录做过）
  </acceptance_criteria>
  <done>行为门守住 SELECT-01 全部错误语义/幂等/事务/D-07，wave-2 verify 入口就绪</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→canvas API | 请求体跨边界进 zod 校验后才触库 |
| canvas 事务→SQLite | winner_node_id/is_winner 经参数化 builder 写入 |
| canvas 端点→o_assets | D-07 跨表置换，失败隔离（warn 不回滚） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | select-winner body | mitigate | zod 全字段校验 + winnerNodeId maxLength 128 + not_in_group 409（越权/不存在路径节点一律拒写） |
| T-49-02 | Tampering | DB 写入 | mitigate | 全参数化 knex builder；groupId 来自路由参数仅用于 where 等值匹配 |
| T-49-03 | DoS/Repudiation | 事务边界 | mitigate | 单事务原子写；broadcastToProject 留事件痕迹（variant:selected + timestamp） |
| T-49-04 | Tampering | D-07 置换 | mitigate | 置换只动同 projectId 且同组候选（assetsId/memberOAssetIds 约束）；失败 warn 隔离不回滚真值源 |
| T-49-SC | Tampering | npm 安装 | accept | 本 plan 零新增依赖（better-sqlite3/knex/tsx 均既有） |
</threat_model>

<verification>
- npx tsc --noEmit exit 0
- npm run verify:phase-49 exit 0（含强制失败 sanity）
- grep -n "variant-groups" src/router.ts 命中新挂载
- 累计 git diff 仅覆盖 files_modified 列表
</verification>

<success_criteria>
- SELECT-01 全语义（404/409×2/幂等/事务/悬空成员容忍）由 verify:phase-49 机器断言
- D-07 置换 + 失败隔离落地且不与 registry 路由成环
- wave-2 三计划共享的 verify 入口注册完毕，无同波次 package.json 冲突
</success_criteria>

<output>
Create .planning/phases/49-selection-write-back-canvas-endpoint-asset-center-linkage-km/49-01-SUMMARY.md when done
</output>
