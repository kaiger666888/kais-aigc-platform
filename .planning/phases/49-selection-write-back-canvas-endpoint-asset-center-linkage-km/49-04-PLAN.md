---
phase: 49
plan: 04
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - packages/infinite-canvas/src/services/canvasApi.ts
  - packages/infinite-canvas/src/store/canvasStore.ts
  - packages/infinite-canvas/src/store/__tests__/selectWinner.test.ts
autonomous: true
requirements: [SELECT-02]
must_haves:
  truths:
    - "前端 canvasStore.selectWinner 保持现有本地乐观行为，追加调用 POST /api/canvas/v2/variant-groups/:groupId/select-winner；端点失败时 UI 回滚到 prevSnapshot——不再出现 'UI 已换选但库里没写' 的假象 (D-04)"
    - "零新视觉设计：不新增组件/样式/图标，VariantBadge 与现有交互外观不变 (D-05)"
    - "graph 路径（V3）与旧路径（可变 RF 状态）都接后端；旧路径回滚复用 variantOps 的 rollbackWinnerSelection"
  artifacts:
    - path: "packages/infinite-canvas/src/services/canvasApi.ts"
      provides: "selectVariantWinner API 客户端函数"
      contains: "selectVariantWinner"
    - path: "packages/infinite-canvas/src/store/canvasStore.ts"
      provides: "selectWinner 改造：乐观更新 → await 后端 → 失败回滚"
      contains: "selectVariantWinner"
    - path: "packages/infinite-canvas/src/store/__tests__/selectWinner.test.ts"
      provides: "vitest 行为测试（mock canvasApi：成功不回滚 / 失败回滚 / 校验失败不调 API）"
      contains: "describe"
  key_links:
    - from: "packages/infinite-canvas/src/store/canvasStore.ts"
      to: "packages/infinite-canvas/src/services/canvasApi.ts"
      via: "selectWinner 内 await selectVariantWinner(projectId, episodesId, groupId, nodeId)"
      pattern: "selectVariantWinner"
    - from: "packages/infinite-canvas/src/services/canvasApi.ts"
      to: "POST /api/canvas/v2/variant-groups/:groupId/select-winner"
      via: "apiCall fetch"
      pattern: "variant-groups"
---

<objective>
SELECT-02 落地：前端 selectWinner 接后端。canvasStore.selectWinner（graph 路径 + 旧路径）保持即时乐观更新，追加 49-01 端点调用；失败回滚——graph 路径恢复前一个 graph 对象，旧路径复用 variantOps 的 rollbackWinnerSelection（正是 canvasStore.ts:574-576 注释预留的接缝）。纯接线，零新视觉（D-05）。

Purpose: 选定从 "本地乐观 + 用户手动 💾 save-v2 整体落盘" 升级为即时持久化——消除刷新丢失/多端不一致/半写假象（SC-2）。
Output: canvasApi 新函数 + canvasStore.selectWinner 改造 + vitest 行为测试。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/49-selection-write-back-canvas-endpoint-asset-center-linkage-km/49-CONTEXT.md
@.planning/phases/49-selection-write-back-canvas-endpoint-asset-center-linkage-km/49-01-SUMMARY.md

<verified_facts>
规划期已核实（注意：CONTEXT.md 里写的 `src/api/canvasApi.ts` 路径已过时）：

1. **API 客户端实际位置**：`packages/infinite-canvas/src/services/canvasApi.ts`（无 src/api/ 目录）。`API_BASE='/api'`（:3），核心 helper `apiCall<T>(path, body, {cancelToken, timeout})`（:50，含重试/超时/ApiError），同族用法参考 loadCanvasGraph :336-344。updateAsset :303-313 展示了裸 fetch + ApiError 模式——新函数走 apiCall（POST 语义与 saveCanvasGraph :318-325 一致）。
2. **selectWinner 现状**：canvasStore.ts:524-578。graph 路径 :528-548 用包内纯函数 `selectVariant(graph, group.id, nodeId)`（from '@kais/flowgraph-v3'，packages/flowgraph-v3/ts/src/variants.ts:26-70——组不存在/selectMode!=='single'/winner 不在 variantNodeIds/悬空 winner/curation locked 均同步 throw），**注释 :536-537 明言 '此处不调后端'——本 plan 就是补这半句**；旧路径 :550-577 用 applyWinnerSelection + 预留 rollbackWinnerSelection 调用点 :574-576。
3. **乐观+回滚家族样板**：同文件 approveNode :455-467（graph 路径）/ rejectNode :488-504——setGraph(next) → await api → catch 恢复 prev → toast error。selectWinner 改造完全对齐此形状。
4. **store 上下文字段**：projectId/episodesId 是 store state（rejectNode :489 的 get() 解构 + 缺失早退 :490 样板）；selectWinner 现为同步签名 `(nodeId) => void`，改 async 后现有调用点（组件内 onClick 直呼、不 await）不受影响——执行前 grep selectWinner( 调用点确认无依赖同步完成处。
5. **selectVariant 不可变性**：内部 clone(graph)（variants.ts:31）——传入的 prev graph 对象不被改动，保留引用即可零成本回滚。
6. **vitest 就绪**：packages/infinite-canvas package.json "test": "vitest run"；既有测试在 src/v3/__tests__、src/components/__tests__（如 adapter.test.ts）。store 测试放 src/store/__tests__/selectWinner.test.ts，vi.mock('../services/canvasApi') 提供 selectVariantWinner/approveNode/rejectNode 等 store 已 import 的全部导出。
7. **端点契约**（49-01）：POST /api/canvas/v2/variant-groups/:groupId/select-winner，body { projectId, episodesId, winnerNodeId } → 200 { applied: true|false } / 404 / 400 / 409。apiCall 对非 2xx 抛 ApiError（business/network）——catch 即回滚。
</verified_facts>

<interfaces>
消费的既有契约：

From packages/infinite-canvas/src/services/canvasApi.ts:
```typescript
async function apiCall<T>(path: string, body: unknown, options?): Promise<T>  // 内部 helper，非导出
// 家族样板：saveCanvasGraph → await apiCall<void>('/canvas/save', {...})
```

From packages/flowgraph-v3/ts/src/variants.ts:
```typescript
selectVariant(graph: FlowGraphV3, groupId: string, winnerNodeId: string): FlowGraphV3  // throws on invalid
```

From packages/infinite-canvas/src/store/variantOps.ts:
```typescript
applyWinnerSelection({nodes, edges, variantGroupId, winnerNodeId}): WinnerUpdateOutcome  // {nextNodes, nextEdges, prevSnapshot}
rollbackWinnerSelection(outcome): { nodes, edges }
```

本 plan 创建（端点契约见 49-01）:
```typescript
// services/canvasApi.ts
export async function selectVariantWinner(
  projectId: number, episodesId: number, groupId: string, winnerNodeId: string,
): Promise<void>  // 非 2xx → throw ApiError
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: canvasApi.selectVariantWinner + canvasStore.selectWinner 改造</name>
  <files>packages/infinite-canvas/src/services/canvasApi.ts, packages/infinite-canvas/src/store/canvasStore.ts</files>
  <read_first>
  - packages/infinite-canvas/src/store/canvasStore.ts:440-578（approveNode/rejectNode 乐观回滚样板 + selectWinner 全文）
  - packages/infinite-canvas/src/services/canvasApi.ts:300-345（updateAgent/loadCanvasGraph 的 apiCall 用法）
  - packages/infinite-canvas/src/store/variantOps.ts:56-164（prevSnapshot/rollbackWinnerSelection 契约）
  - packages/infinite-canvas/src/v3/adapter.ts:279-286, :739（winner round-trip——理解为何写库即生效）
  - grep -rn "selectWinner(" packages/infinite-canvas/src --include="*.tsx" --include="*.ts"（调用点兼容性确认）
  </read_first>
  <action>
canvasApi.ts：在 rejectNode 附近新增导出 `selectVariantWinner(projectId, episodesId, groupId, winnerNodeId)` → `await apiCall<void>('/canvas/v2/variant-groups/' + encodeURIComponent(groupId) + '/select-winner', { projectId, episodesId, winnerNodeId })`，JSDoc 引 49-01 端点契约。canvasStore.ts selectWinner 改 async：① 顶部解构补 projectId/episodesId（get()），缺失 → showToast('缺少项目上下文','warning') 早退（对齐 rejectNode :490）；② graph 路径：先 `const prevGraph = graph`，在现有 try 内调 selectVariant 得 next 并 setGraph(next, get().warnings)，随后 `await selectVariantWinner(projectId, episodesId, group.id, nodeId)`，成功 → success toast（沿用现文案）；selectVariant 同步 throw（locked/multi/悬空校验）→ 维持现有 error toast，**不调 API**（catch 内区分：setGraph 未发生无需回滚）；API reject → catch 内 `get().setGraph(prevGraph, get().warnings)` + showToast('选定失败已回滚: ...','error')；③ 旧路径：outcome = applyWinnerSelection（不变）→ 乐观 set/setEdges（不变）→ 追加 `await selectVariantWinner(projectId, episodesId, variantGroupId, nodeId)`（variantGroupId 为 asVariantGroupId 后值），catch → `const rb = rollbackWinnerSelection(outcome); set({ nodes: rb.nodes }); setEdges(rb.edges)` + error toast——替换掉 :574-576 的 void outcome/void rollbackWinnerSelection 占位注释；④ 删除 :536-537 "此处不调后端" 注释，改为指向本 phase 的持久化说明。store 接口若为显式 type 声明同步改 `(nodeId: string) => Promise<void>`。**D-05：不新增任何组件/样式/类名**——只动 store 与 api 层。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx tsc --noEmit && grep -c "selectVariantWinner" src/services/canvasApi.ts src/store/canvasStore.ts</automated>
  </verify>
  <acceptance_criteria>
- selectVariantWinner 走 apiCall 且 path 含 encodeURIComponent(groupId)（防特殊字符 groupId 破坏 URL）
- graph 路径：prevGraph 捕获于 setGraph 之前；API 失败分支调用 setGraph(prevGraph)（源码可见）
- 旧路径：失败分支调用 rollbackWinnerSelection(outcome) 并恢复 nodes+edges（源码可见，替换了原 void 占位）
- selectVariant 同步校验 throw 时不发 API 请求（控制流可证：throw 发生在 await 之前）
- grep -rn "selectWinner(" 组件调用点无破坏（无需逐个改——async 化对 void 调用透明，SUMMARY 列出调用点清单）
- 零新样式/组件：git diff 不含 .css/新 .tsx
  </acceptance_criteria>
  <done>selectWinner 双路径接后端、失败回滚闭环，纯接线零视觉改动</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: vitest 行为测试（成功不回滚 / 失败回滚 / 校验不调 API）</name>
  <files>packages/infinite-canvas/src/store/__tests__/selectWinner.test.ts</files>
  <read_first>
  - packages/infinite-canvas/src/v3/__tests__/adapter.test.ts（vitest 组织样板 + FlowGraphV3 fixture 形状）
  - packages/infinite-canvas/src/store/canvasStore.ts（store 创建 + state 字段 + setGraph/showToast 签名——文件头 state 定义区）
  - packages/infinite-canvas/src/store/variantOps.ts（旧路径 outcome 形状）
  </read_first>
  <behavior>
  - vi.mock('../../services/canvasApi')（或相对路径按文件位置）提供 selectVariantWinner/approveNode/rejectNode/其余 store 已 import 导出的 mock；vi.mocked 可按用例改 resolved/rejected
  - graph 路径成功：fixture graph（2 成员 single 组，winner=A）→ selectWinner(B) → state.graph.variantGroups[0].winnerNodeId==='B' 且 mock 恰被调 1 次（参数 projectId/episodesId/groupId/nodeId 正确）
  - graph 路径失败：mock reject → 断言 winnerNodeId 回滚为 'A'（prevGraph 恢复）
  - graph 路径校验 throw：multi 组（selectMode:'locked'）→ mock 0 次调用、winner 不变
  - 旧路径成功：无 graph、nodes/edges fixture（data.variantGroupId）→ selectWinner 后 mock 调 1 次、isWinner 置位
  - 旧路径失败：mock reject → nodes 恢复 prevSnapshot（旧 winner 的 isWinner=true 还原）
  - 缺 projectId：早退、mock 0 次
  </behavior>
  <action>
新建 packages/infinite-canvas/src/store/__tests__/selectWinner.test.ts 按 behavior 用例实现。fixture 最小化：graph 用 FlowGraphV3 兼容形状（nodes[{id,kind:'asset',variantGroupId,curation}], variantGroups[{id,selectMode:'single',variantNodeIds}]——以 selectVariant 实际校验的字段为准，读 variants.ts 后落形）。store 实例取 useCanvasStore.getState()（zustand 直取，必要时 store.setState 注入 projectId/episodesId/graph/nodes/edges 初值——字段名以 store 头部定义为准）。showToast 副作用可不断言或 spy console。文件头注释标注 SELECT-02/D-04。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx vitest run src/store/__tests__/selectWinner.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
- vitest 退出码 0，≥6 用例全过（成功×2 / 回滚×2 / 校验不调 API / 缺上下文早退）
- 测试不打真实网络（vi.mock 生效——apiCall 从未被真实 fetch 调用）
- 仓库根 npx tsc --noEmit exit 0
- 强制失败 sanity：临时让一个用例期望错值 → vitest exit 1 → 还原（SUMMARY 记录）
  </acceptance_criteria>
  <done>SC-2 行为闭环有回归门：乐观更新、失败回滚、校验前置全部可重复验证</done>
</task>

<human_check>
自动化完成后可选人工冒烟（不阻塞）：dev server + 画布上点选非 winner 变体徽标 → toast 成功 → F5 刷新 → winner 保持；DevTools 断网状态下点选 → UI 回滚 + 错误 toast。
</human_check>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA→canvas API | 既有 fetch 通道，新增一次 POST 调用 |
| API 失败→UI 状态 | 回滚路径保证 UI 不呈现未持久化的 winner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-14 | Tampering | groupId 进 URL | mitigate | encodeURIComponent(groupId)（49-01 端点侧 zod 再校验） |
| T-49-15 | DoS | apiCall 重试放大 | accept | 沿用 apiCall 家族默认（15s 超时/2 重试）——与 save/approve 同级，不另开配置 |
| T-49-16 | Tampering | 失败假象（SC-2 核心） | mitigate | 双路径回滚（graph prevGraph / 旧路径 rollbackWinnerSelection）+ vitest 行为门 |
| T-49-SC | Tampering | npm 安装 | accept | 零新增依赖（vitest 既有） |
</threat_model>

<verification>
- packages/infinite-canvas: npx tsc --noEmit exit 0；npx vitest run 新测试 exit 0
- 仓库根 npx tsc --noEmit exit 0
- git diff 零视觉文件（.css/新组件）
- npm run verify:phase-49 复跑 exit 0（后端无回归）
</verification>

<success_criteria>
- SELECT-02 完成：selectWinner 不再是纯本地乐观——端点失败 UI 必回滚，成功即持久（刷新可证）
- D-05 守住：零新视觉，交互外观与 Phase 48 前完全一致
</success_criteria>

<output>
Create .planning/phases/49-selection-write-back-canvas-endpoint-asset-center-linkage-km/49-04-SUMMARY.md when done
</output>
