---
phase: 49
plan: 03
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - src/lib/canvasAssetLinkage.ts
  - src/routes/v1/assets-registry/index.ts
  - scripts/verify-phase-49-linkage.ts
autonomous: true
requirements: [SELECT-03]
must_haves:
  truths:
    - "资产中心选定操作（PATCH /api/v1/assets-registry/:id 置 isPrimaryView=true）成功后，若该资产映射到 canvas 节点且节点在变体组内，对应 canvas_variant_groups 的 winner 同步更新——画布侧刷新后显示同一 winner (D-06)"
    - "映射不到 canvas 节点、或节点不在任何变体组 → logger.info 静默跳过，registry 主流程（响应）完全不受影响 (D-06)"
    - "联动方向 registry→canvas 单向；49-01 端点的 canvas→o_assets 置换直写库不走本 hook——无递归环"
  artifacts:
    - path: "src/lib/canvasAssetLinkage.ts"
      provides: "findCanvasNodeForAsset + applyRegistrySelectionToCanvas（db 注入、复用 selectWinnerInGroup）"
      contains: "applyRegistrySelectionToCanvas"
    - path: "src/routes/v1/assets-registry/index.ts"
      provides: "PATCH :id 的 isPrimaryView===true 选定路径联动钩子"
      contains: "applyRegistrySelectionToCanvas"
    - path: "scripts/verify-phase-49-linkage.ts"
      provides: ":memory: sqlite 上 registry→canvas 联动行为断言（替换 49-01 scaffold）"
      contains: "assert"
  key_links:
    - from: "src/routes/v1/assets-registry/index.ts"
      to: "src/lib/canvasAssetLinkage.ts"
      via: "PATCH 成功后 void applyRegistrySelectionToCanvas(u.db, id).catch(warn)"
      pattern: "applyRegistrySelectionToCanvas"
    - from: "src/lib/canvasAssetLinkage.ts"
      to: "src/lib/canvasRelationalStore.ts"
      via: "selectWinnerInGroup 复用调用（不重写选定逻辑）"
      pattern: "selectWinnerInGroup"
---

<objective>
SELECT-03 落地：资产中心 ↔ 画布联动的 registry→canvas 半向。`handleSelect` → PATCH `o_assets.isPrimaryView=true` 成功后，经 o_assets→canvas node 映射（`a-oasset-{id}` 节点 id / `data.oAssetId`——规划期已核实的 sync-assets.ts 映射机制）找到对应画布节点，复用 49-01 的 selectWinnerInGroup 更新其所在变体组 winner；映射不到则静默跳过。canvas→o_assets 反向已由 49-01 D-07 承担，两半向合起来满足 SC-3 "任一侧换选、另一侧刷新后一致"。

Purpose: 打通两套选定词汇（o_assets.isPrimaryView ↔ canvas_variant_groups.winner_node_id）。
Output: src/lib/canvasAssetLinkage.ts + registry PATCH 钩子 + verify:phase-49-linkage 行为门。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/49-selection-write-back-canvas-endpoint-asset-center-linkage-km/49-CONTEXT.md
@.planning/phases/49-selection-write-back-canvas-endpoint-asset-center-linkage-km/49-01-SUMMARY.md

<verified_facts>
规划期已核实（CONTEXT D-06 "映射机制必须先核实" 项的结论，执行者采信）：

1. **o_assets → canvas node 映射**（src/routes/canvas/v2/sync-assets.ts）：sync 建的节点 `id = "a-oasset-" + o_assets.id`（:69），`data.oAssetId = o_assets.id`（:96），节点在 asset.projectId 对应 project 下。正查优先走确定性 id 等值匹配（同一 project_id 内，任意 episodes_id——o_assets 无 episodes 维度，取首行）；兜底 `json_extract(data, '$.oAssetId')` 命中非 sync 来源但带引用的节点。
2. **组关系现实**：sync 建的节点 `variant_group_id` 为 NULL——只有用户 UI 分组并 save-v2 后才有组。"节点无组 → 静默跳过 + logger.info" 是本联动的常态路径（D-06 明文）。
3. **registry PATCH 挂点**：src/routes/v1/assets-registry/index.ts:195-223 `router.patch("/:id")`——zod updateSchema 含 isPrimaryView（:186），成功路径 `:217 update → :218 200 success`。钩子插在 update 成功之后、return 200 之前/之后皆可（不阻塞）。
4. **前端 handleSelect 调用链**：canvasApi.ts `updateAsset(assetId, { isPrimaryView: ... })` → PATCH /api/v1/assets-registry/:id（services/canvasApi.ts:303-313）——钩子在 PATCH 路由内生效即覆盖资产中心操作。
5. **loop 预防（必须在代码注释中写明）**：49-01 select-winner 端点的 D-07 置换**直写 o_assets 表**，不经 registry PATCH 路由、不调本模块 → registry→canvas hook 与 canvas→o_assets 直写不构成环。本模块也绝不调用 registry 路由或改 o_assets（只读 + 写 canvas）。
6. **selectWinnerInGroup 签名**（49-01 交付）：`selectWinnerInGroup(trxDb, {projectId, episodesId}, groupId, winnerNodeId) → SelectWinnerResult`——status not_found/multi_mode 等 here 是 info 跳过而非错误（canvas 组可能 multi——registry 侧无法预知，跳过即可）。
</verified_facts>

<interfaces>
消费的既有契约：

From src/lib/canvasRelationalStore.ts (49-01):
```typescript
selectWinnerInGroup(trxDb, scope: { projectId: number; episodesId: number }, groupId: string, winnerNodeId: string): Promise<SelectWinnerResult>
```

From src/routes/v1/assets-registry/index.ts（挂点现状）:
```typescript
// router.patch("/:id") 内：await u.db("o_assets").where("id", id).update(updates);
// return res.status(200).send(success({ id, updated: Object.keys(updates) }));
```

本 plan 创建：

```typescript
// src/lib/canvasAssetLinkage.ts
export interface CanvasNodeRef {
  nodeId: string;
  projectId: number;
  episodesId: number;
  variantGroupId: string | null;
}
export async function findCanvasNodeForAsset(db: any, oAssetId: number): Promise<CanvasNodeRef | null>;
export async function applyRegistrySelectionToCanvas(db: any, oAssetId: number): Promise<void>; // 永不 throw
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/lib/canvasAssetLinkage.ts 联动库</name>
  <files>src/lib/canvasAssetLinkage.ts</files>
  <read_first>
  - src/routes/canvas/v2/sync-assets.ts:60-100（a-oasset-{id} / data.oAssetId 映射事实）
  - src/lib/canvasRelationalStore.ts（selectWinnerInGroup 签名 + SelectWinnerResult）
  - src/lib/ingestAssets.ts（db 注入库文件样板）
  </read_first>
  <behavior>
  - findCanvasNodeForAsset：① db("o_assets") 读 projectId（行不存在 → null）② `db("canvas_nodes").where({ id: "a-oasset-" + oAssetId, project_id: projectId }).first()` ③ 未命中兜底 `where({ project_id }).whereRaw("json_extract(data, '$.oAssetId') = ?", [oAssetId]).first()` ④ 仍无 → null；命中返回 { nodeId, projectId, episodesId, variantGroupId }
  - applyRegistrySelectionToCanvas：node null → logger.info("资产 {id} 未映射画布节点，跳过联动")；variantGroupId null → logger.info("节点 {nodeId} 不在变体组，跳过联动")；否则调 selectWinnerInGroup(db, {projectId, episodesId}, variantGroupId, nodeId)，status==="updated" → info 成功，其余 status（not_found/multi_mode/not_in_group/idempotent）→ info 跳过——全部非异常路径
  - 对外永不 throw：顶层 try/catch，异常 → console.warn("[canvasAssetLinkage] 联动失败(不影响 registry 主流程):", err)
  - 只写 canvas 表；不 import @/utils；不改 o_assets（只读 projectId 一列）
  </behavior>
  <action>
新建 src/lib/canvasAssetLinkage.ts 按 <interfaces> 签名实现 behavior。模块头注释写明：联动方向 registry→canvas 单向（D-06）；反向由 select-winner 端点 D-07 直写承担且不经 registry 路由（loop 预防，引用 verified_facts 第 5 条）；"静默跳过是常态"（sync 节点默认无组）。logger 用注入或 console.info/warn 直用（与 repo 惯例一致即可，无需引 winston）。
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "applyRegistrySelectionToCanvas" src/lib/canvasAssetLinkage.ts</automated>
  </verify>
  <acceptance_criteria>
- 文件无 @/utils import、无对 o_assets 的 update/insert（只 .select/.first 读）
- selectWinnerInGroup 从 canvasRelationalStore import（不重写选定逻辑）
- json_extract 走 whereRaw 占位参数（无字符串拼接值）
- 顶层 try/catch 兜底可见（导出函数不向外抛）
- 文件头注释含 "registry→canvas" 与 loop 预防记述
  </acceptance_criteria>
  <done>联动库落地：查找（双路径）/跳过（三种常态）/复用选定/异常吞噬全部实现</done>
</task>

<task type="auto">
  <name>Task 2: registry PATCH 钩子 + verify-phase-49-linkage.ts 行为门（替换 scaffold）</name>
  <files>src/routes/v1/assets-registry/index.ts, scripts/verify-phase-49-linkage.ts</files>
  <read_first>
  - src/routes/v1/assets-registry/index.ts:178-223（PATCH handler 全文——挂点）
  - scripts/verify-phase-49.ts（:memory: harness + canvas/o_assets DDL 镜像样板——直接复用其建表代码形状）
  - src/lib/canvasAssetLinkage.ts（Task 1 产出）
  </read_first>
  <action>
index.ts PATCH handler：在 `await u.db("o_assets").where("id", id).update(updates)` 成功之后（return 200 之前）插入：仅当 `updates.isPrimaryView === true` 时 `void applyRegistrySelectionToCanvas(u.db, id).catch((e) => console.warn("[assets-registry] 选定联动画布失败(不影响资产更新):", e))`。注释标注 D-06 + 只挂 true（选定事件；置 false 是取消选定，不产生新 winner）。import 走相对/别名路径与文件既有 import 风格一致。**不改动 PATCH 其余任何行为**（zod/allowed/404/200 形状不动）。scripts/verify-phase-49-linkage.ts 整体替换 49-01 scaffold：:memory: sqlite 建镜像 canvas_nodes/canvas_variant_groups/o_assets（复用 verify-phase-49.ts 的 DDL 形状），直接调 applyRegistrySelectionToCanvas(db, id) 断言：(a) 资产有 a-oasset- 节点且在 g1 组 → g1.winner_node_id 更新为该节点、原 winner is_winner 置 false、新 winner true；(b) 同场景换选另一资产 → winner 再次迁移；(c) 节点无 variant_group_id → 返回不 throw，DB 无变化，(d) 资产无 canvas 节点 → 不 throw 无变化；(e) multi 组资产 → 不 throw 无变化（info 跳过）；(f) json_extract 兜底：节点 id 非 a-oasset- 形状但 data.oAssetId 命中 → 仍联动成功；(g) 路由源断言：index.ts 中钩子调用出现在 update 之后且被 void+catch 包裹、仅在 isPrimaryView===true 分支（源码 grep 形状断言）。末尾 PASS/FAIL 汇总 exit 码。**package.json 不动**（49-01 已注册）。
  </action>
  <verify>
    <automated>npm run verify:phase-49-linkage && npx tsc --noEmit && grep -c "applyRegistrySelectionToCanvas" src/routes/v1/assets-registry/index.ts</automated>
  </verify>
  <acceptance_criteria>
- npm run verify:phase-49-linkage exit 0，≥8 条断言全 PASS，无 "SKIP: scaffold" 残留
- index.ts 的钩子：调用前有 updates.isPrimaryView === true 条件、调用带 void 前缀与 .catch、位于 o_assets update 之后（grep 上下文可证）
- PATCH 的 200 响应形状未变（success({ id, updated }) 原样）
- verify 脚本 grep '"db2.sqlite"' = 0
- npm run verify:phase-49 复跑 exit 0（无回归）
- 强制失败 sanity：翻转一条断言 → exit 1 → 还原（SUMMARY 记录）
  </acceptance_criteria>
  <done>SC-3 registry→canvas 半向闭合：资产中心选定 → 画布组 winner 同步，常态跳过与失败隔离有机器断言</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry PATCH → canvas 表 | 联动跨子系统写（复用 49-01 事务函数） |
| o_assets 行 → canvas_nodes 查找 | json_extract 对存储 JSON 的解析面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-10 | Tampering | json_extract 查询 | mitigate | whereRaw 占位参数传 oAssetId，绝不拼接；等值匹配仅返回自有项目行 |
| T-49-11 | DoS | 联动拖垮 registry 主流程 | mitigate | void + .catch 隔离；钩子在主 update/响应语义之外，任何联动异常不改 PATCH 响应 |
| T-49-12 | Tampering | 跨项目误写 | mitigate | 查找限定 project_id = 资产 projectId；selectWinnerInGroup 内组/节点双重 scope 校验（49-01 已含） |
| T-49-13 | Tampering | 双向联动死循环 | mitigate | 方向性设计：本模块只写 canvas；49-01 D-07 直写 o_assets 不经本 hook——代码注释 + verify 源断言固化 |
| T-49-SC | Tampering | npm 安装 | accept | 零新增依赖 |
</threat_model>

<verification>
- npx tsc --noEmit exit 0
- npm run verify:phase-49-linkage exit 0
- npm run verify:phase-49 复跑 exit 0
- git diff 累计仅触 files_modified 三文件
</verification>

<success_criteria>
- SELECT-03 registry→canvas 半向交付且行为全断言；与 49-01 D-07 合成 SC-3 双向一致性
- registry 主流程零风险（响应形状不变、失败隔离可证）
</success_criteria>

<output>
Create .planning/phases/49-selection-write-back-canvas-endpoint-asset-center-linkage-km/49-03-SUMMARY.md when done
</output>
