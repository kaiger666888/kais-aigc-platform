---
phase: 52-prompt-edit-regenerate-loop
plan: 06
type: execute
wave: 5
depends_on:
  - "03"
  - "04"
  - "05"
  - "07"
  - "08"
files_modified:
  - scripts/verify-phase-52.ts
  - package.json
  - .planning/phases/52-prompt-edit-regenerate-loop/52-VERIFICATION.md
  - .planning/phases/52-prompt-edit-regenerate-loop/52-VALIDATION.md
autonomous: true
requirements: [REGEN-01, REGEN-02, REGEN-03, REGEN-04]
must_haves:
  truths:
    - "scripts/verify-phase-52.ts 为 Phase 52 聚合契约门(GUARD 收尾传统,ROADMAP 决策 #7):S1 REGEN-01 source-shape + S2 REGEN-02 接线形状 + S3 REGEN-03 stale 语义 + S4 REGEN-04 交互形状 + S5 命令门/回归汇总 + forced-failure 自检(51 范式,意外 PASS 即整门红)"
    - "verify-phase-52 头注释沿袭 e2e 前置说明:e2e 跑 dist 非源码,任何 e2e 复跑前必须 npm run build(地雷 #10);聚合门自身不跑 e2e"
    - "npm script verify:phase-52 注册进 package.json(与 verify:phase-51 同区,注册一次)"
    - "52-VERIFICATION.md 记录:全套命令门结果、地雷 #11 遗留说明(重生成后下游不自动标 stale——锁定决策未授权,本期不做)、人工项(审片面板开合体验,52-VALIDATION Manual-Only)"
    - "52-VALIDATION.md frontmatter 收尾:nyquist_compliant: true、wave_0_complete: true、status 更新"
  artifacts:
    - path: "scripts/verify-phase-52.ts"
      provides: "Phase 52 聚合 grep/source-shape 门 + forced-failure 自检"
      contains: "verify-phase-52"
    - path: "package.json"
      provides: "verify:phase-52 npm script"
      contains: "verify:phase-52"
    - path: ".planning/phases/52-prompt-edit-regenerate-loop/52-VERIFICATION.md"
      provides: "Phase 52 验证报告 + 遗留说明"
  key_links:
    - from: "scripts/verify-phase-52.ts"
      to: "packages/infinite-canvas + packages/flowgraph-v3 + src/routes/canvas"
      via: "绝对 REPO_ROOT 路径 grep source-shape(与 verify-phase-51 同范式)"
      pattern: "REPO_ROOT"
---

<objective>
Phase 52 收尾聚合门:新建 `scripts/verify-phase-52.ts`(沿袭 verify-phase-51 范式——S1-S5 分节、绝对路径 grep、forced-failure 自检、头注释 e2e 前置说明),注册 `verify:phase-52` npm script,产出 52-VERIFICATION.md(含地雷 #11 遗留说明与人工项),并收尾 52-VALIDATION.md frontmatter。确认全套 vitest/tsc/e2e 绿。

本 plan 不改任何产品代码;如聚合门发现红项,回退对应 plan 修复而非在聚合门里放行。

输出:verify-phase-52.ts + npm script + 52-VERIFICATION.md + 52-VALIDATION.md 收尾。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-prompt-edit-regenerate-loop/52-CONTEXT.md
@.planning/phases/52-prompt-edit-regenerate-loop/52-RESEARCH.md
@.planning/phases/52-prompt-edit-regenerate-loop/52-VALIDATION.md

**COORD-01 开工 checklist:本 plan 触碰零 kmc 侧文件(kais-hermes-skills),变更面检查 N/A。**

<interfaces>
<!-- 聚合门分节设计,提取自 52-RESEARCH.md Validation Architecture 表 -->

verify-phase-51 范式(scripts/verify-phase-51.ts,必读):头注释分节说明 + e2e 前置 NOTE;tsx 直跑;绝对 REPO_ROOT grep;forced-failure 自检(50-02 先例,意外 PASS 整门红);exit 0/1/2 语义。

S 分节(每节 = grep/source-shape 断言组,不重新实现逻辑):
- **S1 REGEN-01**:canvasStore.ts 含 `updateEventParams(` 与 `persistEventParams`;NodeDetailPanel.tsx 含 PromptSection;serialize.ts 含配方反向覆盖特征(role === 'output' 反查 + script 例外注释);execute.ts zod 含 `prompt` 与 `params`;canvasApi.ts executeNode body 含 `...extra`;**52-07 增补**:canvasAssetSchema.ts asset 段含 nullish 化特征(`nullish`);**52-08 增补**:FlowCanvas.tsx 含 `syntheticDetailNode`(落选变体详情入口)
- **S2 REGEN-02**:EventParamsPopover.tsx 无「执行后端待接入」与 console.log 残桩、含 `executeNode(` 与 `updateEventParams(`;eventChipBus.ts anchor 含 projectId/episodesId;FlowCanvas.tsx 含 `{ ...info` 注入行
- **S3 REGEN-03**:stale.ts 含 `getDownstreamIds` export;orchestrate.ts skip 谓词含 `data.stale`;mock server.mjs 含 `data.stale` 镜像 + execute/orchestrate logCall 记完整 body;canvasStore.ts applySocketNodeState 含 stale 清除分支;useStaleRerun.ts 存在且含 rerunStaleChain;NodeBadges.tsx 含 stopPropagation;serialize.ts 含 `data.stale`;migrate.ts 无裸 `stale: null` 硬编码
- **S4 REGEN-04**:NodeDetailPanel.tsx 默认宽含 `480`;FlowCanvas.tsx onNodeClick 含修饰键守卫(ctrlKey/metaKey/shiftKey)与 detailNode 条件分支
- **S5 命令门汇总**:双根 tsc、双包 vitest 已由各 plan 保证——本节复核运行(命令门):根 `npx tsc --noEmit`、pkg `tsc -b`、flowgraph-v3 `tsc --noEmit`、双包 vitest run、**`npm run verify:save-v2-legacy`(52-07 产出的离线行为锁,必跑)**;e2e 不跑(头注释声明前置 `npm run build` + `npm run test:e2e` 由 VERIFICATION 记录结果;**全套 e2e 含 phase52-regen.mjs,其 2 处装置修复由 52-08 落地——本 plan 必须 wave 5 于 52-08 之后执行,否则全套终跑假红**)
- **forced-failure 自检**:一组 must-fail 断言(如对不存在文件/不存在字符串的 grep 必须为 0 命中类反向断言),意外 PASS 即整门红
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/verify-phase-52.ts 聚合门(S1-S5 + forced-failure)+ npm script 注册</name>
  <files>scripts/verify-phase-52.ts (new), package.json</files>
  <read_first>scripts/verify-phase-51.ts (全文,分节/头注释/forced-failure/exit 语义范式), scripts/verify-phase-50.ts (forced-failure 先例), package.json (verify:phase-51 注册行 L44 区域), 52-01..52-05 各 SUMMARY(落地特征行以各 plan 实际产出为准——grep 特征须与实现核对,不得照抄 plan 文本)</read_first>
  <action>
1. 新建 scripts/verify-phase-52.ts(tsx 直跑,shebang + 头注释):头注释写明分节内容与 e2e 前置 NOTE(e2e 跑 dist,复跑前必须 `npm run build`,沿袭 51 范式,地雷 #10);按 interfaces S1-S5 实现 grep/source-shape 断言——**先读各 plan 实际落地代码再定 grep 特征行**(SUMMARY + 源文件实证),严禁照抄 plan 文本导致假绿/假红。
2. forced-failure 自检段:must-fail 断言组,意外 PASS 整门红(50-02/51 范式)。
3. S5 命令门:spawn 执行三根 tsc + 双包 vitest(输出 tail 摘要);任一非零 exit 即红。
4. package.json scripts 注册 `"verify:phase-52": "npx tsx scripts/verify-phase-52.ts"`(与 verify:phase-51 同区,只加一行,不重排)。
5. 跑通:`npm run verify:phase-52` exit 0;若有红项,定位责任 plan 修复后复跑(本 plan 不改产品代码,修复回到对应 plan 的文件)。
  </action>
  <verify>
    <automated>npm run verify:phase-52 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:phase-52` exit 0(S1-S5 全绿 + forced-failure 自检行为正确)
    - `grep -c 'verify:phase-52' package.json` ≥ 1
    - verify-phase-52.ts 头注释含 e2e 前置(npm run build)说明
    - package.json diff 仅一行新增
  </acceptance_criteria>
  <done>Phase 52 聚合门落地并跑绿,verify:phase-52 成为后续回归的单入口。</done>
</task>

<task type="auto">
  <name>Task 2: 52-VERIFICATION.md + 52-VALIDATION.md 收尾 + 全套终跑</name>
  <files>.planning/phases/52-prompt-edit-regenerate-loop/52-VERIFICATION.md (new), .planning/phases/52-prompt-edit-regenerate-loop/52-VALIDATION.md (frontmatter)</files>
  <read_first>.planning/phases/51-canonical-write-path-coordination-guard/51-VERIFICATION.md (报告格式范式), .planning/phases/52-prompt-edit-regenerate-loop/52-VALIDATION.md (Manual-Only 表 + frontmatter 字段), 52-01..52-05 SUMMARY(各 plan 实证结果汇总)</read_first>
  <action>
1. 全套终跑(按 52-VALIDATION.md Full suite command):`cd packages/infinite-canvas && npm run build && npm test && npm run test:e2e && cd ../flowgraph-v3/ts && npm test && npx tsc --noEmit && npx tsx scripts/verify-phase-52.ts`(根 tsc 由 S5 已含,此处按 VALIDATION 全量命令实证)。
2. 写 52-VERIFICATION.md:四需求(REGEN-01..04)各自验证证据(plan → 自动化断言 → 结果);命令门结果表;**地雷 #11 遗留说明**:重生成后下游不自动标 stale(socket success 路径无 triggerStaleCascade 接线,锁定决策未授权,贸然全局触发会把 Phase 37 批量执行下游全标脏)——本期不做,如未来需要,最小方案是 per-request 关联后仅在 PromptSection/reroll 发起路径触发;§14 窄通道遗留(steps/cfg/lora/quant 等全配方持久化出范围,地雷 #3);人工验证项:审片场景面板开合体验(52-VALIDATION Manual-Only,480px 视觉 + 单击跟随流畅度)。
3. 52-VALIDATION.md frontmatter 收尾:`nyquist_compliant: true`、`wave_0_complete: true`、status 更新;Per-Task Verification Map Status 列按实际结果标 ✅。
  </action>
  <verify>
    <automated>npm run verify:phase-52 2>&1 | tail -5 && grep -c 'nyquist_compliant: true' .planning/phases/52-prompt-edit-regenerate-loop/52-VALIDATION.md && grep -c '地雷 #11\|triggerStaleCascade' .planning/phases/52-prompt-edit-regenerate-loop/52-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 全套终跑命令链 exit 0(build → 双 vitest → e2e 全套 → tsc → verify:phase-52)
    - 52-VERIFICATION.md 存在且含四需求证据 + 地雷 #11/窄通道遗留 + 人工项
    - 52-VALIDATION.md frontmatter 含 nyquist_compliant: true 与 wave_0_complete: true
  </acceptance_criteria>
  <done>Phase 52 验证闭环:全套自动化绿、聚合门绿、遗留与人工项成文。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify 脚本 → 源文件 grep / spawn 命令 | 本地只读验证 + 本地构建命令;不触网、不写产品代码 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-06-01 | Tampering | 聚合门假绿(grep 特征与实现不符) | mitigate | 特征行先实证源文件再写;forced-failure 自检证明门能红(50-02/51 范式) |
| T-52-06-02 | Tampering | e2e 跑旧 dist 造成假绿/假红 | mitigate | 头注释前置声明 + VERIFICATION 记录 build→e2e 顺序(地雷 #10) |
| T-52-06-SC | Tampering | package installs | accept | 零新包,无 install 任务 |

**结论:纯验证层,无攻击面。无 high 级威胁。**
</threat_model>

<verification>
- `npm run verify:phase-52` exit 0(S1-S5 + forced-failure)
- 全套终跑:build → 双包 vitest → `npm run test:e2e` → 三根 tsc → verify:phase-52 全绿
- 52-VERIFICATION.md / 52-VALIDATION.md 收尾完成
</verification>

<success_criteria>
- Phase 52 四需求全部有自动化锁死并经聚合门单入口复验(GUARD 收尾传统,ROADMAP 决策 #7)
- 地雷 #11 与窄通道遗留成文,人工验证项登记
</success_criteria>

<output>
Create `.planning/phases/52-prompt-edit-regenerate-loop/52-06-SUMMARY.md` when done. 记录:聚合门 grep 特征与实际实现的核对过程、终跑全套结果。
</output>
