---
phase: 52-prompt-edit-regenerate-loop
plan: 05
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - packages/infinite-canvas/src/hooks/useStaleRerun.ts
  - packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx
  - packages/infinite-canvas/src/components/badges/NodeBadges.tsx
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
  - packages/infinite-canvas/test/e2e/tests/phase52-stale-panel.mjs
autonomous: true
requirements: [REGEN-03, REGEN-04]
must_haves:
  truths:
    - "useStaleRerun.rerunStaleChain(nodeId):getDownstreamIds(graph, nodeId)+自身 → 过滤 kind==='asset' && stale!=null → 空则 toast「无 stale 下游」早退 → 保存(serializeGraphToV2,data.stale 已上 wire)→ orchestrateCanvas(pid, eid, nodeIds 子集)→ 既有 orchestrate:* socket 进度链零改动复用"
    - "双出口:StaleSection 加「🔄 重跑下游」按钮(props 加 nodeId,内部组件无外部契约)+ NodeBadges stale 角标可点击(onClick + e.stopPropagation() + cursor:pointer + title)——stopPropagation 必须,否则冒泡触发 RF onNodeClick 连带切面板(地雷 #8);slots/NodeBadgesProps 契约不改"
    - "重跑完成后 stale 标记消除:node:state running/success → applySocketNodeState 自动清 stale(52-01)→ 角标/StaleSection 自动消失;服务端 stale success 不跳过(52-02)使重跑真正执行"
    - "NodeDetailPanel 默认宽 window.innerWidth * 0.75 → 480(L36 一行改;拖拽调宽与 min 400 不动)"
    - "onNodeClick:面板开着时单击另一节点 setDetailNode(node) 保持打开并跟随刷新;面板关着时单击不打开;ctrl/shift/⌘ 修饰键守卫——多选点击只选不切面板(地雷 #9);点空白/Esc/关闭按钮/双击/eventChip early-return 全维持现状"
    - "projectId/episodesId 缺失(fixture 模式)→ 重跑早退 toast(deleteNode 范式,地雷 #13)"
  artifacts:
    - path: "packages/infinite-canvas/src/hooks/useStaleRerun.ts"
      provides: "rerunStaleChain(nodeId) 统一处理器(双出口共用)"
      contains: "rerunStaleChain"
    - path: "packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx"
      provides: "StaleSection 按钮 + 默认宽 480"
      contains: "480"
    - path: "packages/infinite-canvas/src/components/badges/NodeBadges.tsx"
      provides: "stale 角标可点击(stopPropagation)"
      contains: "stopPropagation"
    - path: "packages/infinite-canvas/src/components/FlowCanvas.tsx"
      provides: "onNodeClick 面板保持打开 + 修饰键守卫"
    - path: "packages/infinite-canvas/test/e2e/tests/phase52-stale-panel.mjs"
      provides: "REGEN-03/04 e2e:stale 注入→重跑→消除 + 面板宽/跟随断言"
  key_links:
    - from: "packages/infinite-canvas/src/hooks/useStaleRerun.ts"
      to: "packages/flowgraph-v3/ts/src/stale.ts + services/canvasApi.ts"
      via: "getDownstreamIds → saveCanvasGraph(serializeGraphToV2) → orchestrateCanvas(nodeIds)"
      pattern: "getDownstreamIds"
    - from: "packages/infinite-canvas/src/components/badges/NodeBadges.tsx"
      to: "packages/infinite-canvas/src/hooks/useStaleRerun.ts"
      via: "stale svg onClick → rerunStaleChain(nodeId)(包内直接 import,C 层契约不变)"
      pattern: "rerunStaleChain"
    - from: "src/routes/canvas/orchestrate.ts"
      to: "packages/infinite-canvas/src/store/canvasStore.ts"
      via: "stale success 不跳过(52-02)→ 执行 → node:state → applySocketNodeState 清 stale(52-01)→ 角标消失"
      pattern: "stale"
---

<objective>
REGEN-03 出口落地 + REGEN-04 面板交互:新建 useStaleRerun 统一处理器(getDownstreamIds 收集 stale 链 → 保存 → orchestrate nodeIds 子集,复用既有 orchestrate:* 进度链),接双出口(StaleSection 按钮 + stale 角标可点击);面板默认宽改 ~480px;onNodeClick 改为"面板开着则跟随切换、关着不打开"并加多选修饰键守卫。e2e 锁 stale 注入→重跑→消除全链与面板宽/跟随行为。

**Wave 3 串行理由:** 与 52-03(W2)共享 NodeDetailPanel.tsx(区域不同——本 plan 碰 StaleSection L580-593 + L36 宽度,52-03 碰区块序 L156-157 插入 PromptSection——但同文件必须串行,depends_on 含 03);与 52-04(W2)共享 FlowCanvas.tsx(本 plan 仅 L470-477 onNodeClick 区,52-04 仅 L197-198 chip 区,wave 2→3 天然串行,区域边界互不越界)。

输出:useStaleRerun + 双出口、面板 480 + 单击跟随、phase52-stale-panel e2e。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-prompt-edit-regenerate-loop/52-CONTEXT.md
@.planning/phases/52-prompt-edit-regenerate-loop/52-RESEARCH.md

**COORD-01 开工 checklist:本 plan 触碰零 kmc 侧文件(kais-hermes-skills),变更面检查 N/A。**

<interfaces>
<!-- 提取自 52-RESEARCH.md Task 5 / Task 6,执行者直接取用 -->

W1/W2 已就绪能力(纯消费):
- getDownstreamIds(graph, nodeId)(52-01,flowgraph-v3 stale.ts 导出,经包 index 自动生效)
- data.stale 已上 wire + orchestrate stale success 不跳过 + mock 镜像(52-02)
- applySocketNodeState running/success 自动清 stale(52-01)
- executeNode/orchestrateCanvas/saveCanvasGraph 既有签名

rerunStaleChain 处理器(新建 src/hooks/useStaleRerun.ts,或并入 useStale.ts——新建独立文件,useStale.ts 保持原样):
1. getDownstreamIds(graph, nodeId) + 自身 → 过滤 kind==='asset' && stale != null → nodeIds(空 → toast「无 stale 下游」早退)
2. projectId/episodesId 缺失 → toast「缺少项目上下文」早退(地雷 #13);orchestration.status==='running' 守卫(FlowCanvas handleOrchestrate L551 范式)
3. 保存:saveCanvasGraph(pid, eid, serializeGraphToV2(graph, rawDataByNodeId))(此刻 data.stale 已上 wire,服务端可见)→ orchestrateCanvas(pid, eid, nodeIds)(handleBatchExecute L118-128 同款范式)
4. 进度:既有 orchestrate:* socket 链(useCanvasSocket L163-171 → FlowCanvas L225-249)零改动;node:state → applySocketNodeState 清 stale → UI 自动消失

StaleSection(NodeDetailPanel L580-593):现纯展示;加「🔄 重跑下游」按钮调 rerunStaleChain(node.id)——props 现 {stale, graph},加 nodeId 字段(内部组件无外部契约)

NodeBadges(packages/infinite-canvas/src/components/badges/NodeBadges.tsx L71-80):C 层经 slots 注册(registerCInteractions.ts),包内可直接 import hooks(L14 已 import useStalePulse);给 stale svg 包 onClick + **e.stopPropagation()**(必须,地雷 #8:否则冒泡到 RF onNodeClick,REGEN-04 落地后连带切详情面板)+ cursor:'pointer' + title「重跑下游」;**不改 NodeBadgesProps/slots 契约**;fallback NodeBadgesDefault.tsx 不在本 plan 范围(注释注明,C 层为线上路径)

onNodeClick(FlowCanvas L470-477)改法:
```ts
// 面板开着时单击另一节点 → setDetailNode(node) 保持打开;面板关着 → 不打开
// 修饰键守卫:ctrl/meta/shift 多选点击只选不切面板(地雷 #9)
```
- detailNode 随派生重解析已安全(store setGraph L430-431);同 id 切换时 NodeDetailPanel L61 useEffect 重置 tab(CONTEXT 锁定维持)
- 维持现状:点空白(onPaneClick L462-468)、Esc(L729-735)、双击(L482-487,zoomOnDoubleClick=false 已设)、eventChip early-return(L472)
- 拖拽:RF 内部位移抑制,理论安全;不加防护,留 onNodeDragStop suppress 逃生口注释
- phase37 多选 e2e 经 testMode hook 驱动(setSelectedNodeIds),不点节点,不受守卫影响

默认宽度:NodeDetailPanel L36 `window.innerWidth * 0.75` → `480`(Math.max(400, 480)=480;L68 拖拽/min 400 不动),一行改

e2e 注入范式:save-v2 注入带 data.stale 的 success 节点(phase40 L54-57 同款);e2e 断言 mock orchestrate 调用 nodeIds 含该节点且未跳过(response total 计入);**复跑前必须 npm run build(地雷 #10)**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useStaleRerun hook(rerunStaleChain 统一处理器)</name>
  <files>packages/infinite-canvas/src/hooks/useStaleRerun.ts (new)</files>
  <read_first>packages/infinite-canvas/src/hooks/useStale.ts (triggerStaleCascade 既有范式 + stale pulse store), packages/infinite-canvas/src/components/FlowCanvas.tsx (handleBatchExecute L118-128 区域:保存+orchestrateCanvas nodeIds 范式;handleOrchestrate L551-568 orchestration.status 守卫), packages/flowgraph-v3/ts/src/stale.ts (getDownstreamIds 签名,52-01 落地后), packages/infinite-canvas/src/store/canvasStore.ts (graph/rawDataByNodeId/projectId/episodesId/orchestration/showToast 选择子), packages/infinite-canvas/src/v3/serialize.ts (serializeGraphToV2 签名)</read_first>
  <action>
1. 新建 src/hooks/useStaleRerun.ts:导出 `useStaleRerun()` 返回 `{ rerunStaleChain: (nodeId: string) => Promise<void> }`;实现按 interfaces 四步:守卫(graph null / pid·eid 缺失 / orchestration.status==='running' → toast 早退)→ getDownstreamIds + 自身过滤 stale 资产(空 → toast「无 stale 下游」)→ saveCanvasGraph(serializeGraphToV2)→ orchestrateCanvas(pid, eid, nodeIds)。
2. 错误处理:保存或 orchestrate 失败 catch → showToast(err.message, 'error')(handleSave 51-01 范式);不在 hook 内清 stale(清除交给 socket node:state → applySocketNodeState 52-01 链)。
3. 注释写明:进度反馈复用 orchestrate:* socket 链,本 hook 不订阅 socket。
4. 编译门:`npx tsc -b` clean。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx tsc -b 2>&1 | tail -3 && grep -c 'rerunStaleChain' src/hooks/useStaleRerun.ts && grep -c 'getDownstreamIds' src/hooks/useStaleRerun.ts && grep -c 'orchestrateCanvas' src/hooks/useStaleRerun.ts</automated>
  </verify>
  <acceptance_criteria>
    - useStaleRerun.ts 含 rerunStaleChain 导出 + getDownstreamIds/orchestrateCanvas/serializeGraphToV2 调用(grep 各 ≥ 1)
    - 三类早退守卫齐全(缺上下文 / running 中 / 无 stale 下游)
    - `cd packages/infinite-canvas && npx tsc -b` exit 0;`npm test` 全绿
  </acceptance_criteria>
  <done>重跑链统一处理器落地,双出口可共用;保存→orchestrate 子集范式与 handleBatchExecute 同构。</done>
</task>

<task type="auto">
  <name>Task 2: 双出口接线——StaleSection 按钮 + NodeBadges 角标可点击</name>
  <files>packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx (StaleSection L580-593 + 调用处 props), packages/infinite-canvas/src/components/badges/NodeBadges.tsx (L71-80 stale svg)</files>
  <read_first>packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx (StaleSection 全文 + AssetDetail 区块序中 StaleSection 调用处,确认与 52-03 PromptSection 插入点无区域冲突——52-03 已合并), packages/infinite-canvas/src/components/badges/NodeBadges.tsx (全文:stale svg L71-80、useStalePulse import L14、组件 props), packages/infinite-canvas/src/hooks/useStaleRerun.ts (Task 1 产出), packages/infinite-canvas/src/components/canvas/slots.ts (NodeBadgesProps 契约,确认不改)</read_first>
  <action>
1. NodeDetailPanel StaleSection:props 加 `nodeId: string`(调用处传 node.id);在现有展示内容下加「🔄 重跑下游」按钮 → 调 useStaleRerun 的 rerunStaleChain(nodeId);按钮样式与区块既有风格一致;加 data-testid="stale-rerun-btn"。
2. NodeBadges.tsx:stale svg 外包一层(或直接给 svg)onClick → `e.stopPropagation()`(**必须**,注释注明防冒泡触发 RF onNodeClick 连带切面板,地雷 #8)→ rerunStaleChain(nodeId);style 加 cursor:'pointer';svg 加 title「重跑下游」。组件内直接 import useStaleRerun(与 L14 useStalePulse 同款包内 import),**不改 NodeBadgesProps/slots/registerCInteractions**。
3. NodeBadges 需要 nodeId——确认组件既有 props 已含 nodeId(L18 useStalePulse 选择子已用 nodeId,实证后取用);若无则从既有 props 取,不得改 slots 契约。
4. **区域纪律**:NodeDetailPanel.tsx 本 plan 只碰 StaleSection 定义 + 其调用处 + L36 宽度(Task 3);PromptSection 区(52-03)不动。
5. 编译 + 单测门。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx tsc -b 2>&1 | tail -3 && grep -c 'rerunStaleChain' src/components/panel/NodeDetailPanel.tsx && grep -c 'stopPropagation' src/components/badges/NodeBadges.tsx && grep -c 'rerunStaleChain' src/components/badges/NodeBadges.tsx</automated>
  </verify>
  <acceptance_criteria>
    - NodeDetailPanel.tsx StaleSection 含 rerunStaleChain 调用 + data-testid="stale-rerun-btn"
    - NodeBadges.tsx stale 角标含 stopPropagation + rerunStaleChain(grep 各 ≥ 1)
    - slots.ts 与 registerCInteractions.ts 零 diff
    - `cd packages/infinite-canvas && npx tsc -b` exit 0;`npm test` 全绿
  </acceptance_criteria>
  <done>双出口接通:StaleSection 按钮 + 角标点击均进 rerunStaleChain,冒泡隔离有 stopPropagation 锁死。</done>
</task>

<task type="auto">
  <name>Task 3: REGEN-04——面板默认宽 480 + onNodeClick 跟随/修饰键守卫</name>
  <files>packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx (L36), packages/infinite-canvas/src/components/FlowCanvas.tsx (仅 L470-477 onNodeClick 区)</files>
  <read_first>packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx (L35-38 宽度初始化 + L68 拖拽 min 400 逻辑), packages/infinite-canvas/src/components/FlowCanvas.tsx (L462-468 onPaneClick、L470-477 onNodeClick、L482-487 onNodeDoubleClick、L729-735 Esc、detailNode 选择子), packages/infinite-canvas/test/e2e/tests/phase35-storyboard-metadata.mjs (L60-105 dblclick 开/✕ 关范式,回归基准)</read_first>
  <action>
1. NodeDetailPanel L36:`window.innerWidth * 0.75` → `480`(fallback 常量 960 可保留或同步 480,Claude discretion);L68 拖拽调宽与 min 400 不动。一行改。
2. FlowCanvas onNodeClick(L470-477)重写:修饰键守卫——`event.ctrlKey || event.metaKey || event.shiftKey` 时只 setSelectedNode 不动面板(多选语义优先,地雷 #9);否则 setSelectedNode(node) 后,若当前 detailNode != null(面板开着)→ setDetailNode(node) 跟随切换;面板关着 → 不打开(维持现状)。detailNode 用组件内既有订阅值或 getState(实证取一致风格)。
3. 不动:onPaneClick/Esc/onNodeDoubleClick/eventChip early-return/handleLocateNode;加注释:拖拽误触理论安全(RF 位移抑制),若实测误触留 onNodeDragStop suppress 逃生口。
4. **区域纪律**:FlowCanvas.tsx 本 plan diff 仅限 onNodeClick 函数体;L197-198 chip 区(52-04)不动。
5. 编译 + 单测门。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx tsc -b 2>&1 | tail -3 && grep -n '480' src/components/panel/NodeDetailPanel.tsx | head -3 && grep -n 'ctrlKey' src/components/FlowCanvas.tsx</automated>
  </verify>
  <acceptance_criteria>
    - NodeDetailPanel.tsx 默认宽含 `480`(grep 命中);`window.innerWidth * 0.75` 不再存在于宽度初始化
    - FlowCanvas.tsx onNodeClick 含修饰键守卫(ctrlKey/metaKey/shiftKey grep 命中)+ detailNode 条件分支
    - FlowCanvas.tsx diff 仅限 onNodeClick 区域
    - `cd packages/infinite-canvas && npx tsc -b` exit 0;`npm test` 全绿
  </acceptance_criteria>
  <done>面板 480 默认宽 + 单击跟随切换 + 多选守卫落地,关闭路径全部维持现状。</done>
</task>

<task type="auto">
  <name>Task 4: phase52-stale-panel e2e——stale 注入→重跑→消除 + 面板宽/跟随</name>
  <files>packages/infinite-canvas/test/e2e/tests/phase52-stale-panel.mjs (new)</files>
  <read_first>packages/infinite-canvas/test/e2e/tests/phase40-review-gate-hardening.mjs (L54-57 save-v2 注入范式), packages/infinite-canvas/test/e2e/tests/phase36-orchestrator.mjs (orchestrate mock 断言 + getCalls), packages/infinite-canvas/test/e2e/tests/phase35-storyboard-metadata.mjs (detail-panel 定位 + 尺寸断言手法), packages/infinite-canvas/test/e2e/helpers.mjs (只复用不修改)</read_first>
  <action>
1. 新建 test/e2e/tests/phase52-stale-panel.mjs(node:test 同构)。
2. 用例 REGEN-03-a(stale 重跑全链):经 save-v2 注入含 `data.stale`(三字段)的 success 节点图(phase40 注入范式)→ load → 该节点 stale 角标可见 → 点击角标(或 dblclick 开面板点 StaleSection「重跑下游」)→ 断言 mock orchestrate 调用 body nodeIds 含该节点且 response total 计入(stale success 未跳过,mock 52-02 镜像)→ 等 mock 广播 node:state success → stale 角标消失(applySocketNodeState 清除,52-01)。
3. 用例 REGEN-03-b(stale 持久化,地雷 #2 e2e 防线):注入 stale 图 → reload → 角标仍在(stale 刷新不丢)。
4. 用例 REGEN-04-a(宽度):dblclick 节点 → detail-panel 可见且宽度 ≈480(≤520 容差)。
5. 用例 REGEN-04-b(跟随):面板开着 → 单击另一节点 → 面板仍可见 + 标题/内容跟随为新节点;✕ 关闭 → 单击节点面板不打开(回归);单击空白 → 面板关闭(回归)。
6. 文件头注释写明「先 npm run build」前置(地雷 #10);`npm run test:e2e` 全套回归绿(phase35/40 全用 dblclick 开 + ✕ 关,与单击行为变更零冲突——research 实证)。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npm run build 2>&1 | tail -2 && npx node --test test/e2e/tests/phase52-stale-panel.mjs 2>&1 | tail -8 && npm run test:e2e 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - phase52-stale-panel.mjs 四用例全绿(stale 重跑+消除;stale 持久化;宽度 480;单击跟随+关闭回归)
    - `npm run test:e2e` 全套回归绿
    - helpers.mjs 零 diff
  </acceptance_criteria>
  <done>REGEN-03/04 交付:stale 双出口重跑链端到端闭环(注入→重跑→不跳过→消除),面板 480+跟随有 e2e 锁死——ROADMAP Phase 52 成功标准 #3/#4。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 画布 SPA → POST /api/canvas/orchestrate + /api/canvas/v2/save-v2 | 内部 localhost 工具,复用既有端点,无认证面变化 |
| 角标点击 → RF 事件冒泡链 | UI 事件域;冒泡失控可致面板状态错乱 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-05-01 | Tampering | 角标点击冒泡连带切详情面板(状态错乱) | mitigate | stopPropagation 强制 + 注释 + e2e 跟随用例回归(地雷 #8) |
| T-52-05-02 | Tampering | 多选操作意外切面板打断批量流 | mitigate | ctrl/shift/⌘ 修饰键守卫(地雷 #9)+ 单测门 |
| T-52-05-03 | Tampering | stale success 被 orchestrate 跳过(重跑空转) | mitigate | 52-02 双侧不跳过 + e2e total 计入断言 |
| T-52-05-04 | Denial of Service | 重跑链重复触发(orchestration 并发) | mitigate | orchestration.status==='running' 守卫(handleOrchestrate 范式) |
| T-52-05-05 | Tampering | 无项目上下文重跑(孤儿请求) | mitigate | pid/eid 缺失 toast 早退(地雷 #13) |
| T-52-05-SC | Tampering | package installs | accept | 零新包,无 install 任务 |

**结论:内部工具 UI/编排接线,无新增外部攻击面;风险由守卫 + e2e 四用例覆盖。无 high 级威胁。**
</threat_model>

<verification>
- `cd packages/infinite-canvas && npx tsc -b` exit 0;`npm test` 全绿
- `npm run build` 后 `npx node --test test/e2e/tests/phase52-stale-panel.mjs` 四用例绿
- `npm run test:e2e` 全套回归绿(phase35/36/37/40/41 + 52 新三件套)
- grep 门:useStaleRerun 含 rerunStaleChain;NodeBadges 含 stopPropagation;NodeDetailPanel 含 480 + stale-rerun-btn;FlowCanvas 含修饰键守卫
</verification>

<success_criteria>
- REGEN-03 交付:上游变更后 stale 角标出现,双出口一键重跑复用 orchestrate 通道,重跑完成 stale 消除——ROADMAP Phase 52 成功标准 #3
- REGEN-04 交付:面板默认 ~480px,面板开着单击切换节点保持打开并跟随——ROADMAP Phase 52 成功标准 #4
- 地雷 #8/#9/#13 在本 plan 闭环;地雷 #2 e2e 防线(stale 刷新不丢)落地
</success_criteria>

<output>
Create `.planning/phases/52-prompt-edit-regenerate-loop/52-05-SUMMARY.md` when done. 记录:NodeBadges nodeId 来源实证、rerunStaleChain 空链早退的 UX 文案、onNodeClick 逃生口注释位置。
</output>
