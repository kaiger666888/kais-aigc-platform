---
phase: 52-prompt-edit-regenerate-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/infinite-canvas/src/v3/serialize.ts
  - packages/infinite-canvas/src/v3/__tests__/serialize.test.ts
  - packages/flowgraph-v3/ts/src/migrate.ts
  - packages/flowgraph-v3/ts/tests/migrate.test.ts
  - src/routes/canvas/orchestrate.ts
  - src/routes/canvas/execute.ts
  - packages/infinite-canvas/src/services/canvasApi.ts
  - packages/infinite-canvas/test/e2e/mock-backend/server.mjs
autonomous: true
requirements: [REGEN-01, REGEN-02, REGEN-03]
must_haves:
  truths:
    - "serializeGraphToV2 对每个资产节点经 role:'output' 边反查产生事件,把 event.params.prompt→data.prompt / seed→data.seed / modelVersion→data.engine 反向覆盖进 data 袋(在 {...raw, ...flattenMeta} 之后覆盖,canonical 事件配方为最终真值);script stage 跳过 prompt 覆盖;无产生事件或事件无该字段不伪造"
    - "asset.stale != null 时 data.stale 三字段(since/triggerAssetId/triggerEventId)落 wire;migrate.ts L552 stale:null 改为从 d.stale 还原——stale 刷新即丢的预存缺口连带修复"
    - "orchestrate 服务端 success/cached 且无 stale 标记才跳过;mock server.mjs skip 逻辑同构镜像;ORCHESTRATE-04 e2e(skipped===1,fixture success 节点无 stale)保持绿"
    - "mock logCall 对 execute/orchestrate 记完整 req.body(不再挑字段)——e2e 断言任务参数含新 prompt/seed 的观测点"
    - "canvasApi.executeNode 第 5 参数前插 extra?: { prompt?, seed?, params? },body 展开 {...extra},向后兼容(CanvasContextMenu handleExecute 零改动);execute.ts zod shape 补 params: z.record(z.string(), z.unknown()).optional()(契约诚实,validateFields 只校验不回写故无行为变化)"
  artifacts:
    - path: "packages/infinite-canvas/src/v3/serialize.ts"
      provides: "事件配方反向覆盖 + data.stale 序列化"
      contains: "data.stale"
    - path: "packages/flowgraph-v3/ts/src/migrate.ts"
      provides: "d.stale → asset.stale 还原"
    - path: "src/routes/canvas/orchestrate.ts"
      provides: "stale-success 不跳过谓词"
      contains: "stale"
    - path: "packages/infinite-canvas/test/e2e/mock-backend/server.mjs"
      provides: "skip 镜像 + logCall 完整 body"
    - path: "packages/infinite-canvas/src/services/canvasApi.ts"
      provides: "executeNode extra 参数通道"
  key_links:
    - from: "packages/infinite-canvas/src/v3/serialize.ts"
      to: "packages/flowgraph-v3/ts/src/migrate.ts"
      via: "data.prompt/seed/engine + data.stale 的 round-trip 互逆(§14 recipeParams 重建 + stale 还原)"
      pattern: "data.stale"
    - from: "src/routes/canvas/orchestrate.ts"
      to: "packages/infinite-canvas/test/e2e/mock-backend/server.mjs"
      via: "stale-success 不跳过谓词双侧同构(e2e 与生产语义不分叉)"
      pattern: "stale"
    - from: "packages/infinite-canvas/src/services/canvasApi.ts"
      to: "src/routes/canvas/execute.ts"
      via: "executeNode body 展开 extra → zod shape 接受 prompt/params/seed"
      pattern: "params"
---

<objective>
落地本 phase 两颗成败手地雷的通道层修复:**①事件配方反向覆盖**(serializeGraphToV2 丢弃 event 节点,migrate §14 只从资产 flat data.prompt/seed/engine 重建 params——不加反向覆盖,52-01 的 updateEventParams 编辑在保存+刷新后无声丢失);**②stale 上 wire**(migrate 硬编码 stale:null、序列化器不写 stale,orchestrate 服务端只读持久化 blob——没有 data.stale 通道,REGEN-03「stale success 不跳过」锁定决策无信息源,顺带修复 stale 刷新即丢的预存缺口)。

随刀完成契约层三件套:orchestrate/mock 双侧 stale-success 不跳过(镜像同构)、mock logCall 记完整 body(e2e 断言观测点)、executeNode extra 参数 + execute.ts zod params(52-03/52-04 的提交通道,W1 先行使两个 W2 plan 成为纯消费者、零文件交集)。

本 plan 无 UI,与 52-01(store 地基)文件零交集,W1 并行。

输出:serialize/migrate round-trip(配方+stale,含 vitest)、orchestrate+mock stale 语义、mock logCall 完整 body、execute 通道契约。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-prompt-edit-regenerate-loop/52-CONTEXT.md
@.planning/phases/52-prompt-edit-regenerate-loop/52-RESEARCH.md
@.planning/phases/51-canonical-write-path-coordination-guard/51-01-PLAN.md (serialize.ts 既有结构与本 plan 的逆变换映射先例)

**COORD-01 开工 checklist:本 plan 触碰零 kmc 侧文件(kais-hermes-skills),变更面检查 N/A。** 全部改动在 kais-aigc-platform 仓内。

<interfaces>
<!-- 提取自 52-RESEARCH.md Task 1 Round-trip / Task 3 / Task 5,执行者直接取用 -->

序列化器现状(packages/infinite-canvas/src/v3/serialize.ts,51-01 建):
- event 节点不落盘(`if (n.kind !== 'asset') continue`);role:'output' 边折叠丢弃
- data 袋重建公式:`data = { ...rawDataByNodeId?.get(id), ...flattenMeta(asset.meta), filePath, thumbnailUrl }`——反向覆盖必须在此公式**之后**执行,保证 canonical 事件配方是最终真值
- serializeGraphToV2 已拿完整 graph(含 event 节点与 links),改动自包含

reload 重建(packages/flowgraph-v3/ts/src/migrate.ts):
- §14 recipeParams(L153-160):params.prompt = d.prompt;params.seed = d.seed;params.modelVersion = d.engine
- script stage 例外(L473-479):prompt → content,不进 params(P4 防两处抄)——**反向覆盖时 script stage 跳过 prompt**
- import 种子事件(L466-472):params 仅 sourcePath,无 prompt/seed——不伪造
- L552 `stale: null` 硬编码 → 改 `d.stale ? {since, triggerAssetId, triggerEventId} : null`(轻校验三字段存在)

窄通道警告(地雷 #3):§14 采集只有 prompt/seed/engine 三字段,steps/cfg/lora/quant 等配方字段保存+刷新后本来就丢(预存损耗,非本期引入)。本 plan 只反向覆盖 prompt/seed/engine 三键;全配方持久化出范围,注释注明防 scope creep。

orchestrate 服务端(src/routes/canvas/orchestrate.ts):
- L56:`const targets = filtered.filter((n) => n.state !== "success" && n.state !== "cached")`
- 改法:success/cached 且无 stale 标记才跳过——谓词加 `|| (n.data != null && n.data.stale != null)`;**不加 force 参数**(CONTEXT 锁定),stale 即重跑语义;勿顺手重构 filter 语义(地雷 #7)
- 兼容实证:ORCHESTRATE-04 e2e(phase36 L25-33)断言 skipped===1,fixture success 节点(script-0)无 data.stale,改动后仍被跳过,用例保持绿

mock backend(packages/infinite-canvas/test/e2e/mock-backend/server.mjs):
- L260 区域 orchestrate skip 谓词镜像服务端同一改动
- L349 execute logCall 只记 {projectId, episodesId, nodeId, nodeType} → 改记完整 req.body;L268 orchestrate logCall 同样改记完整 body(地雷 #6:e2e 断言观测点)
- 服务端 zod `data: z.record(z.string(), z.any())` 全放行,data.stale 无 allowlist 风险

execute 通道(src/routes/canvas/execute.ts + packages/infinite-canvas/src/services/canvasApi.ts):
- execute.ts zod shape L13-24 已含 prompt?: string、branchId?: string;补 `params: z.record(z.string(), z.unknown()).optional()`(契约诚实——validateFields middleware.ts L8-24 safeParse 后不回写 req.body,extra key 原样穿透无行为变化,但防未来有人加 strip 回写踩雷;模拟器语义不变,prompt/params 归宿=接受并忽略)
- canvasApi.executeNode L370-378:现签名 (projectId, episodesId, nodeId, nodeType, cancelToken?),body 只发四字段;第 5 参数前插 `extra?: { prompt?: string; seed?: number; params?: Record<string, unknown> }`,body 展开 {projectId, episodesId, nodeId, nodeType, ...extra};唯一其他调用方 CanvasContextMenu handleExecute(L57)不传 extra,向后兼容零改动
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: serialize.ts 事件配方反向覆盖 + data.stale 落 wire + vitest round-trip</name>
  <files>packages/infinite-canvas/src/v3/serialize.ts, packages/infinite-canvas/src/v3/__tests__/serialize.test.ts</files>
  <read_first>packages/infinite-canvas/src/v3/serialize.ts (全文,event 跳过分支 + data 袋重建公式位置), packages/flowgraph-v3/ts/src/migrate.ts (§14 recipeParams L153-160 + script 例外 L473-479 + import 种子事件 L466-472,反向覆盖的互逆依据), packages/infinite-canvas/src/v3/__tests__/serialize.test.ts (既有 round-trip 用例风格), packages/flowgraph-v3/ts/src/types.ts (StaleInfo 三字段 / EventNodeV3.params)</read_first>
  <action>
1. serialize.ts 资产节点 data 袋重建之后追加"事件配方反向覆盖"段:经 graph.links 找 `role === 'output' && target === asset.id` 的 source 事件节点(kind==='event'),把其 params 三键覆盖进 data——prompt→data.prompt、seed→data.seed、modelVersion→data.engine;**script stage 跳过 prompt 覆盖**(其真值是 content,flattenMeta 已处理,P4);seed/modelVersion 覆盖对 script stage 同样无对应字段则不伪造;资产无产生事件、或事件 params 无该字段(undefined)时不写不伪造。注释写明:§14 窄通道只 round-trip prompt/seed/engine 三键,steps/cfg/lora/quant 等全配方持久化出范围(地雷 #3)。
2. serialize.ts 追加 stale 序列化:`asset.stale != null` → `data.stale = { since, triggerAssetId, triggerEventId }`(三字段照 StaleInfo 形状);stale 为 null 不写键。
3. serialize.test.ts 追加断言组:
   a. 配方反向覆盖:构造 event(params.prompt/seed/modelVersion)→output 边→资产,序列化后资产 data.prompt/seed/engine 等于事件配方(且覆盖 rawData/flattenMeta 同名键,canonical 优先);
   b. script stage 例外:script 资产的产生事件 params.prompt 不覆盖进 data(content 真值不动);
   c. 不伪造:无产生事件的资产 data 无 prompt/seed/engine 新增键;事件 params 缺 seed 时 data 无 seed 键;
   d. stale round-trip:带 stale 的资产序列化后 data.stale 三字段齐全;`adaptV2Graph(serializeGraphToV2(g, raw))` 往返后 asset.stale 保真(依赖 Task 2 migrate 还原,若同 wave 执行顺序未定,本断言允许在 Task 2 完成后一并跑绿——两 task 同 plan 同提交);
   e. 既有 7 断言组全绿无回归。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx vitest run src/v3/__tests__/serialize.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/infinite-canvas && npx vitest run src/v3/__tests__/serialize.test.ts` exit 0(新断言组 + 51-01 既有组无回归)
    - `grep -c 'data.stale' packages/infinite-canvas/src/v3/serialize.ts` ≥ 1
    - serialize.ts 含 `role === 'output'` 反查逻辑(grep 命中)
    - `cd packages/infinite-canvas && npx tsc -b` exit 0
  </acceptance_criteria>
  <done>事件配方反向覆盖 + stale 落 wire 落地,round-trip 单测锁死,prompt 编辑保存+刷新不丢(配合 Task 2)。</done>
</task>

<task type="auto">
  <name>Task 2: migrate.ts d.stale 还原 + vitest</name>
  <files>packages/flowgraph-v3/ts/src/migrate.ts (L552), packages/flowgraph-v3/ts/tests/migrate.test.ts</files>
  <read_first>packages/flowgraph-v3/ts/src/migrate.ts (L540-560 stale:null 上下文 + 资产节点构建段), packages/flowgraph-v3/ts/src/types.ts (StaleInfo 形状), packages/flowgraph-v3/ts/tests/migrate.test.ts (既有用例风格)</read_first>
  <action>
1. migrate.ts L552 `stale: null` 改为条件还原:`d.stale` 存在且含 since/triggerAssetId/triggerEventId 三字段时重建 StaleInfo,否则 null(轻校验,畸形数据降级 null 不 throw,与 migrate 宽容风格一致)。
2. migrate.test.ts 追加用例:V2 节点 data.stale 三字段 → adapt 后 asset.stale 还原相等;data.stale 缺失 → null;data.stale 畸形(缺字段)→ null 不 throw。
3. 与 Task 1 联跑:`adaptV2Graph(serializeGraphToV2(g, raw))` stale round-trip 保真(serialize.test.ts 断言组 d 在此闭环)。
  </action>
  <verify>
    <automated>cd packages/flowgraph-v3/ts && npx vitest run tests/migrate.test.ts 2>&1 | tail -5; cd ../../infinite-canvas && npx vitest run src/v3/__tests__/serialize.test.ts 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/flowgraph-v3/ts && npx vitest run tests/migrate.test.ts` exit 0(新用例 + 回归)
    - `grep -n 'stale' packages/flowgraph-v3/ts/src/migrate.ts` 命中 d.stale 还原分支(不再有裸 `stale: null` 硬编码行)
    - serialize round-trip 中 stale 保真断言绿
    - `cd packages/flowgraph-v3/ts && npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>stale wire round-trip 闭环,刷新即丢预存缺口修复,orchestrate 服务端改动有信息源。</done>
</task>

<task type="auto">
  <name>Task 3: orchestrate stale-success 不跳过 + mock 镜像 + logCall 完整 body</name>
  <files>src/routes/canvas/orchestrate.ts (L56), packages/infinite-canvas/test/e2e/mock-backend/server.mjs (orchestrate skip 谓词 + L268/L349 logCall)</files>
  <read_first>src/routes/canvas/orchestrate.ts (全文,skip 谓词 L56 + 响应形状), packages/infinite-canvas/test/e2e/mock-backend/server.mjs (orchestrate handler L250-280 区域 + logCall L148-150 + execute logCall L349), packages/infinite-canvas/test/e2e/tests/phase36-orchestrator.mjs (ORCHESTRATE-04 skipped===1 断言,兼容性基准)</read_first>
  <action>
1. orchestrate.ts L56 skip 谓词改为:`(n.state !== "success" && n.state !== "cached") || (n.data != null && n.data.stale != null)`——success/cached 且有 stale 标记的节点进入 targets;不加 force 参数(CONTEXT 锁定);注释注明"stale 即需重跑语义,52-02 起 data.stale 随 wire 持久化"。不重构 filter 其余语义(地雷 #7)。
2. mock server.mjs orchestrate handler 的 skip 谓词做**同构镜像**(同一条件表达式语义),保证 e2e 与生产语义不分叉。
3. mock logCall 改记完整 body:execute(L349)与 orchestrate(L268)两处 logCall 的 body 参数从挑字段对象改为透传完整 req.body(load/save-v2/convert 等其他 logCall 行不动,最小改动面)。
4. 验证兼容:`cd packages/infinite-canvas && npm run build && npm run test:e2e`(e2e 跑 dist 非源码,地雷 #10——复跑前必须 build)中 phase36 ORCHESTRATE-04 skipped===1 保持绿(fixture success 节点无 data.stale)。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npm run build 2>&1 | tail -2 && npx node --test test/e2e/tests/phase36-orchestrator.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'data.stale' src/routes/canvas/orchestrate.ts` 命中 ≥ 1
    - `grep -n 'data.stale' packages/infinite-canvas/test/e2e/mock-backend/server.mjs` 命中 ≥ 1(镜像)
    - mock server.mjs execute/orchestrate logCall 行含完整 req.body 透传(grep 确认不再只列四字段)
    - `npx tsc --noEmit`(根)exit 0
    - build 后 phase36-orchestrator e2e 全绿(ORCHESTRATE-04 skipped===1 回归通过)
  </acceptance_criteria>
  <done>stale-success 双侧不跳过,e2e 断言观测点(完整 body)就绪,phase36 兼容性实证绿。</done>
</task>

<task type="auto">
  <name>Task 4: execute 通道契约——executeNode extra 参数 + execute.ts zod params</name>
  <files>packages/infinite-canvas/src/services/canvasApi.ts (executeNode L370-378), src/routes/canvas/execute.ts (zod shape L13-24)</files>
  <read_first>packages/infinite-canvas/src/services/canvasApi.ts (executeNode 全文 + 文件内其他函数 extra/options 参数先例), src/routes/canvas/execute.ts (全文,zod shape + handler + queued stub 分支), src/middleware.ts (validateFields L8-24 只校验不回写实证), packages/infinite-canvas/src/components/CanvasContextMenu.tsx (handleExecute L57 调用点,确认向后兼容)</read_first>
  <action>
1. canvasApi.executeNode 第 5 参数前插 `extra?: { prompt?: string; seed?: number; params?: Record<string, unknown> }`(原 cancelToken 顺延;若现有第 5 参数即 cancelToken,则 extra 插在其前并保持 cancelToken 可选);body 构造展开 `{ projectId, episodesId, nodeId, nodeType, ...extra }`。CanvasContextMenu handleExecute 不传 extra,类型可选故零改动(读完后确认编译即可,**不改 CanvasContextMenu.tsx**——避免与 wave 内/后续 plan 争用)。
2. execute.ts zod shape 补 `params: z.record(z.string(), z.unknown()).optional()`;注释注明"validateFields 只校验不回写,extra key 本就穿透;此字段为契约诚实 + 防未来 strip 回写踩雷"。**模拟器语义不变**:handler 不把 prompt/params 传给 simulateExecution(归宿=接受并忽略,断言只锁参数到达契约层,CONTEXT 锁定真实引擎派发维持现状)。
3. 编译门:双根 tsc clean。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -3; cd packages/infinite-canvas && npx tsc -b 2>&1 | tail -3; grep -n 'params' src/routes/canvas/execute.ts | head -3; grep -n 'extra' packages/infinite-canvas/src/services/canvasApi.ts | head -3</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/canvas/execute.ts zod shape 含 `params` 字段(grep 命中)且仍含既有 `prompt`
    - canvasApi.ts executeNode body 展开含 `...extra`(grep 命中)
    - CanvasContextMenu.tsx 零 diff(本 task 不修改该文件)
    - 双根 tsc exit 0;`cd packages/infinite-canvas && npm test` 全绿
  </acceptance_criteria>
  <done>execute 提交通道契约就绪,52-03/52-04 可直接以 extra 传新 prompt/新 seed,零服务端行为变化。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical graph → wire 序列化 | 数据变换错误可致配方/stale 字段丢失(本 phase 成败手) |
| 前端 → POST /api/canvas/orchestrate + /api/canvas/execute | 内部 localhost 工具,无认证面变化;zod 为契约层 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-02-01 | Tampering | 事件配方保存+刷新无声丢失 | mitigate | 反向覆盖 + round-trip vitest(地雷 #1);script stage 例外单测 |
| T-52-02-02 | Tampering | stale 刷新即丢 / 服务端不可见 | mitigate | data.stale 序列化 + migrate 还原 + round-trip 单测(地雷 #2) |
| T-52-02-03 | Tampering | 反向覆盖伪造不存在的配方字段 | mitigate | 无事件/无字段不写键 + 单测 |
| T-52-02-04 | Tampering | e2e 与生产 orchestrate 语义分叉 | mitigate | mock skip 谓词同构镜像 + phase36 回归 e2e |
| T-52-02-05 | Information Disclosure | logCall 记完整 body 泄露敏感字段 | accept | 仅 e2e mock 进程内内存态,不持久化不出仓;execute/orchestrate body 无密钥类字段 |
| T-52-02-SC | Tampering | package installs | accept | 零新包,无 install 任务 |

**结论:通道层数据完整性风险全部由 round-trip 单测 + e2e 回归覆盖;无新增外部攻击面,无 high 级威胁。**
</threat_model>

<verification>
- `cd packages/infinite-canvas && npm test` 全绿(serialize 新断言组 + 回归)
- `cd packages/flowgraph-v3/ts && npm test` 全绿(migrate 新用例 + 回归)
- `npx tsc --noEmit`(根)+ `cd packages/infinite-canvas && npx tsc -b` + `cd packages/flowgraph-v3/ts && npx tsc --noEmit` exit 0
- `npm run build` 后 phase36-orchestrator e2e 全绿(ORCHESTRATE-04 skipped===1 回归)
- grep 门:serialize.ts 含 `data.stale` 与 `role === 'output'` 反查;orchestrate.ts 与 mock 各含 `data.stale`;execute.ts zod 含 `params`;canvasApi 含 `...extra`
</verification>

<success_criteria>
- 地雷 #1(事件配方不落盘)与 #2(stale 不上 wire)双闭环,round-trip 自动化锁死
- 地雷 #3(窄通道)范围声明入注释;地雷 #6(mock logCall)修复;地雷 #7(validateFields 契约诚实)修复
- REGEN-03 服务端语义(stale success 不跳过)双侧落地且 phase36 兼容
- 52-03/52-04 的提交通道(executeNode extra + zod params)W1 先行就绪
</success_criteria>

<output>
Create `.planning/phases/52-prompt-edit-regenerate-loop/52-02-SUMMARY.md` when done. 记录:反向覆盖三键范围(§14 窄通道声明)、mock logCall 完整 body 的 body 形状变化对既有 e2e 断言的影响核查结果。
</output>
