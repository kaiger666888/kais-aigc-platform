---
phase: 52-prompt-edit-regenerate-loop
plan: 07
type: execute
wave: 3
depends_on:
  - "03"
files_modified:
  - src/lib/canvasAssetSchema.ts
  - packages/infinite-canvas/src/v3/serialize.ts
  - scripts/canvas/verify-save-v2-legacy-asset.ts
  - package.json
  - packages/infinite-canvas/test/e2e/probe-52-real.mjs
autonomous: true
gap_closure: true
requirements: [REGEN-01]
must_haves:
  truths:
    - "真实 sync 存量图可以整体保存:项目 1/2/2001/9999 的 load-v2 原图原样回发 save-v2 全部 200(UAT 实测修前全 400)——画布顶栏保存与 52 的 prompt 编辑保存在真实项目上不再被结构化参数门拦死"
    - "宽容只落在证据指向的字段:asset 型 filePath/label/assetType nullish 化(诊断回放若证明其他类型存量形态也失败,按同款注释模式扩展);audio/video 的 universalRequired(filePath/shot_id/engine/duration_sec/resolution)一字不动——管线媒体节点「结构化参数必填」契约在真正承载它的类型上原样强制"
    - "离线行为锁:verify-save-v2-legacy-asset 断言「无 filePath/label/assetType 的 asset 摘要卡形态 → null(放行)」与「缺 filePath 的 audio → 错误串(拦截)」双分支,npm run verify:save-v2-legacy exit 0"
    - "REGEN-01 真机闭环(52-UAT Test 3 missing 项):项目 9999 深链开面板 → 编辑 prompt → 保存 200 → reload 往返仍显新 prompt → 点重生成 → 真后端提交可见反馈(toast + 节点 running → success 回贴);探针全程捕获-恢复原图,零净足迹"
    - "serialize.ts L261-263 的「filePath 必填由管线数据保证」注释按新事实改写(仅注释零行为变更)——假契约文档不再误导后来者"
  artifacts:
    - path: "src/lib/canvasAssetSchema.ts"
      provides: "asset 型存量宽容(filePath/label/assetType nullish)+ 契约保留注释"
      contains: "nullish"
    - path: "scripts/canvas/verify-save-v2-legacy-asset.ts"
      provides: "save-v2 存量宽容行为锁(离线断言 + save-v2 source 门)"
      contains: "verify-save-v2-legacy"
    - path: "packages/infinite-canvas/test/e2e/probe-52-real.mjs"
      provides: "4 项目回放门 + REGEN-01 真机闭环探针(含原图恢复)"
    - path: "package.json"
      provides: "verify:save-v2-legacy npm script(单行)"
  key_links:
    - from: "src/routes/canvas/v2/save-v2.ts"
      to: "src/lib/canvasAssetSchema.ts"
      via: "validateGraphNodes(validGraph.nodes) —— 门不拆,schema 判定核心放宽(与 nodes.ts POST/PATCH 的 validateNodeData 共享同一判定,存量节点 PATCH 不再 400)"
      pattern: "validateGraphNodes"
    - from: "packages/infinite-canvas/test/e2e/probe-52-real.mjs"
      to: "http://localhost:10588 /api/canvas/v2/load-v2 + save-v2 + /infinite-canvas 深链"
      via: "load 原图 → 回发 save 断言 200;深链 focus 开面板走 REGEN-01 全链"
      pattern: "save-v2"
---

<objective>
修复 52-UAT **blocker(Test 2 / gap #1)**:canvasAssetSchema 的 universalRequired 把 filePath 设为一切 asset 型节点必填,而 kmc sync 直写 DB 的真实图普遍含无媒体 asset 节点(sum-* 摘要卡、文本资产;label/assetType 同理)——save-v2 对全部真实项目整图 400,画布保存路径整体不可用,REGEN-01 真机闭环被挡断(gap #2 同 plan 收口)。

**修复方向裁定(三选一中的 schema 侧存量宽容):** 与该文件自身先例同构——prompt/description 当年正是因「rejecting on full-graph save would break the UI for 689 pre-existing asset rows」而可选化;本次 asset 型 filePath/label/assetType 按同一先例 nullish 化。**不选** save-v2 宽容校验模式(需区分新旧节点,saveFullGraph 全量替换语义下要逐行 DB 比对,复杂度错配);**不选** sync 侧补齐(kmc 仓跨域 + 689+ 存量行仍需回填,救不了当下)。管线媒体节点的强制契约由 audio/video 类型继续承载(管线写入本来直写 DB 绕过 HTTP,此门真正拦的只有 UI 回写,宽松化不弱化任何管线强制)。

**Wave 3 / 文件边界(与 52-05 并行零交集;wave 由 2 上调为 3 以满足 wave=max(deps)+1——03 已预执行故原标 2,修订后调度器重算不再漂移):** 本 plan 只碰根侧 src/lib + scripts + package.json + serialize.ts 注释一行 + probe 脚本;eventChipBus/FlowCanvas/EventParamsPopover/e2e 新文件归 52-04(wave 2),useStaleRerun/NodeDetailPanel/NodeBadges/FlowCanvas onNodeClick 归 52-05(同 wave 3,文件零交集可并行;52-05 的 rerunStaleChain 运行时依赖能用的 save——其自动化门全为 mock e2e 不受影响,真机组合行为由 wave 5 的 52-06 聚合门与本 plan Part A 回放共同覆盖)。

**52-06 衔接(已直接修订 52-06-PLAN,checker blocker#1):** 52-06 frontmatter 已上调 wave 5 + depends_on 增 07/08,S5 已含 `npm run verify:save-v2-legacy`、S1 已含 nullish/syntheticDetailNode grep 锚——执行器照 52-06-PLAN 现文执行即可,无须再现场补改。

输出:canvasAssetSchema 宽容化 + 行为锁 verify 脚本 + 4 项目回放 200 + REGEN-01 真机闭环实证。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-prompt-edit-regenerate-loop/52-CONTEXT.md
@.planning/phases/52-prompt-edit-regenerate-loop/52-UAT.md

**COORD-01 开工 checklist:本 plan 触碰零 kmc 侧文件(kais-hermes-skills),变更面检查 N/A。**

<interfaces>
<!-- 执行者直接取用;均已实证于 master @ a42967bd -->

canvasAssetSchema.ts 现状(src/lib):
- L23-24 `universalRequired = { filePath: z.string().min(1, "filePath is required for media nodes") }`,被 audio(L53-54)/video(L64-65)/asset(L77)三处展开
- asset 段 L76-94:`...universalRequired` + `label: z.string().min(1)` + `assetType: z.string().min(1)` + prompt/description 可选(注释先例:"Kept optional because legacy data lacks both, and rejecting on full-graph save would break the UI for 689 pre-existing asset rows")
- storyboard 段 filePath 本就 optional(L103);script 段必填 label+description(L110-118)
- `withYamlOptional` 只 extend 可选额外字段,与既有键零冲突
- `EXPECTED_PARAM_FIELDS_BY_TYPE`(L194-204)**零改动**——verify-schema-drift 门 diffs 它 vs Python MANIFEST_PARAM_SCHEMA,动则 drift 假红

save-v2.ts 门(L46-58):`validateGraphNodes(validGraph.nodes)` 非空 → 400 detail 逐节点列出——诊断回放直接消费该 detail 格式,本 plan **不拆此门**;nodes.ts POST(L67)/batch(L123)/PATCH merged(L201)共享 validateNodeData 判定,asset 存量行 PATCH 同步受益,无需单改

serialize.ts L261-263 注释(仅注释改写,行为不动):现文"服务端结构化参数 schema 的 filePath 必填由管线数据保证,序列化器不兜底"——UAT 证伪(sync 写入路径无此保证)

API / 部署链(实证):
- `POST /api/canvas/v2/load-v2` body `{projectId, episodesId, since?}` → `{graph}`(持久化 wire 形状);`POST /api/canvas/v2/save-v2` body `{projectId, episodesId, graph}`;`GET /api/canvas/v2/health` 探活
- 部署:`npm run build:server`(esbuild → data/serve/app.js)→ `sudo systemctl restart kais-aigc-platform` → 服务 PORT=10588;画布页 `/infinite-canvas/?projectId=9999&episodesId=1&testMode=1&focus=<nodeId>`(UAT 探针实证过的深链形态)
- probe 基底:packages/infinite-canvas/test/e2e/probe-52-real.mjs(未跟踪,BASE='http://localhost:10588',项目 9999 ep1,UAT 会话实证 focus=a-p04-art4 有 902 字 prompt 可编辑);诊断回放参照 UAT 手法"load-v2 原图原样回发 save-v2"
- 项目→episodesId 枚举:projects API(src/routes/canvas/projects.ts)或直接 DB;每项目取第一个 episodesId 即可,SUMMARY 记录映射

守门脚本影响面(实证核对):
- scripts/canvas/verify-save-gates.ts 已 DEPRECATED(Phase 51 头注释,运行即 throw)——其 audio 行为断言仍是本修复的基线参照,但不作为门
- scripts/verify-canvas-shot-timeline.ts E2「additive-only 计数」(.optional()/.nullable() head ≤ master):本修复有意为 asset 型 +3 nullish 突破该一次性 Phase 46 脚本计数;**该脚本未挂任何 npm verify 门**(package.json 无注册),突破记入 SUMMARY 即可;audio/video 计数不变
- scripts/canvas/verify-schema-drift.ts(挂 verify:schema-drift):只解析 EXPECTED_PARAM_FIELDS_BY_TYPE,零改动则不受影响
- scripts/canvas/verify-schema-roundtrip.ts(挂 verify:phase-44):SCHEMA-01 查字段"已声明"——可选化仍是声明,不受影响
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 诊断回放(修前证据枚举)→ canvasAssetSchema asset 存量宽容化 + serialize.ts 注释纠正</name>
  <files>src/lib/canvasAssetSchema.ts, packages/infinite-canvas/src/v3/serialize.ts (仅 L261-263 注释)</files>
  <read_first>src/lib/canvasAssetSchema.ts (全文 205 行:universalRequired L23-24、asset 段 L76-94、prompt/description 先例注释 L83-87、EXPECTED_PARAM_FIELDS L194-204), src/routes/canvas/v2/save-v2.ts (全文 82 行,确认门结构不拆), packages/infinite-canvas/src/v3/serialize.ts (L255-270 filePath 覆盖区), packages/infinite-canvas/test/e2e/probe-52-real.mjs (BASE/项目参数/请求手法)</read_first>
  <action>
  1. **修前诊断回放**(改代码之前跑,证据定宽容面):对真后端 10588 的项目 1/2/2001/9999(每项目取第一 episodesId,projects API 或 DB 枚举,映射记 SUMMARY)逐个 `POST /api/canvas/v2/load-v2` → 把返回 `graph` 原样 `POST /api/canvas/v2/save-v2` → 收集 400 response 的 detail 数组,按 `{node type × 缺失字段}` 分类汇总(UAT 预判:asset 型 filePath 缺失 80+ 节点 + label/assetType 缺失;以实测为准,产物是临时 JSON/表格,只进 SUMMARY 不入库)。
  2. **schema 改造**:asset 段摘除 `...universalRequired`,`filePath/label/assetType` 三字段改 `z.string().min(1).nullish()`(诊断证据若出现空串形态则降级 `z.string().nullish()`,SUMMARY 记录依据);字段上方注释块写明:52-UAT gap#1(2026-08-22)实测项目 1/2/2001/9999 load-v2 原图回发全 400、与 prompt/description 可选化同一先例、**audio/video 的 universalRequired 原样保留 = 管线媒体节点契约承载者**。audio/video/storyboard/script 段零改动,除非诊断回放证明其他类型存量形态也触发 400——若是,按同款注释模式对证据指向字段做 nullish 化(SUMMARY 逐条记录)。
  3. **serialize.ts L261-263 注释改写**(仅注释):新表述 = filePath 仅非空时覆盖、空媒体不伪造字段不变;服务端 schema 对 **asset 型** filePath 已 nullish 宽容(52 gap#1 存量先例),**audio/video 仍必填**由管线数据保证,序列化器依旧不兜底。行为零变更。
  4. `EXPECTED_PARAM_FIELDS_BY_TYPE` 零改动(import-from-dir warn 语义与 verify-schema-drift 门均不受影响)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc --noEmit 2>&1 | tail -3 && grep -c 'nullish' src/lib/canvasAssetSchema.ts && grep -c 'universalRequired' src/lib/canvasAssetSchema.ts && grep -n '管线数据保证' packages/infinite-canvas/src/v3/serialize.ts | head -2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'nullish' src/lib/canvasAssetSchema.ts` ≥ 3( asset 三字段);`grep -c 'universalRequired'` = 3(1 定义 + audio/video 两处展开,asset 段不再展开)
    - asset 段注释含 UAT gap#1 与 audio/video 契约保留说明;audio/video 段 diff 为零
    - serialize.ts 该区 diff 仅注释行;根 `npx tsc --noEmit` exit 0
    - 修前诊断回放结果(4 项目 400 detail 分类)已记入本 plan 工作笔记(SUMMARY 引用)
  </acceptance_criteria>
  <done>宽容面由实测证据锁定并落地:asset 存量形态放行,audio/video 严格性原样,假契约注释纠正。</done>
</task>

<task type="auto">
  <name>Task 2: verify-save-v2-legacy-asset 行为锁 + npm script 注册</name>
  <files>scripts/canvas/verify-save-v2-legacy-asset.ts (new), package.json (单行)</files>
  <read_first>scripts/canvas/verify-harness 邻近范式:scripts/canvas/verify-schema-roundtrip.ts (source grep + 行为断言混排、createHarness 用法), scripts/canvas/verify-save-gates.ts (DEPRECATED 头注释 + validateNodeData 行为断言写法 L92-119——本脚本的 audio 基线断言即参照它), package.json (scripts 区 verify:phase-* 邻近行)</read_first>
  <action>
  1. 新建 scripts/canvas/verify-save-v2-legacy-asset.ts(tsx 直跑,shebang + 头注释写明 52-UAT gap#1 根因与本锁目的):用 `createHarness`(scripts/canvas/lib/verify-harness)分两节——
     **行为节**(import validateNodeData/validateGraphNodes,离线纯函数):a) asset 无 filePath/label/assetType 的摘要卡形态(Task 1 诊断实测形态,含字段缺失与 null 两变体)→ null 放行;b) asset 完整形态(filePath/label/assetType 齐)→ null;c) audio 缺 filePath(`{label:'x'}`)→ 错误串且含 "filePath";d) video 缺 resolution → 错误串;e) validateGraphNodes 对 [zone, 无媒体 asset] 混合图 → 0 errors。
     **source 节**(防门被拆):save-v2.ts 仍含 `validateGraphNodes(validGraph.nodes`;nodes.ts 仍含 `validateNodeData(merged.type`(PATCH merged 门未回退);canvasAssetSchema.ts 的 audio/video 段仍含 `...universalRequired`。
  2. package.json scripts 注册 `"verify:save-v2-legacy": "npx tsx scripts/canvas/verify-save-v2-legacy-asset.ts"`——**编辑前重读文件**(本仓有并行会话提交 master,package.json 常被并发修改),单行插入 verify:phase-* 邻近区,不重排。
  3. 跑通并修红:`npm run verify:save-v2-legacy` exit 0。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npm run verify:save-v2-legacy 2>&1 | tail -8 && grep -c 'verify:save-v2-legacy' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:save-v2-legacy` exit 0(行为节 5 断言 + source 节 3 断言全绿)
    - `grep -c 'verify:save-v2-legacy' package.json` = 1;package.json diff 仅一行新增
    - forced-fail 自检:临时注释掉任一行为断言能红(验证门可红,执行后恢复)
  </acceptance_criteria>
  <done>存量宽容与媒体节点严格性双分支被离线回归锁死,后续 schema 改动有单入口门。</done>
</task>

<task type="auto">
  <name>Task 3: 部署 + 4 项目回放门(200)+ REGEN-01 真机闭环探针(gap #2 收口)</name>
  <files>packages/infinite-canvas/test/e2e/probe-52-real.mjs (扩展并 commit)</files>
  <read_first>packages/infinite-canvas/test/e2e/probe-52-real.mjs (全文:BASE/深链 QS/开面板/编辑-保存-回滚现成手法,UAT 会话零足迹纪律), src/routes/canvas/v2/load-v2.ts (请求形状), packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx (PromptSection 保存/重生成按钮 data-testid:prompt-textarea/prompt-save/prompt-regenerate,52-03 已落), src/routes/canvas/execute.ts + _simulate.ts (episodesId 在场 → simulateExecution 走完 running→success socket 链)</read_first>
  <action>
  1. **部署**:`npm run build:server` → `sudo systemctl restart kais-aigc-platform` → 轮询 `GET /api/canvas/v2/health`(10588)至就绪。
  2. **扩展 probe-52-real.mjs 为两段式**(node 直跑,断言失败即非零 exit):
     **Part A 回放门**:Task 1 的同一组请求重放——4 项目 load-v2 原图原样回发 save-v2,逐一断言 HTTP 200(修前同请求全 400,这是 gap#1 的关闭证据)。**每笔 200 后立即验证写入等价**:重新 `POST load-v2` 捕获回读图,与回发前捕获的原图深度比对(剔除 `meta.updatedAt`;nodes/links/groups/variantGroups 递归全等)——修复后的保存必须是 verified no-op,任何字段漂移即探针非零 exit(本仓有 canvas 同步字段保真前科,生产项目 1/2/2001 不容隐性变更);比对结果(4 项目逐项 pass)记入 SUMMARY。
     **Part B REGEN-01 真机闭环**(项目 9999 ep1,节点 a-p04-art4 或任一"有产生事件 + 有 prompt"的媒体资产,深链 focus 开面板):先 `POST load-v2` 捕获原图存内存 → 面板 textarea 改 prompt(原值追加可识别后缀,如 ` [52-07-verify]`)→ 点保存 → 断言无错误 toast + textarea 已显新值 → `page.reload` 往返 → 重开面板断言仍显新 prompt(52-UAT Test 2 truth)→ 点重生成 → 断言「已提交重生成」toast + 节点 state 反馈进入 running 再到 success(node:state socket 链,卡片刷新/结果回贴;52-UAT Test 3 missing 项)→ **收尾恢复基线**:把捕获的原图 `POST save-v2` 回存 → reload 验证 prompt 回到原值(零净足迹,延续 UAT 会话纪律)。
  3. `git add packages/infinite-canvas/test/e2e/probe-52-real.mjs` 随本 plan commit(该文件 UAT 标记为 owed,收编后可复验)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npm run build:server 2>&1 | tail -2 && sudo systemctl restart kais-aigc-platform && sleep 5 && curl -s http://localhost:10588/api/canvas/v2/health | head -c 200 && cd packages/infinite-canvas && node test/e2e/probe-52-real.mjs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Part A:4 项目回放全部 200(与修前 400 对照记入 SUMMARY)+ 每笔回读深度比对原图全等(剔除 meta.updatedAt;verified no-op)
    - Part B:保存 200 + reload 往返新 prompt + 重生成 running→success 反馈全部断言过;恢复基线后 reload 验证回原值(净足迹=0)
    - probe 脚本 exit 0;脚本已入 git(随 plan commit)
    - 服务重启后 10588 health 就绪且其余路由无回归(health + 任一 load-v2 200)
  </acceptance_criteria>
  <done>gap#1 关闭(真实项目整图可保存)+ gap#2 收口(REGEN-01 真机 编辑→保存→重生成→回贴 全链实证,零净足迹)。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 画布 SPA / kmc sync → save-v2 schema 门 | 内部 localhost 服务;宽容化改变的是「什么形状能过门」,非谁能进门 |
| probe → 真后端 10588(生产 DB) | 真机验证写生产数据;唯一越界写点,须捕获-恢复 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-07-01 | Tampering | 宽容化越界(媒体节点必填契约被无声稀释) | mitigate | audio/video 段零改动硬约束 + verify-save-v2-legacy 行为锁(audio 缺 filePath 必拒断言) |
| T-52-07-02 | Tampering | 诊断证据外字段被顺手放宽 | mitigate | 宽容面=回放 400 detail 实测分类驱动,逐字段记 SUMMARY;Task 2 source 节锁 universalRequired 展开数 |
| T-52-07-03 | Tampering | 真机探针污染生产数据 | mitigate | Part A 每笔 200 后回读深度比对原图(verified no-op,漂移即红);Part B 捕获原图 → 结束回存恢复 + reload 复核;项目限定 9999(UAT 既有测试项目)+ Part A 三生产项目只做等价回写 |
| T-52-07-04 | Denial of Service | 服务重启窗口 | accept | 内部单用户工具;health 轮询就绪后再跑探针 |
| T-52-07-SC | Tampering | package installs | accept | 零新包,无 install 任务 |

**结论:内部工具校验层调整,无新增外部攻击面;核心风险(契约稀释/数据足迹)由行为锁 + 捕获恢复覆盖。无 high 级威胁。**
</threat_model>

<verification>
- 根 `npx tsc --noEmit` exit 0;`npm run verify:save-v2-legacy` exit 0
- `verify:schema-drift` 与 `verify:phase-44` 复跑绿(EXPECTED_PARAM_FIELDS 零改动的反证)
- 部署后 probe 两段式全绿:4×回放 200(各含回读等价比对)+ REGEN-01 真机闭环 + 零净足迹恢复
- grep 门:asset 段 nullish ≥ 3;universalRequired 展开恰 2 处(audio/video);save-v2/nodes.ts 门调用仍在
</verification>

<success_criteria>
- 52-UAT Test 2 blocker 关闭:全部真实项目画布保存可用(save-v2 不再对存量图 400)
- 52-UAT Test 3 missing 项关闭:REGEN-01 真机闭环(新 prompt 落盘 + 重生成 running/success 回贴)实证
- audio/video「结构化参数必填」契约原样强制,有离线回归锁
</success_criteria>

<output>
Create `.planning/phases/52-prompt-edit-regenerate-loop/52-07-SUMMARY.md` when done. 记录:修前诊断回放的 400 detail 分类(宽容面依据)、项目→episodesId 映射、nullish 是否降级及证据、verify-canvas-shot-timeline additive-only 计数突破说明(一次性脚本未挂门)、Part A 四项目回读等价比对结果(verified no-op 证据)、Part B 足迹恢复证据。
</output>
