---
phase: 52-prompt-edit-regenerate-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/infinite-canvas/src/store/canvasStore.ts
  - packages/infinite-canvas/src/store/__tests__/canonicalWriteback.test.ts
  - packages/flowgraph-v3/ts/src/stale.ts
  - packages/flowgraph-v3/ts/tests/stale.test.ts
autonomous: true
requirements: [REGEN-01, REGEN-03]
must_haves:
  truths:
    - "updateEventParams 是 store canonical action:守卫早退(graph null/节点不存在/kind!=='event' → console.warn,不 throw),经 applyGraphTransform 纯函数映射写入 EventNodeV3.params"
    - "空值语义与 updateAssetMeta 对齐:undefined/null/'' = 删字段"
    - "persistEventParams(异步)= 乐观写 → save-v2 持久化 → 失败回滚 prevParams + error toast(deleteNode 外科式回滚范式),写入方统一在 store,组件不拼 serialize+api"
    - "applySocketNodeState 收到 running/success 时自动清除该资产节点 stale;error/failed 保留 stale;kind==='asset' 守卫使 evt_ 节点 state 回写不受影响"
    - "getDownstreamIds(graph, nodeId) 从 flowgraph-v3 stale.ts 导出:沿因果边 BFS 返回下游资产 id 集;sequence/isInactive 边排除、locked 终止、环防御;与 markStaleDownstream 共用 buildCausalIndex 零逻辑复制"
  artifacts:
    - path: "packages/infinite-canvas/src/store/canvasStore.ts"
      provides: "updateEventParams + persistEventParams canonical actions;applySocketNodeState stale 清除"
      contains: "updateEventParams"
    - path: "packages/flowgraph-v3/ts/src/stale.ts"
      provides: "buildCausalIndex 私有提取 + getDownstreamIds 导出"
      contains: "getDownstreamIds"
    - path: "packages/infinite-canvas/src/store/__tests__/canonicalWriteback.test.ts"
      provides: "updateEventParams 单测组 + applySocketNodeState stale 清除单测组"
    - path: "packages/flowgraph-v3/ts/tests/stale.test.ts"
      provides: "getDownstreamIds 五类用例(sequence 排除/inactive 排除/locked 终止/环防御/事件起点)"
  key_links:
    - from: "packages/infinite-canvas/src/store/canvasStore.ts"
      to: "packages/infinite-canvas/src/v3/serialize.ts + services/canvasApi.ts"
      via: "persistEventParams 内 saveCanvasGraph(pid, eid, serializeGraphToV2(cur, rawDataByNodeId))"
      pattern: "saveCanvasGraph"
    - from: "packages/flowgraph-v3/ts/src/index.ts"
      to: "packages/flowgraph-v3/ts/src/stale.ts"
      via: "export * from './stale.js' 已存在,getDownstreamIds 导出自动生效"
      pattern: "getDownstreamIds"
---

<objective>
建 REGEN-01/03 的 store 与算法地基:新增 `updateEventParams`(事件配方 canonical 写入,prompt 真值唯一合法存放处 = EventNodeV3.params,P4)与配套带持久化+回滚的异步 action;给 `applySocketNodeState` 补 stale 自动清除(running/success 清、failed 留);从 flowgraph-v3 `stale.ts` 提取 `buildCausalIndex` 并导出 `getDownstreamIds`(REGEN-03 重跑链的下游计算引擎)。

本 plan 纯逻辑无 UI,与 52-02(wire/契约层)文件零交集,W1 并行。

输出:canvasStore 两 action + stale 清除(含 vitest)、stale.ts getDownstreamIds(含 vitest)。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-prompt-edit-regenerate-loop/52-CONTEXT.md
@.planning/phases/52-prompt-edit-regenerate-loop/52-RESEARCH.md

**COORD-01 开工 checklist:本 plan 触碰零 kmc 侧文件(kais-hermes-skills),变更面检查 N/A。** 全部改动在 kais-aigc-platform 仓内。

<interfaces>
<!-- 提取自 52-RESEARCH.md Task 1 / Task 5,执行者直接取用 -->

store 范式(packages/infinite-canvas/src/store/canvasStore.ts):
- updateAssetMeta L573-601:早退守卫(graph===null → console.warn 静默早退不 throw;节点不存在同理)→ applyGraphTransform((g) => ({...g, nodes: g.nodes.map(...)})) 纯函数映射;空值(undefined/null/'')= 删字段
- applySocketNodeState L603-635:同守卫范式;L630-633 transform map 内按 nodeId 匹配更新 state/progress
- deleteNode L739-792:带持久化 action 范式——乐观写 → await saveCanvasGraph(pid, eid, serializeGraphToV2(cur, rawDataByNodeId)) → 失败外科式回滚 + showToast error;L742-745「缺少项目上下文」(projectId/episodesId 空)守卫早退
- applyGraphTransform L315:setGraph 重建派生缓存,memo 按 graph 引用失效(边中点 op 芯片 params 快照随之自动刷新,无需额外失效处理)
- 接口声明区 L136-147:新 action 签名须加进 store interface

事件 params 契约(packages/flowgraph-v3/ts/src/types.ts L181-192):
- GenerationParams 有 `[key: string]: unknown` catchall;zod generationParamsSchema.catchall(z.unknown())(zod.ts L115)——全图唯一非 strict 点,**不需要 allowlist**,任意 key 合法;已知 key(prompt string / seed number)类型不符时可 warn 轻防御(Claude discretion),禁止引入 key 白名单(会堵死 variantRecipes/sourcePath 等 op 级扩展字段)

stale.ts 现状(packages/flowgraph-v3/ts/src/stale.ts):
- markStaleDownstream L33-:L44-63 构建 BFS 索引(assetConsumedByEvents: asset→消费它的事件;eventOutputs: event→产出资产),排除 role:'sequence' 与 isInactive:true 边;locked(curation:'locked')资产为传播终点;BFS visited 去重防环
- index.ts L8 `export * from './stale.js'` 已覆盖,新导出自动生效

getDownstreamIds 语义(锁定):
- 输入 nodeId(资产或事件 id),沿因果边 BFS,返回**下游资产 id 集**(orchestrate nodeIds 只收资产 id)
- sequence/inactive 边排除、curation:'locked' 终止、有向环 visited 防御——全部与 markStaleDownstream 同级语义,经共用 buildCausalIndex 保证零逻辑复制(GUARD 传统)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: updateEventParams + persistEventParams store actions + vitest</name>
  <files>packages/infinite-canvas/src/store/canvasStore.ts, packages/infinite-canvas/src/store/__tests__/canonicalWriteback.test.ts</files>
  <read_first>packages/infinite-canvas/src/store/canvasStore.ts (updateAssetMeta L573-601 守卫/空值范式、applySocketNodeState L603-635、deleteNode L739-792 持久化+回滚范式、interface 声明区 L136-147、applyGraphTransform L315), packages/flowgraph-v3/ts/src/types.ts (GenerationParams L181-192 catchall + EventNodeV3 形状), packages/infinite-canvas/src/store/__tests__/canonicalWriteback.test.ts (既有 vitest 风格与 transform-survival 范式), packages/infinite-canvas/src/v3/serialize.ts (serializeGraphToV2 签名, persistEventParams 调用用)</read_first>
  <action>
1. canvasStore.ts interface 声明区追加两个 action 签名:
   - `updateEventParams: (eventId: string, patch: Partial<GenerationParams>) => void` — 同步 canonical 写入(52-04 换 seed 回写用此同步版,持久化等下一次保存)
   - `persistEventParams: (eventId: string, patch: Partial<GenerationParams>) => Promise<void>` — 带持久化+回滚(52-03 PromptSection 保存按钮用)
2. 实现 updateEventParams(照 updateAssetMeta 范式):graph===null → console.warn 早退;节点不存在 → warn 早退;**加一条 kind!=='event' → warn 早退**(防误传 asset id,research Task 1 守卫要求);applyGraphTransform 纯函数映射合并 params;空值(undefined/null/'')删字段,与 updateAssetMeta「未设置」清空语义对齐。**不引 allowlist**(catchall 契约,research 明令)。
3. 实现 persistEventParams(照 deleteNode 店级范式):projectId/episodesId 缺失 → showToast「缺少项目上下文」早退(L742-745 范式);快照 prevParams → updateEventParams 乐观写 → await saveCanvasGraph(pid, eid, serializeGraphToV2(当前 graph, rawDataByNodeId)) → catch 回滚 prevParams + showToast(err.message,'error')。**不要**让组件自己拼 serialize+api(写入方统一在 store,NodeDetailPanel Props 签名不变是 phase35 契约)。
4. canonicalWriteback.test.ts 追加 updateEventParams 断言组:
   a. 写入:event 节点 params.prompt 更新;transform-survival(写入后做无关 applyGraphTransform,值仍在);
   b. 空值语义:patch '' → 键被删除;
   c. 守卫:graph null / 节点不存在 / kind==='asset' 目标 → console.warn 且不写(各一例);
   d. persistEventParams:mock canvasApi 模块,成功路径 saveCanvasGraph 被调且 graph 参数经 serializeGraphToV2;失败路径 prevParams 回滚 + error toast(范式参照 deleteNode.test.ts 既有 mock 手法)。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx vitest run src/store/__tests__/canonicalWriteback.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/infinite-canvas && npx vitest run src/store/__tests__/canonicalWriteback.test.ts` exit 0(新断言组 + 既有用例无回归)
    - `grep -c 'updateEventParams' packages/infinite-canvas/src/store/canvasStore.ts` ≥ 3(interface 声明 + 实现 + persistEventParams 内调用)
    - canvasStore.ts 中 updateEventParams 实现含 `kind !== 'event'` 守卫(grep 命中)
    - `cd packages/infinite-canvas && npx tsc -b` exit 0
  </acceptance_criteria>
  <done>事件配方 canonical 写入双 action 落地(同步版 + 持久化回滚版),写入方统一在 store,4 组单测锁死。</done>
</task>

<task type="auto">
  <name>Task 2: applySocketNodeState stale 自动清除 + vitest</name>
  <files>packages/infinite-canvas/src/store/canvasStore.ts (applySocketNodeState L603-635), packages/infinite-canvas/src/store/__tests__/canonicalWriteback.test.ts</files>
  <read_first>packages/infinite-canvas/src/store/canvasStore.ts (applySocketNodeState 全文 L603-635,状态归一化逻辑), packages/flowgraph-v3/ts/src/types.ts (AssetNodeV3.stale / StaleInfo 三字段形状), packages/infinite-canvas/src/store/__tests__/canonicalWriteback.test.ts (既有 applySocketNodeState 用例)</read_first>
  <action>
1. applySocketNodeState 的 transform map(L630-633 区域)内,目标节点匹配分支追加 stale 清除:当 `n.kind === 'asset'` 且归一化后 state 为 'running' 或 'success' 时置 `stale: null`;**error/failed 保留 stale**(重跑没产出新事实,脏标记不该消——CONTEXT 只授权 running/success,此为 plan 裁定,须在代码注释写明)。
2. `kind === 'asset'` 守卫天然隔离 evt_ 事件节点(state 回写不受影响,事件节点无 stale 字段)。
3. canonicalWriteback.test.ts 追加三断言:success → stale 清除;running → stale 清除;failed → stale **保留**;外加一条 transform-survival(stale 清除后无关 transform 不复活 stale)。
4. **不做**:socket success 路径全局 triggerStaleCascade(地雷 #11——会把 Phase 37 批量执行的下游全标脏,行为越界;重生成后下游不自动标 stale 记入 VERIFICATION 遗留,归 52-06)。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx vitest run src/store/__tests__/canonicalWriteback.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - vitest exit 0,含 success 清 / running 清 / failed 留 三断言
    - canvasStore.ts applySocketNodeState 实现含 `stale: null` 条件分支且带 `kind === 'asset'` 守卫(grep 命中)
    - `cd packages/infinite-canvas && npx tsc -b` exit 0
  </acceptance_criteria>
  <done>stale 清除与回写同路径(无需新 action),running/success 清、failed 留语义有测试锁死。</done>
</task>

<task type="auto">
  <name>Task 3: stale.ts buildCausalIndex 提取 + getDownstreamIds 导出 + vitest</name>
  <files>packages/flowgraph-v3/ts/src/stale.ts, packages/flowgraph-v3/ts/tests/stale.test.ts</files>
  <read_first>packages/flowgraph-v3/ts/src/stale.ts (markStaleDownstream 全文,BFS 索引 L44-63), packages/flowgraph-v3/ts/src/types.ts (FlowGraphV3 / LinkV3 role 枚举 / curation 形状), packages/flowgraph-v3/ts/tests/stale.test.ts (既有用例风格), packages/flowgraph-v3/ts/src/index.ts (确认 export * 覆盖)</read_first>
  <action>
1. stale.ts 内把 L44-63 的索引构建提取为模块私有 `buildCausalIndex(graph)`(返回 assetConsumedByEvents / eventOutputs 两索引);`markStaleDownstream` 改为调用它(行为不变,零逻辑复制)。
2. 新导出 `getDownstreamIds(graph: FlowGraphV3, nodeId: string): string[]`:复用 buildCausalIndex,从 nodeId(资产或事件起点皆可)沿因果边 BFS;只收集**资产节点 id** 进结果集;排除 role:'sequence' 与 isInactive:true 边(索引已保证);curation:'locked' 资产为终点不越过;visited Set 防环;nodeId 不存在返回 [](不 throw)。
3. stale.test.ts 追加用例组:
   a. 线性链 A→evt→B→evt→C,从 A 得 [B, C];
   b. sequence 边不传播(sequence 相连的下游不进结果);
   c. isInactive:true 边不传播;
   d. locked 资产为终点(locked 下游不再延伸,locked 自身按入结果与否以语义一致为准并在注释写明裁定);
   e. 有向环防御(A→B→A 不死循环,结果集去重);
   f. 事件节点 id 作起点(返回其产出资产的下游链);
   g. 回归:既有 markStaleDownstream 用例全绿(提取不破坏行为)。
  </action>
  <verify>
    <automated>cd packages/flowgraph-v3/ts && npx vitest run tests/stale.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/flowgraph-v3/ts && npx vitest run tests/stale.test.ts` exit 0(新用例组 + 既有回归)
    - `grep -c 'getDownstreamIds' packages/flowgraph-v3/ts/src/stale.ts` ≥ 1 且为 export
    - `grep -c 'buildCausalIndex' packages/flowgraph-v3/ts/src/stale.ts` ≥ 2(定义 + markStaleDownstream/getDownstreamIds 调用)
    - `cd packages/flowgraph-v3/ts && npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>getDownstreamIds 经共用索引导出,六类语义用例 + markStaleDownstream 回归全绿,52-05 重跑链可直接消费。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store canonical graph → 内存态 | 纯前端状态变换,无外部输入面;错误写入可致配方数据损坏 |
| persistEventParams → POST /api/canvas/v2/save-v2 | 内部 localhost 工具,复用 51-01 既有端点,无认证面变化 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01-01 | Tampering | updateEventParams 误写资产节点 | mitigate | kind!=='event' 守卫 warn 早退 + 单测锁死 |
| T-52-01-02 | Tampering | persistEventParams 失败后半写状态 | mitigate | deleteNode 范式外科式回滚 prevParams + error toast,单测锁回滚路径 |
| T-52-01-03 | Tampering | failed 节点 stale 被误清(脏标记丢失) | mitigate | 仅 running/success 清除,failed 保留 + 单测三断言 |
| T-52-01-04 | Denial of Service | getDownstreamIds 有向环死循环 | mitigate | visited Set 防环 + 环防御用例 |
| T-52-01-SC | Tampering | package installs | accept | 零新包,无 install 任务 |

**结论:纯前端逻辑层,无新增外部攻击面;主要风险是状态完整性,全部由 vitest 覆盖。无 high 级威胁。**
</threat_model>

<verification>
- `cd packages/infinite-canvas && npm test` 全绿(canonicalWriteback 新断言组 + 既有无回归)
- `cd packages/flowgraph-v3/ts && npm test` 全绿(stale 新用例 + 回归)
- `cd packages/infinite-canvas && npx tsc -b` 与 `cd packages/flowgraph-v3/ts && npx tsc --noEmit` exit 0
- `grep -c 'updateEventParams' packages/infinite-canvas/src/store/canvasStore.ts` ≥ 3;`grep -c 'getDownstreamIds' packages/flowgraph-v3/ts/src/stale.ts` ≥ 1
</verification>

<success_criteria>
- REGEN-01 地基:事件配方可经 store canonical action 写入并持久化(组件尚未接线,归 52-03)
- REGEN-03 地基:stale 自动清除语义 + 下游计算引擎就绪(服务端 stale 语义与 UI 出口归 52-02/52-05)
- 地雷 #4(nodeId 裁定)不涉及本 plan;地雷 #11(不全局触发 stale 级联)遵守并留证
</success_criteria>

<output>
Create `.planning/phases/52-prompt-edit-regenerate-loop/52-01-SUMMARY.md` when done. 记录:双 action(同步/持久化)拆分的理由、failed 保留 stale 裁定、locked 终点语义裁定。
</output>
