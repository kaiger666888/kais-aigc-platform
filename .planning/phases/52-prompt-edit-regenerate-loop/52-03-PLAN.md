---
phase: 52-prompt-edit-regenerate-loop
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx
  - packages/infinite-canvas/test/e2e/tests/phase52-regen.mjs
autonomous: true
requirements: [REGEN-01]
must_haves:
  truths:
    - "AssetDetail 新增 PromptSection,插入点在 ShotIntentSection 之后、RawDataSection 之前(NodeDetailPanel L156-157 空档);NodeDetailPanel Props={node,onClose} 签名不变(phase35 契约),能力全经 useCanvasStore 获取"
    - "反查产生事件:graph.links role:'output' && target===asset.id → source 事件;落选变体(无产生事件,配方并入 winner variantRecipes)/无产生事件的资产整块只读,保存/重生成按钮 disabled;import 种子事件可编辑(prompt 初始空,放行);多产生事件取第一条 + console.warn"
    - "保存 = persistEventParams(eventId, {prompt})(52-01 store action,乐观写+save-v2+失败回滚);保存后「重生成」可用(防半编辑误触发)"
    - "重生成裁定(地雷 #4,与 CONTEXT 字面 eventId 有出入的明写裁定):executeNode nodeId 用产出资产 id——evt_* 不在持久化 blob,simulate readNode 命中资产,node:state 回写资产卡上屏 + stale 清除链生效;eventId 仅用于 canonical 写回"
    - "e2e 断言:mock execute 调用完整 body 含新 prompt;保存+reload 往返后面板仍显示新 prompt(STORYBOARD-07 范式)"
  artifacts:
    - path: "packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx"
      provides: "PromptSection 组件(反查产生事件 + textarea + 保存/重生成)"
      contains: "PromptSection"
    - path: "packages/infinite-canvas/test/e2e/tests/phase52-regen.mjs"
      provides: "REGEN-01 e2e:编辑→保存→重生成→mock body 断言 + reload 往返保真"
  key_links:
    - from: "packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx"
      to: "packages/infinite-canvas/src/store/canvasStore.ts"
      via: "PromptSection 调 persistEventParams(保存)与读 graph 反查产生事件"
      pattern: "persistEventParams"
    - from: "packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx"
      to: "packages/infinite-canvas/src/services/canvasApi.ts"
      via: "重生成按钮 executeNode(pid, eid, 资产id, nodeType, { prompt: 新prompt, params: {...evt.params, prompt: 新prompt} })"
      pattern: "executeNode"
---

<objective>
把 kmc 最高频创作循环"改 prompt → 重抽"在画布详情面板闭环:AssetDetail 新增 PromptSection(textarea 编辑产生事件的 params.prompt + 保存 + 一键重生成),复用 52-01 的 persistEventParams 落 canonical + save-v2,复用 52-02 的 executeNode extra 通道提交新 prompt,并以 e2e 锁"任务参数含新 prompt"+ reload 往返保真两颗成败手。

**Wave 2 文件边界(与 52-04 并行):** 本 plan 只改 NodeDetailPanel.tsx + 新建 e2e 文件;canvasApi.ts/execute.ts 契约已由 52-02(W1)落地,本 plan 纯消费不修改;FlowCanvas.tsx 归 52-04/52-05,本 plan 不碰。

输出:PromptSection(含边缘 case 只读态)、phase52-regen e2e。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-prompt-edit-regenerate-loop/52-CONTEXT.md
@.planning/phases/52-prompt-edit-regenerate-loop/52-RESEARCH.md

**COORD-01 开工 checklist:本 plan 触碰零 kmc 侧文件(kais-hermes-skills),变更面检查 N/A。**

<interfaces>
<!-- 提取自 52-RESEARCH.md Task 2 / Task 3,执行者直接取用 -->

反查(唯一正道):
```ts
const producingEventIds = graph.links.filter(l => l.role === 'output' && l.target === asset.id).map(l => l.source)
const evt = graph.nodes.find(n => n.kind === 'event' && producingEventIds.includes(n.id))
```

边缘 case 矩阵(research Task 2 实证):
| 情况 | 实证来源 | UI 处理 |
|---|---|---|
| 变体组落选候选 | migrate L714-734:候选事件被删并入 winner params.variantRecipes,候选资产无产生事件 | 只读提示「落选变体配方已并入主事件 variantRecipes」,不给编辑框(地雷 #5) |
| import 种子事件 | migrate L463-472:op:'import',params 仅 sourcePath | 可编辑(prompt 初始空);语义=给导入资产补配方后重抽,放行 |
| 无任何产生事件(fixture 手造图) | fixture/直通模式无 P2 保证 | 整块只读 + 「无产生事件」,保存/重生成 disabled |
| 多个产生事件 | migrate 恒 1:1,理论防御 | 取第一条 link 序 + console.warn,不阻塞 |

插入点(NodeDetailPanel.tsx):区块序 L151-164 = MediaViewer → CurationBadge → MetaRenderer → ShotIntentSection(L156)→ **PromptSection(新)** → RawDataSection(L157)→ ...;别插错位置(地雷 #15:CONTEXT 说"MetaRenderer 与 RawDataSection 之间",实际落 ShotIntentSection 之后,同一空档)。

重生成 nodeId 裁定(地雷 #4,plan 明写):持久化 V2 blob 无 evt_* 节点,_simulate.ts readNode 按 nodeId 查 blob——传 eventId 时 readNode null、simulateOnly 降级能跑,但 applySocketNodeState(evt_id) 只更新不可见 canonical 事件节点,画布资产卡无 running/success 反馈、stale 清除不生效。**故重生成统一用产出资产 id 作 execute nodeId**(params/prompt 携带配方),eventId 仅用于 canonical 写回。CONTEXT 该条核心锁定是"复用 execute 通道 + 同配方提交",nodeId 选择属实现层,此裁定为最终裁定。

可用 API(W1 已就绪):
- persistEventParams(eventId, { prompt })(52-01):乐观写 → save-v2 → 失败回滚+toast
- executeNode(pid, eid, nodeId, nodeType, { prompt?, seed?, params? })(52-02):body 展开 extra
- store 读:graph / projectId / episodesId / showToast 经 useCanvasStore;NodeDetailPanel Props 不加字段(phase35 契约注释 L10)

e2e 基建:
- mock logCall 已记完整 body(52-02)→ `GET /__mock/calls` 断言 execute body 含新 prompt(getCalls helper 既有,phase36/37 同构)
- reload 往返保真:保存后触发 reload(graph:saved / 重新 load),面板仍显示新 prompt(STORYBOARD-07 范式,phase35 e2e 有先例)
- **e2e 跑 dist 非源码(地雷 #10):复跑前必须 `npm run build`**;phase35 e2e 契约(data-testid="detail-panel"、3 tabs、镜头意图 4 selects)不可回归
- fixture:e2e testMode 路径 projectId=1(地雷 #13 不影响 e2e);storyboard-1 节点 prompt 现值「主角进入场景」(research 实证)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PromptSection 组件(反查 + 编辑 + 保存 + 重生成 + 边缘 case)</name>
  <files>packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx</files>
  <read_first>packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx (全文:Props 签名 L10 契约注释、区块序 L147-165、StaleSection L580-593 内部组件范式、useCanvasStore 既有用法), packages/infinite-canvas/src/store/canvasStore.ts (persistEventParams / projectId / episodesId / showToast 选择子), packages/infinite-canvas/src/services/canvasApi.ts (executeNode extra 签名,52-02 落地后), packages/flowgraph-v3/ts/src/types.ts (EventNodeV3 / GenerationParams), packages/infinite-canvas/src/components/panel/MetaRenderer.tsx (区块样式范式)</read_first>
  <action>
1. NodeDetailPanel.tsx 内新增内部组件 PromptSection(与 StaleSection 同级的文件内组件,不新加文件,不改动 NodeDetailPanel 的 Props 签名):props 仅 `{ asset }`(AssetNodeV3);内部经 useCanvasStore 读 graph/projectId/episodesId/showToast 与 persistEventParams action。
2. 反查产生事件( interfaces 公式);按边缘 case 矩阵渲染:
   - 无产生事件且 graph.links 中存在并入痕迹或纯缺失 → 只读提示块(落选变体文案 / 无产生事件文案按是否有 variantGroupId 区分,简化可统一只读文案),textarea 与按钮 disabled;
   - 有产生事件 → textarea(初始值 evt.params.prompt ?? '',import 种子事件即为空)+ 「保存」+「重生成」按钮;
   - 多产生事件取第一条 + console.warn。
3. 保存:本地 editing state → 调 `persistEventParams(evt.id, { prompt })` → 成功后置 saved 标记,「重生成」按钮 enable(保存前 disabled + title 提示,CONTEXT 锁定防半编辑误触发);失败回滚/toast 由 store action 负责。
4. 重生成:`executeNode(projectId, episodesId, asset.id, nodeTypeOfStage, { prompt, params: { ...evt.params, prompt } })`——**nodeId 用资产 id(地雷 #4 裁定,注释写明理由:画布反馈 + stale 清除链依赖)**;projectId/episodesId 缺失 → toast「缺少项目上下文」早退(deleteNode 范式);HTTP 200 后 toast「已提交重生成」,完成反馈交给既有 node:state socket 链(不在组件内等 success)。
5. 插入 AssetDetail 区块序:ShotIntentSection 之后、RawDataSection 之前(L156-157 空档);仅 asset kind 渲染(event 节点详情不渲染 PromptSection)。
6. 样式从简(textarea 行数/按钮排布 Claude discretion),与既有 section 视觉一致;加 data-testid="prompt-section" 供 e2e 定位。
7. **边界**:本 task 只改 NodeDetailPanel.tsx;不碰 FlowCanvas/canvasApi/execute.ts/store(W1 已就绪)。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx tsc -b 2>&1 | tail -3 && npx vitest run src/store 2>&1 | tail -3 && grep -c 'PromptSection' src/components/panel/NodeDetailPanel.tsx && grep -c 'persistEventParams' src/components/panel/NodeDetailPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - NodeDetailPanel.tsx 含 `PromptSection`(定义 + 区块序挂载 ≥ 2 命中)与 `persistEventParams` 调用
    - NodeDetailPanel 导出组件 Props 签名不变(diff 不触及 Props 类型与 phase35 契约注释)
    - 重生成调用 executeNode 的 nodeId 实参为资产 id(grep 确认非 evt.id)
    - `cd packages/infinite-canvas && npx tsc -b` exit 0;`npm test` 全绿
  </acceptance_criteria>
  <done>PromptSection 落地:反查 + 三态(可编辑/只读-落选/只读-无事件)+ 保存(persistEventParams)+ 重生成(资产 id 裁定)。</done>
</task>

<task type="auto">
  <name>Task 2: phase52-regen e2e——编辑→保存→重生成→mock body 断言 + reload 往返</name>
  <files>packages/infinite-canvas/test/e2e/tests/phase52-regen.mjs (new)</files>
  <read_first>packages/infinite-canvas/test/e2e/tests/phase35-storyboard-metadata.mjs (detail-panel 契约 + STORYBOARD-07 reload 往返范式 + dblclick 开面板手法), packages/infinite-canvas/test/e2e/tests/phase36-orchestrator.mjs (getCalls/mock 断言范式), packages/infinite-canvas/test/e2e/helpers.mjs (既有 helper 清单,只复用不修改), packages/infinite-canvas/test/e2e/mock-backend/server.mjs (logCall 完整 body 形状,52-02 落地后)</read_first>
  <action>
1. 新建 test/e2e/tests/phase52-regen.mjs(node:test,与既有 e2e 同构):mock backend + 构建产物起服务(沿用 helpers 既有 boot 范式)。
2. 用例 REGEN-01-a(编辑保存重生成):load fixture 画布 → dblclick storyboard-1(或等价含产生事件的资产节点)→ PromptSection 可见且 textarea 显示既有 prompt(「主角进入场景」,以 fixture 实证值为准,断言前先读值再改)→ 编辑为新 prompt(如「主角转身离开」)→ 点保存 → 点重生成 → 轮询 `GET /__mock/calls` 找 execute 调用,断言其完整 body 含新 prompt 字符串且 nodeId === 资产 id。
3. 用例 REGEN-01-b(reload 往返保真,地雷 #1 端到端防线):保存后触发画布 reload(重新 load-v2 / 页面刷新,STORYBOARD-07 范式)→ 重开该节点面板 → PromptSection 仍显示新 prompt。
4. 用例 REGEN-01-c(只读态,地雷 #5):对一个无产生事件的资产(fixture 落选变体或手造资产,按 fixture 实际选定并在注释注明)开面板 → PromptSection 只读提示可见,保存/重生成按钮 disabled。
5. **前置纪律**:运行本 e2e 前必须 `npm run build`(e2e 跑 dist,地雷 #10;文件头注释写明,沿袭 51-02/verify-phase-51 头注释范式)。
6. 回归:`npm run test:e2e` 全套(phase35 契约 data-testid="detail-panel"/3 tabs/4 selects 不可回归)。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npm run build 2>&1 | tail -2 && npx node --test test/e2e/tests/phase52-regen.mjs 2>&1 | tail -8 && npm run test:e2e 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - phase52-regen.mjs 三用例全绿(mock body 含新 prompt + nodeId 为资产 id;reload 往返保真;只读态 disabled)
    - `npm run test:e2e` 全套绿(phase35/36/37/40/41 无回归)
    - 新 e2e 文件头注释含「先 npm run build」前置说明
    - helpers.mjs 零 diff(只复用)
  </acceptance_criteria>
  <done>REGEN-01 端到端闭环:编辑→保存→重生成链路有自动化断言(任务参数含新 prompt),刷新不丢有 e2e 锁死,既有 e2e 全套回归绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 画布 SPA → POST /api/canvas/v2/save-v2 + /api/canvas/execute | 内部 localhost 工具,复用既有端点,无认证面变化 |
| 用户输入 prompt → canonical graph → wire | 文本输入经 zod/序列化 round-trip;prompt 为自由文本,服务端归宿=模拟器接受并忽略 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-03-01 | Tampering | 编辑半写状态误触发重生成(旧配方被跑) | mitigate | 保存前重生成 disabled(CONTEXT 锁定)+ e2e 断言 body 含新 prompt |
| T-52-03-02 | Tampering | 落选变体编辑无处可写(写入丢失/报错) | mitigate | 无产生事件整块只读 disabled(地雷 #5)+ e2e 只读态用例 |
| T-52-03-03 | Tampering | evt_ id 提交致画布无反馈/stale 不清 | mitigate | nodeId=资产 id 裁定(地雷 #4)+ e2e 断言 nodeId |
| T-52-03-04 | Denial of Service | 重复点击重生成刷任务 | accept | 模拟器语义,内部工具;pending 反馈由 socket 链承担,与既有 execute 入口同级风险 |
| T-52-03-SC | Tampering | package installs | accept | 零新包,无 install 任务 |

**结论:内部工具 UI 接线,无新增外部攻击面;数据完整性风险由 e2e 三用例覆盖。无 high 级威胁。**
</threat_model>

<verification>
- `cd packages/infinite-canvas && npx tsc -b` exit 0;`npm test` 全绿
- `npm run build` 后 `npx node --test test/e2e/tests/phase52-regen.mjs` 三用例绿
- `npm run test:e2e` 全套回归绿(phase35 契约不破)
- grep 门:NodeDetailPanel.tsx 含 PromptSection + persistEventParams;Props 签名不变
</verification>

<success_criteria>
- REGEN-01 交付:详情面板内编辑 prompt 保存后一键重生成,新结果回贴该节点(socket 链),编辑-保存-重跑全链路有自动化断言(任务参数含新 prompt)——ROADMAP Phase 52 成功标准 #1
- 地雷 #1/#4/#5/#10 在本 plan 闭环
</success_criteria>

<output>
Create `.planning/phases/52-prompt-edit-regenerate-loop/52-03-SUMMARY.md` when done. 记录:nodeId=资产 id 裁定的落地证据(e2e nodeId 断言)、只读态 fixture 选定的节点及理由。
</output>
