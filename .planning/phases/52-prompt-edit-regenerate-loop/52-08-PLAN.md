---
phase: 52-prompt-edit-regenerate-loop
plan: 08
type: execute
wave: 4
depends_on:
  - "03"
  - "05"
files_modified:
  - packages/infinite-canvas/src/v3/adapter.ts
  - packages/infinite-canvas/src/v3/__tests__/adapter.test.ts
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
  - packages/infinite-canvas/test/e2e/tests/phase52-regen.mjs
autonomous: true
gap_closure: true
requirements: [REGEN-01]
must_haves:
  truths:
    - "落选变体详情入口可达(52-UAT Test 4 / gap #3 接缝裁定=focusAssetNodeId 未命中分支):侧栏分镜列表点击落选卡 / 深链 focus=<loserId> / 资产库定位 三条既有 focus 通路,对 curation==='deprecated' 资产一律打开详情面板(只读态可见)而非「该资产尚未放置在画布上」死路 toast"
    - "合成面板节点:data.v3 直载 canonical 落选资产(adapter syntheticDetailNode,数据形状与真实 asset RF 节点同构),NodeDetailPanel Props 签名零改动(phase35 契约 + 地雷 #14),PromptSection 只读分支(isLoserVariant)可被用户观察"
    - "REGEN-01-a 修复(gap #7a):保存后面板本就保持打开——不再重开 dblclick(被面板遮挡致 intercept),直接断言仍开面板的 textarea 更新为新 prompt 后点重生成;3 用例全绿"
    - "REGEN-01-c 修复(gap #7b):经真实用户路径(scene_shots 侧栏卡点击为主路径;深链 focus 参数为备路径,记录实际采用)打开 sb-cand-b 面板,只读断言(readonly-hint 可见 + textarea/save/regenerate disabled)通过"
    - "npm run test:e2e 全套绿(phase35/36/37/40/41 + 52 全部);phase52-regen.mjs 随本 plan 入 git(UAT 标记 owed)"
  artifacts:
    - path: "packages/infinite-canvas/src/v3/adapter.ts"
      provides: "syntheticDetailNode(asset) 导出——落选资产的 RF 形状合成节点"
      contains: "syntheticDetailNode"
    - path: "packages/infinite-canvas/src/components/FlowCanvas.tsx"
      provides: "focusAssetNodeId effect 未命中分支的 deprecated 分流"
      contains: "deprecated"
    - path: "packages/infinite-canvas/test/e2e/tests/phase52-regen.mjs"
      provides: "REGEN-01 a/b/c 三用例与实际产品行为对齐"
    - path: "packages/infinite-canvas/src/v3/__tests__/adapter.test.ts"
      provides: "syntheticDetailNode 单测(v3 透传 + label 兜底)"
  key_links:
    - from: "packages/infinite-canvas/src/components/SceneShotBrowser.tsx focusShot (L77-80)"
      to: "packages/infinite-canvas/src/store/canvasStore.ts focusAssetNodeId"
      via: "setFocusAssetNodeId(nodeId) + setViewMode('canvas') —— 零改动纯复用,落选卡天然抵达(经调用)该通路"
      pattern: "focusAssetNodeId"
    - from: "packages/infinite-canvas/src/components/FlowCanvas.tsx focus effect 未命中分支"
      to: "packages/infinite-canvas/src/v3/adapter.ts syntheticDetailNode + store setDetailNode"
      via: "canonical graph 按 id 查 kind==='asset' && curation==='deprecated' → setDetailNode(syntheticDetailNode(loser));跳过 setSelectedNode/fitView(无画布实体)"
      pattern: "syntheticDetailNode"
    - from: "packages/infinite-canvas/test/e2e/tests/phase52-regen.mjs REGEN-01-c"
      to: "scene_shots 侧栏 shot-card(deep-link focus 参数备选)"
      via: "注入变体组 → reload → 切 scene_shots 视图 → 点击落选卡 → 面板开 → 只读断言"
      pattern: "shot-card"
---

<objective>
收口 52-UAT 两个 NEW GAP:**gap #3(落选变体详情入口)**——52-03 的只读保护(commit 1690e36c)实现完整但 UI 不可达:渲染端 P12 把 deprecated 候选折叠进 winner 牌堆不上画布,侧栏 focusShot 只 focusAssetNodeId 而未命中时只弹「尚未放置」toast,接缝处无人能打开落选面板;**gap #7(test harness)**——phase52-regen.mjs 两个失败用例均为装置与真实产品行为的错位(a 假设保存后须重开面板,实际面板保持打开致 dblclick 被遮挡;c 假设注入的落选节点会上画布,实际不渲染)。

**入口裁定(focusAssetNodeId 未命中分支,非变体墙/非侧栏专属):** 三选一中选单一接缝分流——侧栏 focusShot、深链 focus 参数(57-03)、资产库「📍定位」三条既有通路全部经 focusAssetNodeId;在其未命中分支对 canonical deprecated 资产分流开面板,一处修复三路受益,且零触碰 Phase 53 变体墙领域(VariantWall 改动属跨 phase 域,modal 叠加面板还有 UX/z-index 复杂度)。落选面板节点由 adapter 新增 syntheticDetailNode 合成(数据形状与真实 asset RF 节点同构),已知语义:合成 detailNode 在下一次 setGraph 派生重解析时关闭(落选不在 rfNodes)——只读查阅场景可接受,SUMMARY 记录。

**Wave 4 / 文件边界(串行于 52-05 之后):** FlowCanvas.tsx 与 52-04(chip 区)/52-05(onNodeClick 区)共享,本 plan diff 仅限 focus effect 未命中分支(L822-828 区域);全套 e2e 回归含 52-04/52-05 新增用例,故 wave 4;与 52-06(scripts/package.json/planning docs)零文件交集可并行。

**52-06 衔接(下游关注):** 52-06 的全套终跑包含 phase52-regen.mjs——**必须在 52-08 之后执行**;52-06 执行时 S5 命令集补一行 `npm run verify:save-v2-legacy`(52-07 产出),并建议 S1 追加两条 grep(canvasAssetSchema 含 nullish、FlowCanvas 含 syntheticDetailNode)——增量小改,其余照旧。

输出:落选详情入口(合成节点 + focus 分流)、phase52-regen 三用例绿、全套 e2e 回归绿。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-prompt-edit-regenerate-loop/52-CONTEXT.md
@.planning/phases/52-prompt-edit-regenerate-loop/52-UAT.md

**COORD-01 开工 checklist:本 plan 触碰零 kmc 侧文件(kais-hermes-skills),变更面检查 N/A。**

<interfaces>
<!-- 执行者直接取用;均已实证于 master @ a42967bd(52-01/02/03 已合入) -->

focusAssetNodeId effect 现状(FlowCanvas.tsx L819-841):
- 命中(nodes 数组按 id 找到)→ setSelectedNode + setDetailNode + fitView + 1.5s 清 focus
- **未命中 → `showToast('该资产尚未放置在画布上', 'info')` + 清 focus —— 本 plan 的改动点**:先查 canonical graph(`useCanvasStore.getState().graph` 或组件内既有 graph 订阅,与文件既有风格一致)`nodes.find(n => n.kind === 'asset' && n.id === focusAssetNodeId && n.curation === 'deprecated')`;命中 → setDetailNode(syntheticDetailNode(loser)),跳过 setSelectedNode/fitView(无画布实体),保留 1.5s 清 focus 计时;未命中 → 原有 toast 分支原样
- 深链投放 effect(L846-878)只负责把 deepLinkPending 交给本 effect——零改动自动受益

adapter asset RF 节点 data 形状(L780-800,data 构造基准):`{ v3: n, stage, modality, scope, media, meta, ...(content != null ? {content} : {}), ...(timeline != null ? {timeline} : {}), curation, stale, ...(variantGroupId != null ? {variantGroupId} : {}), label: n.phaseName || n.id }`(label 具体字段名以文件实证为准)——syntheticDetailNode(asset: AssetNodeV3): Node 按此形状合成,position 取 asset.position,type 用 asset 的 RF 类型映射(rfTypeOfAsset 若可复用则复用)

setGraph detailNode 重解析语义(store L445-449):`vm.rfNodes.find(id) ?? null`——合成节点在下次 graph 变换时被置 null(面板自动关)。已知可接受语义(落选只读查阅,无编辑),写进代码注释 + SUMMARY。

NodeDetailPanel 消费契约(零改动):`asset = node.data.v3`;`title = data.label ?? asset.phaseName ?? node.id`;Props={node,onClose} 签名不变(phase35 契约 + 地雷 #14);PromptSection 只读分支 testid:prompt-section/prompt-readonly-hint(文案含「落选变体」)/prompt-textarea(disabled)/prompt-save/prompt-regenerate(disabled)

e2e 装置事实:
- 侧栏:FlowCanvas L930 viewMode 按钮 → 'scene_shots' 渲染 SceneShotBrowser;ShotCard `data-testid="shot-card-${displayShotId}"`,displayShotId = raw.label 优先(StoryBoardTimeline extractShots L310+,无 deprecated 过滤——落选卡天然在列);focusShot(L77-80)= setFocusAssetNodeId + setViewMode('canvas')
- phase52-regen.mjs 现状:REGEN-01-a 在 editAndSavePrompt 后调 openDetailPanel(重开 dblclick 被仍开的面板遮挡——UAT 实证产品行为是保存后面板保持打开且内容随 graph:saved reload 刷新,PromptSection useEffect 已按 canonicalPrompt 重置草稿);REGEN-01-c 注入 sb-cand-a/b + var-1 后对 sb-cand-b dblclick(不渲染,必败)
- 深链备路径:URL `/infinite-canvas/?projectId=1&episodesId=1&testMode=1&focus=sb-cand-b`(probe-52-real.mjs 实证过的参数形态)
- **e2e 前置纪律(地雷 #10):任何 e2e 复跑前必须 `cd packages/infinite-canvas && npm run build`(跑 dist 非源码)**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: adapter syntheticDetailNode + FlowCanvas focus 未命中分支 deprecated 分流</name>
  <files>packages/infinite-canvas/src/v3/adapter.ts, packages/infinite-canvas/src/components/FlowCanvas.tsx (仅 focusAssetNodeId effect 未命中分支), packages/infinite-canvas/src/v3/__tests__/adapter.test.ts</files>
  <read_first>packages/infinite-canvas/src/v3/adapter.ts (asset RF 节点 data 构造 L780-800、rfTypeOfAsset、既有 export 风格), packages/infinite-canvas/src/components/FlowCanvas.tsx (focus effect L819-841、graph 订阅方式、showToast/setDetailNode/setFocusAssetNodeId 既有引用), packages/infinite-canvas/src/v3/__tests__/adapter.test.ts (既有用例范式、fixture graph 构造), packages/flowgraph-v3/ts/src/types.ts (AssetNodeV3 形状,curation 字段)</read_first>
  <action>
  1. adapter.ts 新增导出 `syntheticDetailNode(asset: AssetNodeV3): Node`:按 interfaces 的 data 形状合成(v3 直载 canonical 资产 + stage/modality/scope/media/meta/curation/stale/variantGroupId/label 同构镜像;content/timeline 仅非空时带);position 取 asset.position;type 走 rfTypeOfAsset 同款映射(或其等价物,以文件实证为准)。头注释写明:用途=落选变体详情入口(focus 未命中分支,52 gap#3)、已知语义=下次 setGraph 派生重解析会关闭(rfNodes 无此 id,只读查阅可接受)。
  2. FlowCanvas focus effect 未命中分支(L822-828 区域)改造:查 canonical graph 中 `kind==='asset' && id===focusAssetNodeId && curation==='deprecated'` 的节点 → 命中则 `setDetailNode(syntheticDetailNode(loser))`,不 setSelectedNode、不 fitView(无画布实体),保留 1.5s 清 focus 计时;未命中保持原 toast 分支与清空逻辑逐字不动。分支注释写明三条受益通路(侧栏 focusShot / 深链 focus / 资产库定位)与 Phase 53 变体墙域边界(本分流不触碰 VariantWall)。
  3. adapter.test.ts 补用例:a) syntheticDetailNode 透传 v3(返回值 data.v3 === 入参引用)且 label 兜底(phaseName 缺失时用 id);b) deprecated 资产输入产出 curation==='deprecated' 的 data(面板 isLoserVariant 分支可触发)。
  4. **区域纪律**:FlowCanvas.tsx diff 仅限 focus effect 未命中分支(±10 行级);onNodeClick 区(52-05)、chip 区(52-04)、深链投放 effect(L846-878)一律不动。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx tsc -b 2>&1 | tail -3 && grep -c 'syntheticDetailNode' src/v3/adapter.ts src/components/FlowCanvas.tsx src/v3/__tests__/adapter.test.ts && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - adapter.ts 导出 syntheticDetailNode;FlowCanvas focus 未命中分支含 `curation === 'deprecated'` 分流 + syntheticDetailNode 调用(grep 各 ≥ 1)
    - 原「该资产尚未放置在画布上」toast 分支保留(非 deprecated 未命中仍走原路)
    - FlowCanvas diff 仅 focus effect 区;adapter.test.ts 两用例绿;`npx tsc -b` exit 0;`npm test` 全绿
  </acceptance_criteria>
  <done>落选资产经任一 focus 通路打开详情面板(只读态可观察),合成节点与真实 RF data 形状同构,三条既有通路一处分流。</done>
</task>

<task type="auto">
  <name>Task 2: phase52-regen.mjs 对齐真实行为(a 去重开 / c 改侧栏入口)+ 全套 e2e 回归 + 入 git</name>
  <files>packages/infinite-canvas/test/e2e/tests/phase52-regen.mjs</files>
  <read_first>packages/infinite-canvas/test/e2e/tests/phase52-regen.mjs (全文:openDetailPanel/editAndSavePrompt 现状、REGEN-01-c 注入块), packages/infinite-canvas/test/e2e/helpers.mjs (loadCanvas/switchToCanvasView/getCalls,只复用不修改), packages/infinite-canvas/src/components/FlowCanvas.tsx (L915-931 ViewModeButton 区:scene_shots 切换定位), packages/infinite-canvas/src/components/SceneShotBrowser.tsx (ShotCard testid 构成 L136), packages/infinite-canvas/src/components/StoryboardTimeline.tsx (extractShots displayShotId=raw.label 优先,实证注入节点的卡 testid)</read_first>
  <action>
  1. **REGEN-01-a 修复(gap #7a)**:editAndSavePrompt 之后的 `openDetailPanel(page, 'storyboard-1')` 重开调用删除——保存后面板保持打开且随 graph:saved reload 刷新(UAT 实证产品行为);改为直接 `expect(textarea).toHaveValue(NEW_PROMPT)`(等 canonical 重置草稿)→ 断言 regenerate enabled → 点击 → 既有 execute body 断言不变(body.prompt/params.prompt/nodeId/nodeType)。文件头注释同步更正「保存后需重开面板」的过时假设。
  2. **REGEN-01-c 修复(gap #7b)**:注入块不变;打开 sb-cand-b 面板改走真实用户路径——**主路径**:切 scene_shots 视图(点 L930 区域 ViewModeButton)→ 点击 sb-cand-b 的 shot-card(testid 按 raw.label 推导,先实证)→ focusShot 自动切回 canvas + 面板开;**备路径**(主路径装置推导不通时):page.goto 追加 `&focus=sb-cand-b` 深链。只读断言组(readonly-hint 可见含「落选变体」、textarea/save/regenerate disabled)与 winner sb-cand-a 可编辑对照断言全部保留。
  3. 文件头注释写明「先 npm run build」前置(地雷 #10)与 a/c 两处装置修正依据(52-UAT Session Notes)。
  4. **全套回归**:`npm run build` → `npx node --test test/e2e/tests/phase52-regen.mjs` 3 用例绿 → `npm run test:e2e` 全套绿(含 52-04 reroll / 52-05 stale-panel 新用例)。
  5. `git add packages/infinite-canvas/test/e2e/tests/phase52-regen.mjs` 随本 plan commit(UAT 标记 owed 的测试资产收编)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm run build 2>&1 | tail -2 && npx node --test test/e2e/tests/phase52-regen.mjs 2>&1 | tail -8 && npm run test:e2e 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - phase52-regen.mjs 3 用例全绿(a 无重开直连重生成;c 经侧栏/深链打开落选面板只读断言过)
    - `npm run test:e2e` 全套绿(phase35/36/37/40/41 + 52 全部用例,零回归)
    - helpers.mjs 零 diff;文件已入 git
    - SUMMARY 记录 c 用例实际采用路径(主/备)与 shot-card testid 实证值
  </acceptance_criteria>
  <done>52-UAT 审计注记的 2 e2e failures 清零,测试装置与真实产品行为(面板保持打开/落选不上画布)对齐,REGEN-01 三断言链(编辑保存往返/重生成提交/落选只读)全部有 e2e 锁死。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| focus 通路(侧栏/深链/资产库)→ focusAssetNodeId → 详情面板 | 纯前端数据流;deprecated 分流只改「未命中时的呈现」,不新增写路径 |
| 合成 detailNode → NodeDetailPanel 渲染 | 面板对 data.v3 只读消费;落选态保存/重生成按钮 disabled(52-03),无写面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-08-01 | Tampering | 合成节点绕过 RF 模型造成面板状态错乱 | mitigate | data 形状与 adapter 真实节点同构 + setGraph 重解析自动关闭语义注释 + e2e 全套回归(phase35 detail-panel 契约) |
| T-52-08-02 | Tampering | 落选面板误开编辑写 winner 配方(地雷 #5 复燃) | mitigate | 52-03 isLoserVariant 整块只读已落;REGEN-01-c 断言 save/regenerate disabled 锁死 |
| T-52-08-03 | Tampering | focus 分流误吞非 deprecated 未命中(资产库「未放置」提示语义回归) | mitigate | 分流条件显式三连(kind+id+curation);原 toast 分支保留;adapter 单测 + e2e 复核 |
| T-52-08-04 | Tampering | e2e 假绿(跑旧 dist) | mitigate | 文件头 npm run build 前置 + verify 命令链内建 build(地雷 #10) |
| T-52-08-SC | Tampering | package installs | accept | 零新包,无 install 任务 |

**结论:纯前端呈现层接缝修复,无新增写路径/外部攻击面;只读保护由 52-03 既有实现 + 本 plan e2e 双锁。无 high 级威胁。**
</threat_model>

<verification>
- `cd packages/infinite-canvas && npx tsc -b` exit 0;`npm test` 全绿(含 adapter 新用例)
- `npm run build` 后 phase52-regen.mjs 3 用例绿;`npm run test:e2e` 全套绿
- grep 门:adapter 含 syntheticDetailNode;FlowCanvas focus 分支含 deprecated 分流;原 toast 文案仍在;helpers.mjs 零 diff
</verification>

<success_criteria>
- 52-UAT Test 4 truth 关闭:落选变体只读态可被用户观察(侧栏/深链/资产库定位三路入口开面板)
- 52-UAT gap #7 关闭:phase52-regen.mjs 与真实产品行为对齐,审计注记的 2 e2e failures 清零且入 git
- phase35 detail-panel 契约与 52-04/05 新用例零回归
</success_criteria>

<output>
Create `.planning/phases/52-prompt-edit-regenerate-loop/52-08-SUMMARY.md` when done。记录:REGEN-01-c 实际采用路径(主/备)与 shot-card testid 实证、合成 detailNode 的自动关闭语义实测、52-06 衔接确认(S5 补 verify:save-v2-legacy 一行 + S1 建议增量)。
</output>
