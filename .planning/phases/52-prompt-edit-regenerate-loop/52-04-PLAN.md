---
phase: 52-prompt-edit-regenerate-loop
plan: 04
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - packages/infinite-canvas/src/components/canvas/eventChipBus.ts
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
  - packages/infinite-canvas/src/components/eventParams/EventParamsPopover.tsx
  - packages/infinite-canvas/test/e2e/tests/phase52-reroll.mjs
autonomous: true
requirements: [REGEN-02]
must_haves:
  truths:
    - "EventChipClickInfo 加可选 projectId/episodesId(不破坏 EventChipNode 发射端);FlowCanvas handleEventChipClick 注入 { ...info, projectId, episodesId }(FlowCanvas 持有,popover 当前缺这两个值)"
    - "handleRerollSeed 不再是 TODO/console.log:「执行后端待接入」残桩删除;点击后以同配方+新 seed 提交 executeNode,popover 内 pending 态(按钮 disabled + 文案)+ toast 提交提示"
    - "新 seed 保留 Math.floor(Math.random() * 1_000_000) 域(与既有芯片 tooltip seed 量级一致,免改 chipSummary)"
    - "重生成 nodeId 用产出资产 id(地雷 #4 裁定,与 52-03 同一裁定);提交成功后 updateEventParams(eventId, { seed: newSeed }) 落 canonical(芯片 tooltip/popover 立即显示新 seed;持久化等下一次保存,地雷 #12)"
    - "anchor.projectId/episodesId 缺失(fixture 模式)→ 重跑按钮 disabled 或 toast「缺少项目上下文」早退;不在 popover 里等 socket success 解 pending(无 per-request 关联,会泄漏)"
    - "e2e:芯片→popover→🎲→mock execute body 同配方(prompt 不变)+ 新 seed(≠旧 seed)+ pending 态 + toast"
  artifacts:
    - path: "packages/infinite-canvas/src/components/canvas/eventChipBus.ts"
      provides: "EventChipClickInfo anchor 扩展(projectId/episodesId 可选)"
    - path: "packages/infinite-canvas/src/components/eventParams/EventParamsPopover.tsx"
      provides: "handleRerollSeed 接通 + pending 态"
      contains: "executeNode"
    - path: "packages/infinite-canvas/test/e2e/tests/phase52-reroll.mjs"
      provides: "REGEN-02 e2e:换 seed 提交断言"
  key_links:
    - from: "packages/infinite-canvas/src/components/FlowCanvas.tsx"
      to: "packages/infinite-canvas/src/components/canvas/eventChipBus.ts"
      via: "handleEventChipClick 注入 projectId/episodesId → setActiveChip → EventParamsPopover anchor"
      pattern: "setActiveChip"
    - from: "packages/infinite-canvas/src/components/eventParams/EventParamsPopover.tsx"
      to: "packages/infinite-canvas/src/services/canvasApi.ts + store/canvasStore.ts"
      via: "executeNode(pid, eid, 资产id, nodeType, { params: {...recipe, seed} }) + updateEventParams(eventId, { seed })"
      pattern: "executeNode"
---

<objective>
接通 EventParamsPopover「同配方换 seed 重跑」既有接缝(L49-56,当前 console.log + toast「执行后端待接入」):经 eventChipBus anchor 扩展注入 projectId/episodesId,点击后同配方+新 seed 提交 execute 通道,pending/toast 反馈,提交成功后新 seed 回写 canonical(防 reload 回旧值)。e2e 锁"同配方 + 新 seed"断言。

**Wave 2 文件边界(与 52-03 并行):** 本 plan 改 eventChipBus.ts / FlowCanvas.tsx(**仅 L197-198 handleEventChipClick 区**) / EventParamsPopover.tsx + 新建 e2e;canvasApi.executeNode extra 签名由 52-02(W1)落地,本 plan 纯消费**不修改 canvasApi.ts**(52-03 也不改它,零交集);NodeDetailPanel.tsx 归 52-03,本 plan 不碰;FlowCanvas.tsx 的 onNodeClick 区(L470-477)归 52-05(W3 串行),本 plan 不碰。

输出:anchor 注入、handleRerollSeed 接通 + pending、phase52-reroll e2e。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-prompt-edit-regenerate-loop/52-CONTEXT.md
@.planning/phases/52-prompt-edit-regenerate-loop/52-RESEARCH.md

**COORD-01 开工 checklist:本 plan 触碰零 kmc 侧文件(kais-hermes-skills),变更面检查 N/A。**

<interfaces>
<!-- 提取自 52-RESEARCH.md Task 4,执行者直接取用 -->

eventChipBus 现状(packages/infinite-canvas/src/components/canvas/eventChipBus.ts,全文 27 行):
- `EventChipClickInfo = { eventId, op, clientX, clientY }`,Context 默认 noop;EventChipNode.handleClick 发射;FlowCanvas handleEventChipClick(L197-198)`setActiveChip(info)` 接收;`<EventParamsPopover anchor={activeChip}>`(L1005 区域)消费

最小注入(CONTEXT 锁定经 anchor 扩展,不用 useCanvasStore 直读备选):
1. EventChipClickInfo 加 `projectId?: number | null; episodesId?: number | null`(可选,不破坏 EventChipNode 发射端)
2. FlowCanvas handleEventChipClick 一行改:`setActiveChip({ ...info, projectId, episodesId })`——FlowCanvas L151-152 已从 store 拿到这两个值
3. popover 内 anchor.projectId/episodesId 守卫:空 → disabled/toast「缺少项目上下文」早退(deleteNode L742-745 范式)

handleRerollSeed 接缝(EventParamsPopover.tsx L49-56):
- 现接缝已是 `Math.floor(Math.random() * 1_000_000)`——**保留 1e6 域**(与芯片 tooltip seed 量级一致)
- 提交:`executeNode(pid, eid, outputAssetId, nodeTypeOfStage, { params: { ...evt.params, seed: newSeed } })`;nodeId 用**产出资产 id**(地雷 #4 裁定,与 52-03 一致;popover 需经 graph 反查该事件的产出资产——role:'output' && source===eventId 的 target)
- 提交成功后 `updateEventParams(eventId, { seed: newSeed })` 落 canonical(52-01 同步 action;持久化等下一次保存——地雷 #12 plan 裁定:做)
- pending 态:useState pending → 按钮 disabled + 文案「重跑中…」→ finally 复位;HTTP 200 后 toast「已提交」;**不**在 popover 等 socket success(无 per-request 关联,会泄漏;完成反馈由 node:state → applySocketNodeState 上屏,51-02 链)

executeNode extra 签名(52-02 已落地,纯消费):`executeNode(projectId, episodesId, nodeId, nodeType, extra?, cancelToken?)`

e2e 基建:mock logCall 已记完整 body(52-02);断言 execute body 同配方(prompt 不变)+ 新 seed ≠ 旧 seed;e2e testMode fixture projectId=1 有项目上下文(地雷 #13 不影响);**复跑前必须 npm run build(地雷 #10)**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: eventChipBus anchor 扩展 + FlowCanvas 注入 projectId/episodesId</name>
  <files>packages/infinite-canvas/src/components/canvas/eventChipBus.ts, packages/infinite-canvas/src/components/FlowCanvas.tsx (仅 L197-198 handleEventChipClick 区)</files>
  <read_first>packages/infinite-canvas/src/components/canvas/eventChipBus.ts (全文 27 行), packages/infinite-canvas/src/components/FlowCanvas.tsx (L26 import、L96-103 URL 参数初始化、L151-152 store projectId/episodesId 选择子、L197-198 handleEventChipClick、L1005 区域 EventParamsPopover 挂载), packages/infinite-canvas/src/components/nodes/ (EventChipNode 发射端,确认不破坏)</read_first>
  <action>
1. eventChipBus.ts:EventChipClickInfo 类型加 `projectId?: number | null` 与 `episodesId?: number | null`(均可选,EventChipNode 发射端零改动)。
2. FlowCanvas.tsx handleEventChipClick(L197-198)改为注入:`setActiveChip({ ...info, projectId, episodesId })`(projectId/episodesId 用组件内既有 store 选择子值)。
3. **区域纪律**:本 plan 对 FlowCanvas.tsx 的 diff 仅限 handleEventChipClick 一处(±2 行);onNodeClick(L470-477)归 52-05,import 区尽量不动。
4. 编译门:`npx tsc -b` clean(EventChipNode 等发射端不受可选字段影响)。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx tsc -b 2>&1 | tail -3 && grep -n 'projectId' src/components/canvas/eventChipBus.ts && grep -n 'setActiveChip' src/components/FlowCanvas.tsx</automated>
  </verify>
  <acceptance_criteria>
    - eventChipBus.ts EventChipClickInfo 含 projectId/episodesId 可选字段(grep 命中)
    - FlowCanvas.tsx setActiveChip 调用含注入展开(grep 命中 `{ ...info`)
    - FlowCanvas.tsx diff 仅限 handleEventChipClick 区域(行数变化 ≤ 4 行)
    - `cd packages/infinite-canvas && npx tsc -b` exit 0;`npm test` 全绿
  </acceptance_criteria>
  <done>anchor 携带项目上下文,popover 获得提交所需 pid/eid,发射端零破坏。</done>
</task>

<task type="auto">
  <name>Task 2: handleRerollSeed 接通 + pending 态 + seed 回写 canonical</name>
  <files>packages/infinite-canvas/src/components/eventParams/EventParamsPopover.tsx</files>
  <read_first>packages/infinite-canvas/src/components/eventParams/EventParamsPopover.tsx (全文:anchor 消费、handleRerollSeed L49-56 接缝、🎲 按钮 L115、showToast 既有用法、evt.params 获取方式), packages/infinite-canvas/src/services/canvasApi.ts (executeNode extra 签名), packages/infinite-canvas/src/store/canvasStore.ts (updateEventParams 同步 action 签名 + graph 反查产出资产的 links 形状)</read_first>
  <action>
1. handleRerollSeed 重写:守卫(anchor.projectId/episodesId 空 → toast「缺少项目上下文」早退;pending 中 → 忽略重复点击)→ 新 seed `Math.floor(Math.random() * 1_000_000)`(保留 1e6 域)→ 经 store graph 反查产出资产(`links.filter(l => l.role === 'output' && l.source === anchor.eventId)` 的 target;查不到 → toast 早退)→ `executeNode(pid, eid, outputAssetId, nodeTypeOfStage, { params: { ...evt.params, seed: newSeed } })`(nodeId=资产 id,地雷 #4 裁定注释写明)。
2. 成功后:`updateEventParams(anchor.eventId, { seed: newSeed })` 落 canonical(52-01 同步 action;芯片 tooltip/popover 立即显示新 seed;持久化等下一次保存——地雷 #12 裁定:做,注释注明)+ toast「已提交」。
3. pending 态:useState pending → 提交期间按钮 disabled + 文案「重跑中…」→ finally 复位;**不**等 socket success(注释注明无 per-request 关联)。
4. 删除「执行后端待接入」文案与 console.log 残桩。
5. **边界**:本 task 只改 EventParamsPopover.tsx;不碰 canvasApi/store(W1 已就绪)。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npx tsc -b 2>&1 | tail -3 && grep -c 'executeNode' src/components/eventParams/EventParamsPopover.tsx && grep -c '执行后端待接入' src/components/eventParams/EventParamsPopover.tsx; grep -c 'updateEventParams' src/components/eventParams/EventParamsPopover.tsx</automated>
  </verify>
  <acceptance_criteria>
    - EventParamsPopover.tsx 含 `executeNode(` 与 `updateEventParams(` 调用
    - `grep -c '执行后端待接入' src/components/eventParams/EventParamsPopover.tsx` = 0;无 console.log 残桩
    - pending 态存在(disabled/「重跑中…」grep 命中)
    - `cd packages/infinite-canvas && npx tsc -b` exit 0;`npm test` 全绿
  </acceptance_criteria>
  <done>换 seed 重跑接通:同配方+新 seed 提交、pending 反馈、seed 回写 canonical,残桩清零。</done>
</task>

<task type="auto">
  <name>Task 3: phase52-reroll e2e——芯片→popover→🎲→mock body 同配方+新 seed 断言</name>
  <files>packages/infinite-canvas/test/e2e/tests/phase52-reroll.mjs (new)</files>
  <read_first>packages/infinite-canvas/test/e2e/tests/phase36-orchestrator.mjs (getCalls/断言范式), packages/infinite-canvas/test/e2e/helpers.mjs (boot/点击 helper,只复用不修改), packages/infinite-canvas/test/e2e/tests/phase52-regen.mjs (52-03 新建,同 phase e2e 风格基准;若并行执行尚未落地则参照 phase35), packages/infinite-canvas/src/components/eventParams/EventParamsPopover.tsx (🎲 按钮文案/data 属性,e2e 定位用——若无稳定定位符,Task 2 中补 data-testid="reroll-seed-btn")</read_first>
  <action>
1. 新建 test/e2e/tests/phase52-reroll.mjs(node:test 同构):fixture 画布含带配方(prompt+seed)的事件芯片(按既有 fixture 实证选定,注释注明)。
2. 用例 REGEN-02-a:点击 event 芯片 → popover 可见 → 记录当前 seed 展示值 → 点 🎲 → 断言按钮进入 pending(disabled)→ 轮询 `GET /__mock/calls` 找 execute 调用,断言 body:prompt 与旧配方一致(同配方)、seed 存在且 ≠ 旧 seed、nodeId === 产出资产 id。
3. 用例 REGEN-02-b:提交后 popover/芯片展示新 seed(updateEventParams 回写上屏)且 toast「已提交」出现。
4. 文件头注释写明「先 npm run build」前置(地雷 #10)。
5. 回归:`npm run test:e2e` 全套绿。
  </action>
  <verify>
    <automated>cd packages/infinite-canvas && npm run build 2>&1 | tail -2 && npx node --test test/e2e/tests/phase52-reroll.mjs 2>&1 | tail -8 && npm run test:e2e 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - phase52-reroll.mjs 两用例全绿(同配方 + 新 seed ≠ 旧 + nodeId 资产 id;新 seed 上屏 + toast)
    - `npm run test:e2e` 全套回归绿
    - helpers.mjs 零 diff
  </acceptance_criteria>
  <done>REGEN-02 交付:换 seed 重跑不再是 TODO,同配方+新 seed 提交与 UI 反馈有 e2e 锁死——ROADMAP Phase 52 成功标准 #2。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 画布 SPA → POST /api/canvas/execute | 内部 localhost 工具,复用既有端点,无认证面变化 |
| popover anchor → 项目上下文注入 | 纯前端数据流;pid/eid 为内部数值 id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04-01 | Tampering | 无项目上下文提交(请求 400/孤儿任务) | mitigate | anchor pid/eid 守卫早退 + toast(地雷 #13) |
| T-52-04-02 | Tampering | 新 seed 不回写则 reload 回旧值(半个地雷 #1) | mitigate | 提交后 updateEventParams 落 canonical(地雷 #12 裁定)+ e2e 上屏断言 |
| T-52-04-03 | Denial of Service | 连点 🎲 刷任务 | mitigate | pending 态 disabled 抑制;不在 popover 等 socket(防 pending 泄漏) |
| T-52-04-04 | Tampering | evt_ id 提交致画布无反馈 | mitigate | nodeId=产出资产 id 裁定(地雷 #4)+ e2e 断言 |
| T-52-04-SC | Tampering | package installs | accept | 零新包,无 install 任务 |

**结论:内部工具 UI 接线,无新增外部攻击面;风险由守卫 + e2e 覆盖。无 high 级威胁。**
</threat_model>

<verification>
- `cd packages/infinite-canvas && npx tsc -b` exit 0;`npm test` 全绿
- `npm run build` 后 `npx node --test test/e2e/tests/phase52-reroll.mjs` 两用例绿
- `npm run test:e2e` 全套回归绿
- grep 门:EventParamsPopover 含 executeNode/updateEventParams,无「执行后端待接入」;eventChipBus 含 projectId/episodesId;FlowCanvas diff ≤ 4 行
</verification>

<success_criteria>
- REGEN-02 交付:「换 seed 重跑」接通,同配方+新 seed 提交,UI pending/完成反馈——ROADMAP Phase 52 成功标准 #2
- 地雷 #4/#12/#13 在本 plan 闭环
</success_criteria>

<output>
Create `.planning/phases/52-prompt-edit-regenerate-loop/52-04-SUMMARY.md` when done. 记录:1e6 seed 域保留理由、产出资产反查失败时的早退路径实证。
</output>
