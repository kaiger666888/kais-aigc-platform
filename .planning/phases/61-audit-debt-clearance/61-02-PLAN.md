---
phase: 61-audit-debt-clearance
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/reviewBridge.ts
  - src/lib/__tests__/reviewBridge.test.ts
autonomous: true
requirements: [DEBT-02]
must_haves:
  truths:
    - "选定 winner 后 reviewBridge 发出的 reviews 列表请求 URL 是 /api/v1/reviews/?…(带尾斜杠)——review-nginx 直连 200,不再经 307 中间跳(Location 丢端口→404 链路消除,DEBT-02)"
    - "该行为被回归测试锁死:删掉斜杠测试必红(断言字面量,非响应成功——Pitfall 2 假绿防线)"
    - "分页第二跳(next_cursor 跟进)的 URL 同样带斜杠"
  artifacts:
    - path: "src/lib/reviewBridge.ts"
      provides: "L182 列表 URL 补尾斜杠 + 模块头契约注释(L19)同步 + 54-01 同陷阱行内注记"
      contains: "/api/v1/reviews/?"
    - path: "src/lib/__tests__/reviewBridge.test.ts"
      provides: "node:test 注入 fetchImpl 回归锁(URL 字面量断言 + 分页双跳断言)"
      contains: "reviews/?"
  key_links:
    - from: "reviewBridge.ts resolveOpenReviewForSelection L182"
      to: "review-platform GET /api/v1/reviews/?"
      via: "fetchImpl(`${baseUrl}/api/v1/reviews/?${qs}`)"
      pattern: "reviews/\\?\\$\\{|reviews/\\?"
    - from: "reviewBridge.test.ts"
      to: "resolveOpenReviewForSelection(params, {fetchImpl})"
      via: "deps.fetchImpl 原生注入位捕获出站 URL"
      pattern: "fetchImpl"
---

<objective>
DEBT-02 一字修: reviewBridge 列表请求 URL 补尾斜杠,消除 Starlette redirect_slashes 的
307 中间跳(307 → Location 丢端口 → 404 活体已证);同步模块头契约注释,并用双形态回归锁死
(node:test 注入断言字面量 + 61-05 聚合门静态 grep——CONTEXT discretion 裁定:两者都上,
54-01 gateStateService L323-324 + verify-phase-54 L193 先例)。

Purpose: 清偿 TD-4——仓内 reviews 列表调用点最后一个无斜杠残留(gateStateService 已修,本处修完后 100% 带斜杠)。
Output: 一行修复 + 契约注释 + node:test 回归锁。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-audit-debt-clearance/61-CONTEXT.md
@.planning/phases/61-audit-debt-clearance/61-RESEARCH.md

<interfaces>
<!-- 已提取契约 -->

reviewBridge.ts 结构(全文已核):
  export interface ReviewBridgeParams { projectId; episodesId; groupId; winnerNodeId;
    variantIndex(1-based); winnerPhaseName: string | null }
  export interface ReviewBridgeDeps { baseUrl?; fetchImpl?: typeof fetch; logger?; timeoutMs? }
  export async function resolveOpenReviewForSelection(params, deps): Promise<void>
  —— 永不 reject,失败路径 warn/info + skip(D-08/D-10)。
  列表调用唯一现场 L182:
    fetchImpl(`${baseUrl}/api/v1/reviews?${qs.toString()}`, { signal: AbortSignal.timeout(timeoutMs) })
    qs 恒含 {source:'kais-movie-agent', status:'APPROVING', limit:'100'} + 可选 cursor。
  phaseToken 门(L159-163): winnerPhaseName 为 null/无 p<digits> 前缀 → info skip,不发列表请求
  ——测试要打到列表 fetch,必须给形如 'p11a0...' 的 winnerPhaseName(derivePhaseToken 取 leading p<digits>)。
  分页: has_more===true 且 next_cursor 可用 → 循环最多 MAX_LIST_PAGES=10 跳。

修复先例(src/lib/gateStateService.ts L323-324,54-01):
  行内注释「尾斜杠(54-01):/api/v1/reviews 无斜杠 307 → location 丢端口 → 404。」+
  URL `/api/v1/reviews/?${qs.toString()}`——本 plan 逐字同款。

活体验证证据(61-RESEARCH Code Examples,2026-08-24 本机 :8090):
  无斜杠 → HTTP 307 + Location: http://localhost/api/v1/reviews/?(端口 8090 被丢)→ 404;
  带斜杠 → 200 {"data":{"items":[],...}}。根因 = Starlette redirect_slashes + nginx Location 丢端口。

测试基建事实(planner 勘正 RESEARCH 的 vitest 建议):
  根仓无 vitest(仅两 packages 有);根仓测试先例 = node:test +
  `node --import tsx --test src/lib/__tests__/<file>.test.ts`
  (gpuQueueCrossProc.test.ts + verify-phase-49-core.ts L154-157 spawn 范式)。
  本 plan 测试按 node:test + assert/strict 写,tsconfig 类型由根 npx tsc --noEmit 兜底。

Response 桩: node 24 全局 Response 可用;或最小对象 {ok:true, status:200,
json: async () => body} as unknown as Response(fetchImpl 类型收口)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: L182 尾斜杠一字修 + 模块头契约注释同步</name>
  <files>src/lib/reviewBridge.ts</files>
  <read_first>
  - src/lib/reviewBridge.ts L11-26(模块头 verified platform contract——第 2 条即列表契约注释行)、L146-212(resolveOpenReviewForSelection 主体:phaseToken 门/列表循环/L182 现场)
  - src/lib/gateStateService.ts L320-326(54-01 同陷阱修复先例——注释与 URL 形状逐字参照)
  </read_first>
  <action>
  (1) L182: `${baseUrl}/api/v1/reviews?${qs.toString()}` → `${baseUrl}/api/v1/reviews/?${qs.toString()}`,
      行上方加行内注释(54-01 同款措辞):尾斜杠(61-02/54-01 同陷阱):/api/v1/reviews 无斜杠
      307 → Location 丢端口 → 404;带斜杠直连 200。
  (2) 模块头契约注释 L19 第 2 条:`GET /api/v1/reviews?status=...` → `GET /api/v1/reviews/?status=...`
      (仅该一处字符,注释其余逐字不动)。
  (3) 不改 baseUrl 解析/分页逻辑/approve 调用(D-改动面:只动 path 字面量)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && grep -c 'reviews/?' src/lib/reviewBridge.ts && ! grep -Eq 'reviews\?[${\\s]' src/lib/reviewBridge.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
  - reviewBridge.ts 中 /api/v1/reviews/? 字面量恰 1 处(L182);裸 /api/v1/reviews? 字面量 0 处(grep -E 'reviews\?' 排除 reviews/? 后无命中);
  - 模块头 L19 契约注释已带斜杠;
  - npx tsc --noEmit exit 0。
  </acceptance_criteria>
  <done>出站列表 URL 直连化;307 中间跳在源码层消除。</done>
</task>

<task type="auto">
  <name>Task 2: node:test 回归锁(注入 fetchImpl 断言 URL 字面量)</name>
  <files>src/lib/__tests__/reviewBridge.test.ts</files>
  <read_first>
  - src/lib/__tests__/gpuQueueCrossProc.test.ts L1-25(node:test + assert/strict 头部范式)
  - src/lib/reviewBridge.ts L65-80(Params/Deps 接口——测试构造形状)、L146-212(列表循环/分页/has_more 语义)
  - scripts/verify-phase-49-core.ts L154-166(node --import tsx --test 调用与 pass/fail 判定范式——61-05 B 段照此接入)
  </read_first>
  <action>
  新建 src/lib/__tests__/reviewBridge.test.ts(import { test } from 'node:test';
  import assert from 'node:assert/strict';import { resolveOpenReviewForSelection } from '../reviewBridge'):
  公共桩: mkParams = { projectId:1, episodesId:1, groupId:'g', winnerNodeId:'n', variantIndex:1,
  winnerPhaseName:'p11a0'(phaseToken 门放行) };mkFetch(capturedUrls, pages) 返回
  async (url) => { capturedUrls.push(String(url)); return { ok:true, status:200,
  json: async () => ({data: pages.shift()}) } as unknown as Response }。
  Test 1 「列表 URL 带尾斜杠直连(61-02/DEBT-02)」: pages=[{items:[], next_cursor:null,
  has_more:false}](单跳即止)→ await resolveOpenReviewForSelection(mkParams,
  { baseUrl:'http://mock-review', fetchImpl: mkFetch(urls, pages), logger: silentLogger }) →
  断言 capturedUrls.length === 1 且 capturedUrls[0].includes('/api/v1/reviews/?') 且
  !capturedUrls[0].match(/\/api\/v1\/reviews\?/)(Pitfall 2:字面量正反双断言,删斜杠必红)。
  Test 2 「分页第二跳同样带斜杠」: pages=[{items:[], next_cursor:'c1', has_more:true},
  {items:[], next_cursor:null, has_more:false}] → 断言 capturedUrls.length === 2 且
  every(includes('/api/v1/reviews/?')) 且第二条含 cursor=c1(querystring 透传)。
  Test 3 「winnerPhaseName 空 → 不发列表请求(回归网)」: winnerPhaseName:null →
  capturedUrls.length === 0。
  头注释写运行命令: cd 仓库根 && node --import tsx --test src/lib/__tests__/reviewBridge.test.ts。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && node --import tsx --test src/lib/__tests__/reviewBridge.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
  - node --test exit 0,pass ≥ 3 fail = 0;
  - Test 1 的反断言(裸 reviews? 无命中)在场——验证方式:临时把 L182 斜杠删掉跑一次必红(执行时做一次即还原,SUMMARY 记录);
  - 测试零网络依赖(纯注入,CI/沙箱可重复)。
  </acceptance_criteria>
  <done>DEBT-02 行为锁在位:URL 字面量正反断言 + 分页覆盖 + skip 分支回归网。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kap 服务端 → review-platform 出站 fetch | 桥是 fire-and-forget 出站方向;URL path 字面量属服务端常量 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-05 | Tampering | baseUrl 出站 SSRF | accept | baseUrl 仅 env 级配置注入既有纪律;本 plan 只改 path 字面量,不碰 baseUrl 解析(RESEARCH Security Domain 同裁定) |
| T-61-06 | Spoofing | 307 开放重定向滥用 | accept(修复反而消除) | 307 是出站方向被动行为,非用户可控 URL;补斜杠后中间跳消失 |
| T-61-07 | Information Disclosure | 测试桩泄漏真实端点 | mitigate | 测试 baseUrl 用 http://mock-review 假主机,零真实网络调用 |
| T-61-SC | Tampering | npm/pip 安装 | accept | 零新依赖,node:test 与 tsx 均仓内既有 |
</threat_model>

<verification>
- grep:reviews/? 恰 1 处、裸 reviews? 0 处;
- node --import tsx --test 三用例绿;
- 根 npx tsc --noEmit 绿;
- (61-05 聚合门 S1 静态 grep 为第二形态锁,本 plan 不预写)。
</verification>

<success_criteria>
- 列表请求直连 200(字面量证明 + 活体证据已在 RESEARCH 存档);
- 回归锁满足「删斜杠必红」的防假绿标准;
- approve 调用与分页逻辑零行为变化。
</success_criteria>

<output>
Create `.planning/phases/61-audit-debt-clearance/61-02-SUMMARY.md` when done
</output>
