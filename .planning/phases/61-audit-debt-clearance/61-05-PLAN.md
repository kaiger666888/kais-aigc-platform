---
phase: 61-audit-debt-clearance
plan: "05"
type: execute
wave: 2
depends_on: ["61-01", "61-02", "61-03", "61-04"]
files_modified:
  - scripts/verify-phase-61.ts
  - package.json
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DEBT-01, DEBT-02, DEBT-03, DEBT-04]
must_haves:
  truths:
    - "npm run verify:phase-61 一条命令对四笔债全部验收:S 静态锁(DEBT-01..04 各自 grep 锚)+ B 行为门(root tsc/两包 vitest/reviewBridge node:test/infinite-canvas build/phase61 e2e)+ F forced-failure 自检(三个变异样本必使对应锁判 false),exit 0(58/59/60 范式)"
    - "门能红: S1/S2/S4 三锁各有内存变异样本证明非恒真(F 段与 S 段同源检查函数,T-60-09 假绿缓解纪律)"
    - "门内零 live probe:无 :8090/:10588 探测(orchestrator 裁定——四债全可 mock/静态锁定;review-nginx 活体证据已存档 61-RESEARCH)"
    - "D-04 销账: REQUIREMENTS.md 四项 DEBT 勾选 + Traceability 表 Pending→Complete(在门全绿之后执行)"
  artifacts:
    - path: "scripts/verify-phase-61.ts"
      provides: "verify:phase-61 聚合门(S1-S5 静态锁 + B1-B6 行为门 + F1-F3 forced-failure,spawnSync 子进程纪律)"
      contains: "verify-phase-61"
    - path: "package.json"
      provides: "verify:phase-61 script 注册"
      contains: "verify:phase-61"
  key_links:
    - from: "scripts/verify-phase-61.ts S 段"
      to: "61-01..04 四笔产物文件"
      via: "每债至少一正一负 grep 锚(正: 语义标记在场;负: 反模式/退役面零残留)"
      pattern: "assert\\("
    - from: "scripts/verify-phase-61.ts B 段 runCmd"
      to: "spawnSync 子进程"
      via: "49-01 教训:不与父进程共享 knex/事件循环;Pitfall 9"
      pattern: "spawnSync"
---

<objective>
Phase 61 收口门: verify:phase-61 聚合门把四笔债的静态锁/行为门/forced-failure 自检统一为
一条命令(verify-phase-60.ts 同骨架),随后执行 D-04 销账(REQUIREMENTS.md 勾选 + Traceability 更新)。

Purpose: v3.1「守护传统延续」决策的最后一环——每笔债的回归锁不再是散落断言,而是 phase 验收门。
Output: scripts/verify-phase-61.ts + package.json 注册 + REQUIREMENTS.md 销账。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-audit-debt-clearance/61-CONTEXT.md
@.planning/phases/61-audit-debt-clearance/61-RESEARCH.md
@.planning/phases/61-audit-debt-clearance/61-01-SUMMARY.md
@.planning/phases/61-audit-debt-clearance/61-02-SUMMARY.md
@.planning/phases/61-audit-debt-clearance/61-03-SUMMARY.md
@.planning/phases/61-audit-debt-clearance/61-04-SUMMARY.md
@.planning/phases/61-audit-debt-clearance/61-DEBT-04-VERDICT.md

<interfaces>
<!-- 聚合门骨架契约(verify-phase-60.ts 已核,逐段照抄结构) -->

verify-phase-60.ts 模板(L1-116 已核):
  assert(cond, name, detail) 收集 TestResult + console PASS/FAIL;
  read(rel)/exists(rel) 仓库根相对读;runCmd(name, cwdRel, cmd, tailLines) =
  spawnSync(cmd, {cwd, shell:true, timeout:300_000, maxBuffer:16MB}) 非零 exit 红
  (49-01 教训: 行为门一律子进程,禁与父进程共享 knex/事件循环——Pitfall 9);
  F 段: 锁检查函数纯函数化(输入源文本字符串,输出 LockOutcome),对同一函数跑内存变异样本
  (字符串替换,不写盘)——变异样本被判 true(锁恒真)→ 整门 exit 1;
  末尾失败计数 → process.exit(0 全绿 / 1 任一失败 / 2 crash)。

B4 node:test 判定范式(scripts/verify-phase-49-core.ts L154-166 已核):
  spawnSync(process.execPath, ['--import','tsx','--test',
  'src/lib/__tests__/reviewBridge.test.ts']) → 输出正则 /ℹ pass (\d+)/ 与 /ℹ fail (\d+)/
  解析计数,exit 0 且 fail=0 为绿。

S 段锁锚(全部来自 61-01..04 产物,规划期已定规格):
  S1 (DEBT-02): src/lib/reviewBridge.ts —— 正: /\/api\/v1\/reviews\/\?/ ≥ 2 处
    (模块头契约注释 + L182 代码字面量);负: /\/api\/v1\/reviews\?/ 0 处(裸斜杠零残留)。
  S2 (DEBT-04): FlowCanvas.tsx onNewAsset 块(slice 于 'onNewAsset:' 与
    'onOrchestrateStart' 之间,内容锚非行号——61-04 verdict 规格)内 'addNodeFromSocket' ≥1
    且 'setNodes' 0 处;useCanvasSocket.ts 含 socket.on('node:created' 与 payload?.node。
  S3 (DEBT-01): FlowCanvas.tsx "anchor: 'source'" 恰 1 处 + onDrop 处理器在场;
    canvasApi.ts 含 placeAssetNode 与 '/canvas/v2/nodes/';AssetLibrary.tsx 含
    ASSET_DRAG_MIME 且 draggable;负(全 packages/infinite-canvas/src/ 递归):
    placeAssetOnCanvas / handleAddToCanvas / am-card__add 三 token 均零命中;
    mock server.mjs 含两新路由;test/e2e/tests/phase61-debt.mjs 存在。
  S4 (DEBT-03): packages/flowgraph-v3/ts/src/migrate.ts 计数锁 ——
    'd.emotion != null' == 2(audio 既有 + script 新增)/ 'd.promptMeta != null' == 1 /
    'd.murchGrade != null' == 1 / 'd.archetype != null' == 1 / 'd.viewAngle != null' == 1
    (58-04 计数锁纪律: == 而非 ≥,防重复接线)。
  S5 (DEBT-04 文档): .planning/phases/61-audit-debt-clearance/61-DEBT-04-VERDICT.md
    存在且含 'Branch A' 与 'addNodeFromSocket'。

B 段命令(cwd 相对仓库根):
  B1 根 npx tsc --noEmit | B2 cd packages/flowgraph-v3 && npm test |
  B3 cd packages/infinite-canvas && npm test | B4 node --import tsx --test
  src/lib/__tests__/reviewBridge.test.ts(49-01 计数判定)|
  B5 cd packages/infinite-canvas && npm run build(dist 纪律,地雷 #10)|
  B6 cd packages/infinite-canvas && npx playwright test test/e2e/tests/phase61-debt.mjs

F 段变异样本(三锁各一,内存字符串):
  F1 = S1 样本删尾斜杠 → checkS1 false;F2 = S2 块样本 addNodeFromSocket→setNodes 替换
  → checkS2 false;F3 = S4 样本删一行 promptMeta → checkS4 false。

REQUIREMENTS.md 销账面: DEBT-01..04 四行 checkbox + Traceability 表四行
  Status Pending→Complete(仅这 8 处编辑,其余逐字不动)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/verify-phase-61.ts 聚合门 + package.json 注册</name>
  <files>scripts/verify-phase-61.ts, package.json</files>
  <read_first>
  - scripts/verify-phase-60.ts L1-180(门骨架: assert/read/exists/runCmd/LockOutcome 纯函数/checkFlag1Order 次序锁范本——本门结构逐段照抄)
  - scripts/verify-phase-54.ts L185-200(S1 尾斜杠静态锁先例: uriLog.every 断言形态)
  - scripts/verify-phase-49-core.ts L154-166(node:test spawn + pass/fail 正则判定)
  - package.json L52-53(verify:phase-59/60 注册位——61 插在其后)
  - 61-DEBT-04-VERDICT.md 静态锁规格节(S2 锁的内容锚口径)
  - 61-01/02/03-SUMMARY.md(各债实际落地形态——锁锚与产物对齐)
  </read_first>
  <action>
  (1) 新建 scripts/verify-phase-61.ts: 头注释(逐 60 范式)写明 S1-S5/B1-B6/F1-F3 地图与
      exit 0/1/2 契约、门内零 live probe 裁定(四债 mock/静态可锁;:8090 活体证据存档 61-RESEARCH)。
  (2) 骨架照抄 verify-phase-60: TestResult/assert/read/exists/runCmd(300s/16MB/spawnSync)+ warnCount 不需要(无 D 段)。
  (3) S 段: S1-S5 按 <interfaces> 规格实现;S1/S2/S4 的检查逻辑写成导出式纯函数
      (checkSlashLock(text) / checkNewAssetChain(flowSrc, socketSrc) / checkMetaCounts(text))
      供 F 段对同函数跑变异样本(锁与自检同源,非两套逻辑——60-05 F 段纪律)。
      S3 负向扫描用递归 readdir 收集 packages/infinite-canvas/src 全部 .ts/.tsx 后逐文件
      indexOf 三退役 token(60 S4 suppressGraphSaved 零命中扫法同款)。
  (4) B 段: B1-B6 按 <interfaces> 命令表;B4 用 process.execPath + ['--import','tsx',
      '--test', ...] 直拼(不经 shell,49-01 范式)并解析 /ℹ pass/ℹ fail 计数断言 fail=0;
      B5 build 在 B6 e2e 之前(dist 纪律次序锁)。
  (5) F 段: F1-F3 三变异样本(纯内存字符串替换,不写盘): 每样本断言对应 check 函数返回
      ok:false——任一变异样本被判 true → 直接置全局失败(门恒真 = 门无效)。
  (6) 末尾: 汇总打印 PASS/FAIL 计数 + 逐项明细已由 assert 打出;process.exit(fail>0?1:0),
      顶层 catch → exit 2。
  (7) package.json scripts 注册 "verify:phase-61": "npx tsx scripts/verify-phase-61.ts"
      (插在 verify:phase-60 行后,保持字典序段)。
  (8) 首跑: npm run verify:phase-61 —— 预期全绿(61-01..04 已在 wave 1 落地);任何红先分诊
      是锁锚写错(修脚本)还是真回归(修产品代码),禁放宽断言迁就。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npm run verify:phase-61</automated>
  </verify>
  <acceptance_criteria>
  - npm run verify:phase-61 exit 0,S1-S5 + B1-B6 + F1-F3 全 PASS(明细逐项打印);
  - F 段三变异样本全部被判 false(变异样本 judged-true 计数为 0);
  - 门内无 :8090/:10588/任何 live URL 字面量(grep verify-phase-61.ts 'localhost:80\|:10588\|:8090' 零命中);
  - package.json 注册在位且 npm run 能解析;
  - B 段全部 spawnSync 子进程(脚本内无对 app/knex 的 import)。
  </acceptance_criteria>
  <done>verify:phase-61 聚合门成为四债的 phase 验收门,可红可绿自证(F 段)。</done>
</task>

<task type="auto">
  <name>Task 2: 门全绿复核 + D-04 销账(REQUIREMENTS.md)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
  - .planning/REQUIREMENTS.md(全文 82 行——DEBT 四行 checkbox 位置 L30-33 + Traceability 表 L78-81)
  - 61-DEBT-04-VERDICT.md D-04 清偿标注节(销账动作与 verdict 记录一致性)
  - 61-CONTEXT.md D-04(销账口径原文)
  </read_first>
  <action>
  (1) 复跑 npm run verify:phase-61 确认仍 exit 0(销账前置条件——两连绿,排除环境侥幸)。
  (2) REQUIREMENTS.md 编辑恰 8 处:
      a. `- [ ] **DEBT-01**..` ~ `- [ ] **DEBT-04**..` 四行 checkbox → `- [x]`;
      b. Traceability 表 DEBT-01..04 四行 Status Pending → Complete。
      其余内容(含 Future/Out of Scope 节)逐字不动。
  (3) SUMMARY 记录: 门两连绿明细计数 + 销账 8 处 diff 摘要;标注 51-REVIEW finding 级清偿
      记录位于 61-DEBT-04-VERDICT.md(A6 降级条款,I5)与 61-03-SUMMARY(I1)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npm run verify:phase-61 && grep -c "\- \[x\] \*\*DEBT-0" .planning/REQUIREMENTS.md && grep -c "| DEBT-0[1-4] | Phase 61 | Complete |" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
  - verify:phase-61 两连绿(exit 0 × 2);
  - REQUIREMENTS.md 中 [x] DEBT 计数 = 4,Traceability Complete 计数 = 4;
  - 除 8 处销账编辑外 REQUIREMENTS.md 零其他 diff。
  </acceptance_criteria>
  <done>Phase 61 四债销账闭环: 修复→守护→门验收→登记,全部成文可查。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify 脚本 → 子进程命令执行 | runCmd/spawnSync 执行仓内固定命令;无外部输入拼入命令串 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-11 | Tampering | 门命令注入 | mitigate | 全部命令为脚本内字面量,B4 参数数组直拼不经 shell;无用户输入进 spawn |
| T-61-12 | Repudiation | 门假绿(锁恒真) | mitigate | F 段 forced-failure 三变异样本,变异 judged-true → exit 1(60-05 纪律) |
| T-61-13 | DoS | 门挂起 | mitigate | runCmd timeout 300_000 + maxBuffer 16MB;B 段子进程隔离(49-01/Pitfall 9) |
| T-61-SC | Tampering | npm/pip 安装 | accept | 零新依赖;tsx/playwright/vitest 均仓内既有 |
</threat_model>

<verification>
- npm run verify:phase-61 两连绿,S/B/F 三段明细齐;
- REQUIREMENTS.md 四债勾选 + Traceability Complete;
- 门脚本零 live probe、零 app import、F 段自证可红。
</verification>

<success_criteria>
- 四笔债各有一正一负静态锁 + 行为门覆盖,聚合为单命令验收;
- 门本身有可失败证明(forced-failure);
- v3.1 里程碑的最后一组 requirement(DEBT-01..04)状态登记完毕,可进 /gsd:verify-work。
</success_criteria>

<output>
Create `.planning/phases/61-audit-debt-clearance/61-05-SUMMARY.md` when done
</output>
