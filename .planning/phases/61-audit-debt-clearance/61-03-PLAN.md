---
phase: 61-audit-debt-clearance
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/flowgraph-v3/ts/src/migrate.ts
  - packages/flowgraph-v3/ts/tests/migrate.test.ts
  - packages/infinite-canvas/src/v3/__tests__/serialize.test.ts
autonomous: true
requirements: [DEBT-03]
must_haves:
  truths:
    - "保存含 emotion/promptMeta/murchGrade/archetype/viewAngle 的节点 meta 后 reload,五个字段值在 canonical V3 meta 读回一致(buildMeta 读侧缺口补齐,51-REVIEW I1,DEBT-03)"
    - "读回修复发生在 migrate 层(buildMeta 分支内),非客户端 post-hoc 补丁——V3 直通/fixture 图(无 rawData 袋)同样保真"
    - "script 分支 emotion 是 number、audio 分支 emotion 是 string 的双类型契约被测试锁死(zod strict 判别联合是回归网,Pitfall 3)"
    - "往返断言打在 adapt∘serialize 之后的 canonical meta 字段(rawDataByNodeId=null 最严格档,raw 透传假绿防线,Pitfall 4)"
  artifacts:
    - path: "packages/flowgraph-v3/ts/src/migrate.ts"
      provides: "buildMeta 四分支(script/storyboard/video/global)各补 1-2 行条件展开读回"
      contains: "d.emotion != null"
    - path: "packages/flowgraph-v3/ts/tests/migrate.test.ts"
      provides: "五字段读回单测(五 stage 各一)+ validateFlowGraphV3 逐例校验"
      contains: "promptMeta"
    - path: "packages/infinite-canvas/src/v3/__tests__/serialize.test.ts"
      provides: "adapt∘serialize 集成往返用例(rawDataByNodeId=null 档,canonical meta 断言)"
      contains: "rawDataByNodeId"
  key_links:
    - from: "serialize.ts flattenMeta(L171-209,写侧,零改动)"
      to: "migrate.ts buildMeta(L246-310,读侧,本 plan 补)"
      via: "flattenMeta {...rest} 已摊平全部 meta 字段;buildMeta 分支按 stage 拣选读回——本次补齐 5 个漏拣字段"
      pattern: "d\\.(emotion|promptMeta|murchGrade|archetype|viewAngle) != null"
    - from: "serialize.test.ts 往返用例"
      to: "serializeGraphToV2(g3, null, undefined) + adaptV2Graph"
      via: "rawDataByNodeId=null → 纯 flattenMeta 输出(最严格:无 raw 兜底)"
      pattern: "serializeGraphToV2\\(.*null"
---

<objective>
DEBT-03 外科手术: flowgraph-v3 migrate.ts buildMeta 四分支补 5 个持久化字段读回
(script.emotion(number)/storyboard.promptMeta/video.murchGrade/global.archetype+viewAngle),
两侧测试锁死——flowgraph-v3 单元级(migrate 直测)+ infinite-canvas 集成级
(adapt∘serialize 往返,raw=null 档)。

Purpose: 清偿 51-REVIEW I1——写侧 flattenMeta 无缺口,读侧 buildMeta 漏拣导致 reload 后 meta 5 字段静默丢失。
Output: 5 行条件展开 + 两包测试用例,三面(两包 vitest + root tsc)全绿(Pitfall 10)。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-audit-debt-clearance/61-CONTEXT.md
@.planning/phases/61-audit-debt-clearance/61-RESEARCH.md

<interfaces>
<!-- 已提取契约 -->

buildMeta 现状(packages/flowgraph-v3/ts/src/migrate.ts L246-314,已核):
  switch(stage) 各分支以 ...(d.X != null ? { X: d.X } : {}) 句式拣选 flat data 字段。
  补丁点(照抄同文件 L284 audio emotion 句式,Pattern 4):
    case 'script'(L248-255): + ...(d.emotion != null ? { emotion: d.emotion } : {})  ← number!
    case 'storyboard'(L256-269): + ...(d.promptMeta != null ? { promptMeta: d.promptMeta } : {})  ← PromptFacets 对象直传
    case 'video'(L272-277): + ...(d.murchGrade != null ? { murchGrade: d.murchGrade } : {})  ← string
    case 'global'(L304-307): + ...(d.archetype != null ...) 与 ...(d.viewAngle != null ...)  ← 各 string
  audio 分支(L278-286)已有 emotion(string)——不动,其存在正是 Pitfall 3 双类型陷阱面。

类型/zod 契约(types.ts L112-129 + zod.ts L149-189,已核,零 schema 改动):
  script meta: emotion?: number | audio(voice/foley/bgm) meta: emotion?: string
  storyboard: promptMeta?: PromptFacets(zod promptFacetsSchema)
  video: murchGrade?: string | global: archetype?: string; viewAngle?: string
  五字段全部 optional 在场——补读不改 schema,zod strict 判别联合在类型错配时打回(正好当回归网)。
  PromptFacets 精确键集:执行时读 types.ts L135+ PromptFacets interface(7-facet),测试用完整合法对象。

测试挂点 A(packages/flowgraph-v3/ts/tests/migrate.test.ts,已核头部):
  既有范式: import v2Sample fixture + migrateV2toV3 + asset(graph,id) 查节点 +
  validateFlowGraphV3(graph)(zod.js 导出)。新用例按需内联构造最小单节点
  FlowGraphV2Export(形状参照 fixtures/v2-export.sample.json + src/v2types.ts:
  meta/nodes[{id,type,branchId,position,size,state,data}]/links/branches)。

测试挂点 B(packages/infinite-canvas/src/v3/__tests__/serialize.test.ts,已核头部):
  既有工具链: asset(partial) V3 资产构造器 + buildGraph + serializeGraphToV2 +
  adaptV2Graph + FlowGraphV2Schema.safeParse;往返断言必须
  serializeGraphToV2(g3, null, undefined)(第二参 rawDataByNodeId=null——Pitfall 4 关键)
  → JSON.parse(JSON.stringify(wire)) → adaptV2Graph → 找 node 断言 meta 字段 toEqual 原值。

写侧事实(serialize.ts L171-209 flattenMeta,零改动,理解用):
  const { stage, ...rest } = meta; out = { ...rest }——写侧天然摊平全部 meta 字段,
  损失只发生在读侧 buildMeta 漏拣;serialize 公式 {...raw, ...flattenMeta} 意味着带 raw 袋时
  wire 层看似保真(透传假绿),唯 raw=null 档才暴露真缺口。

三面测试口径(Pitfall 10,v3.0 审计「3× tsc clean」同款):
  cd packages/flowgraph-v3 && npm test(= cd ts && vitest run)
  cd packages/infinite-canvas && npm test(vitest run)
  仓库根 npx tsc --noEmit(flowgraph-v3 exports 是 ts 源码直消费,两消费方 + 自身包三面都要绿)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: buildMeta 四分支 5 行读回 + flowgraph-v3 五用例</name>
  <files>packages/flowgraph-v3/ts/src/migrate.ts, packages/flowgraph-v3/ts/tests/migrate.test.ts</files>
  <read_first>
  - packages/flowgraph-v3/ts/src/migrate.ts L246-314(buildMeta 全函数——四个补丁点 + L284 句式范本)
  - packages/flowgraph-v3/ts/src/types.ts L110-160(AssetStageMeta 判别联合 + PromptFacets 精确键集)
  - packages/flowgraph-v3/ts/src/zod.ts L143-190(assetStageMetaSchema 各分支 optional 在场证据)
  - packages/flowgraph-v3/ts/tests/migrate.test.ts L1-80(既有用例范式 + asset()/event() 工具 + validateFlowGraphV3)
  - packages/flowgraph-v3/fixtures/v2-export.sample.json + packages/flowgraph-v3/ts/src/v2types.ts(内联构造 FlowNodeV2 的最小合法形状)
  </read_first>
  <action>
  (1) migrate.ts 四分支各补条件展开(照 L284 audio emotion 同款句式,零新范式):
      script 分支 premise 行后 + d.emotion(number,勿 cast——zod strict 即类型网,Pitfall 3);
      storyboard 分支 pacing 行后 + d.promptMeta(对象直传);
      video 分支 observedEndState 行后 + d.murchGrade;
      global 分支 assetType 行前 + d.archetype 与 d.viewAngle 两行。
      仅此 5 行,函数其余逐字不动。
  (2) migrate.test.ts 新增 describe('DEBT-03 buildMeta 5 字段读回(61-03)'):
      每用例内联构造最小 FlowGraphV2Export(单节点 + 空 links/branches)→ migrateV2toV3 →
      asset(graph, id) 断言 meta 字段 + validateFlowGraphV3(graph) 通过:
      a. script: type:'script', data:{emotion: 7, hookType:'悬念'} → meta.emotion === 7
         且 typeof === 'number'(Pitfall 3 正断言);
      b. storyboard: data:{shotType:'close-up', durationS:4, promptMeta:<完整合法 PromptFacets,
         键集照 types.ts>} → meta.promptMeta toEqual 原对象(deep equal);
      c. video: data:{shotId:'shot-001', murchGrade:'A'} → meta.murchGrade === 'A';
      d. global: data:{assetType:'role', archetype:'sage', viewAngle:'front'} →
         meta.archetype === 'sage' && meta.viewAngle === 'front';
      e. audio 回归锁: type:'audio', data:{audioType:'voice', emotion:'激动'} →
         stage 'voice' 且 meta.emotion === '激动'(string;双类型契约另一半)。
      另加一行负向: script data 无 emotion → meta 不含 emotion 键(undefined)。
  (3) 跑本包测试与自身 typecheck:cd packages/flowgraph-v3 && npm test && npm run typecheck
      (ts package.json: typecheck = tsc --noEmit)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/flowgraph-v3 && npm test && npm run typecheck && grep -c "d.emotion != null\|d.promptMeta != null\|d.murchGrade != null\|d.archetype != null\|d.viewAngle != null" ts/src/migrate.ts</automated>
  </verify>
  <acceptance_criteria>
  - npm test 全绿(既有用例零回归 + 新增 ≥6 用例绿);
  - grep 五句式命中 ≥6 行(emotion 2 处:audio 既有 + script 新增;其余各 1);
  - script 用例断言 typeof meta.emotion === 'number'(类型陷阱有正断言);
  - validateFlowGraphV3 在每用例内被调用且通过。
  </acceptance_criteria>
  <done>读侧缺口在 migrate 层补齐,flowgraph-v3 包内单测锁死五字段 + 双类型契约。</done>
</task>

<task type="auto">
  <name>Task 2: infinite-canvas 集成往返用例(raw=null 档)+ 三面收口</name>
  <files>packages/infinite-canvas/src/v3/__tests__/serialize.test.ts</files>
  <read_first>
  - packages/infinite-canvas/src/v3/__tests__/serialize.test.ts L1-100(asset() 构造器/buildGraph/断言组 a-g 全景——新用例挂尾)
  - packages/infinite-canvas/src/v3/serialize.ts L160-260(flattenMeta {...rest} 摊平 + serializeGraphToV2 签名:第二参 rawDataByNodeId)
  - packages/infinite-canvas/src/v3/adapter.ts L518+(adaptV2Graph 签名与返回 {graph, warnings,...})
  - packages/flowgraph-v3/ts/src/types.ts L135+(PromptFacets 键集——往返用例构造值)
  </read_first>
  <action>
  (1) serialize.test.ts 新增 it('DEBT-03(61-03): 五字段 meta save→reload 往返保真(raw=null 最严格档)'):
      用既有 asset() builder 构造含五字段的 V3 图(script.meta.emotion=7(number)/
      storyboard.meta.promptMeta=<完整 PromptFacets>/video.meta.murchGrade='A'/
      global.meta.archetype='sage'+viewAngle='front';其余字段沿用 buildGraph 既有值习惯)→
      wire = serializeGraphToV2(g3, null, undefined)(第二参 null 是关键——带 raw 袋时
      {...raw,...flattenMeta} 透传会掩盖读侧缺口,Pitfall 4)→
      back = adaptV2Graph(JSON.parse(JSON.stringify(wire))) →
      五节点各找 back.graph 节点,断言 meta 对应字段 toEqual 原值(canonical meta 层断言,
      禁断 wire body/rawData 层——Anti-Pattern:Pitfall 4 警示标志)。
  (2) 三面收口(Pitfall 10):cd packages/infinite-canvas && npm test(全量,证明既有 a-g 断言组
      零回归)→ cd packages/flowgraph-v3 && npm test → 仓库根 npx tsc --noEmit。
      顺序先单文件调试(npx vitest run src/v3/__tests__/serialize.test.ts)再全量。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/v3/__tests__/serialize.test.ts && npm test && cd ../flowgraph-v3 && npm test && cd /data/workspace/kais-aigc-platform && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
  - serialize.test.ts 新用例绿且断言面全部在 back.graph.nodes[i].meta.*(canonical 层);
  - serializeGraphToV2 第二参传 null(用例源码可见,grep 'serializeGraphToV2(' 新用例处第二参为 null);
  - 两包 npm test 全量绿(既有断言组 a-g 零回归);
  - 根 tsc --noEmit exit 0(三面收口)。
  </acceptance_criteria>
  <done>DEBT-03 双面证据在位:单元级(migrate 直测)+ 集成级(adapt∘serialize 往返,raw=null 档),
  三面(两包 vitest + root tsc)全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (无新边界) | 纯前端数据层读回映射,无网络/存储/输入面变化 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-08 | Tampering | 恶意 wire data 字段类型注入 meta | mitigate | zod strict 判别联合既有门(类型错配打回);本次仅扩读不改校验面 |
| T-61-09 | Tampering | promptMeta 对象深引用别名 | mitigate | 测试经 JSON.parse(JSON.stringify(wire)) 切引用链;flattenMeta 展开已是浅拷贝语义 |
| T-61-SC | Tampering | npm/pip 安装 | accept | 零新依赖 |
</threat_model>

<verification>
- 两包 npm test 全量绿 + 根 tsc --noEmit 绿(三面口径);
- buildMeta 五句式 grep 计数符合(emotion×2 / 其余×1);
- (61-05 聚合门 S4 将对五句式做计数静态锁 + forced-failure 变异样本)。
</verification>

<success_criteria>
- 含五字段的节点 save→reload 后 canonical meta 值一致(往返用例证明);
- script number / audio string 双类型契约有正断言;
- 三面(两包 + root tsc)零回归。
</success_criteria>

<output>
Create `.planning/phases/61-audit-debt-clearance/61-03-SUMMARY.md` when done
</output>
