---
phase: 61-audit-debt-clearance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/infinite-canvas/test/e2e/mock-backend/server.mjs
  - packages/infinite-canvas/test/e2e/tests/phase61-debt.mjs
  - packages/infinite-canvas/src/services/canvasApi.ts
  - packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
  - packages/infinite-canvas/src/main.tsx
autonomous: true
requirements: [DEBT-01]
must_haves:
  truths:
    - "用户在资产中心抓住资产卡片拖到顶栏「画布」页签,视图切到画布,继续拖到画布面板上松手——节点出现在拖放点附近(各轴 ≤64px 有界,DEBT-01/D-01)"
    - "拖入产出的节点走 POST /api/canvas/v2/nodes/ 落库(mock /__mock/calls 可见调用记录),id 遵循 asset-${a.id} 约定,服务端广播 node:created 后经 addNodeFromSocket 进 canonical graph(Pitfall 8)"
    - "同一资产二次拖入 → 服务端 409 → toast「已在画布」提示,画布上该 id 节点仍恰 1 个"
    - "「＋ 画布」按钮与 handleAddToCanvas/placeAssetOnCanvas stub 全部退役——拖入是 anchor='source' 唯一活调用方(D-01 sole-caller 纪律,Pitfall 6)"
    - "placeNewAsset 本体零改动(既有 8 用例单测原样绿;A2 裁定:PLACE_GRID.source=4px 既有语义胜出,D-01 文字「8px」不落地为代码改动)"
  artifacts:
    - path: "packages/infinite-canvas/src/services/canvasApi.ts"
      provides: "placeAssetNode(POST /canvas/v2/nodes/ 真封装,409 语义化)+ ASSET_DRAG_MIME 常量 + AssetDragPayload 类型;placeAssetOnCanvas stub 删除"
      contains: "placeAssetNode"
    - path: "packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx"
      provides: "资产卡片 draggable + dragstart MIME 载荷;「＋ 画布」按钮/handler 退役"
      contains: "ASSET_DRAG_MIME"
    - path: "packages/infinite-canvas/src/components/FlowCanvas.tsx"
      provides: "画布页签 dragover 切视图 + ReactFlow onDragOver/onDrop → placeNewAsset(anchor='source') → placeAssetNode"
      contains: "anchor: 'source'"
    - path: "packages/infinite-canvas/test/e2e/mock-backend/server.mjs"
      provides: "POST /api/canvas/v2/nodes/(zod-lite 校验+409 查重+node:created 回放)+ POST /api/v1/assets-registry/search(已知 fixture)"
      contains: "assets-registry/search"
    - path: "packages/infinite-canvas/test/e2e/tests/phase61-debt.mjs"
      provides: "拖入有界落点 / 409 重复 / stub 退役 三用例 e2e"
      contains: "phase61"
  key_links:
    - from: "AssetLibrary.tsx 卡片 onDragStart"
      to: "dataTransfer(application/x-kais-asset)"
      via: "JSON 载荷 {id,uuid,name,assetType,filePath}"
      pattern: "setData\\(ASSET_DRAG_MIME"
    - from: "FlowCanvas.tsx onDrop"
      to: "placeNewAsset(anchor='source')"
      via: "screenToFlowPosition(drop point) 作 sourcePosition"
      pattern: "anchor: 'source'"
    - from: "FlowCanvas.tsx onDrop"
      to: "canvasApi.placeAssetNode → POST /canvas/v2/nodes/"
      via: "apiCall('/canvas/v2/nodes/', {projectId, episodesId, node})"
      pattern: "placeAssetNode\\("
    - from: "mock server.mjs POST /nodes"
      to: "broadcastToProject('node:created', {node})"
      via: "setTimeout 5ms 回放(save-v2 同款)"
      pattern: "node:created"
    - from: "phase61-debt.mjs"
      to: "window.__kaisCanvas.getGraph()/screenToFlow()"
      via: "canonical position 有界断言(rfNodes 是布局缓存禁断言,Anti-Pattern 2)"
      pattern: "getGraph"
---

<objective>
DEBT-01 落地: placeNewAsset(anchor='source') 获得唯一活调用方——资产中心拖资产卡入画布,
拖放点锚定的有界落位 + 真持久化(现成 POST /api/canvas/v2/nodes/)+ 同步处置 stub 链
(「＋ 画布」按钮 / handleAddToCanvas / placeAssetOnCanvas 假实现),附三用例 e2e。

Purpose: 清偿 51 里程碑审计 TD-3——放置纯函数一直无 source 锚活调用方,资产中心放置入口是恒 true 占位。
Output: 拖入接线(4 生产文件)+ mock 两条新路由 + phase61-debt.mjs e2e。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-audit-debt-clearance/61-CONTEXT.md
@.planning/phases/61-audit-debt-clearance/61-RESEARCH.md

<interfaces>
<!-- 已提取契约——执行者直接消费,无需再探索 -->

placeNewAsset(packages/infinite-canvas/src/utils/placeNewAsset.ts,零改动复用):
  placeNewAsset(opts: { sourcePosition?: {x,y}|null; viewportCenter: {x,y}; anchor?: 'source'|'center' })
  → {x,y};source 分支 = snap(source + {24,−16}, 4px 网格);PLACE_OFFSET/PLACE_GRID 已导出;
  非有限 source 防御性落 center 分支(T-55-02)。

服务器通道(src/routes/canvas/v2/nodes.ts L48-97,零后端改动):
  POST /api/canvas/v2/nodes/  body = { projectId:number, episodesId:number, node: {
    id?(string), type:'asset'(在 NODE_TYPE_ENUM), branchId:string, phaseIndex:int≥0, phaseName:string,
    position:{x,y}必填, size:{width,height}, data:record, state:'idle'... } }
  → 200 {code:200,data:{node}} + broadcastToProject(projectId,'node:created',{node});
    id 重复 → 409;data 过 validateNodeData('asset', data):filePath/label/assetType 均
    string.min(1).nullish()(canvasAssetSchema L109-115)——三者给非空字符串即合法。

客户端 API 骨架(canvasApi.ts):
  apiCall<T>(path, body, opts) —— path 相对 API_BASE 无 /api 前缀(如 '/canvas/v2/save-v2');
  4xx → 立即 throw ApiError(type:'business', .status=res.status, 不重试);5xx/网络错误才重试。
  saveCanvasGraph(L338)是 apiCall POST 范式直接照抄对象。
  退役面: placeAssetOnCanvas(L1202-1209,恒 true stub)——本 plan 删除改造。

客户端写回链(全部现成,零改动):
  useCanvasSocket L188-195 socket.on('node:created', {node}) → FlowCanvas onNewAsset L284-304:
  服务端 position 有限即用(真相优先)→ addNodeFromSocket(canvasStore L811-842):
  adaptV2Node → id 查重(重播 warn+ignore)→ setGraph canonical 重建 + rawData 袋注入。

视图结构(本 plan 关键事实,RESEARCH 未覆盖的补充裁定):
  viewMode 互斥(store 默认 'assets')——资产中心(AssetManager→AssetLibrary)与 ReactFlow
  不同时挂载;顶栏视图切换簇(FlowCanvas L986-1001)在两种视图下都挂载。
  「画布」页签 = getByRole('button', {name:'画布', exact:true})(helpers.mjs switchToCanvasView 同源)。
  ⇒ 拖入机制(P3 裁定,plan 内 pin):卡片 dragstart 置 MIME 载荷 → 拖到「画布」页签上
  dragover 即 setViewMode('canvas')(Chromium 拖拽会话跨源元素卸载存活,dragend 仍发)→
  ReactFlow 挂载后继续拖到面板 drop。e2e 用合成 DragEvent(同一 DataTransfer 挂 window)
  驱动,不依赖浏览器原生会话存活;真实手感由 VALIDATION 既有 manual UAT 行(:10588)兜底。

资产中心数据面:
  useRealAssets → searchAssets → POST /v1/assets-registry/search(分页 limit 200,<200 即止);
  卡片 = .am-card + data-uuid(AssetLibrary renderCard L896);AssetDetail 必备字段
  id:number/uuid/name/type/filePath(canvasApi L203-222)。
  AssetItem.assetType 来源 = AssetItem.type(细/粗粒度字符串,服务端 schema 只要求 min(1))。

e2e 基建:
  helpers.mjs: test(自动 __mock/reset)/loadCanvas(切画布视图)/switchToCanvasView;
  loadCanvas 会点「画布」页签——拖入用例需留在 assets 视图:本地 helper 复制 loadCanvas 的
  goto+双导航序列但跳过 switchToCanvasView。
  mock server.mjs: state.canvas.nodes 数组 / logCall('POST', path, body, response) 落
  /__mock/calls / broadcastToProject(projectId,event,data)(L163)/ save-v2 路由(L191-212)
  是新 POST /nodes 路由的模板(setTimeout 5ms 广播 + logCall + res.json({code:200,...}))。
  地雷 #10: e2e 跑 dist——先 npm run build。

testMode 桥(main.tsx L31+):
  getGraph()/getViewCenter()/addNodeForTest 既有;60-04 getDetailNode 是只读 seam 先例。
  本 plan 增 screenToFlow 只读访问器(与 paneFlowCenter 同基准:pane rect 原点 + live viewport
  transform 换算,顶栏 48px 偏移已含在 rect.top)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 契约面 — mock 两条新路由 + phase61 e2e 骨架</name>
  <files>packages/infinite-canvas/test/e2e/mock-backend/server.mjs, packages/infinite-canvas/test/e2e/tests/phase61-debt.mjs</files>
  <read_first>
  - packages/infinite-canvas/test/e2e/mock-backend/server.mjs(全文 524 行——路由表/state/logCall/broadcastToProject/save-v2 模板/express.static 顺序:新路由必须放 /__mock 段之前)
  - packages/infinite-canvas/test/e2e/helpers.mjs(全文——loadCanvas/goto 序列/switchToCanvasView)
  - packages/infinite-canvas/test/e2e/tests/phase55-nav.mjs L1-60, L120-152(头部注释范式 + 有界断言面)
  - src/routes/canvas/v2/nodes.ts L48-97(真实 POST /nodes 语义——mock 镜像对象:409 查重/200 回 node/广播)
  - packages/infinite-canvas/src/services/canvasApi.ts L203-222(AssetDetail 字段——search fixture 形状)
  </read_first>
  <action>
  (1) server.mjs 增路由 POST /api/canvas/v2/nodes/(放 save-v2 路由之后、/__mock 段之前,注释标明
      "61-01 DEBT-01: 镜像真实 nodes.ts L48-97 最小语义"):
      取 {projectId, episodesId, node} = req.body;守卫 node?.id 为 string 且 node.position 的
      x/y 均为有限 number,否则 res.json({code:400, message:'节点载荷非法'});查重
      state.canvas.nodes.some(n => n.id === node.id) → res.json({code:409, message:'节点已存在'});
      否则 state.canvas.nodes.push({...node});logCall('POST', '/api/canvas/v2/nodes/',
      {projectId, episodesId, nodeId: node.id, x: node.position.x, y: node.position.y}, null);
      res.json({code:200, data:{node}});setTimeout(() => broadcastToProject(projectId,
      'node:created', {node: state.canvas.nodes.at(-1)}), 5)(save-v2 同款 5ms 回放)。
  (2) server.mjs 增路由 POST /api/v1/assets-registry/search:返回 {code:200, data:{assets:
      [两条固定 fixture]}};fixture 为完整 AssetDetail 形状,id 90001/90002、uuid
      'e2e-asset-90001'/'e2e-asset-90002'、name 'E2E拖入资产A'/'E2E拖入资产B'、type 'character'、
      filePath '/oss/e2e/asset-a.png'(非空——卡片缩略图路径)、projectId 1、isPrimaryView
      true/false 各一、state 'active',其余 nullable 字段全 null;同样 logCall。
      不校验查询参数(useRealAssets 分页 limit 200,<200 即止,fixture 2 条自然收敛)。
  (3) 新建 test/e2e/tests/phase61-debt.mjs:头部注释(61-01 三用例地图 + 地雷 #10 serve dist
      + 视图互斥裁定:拖入经「画布」页签 dragover 切视图,合成 DragEvent 驱动);本地 helper
      loadAssetCenter(page) = loadCanvas 的 goto+双 reset 导航序列但去掉 switchToCanvasView,
      等 locator('.am-card').first() 可见(fixture 经新 search 路由到达);
      三个 test 先立骨架(标题含 -g 组词 drag-in-bounded / drag-in-duplicate-409 /
      stub-disposed),用例体 Task 3 完成;文件顶部从 '../helpers.mjs' import { test, expect }。
  (4) 提交前 node --check 两个 .mjs 文件确认语法。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && node --check test/e2e/mock-backend/server.mjs && node --check test/e2e/tests/phase61-debt.mjs && grep -c "api/canvas/v2/nodes/" test/e2e/mock-backend/server.mjs && grep -c "assets-registry/search" test/e2e/mock-backend/server.mjs</automated>
  </verify>
  <acceptance_criteria>
  - server.mjs 两条新路由在场且 node --check 通过;新路由位于 express.static(DIST_DIR) 之前;
  - POST /nodes mock 具备 400(载荷非法)/409(查重)/200(append+node:created 回放)三分支;
  - search fixture 恰 2 条,含非空 filePath 与数字 id 90001/90002;
  - phase61-debt.mjs 存在三用例标题骨架,文件可被 node --check 解析。
  </acceptance_criteria>
  <done>DEBT-01 的 e2e 前置(mock 通道 + 测试骨架)落地,Task 2 生产接线后即可填用例体。</done>
</task>

<task type="auto">
  <name>Task 2: 生产接线 — canvasApi 真封装 + 卡片 draggable + stub 退役 + FlowCanvas onDrop</name>
  <files>packages/infinite-canvas/src/services/canvasApi.ts, packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx, packages/infinite-canvas/src/components/FlowCanvas.tsx</files>
  <read_first>
  - packages/infinite-canvas/src/services/canvasApi.ts L40-135(ApiError/apiCall 语义:4xx business 不重试,.status 可判 409)、L329-353(saveCanvasGraph 的 apiCall POST 范式)、L1196-1209(placeAssetOnCanvas stub——本任务删除)
  - packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx L1-40(import 段)、L780-835(handleAddToCanvas stub + handleLocateOnCanvas 不动区)、L883-935(renderCard 卡片 JSX + .am-card__add 按钮)
  - packages/infinite-canvas/src/components/FlowCanvas.tsx L141(useReactFlow 实例)、L271-304(useCanvasSocket onNewAsset——服务端 position 真相优先语义,不碰)、L984-1002(顶栏视图切换簇/「画布」ViewModeButton)、L1079-1102(ReactFlow props 段——onDrop/onDragOver 插入点)
  - packages/infinite-canvas/src/utils/placeNewAsset.ts(全文 43 行——零改动,仅 import)
  - packages/infinite-canvas/src/components/assetManager/assetManagerData.ts L33-70(AssetItem 字段:id/uuid/name/type/filePath)
  </read_first>
  <action>
  (1) canvasApi.ts:删除 placeAssetOnCanvas stub(L1196-1209);新增并导出:
      const ASSET_DRAG_MIME = 'application/x-kais-asset';
      interface AssetDragPayload { id: number; uuid: string; name: string; assetType: string; filePath: string | null }
      async function placeAssetNode(projectId: number, episodesId: number, node: Record<string, unknown>):
      Promise<{ ok: true } | { ok: false; status: number; message: string }> —— 内部
      await apiCall('/canvas/v2/nodes/', {projectId, episodesId, node});catch (err) 时
      err instanceof ApiError && err.status === 409 → {ok:false, status:409, message:'已在画布'};
      其他 ApiError/异常 → {ok:false, status:err.status ?? 0, message:err.message};绝不向调用方 throw
      (拖入是 fire-action,错误全部 toast 化)。JSDoc 注明:61-01 DEBT-01,替代恒 true stub,
      通道 = 既有 POST /api/canvas/v2/nodes/(server truth-first;客户端本地写会重演 I5 ephemeral 陷阱)。
  (2) AssetLibrary.tsx:renderCard 的 .am-card div(L897-904)加 draggable={true} 与
      onDragStart={(e) => { const a = assetDetailToItem 已有局部 a;若 a.id == null →
      e.preventDefault() + showToast('该资产缺少数字 id,无法拖入','warning') + return;
      e.dataTransfer.setData(ASSET_DRAG_MIME, JSON.stringify({id: a.id, uuid: a.uuid,
      name: a.name, assetType: a.type, filePath: a.filePath ?? null} as AssetDragPayload));
      e.dataTransfer.effectAllowed = 'copy' }};import 改从 canvasApi 引 ASSET_DRAG_MIME 与
      type AssetDragPayload(placeAssetOnCanvas import 移除)。
      退役(Pitfall 6/D-01 sole-caller):删除「＋ 画布」按钮 JSX(L911-915)与 handleAddToCanvas
      整段(L817-825);.am-card__locate 定位按钮与既有 handler 不动;头部注释 L18 的
      「添加到画布」行改为拖入语义说明。
  (3) FlowCanvas.tsx 顶栏:「画布」ViewModeButton(L986-987)加
      onDragOver={(e) => { if (e.dataTransfer.types.includes(ASSET_DRAG_MIME)) {
      e.preventDefault(); useCanvasStore.getState().setViewMode('canvas') } }} —— 注释 pin
      P3 裁定:视图互斥下拖入必经页签 dragover 切换;Chromium 拖拽会话跨源卸载存活,
      合成事件面由 e2e 驱动,真实手感有 :10588 manual UAT 行兜底;setViewMode 幂等(dragover
      连发无害)。其他 ViewModeButton 不动。
  (4) FlowCanvas.tsx ReactFlow props 增:
      onDragOver={(e) => { if (e.dataTransfer.types.includes(ASSET_DRAG_MIME)) {
      e.preventDefault(); e.dataTransfer.dropEffect = 'copy' } }}
      onDrop={handleAssetDrop}(具名 useCallback,放 onNewAsset 定义之后):
      a. try/catch 解析 e.dataTransfer.getData(ASSET_DRAG_MIME) 为 AssetDragPayload;
         id 非有限 number 或 name/uuid 非 string → showToast('拖入载荷无效','warning') return。
      b. projectId/episodesId 缺 → showToast('请先在顶栏选择项目和剧集,再拖入资产','warning') return。
      c. flowPoint = reactFlow.screenToFlowPosition({x: e.clientX, y: e.clientY});
         position = placeNewAsset({ sourcePosition: flowPoint,
         viewportCenter: reactFlow.screenToFlowPosition({x: window.innerWidth/2, y: window.innerHeight/2}),
         anchor: 'source' })(D-01;A2 裁定注记:PLACE_GRID.source=4px 既有语义优先,CONTEXT
         「8px」措辞不落地为代码改动)。
      d. node 构造:{ id: `asset-${payload.id}`, type: 'asset', branchId: 'main',
         phaseIndex: 0, phaseName: 'asset', position, size: {width: 240, height: 160},
         state: 'idle', data: { label: payload.name || payload.uuid, assetType: payload.assetType,
         filePath: payload.filePath ?? null } }(label/assetType 非空满足 canvasAssetSchema min(1);
         id 约定与 handleLocateOnCanvas 的 asset-${a.id} 同源,Pitfall 8)。
      e. result = await placeAssetNode(projectId, episodesId, node);
         result.ok → 不做本地写(服务端广播 node:created → onNewAsset 服务端 position 即本方
         position,真相优先,勿二次偏移——Anti-Pattern 3);result.status === 409 →
         showToast('该资产已在画布上','info');否则 showToast('放置失败: ' + result.message,'error')。
      f. handler 注释块:61-01 DEBT-01 / D-01 唯一 anchor='source' 活调用方 / WRITE-03 写回
         经广播+addNodeFromSocket,本 handler 零 setNodes。
  (5) main.tsx 桥增只读访问器(60-04 getDetailNode 同型 seam):screenToFlow: (p) => 在场时
      取 getLiveViewport(),pane=document.querySelector('.react-flow')?.getBoundingClientRect(),
      返回 {x: (p.x - (pane?.left ?? 0) - v.x)/v.zoom, y: (p.y - (pane?.top ?? 0) - v.y)/v.zoom}
      (与 paneFlowCenter 同基准;v 无效/zoom≤0 → null)。e2e 有界断言的锚换算面。
  (6) 自检:npm run build(tsc -b && vite build)通过;grep 确认 placeAssetOnCanvas/
      handleAddToCanvas/am-card__add 仓内 infinite-canvas src 零残留。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm run build && npx vitest run src/components/__tests__/placeNewAsset.test.ts && grep -rc "placeAssetOnCanvas\|handleAddToCanvas\|am-card__add" src/ | grep -v ":0" | wc -l</automated>
  </verify>
  <acceptance_criteria>
  - npm run build exit 0;placeNewAsset 既有 8 用例零改动全绿(本体未触碰);
  - grep placeAssetOnCanvas|handleAddToCanvas|am-card__add 在 packages/infinite-canvas/src/ 命中数为 0(wc -l 输出 0 行);
  - FlowCanvas.tsx 出现 anchor: 'source' 调用恰 1 处(onDrop 内);onNewAsset 段(L284-304)diff 为零;
  - canvasApi 导出 ASSET_DRAG_MIME/AssetDragPayload/placeAssetNode,三者被 AssetLibrary/FlowCanvas 实际 import;
  - placeAssetNode 对 409 返回 {ok:false,status:409},不 throw。
  </acceptance_criteria>
  <done>拖入全链生产代码在位:卡片 → 页签切视图 → 面板 drop → placeNewAsset(source) → POST /nodes;
  stub 链退役,placeNewAsset 本体零改动。</done>
</task>

<task type="auto">
  <name>Task 3: phase61 e2e 三用例全绿 + 放置面回归</name>
  <files>packages/infinite-canvas/test/e2e/tests/phase61-debt.mjs</files>
  <read_first>
  - 本 plan Task 1/Task 2 产物(骨架 + 接线在位)
  - packages/infinite-canvas/test/e2e/tests/phase55-nav.mjs L120-152(有界断言范本:expect.poll getGraph + 各轴 ≤64)
  - packages/infinite-canvas/test/e2e/helpers.mjs(getCalls 未导出则经 page.request.get('/__mock/calls') 读)
  - packages/infinite-canvas/src/hooks/useToast.tsx(toast DOM 形状——409 用例的文案捕获,3s 自灭窗口内断言)
  </read_first>
  <action>
  合成拖拽序列 helper(三用例共用,page.evaluate 内三步,同一 DataTransfer 存 window.__e2eDt):
    ① const card = document.querySelector('.am-card');card.dispatchEvent(new DragEvent('dragstart',
      {dataTransfer: dt, bubbles:true, cancelable:true}));
    ② 「画布」页签 locator.dispatchEvent dragover(getByRole('button',{name:'画布',exact:true})
      .evaluate(el => el.dispatchEvent(new DragEvent('dragover',
      {dataTransfer: window.__e2eDt, bubbles:true, cancelable:true})))) → 等
      '.react-flow__pane' 挂载;
    ③ pane.dispatchEvent dragover 后 drop,均带 {dataTransfer: window.__e2eDt, bubbles:true,
      cancelable:true, clientX:700, clientY:400}。
  Test 1 「drag-in-bounded: 拖入落点与拖放点各轴 ≤64px(D-01)」:
    loadAssetCenter → 拖拽序列 → expect.poll window.__kaisCanvas.getGraph() 中出现
    id==='asset-90001' 节点 → anchor = window.__kaisCanvas.screenToFlow({x:700,y:400});
    断言 |node.position.x−anchor.x| ≤ 64 且 |node.position.y−anchor.y| ≤ 64(source 锚理论紧界
    26/18,64 留裕量——Pattern 3 口径);断言 .react-flow__node[data-id="asset-90001"] DOM 在场
    (广播→addNodeFromSocket→setGraph→RF 渲染全链证据);读 /__mock/calls 断言存在
    POST /api/canvas/v2/nodes/ 记录,body.nodeId==='asset-90001' 且 body.x/y 与 canonical
    position 相等(POST 载荷=落库真值=客户端显示,truth-first 三点一线)。
  Test 2 「drag-in-duplicate-409: 同资产二次拖入 → 409 + toast(已在画布」:
    Test 1 状态延续或重走序列 → 再次拖同一卡片(fixture 固定 id 90001)→ 断言
    /__mock/calls 该路由恰 2 条且第 2 条发生在后(mock 不记 response 则以副作用断言);
    getGraph 中 asset-90001 计数仍 1;toast 文案含「已在画布」(3s 窗口内 expect.poll)。
  Test 3 「stub-disposed: ＋画布 按钮退役(D-01 sole-caller)」:
    loadAssetCenter → page.locator('.am-card__add').count() === 0;.am-card 与
    .am-card__locate 仍在(退役面精准,不误伤定位)。
  回归:npm run build 后单跑 phase55-nav.mjs(放置断言面共享,center 语义必须零回归)+
  本文件;若 phase55-nav 个别用例红,隔离重跑一次判 flake(STATE 负载噪音先例),复现性红=真回归修接线。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm run build && npx playwright test test/e2e/tests/phase61-debt.mjs && npx playwright test test/e2e/tests/phase55-nav.mjs</automated>
  </verify>
  <acceptance_criteria>
  - phase61-debt.mjs 三用例全绿(exit 0),三个 -g 组词(drag-in-bounded/drag-in-duplicate-409/stub-disposed)各命中 ≥1 用例;
  - 有界断言数值口径 ≤64(两轴各自);
  - phase55-nav.mjs 单独跑全绿(或唯一红项隔离重跑判 flake 并记录);
  - 断言只读 getGraph/screenToFlow(bridge),不断言 rfNodes position(Anti-Pattern 2)。
  </acceptance_criteria>
  <done>DEBT-01 三条 truth 全部有行为证据:有界落位/持久化三点一线/409 反馈/stub 退役。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 拖入 payload → 客户端 onDrop | dataTransfer 载荷由本应用卡片写入,但 drop 处理器 defensively 解析(字段类型校验) |
| 客户端 → POST /api/canvas/v2/nodes/ | 既有 zod nodeInputSchema 门 + validateNodeData(服务端权威,本 plan 不新增服务端面) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | onDrop position/NaN 载荷 | mitigate | placeNewAsset finitePoint 防御既有(T-55-02)+ 服务端 zod number 门 + mock 400 分支;onDrop 解析失败 toast+return |
| T-61-02 | Tampering | 拖入 label/assetType XSS | accept | React 文本插值自动转义(51-REVIEW Verified-Positive 全包无 dangerouslySetInnerHTML);渲染走既有资产节点路径 |
| T-61-03 | Spoofing | 异源页面伪造同 MIME drop | accept | 载荷字段强校验(id 有限 number/uuid string),最坏效果=经服务端 zod 门的节点创建,与用户点击等权;无提权面 |
| T-61-04 | Repudiation | placeAssetNode 失败静默 | mitigate | 全部错误路径 toast 化,不 throw 不吞 |
| T-61-SC | Tampering | npm/pip 安装 | accept | 本 plan 零新依赖(RESEARCH Package Audit:零新装),无 install 动作 |
</threat_model>

<verification>
- npm run build(dist 纪律,地雷 #10)后 phase61-debt.mjs 三用例绿;
- placeNewAsset 既有单测零改动绿(本体未触碰的直接证明);
- phase55-nav.mjs standalone 绿(共享放置面零回归);
- grep 退役三件套(placeAssetOnCanvas/handleAddToCanvas/am-card__add)src 零残留。
</verification>

<success_criteria>
- 拖入 → 页签切视图 → 面板 drop → 节点出现在拖放点 ≤64px 且经 POST /nodes 落库(_calls 记录);
- 重复拖入 409 + toast,节点不重复;
- 拖入是 anchor='source' 唯一活调用方,stub 链无残留;
- DEBT-01 三条 e2e truth 成文可重复。
</success_criteria>

<output>
Create `.planning/phases/61-audit-debt-clearance/61-01-SUMMARY.md` when done
</output>
