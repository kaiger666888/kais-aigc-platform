---
phase: 50-historical-backfill-contract-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/backfill-candidate-groups.ts
  - package.json
  - .planning/phases/50-historical-backfill-contract-guards/backfill-baseline-dryrun.txt
  - .planning/phases/50-historical-backfill-contract-guards/backfill-apply-log.txt
  - .planning/phases/50-historical-backfill-contract-guards/backfill-post-run-verify.txt
autonomous: true
requirements: [INGEST-04, PHASE-02]
must_haves:
  truths:
    - "Running the script with no flags prints a dry-run baseline report and mutates 0 rows"
    - "npm run backfill:phase-50 -- --apply WITHOUT --i-backed-up-db exits 1 and prints the backup command"
    - "After --apply, every planned group has exactly one isPrimaryView=1 primary row (assetsId NULL) and all members carry assetsId=primary.id, isPrimaryView=0"
    - "After --apply, zero state='eliminated' rows changed (byte-identical pre/post snapshot)"
    - "A second --apply run reports Planned changes: 0 and Executed updates: 0"
    - "After --apply, the count of workflow_phase-NULL rows drops from the 1456 baseline; sampled /p04/ rows carry 'p04' and /p07/ rows carry 'p07'"
  artifacts:
    - path: "scripts/backfill-candidate-groups.ts"
      provides: "ONE-OFF idempotent backfill: planBackfill(db)/applyBackfill(db) + CLI (dry-run default, --apply gated by --i-backed-up-db)"
      contains: "planGroups"
    - path: "package.json"
      contains: "backfill:phase-50"
    - path: ".planning/phases/50-historical-backfill-contract-guards/backfill-baseline-dryrun.txt"
      provides: "Pre-apply baseline report (milestone-close quantitative evidence)"
  key_links:
    - from: "scripts/backfill-candidate-groups.ts"
      to: "src/lib/candidateGrouping.ts"
      via: "import { planGroups, deriveWorkflowPhase, parseVariantName }"
      pattern: "from \"\\.\\./src/lib/candidateGrouping\""
    - from: "scripts/backfill-candidate-groups.ts"
      to: "data/db2.sqlite"
      via: "self-built knex handle (better-sqlite3, filename CLI arg, default data/db2.sqlite)"
      pattern: "better-sqlite3"
---

<objective>
Build and RUN the Phase 50 historical backfill (INGEST-04 + PHASE-02 merged into one script, per v2.1 architecture decision 6): the 1612 existing o_assets rows — all currently ungrouped (assetsId NULL on 100% of rows) — gain the same organization as new ingests: candidate groups (member assetsId → group primary, exactly one isPrimaryView per group) and workflow_phase values derived from meta/path.

This is the Phase 47 pattern applied to the candidate-group shape: dry-run baseline first, DB backup, then idempotent --apply, then post-run verification. Grouping decisions come from the Phase 48 pure contract layer (candidateGrouping) — zero logic duplication.

Output: scripts/backfill-candidate-groups.ts + npm run backfill:phase-50 + applied production DB + audit trail (3 txt files in the phase dir).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-historical-backfill-contract-guards/50-CONTEXT.md

<interfaces>
<!-- Extracted from codebase 2026-08-19. Executor uses these directly — no exploration needed. -->

From src/lib/candidateGrouping.ts (pure module — no DB/fs/network):
```typescript
export interface IngestImageInput {
  filePath: string;
  assetName: string;
  assetType?: string;
  prompt?: string;
  description?: string;
  characterId?: string;
  viewAngle?: string;
  subtype?: string;
  meta?: Record<string, unknown>;
}
export interface AssetGroupPlan {
  groupKey: string;            // "name:{parentDir}/{stem}" for naming channel
  primaryFilePath: string;
  memberFilePaths: string[];   // includes the primary's own path
  characterId?: string;
  metaSubtype?: string;
  source: "manifest" | "naming";
}
export interface GroupPlanResult {
  groups: AssetGroupPlan[];
  ungroupedFilePaths: string[];  // standalone — current flat behavior, no error
  warnings: string[];
}
export function parseVariantName(filePath: string): { base: string; variant: number } | null;
export function planGroups(images: IngestImageInput[], manifests?: ManifestFrameEntry[]): GroupPlanResult;
// Naming channel (no manifests arg): groups *_v{N} siblings by parentDir+base;
// primary = canonical no-suffix file if present, else LOWEST variant number;
// groups form fine with v1..v3 only (no canonical) — matches /p11/first_last_frames/S01_first_v{1,2,3}.png stock.
export function deriveWorkflowPhase(phase: string | undefined, filePath: string): string | null;
// "p04_turnaround"→"p04"; "/oss/{proj}/p07/..."→"p07"; underivable→null (NEVER guesses, D-08)
```

From src/lib/ingestAssets.ts (the transaction/assertion pattern to mirror — backfill does NOT call it, existing rows are UPDATEd not INSERTed, per D-03):
```typescript
// shape landed by ingest: primary row = assetsId NULL + isPrimaryView=true;
// member rows = assetsId <primary integer id> + isPrimaryView=false; state always 'active'.
// In-trx per-group assertion via whereIn(memberAssetIds) → throw → rollback (copy this pattern).
```

o_assets columns relevant here (src/lib/initDB.ts): id (int PK), uuid, name, type, imageId (→ o_image.id), assetsId (int, parent ref), projectId, characterId, isPrimaryView (bool), state ('active'|'eliminated' in actual data), meta (JSON text), createdAt, createdBy, workflow_phase (string, nullable).

Production-baseline facts (read-only query, 2026-08-19 — supersedes the stale 368-row figure in .task-pipeline-asset-gap-analysis.md):
- o_assets total 1612; state: active 1226 / eliminated 386 (D-05 red line — never touch eliminated)
- assetsId NULL on 1612/1612 rows (zero grouping in stock); isPrimaryView=1 on 768 rows
- workflow_phase NULL/empty on 1456 rows (156 already carry values incl. p04_turnaround/voice_design — NOT rewritten, idempotency)
- Of wf-null rows: ~535 derivable from a p{NN} path segment, ~237 have meta JSON with "phase": "p{NN}_…" field, 178 rows have imageId NULL (no filePath)
- Stock path shapes: /oss/{projectId}/p04/turnaround_sheets/base_turnaround_{char}[_v{1,2,3}].png, /oss/{proj}/p07/scene_images/scene_S{NN}[_v{1,2,3}].png, /oss/{proj}/p11/first_last_frames/S{NN}_{first,last}_v{1,2,3}.png; plus non-pipeline rows (kais-movie-agent absolute paths, e2e tests) that stay NULL — correct per D-04
</interfaces>

Phase 47 range template (read for convention, do not copy logic): scripts/canvas/oneoffs/backfill-asset-descriptions.py header comment (dry-run/--apply/backup-gate conventions; NOTE: the Phase 47 planning dir was archived at v2.1 milestone start — for its audit-trail format run `git log --oneline --grep="47-01"` and inspect the archived files from git history instead of the filesystem).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill script (planBackfill/applyBackfill + gated CLI) + npm registration</name>
  <files>scripts/backfill-candidate-groups.ts (new), package.json (add one script entry)</files>
  <read_first>src/lib/candidateGrouping.ts, src/lib/ingestAssets.ts (transaction + assertion pattern), scripts/canvas/oneoffs/backfill-asset-descriptions.py (header convention), src/lib/initDB.ts o_assets/o_image builders (lines ~447-500)</read_first>
  <action>
Create scripts/backfill-candidate-groups.ts implementing per D-01..D-06. Structure:

1. Header: ONE-OFF marker in the Phase 47 style — states this is the Phase 50 one-shot repair (INGEST-04+PHASE-02), never cron/startup, --apply requires --i-backed-up-db, and points at the Phase 48 ingest route as the live path for new data. IMPORTANT (self-referential grep trap, learned in 49-02): word the header comment "never imports the utils barrel" — do NOT write the literal "@/utils" anywhere in this file; acceptance greps it for zero.

2. Reusable core (exported, db-parameterized — Plan 50-02's verify drives these on :memory:):
   - `planBackfill(db: Knex): BackfillPlan` — READ-ONLY. Query: SELECT a.id, a.projectId, a.imageId, a.assetsId, a.isPrimaryView, a.state, a.meta, o.filePath FROM o_assets a LEFT JOIN o_image o ON o.id = a.imageId WHERE a.state != 'eliminated' (D-05 red line enforced at query level). Group the rows into per-projectId buckets; within each bucket build IngestImageInput[] (filePath + assetName = a.name or basename; minimal fields — grouping keys off filePath) for rows with a non-empty filePath; call planGroups(bucketImages) with NO manifests argument (naming channel only — historical rows have no in-batch manifest; document why in a comment). Rows sharing an identical filePath with another row are skipped from grouping and counted as duplicate-path anomalies in the report (deterministic, no arbitrary pick). Map each AssetGroupPlan back to row ids via filePath. Compute per-row target state and a DIFF list (only rows where current value ≠ target):
     * group primary row: assetsId=NULL, isPrimaryView=1, state='active'
     * group member rows: assetsId=primary row id, isPrimaryView=0, state='active'
     * workflow_phase target for EVERY eligible row (grouped or standalone, with or without imageId): parse meta JSON and resolve the phase signal as `meta.phase` **falling back to nested `meta.provenance.phase`** (checker-verified production fact: 129 active rows — p10_voice ×58, p09_shot_breakdown ×46, p07_scene_generation ×17, p11b_final_render ×8 — carry phase ONLY in meta.provenance.phase; top-level-only reaches just 28 rows); pass the resolved signal and filePath (or "") to deriveWorkflowPhase(metaPhase, filePath ?? "") — this implements D-04 priority meta > path > NULL in one pure-function call and matches the online D-08 semantics (normalized "p04"/"p07"/"p11"); target NULL stays NULL (never guessed).
     BackfillPlan = { scanned, excludedEliminated, noImageId, duplicatePathSkipped, groups: [{groupKey, primaryRowId, memberRowIds, primaryFlipsFrom}], standaloneRows, wfFromMeta, wfFromPath, wfUnderivable, diffs: [{id, assetsId?, isPrimaryView?, state?, workflowPhase?}] }.
   - `applyBackfill(db: Knex, plan: BackfillPlan): { executedUpdates: number }` — ONE transaction; for each diff row a single UPDATE o_assets SET (only the differing columns) WHERE id = ? AND state != 'eliminated' (second red-line guard inside every statement, D-05). After the updates, in-transaction per-group assertion copied from ingestAssets: reload member rows via whereIn(primaryRowIds ∪ memberRowIds), assert exactly one isPrimaryView=1 row == primaryRowId, that primary row's state === 'active' (checker WR-3: a user-eliminated primary between plan and apply must roll back, not link members to an eliminated row), and every member's assetsId == primaryRowId — violation throws and rolls back everything.
   - Idempotency by construction: diffs only include actual value changes, so a re-run after apply yields an empty diff list.

3. Report printer (dry-run and apply share it; Claude's-discretion format, must include at minimum): total/scanned/eliminated-excluded/no-imageId counts (D-06 — SELECT COUNT(*) printed before any work), groups planned + grouped rows + standalone rows, assetsId writes count, isPrimaryView 0→1 and 1→0 counts, workflow_phase from-meta / from-path / underivable-NULL / already-set counts, convergence-decisions list (D-05: every group where the primary flips existing isPrimaryView=1 rows, listing groupKey + chosen primary row id + flipped row ids), final lines `Planned changes: {N}` and `Executed updates: {N}` (0 in dry-run).

4. CLI main(): flags --dry-run (default), --apply, --i-backed-up-db, --db <path> (default data/db2.sqlite). knex handle self-built: knex({ client: "better-sqlite3", connection: { filename: dbPath }, useNullAsDefault: true }) — D-06, never the utils barrel. If --apply without --i-backed-up-db: print the D-02 hint line `cp data/db2.sqlite data/db2-backup-pre-phase-50.sqlite` (plus a note that sqlite3 .backup is the consistency-safe variant) and process.exit(1) BEFORE opening a write. --apply path: print report, applyBackfill, print executed counts.

5. package.json scripts: add `"backfill:phase-50": "npx tsx scripts/backfill-candidate-groups.ts"` next to the verify:phase-* entries (dry-run by default; apply via `npm run backfill:phase-50 -- --apply --i-backed-up-db`).

Do NOT touch: state='eliminated' rows, o_image rows, rows' type/name/prompt/meta/characterId columns, the online ingest path, kmc/review-platform (D-11).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run backfill:phase-50 --silent > /tmp/bf-dryrun.txt 2>&1; echo "dryrun-exit=$?"; tail -5 /tmp/bf-dryrun.txt; npx tsx scripts/backfill-candidate-groups.ts --apply; echo "gate-exit=$?"; grep -c '@/utils' scripts/backfill-candidate-groups.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exit 0
    - `npm run backfill:phase-50` exits 0, prints the baseline report, ends with `Planned changes: N` (N > 0) and `Executed updates: 0`
    - `sqlite3 -readonly data/db2.sqlite "SELECT COUNT(*) FROM o_assets WHERE assetsId IS NOT NULL"` still 0 after the dry-run (read-only proof)
    - `npx tsx scripts/backfill-candidate-groups.ts --apply` (no backup flag) exits 1 and prints the cp backup command (D-02 gate works)
    - `grep -c '@/utils' scripts/backfill-candidate-groups.ts` → 0 (header worded "utils barrel")
    - `grep -c 'planGroups\|deriveWorkflowPhase' scripts/backfill-candidate-groups.ts` ≥ 2 (D-03 reuse, no copy-paste)
    - `grep -c "state != 'eliminated'\|state != \"eliminated\"" scripts/backfill-candidate-groups.ts` ≥ 2 (query-level + UPDATE-level D-05 guards)
    - package.json contains `"backfill:phase-50": "npx tsx scripts/backfill-candidate-groups.ts"`
  </acceptance_criteria>
  <done>Script + npm entry exist; dry-run reports a non-empty plan without writing; the apply gate refuses to run without --i-backed-up-db; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute against production — baseline capture, DB backup, --apply, red-line + idempotency proof</name>
  <files>.planning/phases/50-historical-backfill-contract-guards/backfill-baseline-dryrun.txt (new), backfill-apply-log.txt (new), backfill-post-run-verify.txt (new); DB backup data/db2-backup-pre-phase-50.sqlite (created, gitignored, NOT committed)</files>
  <read_first>Task 1's script (fresh in context), scripts/canvas/oneoffs/backfill-asset-descriptions.py (header conventions; the 47-* audit txt files live only in git history — `git show` them if needed)</read_first>
  <behavior>
    - Behavior 1: after --apply, `SELECT COUNT(*) FROM o_assets WHERE assetsId IS NOT NULL AND isPrimaryView=1` = 0 (no member is primary)
    - Behavior 2: `SELECT COUNT(*) FROM o_assets m LEFT JOIN o_assets p ON p.id=m.assetsId WHERE m.assetsId IS NOT NULL AND (p.id IS NULL OR p.isPrimaryView!=1)` = 0 (every group's target primary exists and is flagged)
    - Behavior 3: eliminated rows byte-identical pre/post (snapshot diff empty)
    - Behavior 4: second --apply prints Planned changes: 0, Executed updates: 0
    - Behavior 5: workflow_phase NULL count < 1456; DISTINCT workflow_phase for /p04/ rows = 'p04', for /p07/ rows = 'p07'
  </behavior>
  <action>
Run the Phase 47 sequence against data/db2.sqlite, in this order:

1. Baseline: `npm run backfill:phase-50 > .planning/phases/50-historical-backfill-contract-guards/backfill-baseline-dryrun.txt 2>&1` — verify exit 0 and Planned changes > 0 inside the file.
2. Eliminated red-line pre-snapshot: `sqlite3 -readonly data/db2.sqlite "SELECT id,assetsId,isPrimaryView,workflow_phase,state FROM o_assets WHERE state='eliminated' ORDER BY id" > /tmp/elim-before.txt`.
3. DB backup (consistency-safe, supersedes raw cp): `sqlite3 data/db2.sqlite ".backup 'data/db2-backup-pre-phase-50.sqlite'"` then assert the file exists and its size is within 20% of data/db2.sqlite (Phase 47 backup was ~328MB; sanity-check scale).
4. Apply: `npm run backfill:phase-50 -- --apply --i-backed-up-db > .planning/phases/50-historical-backfill-contract-guards/backfill-apply-log.txt 2>&1`; assert exit 0, `Executed updates: N` == `Planned changes: N` from the baseline, and no error/rollback text.
5. Post-run verification — write results to backfill-post-run-verify.txt: (a) rerun the eliminated snapshot to /tmp/elim-after.txt and `diff /tmp/elim-before.txt /tmp/elim-after.txt` (must be empty); (b) run the two group-integrity SQL checks from <behavior> plus orphan check `SELECT COUNT(*) FROM o_assets m LEFT JOIN o_assets p ON p.id=m.assetsId WHERE m.assetsId IS NOT NULL AND p.id IS NULL` = 0; (c) `SELECT COUNT(*) FROM o_assets WHERE workflow_phase IS NULL OR workflow_phase=''` — record the drop from 1456 and the underivable remainder (expect ≈ non-pipeline rows: kais-movie-agent paths / e2e / no-imageId-no-meta); (d) `SELECT DISTINCT workflow_phase FROM o_assets a JOIN o_image o ON o.id=a.imageId WHERE o.filePath LIKE '%/p04/%'` → only 'p04' (same for /p07/ → 'p07', /p11/ → 'p11'); (e) idempotency: run `npm run backfill:phase-50 -- --apply --i-backed-up-db` again, capture output, assert `Planned changes: 0` AND `Executed updates: 0`; (f) record the headline before/after numbers (rows grouped, groups created, wf NULL before/after) at the top of the file — these are the milestone-close quantitative evidence.
6. Commit the three txt audit files (backup .sqlite stays gitignored).
  </action>
  <verify>
    <automated>diff /tmp/elim-before.txt /tmp/elim-after.txt && echo ELIM-UNTouched; sqlite3 -readonly data/db2.sqlite "SELECT COUNT(*) FROM o_assets WHERE assetsId IS NOT NULL AND isPrimaryView=1; SELECT COUNT(*) FROM o_assets WHERE workflow_phase IS NULL OR workflow_phase=''"; grep -c "Executed updates: 0" .planning/phases/50-historical-backfill-contract-guards/backfill-apply-log.txt 2>/dev/null; tail -3 .planning/phases/50-historical-backfill-contract-guards/backfill-post-run-verify.txt</automated>
  </verify>
  <acceptance_criteria>
    - backfill-baseline-dryrun.txt exists with Planned changes > 0 and Executed updates: 0
    - data/db2-backup-pre-phase-50.sqlite exists (git status does NOT list it)
    - diff of eliminated snapshots is empty (D-05 red line held on production data)
    - Member-is-primary count = 0; unflagged/orphan primary count = 0
    - workflow_phase NULL count strictly < 1456 and the drop is recorded; /p04/ rows all 'p04', /p07/ all 'p07', /p11/ all 'p11'
    - Second apply output contains Planned changes: 0 and Executed updates: 0 (idempotent)
    - backfill-post-run-verify.txt committed with headline before/after numbers
  </acceptance_criteria>
  <done>Production DB backfilled: candidate groups formed with exactly-one-primary, eliminated rows untouched, workflow_phase populated for every derivable row, second run is a no-op, audit trail committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backfill script → production data/db2.sqlite | unattended bulk UPDATE of 1600+ production rows |
| stock data → grouping decisions | historical rows (mixed path shapes, duplicate paths, eliminated rows) crossing into write decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Tampering | applyBackfill bulk UPDATE | mitigate | dry-run default; --apply hard-gated on --i-backed-up-db (exit 1 before opening a write, D-02); consistency-safe sqlite3 .backup taken before apply; whole apply in ONE transaction — any assertion failure rolls back everything |
| T-50-02 | Tampering | eliminated-row red line (D-05) | mitigate | excluded at SELECT level AND `WHERE state != 'eliminated'` inside every UPDATE statement; proven on production via pre/post byte-diff of the eliminated snapshot |
| T-50-03 | Repudiation | apply outcome | mitigate | audit trail: baseline/apply/post-run txt files committed in the phase dir (Phase 47 convention); backup file retained gitignored |
| T-50-04 | Tampering | idempotency regression / partial apply | mitigate | diff-only UPDATE list + in-trx per-group exactly-one-primary assertion (ingestAssets pattern) + second-run 0/0 proof |
| T-50-SC | Tampering | package installs | accept | zero new packages — tsx/knex/better-sqlite3 already in use; no install tasks |
</threat_model>

<verification>
- `npx tsc --noEmit` exit 0
- `npm run backfill:phase-50` dry-run: report + 0 writes; apply gate blocks without the backup flag
- Production apply: executed == planned; eliminated byte-diff empty; group integrity SQL = 0 violations; wf NULL drop recorded; /p04//p07//p11 samples carry correct normalized values
- Second apply: Planned changes: 0, Executed updates: 0
- Audit files committed: backfill-baseline-dryrun.txt, backfill-apply-log.txt, backfill-post-run-verify.txt
</verification>

<success_criteria>
- INGEST-04: stock flat assets regrouped — members point assetsId at an existing flagged primary, exactly one per group, idempotent re-run (D-01..D-05)
- PHASE-02: workflow_phase backfilled via deriveWorkflowPhase(meta.phase ∥ meta.provenance.phase > path), underivable rows stay NULL and are itemized in the report (D-04)
- Phase 47 flow honored end-to-end: dry-run baseline → backup → gated apply → post-run verify → audit trail committed
</success_criteria>

<output>
Create `.planning/phases/50-historical-backfill-contract-guards/50-01-SUMMARY.md` when done. Record the headline before/after numbers (baseline counts vs post-apply) — they are the v2.1 milestone-close quantitative evidence.
</output>
