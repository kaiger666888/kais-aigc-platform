Phase 50-01 post-run verification — production data/db2.sqlite
Executed: 2026-08-19 (UTC) by npm run backfill:phase-50 -- --apply --i-backed-up-db
Backup:   data/db2-backup-pre-phase-50.sqlite (371,773,440 bytes, gitignored,
          created via `sqlite3 data/db2.sqlite ".backup ..."` BEFORE any write)

===========================================================================
HEADLINE BEFORE/AFTER NUMBERS (v2.1 milestone-close quantitative evidence)
===========================================================================

Metric                              BEFORE          AFTER           DELTA
o_assets total                      1612            1612            0
state='active'                      1226            1226            0
state='eliminated' (red line)       386             386             0  (byte-identical, proof (a))
candidate groups planned            0 (flat stock)  154 formed      154
  — groups with >=2 rows (linked)   0               121             121
  — single-variant primaries        0               33              33
member rows with assetsId set       0               240             +240
isPrimaryView=1 total               768             710             -61 demotions +3 promotions
workflow_phase NULL/empty (all)     1456            922             -534
workflow_phase NULL/empty (active)  1129            595             -534
workflow_phase populated (all)      156             690             +534
  — derived from meta(.provenance)  —               237
  — derived from path p{NN} seg     —               297
Planned changes / Executed updates  538 / 0 (dry)   538 / 538       second apply: 0 / 0 (idempotent)

Pre-apply RED evidence (TDD): wf NULL count = 1456 (assert "< 1456" FAILED),
grouped member rows = 0 — both flipped GREEN after apply (below).

===========================================================================
(a) D-05 RED LINE: eliminated rows byte-identical
===========================================================================
snapshot: SELECT id,assetsId,isPrimaryView,workflow_phase,state FROM o_assets
          WHERE state='eliminated' ORDER BY id  (386 rows, pre vs post)
$ diff /tmp/elim-before.txt /tmp/elim-after.txt
(empty diff — no output)  => PASS, 386/386 eliminated rows untouched.
Additional guards: excluded at SELECT level (WHERE a.state != 'eliminated')
AND inside every UPDATE statement; wf-NULL among eliminated stayed 327/386.

===========================================================================
(b) GROUP INTEGRITY SQL (all expect 0)
===========================================================================
member-is-primary:
  SELECT COUNT(*) FROM o_assets WHERE assetsId IS NOT NULL AND isPrimaryView=1;
  => 0   PASS (Behavior 1)
unflagged-primary (target primary missing or not flagged):
  SELECT COUNT(*) FROM o_assets m LEFT JOIN o_assets p ON p.id=m.assetsId
   WHERE m.assetsId IS NOT NULL AND (p.id IS NULL OR p.isPrimaryView!=1);
  => 0   PASS (Behavior 2)
orphan members (assetsId points at nonexistent row):
  SELECT COUNT(*) FROM o_assets m LEFT JOIN o_assets p ON p.id=m.assetsId
   WHERE m.assetsId IS NOT NULL AND p.id IS NULL;
  => 0   PASS
positive form: all 240 member rows join a primary with isPrimaryView=1 AND
state='active' (SELECT COUNT(*) ... p.isPrimaryView=1 AND p.state='active' => 240).
121 distinct primaries referenced; 154 groups total (33 are single-variant
groups whose only row is its own primary — no member link needed).

===========================================================================
(c) workflow_phase BACKFILL (Behavior 5)
===========================================================================
NULL/empty count: 1456 -> 922  (drop of 534 recorded; 922 < 1456)  PASS
Breakdown of the remaining 922:
  - 327 eliminated rows (never touched by design, D-05)
  - 595 active rows underivable per D-04/D-08 — itemized row-by-row in
    backfill-baseline-dryrun.txt (kais-movie-agent absolute paths, non-pipeline
    /oss/pipeline/* paths with no p{NN} segment, rows with no filePath and no
    meta.phase) — correctly left NULL, never guessed.
Every written value is the normalized 2-digit form (p04/p07/p09/p10/p11):
deriveWorkflowPhase output only.

===========================================================================
(d) PHASE-VALUE SPOT CHECKS BY PATH SHAPE
===========================================================================
DISTINCT workflow_phase, active rows, path LIKE '%/p07/%':  => [p07] only   PASS
DISTINCT workflow_phase, active rows, path LIKE '%/p11/%':  => [p11] only   PASS
DISTINCT workflow_phase, active rows, path LIKE '%/p04/%':
  => [p04]:11  [p04_turnaround]:21  [p04_character_design]:4  [p09_shot_breakdown]:2

  /p04/ decomposition (verified against the pre-apply backup):
  - PRE:  p04_turnaround 21, NULL 11, p04_character_design 4, p09_shot_breakdown 2
  - POST: p04_turnaround 21, p04 11,         p04_character_design 4, p09_shot_breakdown 2
  Every wf-NULL /p04/-path row (11/11) now carries exactly 'p04' — all rows the
  backfill derived from the path are 'p04'. PASS.
  The 27 non-'p04' values are PRE-EXISTING non-NULL values (raw legacy strings
  like 'p04_turnaround'), which the plan mandates NOT rewriting ("156 already
  carry values — NOT rewritten, idempotency") and which the backfill never
  emits (it only writes normalized 2-digit forms). Untouched by design.

===========================================================================
(e) IDEMPOTENCY PROOF (Behavior 4)
===========================================================================
$ npm run backfill:phase-50 -- --apply --i-backed-up-db   (second run)
exit code: 0
output ends: "Planned changes: 0" / "Executed updates: 0"   PASS
(diff-only plan: zero rows where current value != target after first apply)

===========================================================================
VERDICT
===========================================================================
ALL CHECKS PASS. Production DB backfilled: 154 candidate groups with
exactly-one-flagged-active primary each, 240 members linked, 534
workflow_phase values derived (meta 237 / path 297), eliminated rows
byte-identical, second apply a verified no-op.
