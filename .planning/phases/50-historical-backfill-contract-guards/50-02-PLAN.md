---
phase: 50-historical-backfill-contract-guards
plan: 02
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - scripts/verify-phase-50.ts
  - package.json
  - scripts/backfill-candidate-groups.ts
  - scripts/canvas/register_turnaround_b2.py
  - scripts/canvas/register_character_design.py
  - scripts/canvas/register_turnaround_costume.py
  - scripts/canvas/register_storyboard_sheets.py
  - scripts/canvas/register-scene-refs.py
autonomous: true
requirements: [GUARD-01, GUARD-02]
must_haves:
  truths:
    - "npm run verify:phase-50 exits 0 with a GUARD-01 contract group asserting the fixture-derived o_assets shape (group count / exactly-one isPrimaryView per group / state domain / assetsId self-consistency)"
    - "The verify suite proves backfill idempotency and the eliminated red line on a seeded :memory: DB (apply twice → second run 0 planned / 0 executed; eliminated row untouched)"
    - "Vocabulary no-drift is asserted by import identity: assets-registry consumes CANONICAL_ASSET_TYPES from src/lib/assetTypes.ts; legacy alias table has no orphans in either direction"
    - "verify output ends with a Summary line and a PASS/FAIL verdict; exit code 0/1 follows the verdict"
    - "verify output prints exactly one WARN line flagging the SC-4 consumer-side debt (no assertion, D-11)"
    - "All five manual register_*.py scripts carry DEPRECATED headers pointing at the Phase 48 ingest chain; none deleted (D-12)"
  artifacts:
    - path: "scripts/verify-phase-50.ts"
      provides: "GUARD-01 contract group + GUARD-02 aggregate gate"
      contains: "phase48-p11-manifest.fixture.json"
    - path: "package.json"
      contains: "verify:phase-50"
  key_links:
    - from: "scripts/verify-phase-50.ts"
      to: "scripts/backfill-candidate-groups.ts"
      via: "import { planBackfill, applyBackfill }"
      pattern: "planBackfill"
    - from: "scripts/verify-phase-50.ts"
      to: "src/lib/ingestAssets.ts"
      via: "ingestImagesPayload on a temp :memory: knex (landed-shape contract)"
      pattern: "ingestImagesPayload"
    - from: "scripts/verify-phase-50.ts"
      to: "src/lib/assetTypes.ts"
      via: "import CANONICAL_ASSET_TYPES / LEGACY_ASSET_TYPE_ALIASES / expandTypesForQuery (identity, not literals)"
      pattern: "from \"\\.\\./src/lib/assetTypes\""
---

<objective>
Lock the entire v2.1 milestone behind automated contract guards: GUARD-01 formalizes Phase 48's ingest grouping contract (fixture → planGroups → landed o_assets shape) as a permanent regression suite; GUARD-02 aggregates it with backfill idempotency/red-line checks, dual-side enum no-drift assertions, Phase 48/49 spot invariants, and the SC-4 debt warning into scripts/verify-phase-50.ts — the final gate of the v2.0→v2.1 verify tradition. Then close the historical tooling loop per D-12: deprecation headers on the five manual registration scripts the Phase 48 ingest chain replaces (kept as historical evidence, never deleted).

Output: scripts/verify-phase-50.ts + npm run verify:phase-50 + deprecation headers + archival annotation on the applied backfill script.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-historical-backfill-contract-guards/50-CONTEXT.md
@.planning/phases/50-historical-backfill-contract-guards/50-01-SUMMARY.md

<interfaces>
<!-- Extracted from codebase 2026-08-19. Executor uses these directly — no exploration needed. -->

From scripts/backfill-candidate-groups.ts (created by Plan 50-01):
```typescript
export function planBackfill(db: Knex): BackfillPlan;   // read-only; diffs only
export function applyBackfill(db: Knex, plan: BackfillPlan): { executedUpdates: number };
// BackfillPlan.diffs: [{id, assetsId?, isPrimaryView?, state?, workflowPhase?}]
```

From src/lib/candidateGrouping.ts / ingestAssets.ts / assetTypes.ts:
```typescript
planGroups(images: IngestImageInput[], manifests?: ManifestFrameEntry[]): GroupPlanResult;
deriveWorkflowPhase(phase: string | undefined, filePath: string): string | null;
ingestImagesPayload(db: Knex, payload: IngestImagesPayload): Promise<IngestResult>;
// IngestImagesPayload = { projectId, phase?, images: IngestImageInput[], manifests?: ManifestFrameEntry[] }
CANONICAL_ASSET_TYPES: readonly string[];   // 11 values
LEGACY_ASSET_TYPE_ALIASES: Record<string, CanonicalAssetType>;  // { role: "character", tool: "prop" }
normalizeAssetType(input: string): CanonicalAssetType | null;
expandTypesForQuery(canonical: string): string[];
```

Fixture scripts/fixtures/phase48-p11-manifest.fixture.json: 2 shots (S01_B01 3 variants, S02_B01 2 variants), paired first/last lists, selected_first_variant=2 on S01, selected_last_variant=2 on S02, frame prompts. Manifest paths are workdir-relative ("assets/P11/iframes_{shot}/first_frame_v{N}.png") — the ingest batch must use OSS-style paths preserving the iframes_{shot} dir (e.g. /oss/1785508691757/p11/iframes_S01_B01/first_frame_v1.png) so the manifest channel's dirBase matching resolves, exactly like verify-phase-48 Part 2.

Temp-DB pattern (copy from scripts/verify-phase-48.ts Part 2, lines ~462-510): knex({ client: "better-sqlite3", connection: { filename: ":memory:" }, useNullAsDefault: true }) + createTable o_assets/o_image mirroring initDB DDL column-for-column. NEVER a data/db2.sqlite literal in this file.

Output convention (repo standard): assert() helper printing `  PASS|FAIL: {name}[ — detail]`, section banners with ===, final `=== Summary: N/N assertions passed ===` + ✅/❌ verdict line + process.exit(0|1); catch → exit(2).

Phase 49 spot-invariant targets (existence + identifiers, no re-run of their gates per D-09):
- src/routes/canvas/v2/select-winner.ts — contains `void resolveOpenReviewForSelection(` and `syncAssetPrimaryForWinner`
- src/lib/canvasRelationalStore.ts — exports `selectWinnerInGroup`
- src/lib/reviewBridge.ts — exports `resolveOpenReviewForSelection`
- src/lib/canvasAssetLinkage.ts — exports `applyRegistrySelectionToCanvas`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/verify-phase-50.ts — GUARD-01 contract group + GUARD-02 aggregate gate + npm registration</name>
  <files>scripts/verify-phase-50.ts (new), package.json (add verify:phase-50)</files>
  <read_first>scripts/verify-phase-48.ts (Part 2 temp-DDL + assertion style, lines 40-60 & 461-530), scripts/fixtures/phase48-p11-manifest.fixture.json, src/lib/assetTypes.ts, scripts/backfill-candidate-groups.ts, src/routes/canvas/v2/select-winner.ts (top 30 lines + mount site)</read_first>
  <behavior>
    - GUARD-01 plan-level: 10-image manifest-only fixture batch through planGroups yields 4 groups (2 shots × first/last); exactly one primaryFilePath per group and it is a member; S01 first primary = first_frame_v2 (selected_first_variant=2), S02 last primary = last_frame_v2
    - GUARD-01 landed-shape: after ingestImagesPayload on :memory:, SQL asserts every group has exactly one isPrimaryView=1 row (assetsId NULL), all members assetsId=primary.id, all rows state='active', workflow_phase='p11'
    - Idempotency: seeded :memory: stock → applyBackfill, re-planBackfill → 0 diffs; second applyBackfill → 0 executed updates
    - Red line: seeded eliminated row's assetsId/isPrimaryView/workflow_phase/state unchanged after applyBackfill
    - D-04 priority: seeded row with meta.phase="p05_x" + /p07/ path → workflow_phase 'p05'; AND seeded row with only nested meta.provenance.phase="p10_voice" + path-underivable filePath → workflow_phase 'p10' (checker BL-1: nested channel must be covered by the contract)
    - No-drift: registry source contains z.enum(CANONICAL_ASSET_TYPES) with the truth-source import; every alias key ∉ canonical, every alias target ∈ canonical, expandTypesForQuery(target) ⊇ {target, alias}; no inline role/scene/tool enum in images.ts
  </behavior>
  <action>
Create scripts/verify-phase-50.ts with five sections plus the debt WARN, per D-07/D-08/D-09/D-10. Use the repo assert/PASS/FAIL/Summary/exit convention; target ≥ 30 assertions total.

Section 1 — GUARD-01 contract group (D-07):
(a) Plan-level: build the 17-image fixture batch exactly like verify-phase-48 Part 2 (S01 v1-3 first+last, S02 v1-2 first+last as /oss/{PROJ}/p11/iframes_{shot}/…, plus turnaround canonical+_v1..v3 and a scene pair per Part 2's shape — checker BL-2 correction: that batch yields 6 groups, NOT 4); planGroups(batch, fixtureManifests) → assert: **6 groups total**, with the 4 manifest groups' groupKeys shot:S01_B01:first|last, shot:S02_B01:first|last present among them (subset assertion); each group exactly one primaryFilePath and primaryFilePath ∈ memberFilePaths (assetsId self-consistency at plan level); selected_*_variant honored (S01 first → v2 file, S02 last → v2 file, unselected S02 first → v1 fallback).
(b) Landed-shape: temp :memory: knex with the mirrored o_assets/o_image DDL → await ingestImagesPayload(tempDb, { projectId, phase: "p11", images: batch, manifests: fixture }) → SQL assertions: per-group exactly one isPrimaryView=1; that row has assetsId NULL and its id equals every member's assetsId (self-consistency); zero rows with state ∉ {active}; zero member rows with isPrimaryView=1; workflow_phase='p11' on all 10 manifest rows. Destroy the temp pool after.

Section 2 — Backfill idempotency + red line (D-09 ②): fresh :memory: DB with the same DDL, seeded via raw inserts to mimic stock: project 1 — p04 family (canonical + v1..v3, assetsId all NULL, isPrimaryView mixed 1/1/1/0), p07 family (v1..v3 only, all isPrimaryView=0), plus (i) an eliminated row whose filePath joins the p07 family (must stay byte-untouched), (ii) a row with meta JSON {"phase":"p05_x"} and a /p07/ path (D-04 meta-wins → 'p05'), (ii-b) a row with meta JSON {"provenance":{"phase":"p10_voice"}} and a path-underivable filePath like /oss/pipeline/c267871b/S1_0000.wav (D-04 nested-meta fallback → 'p10', checker BL-1), (iii) a row with no phase signal (stays NULL), (iv) a duplicate-path pair (skipped from grouping, reported). planBackfill → assert planned diffs > 0 and the expected targets (groups=2, member assetsId writes, primary flips listed); applyBackfill → assert executedUpdates == diff count; SQL re-assert exactly-one-primary + member linkage (same checks as Section 1b); re-planBackfill → assert diffs length === 0 (idempotency); applyBackfill(second) → executedUpdates === 0; assert eliminated row unchanged; assert meta-priority row workflow_phase === 'p05'; assert nested-provenance row workflow_phase === 'p10'; assert duplicate-path rows still ungrouped.

Section 3 — Vocabulary no-drift (D-08/D-09 ③): import CANONICAL_ASSET_TYPES, LEGACY_ASSET_TYPE_ALIASES, expandTypesForQuery from ../src/lib/assetTypes (identity — never copy literals into the verify script); read src/routes/v1/assets-registry/index.ts source text and assert it imports from the truth source AND contains `z.enum(CANONICAL_ASSET_TYPES)`; read src/routes/v1/pipeline/ingest/images.ts and assert the inline legacy enum literal is absent; assert every alias key is NOT a canonical value, every alias target IS canonical, and for each alias expandTypesForQuery(alias.target) contains both the target and the alias key (bidirectional, no orphans).

Section 4 — Phase 48/49 spot invariants (D-09 ④ — existence/identifier checks ONLY, never spawning verify:phase-48/49): fs.existsSync on src/routes/canvas/v2/select-winner.ts, src/lib/canvasRelationalStore.ts, src/lib/reviewBridge.ts, src/lib/canvasAssetLinkage.ts, src/lib/ingestAssets.ts, src/lib/candidateGrouping.ts, src/lib/assetTypes.ts; source-text assertions: select-winner.ts contains `void resolveOpenReviewForSelection(` and `syncAssetPrimaryForWinner`; canvasRelationalStore.ts exports selectWinnerInGroup; reviewBridge.ts exports resolveOpenReviewForSelection; canvasAssetLinkage.ts exports applyRegistrySelectionToCanvas.

Section 5 — SC-4 debt notice (D-11): exactly one console.log line starting `[WARN]` referencing the SC-4 consumer-side half-loop debt and pointing at .planning/phases/49-*/49-HUMAN-UAT.md G-1 (kmc 30s poller vocabulary mismatch — cross-repo debt, out of scope). No assertion attached.

Register in package.json: `"verify:phase-50": "npx tsx scripts/verify-phase-50.ts"` directly after the verify:phase-49-linkage entry (D-10).

Safety invariants for this file: ":memory:" only — zero occurrences of the string db2.sqlite anywhere including comments (header says "the production database file is never touched"); no dynamic import of app server code; pool destroyed before exit.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run verify:phase-50; echo "verify-exit=$?"; grep -c 'db2.sqlite' scripts/verify-phase-50.ts; grep -c '"verify:phase-50"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exit 0
    - `npm run verify:phase-50` exit 0, prints ≥ 30 assertions with zero FAIL lines, ends with `=== Summary: N/N assertions passed ===` and a PASSED verdict line
    - Output contains the GUARD-01 section banner and exactly one `[WARN]` SC-4 debt line
    - Forced-failure sanity (49-02 tradition): temporarily flip one Section 1b expected value → run exits 1 with a FAIL line; restore → green again
    - `grep -c 'db2.sqlite' scripts/verify-phase-50.ts` → 0 (comments included)
    - `grep -c 'planBackfill\|applyBackfill' scripts/verify-phase-50.ts` ≥ 2 (idempotency harness drives the real backfill core)
    - `grep -c '"verify:phase-50": "npx tsx scripts/verify-phase-50.ts"' package.json` → 1
    - `npm run verify:phase-48` and `npm run verify:phase-49` still exit 0 (no cross-gate regression)
  </acceptance_criteria>
  <done>GUARD-01 formalized as a green permanent contract group; verify:phase-50 aggregates contract + idempotency + red line + no-drift + spot invariants + SC-4 WARN; registered in package.json; exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: D-12 deprecation headers on manual registration scripts + backfill archival annotation</name>
  <files>scripts/canvas/register_turnaround_b2.py, scripts/canvas/register_character_design.py, scripts/canvas/register_turnaround_costume.py, scripts/canvas/register_storyboard_sheets.py, scripts/canvas/register-scene-refs.py (comment-only edits), scripts/backfill-candidate-groups.ts (header annotation only)</files>
  <read_first>scripts/canvas/oneoffs/backfill-asset-descriptions.py (lines 1-25 — Phase 47 deprecation-header format), head of each of the five target scripts</read_first>
  <action>
1. For each of the five manual registration scripts (register_turnaround_b2.py, register_character_design.py, register_turnaround_costume.py, register_storyboard_sheets.py, register-scene-refs.py): insert a deprecation block immediately after the shebang line, BEFORE the existing module docstring content, following the Phase 47 header style — a `[DEPRECATED — Phase 50, 2026-08-19]` marker stating: new assets flow through the Phase 48 candidate-aware ingest route POST /api/v1/pipeline/ingest/images (auto grouping + workflow_phase); this script is a manual historical registration tool kept as historical evidence per D-12; do not use for new registrations; do not cron. Keep the entire existing file content untouched otherwise — comment insertion only, no deletion (D-12: historical evidence).

2. scripts/backfill-candidate-groups.ts: extend the ONE-OFF header with the archival annotation lines recording the completed apply — APPLIED date, executed update count and headline grouping/wf numbers from 50-01's backfill-post-run-verify.txt, backup filename data/db2-backup-pre-phase-50.sqlite, pointer to the three audit txt files. Wording: "applied and archived — do not run again" (mirrors the Phase 47 archive semantics; the script already refuses --apply without --i-backed-up-db as the mechanical backstop). Comment-only change; do not touch any code path.

Self-referential grep hygiene (49-02 lesson): the annotations must not introduce the literal strings this phase's acceptance greps require to be zero (e.g. never write the utils-barrel path literal or a production-DB path mention that a verify gate counts).
  </action>
  <verify>
    <automated>for f in scripts/canvas/register_turnaround_b2.py scripts/canvas/register_character_design.py scripts/canvas/register_turnaround_costume.py scripts/canvas/register_storyboard_sheets.py scripts/canvas/register-scene-refs.py; do head -8 "$f" | grep -c 'DEPRECATED — Phase 50'; done; python3 -m py_compile scripts/canvas/register_turnaround_b2.py scripts/canvas/register_character_design.py scripts/canvas/register_turnaround_costume.py scripts/canvas/register_storyboard_sheets.py scripts/canvas/register-scene-refs.py && echo PY-OK; npm run verify:phase-50 --silent > /dev/null; echo "verify-exit=$?"; git diff --stat</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 scripts: `head -8 <file> | grep -c 'DEPRECATED — Phase 50'` → 1
    - Each header mentions the Phase 48 ingest route path (`pipeline/ingest/images` substring present in the inserted block)
    - `python3 -m py_compile` passes for all five (comment-only edits, syntax intact)
    - `git diff --stat` for the five .py files shows insertions only (no deletions)
    - scripts/backfill-candidate-groups.ts header contains "applied and archived" + the backup filename; `npm run verify:phase-50` still exits 0 after the header edit
    - No file deleted: all five register scripts still present on disk
  </acceptance_criteria>
  <done>Legacy manual-registration tooling visibly deprecated with pointers to the Phase 48 chain; backfill script archived with its apply record; verify:phase-50 still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify suite → source files under test | verify reads route/lib sources and asserts identifiers — read-only, never executes app code |
| seeded :memory: fixtures → assertions | synthetic stock data stands in for production rows; must never reference a real DB file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-05 | Tampering | verify hitting production DB by mistake | mitigate | ":memory:" filename literal only; zero occurrences of the production DB path string anywhere in the file (grep gate, 48-02 convention); knex pool destroyed before exit |
| T-50-06 | Repudiation | contract suite passing vacuously | mitigate | forced-failure sanity step (flip one expectation → exit 1 FAIL) proves the gate can fail; assertion count printed in Summary |
| T-50-07 | Tampering | deprecation headers altering script behavior | mitigate | comment-only insertions verified by py_compile + git diff --stat insertions-only |
| T-50-SC | Tampering | package installs | accept | zero new packages — tsx/knex/better-sqlite3/fs built-ins only |
</threat_model>

<verification>
- `npx tsc --noEmit` exit 0
- `npm run verify:phase-50` exit 0, ≥ 30 assertions, zero FAIL, Summary + verdict line, one [WARN] SC-4 line
- Forced-failure sanity demonstrated then restored
- `npm run verify:phase-48` / `verify:phase-49` / `verify:phase-49-bridge` / `verify:phase-49-linkage` all still exit 0
- 5 register scripts carry DEPRECATED headers, py_compile clean, insertions-only diff
- backfill script header carries the applied-and-archived annotation
</verification>

<success_criteria>
- GUARD-01: fixture→planGroups→landed-shape contract formalized and green (D-07)
- GUARD-02: verify:phase-50 aggregates contract + backfill idempotency/red-line + enum no-drift + 48/49 spot invariants + SC-4 WARN; registered per the verify tradition (D-09/D-10/D-11)
- D-12: legacy manual registration scripts deprecated (not deleted); backfill archived
</success_criteria>

<output>
Create `.planning/phases/50-historical-backfill-contract-guards/50-02-SUMMARY.md` when done. This is the final plan of v2.1 — note the milestone is complete (48 ✓ 49 ✓ 50 ✓) in the SUMMARY so milestone-close tooling can pick it up.
</output>
