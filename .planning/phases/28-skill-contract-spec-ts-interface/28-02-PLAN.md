---
phase: 28-skill-contract-spec-ts-interface
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - .planning/specs/SKILL-CONTRACT.md
  - src/skills/__tests__/contract.test.ts
  - scripts/verify-phase-28.ts
autonomous: true
requirements:
  - CONTRACT-03
  - CONTRACT-04

must_haves:
  truths:
    - "A developer reading .planning/specs/SKILL-CONTRACT.md can enumerate every required and optional manifest field without opening any other file"
    - "SKILL-CONTRACT.md contains a '## Contract Invariants' section that explicitly lists all four invariants: descriptive-only, version major.minor additive, platform accepts any 1.x, node IDs are <skill_id>::<type>"
    - "SKILL-CONTRACT.md contains a '## Versioning' section stating major.minor format, minor = strictly additive, major = breaking, platform accepts any 1.x"
    - "Running the field-equality drift test fails with a per-field diagnostic like \"Field '<name>' present in zod but missing in spec\" if the zod schema and spec field table disagree"
    - "Running the negative-input validator tests confirms validateManifest() returns the correct ruleId for each malformed input class"
    - "tsx scripts/verify-phase-28.ts exits 0 when all assertions pass and exits 1 when any assertion fails"
  artifacts:
    - path: ".planning/specs/SKILL-CONTRACT.md"
      provides: "Human-readable Skill Manifest contract spec — single source of truth for skill authors"
      contains: "## Contract Invariants"
    - path: "src/skills/__tests__/contract.test.ts"
      provides: "Field-equality drift test (spec ↔ zod) + negative-input validator tests covering all five ruleIds"
      contains: "MANIFEST_REQUIRED_FIELD"
      exports:
        - "testFieldEqualityDrift"
        - "testNegativeInputs"
    - path: "scripts/verify-phase-28.ts"
      provides: "Executable test runner (no vitest/jest — follows repo verify-phase-*.ts convention)"
      contains: "verify-phase-28"
  key_links:
    - from: "src/skills/__tests__/contract.test.ts"
      to: "src/skills/validator.ts"
      via: "import { validateManifest, manifestSchema }"
      pattern: "from [\"']\\.{2}/validator[\"']|from [\"']@/skills/validator[\"']"
    - from: "src/skills/__tests__/contract.test.ts"
      to: ".planning/specs/SKILL-CONTRACT.md"
      via: "fs.readFileSync + markdown field-table parse"
      pattern: "SKILL-CONTRACT\\.md"
    - from: "scripts/verify-phase-28.ts"
      to: "src/skills/__tests__/contract.test.ts"
      via: "import test functions"
      pattern: "from [\"']\\.\\./src/skills/__tests__/contract\\.test[\"']"
---

<objective>
Write the human-readable Skill Manifest contract spec at `.planning/specs/SKILL-CONTRACT.md`, then create a field-equality drift test + negative-input validator test suite that mechanically links the spec to the zod schema from Plan 01.

Purpose: This closes the C1 pitfall (spec doc vs validator divergence) and codifies the four contract invariants (Pitfalls A2, A3, A4) in human-readable prose. The drift test is the CI gate that prevents the spec from silently going stale when someone adds a field to `validator.ts` without updating the doc. The negative tests lock in the ruleId vocabulary that Phase 33's compliance tests will assert against.

Output: Three files — the spec doc, the test module, and a verify-phase-28.ts runner that wires the test module into the repo's existing `tsx scripts/verify-phase-*.ts` convention (Pitfalls B3: no vitest/jest in this project; v1.5 used `verify-phase-23.ts`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-skill-contract-spec-ts-interface/28-CONTEXT.md
@.planning/phases/28-skill-contract-spec-ts-interface/28-01-PLAN.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- Plan 01 produces contract.ts + validator.ts. Plan 02 consumes them. -->
<!-- The drift test reads zod's .shape to enumerate fields, then compares against -->
<!-- the spec doc's field table. The negative tests invoke validateManifest() -->
<!-- and assert on error.ruleId. -->

From src/skills/contract.ts (produced by Plan 01):
- export interface SkillManifest { skill_id, version, display_name, description, media_types, node_types, phase_taxonomy, asset_categories, review_criteria, engine_task_types, runtime }
- export interface ManifestValidationError { ruleId, field, message, raw }
- export type ManifestValidationResult = { ok: true; value: SkillManifest } | { ok: false; errors: ManifestValidationError[] }

From src/skills/validator.ts (produced by Plan 01):
- export const manifestSchema: z.ZodObject<...>  (walk .shape for drift test)
- export function validateManifest(input: unknown): ManifestValidationResult

Rule ID vocabulary (locked in CONTEXT.md, enforced by Plan 01):
- 'MANIFEST_REQUIRED_FIELD' — missing required field
- 'MANIFEST_TYPE_MISMATCH' — wrong type
- 'MANIFEST_VERSION_FORMAT' — version not major.minor
- 'NODE_ID_NAMESPACING' — bare node type id
- 'MANIFEST_UNKNOWN_FIELD' — strict mode rejected unknown key

From scripts/verify-phase-23.ts (v1.5 precedent — the test runner pattern):
- #!/usr/bin/env tsx
- import { ... } from "../src/services/gpu"
- function assert(cond, name, detail?) — pushes to results[]
- main().catch(err => process.exit(2)); exit 1 if any failed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write .planning/specs/SKILL-CONTRACT.md — full manifest field reference + four contract invariants</name>
  <files>.planning/specs/SKILL-CONTRACT.md</files>
  <read_first>
    - src/skills/contract.ts (the interface this spec must mirror — every exported field must appear in the spec's field table)
    - src/skills/validator.ts (the zod schema — confirm required vs optional flag for each field by reading .shape; required vs optional affects the spec's "Required" column)
    - .planning/phases/28-skill-contract-spec-ts-interface/28-CONTEXT.md (LOCKED: four invariants must be stated explicitly; English-first prose)
    - .planning/research/ARCHITECTURE.md lines 142-185 (the field semantics — borrow descriptive prose for each field)
    - .planning/research/PITFALLS.md sections A2, A3, A4 (the invariants the spec must codify)
  </read_first>
  <action>
    Create directory `.planning/specs/` (does not exist yet — confirmed in Plan 01 context). Write `.planning/specs/SKILL-CONTRACT.md` as English-first human-readable prose (per CONTEXT.md deferred item — spec is English-first for skill-author portability; Phase 34 docs may have bilingual variants).

    Structure the document with these sections, in this order:

    **1. `# Skill Manifest Contract`** — one-paragraph intro: what a Skill Manifest is, that it lives at `src/skills/contract.ts` (TypeScript) + `src/skills/validator.ts` (zod), and that this document is the human-readable companion.

    **2. `## Contract Invariants`** — explicitly enumerate the four invariants (per CONTEXT.md specifics, these MUST appear verbatim in concept):
      - **Invariant 1 — Descriptive only.** A Skill Manifest declares data shape only. It contains no functions, no methods, no executable code, no React component URLs. Behavior is platform-side and dispatched by `phase_id` and `node_type`. (References CONTRACT-06, Pitfalls A4.)
      - **Invariant 2 — Version is `major.minor`.** The version string uses exactly two numeric segments (e.g., `1.0`, `1.1`, `2.0`). No patch segment, no leading `v`. (References CONTRACT-04.)
      - **Invariant 3 — Minor versions are strictly additive; major versions may break.** A minor bump may add optional fields with safe defaults ONLY. No new required fields, no field removals, no enum narrowing, no type changes. A major bump may do any of those. (References CONTRACT-04, Pitfalls A2.)
      - **Invariant 4 — Platform accepts any `1.x` manifest at runtime.** The platform's runtime major version must match the manifest's major. Any minor version within the same major is accepted. Refusing a manifest requires a major-version mismatch. (References CONTRACT-04, Pitfalls A2.)
      - **Invariant 5 — Node type IDs are namespaced `<skill_id>::<type>`.** Bare IDs like `script` are rejected by the validator. Namespacing prevents collisions when multiple skills coexist (v1.7+ scope). The format regex is `^[a-z0-9-]+::[a-z0-9-]+$`. (References CONTRACT-05, Pitfalls A3.)

    Note: the ROADMAP mentions "four contract invariants" but CONTEXT.md specifics and Pitfalls together require five distinct statements (the versioning rule splits into format + semantics + accept-policy). Number them 1-5 in the spec; the test in Task 2 asserts the section exists and contains the key phrases, not a specific count.

    **3. `## Versioning`** — expand Invariants 2-4 into a dedicated section. State explicitly: (a) format `^\d+\.\d+$`, (b) minor = additive-only with safe-default optional fields, (c) major = breaking change permitted, (d) platform accepts any `1.x` at runtime, (e) only the current major is supported (v1.x → v2.x migration is the skill author's problem — Pitfalls C3).

    **4. `## Field Reference`** — a markdown table with one row per manifest field. Columns: `Field`, `Type`, `Required`, `Description`. This table is the source-of-truth the drift test parses (Task 2). Walk every field of `SkillManifest` + its sub-interfaces from contract.ts and produce one row for each.

    Root-level rows (11 — must match SkillManifest exactly):
    | Field | Type | Required | Description |
    | `skill_id` | string | yes | Unique registry key (e.g., `movie-v1`) |
    | `version` | string (major.minor) | yes | Manifest version, e.g. `1.0` |
    | `display_name` | string | yes | Human-readable skill name |
    | `description` | string | yes | One-paragraph skill description |
    | `media_types` | MediaType[] | yes | Output media types: video, image, audio, 3d |
    | `node_types` | NodeTypeDecl[] | yes | Canvas node types this skill contributes |
    | `phase_taxonomy` | PhaseDecl[] | yes | Ordered pipeline phases |
    | `asset_categories` | AssetCategoryDecl[] | yes | Asset kinds this skill owns |
    | `review_criteria` | ReviewCriteriaDecl | yes | Scoring thresholds |
    | `engine_task_types` | string[] | yes | Subset of gold-team TaskType enum |
    | `runtime` | SkillRuntimeDecl | yes | How the platform talks to the skill orchestrator |

    Sub-type rows (append a sub-table or sub-section for each sub-interface — the drift test parses both root + nested):
    - `NodeTypeDecl`: type, label, icon, color, data_schema_uri, default_renderer (all required)
    - `PhaseDecl`: id, order, label, requires_review, ingest_outputs (all required)
    - `AssetCategoryDecl`: id, label (both required)
    - `ReviewCriteriaDecl`: auto_threshold, human_threshold (both required)
    - `SkillRuntimeDecl`: type (required), endpoint, healthcheck_path, callback_url_template (all optional)

    Use the literal phrase "Required: yes" or "Required: no" in the column — the drift test greps for that token. Do NOT use checkmarks or emojis (the test parser is text-based).

    **5. `## Validation Rules`** — one subsection per ruleId, explaining when each fires:
    - `MANIFEST_REQUIRED_FIELD` — a required field is absent
    - `MANIFEST_TYPE_MISMATCH` — a field has the wrong type
    - `MANIFEST_VERSION_FORMAT` — version does not match `^\d+\.\d+$`
    - `NODE_ID_NAMESPACING` — a node type id lacks the `<skill_id>::<type>` prefix
    - `MANIFEST_UNKNOWN_FIELD` — strict mode rejected an unrecognized top-level or nested key

    Each subsection: 2-3 sentences explaining the trigger condition, then one example of an input that trips it and the error shape returned.

    **6. `## Strict Mode`** — short paragraph: zod root schema uses `.strict()`, every nested object uses `.strict()`. Unknown keys at any level are rejected. This forces manifest authors to declare every field and catches typos. (References CONTEXT.md locked decision.)

    **7. `## Out of Scope (v1.7+)`** — list deferred features so skill authors know what NOT to expect: custom_renderer_url (custom React components over HTTP), capability_negotiation, permission_matrix, sandboxing, marketplace discovery. One bullet per item with a one-sentence rationale.

    Do NOT include a worked `movie-v1.manifest.json` example in this spec — that artifact is produced by Phase 33 (COMPLIANCE-01) and belongs in `docs/skill-author-guide.md` (Phase 34, DOCS-04). The spec is the SCHEMA reference; the guide is the EXAMPLE reference.

    Do NOT auto-generate this doc from zod (per CONTEXT.md locked decision — auto-generation loses explanatory prose). Hand-write it.
  </action>
  <verify>
    <automated>test -f /data/workspace/kais-aigc-platform/.planning/specs/SKILL-CONTRACT.md && grep -c "## Contract Invariants" /data/workspace/kais-aigc-platform/.planning/specs/SKILL-CONTRACT.md | grep -q '^1$' && grep -c "## Versioning" /data/workspace/kais-aigc-platform/.planning/specs/SKILL-CONTRACT.md | grep -q '^1$' && grep -c "## Field Reference" /data/workspace/kais-aigc-platform/.planning/specs/SKILL-CONTRACT.md | grep -q '^1$' && echo "spec structure OK"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/specs/SKILL-CONTRACT.md` exists
    - Contains section `## Contract Invariants` enumerating at minimum: descriptive-only, version major.minor, additive minor, platform accepts any 1.x, node IDs are namespaced
    - Contains section `## Versioning` with the regex `^\d+\.\d+$` stated and the minor-additive rule
    - Contains section `## Field Reference` with a markdown table whose rows cover all 11 root-level SkillManifest fields (skill_id, version, display_name, description, media_types, node_types, phase_taxonomy, asset_categories, review_criteria, engine_task_types, runtime)
    - Each sub-interface (NodeTypeDecl, PhaseDecl, AssetCategoryDecl, ReviewCriteriaDecl, SkillRuntimeDecl) has its fields enumerated in a table or sub-section
    - Each field's "Required" column contains the literal token "yes" or "no" (no checkmarks/emojis — drift test greps text)
    - Contains section `## Validation Rules` with one subsection per ruleId (MANIFEST_REQUIRED_FIELD, MANIFEST_TYPE_MISMATCH, MANIFEST_VERSION_FORMAT, NODE_ID_NAMESPACING, MANIFEST_UNKNOWN_FIELD)
    - Contains section `## Strict Mode` mentioning zod `.strict()`
    - Contains section `## Out of Scope (v1.7+)` listing at least: custom_renderer_url, capability_negotiation, permission_matrix, sandboxing, marketplace
    - File is English-first (no Chinese prose in the body — section titles and field descriptions all English)
  </acceptance_criteria>
  <done>
    `.planning/specs/SKILL-CONTRACT.md` is a complete, human-readable reference. A developer reading only this file can enumerate every required and optional manifest field, state the four/five contract invariants, and predict which ruleId a given malformed input will trigger.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/skills/__tests__/contract.test.ts + scripts/verify-phase-28.ts — drift test + negative validator tests</name>
  <files>src/skills/__tests__/contract.test.ts, scripts/verify-phase-28.ts</files>
  <read_first>
    - src/skills/contract.ts (Plan 01 output — confirms the field set the drift test walks)
    - src/skills/validator.ts (Plan 01 output — confirms manifestSchema.shape structure + validateManifest signatures)
    - .planning/specs/SKILL-CONTRACT.md (Task 1 output — the doc the drift test parses)
    - scripts/verify-phase-23.ts (v1.5 test runner precedent — copy the `assert()` + `results[]` + `main().catch(exit 2)` pattern)
    - .planning/phases/28-skill-contract-spec-ts-interface/28-CONTEXT.md (LOCKED: failure message format `"Field '<name>' present in zod but missing in spec"` or reverse)
    - .planning/research/PITFALLS.md section C1 (the drift-prevention rationale)
  </read_first>
  <behavior>
    Drift test expectations:
    - For every top-level key in manifestSchema.shape, the spec's root field-reference table contains a row whose first column matches that key
    - For every nested-object schema (node_types element, phase_taxonomy element, asset_categories element, review_criteria, runtime), every key in that schema's .shape appears in the corresponding spec sub-section
    - For every row in the spec's root field-reference table, the field name exists as a key in manifestSchema.shape
    - When a field is marked "Required: yes" in the spec, the corresponding zod field is NOT wrapped in .optional() (i.e., it is required in the schema)
    - When a field is marked "Required: no" in the spec, the corresponding zod field IS .optional()
    - If any of the above fails, the assertion message is exactly the format: `Field '<name>' present in zod but missing in spec` (or the reverse `Field '<name>' present in spec but missing in zod`, or `Field '<name>' required-flag mismatch: spec=<yes|no>, zod=<required|optional>`)

    Negative validator test expectations (one per ruleId):
    - MANIFEST_REQUIRED_FIELD: validateManifest({}) returns ok:false with errors[0].ruleId === 'MANIFEST_REQUIRED_FIELD'
    - MANIFEST_TYPE_MISMATCH: validateManifest(<manifest with skill_id: 123>) returns ok:false with some errors[i].ruleId === 'MANIFEST_TYPE_MISMATCH'
    - MANIFEST_VERSION_FORMAT: validateManifest(<manifest with version '1.0.0'>) returns ok:false with some errors[i].ruleId === 'MANIFEST_VERSION_FORMAT'
    - NODE_ID_NAMESPACING: validateManifest(<manifest with node_types[0].type: 'script'>) returns ok:false with some errors[i].ruleId === 'NODE_ID_NAMESPACING'
    - MANIFEST_UNKNOWN_FIELD: validateManifest(<manifest with extra top-level 'foobar': 1>) returns ok:false with some errors[i].ruleId === 'MANIFEST_UNKNOWN_FIELD'

    Happy-path test:
    - validateManifest(<well-formed manifest with version '1.0', namespaced node types 'movie-v1::script'>) returns ok:true with value deeply equal to the input

    Runner expectations:
    - tsx scripts/verify-phase-28.ts exits 0 when all assertions pass
    - tsx scripts/verify-phase-28.ts exits 1 when any assertion fails
    - tsx scripts/verify-phase-28.ts exits 2 on uncaught exception
  </behavior>
  <action>
    Create directory `src/skills/__tests__/` (does not exist yet). Create `src/skills/__tests__/contract.test.ts` as a plain TypeScript module that exports two async functions: `testFieldEqualityDrift(): Promise<void>` and `testNegativeInputs(): Promise<void>`. Each function collects assertion results into a local `results` array (matching the verify-phase-23 pattern) and throws on first failure OR returns the array — pick the convention that verify-phase-28.ts can aggregate. (Recommended: each function returns `{ passed: number; failed: number; failures: string[] }` so the runner can sum them.)

    **testFieldEqualityDrift() implementation:**

    1. Import `manifestSchema` from `../validator`. Access `manifestSchema.shape` to get the root-level keys (Object.keys(manifestSchema.shape)).
    2. Read `.planning/specs/SKILL-CONTRACT.md` via `fs.readFileSync(path.resolve(__dirname-or-process.cwd(), '.planning/specs/SKILL-CONTRACT.md'), 'utf8')`. Use `path.resolve(process.cwd(), ...)` so the test runs regardless of cwd.
    3. Parse the spec's root `## Field Reference` table. The table rows use pipe-delimited markdown (`| field | type | required | description |`). Extract rows under the root table (before any sub-section header). Build a Map<string, { required: 'yes' | 'no' }> keyed by field name.
    4. For each key in `manifestSchema.shape`: assert the spec Map has that key. If missing, push failure `"Field '<key>' present in zod but missing in spec"`.
    5. For each key in the spec Map: assert `manifestSchema.shape` has that key. If missing, push failure `"Field '<key>' present in spec but missing in zod"`.
    6. For each key present in both: determine zod's required-vs-optional by checking whether the schema element is a `ZodOptional` (check `manifestSchema.shape[key] instanceof z.ZodOptional` — or in zod v4, check `def.typeName === 'ZodOptional'` or use `.isOptional()` method if available). Compare against the spec's "Required" column. If mismatch, push `"Field '<key>' required-flag mismatch: spec=<yes|no>, zod=<required|optional>"`.
    7. Repeat steps 3-6 for each nested schema. Access nested shapes via: `manifestSchema.shape.node_types.element.shape`, `manifestSchema.shape.phase_taxonomy.element.shape`, `manifestSchema.shape.asset_categories.element.shape`, `manifestSchema.shape.review_criteria.shape`, `manifestSchema.shape.runtime.shape`. Parse the corresponding sub-sections in the spec. (The spec's sub-sections must be unambiguously headed — Task 1's action specifies sub-tables per sub-interface; the test parses each sub-table by locating the sub-interface name as a heading.)

    Note: the exact zod v4 API for "is this field optional" — verify by reading `node_modules/zod` types or running a quick probe in the runner. zod v4 schemas expose `.isOptional()` on ZodType in many builds; if unavailable, check `element._zod.def.type === 'optional'` (zod v4 internal) or wrap in a try/catch. Do NOT hand-roll regexes over the source code — use the runtime schema object.

    **testNegativeInputs() implementation:**

    Construct five minimal malformed manifest fixtures (each is a partial object — only enough fields to demonstrate the target ruleId). Importing the full set of required fields from a shared helper is fine. Each fixture exercises one ruleId:

    Fixture A (MANIFEST_REQUIRED_FIELD): `validateManifest({})` → assert `result.ok === false` and `result.errors.some(e => e.ruleId === 'MANIFEST_REQUIRED_FIELD')`.

    Fixture B (MANIFEST_TYPE_MISMATCH): start from a valid manifest base, set `skill_id: 123` (number instead of string). → assert `ok === false` and errors contain `ruleId === 'MANIFEST_TYPE_MISMATCH'`.

    Fixture C (MANIFEST_VERSION_FORMAT): valid base, set `version: '1.0.0'`. → assert `ruleId === 'MANIFEST_VERSION_FORMAT'`.

    Fixture D (NODE_ID_NAMESPACING): valid base, set `node_types: [{ type: 'script', label: 'Script', icon: 'page', color: '#fff', data_schema_uri: 'x', default_renderer: 'script' }]` (bare id, no namespace). → assert `ruleId === 'NODE_ID_NAMESPACING'`.

    Fixture E (MANIFEST_UNKNOWN_FIELD): valid base, add top-level `foobar: 1`. → assert `ruleId === 'MANIFEST_UNKNOWN_FIELD'`.

    Happy-path fixture: valid base with `version: '1.0'` and `node_types: [{ type: 'movie-v1::script', ... }]`. → assert `result.ok === true` and `result.value.skill_id === <base skill_id>`.

    Define the valid base manifest as a module-local constant `VALID_BASE` so all fixtures reuse it. The base uses skill_id `'movie-v1'`, version `'1.0'`, display_name `'Movie v1'`, description `'Reference skill'`, media_types `['video','image','audio']`, one valid node_type, one valid phase, one asset category, review_criteria `{ auto_threshold: 0.7, human_threshold: 0.9 }`, engine_task_types `['text-to-image','text-to-video']`, runtime `{ type: 'external-http', endpoint: 'http://localhost:8001' }`.

    **scripts/verify-phase-28.ts implementation:**

    Follow the `verify-phase-23.ts` pattern precisely:
    - `#!/usr/bin/env tsx` shebang
    - Header banner
    - Import the two test functions from `../src/skills/__tests__/contract.test`
    - A local `assert(cond, name, detail?)` helper that pushes to a `results` array
    - A `main()` async function that calls both test functions, sums their pass/fail counts, prints a summary table, and calls `process.exit(1)` if any failed
    - `main().catch(err => { console.error(err); process.exit(2); })`

    Add `scripts/verify-phase-28.ts` so it is executable via `tsx scripts/verify-phase-28.ts` from project root (matching how verify-phase-23.ts is invoked).

    Do NOT register the script in package.json scripts (no `"verify:phase-28"` key) — Phase 28 does not modify package.json. The user invokes `tsx scripts/verify-phase-28.ts` directly, same as v1.5.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && tsx scripts/verify-phase-28.ts 2>&1 | tail -5 && echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `src/skills/__tests__/contract.test.ts` exists and exports `testFieldEqualityDrift` and `testNegativeInputs`
    - `scripts/verify-phase-28.ts` exists with `#!/usr/bin/env tsx` shebang
    - `tsx scripts/verify-phase-28.ts` exits 0 (assuming Plan 01 + Task 1 are correct — if it fails, the failure messages use the exact CONTEXT.md format `"Field '<name>' present in zod but missing in spec"` or `"Field '<name>' present in spec but missing in zod"`)
    - The negative test covers all five ruleIds: MANIFEST_REQUIRED_FIELD, MANIFEST_TYPE_MISMATCH, MANIFEST_VERSION_FORMAT, NODE_ID_NAMESPACING, MANIFEST_UNKNOWN_FIELD
    - The happy-path test confirms a well-formed manifest returns `{ ok: true, value: ... }`
    - The drift test compares BOTH directions (zod → spec AND spec → zod) so additions on either side are caught
    - The drift test compares required-vs-optional flags, not just field names
    - `tsc --noEmit` introduces zero new errors in the test files
  </acceptance_criteria>
  <done>
    `tsx scripts/verify-phase-28.ts` exits 0. The drift test would fail with a per-field diagnostic if anyone later adds a field to `validator.ts` without updating `SKILL-CONTRACT.md` (or vice versa). The negative tests lock in the ruleId vocabulary Phase 33 will assert against. The runner follows the repo's verify-phase-*.ts convention (no new test framework introduced).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec-author → test runner | The drift test parses a markdown file. A malicious edit to the spec could inject content that confuses the parser. Mitigation: parser is read-only `fs.readFileSync` + regex; no eval, no dynamic import based on spec content. |
| test input → validateManifest | The negative tests construct malformed fixtures in-code (not from external input). No new trust boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | Tampering | drift test parser | accept | Parser is text-grep over a markdown file committed to the repo. An attacker who can edit the spec can also edit the test. No additional mitigation beyond repo access controls. |
| T-28-06 | Denial of Service | verify-phase-28.ts runtime | accept | Test runs in <2s on local tsx. No external I/O except reading one markdown file. Acceptable. |
| T-28-SC | Tampering | npm install | accept | No new packages installed in this plan. fs/path/zod all already in package.json. |

</threat_model>

<verification>
Phase 28 Plan 02 is complete when:

1. **Spec doc is complete:** `.planning/specs/SKILL-CONTRACT.md` contains all required sections per Task 1's acceptance criteria, with the field table covering all 11 root fields + all sub-interface fields.

2. **Drift test passes:** `tsx scripts/verify-phase-28.ts` exits 0. The drift test confirms spec ↔ zod field-for-field equality (names + required-flags) in BOTH directions.

3. **Negative tests pass:** All five ruleIds are exercised. Each malformed fixture produces the expected ruleId in the errors array.

4. **No spec drift can occur silently:** If an executor temporarily adds a fake field to validator.ts (e.g., `priority: z.string()`) without updating the spec, `tsx scripts/verify-phase-28.ts` exits 1 with message `Field 'priority' present in zod but missing in spec`. (Executor should NOT actually do this — it's a verification thought-experiment.)

5. **No test framework added:** `grep -E '"vitest"|"jest"|"mocha"' package.json` still returns empty. The verify-phase-*.ts convention is preserved.
</verification>

<success_criteria>
- CONTRACT-03 satisfied: spec doc at `.planning/specs/SKILL-CONTRACT.md` documents the contract; field-equality drift test mechanically links spec to zod schema (no silent drift possible).
- CONTRACT-04 satisfied: spec explicitly states versioning rules — `major.minor` format, minor = strictly additive, major = breaking, platform accepts any `1.x` at runtime.
- CONTRACT-06 (prose): spec's `## Contract Invariants` section explicitly states "manifest is descriptive; behavior is platform-side — no executable code in manifest."
</success_criteria>

<output>
Create `.planning/phases/28-skill-contract-spec-ts-interface/28-02-SUMMARY.md` when done.
</output>
