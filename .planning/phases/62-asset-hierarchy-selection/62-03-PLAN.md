---
phase: 62-asset-hierarchy-selection
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/infinite-canvas/test/e2e/mock-backend/server.mjs
autonomous: true
requirements: [HIER-05]
must_haves:
  truths:
    - "PATCH /api/v1/assets-registry/:id mock 在场:记录调用到 /__mock/calls 并真实变更内存 fixture 的 isPrimaryView/state(D-05 序列断言与后续 UI 态的基础)"
    - "POST /api/canvas/v2/variant-groups/:groupId/select-winner mock 镜像真端点形状(200 applied:false 幂等/404/409/可注入 500),全尝试落 /__mock/calls"
    - "generation-config GET/PUT mock 在场:rows 由可注入 fixture(覆盖层/requirement 形态三档)生成,PUT 返回 writeState 可经 /__mock/config 注入 synced/file-fail"
    - "search fixture 扩展为可 reset 的多组结构(角色概念组/场景组/声纹组/keyframe 组/单产物含 reportAudit),默认 preset 字节等价 61 既有 2 条 fixture(phase61-debt 回归锚零扰动)"
    - "全部新路由位于 express.static(DIST_DIR) 之前(mock 面不被静态兜底吞掉)"
  artifacts:
    - path: "packages/infinite-canvas/test/e2e/mock-backend/server.mjs"
      provides: "PATCH assets-registry / select-winner / generation-config GET+PUT / rich search preset(/__mock/config 切换)五组 mock 面"
      contains: "select-winner"
  key_links:
    - from: "PATCH mock"
      to: "state.fixtureAssets(内存 fixture)"
      via: "按 id 命中即改 isPrimaryView/state,响应 200"
      pattern: "assets-registry/"
    - from: "PUT overrides mock"
      to: "/__mock/calls + state.generationConfig.overrides"
      via: "载荷保真记录 + 内存覆盖层,writeState 按 state.config.genCfgWriteState 注入"
      pattern: "genCfgWriteState"
    - from: "search rich preset"
      to: "62-01 getGroupKey 词表(char:/scene:/keyframe:)"
      via: "fixture 组键覆盖三态/手动组/单件/reportAudit 各形态"
      pattern: "assetFixture"
---

<objective>
P7 mock-backend 扩面:补齐 Phase 62 e2e(62-07)所需的五组 mock 面——PATCH assets-registry、
select-winner、generation-config GET/PUT、多组可 reset 的 search fixture——默认行为与
61 既有面字节等价(回归零扰动),Phase 62 用例经 /__mock/config 预设切换。

Purpose: RESEARCH G 实测既有 mock 撑不起层级/三态/计数断言(固定 2 条 fixture);
62-07 三文件全部断言面依赖本 plan。
Output: server.mjs 单文件扩展(与 62-02 并行开发,契约按 UI-SPEC C8 锁形 +
RESEARCH C select-winner 形状,不依赖 62-02 实码)。

裁定(plan 内锁定):
- 默认 search 响应字节等价 61 fixture(id 90001/90002 两条原样);rich preset 经
  POST /__mock/config { assetFixture: 'rich' } 激活、/__mock/reset 归位默认——
  phase61-debt 的 `.am-card` 可见性断言零扰动。
- select-winner mock 不模拟服务端 PATCH-linkage 联动(RESEARCH C:真服务端在
  PATCH isPrimaryView=true 时自动 applyRegistrySelectionToCanvas;e2e 断言纪律
  已按「客户端 POST 发出 + 最终态正确」写,勿断 applied:true)——mock 保持两通道
  分离,客户端 POST 的真实增量(恰一次调用)即可观测。
- generation-config mock 的 rows 生成逻辑**不复制** 62-02 服务端合并实现:按
  /__mock/config 注入的三档文件形态(not-found/requirement-v25/legacy)+ 内存 overrides
  直接查表生成(简单映射,e2e 只锁 UI 断言面不锁服务端合并算法——合并算法由 62-02
  node:test 锁)。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/62-asset-hierarchy-selection/62-RESEARCH.md
@.planning/phases/62-asset-hierarchy-selection/62-UI-SPEC.md
@.planning/phases/62-asset-hierarchy-selection/62-PATTERNS.md

<interfaces>
<!-- 已提取契约——执行者直接消费,无需再探索 -->

server.mjs 现状(524 行量级):
  - state 结构 + reset() 在「Mock State」段;logCall(method, path, body, response)
    落 /__mock/calls(GET /__mock/calls 读);broadcastToProject(projectId, event, data);
  - POST /api/v1/assets-registry/search(:255-266 附近)固定 2 条 fixture 无条件返回;
  - POST /api/canvas/v2/nodes/(:218-250)是「logCall 全尝试记录 + 400/409/200 + 5ms
    setTimeout 广播」模板;
  - /__mock 段(:513-529):state|reset|emit|calls|config;state.config 现有
    { orchDelay, previewDelay, failSecondNode };
  - express.static(DIST_DIR) 在 /__mock 段**之后**——新路由必须插在 static 之前。
真 select-winner 形状(RESEARCH C,镜像对象):
  body { projectId, episodesId, winnerNodeId(1-128), frameSlot? };
  200 { groupId, winnerNodeId, applied: true|false }(同 winner 重放 → applied:false);
  404 组不存在;409 winner 不在组内/非 single/locked;400 载荷;zod-lite 校验即可。
  注意「图未加载不是失败」——mock 直查内存 variantGroups 表。
UI-SPEC C8 锁形(UI-GREY-2 已裁):GET ?projectId&episodesId → { rows: [...]
  { phaseKey, tier, label, pre, final, source, editable, ... } };
  PUT /overrides/:phaseKey body { projectId, episodesId, nCandidates, finalCandidates }
  → { writeState: 'override'|'synced'|'file-fail' }。
search rich preset 形状(供 62-04/05/07 断言,组键走 getGroupKey 词表):
  - char 组:characterId 'shenzhiyi' × 3(1 selected isPrimaryView=true state active +
    2 pending,meta.subtype 缺省→concept 键 char:shenzhiyi:concept,createdAt 递增);
  - scene 组:type 'scene' name '宴会厅' × 2(全 pending——手动组,isSceneGroup);
  - voice 组:characterId 'shenzhiyi' type 'voice' × 2(isVoiceGroup 手动组);
  - keyframe 组:type 'keyframe' characterId 'S01' name 'S01_first_v1'/'S01_first_v2'
    (剥 _v 后同键)× 2 + meta.phaseCode 'P09' 直读样本;
  - 媒体单件:type 'video' name 'SH01' × 1(pending);
  - 文本单件:type 'outline' × 1(isPrimaryView=true);
  - reportAudit 单件:subtype delivery_package(meta.subtype)× 1;
  - id 从 91001 起递增;每条含完整 AssetDetail 形状 + createdAt(排序键面,UI-GREY-1 裁定)
    + filePath 非空(除文档型)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: rich search fixture(可 reset + preset 切换)+ PATCH assets-registry</name>
  <files>packages/infinite-canvas/test/e2e/mock-backend/server.mjs</files>
  <read_first>
  - packages/infinite-canvas/test/e2e/mock-backend/server.mjs 全文(重点:Mock State 段/reset()/search 路由/__mock 段与 static 顺序)
  - packages/infinite-canvas/src/services/canvasApi.ts L203-222(AssetDetail 完整字段)
  - 62-01-PLAN.md <interfaces>(getGroupKey 分支词表——fixture 组键依据)
  </read_first>
  <action>
  (1) fixture 层:新增 buildRichFixture() 返回 <interfaces> 形状的全量数组(注释注明
      「62-03:Phase 62 层级/三态/计数断言面;组键对齐 getGroupKey 词表」);
      state 增 state.fixtureAssets = null(默认 null = 用既有 2 条常量);
      reset() 归位 state.fixtureAssets = null。
  (2) search 路由改造:响应 assets = state.fixtureAssets ?? 既有 2 条常量(默认路径
      响应字节不变);logCall 照旧。
  (3) /__mock/config 扩展:接受 { assetFixture: 'rich' } → state.fixtureAssets =
      buildRichFixture();接受 { assetFixture: null } 清回默认。/__mock/reset 后
      config 归位既有默认(keepConfig 分支语义保持)。
  (4) 新路由 PATCH /api/v1/assets-registry/:id(插在 search 路由后、static 前):
      logCall('PATCH', `/api/v1/assets-registry/${id}`, body, null) 全尝试记录;
      按 Number(req.params.id) 在当前生效 fixture(含默认 2 条)中查行——命中则
      逐键应用 body 中 isPrimaryView/state/tags(仅白名单键),响应
      { code: 200, data: { asset: 变更后行 } };未命中 { code: 404, message: '资产不存在' }。
  (5) 自检:node --check server.mjs;起服务冒烟(MOCK_PORT=9891 后台起,curl POST
      /__mock/config 激活 rich → POST search 断言条数 → PATCH 一条 → GET /__mock/calls
      断言记录 → kill)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && node --check test/e2e/mock-backend/server.mjs && grep -c "assetFixture" test/e2e/mock-backend/server.mjs && (MOCK_PORT=9891 node test/e2e/mock-backend/server.mjs & sleep 1; curl -s -X POST localhost:9891/__mock/config -H 'Content-Type: application/json' -d '{"assetFixture":"rich"}' >/dev/null; N=$(curl -s -X POST localhost:9891/api/v1/assets-registry/search -H 'Content-Type: application/json' -d '{}' | grep -o '"id"' | wc -l); curl -s -X PATCH localhost:9891/api/v1/assets-registry/91001 -H 'Content-Type: application/json' -d '{"isPrimaryView":false,"state":"eliminated"}' >/dev/null; C=$(curl -s localhost:9891/__mock/calls | grep -c "PATCH"); kill %1; echo "search-rows=$N patch-calls=$C"; test "$N" -ge 12 && test "$C" -ge 1)</automated>
  </verify>
  <acceptance_criteria>
  - node --check 通过;rich fixture ≥ 12 条(三组×2-3 + 单件×3 各形态);
  - PATCH 落 /__mock/calls 且 fixture 态被改(第二次 search 可见,冒烟只验调用记录);
  - 默认(未激活 preset)search 响应与 61 版本逐字节一致(fixture 常量不动)。
  </acceptance_criteria>
  <done>Phase 62 资产面 mock 数据底座 + 写通道记录面在位,62-04/05 可起真实交互断言。</done>
</task>

<task type="auto">
  <name>Task 2: select-winner mock + generation-config GET/PUT mock</name>
  <files>packages/infinite-canvas/test/e2e/mock-backend/server.mjs</files>
  <read_first>
  - Task 1 产物 + server.mjs POST /api/canvas/v2/nodes/ 路由(logCall/守卫/200 形状模板)
  - 62-RESEARCH.md C 节(select-winner 契约与失败形状;客户端常为幂等 no-op 的双通道事实)
  - 62-UI-SPEC.md C8(rows/writeState 锁形)与 E2E Hooks 表三文件断言面
  </read_first>
  <action>
  (1) select-winner mock:POST /api/canvas/v2/variant-groups/:groupId/select-winner
      (插 static 前;注释「62-03:镜像 select-winner.ts 200/404/409/400 形状;
      不模拟服务端 PATCH-linkage 联动——e2e 断言纪律按 RESEARCH C:勿断 applied:true」):
      a. logCall('POST', 完整路径, { projectId, episodesId, groupId, winnerNodeId, frameSlot }, null)
         全尝试记录(断言面:恰一次调用 + body winnerNodeId 为画布节点 id 前缀 asset-/a-oasset-);
      b. state.config.failSelectWinner === true → 500 { code: 500, message: '选定操作失败' };
      c. 内存 variantGroups 注册表:state.fixtureVariantGroups = [{ groupId: 'vg-e2e-1',
         variantNodeIds: ['asset-91001', 'asset-91002'], winnerNodeId: null, selectMode: 'single' }]
         (reset 归位;rich preset 激活时同步挂上;__mock/config 可覆盖注入自定义组);
      d. 校验:winnerNodeId 非 string → 400;组不在 → 404「变体组不存在」;
         winnerNodeId ∉ variantNodeIds → 409;winnerNodeId === 现 winnerNodeId →
         200 { applied: false };否则置 winnerNodeId → 200 { applied: true }。
  (2) generation-config mock(契约 = UI-SPEC C8 锁形,rows 查表生成不复制服务端合并):
      a. state.generationConfig = { overrides: {}, fileShape: 'not-found' }
         ('not-found' | 'requirement-v25' | 'legacy');fileShape 由 __mock/config 注入,
         reset 归位 not-found;
      b. GET /api/canvas/v2/generation-config:忽略 query;对 14 键逐一生成行:
         overrides[phaseKey] 命中 → source 'override' + 该值;
         否则 fileShape='requirement-v25' → source 'requirement' + 文件值(内置常量表:
         全键 { pre: 2, final: 1 } 作「实测值」样本);fileShape='legacy' → 快照默认 +
         sourceLegacy:true;not-found → source 'snapshot' + 快照默认(嵌套键 1;扁平键 pre=3、p02/p03 final=1、p01 final 缺省=pre);
         tier/label/preCap1/unwired/gpuHint 按 14 键常量(mock 内联一份精简表,键集与
         62-01/62-02 一致由 62-07 S-门对真表锁,mock 不背此责)。
         响应 { code: 200, data: { rows, fileState } }。
      c. PUT /api/canvas/v2/generation-config/overrides/:phaseKey:logCall 载荷保真
         ({ projectId, episodesId, phaseKey, nCandidates, finalCandidates });
         phaseKey ∉ 14 键 → 400;preCap1 键且 nCandidates>1 → 400
         「确定性派生 · pre 固定为 1」;否则 overrides[phaseKey] = 载荷值,
         writeState = state.config.genCfgWriteState ?? 'override'
         (可注入 'synced'|'file-fail' 两 fixture 态);响应
         { code: 200, data: { phaseKey, writeState } }。
  (3) 自检:node --check;冒烟覆盖四路由(GET 三 fileShape 档各一 curl、PUT 正常/400/写态
      注入、select-winner 200/404/409/500 各一 curl,/__mock/calls 计数)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && node --check test/e2e/mock-backend/server.mjs && (MOCK_PORT=9892 node test/e2e/mock-backend/server.mjs & sleep 1; R=$(curl -s "localhost:9892/api/canvas/v2/generation-config?projectId=1&episodesId=1" | grep -o '"phaseKey"' | wc -l); curl -s -X POST localhost:9892/__mock/config -H 'Content-Type: application/json' -d '{"genCfgWriteState":"synced"}' >/dev/null; W=$(curl -s -X PUT localhost:9892/api/canvas/v2/generation-config/overrides/p06_script.spatio_temporal -H 'Content-Type: application/json' -d '{"projectId":1,"episodesId":1,"nCandidates":3,"finalCandidates":2}' | grep -c 'synced'); B=$(curl -s -X PUT localhost:9892/api/canvas/v2/generation-config/overrides/p07_style.style_vector -H 'Content-Type: application/json' -d '{"projectId":1,"episodesId":1,"nCandidates":3,"finalCandidates":2}' | grep -c '400'); S=$(curl -s -X POST localhost:9892/api/canvas/v2/variant-groups/vg-e2e-1/select-winner -H 'Content-Type: application/json' -d '{"projectId":1,"episodesId":1,"winnerNodeId":"asset-91001"}' | grep -c '"applied"'); kill %1; echo "rows=$R put-synced=$W cap-400=$B sw-200=$S"; test "$R" -eq 14 && test "$W" -eq 1 && test "$B" -eq 1 && test "$S" -ge 1)</automated>
  </verify>
  <acceptance_criteria>
  - GET rows 恰 14 行;PUT writeState 注入生效;preCap1 越界 400;select-winner 200 applied 在场;
  - select-winner 500 注入开关(failSelectWinner)与 404/409 分支在码(node --check + 代码评审锚);
  - 全部新路由在 express.static 之前;默认 config 面不含 Phase 62 键(未注入时 fileShape=not-found 不影响既有用例)。
  </acceptance_criteria>
  <done>62-07 三文件全部 mock 断言面在位;与 62-02 真路由契约形状一致(锁形源头同为 UI-SPEC C8)。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试代码 → mock HTTP | mock 仅测试期 9876 端口本地起,无鉴权面(既有形态) |
| mock fixture → 断言 | fixture 是断言的预期源,形状漂移 = 假绿;S-门(62-07)锁 mock 键集与真表一致 |

## STRIDE Threat Register(ASVS L1 基线;high 置 mitigate 且 block_on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-09 | Tampering | mock 静默吞路由(静态兜底先匹配) | mitigate | 新路由全部插 express.static 之前 + 冒烟 curl 断言路由可达(verify 命令内置) |
| T-62-10 | Repudiation | mock 假成功形状漂移 | mitigate | select-winner/PUT 形状注释标镜像源文件行号;62-07 e2e 对 /__mock/calls 计数三点一线 |
| T-62-11 | Tampering | 默认 fixture 漂移破坏 61 回归 | mitigate | 默认路径字节等价 + 62-07 回归面全量跑 phase61-debt |
| T-62-SC | Tampering | npm/pip 安装 | accept | 零新依赖 |
</threat_model>

<verification>
- node --check 通过;两条冒烟链(search-rich+PATCH / config 四路由+select-winner)全过;
- grep:assetFixture/genCfgWriteState/failSelectWinner/select-winner 锚在场。
</verification>

<success_criteria>
- 五组 mock 面就绪,62-07 断言面(selector/calls/写态注入)全部可观测;
- 既有 61/55/52 用例所用 mock 行为零变化(默认路径字节等价)。
</success_criteria>

<output>
Create `.planning/phases/62-asset-hierarchy-selection/62-03-SUMMARY.md` when done
</output>
