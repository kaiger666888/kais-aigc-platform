---
phase: 62-asset-hierarchy-selection
plan: "04"
type: execute
wave: 2
depends_on: ["62-01"]
files_modified:
  - packages/infinite-canvas/src/components/assetManager/assetHierarchy.ts
  - packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx
  - packages/infinite-canvas/src/components/assetManager/AssetHierarchy.tsx
  - packages/infinite-canvas/src/components/assetManager/groupCanvasLinkage.ts
  - packages/infinite-canvas/src/components/assetManager/assetManager.css
  - packages/infinite-canvas/src/store/canvasStore.ts
  - packages/infinite-canvas/src/components/assetManager/AssetManager.tsx
  - packages/infinite-canvas/src/components/assetManager/__tests__/groupCanvasLinkage.test.ts
autonomous: true
requirements: [HIER-01, HIER-04]
must_haves:
  truths:
    - "资产管理中心出现第 5 个 Tab「资产层级」:三域折叠树 + getGroupKey 组折叠卡 + 单件桶 + 阶段/画布组徽标 + 三态计数芯片;默认视图仍 'library',既有 4 Tab 与资产库行为零扰动(HIER-04)"
    - "L3 卡片复用 renderCard(checker FLAG:两处增量=模式参数 + 单件徽标):库路径 tab 门控行为字节等价;层级路径按卡自身三态出按钮(选定/取消选定/恢复语义原样)"
    - "单组选定走共享 selectGroupWinner:既有 handleSelect 全语义(updateAsset 互斥淘汰 + 乐观更新 + 失败回滚)+ D-05 fire-and-forget selectVariantWinner(组映射画布且 winner 有节点时;失败仅 toast「画布侧同步失败」不回滚);资产库与层级同调用点"
    - "计数聚合逐字复用 62-01 判定式(isAssetSelected/Pending/Eliminated),无第二套;data-count-* 属性暴露供 e2e(D-04)"
    - "单件桶:每域 size===1 且非 reportAudit 资产入桶;reportAudit 资产不渲染卡片但计入域计数(D-03)"
  artifacts:
    - path: "packages/infinite-canvas/src/components/assetManager/assetHierarchy.ts"
      provides: "selectGroupWinner/deselectAsset/restoreAsset(D-05 全语义共享 handler;UI-SPEC「含 store 副作用的 handler 家」)"
      contains: "selectGroupWinner"
    - path: "packages/infinite-canvas/src/components/assetManager/AssetHierarchy.tsx"
      provides: "C1 视图壳 + C2 域树 + C3 组卡 + C7 计数芯片 + 单件桶 + C6 徽标 + toolbar(搜索/展开全部/折叠全部/刷新)"
      contains: "hierarchy-view"
    - path: "packages/infinite-canvas/src/components/assetManager/groupCanvasLinkage.ts"
      provides: "层级派生纯函数追加:partitionByDomain/buildHierarchyModel(组+单件桶+计数聚合)"
      contains: "buildHierarchyModel"
    - path: "packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx"
      provides: "renderCard 提取为模块级导出 renderAssetCard(d, deps, opts?)(模式参数);handleSelect/handleDeselect/handleRestore 改调共享 handler"
      contains: "renderAssetCard"
  key_links:
    - from: "AssetManager.tsx TABS"
      to: "AssetHierarchy"
      via: "assetView==='hierarchy' 渲染分支 + Tab 按钮「资产层级」"
      pattern: "资产层级"
    - from: "renderAssetCard"
      to: "assetHierarchy.ts handlers"
      via: "deps.onSelect 等注入;层级 deps 用共享 handler,库 deps 用同 handler(HIER-04 同调用点)"
      pattern: "selectGroupWinner\\("
    - from: "AssetHierarchy 组卡"
      to: "findVariantGroupForAsset"
      via: "主资产(primary ?? first)反查 → ⧉ 画布组徽标(data-vg-id)或「去画布选片 →」降级"
      pattern: "hier-group-vg"
    - from: "buildHierarchyModel"
      to: "isAssetSelected/isAssetPending/isAssetEliminated + domainOfType + PHASE_BY_SUBTYPE"
      via: "62-01 常量消费,零第二套判定"
      pattern: "isAssetPending"
---

<objective>
P3 层级视图 UI(HIER-01 主体 + HIER-04 锚):新建第 5 Tab「资产层级」三段布局(域树 220px /
组卡主区 / 预留右栏位),同时完成两项共享化前置——selectGroupWinner(D-05 单组全语义)提取
到 assetHierarchy.ts、renderCard 提取为模式参数化的 renderAssetCard(资产库路径字节等价,
层级路径按卡态出三态按钮)。

Purpose: 资产中心「选」面的显式三层组织;L3 卡功能完备依赖单组选定 handler,故 D-05 单组
半壁与本 plan 合并(62-05 专注批量决策/手动规则/auto-init 升级)。
Output: 2 新文件(assetHierarchy.ts / AssetHierarchy.tsx)+ AssetLibrary 共享化改造 +
store/Tab 挂载 + css + 派生单测。

裁定(plan 内锁定):
- checker FLAG 1 吸收:renderCard 复用=两处增量(模式参数 + 单件徽标),留居 AssetLibrary.tsx
  导出为模块级 renderAssetCard(d, deps, opts?)(P1-P9 无 AssetCard.tsx 位点,不新建);
  库内 renderCard = (d) => renderAssetCard(d, { mode:'library', tab, ...deps })。
- checker FLAG 2 吸收:toolbar 补「展开全部/折叠全部」按钮,testid `hier-expand-all` /
  `hier-collapse-all`,文案锁「展开全部」「折叠全部」。
- deps 注入形态:AssetCardDeps = { mode, tab?, assets, patchLocal, reload, showToast,
  onSelect, onDeselect, onRestore, openAssetDetail, charPortraitMap }——组件闭包显式化,
  两视图构造各自 deps。
- 单件桶 reportAudit 判定:PHASE_BY_SUBTYPE 命中且 reportAudit:true(62-01 裁定当前仅
  delivery_package);meta.phaseCode 直读优先、缺省查表推导,tooltip 区分
  「资产 meta 直读」/「按子类型推导」(D-01 防启发式漂移)。
- 图未加载/反查未命中:组卡沿用既有「去画布选片 →」降级文案与仅定位行为(UI-SPEC C6-2)。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/62-asset-hierarchy-selection/62-CONTEXT.md
@.planning/phases/62-asset-hierarchy-selection/62-UI-SPEC.md
@.planning/phases/62-asset-hierarchy-selection/62-RESEARCH.md
@.planning/phases/62-asset-hierarchy-selection/62-PATTERNS.md
@.planning/phases/62-asset-hierarchy-selection/62-01-SUMMARY.md

<interfaces>
<!-- 已提取契约——执行者直接消费,无需再探索 -->

62-01 产物(groupCanvasLinkage.ts / generationConfigKeys.ts,直接 import):
  getGroupKey/getGroupDisplayInfo/groupOrder/isSceneGroup/isVoiceGroup/
  isAssetSelected/isAssetPending/isAssetEliminated/parseMetaFields/metaStr/
  canvasNodeIdsForAsset/resolveAssetNodeId/findVariantGroupForAsset;
  domainOfType/PHASE_BY_SUBTYPE/GENERATION_CONFIG_KEYS。
AssetLibrary 关键区(改造对象,行号为 62-01 后现状):
  handleSelect(:849-872 附近,乐观更新+updateAsset 循环+toast);
  renderCard(:875-1010 附近;三态按钮门控 `tab === 'candidate'/'selected'/'eliminated'`,
  内联 deselect/restore handler——本 plan 提取具名化);
  useRealAssets 返回 { assets, loading, error, reload, patchLocal }。
useRealAssets: 模块级缓存(同 projectId 共享)——AssetHierarchy 用同一 hook 即 D-04 同源。
canvasStore: assetView 联合类型(:195-196)与默认 'library'(:1150)——只加成员不动默认。
AssetManager: TABS(:23)+ setAssetView 快照范式(:37-40)+ 渲染分支(:73-76)。
canvasApi 既有封装(零改动消费):
  updateAsset(assetId, { isPrimaryView, state })(:314,PATCH);
  selectVariantWinner(projectId, episodesId, groupId, winnerNodeId, cancelToken?, frameSlot?)
  (:482-494,非 2xx 抛 ApiError——D-05 fire-and-forget 的 catch 面)。
episodesId 来源:useCanvasStore.getState().episodesId(RESEARCH C 尾)。
RESEARCH C 双通道事实:PATCH isPrimaryView=true 服务端已自动 applyRegistrySelectionToCanvas
(常见路径客户端 POST = 幂等 no-op)——UI 不因 applied:false 报错;toast「画布侧同步失败」
仅挂客户端 POST 非 2xx。
UI-SPEC 契约:C1-C7(布局/结构/交互/testid 全表)、Copywriting 精确中文串、
E2E Hooks 表(hierarchy-view/hier-domain-node/hier-group[data-count-*][data-collapsed]/
hier-singletons/hier-card-phase/.am-tab 资产层级)。
CSS:零新全局 token;新增派生类仅 .am-hier 栅格 + .am-group--singleton modifier
+ hier-toolbar(全走既有 --cv-*/am-* 词汇,UI-SPEC Visual Language 表)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: selectGroupWinner/deselect/restore 共享提取 + renderAssetCard 模式参数化</name>
  <files>packages/infinite-canvas/src/components/assetManager/assetHierarchy.ts, packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx</files>
  <read_first>
  - packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx L849-1012(handleSelect 全文/renderCard 全文——提取体;行号以 62-01 改造后实际为准)
  - packages/infinite-canvas/src/services/canvasApi.ts L305-330(updateAsset)与 L475-500(selectVariantWinner 签名与 ApiError 语义)
  - packages/infinite-canvas/src/components/assetManager/useRealAssets.ts L40-100(assets/patchLocal/reload 契约)
  - 62-CONTEXT.md D-05 原文;62-RESEARCH.md C 节(双通道幂等事实)
  </read_first>
  <action>
  (1) 新建 assetHierarchy.ts(文件头:「62-04:UI-SPEC 共享 util 提取契约之 handler 家
      (含 store 副作用);纯函数家在 groupCanvasLinkage.ts」)。导出三 handler + ctx 类型:
      a. type SelectionCtx = { assets: AssetDetail[]; patchLocal(id, patch); reload();
         showToast(msg, kind); syncCanvas?: { projectId: number; episodesId: number;
         graph: FlowGraphV3 | null } };
      b. selectGroupWinner(assetId, groupKey, ctx):handleSelect 全语义逐字搬运(同组其余
         乐观淘汰 → winner 乐观选定 → updateAsset 循环(单项失败忽略)→ 成功 toast
         `已设为选定资产 · {title}（{n} 个变体已自动淘汰）`/失败 toast + reload 回滚);
         **D-05 增量**(成功 toast 后):syncCanvas 存在时 vg = findVariantGroupForAsset(
         graph, assetId),命中 → nodeId = resolveAssetNodeId(graph, assetId),非空则
         `void selectVariantWinner(projectId, episodesId, vg.groupId, nodeId)
         .catch(() => { ctx.showToast('画布侧同步失败', 'warning'); console.warn(...) })`
         ——fire-and-forget 不 await 不回滚不阻断;注释 pin RESEARCH C 双通道事实
         (常见路径幂等 no-op 属预期,勿据 applied:false 报错)。
      c. deselectAsset(d, ctx) 与 restoreAsset(d, ctx):renderCard 内联的取消选定
         (同组淘汰恢复)/恢复待选 handler 体提取具名化(乐观更新 + updateAsset +
         toast 文案逐字保留:`已退回待选 · {title}（{n} 个淘汰变体已恢复）`/`已恢复到待选`)。
  (2) AssetLibrary.tsx:handleSelect 改薄壳调 selectGroupWinner(ctx 含 syncCanvas:
      { projectId, episodesId: useCanvasStore.getState().episodesId, graph:
      useCanvasStore.getState().graph })(projectId 组件已有);deselect/restore 内联体
      换调共享函数;toast/乐观行为零变(HIER-04 库路径锚)。
  (3) renderCard 提取:模块级导出 renderAssetCard(d: AssetDetail, deps: AssetCardDeps,
      opts?: { singletonPhase?: { phaseCode: string; source: 'meta' | 'derived' } }) ——
      JSX 体逐字搬迁,仅两处增量:
      a. 三态按钮门控:mode==='library' → 沿用 deps.tab 门控(字节等价);
         mode==='hierarchy' → per-card isAssetPending(d)→★ 选定按钮、
         isAssetSelected(d)→✕ 取消选定、isAssetEliminated(d)→↻ 恢复;
      b. opts.singletonPhase → 卡右上角徽标
         `[data-testid="hier-card-phase"]` span(文案 = phaseCode,title =
         source==='meta' ? '资产 meta 直读' : '按子类型推导');
      拖入 draggable/定位/双击详情/👍 类名全原样(.am-card 断言面不动)。
      组件内 renderCard = (d) => renderAssetCard(d, { mode: 'library', tab, assets,
      patchLocal, reload, showToast, onSelect: handleSelect, onDeselect: deselect 薄壳,
      onRestore: restore 薄壳, openAssetDetail, charPortraitMap })。
  (4) 自检:npm run build;既有 vitest 全绿(npm test);grep handleSelect 本体逻辑
      (updateAsset 循环)仅 assetHierarchy.ts 一处。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm run build && npm test && grep -rn "已设为选定资产" src/components/assetManager/*.ts* | wc -l && grep -c "renderAssetCard" src/components/assetManager/AssetLibrary.tsx</automated>
  </verify>
  <acceptance_criteria>
  - build + 既有 vitest 全绿;选定 toast 文案定义仅 assetHierarchy.ts 1 处;
  - renderAssetCard 被导出且 AssetLibrary 内部消费;mode='library' 分支与原 tab 门控逐字节同构;
  - selectGroupWinner 含 fire-and-forget 分支(非 2xx → toast「画布侧同步失败」不回滚);
  - deselect/restore toast 文案与原内联体逐字一致。
  </acceptance_criteria>
  <done>D-05 单组选定共享化完成:库/层级同调用点,画布 best-effort 同步并入,L3 卡复用机制就绪。</done>
</task>

<task type="auto">
  <name>Task 2: 层级派生纯函数 + 单测</name>
  <files>packages/infinite-canvas/src/components/assetManager/groupCanvasLinkage.ts, packages/infinite-canvas/src/components/assetManager/__tests__/groupCanvasLinkage.test.ts</files>
  <read_first>
  - packages/infinite-canvas/src/components/assetManager/groupCanvasLinkage.ts(62-01 产物——append 位点)
  - 62-UI-SPEC.md「层间指派规则」表与「计数聚合公式」节(逐字契约)
  - packages/infinite-canvas/src/components/assetManager/__tests__/groupCanvasLinkage.test.ts(62-01 测试——追加位点)
  </read_first>
  <action>
  (1) groupCanvasLinkage.ts 追加(纯函数,零 React):
      a. assetPhaseOf(d): { phaseCode, source: 'meta'|'derived', reportAudit: boolean }
         ——parseMetaFields(d.meta) 直读 phaseCode(字符串且匹配 ^P\d{2}$ 量级校验)
         → source 'meta';缺省 inferSubtype → PHASE_BY_SUBTYPE 查表 → 'derived';
         未命中表 → { phaseCode: '', source: 'derived', reportAudit: false }(空 phaseCode
         时 UI 不渲染徽标);reportAudit 仅 derived 查表路径可 true;
      b. buildHierarchyModel(assets: AssetDetail[]): { domains: Array<{ domain:
         'setting'|'media'|'text', counts: { selected, pending, eliminated, total },
         groups: Array<{ key, title, emoji, counts, items, isManualScene, isManualVoice,
         hasPrimary }>, singletons: { counts, items } }> + all: 全量 counts——
         分组轴 = getGroupKey;域 = domainOfType(d.type);组计数/域计数/单件计数全部经
         isAssetSelected/isAssetPending/isAssetEliminated(判定式单套红线);
         单件桶 = 域内组 size===1 且 !assetPhaseOf(d).reportAudit 的成员
         (reportAudit 资产:不进单件桶渲染集,计入域 total——D-03);
         isManualScene/isManualVoice = isSceneGroup(items)/isVoiceGroup(items);
         组排序 = groupOrder(key) + title 自然序(既有 candidateGroups 排序式);
         单件桶恒排本域末尾(UI-SPEC Layout)。
  (2) 单测追加:fixture 手造 ≥ 10 条覆盖三域/三态/场景组/声纹组/keyframe 组/reportAudit/
      meta.phaseCode 直读——断言:
      a. counts 公式(含 isPrimaryView 整数 0/1;eliminated 且 isPrimaryView 理论不变量);
      b. reportAudit 资产不在 singletons.items 但在域 total;
      c. keyframe 组键剥 _v 后同组;scene/voice 组 isManual 标志;
      d. 直读 meta.phaseCode='P09' → source 'meta';缺省 → 查表 derived;
      e. 组排序:char < scene < keyframe < other,单件桶末位。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/components/assetManager/__tests__/groupCanvasLinkage.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
  - vitest 全绿(62-01 用例不回归 + 新增派生用例);
  - buildHierarchyModel 内零内联三态判定(全经 isAsset* 导出);
  - D-03 reportAudit 排除语义有程序化断言。
  </acceptance_criteria>
  <done>层级视图的全部数据派生成为可单测纯函数,Task 3 只做渲染与接线。</done>
</task>

<task type="auto">
  <name>Task 3: AssetHierarchy 视图壳(C1/C2/C3/C6/C7)+ 第 5 Tab 挂载 + css</name>
  <files>packages/infinite-canvas/src/components/assetManager/AssetHierarchy.tsx, packages/infinite-canvas/src/components/assetManager/AssetManager.tsx, packages/infinite-canvas/src/store/canvasStore.ts, packages/infinite-canvas/src/components/assetManager/assetManager.css</files>
  <read_first>
  - 62-UI-SPEC.md C1/C2/C3/C5/C6/C7 + Layout 图 + Copywriting 表(逐字文案源)
  - packages/infinite-canvas/src/components/assetManager/AssetManager.tsx L23-80(TABS/setAssetView/渲染分支)
  - packages/infinite-canvas/src/store/canvasStore.ts L195-196(assetView 类型)与 L1150(默认值——不动)
  - packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx 域树段(.am-tree 节点类用法)与 .am-group 组卡段(样式词汇参照)
  - packages/infinite-canvas/src/components/assetManager/assetManager.css(.am-group/.am-badge/.am-btn 段——新派生类就近 append)
  - Task 1/2 产物(renderAssetCard/assetHierarchy handlers/buildHierarchyModel)
  </read_first>
  <action>
  (1) canvasStore.ts:assetView 联合类型(:195-196)与 setAssetView 签名加 'hierarchy';
      默认值 'library' 与其余成员零改动(HIER-04 锚,注释 pin)。
  (2) AssetManager.tsx:TABS 追加 { key: 'hierarchy', label: '资产层级' };渲染分支
      `{assetView === 'hierarchy' && <AssetHierarchy />}`;既有 4 Tab 与分支不动。
  (3) AssetHierarchy.tsx 新建(UI-SPEC C1 契约;根 `[data-testid="hierarchy-view"]`):
      a. 数据:useRealAssets(projectId) + useCanvasStore(graph/episodesId/setAssetView
         旁路消费);loading→.am-loading + `[data-testid="hier-loading"]`;error→既有
         「资产加载失败：{error}」+ 重试 + `[data-testid="hier-error"]`;
      b. 派生:buildHierarchyModel(assets) memo;域过滤 state(Set<'setting'|'media'|'text'>,
         空集 = 全部);搜索 state 按 name/tags/prompt 过滤(命中组保留,组内未命中卡隐藏
         并计 data-count-filtered-out);
      c. 左栏域树(C2):.am-tree 原样词汇;「全部」节点 `[data-testid="hier-all-node"]`
         (★n ○n ✕n 全量计数,data-count-* 同值);三域节点
         `[data-testid="hier-domain-node"][data-domain=...]`(计数芯片 C7:★ 青/○ 中性/
         ✕ 玫 + 灰 mono「共 N」;域计数含 reportAudit);折叠箭头 stopPropagation;
         域空→is-empty 灰态可点(固定纲不 disabled);
      d. toolbar(搜索 .am-search placeholder「搜索资产 / 组…」+ 按钮
         `[data-testid="hier-expand-all"]`「展开全部」/`[data-testid="hier-collapse-all"]」
         「折叠全部」(checker FLAG 2 裁定)+ 刷新(reload));展开/折叠全部作用于
         collapsedGroups Set 全清/全置;
      e. 主区组卡(C3):每 getGroupKey 组一张 .am-group 折叠卡
         `[data-testid="hier-group"][data-group-key=...][data-collapsed=...]`
         `[data-count-selected|pending|eliminated|total=...]`;header 序 = checkbox 占位
         (62-05 落,本 plan 渲染 null 占位不留死按钮)→ emoji+title(getGroupDisplayInfo;
         组含选定者 title 前缀 ★)→ 阶段徽标 `[data-testid="hier-group-phase"]`(assetPhaseOf
         组首,tooltip 直读/推导)→ VG 徽标(findVariantGroupForAsset(graph, primary.id??
         items[0].id) 命中 → 可点 chip `[data-testid="hier-group-vg"][data-vg-id=...]`
         文案「⧉ 画布组 · {size}」title「在画布变体墙中并排对比选片 · {groupId}」,点击 =
         openWallByGroup + setViewMode('canvas');未命中 → 既有文案按钮「去画布选片 →」
         仅定位行为)→ 计数芯片(C7)→ 折叠箭头(.am-tree-toggle 复用,stopPropagation);
         折叠只收 L3 网格,header sticky 既有;
      f. L3 网格:renderAssetCard(d, hierarchyDeps)(deps.onSelect = (id, key) =>
         selectGroupWinner(id, key, ctx 含 syncCanvas);mode 'hierarchy');三态按钮由
         卡自身状态出(Task 1 增量);
      g. 单件桶:每域末尾 `[data-testid="hier-singletons"][data-domain=...]`,
         title「单件资产」emoji 📦 modifier .am-group--singleton(金竖条去除),
         tooltip「无互斥组的单产物 · 不参与组间流转」;桶内卡带 singletonPhase 徽标
         (hier-card-phase);空桶整卡不渲染;
      h. 空态:域无资产→「该域暂无资产 —— 运行管线产出后自动注册到这里。」;
         全局空→资产库既有串「资产库为空 —— 运行管线（P04 角色设计 / P07 场景）后会自动注册到这里。」;
      i. 根栅格 .am-hier{grid 220px 1fr}(右栏 340px 位 62-06 落,本 plan 不渲染第三列);
         详情 drawer 沿 AssetManager 既有 openAssetDetail 机制。
  (4) assetManager.css append:.am-hier 栅格 + .am-hier__toolbar + .am-group--singleton
      + 计数芯片/徽标字号对齐 UI-SPEC Typography 表(全既有 token,零新全局变量);
      响应式沿 .am-lib 同款断点。
  (5) 自检:npm run build;grep 默认视图锚(assetView: 'library' 不变);
      testid 清单核对 UI-SPEC E2E Hooks 表(hierarchy 侧全在场)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm run build && npm test && grep -c "hierarchy-view\|hier-domain-node\|hier-group\|hier-singletons\|hier-card-phase\|hier-expand-all\|hier-collapse-all\|hier-group-vg" src/components/assetManager/AssetHierarchy.tsx && grep -n "assetView: 'library'" src/store/canvasStore.ts</automated>
  </verify>
  <acceptance_criteria>
  - build + vitest 全绿;canvasStore 默认 'library' 锚在场;
  - UI-SPEC hierarchy 侧 testid 全部在场(verify 计数 ≥ 7 类);
  - 文案逐字对照 Copywriting 表(Tab/域标签/单件桶/计数/徽标/空态);
  - 既有 TABS 4 项与资产库渲染分支 diff 为零;renderAssetCard 库路径回归由 npm test + 62-07 e2e 兜底。
  </acceptance_criteria>
  <done>HIER-01 三层视图成真:域树导航/组卡折叠/计数聚合/单件桶/双徽标,第 5 Tab 可达且默认视图不动。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| search 响应 → buildHierarchyModel | assets 来自服务端;meta JSON 解析防御沿用共享 parseMetaFields |
| fire-and-forget selectVariantWinner | 异步副作用不阻塞主通道;失败仅 toast,不回滚资产态(D-05 镜像语义) |

## STRIDE Threat Register(ASVS L1 基线;high 置 mitigate 且 block_on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-12 | Tampering | meta.phaseCode 注入任意文案 | mitigate | 直读仅接受 ^P\\d{2}$ 形态(62-01 Task 2 裁定量级校验),异常值回落查表 |
| T-62-13 | Repudiation | 画布同步失败吞掉 | mitigate | catch 内 toast warning + console.warn 双留痕(D-05 指定) |
| T-62-14 | Tampering | XSS 经资产 name/title | accept | React 文本插值自动转义(51-REVIEW 全包结论沿用),无 dangerouslySetInnerHTML |
| T-62-15 | Denial of Service | 大资产量全量组渲染 | accept | useRealAssets 分页 limit 200 既有上界;折叠态默认不渲染内部网格(display:none 既有形态) |
| T-62-SC | Tampering | npm/pip 安装 | accept | 零新依赖 |
</threat_model>

<verification>
- npm run build + npm test(infinite-canvas)全绿;
- grep 锚:默认视图 'library' 不变;hierarchy testid 全套;toast 文案单点(assetHierarchy.ts);
- 文案抽查表:与 UI-SPEC Copywriting 逐串对照。
</verification>

<success_criteria>
- 第 5 Tab「资产层级」呈现三层组织,计数/折叠/单件桶/徽标全可用;
- 资产库路径行为字节等价(共享提取纯移动);单组选定双通道语义(D-05)并入共享点;
- 62-05(批量)/62-06(右栏)/62-07(e2e)的挂载点与断言面全部就绪。
</success_criteria>

<output>
Create `.planning/phases/62-asset-hierarchy-selection/62-04-SUMMARY.md` when done
</output>
