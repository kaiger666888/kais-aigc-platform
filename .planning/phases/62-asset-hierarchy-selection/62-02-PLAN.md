---
phase: 62-asset-hierarchy-selection
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/initDB.ts
  - src/lib/canvasRelationalStore.ts
  - src/lib/generationConfigService.ts
  - src/routes/canvas/v2/generation-config.ts
  - src/router.ts
  - src/lib/__tests__/generationConfig.test.ts
autonomous: true
requirements: [HIER-03]
must_haves:
  truths:
    - "新表 generation_config_overrides 以复合主键 (project_id, episodes_id, phase_key) boot 幂等建表,10588 实例重启即得,零独立迁移脚本"
    - "GET /api/canvas/v2/generation-config?projectId&episodesId 返回 rows[] 且完成三源合并(覆盖层 > requirement.json 实测 > 快照默认),source ∈ override|requirement|snapshot|legacy(D-09)"
    - "PUT /api/canvas/v2/generation-config/overrides/:phaseKey 落覆盖层后 best-effort 写回 requirement.json,响应 writeState ∈ override|synced|file-fail——寻址失败如实三态,绝不假成功(D-08;RESEARCH B:pipe-* 本机恒 EACCES)"
    - "钳制服务端兜底:pre ≥ 1、final=clamp(1,final,pre)、确定性派生键 pre>1 → 400 + 原因文案(D-10;前端禁用是第一道非唯一道)"
    - "判错看 HTTP status,不看 body.code(RESEARCH A:error() 信封 body.code 恒 400 陷阱)"
  artifacts:
    - path: "src/lib/generationConfigService.ts"
      provides: "服务端键面契约拷贝(与前端 62-01 表键集相等)+ mergeThreeSources/clampRedundancy/resolveRequirementFile/applyRequirementWrite 纯函数(fs 可注入)"
      contains: "mergeThreeSources"
    - path: "src/routes/canvas/v2/generation-config.ts"
      provides: "GET 三源合并 + PUT 覆盖层 + requirement.json best-effort(tmp+rename 原子写 + mtime 乐观锁 + 路径守卫)"
      contains: "writeState"
    - path: "src/lib/canvasRelationalStore.ts"
      provides: "listGenerationConfigOverrides / upsertGenerationConfigOverride(scope, phaseKey, values) / deleteGenerationConfigOverride"
      contains: "generation_config_overrides"
    - path: "src/lib/__tests__/generationConfig.test.ts"
      provides: "三源合并优先级/legacy 降级/钳制/写回三态(fs 注入式)node:test"
      contains: "file-fail"
  key_links:
    - from: "src/router.ts"
      to: "src/routes/canvas/v2/generation-config.ts"
      via: "app.use('/api/canvas/v2/generation-config', route)"
      pattern: "generation-config"
    - from: "PUT handler"
      to: "canvasRelationalStore.upsertGenerationConfigOverride"
      via: "覆盖层先落(权威),再 best-effort 文件面"
      pattern: "upsertGenerationConfigOverride\\("
    - from: "resolveRequirementFile"
      to: "GENERATION_CONFIG_REQUIREMENT_FILE env → pipe-* projectId 反查"
      via: "两段寻址,各自如实报 found/legacy/not-found"
      pattern: "GENERATION_CONFIG_REQUIREMENT_FILE"
---

<objective>
HIER-03 服务端半壁(P4/P5):generation_config_overrides 表(PK 三列)+ canvasRelationalStore
CRUD + v2 generation-config 路由(GET 三源合并 / PUT 覆盖层 + requirement.json best-effort
写回三态)+ 服务端键面常量拷贝 + node:test。

Purpose: D-08 两段式写入的权威段与 D-09 三源合并的服务端实现;UI-SPEC UI-GREY-2 已锁
rows/writeState 响应形状,e2e 断言面直接消费。
Output: 表 + store CRUD + 路由 + 挂载 + 纯函数测试。

裁定(plan 内锁定):
- UI-GREY-2 按推荐落地:`GET /api/canvas/v2/generation-config?projectId=&episodesId=` +
  `PUT /api/canvas/v2/generation-config/overrides/:phaseKey`(body
  { projectId, episodesId, nCandidates, finalCandidates },null = 清除该旋钮覆盖,
  两值全 null = 删该行);响应 rows/writeState 字段形状按 UI-SPEC C8 逐字。
- requirement.json 寻址第一段:RESEARCH B 实测 khs runs 目录无 projectId、ep-name 寻址
  kap 侧不可确定性推导 → 落地为专用 env `GENERATION_CONFIG_REQUIREMENT_FILE`(绝对路径,
  部署指到当前 ep 工作区 requirement.json;本机「已同步」态实际只能经此达成);第二段
  `/mnt/agents/output/pipelines/pipe-*/requirement.json` 按 projectId 反查取最新 mtime
  (本部署恒 EACCES → 如实 file-fail)。不复用语义分裂的 KAIS_OUTPUT_DIR(RESEARCH B 建议)。
- requirement.json v2.5 读/写形状:`generation_config: { [phaseKey]: { n_candidates?,
  final_candidates? } }` 单 namespace(11 嵌套 + 3 扁平键名不冲突);写侧 merge 保留其他
  顶层键。khs v2.5 真实落盘形状若异 → 62-07 D-12 e2e 暴露(该裁定即漂移检测器的预期锚)。
- 乐观锁 = mtime 比对(CONTEXT discretion 二选一裁定):读时 stat → rename 前 re-stat,
  变化即放弃覆盖、报 file-fail(stale),不丢他方写入。
- 服务端键面拷贝位点:src/lib/generationConfigService.ts(选点沿 gate-state.ts→
  @/lib/gateStateService 先例——P4 近似物的 route 薄壳 + lib service 形态,非发明位;
  文件零 db/零 fs import,纯函数全注入式,可直接 node:test)与前端 generationConfigKeys.ts
  (62-01)分立——服务端 bundle 不 import 前端包(仓内
  build:server esbuild 分仓形态既定);键集相等由 62-07 verify-phase-62 S-门静态锁。
- GET 行级 data-source 语义:pre/final 各自来源可异(半覆盖);行级 source 取两值中较强源
  (override > requirement > legacy/snapshot)。UI 角标按值级渲染(62-06)。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/62-asset-hierarchy-selection/62-CONTEXT.md
@.planning/phases/62-asset-hierarchy-selection/62-RESEARCH.md
@.planning/phases/62-asset-hierarchy-selection/62-PATTERNS.md

<interfaces>
<!-- 已提取契约——执行者直接消费,无需再探索 -->

DDL 惯例(src/lib/initDB.ts:1256-1392):relationalCanvasTables 数组 append
  { name, builder };boot 时 hasTable 守卫 + createTable(幂等,:1386-1392)。
复合 PK 先例(canvas_graph_meta,initDB.ts:1350-1357):
  table.primary(["project_id", "episodes_id"])。
RESEARCH A 建议的本表 DDL(直接采用):
  generation_config_overrides: project_id/episodes_id integer notNullable,
  phase_key string(64) notNullable, n_candidates integer nullable(半覆盖),
  final_candidates integer nullable, updated_at bigInteger notNullable,
  primary 三列, index(["project_id","episodes_id"], "idx_gco_scope")。
Store UPSERT 先例(canvasRelationalStore.ts:96-146 upsertNode):
  db.raw INSERT ... ON CONFLICT(...) DO UPDATE SET ...(或 knex onConflict().merge())。
v2 路由惯例(src/routes/canvas/v2/gate-state.ts):每文件一 express Router;
  zod safeParse 门 → success()/error() 信封(src/lib/responseFormat.ts)→
  src/router.ts 挂载表注册(如 :206 app.use("/api/canvas/v2/gate-state", route170))。
  GET query 范式:z.coerce.number().int()(gate-state querySchema)。
信封陷阱(RESEARCH A):error() body code 恒 400,HTTP 状态码由 res.status() 给出——
  mock 与客户端判错看 HTTP status。
文件写守卫先例(src/routes/canvas/v2/review-gate.ts:97-110):env base + 路径 sanitize +
  resolve 后 startsWith 包含检查 + writeFileSync;本 plan 升级为 tmp+rename 原子写。
requirement.json 磁盘实况(RESEARCH B):pipe-*/require.json 有 project_id/projectId
  (字符串两键并存)但无 generation_config(v2.5 前旧形态 → source=legacy);
  khs runs/<ep>/requirement.json 无 projectId、可写但 ep-name 寻址;
  pipe-* 子目录 kai 用户 touch 实测 EACCES(容器 uid 错配,非 kap 可修)。
服务端运行形态:knex + better-sqlite3(src/utils/db.ts:19-26,单文件 db2.sqlite);
  运行实例 10588 以 kai 跑 data/serve/app.js——新表重启自动建,零迁移动作。
键面权威 = 62-01 <interfaces> 的 14 键 + LOCKED 表(runner.py:2341-2392 实码口径)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: initDB append 新表 + canvasRelationalStore CRUD</name>
  <files>src/lib/initDB.ts, src/lib/canvasRelationalStore.ts</files>
  <read_first>
  - src/lib/initDB.ts L1256-1392(relationalCanvasTables 数组尾 + boot 建表循环——append 位点)
  - src/lib/initDB.ts L1350-1357(canvas_graph_meta 复合 PK 先例)
  - src/lib/canvasRelationalStore.ts L91-148(upsertNode UPSERT 先例)与 L674-740(getMeta/touchMeta——scope 维表读写先例)
  - src/lib/canvasRelationalStore.ts L1-40(Scope 类型与文件头)
  </read_first>
  <action>
  (1) initDB.ts:relationalCanvasTables 数组**末尾 append** generation_config_overrides
      builder(RESEARCH A 建议DDL 逐字:三列复合 PK + 两可空值列 + updated_at +
      idx_gco_scope);builder 上方注释:「62-02 D-08①:kap 权威覆盖层,pre/final 冗余配置
      两段式第一段;boot 幂等建表,已部署实例重启即得(RESEARCH A 零迁移风险)」。
      不动既有表、不加列(对既有表演进走 fixDB,本 phase 用不到)。
  (2) canvasRelationalStore.ts 追加(同域惯例,Scope 参数):
      listGenerationConfigOverrides(scope) → 按主键序全行;
      upsertGenerationConfigOverride(scope, phaseKey, { nCandidates, finalCandidates })
      → UPSERT(onConflict 三列 merge,updated_at=Date.now();两值全 null 时改 DELETE 行);
      类型导出 GenerationConfigOverrideRow。
  (3) 自检:npx tsc --noEmit 通过。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && grep -c "generation_config_overrides" src/lib/initDB.ts && grep -c "upsertGenerationConfigOverride" src/lib/canvasRelationalStore.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
  - initDB 含新表 builder(三列 PK + idx_gco_scope),位于数组 append 位;
  - store 三函数导出,UPSERT 沿 96-146 先例形态;tsc 零错误;
  - 既有 relationalCanvasTables 条目 diff 为零。
  </acceptance_criteria>
  <done>D-08① 权威覆盖层表与 CRUD 在位,boot 幂等建表零迁移。</done>
</task>

<task type="auto">
  <name>Task 2: 服务端键面拷贝(generationConfigService)+ 三源合并/寻址/写回纯函数 + node:test</name>
  <files>src/lib/generationConfigService.ts, src/lib/__tests__/generationConfig.test.ts</files>
  <read_first>
  - .planning/phases/62-asset-hierarchy-selection/62-01-PLAN.md <interfaces>(键面权威表——逐键对齐)
  - src/routes/v1/assets-registry/index.ts L97-111(search 的 a.* select——pipe-* 读侧形状参照)
  - 62-RESEARCH.md B 节(寻址面/EACCES 实测/两套寻址规则)与 F 节(键面)
  - src/lib/__tests__/reviewBridge.test.ts(node:test + 注入范式)
  </read_first>
  <action>
  (1) src/lib/generationConfigService.ts(服务端拷贝;零 db/零 fs import——纯函数全注入式,route 薄壳消费;文件头注明「与前端
      packages/infinite-canvas/src/components/assetManager/generationConfigKeys.ts 键集
      相等——verify-phase-62 S-门锁;契约源 runner.py 实码」):
      a. GENERATION_CONFIG_KEYS(14 键同 62-01 表:tier/默认/preCap1/unwired/gpuHint/label)、
         LOCKED(tts + 18 汇总);
      b. clampRedundancy(pre, final)(同式);
      c. 纯函数(全部 fs/stat 参数注入,便于 node:test 零真实磁盘):
         - readRequirementConfig(fsLike, path) → { state: 'requirement'|'legacy'|'not-found',
           values: Partial<Record<phaseKey, { pre, final }>> }(JSON.parse try/catch;
           有 generation_config 且含任一已知键 → requirement;文件在但无 v2.5 键 → legacy;
           ENOENT/EACCES 读失败 → not-found——读失败与无文件同为 not-found,不抛);
         - resolveRequirementFile(fsLike, opts { envFile, pipeRoot, projectId }) →
           { path: string|null, state } 两段寻址:envFile 绝对路径直取(存在即用);
           否则 pipeRoot 下 pipe-*/requirement.json 按 JSON.project_id||projectId 等值过滤
           取 mtime 最新;命中零 → path=null;
         - applyRequirementWrite(fsLike, path, phaseKey, values, mtimeAtRead) →
           'synced' | 'file-fail':读现有 JSON(不存在/不可读 → 起始 {})→ merge
           generation_config[phaseKey](保留其他顶层键与同 namespace 其他键)→
           re-stat mtime ≠ mtimeAtRead → 'file-fail'(stale,不覆盖)→
           writeFileSync(tmp) + renameSync(tmp, target) 原子替换;任一步 throw
           (含 EACCES)→ catch 返回 'file-fail'(绝不向上抛、绝不假成功);
         - mergeThreeSources(keys, overrideRow, fileState, fileValues) →
           rows[]:每键 { phaseKey, tier, label, pre, final, source }——值级
           override(列非 null)> requirement > 快照默认;行级 source 取较强源;
           file state=legacy → 行 source 不高于 snapshot,角标数据带 legacy 标志
           (rows 元素带 sourceLegacy?: true 供 UI「无 v2.5 键」角标)。
  (2) src/lib/__tests__/generationConfig.test.ts(node --import tsx --test;fs 用内存 fake
      { readFileSync, writeFileSync, renameSync, statSync, existsSync, readdirSync } 注入,
      零真实磁盘):
      a. 三源优先级:覆盖层 > 文件值 > 快照;半覆盖(仅 final 列)→ pre 走文件值、final 走
         override,行 source=override;
      b. legacy:文件在但无 generation_config → 全行快照默认 + sourceLegacy;
      c. not-found:ENOENT/EACCES 均不抛 → 快照默认;
      d. applyRequirementWrite 三态:成功路径 synced(断言 tmp+rename 被调、其他顶层键
         保留);写抛 EACCES → file-fail;mtime 变化 → file-fail 且 writeFileSync 未被调;
      e. clampRedundancy 四象限(与 62-01 同口径,服务端兜底面);
      f. resolveRequirementFile:envFile 优先;pipe-* projectId 等值过滤 + mtime 最新;
         零命中 → null。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && node --import tsx --test src/lib/__tests__/generationConfig.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
  - node:test 全绿(三源/legacy/写回三态/乐观锁/钳制全有行为断言);
  - 全部纯函数 fs 注入,测试零真实磁盘副作用;
  - 键面 14 键与 62-01 表逐键一致(键集相等留给 62-07 S-门程序化锁)。
  </acceptance_criteria>
  <done>D-09 三源合并与 D-08② best-effort 写回的纯逻辑层完成并可独立验证。</done>
</task>

<task type="auto">
  <name>Task 3: generation-config 路由(GET/PUT)+ 挂载</name>
  <files>src/routes/canvas/v2/generation-config.ts, src/router.ts</files>
  <read_first>
  - src/routes/canvas/v2/gate-state.ts(全文——GET + z.coerce query + success/error 范式)
  - src/routes/canvas/v2/select-winner.ts L40-70(body zod + 状态码分级先例)
  - src/routes/canvas/v2/review-gate.ts L90-115(路径 sanitize + resolve 包含检查)
  - src/router.ts L195-210(挂载表——append 位点)
  - src/lib/responseFormat.ts(success/error 信封;error body code 恒 400 陷阱)
  - Task 1/2 产物(store CRUD + 纯函数)
  </read_first>
  <action>
  (1) 新建 src/routes/canvas/v2/generation-config.ts(文件头注释:62-02 HIER-03,
      UI-GREY-2 锁形;判错看 HTTP status):
      a. GET "/" :query zod { projectId: z.coerce.number().int(), episodesId:
         z.coerce.number().int() }(gate-state 同式)→ resolveRequirementFile(真实 fs,
         env GENERATION_CONFIG_REQUIREMENT_FILE、pipeRoot = process.env.OUTPUT_DIR 默认
         '/mnt/agents/output' 拼 '/pipelines')→ listGenerationConfigOverrides →
         mergeThreeSources → res.json(success({ rows, fileState }))。
         rows 元素含 { phaseKey, tier, label, pre, final, source, sourceLegacy?,
         editable, lockReason?, unwired?, gpuHint?, note? }(editable/锁定语义由键表派生:
         全部 14 键 editable:true——unwired 键亦允许写覆盖层但带 unwired 标注
         「运行时暂不消费」;锁键不入 rows,见 PUT 校验与 62-06 锁定区)。
      b. PUT "/overrides/:phaseKey":body zod { projectId: number, episodesId: number,
         nCandidates: number|null, finalCandidates: number|null }(coerce.int.optional+
         nullable;phaseKey 参数白名单校验:不在 14 键集 → 400 「未知 phase_key」;
         p10_voice.tts/报告审计类不在键集天然 400)。
         服务端钳制兜底(D-10 第二道):preCap1 键 nCandidates>1 → 400
         「确定性派生 · pre 固定为 1」;其余键 clampRedundancy 后落库(记录 clamp 前后
         不强求——khs resolver 语义即 clamp);
         执行序:upsertGenerationConfigOverride(权威,成功后)→ resolveRequirementFile →
         命中 path 时先 stat 得 mtime → applyRequirementWrite → writeState =
         'synced'(成功)| 'file-fail'(写失败/stale)| 'override'(path 未寻址到——
         两段寻址都未命中,只有覆盖层)。文件**读失败/EACCES 不影响 200**,writeState 如实。
         响应 success({ phaseKey, writeState })。
      c. 全路由 try/catch:数据库/未知异常 → res.status(500).json(error(...));
         参数 zod → 400(res.status(400).json(error(...))——HTTP status 为准)。
  (2) src/router.ts 挂载表 append:app.use("/api/canvas/v2/generation-config", route);
      import 命名沿 routeN 惯例取下一序号。
  (3) 自检:npx tsc --noEmit;node --import tsx --test src/lib/__tests__/generationConfig.test.ts
      仍绿(路由不含业务纯逻辑,逻辑全在 Task 2 已测层)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc --noEmit && grep -c "generation-config" src/router.ts && node --import tsx --test src/lib/__tests__/generationConfig.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - GET/PUT 路由在位且挂载;tsc 零错误;
  - PUT 校验链:白名单 → preCap1 400 → clamp → UPSERT → best-effort 写回 → writeState 三态;
  - 任何文件面失败不改 HTTP 200 语义(writeState 承载三态);
  - 路由文件无业务纯函数重复(全部调 src/lib/generationConfigService.ts)。
  </acceptance_criteria>
  <done>HIER-03 服务端读写通道成文:表 + store + 路由 + 挂载,e2e mock(62-03)与 UI(62-06)可对接。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP → PUT /overrides/:phaseKey | projectId/episodesId/phaseKey/数值全部越权输入面,zod + 白名单 + clamp 三道 |
| kap 进程 → 文件系统 requirement.json | 跨容器 uid 面;写入仅限两段寻址命中的固定文件名,守卫 + 原子写 |

## STRIDE Threat Register(ASVS L1 基线;high 置 mitigate 且 block_on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-04 | Tampering | PUT 载荷注入任意 phaseKey/巨值 | mitigate | phaseKey 白名单(14 键)+ z.int + clamp 上界(pre/final 各 ≤ 99 显式上限防巨值写库) |
| T-62-05 | Tampering | requirement.json 路径穿越 | mitigate | envFile 仅接受绝对路径且 resolve 后必须以 .json 结尾固定文件名;pipe 段只扫 pipe-*/requirement.json 固定相对名,不拼接用户输入 |
| T-62-06 | Denial of Service | 文件面写卡死 PUT | mitigate | applyRequirementWrite 全 try/catch 同步小文件,失败即返 file-fail,不重试不阻塞(单次同步 IO) |
| T-62-07 | Repudiation | 写回失败假成功 | mitigate | writeState 三态如实(本机 EACCES 恒 file-fail 即设计验证面);62-07 e2e 双 fixture 断言 |
| T-62-08 | Elevation | 跨 scope 覆盖他人配置 | accept | 无鉴权体系(本地面布工具,ASVS L1 内不引入 auth;scope 由 query 显式给,无提权面) |
| T-62-SC | Tampering | npm/pip 安装 | accept | 零新依赖(knex/better-sqlite3/zod/express 既有),无 install 动作 |
</threat_model>

<verification>
- npx tsc --noEmit 零错误(根 lint 同命令);
- node --import tsx --test src/lib/__tests__/generationConfig.test.ts 全绿;
- grep:新表/CRUD/挂载/GENERATION_CONFIG_REQUIREMENT_FILE 四锚在场。
</verification>

<success_criteria>
- 覆盖层表 boot 幂等建表;GET 三源合并 rows 形状合 UI-SPEC C8;PUT 两段式 + 三态 writeState;
- 寻址失败绝不假成功;判错看 HTTP status;
- 服务端键面拷贝与 62-01 前端表口径一致(键集相等由 62-07 S-门锁)。
</success_criteria>

<output>
Create `.planning/phases/62-asset-hierarchy-selection/62-02-SUMMARY.md` when done
</output>
