---
phase: 62-asset-hierarchy-selection
plan: "07"
type: execute
wave: 5
depends_on: ["62-02", "62-03", "62-04", "62-05", "62-06"]
files_modified:
  - packages/infinite-canvas/test/e2e/tests/phase62-hierarchy.mjs
  - packages/infinite-canvas/test/e2e/tests/phase62-selection.mjs
  - packages/infinite-canvas/test/e2e/tests/phase62-redundancy-config.mjs
  - scripts/verify-phase-62.ts
  - package.json
autonomous: true
requirements: [HIER-02, HIER-03, HIER-04, HIER-05]
must_haves:
  truths:
    - "phase62-hierarchy:第 5 Tab 可达;三域节点计数 = 各域聚合;组卡按 getGroupKey 派生;data-count-pending ≡ 画布图 curation 派生计数(D-04 跨源契约,双 mock 对齐);单件桶只含 size=1 非 reportAudit 成员;折叠/展开全部往返;阶段徽标 ^P\\d{2} + 直读/推导 tooltip 可辨;切回资产库 DOM 原样(负向)"
    - "phase62-selection:层级内单选定 PATCH 序列与资产库一致(互斥淘汰/恢复);组映射画布时恰一次 select-winner 调用(body winnerNodeId = asset- 前缀节点 id;勿断 applied:true);mock 500 → toast「画布侧同步失败」且 PATCH 不回滚;批量选定每组一 PATCH(winner=最新非淘汰)+ 手动组跳过 toast;批量淘汰 arm-confirm 二击 + winner 不动(负向);手动 chip 仅场景/声纹组(负向);VG chip 点击切画布开墙"
    - "phase62-redundancy-config:开合默认收起;14 行完整呈现(D-12 键面契约——漂移即暴露);三源 fixture → data-source override/requirement/legacy 三态;preCap1 五键钉 1 禁用;写侧往返 PUT 载荷保真 + writeState 徽标 synced/file-fail 双 fixture;final>pre 前端禁用 + mock 400 toast;锁定区 19 键两行 reason 在(D-11)"
    - "回归面 phase52 三件套 + phase55-nav + phase61-debt(17 用例)全量绿;HIER-04 负向:场景/声纹组不自动选定、每组仍恰一 winner(checker FLAG 3 收窄锚)"
    - "verify-phase-62 聚合门:S 静态锁(双仓键面常量表键集相等/判定式单套/默认视图 'library'/DAG 公式未改)+ B 行为门(三文件 + 回归命令)+ F forced-failure(变异键集副本后 S-门必须红)"
  artifacts:
    - path: "packages/infinite-canvas/test/e2e/tests/phase62-hierarchy.mjs"
      provides: "层级导航/折叠/计数聚合/单件桶/D-04 一致性/库视图零扰动 用例"
      contains: "hier-singletons"
    - path: "packages/infinite-canvas/test/e2e/tests/phase62-selection.mjs"
      provides: "单组选定/批量决策/画布同步/手动规则 用例"
      contains: "select-winner"
    - path: "packages/infinite-canvas/test/e2e/tests/phase62-redundancy-config.mjs"
      provides: "三源读/编辑写/钳制/锁定区 用例"
      contains: "config-write-badge"
    - path: "scripts/verify-phase-62.ts"
      provides: "S/B/F 三段聚合门(npm run verify:phase-62)"
      contains: "verify-phase-62"
  key_links:
    - from: "e2e 起点"
      to: "getByRole('button', { name: '资产层级' })"
      via: "loadAssetCenter 序列后追加一步(UI-SPEC D-13)"
      pattern: "资产层级"
    - from: "D-04 断言"
      to: "/__mock/calls + window.__kaisCanvas.getGraph()"
      via: "o_assets 派生 DOM 计数 ≡ 图节点 curation 派生计数(测试侧按 model.ts:937 公式计算)"
      pattern: "data-count-pending"
    - from: "verify-phase-62 S-门"
      to: "双仓 generationConfigKeys"
      via: "tsx import 两表断言键集/tier 相等(62-02 裁定的双拷贝漂移锁)"
      pattern: "GENERATION_CONFIG_KEYS"
---

<objective>
P6/P9 收口:HIER-05 三条新链路 e2e(D-13 三文件)+ 既有资产管理 e2e 全量回归零破坏 +
verify-phase-62 聚合门(S 静态锁 + B 行为门 + F forced-failure,沿 verify-phase-61 三连范式)。

Purpose: 全 phase 验收门——D-04 跨源一致性、D-05 双通道纪律、D-12 键面漂移暴露、
HIER-04 负向断言全部在此成为可重复证据。
Output: 3 e2e 文件 + 1 门脚本 + package.json 脚本行。

裁定(plan 内锁定):
- D-04 一致性断言形态:层级 DOM `data-count-pending` ≡ 测试侧从
  window.__kaisCanvas.getGraph() 读节点按 model.ts:937 公式(candidates =
  total - selected - eliminated;curation 桶语义 curationBucket)计算的计数——两侧派生
  代码(AssetHierarchy 判定式 / model.ts)都不改,对齐靠 fixture 双 mock(assets-registry
  rich preset + save-v2 注入同 curation 的 a-* 节点)。不读 DAG 卡片 DOM(脆断言面),
  跨源契约由「UI DOM ↔ canonical 图计算」锁定。
- HIER-04 自动初始化锚(checker FLAG 3):断言收窄为「rich fixture 每组仍恰一 winner」
  (场景/声纹组零自动选定保持负向);不锚定「首个成员」(D-06 已升级 mtime 最新)。
- e2e 数值断言全走 data-count-*/data-write-state/data-armed 属性(UI-SPEC C7 纪律,
  非 innerText)。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/62-asset-hierarchy-selection/62-CONTEXT.md
@.planning/phases/62-asset-hierarchy-selection/62-UI-SPEC.md
@.planning/phases/62-asset-hierarchy-selection/62-RESEARCH.md
@.planning/phases/62-asset-hierarchy-selection/62-PATTERNS.md
@.planning/phases/61-audit-debt-clearance/61-01-PLAN.md

<interfaces>
<!-- 已提取契约——执行者直接消费,无需再探索 -->

e2e 基建(packages/infinite-canvas):
  helpers.mjs:test(自动 __mock/reset)/loadCanvas(切画布)/switchToCanvasView;
  loadCanvas 惯例 ?projectId=1&episodesId=1&testMode=1 + 等 window.__kaisCanvas;
  默认视图 = 资产管理中心——层级用例在默认视图起测,画布侧断言才 switchToCanvasView;
  本地 helper 先例:61-01 loadAssetCenter(goto+双 reset,不切画布)——层级用例复制之
  并追加一步:getByRole('button', { name: '资产层级' }).click()。
  地雷:e2e 跑 dist——每个 verify 命令先 npm run build。
62-03 mock 面(断言依赖):
  /__mock/config { assetFixture:'rich', genCfgWriteState, failSelectWinner, fileShape
  (generationConfig.fileShape) } 注入;/__mock/calls 读调用(PATCH/select-winner/PUT
  overrides 全尝试记录);fixtureVariantGroups(vg-e2e-1:asset-91001/91002)。
62-04/05/06 UI 断言面:UI-SPEC E2E Hooks 三表(testid/属性全表——实现逐行对表)。
curationBucket 语义(model.ts:193-204,candidates 公式 :937):读节点
  v3.curation ?? raw.curation ?? data.curation / curationState / data.isPrimaryView;
  DAG 只统计 nodeMatchesDag 命中(默认 a-* 前缀)——fixture 注入的 a-* 节点才入对照集。
52 三件套 + 55-nav + 61-debt = 回归面 17 用例(RESEARCH G 表)。
verify 范式:scripts/verify-phase-61.ts(S 静态锁 + B 行为门 + F forced-failure 三段;
  npm run verify:phase-61 挂法 package.json:54)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase62-hierarchy.mjs(层级导航/计数/单件桶/D-04 一致性)</name>
  <files>packages/infinite-canvas/test/e2e/tests/phase62-hierarchy.mjs</files>
  <read_first>
  - packages/infinite-canvas/test/e2e/helpers.mjs(全文)
  - packages/infinite-canvas/test/e2e/tests/phase61-debt.mjs(头部注释范式 + loadAssetCenter 先例)
  - packages/infinite-canvas/test/e2e/mock-backend/server.mjs 62-03 改造段(assetFixture 注入/fixture 形状)
  - 62-UI-SPEC.md E2E Hooks phase62-hierarchy 表(逐行断言契约)
  - packages/infinite-canvas/src/components/pipeline/model.ts L937-961(candidates 公式)与 L193-204(curationBucket)
  </read_first>
  <action>
  (1) 头部注释(62-07 用例地图 + D-04 裁定形态 + dist 地雷);本地 helper
      loadHierarchy(page):goto+双 reset → POST /__mock/config { assetFixture:'rich' } →
      重 goto(配置在 reset 后注入,防被清)→ 点「资产层级」→ 等
      [data-testid="hierarchy-view"] 可见。
  (2) 用例 1 tab-fifth-reachable:5 Tab 在场(资产库/角色管理/场景管理/创作文档/资产层级),
      资产层级 is-on 可切换;根 testid 在场。
  (3) 用例 2 domain-counts-aggregate:三 hier-domain-node data-count-selected/pending/
      eliminated 与 fixture 手算值逐一相等(rich fixture 注释内给出期望表);含 reportAudit
      成员的域 total 计入(文本域或其所在域 total 含 delivery_package)。
  (4) 用例 3 group-derivation:已知组键逐一可见——char:shenzhiyi:concept /
      scene:宴会厅 / char:shenzhiyi:voice / keyframe:S01:S01_first /
      character:shenzhiyi?? (voice 组键 = char:{id}:voice)/ media·text 单件桶;组卡
      data-count-* 与组内三态数一致。
  (5) 用例 4 d04-cross-source-consistency:POST save-v2 注入与 char 组对齐的 a-* 节点
      (3 节点:1 data.isPrimaryView=true/curation selected + 2 curation candidate,
      phaseName 匹配 DAG def 可命中组——按 nodeMatchesDag a-* 前缀口径)→ loadCanvas
      式校验 getGraph() 含注入节点 → 断言 hier-group[data-group-key="char:shenzhiyi:
      concept"] 的 data-count-pending === 测试侧从 getGraph() 按 :937 公式计算的
      candidates(跨源契约,两侧派生代码零改动)。
  (6) 用例 5 singletons-bucket:hier-singletons[data-domain] 在场;桶内 .am-card 仅
      size=1 非 reportAudit 资产(uuid 清单断言);reportAudit 资产卡不在任何桶
      (page.locator(`.am-card[data-uuid="<delivery uuid>"]`).count() === 0)但域 total 含之
      (与用例 2 期望表互证)。
  (7) 用例 6 collapse-toggle:hier-group 首组 data-collapsed true/false 往返;
      hier-collapse-all 点击后全部 data-collapsed="true";hier-expand-all 复原全部 false。
  (8) 用例 7 phase-badge:hier-card-phase 文本匹配 ^P\d{2}$;meta 直读样本(keyframe 组)
      tooltip「资产 meta 直读」,推导样本 tooltip「按子类型推导」(hover 断言)。
  (9) 用例 8 library-untouched(负向):切回「资产库」→ .am-tree 四级层级树原样 /
      .am-scope 三态 tab / .am-groups 组容器 / data-group-key 既有断言面在场
      (沿 55-nav/61-debt 所用选择器);批量条/manual chip 在资产库视图 count===0。
  (10) 自检:npm run build 后单文件跑绿;组词命名 -g 层级用例词根清晰。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm run build && npx playwright test test/e2e/tests/phase62-hierarchy.mjs</automated>
  </verify>
  <acceptance_criteria>
  - 8 用例全绿;数值断言全走 data-* 属性;
  - D-04 断言 = DOM 计数 vs getGraph 公式计算(零 DAG UI DOM 依赖);
  - reportAudit 排除/计入双面有断言;库视图负向断言面与 55-nav/61-debt 选择器同源。
  </acceptance_criteria>
  <done>HIER-01/D-01/D-03/D-04 的 e2e 证据链成文。</done>
</task>

<task type="auto">
  <name>Task 2: phase62-selection.mjs + phase62-redundancy-config.mjs</name>
  <files>packages/infinite-canvas/test/e2e/tests/phase62-selection.mjs, packages/infinite-canvas/test/e2e/tests/phase62-redundancy-config.mjs</files>
  <read_first>
  - Task 1 产物(loadHierarchy helper 复用——抽到两文件各自本地复制或提炼共享,沿 61 本地 helper 惯例)
  - 62-UI-SPEC.md E2E Hooks selection/redundancy-config 两表(逐行)
  - packages/infinite-canvas/test/e2e/mock-backend/server.mjs 62-03 段(failSelectWinner/genCfgWriteState/fileShape 注入面 + /__mock/calls 记录形状)
  - src/routes/canvas/v2/select-winner.ts L40-70(失败形状——mock 镜像源)
  </read_first>
  <action>
  (1) phase62-selection.mjs:
      a. single-select-in-hierarchy:hier 组内 .am-card__select-btn 点击 → /__mock/calls
         出 PATCH 序列(winner isPrimaryView true + 同组其余 eliminated)与资产库
         handleSelect 序列一致;UI 计数芯片翻转(★+1 ✕+n);
      b. select-winner-fire-and-forget:fixtureVariantGroups 命中组(char 组主资产
         asset-91001∈vg-e2e-1)→ 选定后 calls 恰 1 条 POST select-winner,body
         winnerNodeId 匹配 ^asset-91001$(**勿断 applied:true**——RESEARCH C 双通道);
         未映射组(media 单件)选定 → select-winner 零调用(负向);
      c. canvas-sync-fail-isolated:/__mock/config { failSelectWinner:true } → 选定 →
         toast「画布侧同步失败」出现 + PATCH 调用仍在(o_assets 已写不回滚);
      d. batch-select:勾选 char 组 + scene 组(hier-group-check)→ batch-bar
         「已选 2 组」→ hier-batch-select → char 组恰一组 PATCH(winner = createdAt
         最新待选),scene 组零 PATCH + toast 含「跳过 1 个手动选择组」;
      e. batch-eliminate-arm-confirm:勾选两组 → hier-batch-eliminate 首击
         data-armed="true" + 文案「确认淘汰 2 组待选？」→ 二击 → 两组待选成员全
         PATCH eliminated;已有 winner 组 winner 行零 PATCH(负向);5s 解除(可选子断言:
         首击后 wait 5.5s 复位 armed=false);
      f. manual-chip-placement:hier-manual-chip 仅场景/声纹组 2 枚;其他组 0(负向);
      g. vg-chip-nav:vg 命中组 hier-group-vg[data-vg-id="vg-e2e-1"] 在场 → 点击 →
         viewMode 切 canvas(window.__kaisCanvas 对照)+ 变体墙开(openWallByGroup 侧效
         ——store 态经 __kaisCanvas 断言或 wall DOM 在场)。
  (2) phase62-redundancy-config.mjs:
      a. toggle-default-collapsed:config-rail count===0;点 config-toggle → rail 在场;
         再点收起;
      b. rows-complete(D-12):fileShape='requirement-v25' → config-row 恰 14 行
         (11 嵌套+3 扁平;p09_shotlist.transition 无独立行);shot_list 行 note
         「转场随分镜表候选整体」可见;
      c. three-source-priority:三档注入逐一断言 data-source——genCfg overrides 命中行
         (先 PUT 一次)→ override;fileShape requirement-v25(无 override 行)→
         requirement;fileShape legacy → snapshot + 「无 v2.5 键」角标可见 + 值为快照默认 1;
      d. pre-cap-locked-inputs:5 确定性键 config-pre-input disabled + 钉 1 + reason
         「确定性派生 · pre 固定为 1」可见;
      e. write-roundtrip:改 p06 行 pre=3/final=2 → config-save → /__mock/calls PUT
         载荷 { nCandidates:3, finalCandidates:2 } 保真;genCfgWriteState='synced' →
         config-write-badge[data-write-state=synced] 文案「已同步 requirement.json」;
         'file-fail' → 「文件面寻址失败——覆盖层已保存」(不假成功双 fixture);
      f. clamp-frontend-and-backend:final 输入 > pre → 保存禁用 + 行内
         「数值越界：pre ≥ 1，final 需在 1..pre 之间」;手改绕过(mock 直接 PUT 由
         后端 400 面覆盖——真端点不在此 e2e 面,以 mock 400 响应注入:PUT p07 pre=3
         → 400 → toast 同文案);
      g. locked-section:config-locked-section 展开恰 2 行(p10_voice.tts + 汇总行)
         summary「不可配键 · 19」+ 两 reason 文案在;禁用行不可交互(opacity/not-allowed
         DOM 评审级断言:locked 行无 input 元素)。
  (3) 自检:npm run build 后两文件全绿。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm run build && npx playwright test test/e2e/tests/phase62-selection.mjs test/e2e/tests/phase62-redundancy-config.mjs</automated>
  </verify>
  <acceptance_criteria>
  - 两文件全绿;selection 7 用例 + redundancy 7 用例对表逐行落地;
  - select-winner 断言不含 applied:true;失败隔离用例 PATCH 不回滚有 calls 证据;
  - 14 行/19 键/三源三态/双写态全为属性断言。
  </acceptance_criteria>
  <done>HIER-02/03 的 e2e 证据链成文,D-05/D-08/D-10/D-11/D-12 断言面全通。</done>
</task>

<task type="auto">
  <name>Task 3: 全量回归 + verify-phase-62 聚合门(S/B/F)+ 挂脚本</name>
  <files>scripts/verify-phase-62.ts, package.json</files>
  <read_first>
  - scripts/verify-phase-61.ts(全文——S/B/F 三段结构 + forced-failure 自检形态 + 退出码纪律)
  - package.json L54 附近(verify:phase-N 挂法)
  - packages/infinite-canvas/src/components/assetManager/generationConfigKeys.ts + src/lib/generationConfigService.ts(双拷贝——S-门对象)
  - packages/infinite-canvas/src/store/canvasStore.ts L1150(默认视图锚)
  - packages/infinite-canvas/src/components/pipeline/model.ts L937(DAG 公式锚)
  </read_first>
  <action>
  (1) 全量回归先行:build 后依次 playwright 跑 phase52-regen/reroll/stale-panel +
      phase55-nav + phase61-debt(17 用例)——发现红先隔离重跑判 flake,复现红=真回归
      修 62-01..06 相应 plan 面,不带病进门。
  (2) scripts/verify-phase-62.ts(照 61 三段结构;tsx 直接 import 双仓常量表):
      S 静态锁:S1 前端 generationConfigKeys.ts 与服务端 generationConfigService.ts 键集+tier 逐键相等(62-02 裁定漂移锁);
      S2 判定式单套——groupCanvasLinkage.ts 定义 isAssetSelected/Pending/Eliminated 且
      AssetLibrary/AssetHierarchy/hierarchy 派生消费之(grep 消费点 ≥3 且无第二套
      内联式:`isPrimaryView &&` 于 assetManager 目录仅判定式文件命中);
      S3 默认视图 `assetView: 'library'`(canvasStore)未变;S4 model.ts candidates
      公式行 `total - selected - eliminated` 在场(DAG 派生零改动);
      S5 锁定区口径:LOCKED reportAudit.count===18 且 rail summary「不可配键 · 19」;
      B 行为门:依次 spawn(子进程判退出码)npm run build → playwright 三 phase62 文件
      → 回归面五文件;
      F forced-failure(锁能红):将前端常量表复制到临时文件、变异(删一键/改一 tier)
      后以同一比较函数断言「应检出差异」——即 S1 的比较器对变异样本必须返回不等
      (红),否则门自身假绿;执行后清理临时文件。61 F1-F3 自检形态照搬。
  (3) package.json scripts 挂 "verify:phase-62": "npx tsx scripts/verify-phase-62.ts"
      (紧邻 verify:phase-61 行)。
  (4) 收口:npm run verify:phase-62 全绿(exit 0)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npm run verify:phase-62</automated>
  </verify>
  <acceptance_criteria>
  - 回归面 17 用例 + 三新文件全绿;
  - 门三段齐:S1-S5 静态断言程序化;B 子进程链完整;F 变异样本使 S1 比较器确实检出差异;
  - npm run verify:phase-62 exit 0。
  </acceptance_criteria>
  <done>Phase 62 聚合门在位,HIER-01..05 验收证据可一条命令复现。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e fixture → 断言预期 | fixture 是预期源;rich fixture 形状由 62-03 契约锁定,S-门锁 mock 键集与真表一致 |
| verify 脚本 → 子进程命令链 | 退出码判绿;任何一步非零即门红(不带病放行) |

## STRIDE Threat Register(ASVS L1 基线;high 置 mitigate 且 block_on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-22 | Repudiation | 门自身假绿(S 比较器恒真) | mitigate | F forced-failure:变异样本必须使 S1 红,否则门失败退出 |
| T-62-23 | Tampering | 回归面选择性执行 | mitigate | B 门固定六文件命令链,子进程退出码逐一判 |
| T-62-24 | Repudiation | D-05 断言 applied:true 假失败/假绿 | mitigate | 断言纪律成文(只断调用发生+最终态);RESEARCH C 双通道注释 pin 在测试头部 |
| T-62-SC | Tampering | npm/pip 安装 | accept | 零新依赖 |
</threat_model>

<verification>
- npm run verify:phase-62 exit 0(S/B/F 三段);
- 三 phase62 文件 + 回归面 17 用例全部独立可复跑。
</verification>

<success_criteria>
- HIER-05 三链路 e2e + 全量回归零破坏成真;
- D-04 跨源契约 / D-05 双通道 / D-12 键面漂移 / HIER-04 负向四组关键锁全部可重复;
- 一条命令(verify:phase-62)复现 Phase 62 全部验收证据。
</success_criteria>

<output>
Create `.planning/phases/62-asset-hierarchy-selection/62-07-SUMMARY.md` when done
</output>
