---
phase: 62-asset-hierarchy-selection
plan: "06"
type: execute
wave: 4
depends_on: ["62-02", "62-04", "62-05"]
files_modified:
  - packages/infinite-canvas/src/components/assetManager/RedundancyConfigRail.tsx
  - packages/infinite-canvas/src/services/canvasApi.ts
  - packages/infinite-canvas/src/components/assetManager/AssetHierarchy.tsx
  - packages/infinite-canvas/src/components/assetManager/assetManager.css
autonomous: true
requirements: [HIER-03]
must_haves:
  truths:
    - "层级视图 toolbar「⚙ 冗余配置」开合第三栏(默认收起;开时栅格 220px 1fr 340px):14 行配置(11 嵌套+3 扁平,transition 键已并入 shot_list 注记)三源合并展示,来源可辨(角标 override/requirement/snapshot/legacy 四态)"
    - "写侧两段式可见:编辑 pre/final → 保存 → PUT overrides → 写结果三态徽标「已存覆盖层」/「已同步 requirement.json」/「文件面寻址失败——覆盖层已保存」——绝不假成功(D-08/D-09)"
    - "钳制前端道:pre ≥ 1、final ∈ 1..pre 越界即禁用保存 + 行内文案「数值越界：pre ≥ 1，final 需在 1..pre 之间」;mock 400 → toast 同文案(D-10 双道)"
    - "5 确定性派生键 pre 钉 1 禁用 + 行内 reason「确定性派生 · pre 固定为 1」;p11_video 行 ⚠「GPU 成本护栏 · 谨慎调高」;bgm/foley「占位未接线」标注(62-01 裁定)"
    - "不可配折叠区「不可配键 · 19」(62-01 裁定:30→19 漂移修正):p10_voice.tts 单行 + 18 报告/审计汇总行,禁用整行 + reason,不隐藏(D-11)"
  artifacts:
    - path: "packages/infinite-canvas/src/components/assetManager/RedundancyConfigRail.tsx"
      provides: "C8 右栏:行列表(档位徽标/来源角标/stepper/保存/写徽标)+ 锁定区 + 空态/错误态"
      contains: "config-rail"
    - path: "packages/infinite-canvas/src/services/canvasApi.ts"
      provides: "fetchGenerationConfig(GET rows)/putGenerationConfigOverride(PUT writeState)客户端封装"
      contains: "putGenerationConfigOverride"
    - path: "packages/infinite-canvas/src/components/assetManager/AssetHierarchy.tsx"
      provides: "toolbar ⚙ 开关(config-toggle)+ rail 挂载 + 三列栅格态"
      contains: "config-toggle"
  key_links:
    - from: "RedundancyConfigRail"
      to: "canvasApi.fetchGenerationConfig"
      via: "projectId/episodesId 拉取 rows,source/sourceLegacy 驱动角标"
      pattern: "fetchGenerationConfig\\("
    - from: "保存按钮(config-save)"
      to: "putGenerationConfigOverride → data-write-state 徽标"
      via: "PUT 载荷 { nCandidates, finalCandidates };writeState 三态映射 override|synced|file-fail"
      pattern: "config-write-badge"
    - from: "行 stepper 越界"
      to: "clampRedundancy(62-01 前端常量表)"
      via: "同一钳制函数驱动禁用/文案,不另写第二套边界判断"
      pattern: "数值越界：pre ≥ 1，final 需在 1..pre 之间"
---

<objective>
HIER-03 UI 半壁:C8 RedundancyConfigRail——khs v2.5 键面快照(62-01 修正口径 14 键)的读侧
三源合并展示 + 写侧两段式编辑,挂载层级视图第三栏。依赖 62-02 路由契约(锁形)、62-04 视图壳、
62-05 文件同域序。

Purpose: D-09 三源可辨 + D-10 钳制双道 + D-11 不可配显式标注 + D-08 三态徽标 UI 落地;
62-07 e2e redundancy-config 断言面全部由本 plan 供testid。
Output: 1 新组件 + canvasApi 两封装 + AssetHierarchy 挂载 + css。

裁定(plan 内锁定):
- UI-SPEC 文案修正(漂移修正 ③ 吸收):锁定区 summary「不可配键 · 30」→「不可配键 · 19」
  (tts 1 + 报告/审计 18 汇总,行数 = 2);C8 行数 15 → 14(transition 并入注记);
  e2e 断言同步(62-07)。其余 Copywriting 串逐字保留。
- bgm/foley 未接线态(RESEARCH F 建议,UI-SPEC 未覆盖):行内灰 chip
  `[data-testid="config-unwired-chip"]`「占位未接线」title「键面占位 · 运行时暂不消费」;
  editable 保持 true(写覆盖层允许,标注运行时暂不消费)。
- GET 客户端形态:canvasApi apiCall 语义为 POST body——GET 沿 updateAsset 原生 fetch
  范式(querystring 拼 API_BASE);PUT 同 fetch(method PUT);!res.ok → throw ApiError
  (HTTP status 为准,62-02 信封陷阱)。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/62-asset-hierarchy-selection/62-CONTEXT.md
@.planning/phases/62-asset-hierarchy-selection/62-UI-SPEC.md
@.planning/phases/62-asset-hierarchy-selection/62-PATTERNS.md
@.planning/phases/62-asset-hierarchy-selection/62-02-SUMMARY.md
@.planning/phases/62-asset-hierarchy-selection/62-04-SUMMARY.md
@.planning/phases/62-asset-hierarchy-selection/62-05-SUMMARY.md

<interfaces>
<!-- 已提取契约——执行者直接消费,无需再探索 -->

62-02 路由契约(UI-GREY-2 已裁锁形):
  GET /api/canvas/v2/generation-config?projectId=&episodesId= →
    { rows: [{ phaseKey, tier, label, pre, final, source, sourceLegacy?, editable,
               lockReason?, unwired?, gpuHint?, note? }], fileState };
    source ∈ override|requirement|snapshot|legacy(legacy 行 source=snapshot + sourceLegacy=true)。
  PUT /api/canvas/v2/generation-config/overrides/:phaseKey
    body { projectId, episodesId, nCandidates, finalCandidates } →
    { phaseKey, writeState: 'override'|'synced'|'file-fail' };preCap1 越界 → HTTP 400
    message「确定性派生 · pre 固定为 1」。
62-01 前端常量:clampRedundancy / GENERATION_CONFIG_KEYS(tier 标签映射:llm→LLM 产物/
  engine→引擎产物/deterministic→确定性派生/text→文本候选)/ LOCKED_CONFIG_KEYS(tts +
  { count: 18, reason: '报告/审计类 · 管线固定' })/ PHASE_KEY_LABEL 显示名。
62-04 AssetHierarchy:toolbar 结构与 .am-hier 栅格(220px 1fr → 本 plan 开第三列 340px)。
UI-SPEC C8 全文 + Copywriting 配置行(列头 阶段/档位/pre/final;角标与 tooltip 表;
写徽标三串;钳制错误串;GPU 护栏串)。
canvasApi:API_BASE/ApiError 既有;updateAsset 原生 fetch 范式(:305-323)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: canvasApi 两封装 + RedundancyConfigRail 组件</name>
  <files>packages/infinite-canvas/src/services/canvasApi.ts, packages/infinite-canvas/src/components/assetManager/RedundancyConfigRail.tsx</files>
  <read_first>
  - packages/infinite-canvas/src/services/canvasApi.ts L305-330(updateAsset 原生 fetch 范式)与 ApiError 定义段
  - 62-UI-SPEC.md C8 全文 + Copywriting 配置区 + E2E Hooks redundancy-config 表
  - packages/infinite-canvas/src/components/assetManager/generationConfigKeys.ts(62-01 产物)
  - packages/infinite-canvas/src/components/assetManager/assetManager.css(.am-badge/.am-diff-row 词汇段,如有)
  </read_first>
  <action>
  (1) canvasApi.ts 追加(原生 fetch,裁定形态):
      fetchGenerationConfig(projectId, episodesId): Promise<{ rows: ConfigRow[];
      fileState: string }>(GET + querystring;!ok → throw new ApiError(`HTTP ${status}`,
      'network', status));ConfigRow 类型导出(字段同 62-02 rows 元素);
      putGenerationConfigOverride(projectId, episodesId, phaseKey, values):
      Promise<{ phaseKey: string; writeState: 'override'|'synced'|'file-fail' }>
      (PUT 同范式;400 时 throw ApiError 带 message 供 toast)。
  (2) RedundancyConfigRail.tsx 新建(根 `[data-testid="config-rail"]`):
      a. props { projectId, episodesId };拉取态 loading(.am-loading)/error(重试按钮);
      b. 头部:标题「冗余配置」+ sub「PRE/FINAL CANDIDATES」(.am-title__sub 式);
         列头行「阶段」「档位」「pre」「final」;
      c. 每键一行 `[data-testid="config-row"][data-phase-key=...][data-source=...]
         [data-tier=llm|engine|deterministic|text]`:
         阶段列 = PHASE_KEY_LABEL 中文显示名 + 下方 mono phase_key 灰小字;
         档位徽标(四文案;确定性派生玫弱底警示其余中性);
         p11_video 行档位旁 ⚠ title「GPU 成本护栏 · 谨慎调高」;
         shot_list 行 note 副文案「转场随分镜表候选整体」;
         pre/final 数值 + 来源角标小 chip:sourceLegacy →「无 v2.5 键」(虚线边,title
         「requirement.json 为 v2.5 前旧形态 · 已回落快照默认」);override →「覆盖层」
         (青弱底,title「kap 覆盖层」);requirement →「文件值」(金弱底,title
         「requirement.json 实测值」);snapshot →「快照默认」(中性,title
         「khs v2.5 快照默认值」);pre/final 各自角标(值级来源:行 source 为行级,
         值级以 local state 编辑覆盖后显示「覆盖层」倾向——简化:未编辑沿用行 source,
         编辑后显示已改数值 + 待保存态);
         unwired 键行内 `[data-testid="config-unwired-chip"]`「占位未接线」;
         编辑控件:两个 number stepper(min=1;preCap1 键 pre input disabled 钉 1 +
         行内灰字「确定性派生 · pre 固定为 1」;final max = 当前 pre 输入值)+
         「保存」按钮(有改动时出现,行内灰字「未保存」);
         越界判定用 clampRedundancy 比对(不等即越界):保存禁用 + 行内
         「数值越界：pre ≥ 1，final 需在 1..pre 之间」;
         保存 → putGenerationConfigOverride → 写徽标
         `[data-testid="config-write-badge"][data-write-state=override|synced|file-fail]`
         文案三串逐字(已存覆盖层/已同步 requirement.json/文件面寻址失败——覆盖层已保存);
         ApiError(400)→ toast 同 message 文案(D-10 后端道兜底);
      d. 锁定区:折叠 `<details>`/state 折叠区 `[data-testid="config-locked-section"]`,
         summary「不可配键 · 19」(裁定修正 30→19);两禁用行
         `[data-testid="config-row-locked"][data-phase-key=...][data-reason=...]`:
         p10_voice.tts(reason「TTS 首选即定（防铺轨污染）· pre 钉死 1」)+
         报告/审计汇总行(phaseKey=`__report_audit_aggregate__`,reason「报告/审计类 · 管线固定」,
         副文案「18 键」);整行 opacity .45 + not-allowed(同 .am-tree-node.is-empty 词汇)。
  (3) 自检:npm run build;testid 对照 UI-SPEC redundancy 表全在场;文案抽查表逐串。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm run build && grep -c "config-rail\|config-row\|config-pre-input\|config-final-input\|config-save\|config-write-badge\|config-locked-section\|config-row-locked\|config-unwired-chip" src/components/assetManager/RedundancyConfigRail.tsx && grep -c "不可配键 · 19" src/components/assetManager/RedundancyConfigRail.tsx</automated>
  </verify>
  <acceptance_criteria>
  - build 通过;9 类 testid 在场;锁定区 summary 计数 19(非 30);
  - 三态写徽标文案逐字(D-08 原文);钳制文案逐字;四档位/四角标文案逐字;
  - preCap1 五键 disabled 逻辑由键表驱动(非逐行硬编码)。
  </acceptance_criteria>
  <done>HIER-03 读写 UI 主体完成,62-07 只剩挂载断言与 e2e。</done>
</task>

<task type="auto">
  <name>Task 2: 挂载层级视图第三栏 + 开合交互 + css</name>
  <files>packages/infinite-canvas/src/components/assetManager/AssetHierarchy.tsx, packages/infinite-canvas/src/components/assetManager/assetManager.css</files>
  <read_first>
  - packages/infinite-canvas/src/components/assetManager/AssetHierarchy.tsx(62-04/05 后现状:toolbar/栅格/状态)
  - 62-UI-SPEC.md Layout(340px 三列/transition/响应式 ≤880px rail 整宽下挂)
  - Task 1 产物(RedundancyConfigRail props)
  </read_first>
  <action>
  (1) 状态:configOpen: boolean(默认 false,UI-SPEC 默认收起)。
  (2) toolbar 追加开关按钮 `[data-testid="config-toggle"]`「⚙ 冗余配置」
      (.am-btn--ghost 形态;开态同文案 + is-on);
  (3) 挂载:configOpen 时主区后渲染 <RedundancyConfigRail projectId episodesId />
      (episodesId 取 useCanvasStore;projectId 组件已有);
  (4) 栅格:configOpen → .am-hier 三列 220px 1fr 340px(transition 防跳变);
      ≤880px rail 整宽下挂区(media query,沿既有断点词汇);
  (5) css append:.am-cfg-rail 列容器 + 行/divider + 角标 chip + 写徽标(--cv- 词汇表内,
      写徽标 600 字重;零新全局 token);
  (6) 自检:npm run build + npm test;grep config-toggle 在场;默认收起(configOpen 初始
      false 评审锚)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm run build && npm test && grep -c "config-toggle" src/components/assetManager/AssetHierarchy.tsx && grep -c "am-cfg" src/components/assetManager/assetManager.css</automated>
  </verify>
  <acceptance_criteria>
  - build + vitest 全绿;开关 testid 在场;默认 false;
  - 三列栅格与响应式规则在 css;rail 组件按需挂载(收起时零渲染)。
  </acceptance_criteria>
  <done>C8 右栏并入层级视图,HIER-03 UI 链路(入口→读→编辑→写→三态反馈)全通。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 服务端 rows → rail 渲染 | rows 字段服务端权威;前端越界再校验(clampRedundancy 双道) |
| PUT 响应 → 写徽标 | writeState 服务端判定;UI 只映射不推断(不假成功) |

## STRIDE Threat Register(ASVS L1 基线;high 置 mitigate 且 block_on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-19 | Tampering | stepper 绕过前端(手改 DOM) | mitigate | 后端 62-02 白名单+clamp 400 兜底;toast 同文案(D-10 双道) |
| T-62-20 | Repudiation | file-fail 误示成功 | mitigate | writeState 三态直映射徽标,UI 无任何「假定成功」分支;62-07 双 fixture 断言 |
| T-62-21 | Tampering | phaseKey/label XSS | accept | React 插值转义;phaseKey 服务端白名单产物 |
| T-62-SC | Tampering | npm/pip 安装 | accept | 零新依赖 |
</threat_model>

<verification>
- npm run build + npm test 全绿;
- grep:9 类 config-* testid + config-toggle + am-cfg css + 「不可配键 · 19」;
- 文案抽查:写徽标三串/钳制串/GPU 串/角标四串逐字对 UI-SPEC。
</verification>

<success_criteria>
- 配置右栏读侧三源可辨、写侧两段式三态如实;
- 14 行 + 锁定区 19 键口径(漂移修正)落地;
- 62-07 redundancy-config 断言面就绪。
</success_criteria>

<output>
Create `.planning/phases/62-asset-hierarchy-selection/62-06-SUMMARY.md` when done
</output>
