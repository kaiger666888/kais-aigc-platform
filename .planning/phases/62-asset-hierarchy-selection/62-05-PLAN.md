---
phase: 62-asset-hierarchy-selection
plan: "05"
type: execute
wave: 3
depends_on: ["62-01", "62-04"]
files_modified:
  - packages/infinite-canvas/src/components/assetManager/assetHierarchy.ts
  - packages/infinite-canvas/src/components/assetManager/AssetHierarchy.tsx
  - packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx
  - packages/infinite-canvas/src/services/canvasApi.ts
  - packages/infinite-canvas/src/components/assetManager/assetManager.css
  - packages/infinite-canvas/src/components/assetManager/__tests__/assetHierarchy.test.ts
autonomous: true
requirements: [HIER-02, HIER-04]
must_haves:
  truths:
    - "组层多选(checkbox)后出现 in-flow 批量条:「批量选定」= 每选中组各选一个 winner(组内最新非淘汰,规则=updatedAt??createdAt??id 排序键);「批量淘汰」= 选中组全部待选置 eliminated(有 winner 组不动 winner);逐组走 62-04 共享 selectGroupWinner,不发明组级事务(D-06)"
    - "场景组/声纹组批量选定跳过且 UI 标注(✋ 手动选择 chip 恒显 + toast 跳过数),批量淘汰不豁免(D-07)"
    - "批量淘汰两段式 arm-confirm:首击 data-armed=true 文案变「确认淘汰 {N} 组待选？」,5s 未二击自动解除;批量选定无确认(与单组零确认一致)(UI-SPEC C4)"
    - "自动初始化 winner 规则升级:activeGroup[0] 取首 → 最新非淘汰(同一排序键);HIER-04 回归锚收窄为「每组仍恰一 winner」(checker FLAG 3)"
    - "AssetDetail 增 createdAt 透传字段(UI-GREY-1 裁定:服务端 a.* 已含,零后端改动)"
  artifacts:
    - path: "packages/infinite-canvas/src/components/assetManager/assetHierarchy.ts"
      provides: "pickLatestActive(排序键 updatedAt??createdAt??id) + planBatchSelection/planBatchElimination 纯规则 + runBatchSelect/runBatchEliminate 编排(逐组共享通道)"
      contains: "pickLatestActive"
    - path: "packages/infinite-canvas/src/components/assetManager/AssetHierarchy.tsx"
      provides: "C4 BatchActionBar + 组 checkbox(hier-group-check) + 手动 chip(hier-manual-chip) + armed 状态机"
      contains: "hier-batch-bar"
    - path: "packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx"
      provides: "自动初始化 needsInit 升级为 pickLatestActive(单组路径同步升级,D-06)"
      contains: "pickLatestActive"
    - path: "packages/infinite-canvas/src/services/canvasApi.ts"
      provides: "AssetDetail.createdAt?: number | null"
      contains: "createdAt"
  key_links:
    - from: "runBatchSelect"
      to: "selectGroupWinner(62-04 共享)"
      via: "逐组循环;手动组跳过计数;成功 toast「批量选定完成 · {N} 组（跳过 {M} 个手动选择组）」"
      pattern: "批量选定完成"
    - from: "runBatchEliminate"
      to: "patchLocal + updateAsset(state:'eliminated')"
      via: "仅待选成员;winner 不动;toast「批量淘汰完成 · {N} 组共 {K} 个待选已淘汰」"
      pattern: "批量淘汰完成"
    - from: "AssetLibrary 自动初始化"
      to: "pickLatestActive(activeGroup)"
      via: "needsInit.push 由 activeGroup[0].id 升级;豁免判定沿 isSceneGroup/isVoiceGroup"
      pattern: "pickLatestActive\\(activeGroup\\)"
---

<objective>
HIER-02 层级化选定(D-06/D-07):C4 批量决策入口(组层多选 + 批量选定/批量淘汰两动作 +
arm-confirm 防误触)+ 场景/声纹手动规则 + winner「最新」规则落地(单组 auto-init 同步升级,
checker FLAG 3 吸收)+ UI-GREY-1 裁定落地(createdAt 透传)。

Purpose: 层级聚合的批量决策是 D-06 保守可测设计(组层多选,不做跨组任意资产组合);
62-04 已落单组通道,本 plan 只加批量编排与规则。
Output: assetHierarchy.ts 批量层 + BatchActionBar UI + auto-init 升级 + 单测。

裁定(plan 内锁定):
- 「最新」排序键 = `updatedAt ?? createdAt ?? id`(AssetDetail 现无 updatedAt 字段——
  排序键写成三元组,服务端未来透传 updated_at 列后自动生效;当前恒落 createdAt(整数
  ms)——UI-GREY-1 裁定的代码形态;mock fixture(62-03)已带 createdAt);
- 批量编排失败语义:逐组 try/catch 单项忽略(沿自动初始化「忽略单项失败」既有范式),
  汇总 toast 报成功组数;不做部分失败回滚;
- checkbox 不禁用手动组(批量淘汰可用,D-07 只绑批量选定)——UI-SPEC C4 原文。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/62-asset-hierarchy-selection/62-CONTEXT.md
@.planning/phases/62-asset-hierarchy-selection/62-UI-SPEC.md
@.planning/phases/62-asset-hierarchy-selection/62-RESEARCH.md
@.planning/phases/62-asset-hierarchy-selection/62-04-SUMMARY.md

<interfaces>
<!-- 已提取契约——执行者直接消费,无需再探索 -->

62-04 产物:
  selectGroupWinner(assetId, groupKey, ctx) —— 批量选定逐组唯一通道(含 D-05 同步);
  renderAssetCard/AssetCardDeps;AssetHierarchy 组件结构(组卡 header 序含 checkbox 占位)。
62-01 产物:isSceneGroup/isVoiceGroup/isAssetPending/isAssetSelected/isAssetEliminated/
  buildHierarchyModel(group.isManualScene/isManualVoice 已派生)。
AssetLibrary 自动初始化区(:536-573 量级,62-01 后行号):
  activeGroup = group.filter(state!=='eliminated');hasPrimary 判定;豁免
  isSceneGroup/isVoiceGroup;needsInit.push(activeGroup[0].id) ← 本 plan 升级点。
canvasApi.updateAsset(assetId, { state }) —— 批量淘汰通道(逐条 PATCH)。
UI-SPEC C4 契约:批量条结构/armed 文案/手动 chip 视觉(--cv-mod-audio 弱底)/全部 testid
(hier-batch-bar/hier-batch-select/hier-batch-eliminate[data-armed]/hier-batch-clear/
hier-group-check[data-group-key]/hier-manual-chip)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: createdAt 透传 + 批量纯规则 + 单测</name>
  <files>packages/infinite-canvas/src/services/canvasApi.ts, packages/infinite-canvas/src/components/assetManager/assetHierarchy.ts, packages/infinite-canvas/src/components/assetManager/__tests__/assetHierarchy.test.ts</files>
  <read_first>
  - packages/infinite-canvas/src/services/canvasApi.ts L203-222(AssetDetail 接口——append 位点)
  - packages/infinite-canvas/src/components/assetManager/assetHierarchy.ts(62-04 产物——append 位点)
  - 62-CONTEXT.md D-06/D-07 原文;62-UI-SPEC.md C4
  </read_first>
  <action>
  (1) canvasApi.ts:AssetDetail 增 `createdAt?: number | null`(JSDoc:「62-05 UI-GREY-1
      裁定:服务端 search select('a.*') 已透传 o_assets.createdAt;批量选定 winner 规则
      的『最新』排序键首位,未来 updated_at 列落地后前移」)。零其他改动。
  (2) assetHierarchy.ts 追加:
      a. assetFreshnessKey(d): number = (d as any).updatedAt ?? d.createdAt ?? d.id
         (类型上 AssetDetail 无 updatedAt——经局部扩展类型声明,注释说明三段键);
      b. pickLatestActive(items: AssetDetail[]): AssetDetail | null——filter 非淘汰后按
         key 降序取首(D-06 winner 规则);
      c. planBatchSelection(groups): { toSelect: Array<{ assetId, groupKey }>;
         skippedManual: number }——isSceneGroup/isVoiceGroup 组跳过(D-07),其余组
         pickLatestActive(已有 winner 组:仍重申选定同 winner?**否**——已有选定组跳过
         提交(幂等无意义),仅无选定组入 toSelect;注释 pin D-06「每个选中组各选一个
         winner」语义按需初始化理解,与 auto-init 升级行为同构);
      d. planBatchElimination(groups): Array<{ assetId }>-——每组 isAssetPending 成员
         (winner/eliminated 不动);手动组**不豁免**(D-07 只绑批量选定);
      e. runBatchSelect(groups, ctx) / runBatchElimination(groups, ctx):编排薄层——
         plan → 逐项调 selectGroupWinner / patchLocal+updateAsset(单项 try/catch 忽略,
         沿自动初始化范式)→ 汇总 toast(文案逐字:「批量选定完成 · {N} 组（跳过 {M} 个
         手动选择组）」/「批量淘汰完成 · {N} 组共 {K} 个待选已淘汰」;N=实际提交组数)。
  (3) __tests__/assetHierarchy.test.ts(vitest;selectGroupWinner 等 IO handler 不测,只测纯规则):
      a. assetFreshnessKey 三段:updatedAt 优先(构造扩展对象)> createdAt > id;
      b. pickLatestActive:淘汰者不中;createdAt 降序;全淘汰 → null;
      c. planBatchSelection:场景组/声纹组入 skippedManual;已有 winner 组不入 toSelect;
         无 winner 普通组恰一条且 = 最新非淘汰;
      d. planBatchElimination:仅待选成员;winner 排除;手动组成员照入(不豁免)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/components/assetManager/__tests__/assetHierarchy.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
  - vitest 全绿;四组纯规则断言程序化;
  - AssetDetail.createdAt 在场且为 optional(既有调用零破坏);
  - 批量编排不出现组级事务(逐组循环结构)。
  </acceptance_criteria>
  <done>D-06/D-07 批量决策的规则层与 UI-GREY-1 数据面就绪并可独立验证。</done>
</task>

<task type="auto">
  <name>Task 2: BatchActionBar UI + 组 checkbox + 手动 chip + 接线</name>
  <files>packages/infinite-canvas/src/components/assetManager/AssetHierarchy.tsx, packages/infinite-canvas/src/components/assetManager/assetManager.css</files>
  <read_first>
  - packages/infinite-canvas/src/components/assetManager/AssetHierarchy.tsx(62-04 产物——组卡 header 序/toolbar/主区结构)
  - 62-UI-SPEC.md C4 全文 + Copywriting 批量条行 + E2E Hooks selection 表
  - packages/infinite-canvas/src/components/assetManager/assetManager.css(.am-btn 段)
  </read_first>
  <action>
  (1) 状态:selectedGroupKeys: Set<string>;armedEliminate: boolean + 5s 自动解除
      timer(useRef 存 timer id,unmount 清理;二击执行后立即复位)。
  (2) 组卡 header 补 checkbox:`[data-testid="hier-group-check"][data-group-key=...]`
      (input + label,checked 读 Set;单件桶无 checkbox——62-04 占位处正式落位,占位
      null 注释同步删除);
  (3) 手动 chip:isManualScene||isManualVoice 组 header 恒显
      `[data-testid="hier-manual-chip"]`「✋ 手动选择」(.am-badge 基底 + --cv-mod-audio
      弱底,title「场景/声纹不参与批量选定 · 需逐组手动选择」);
  (4) BatchActionBar(选中组数 ≥1 渲染,in-flow 粘条 toolbar 下方):
      `已选 {N} 组`(mono)| `[data-testid="hier-batch-select"]`「批量选定」
      (.am-btn--primary,青 accent)| `[data-testid="hier-batch-eliminate"]
      [data-armed=true|false]`「批量淘汰」(.am-btn--ghost;armed 态文案
      「确认淘汰 {N} 组待选？」玫实底,5s 复原)| `[data-testid="hier-batch-clear"]`
      「清除」(.am-btn--ghost);
      批量选定点击 → runBatchSelect → toast;批量淘汰首击 arm、二击 runBatchElimination;
      执行后清选择集。
  (5) css append:.am-hier__batch 粘条 + armed 态玫底 + chip 弱底(全既有 token)。
  (6) 自检:npm run build;testid 对照 UI-SPEC E2E selection 表全在场;手动 chip 不出现在
      非手动组(渲染条件评审)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm run build && grep -c "hier-batch-bar\|hier-batch-select\|hier-batch-eliminate\|hier-batch-clear\|hier-group-check\|hier-manual-chip" src/components/assetManager/AssetHierarchy.tsx</automated>
  </verify>
  <acceptance_criteria>
  - build 通过;6 个 testid 全在场(≥ 6 计数);
  - armed 状态机:5s timer + 复位;文案两态逐字;
  - 手动 chip 仅 isManual 组渲染(条件渲染);
  - 批量选定/淘汰都逐组走共享通道(无组级事务代码)。
  </acceptance_criteria>
  <done>C4 批量决策入口完整:多选/两动作/arm-confirm/手动标注,断言面与 UI-SPEC 对齐。</done>
</task>

<task type="auto">
  <name>Task 3: 自动初始化 winner 规则升级(mtime 最新)+ 回归锚说明</name>
  <files>packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx</files>
  <read_first>
  - packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx 自动初始化段(62-01/62-04 后现状:initRef effect 全文)
  - 62-01 产物 isSceneGroup/isVoiceGroup;Task 1 产物 pickLatestActive
  </read_first>
  <action>
  (1) needsInit.push(activeGroup[0].id) 升级为 needsInit.push(pickLatestActive(activeGroup)!.id)
      (pickLatestActive 对非空 activeGroup 恒非 null——外层已判 activeGroup.length > 0;
      注释 pin:「62-05 D-06:自动初始化取首 → 最新非淘汰(升级为批量选定同规则;
      HIER-04 回归锚收窄为『每组仍恰一 winner』——checker FLAG 3,62-07 e2e 以此断言」)。
      其余自动初始化逻辑(豁免/防重入/reload)零改动。
  (2) 自检:npm run build + npm test;grep activeGroup[0] 零残留(升级完成证明)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm run build && npm test && grep -c "pickLatestActive(activeGroup)" src/components/assetManager/AssetLibrary.tsx && ! grep -q "activeGroup\[0\].id" src/components/assetManager/AssetLibrary.tsx && echo UPGRADED</automated>
  </verify>
  <acceptance_criteria>
  - build + vitest 全绿;升级点唯一且 pickLatestActive 消费在场;
  - 自动初始化豁免/防重入语义 diff 为零(评审锚)。
  </acceptance_criteria>
  <done>单组路径与批量路径 winner 规则同构(D-06「单组路径同步升级」),HIER-04 锚语义更新成文。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 批量动作 → 多条 PATCH | 组循环写;单项失败忽略(沿既有范式),无事务不回滚已成功组 |
| armed 二击确认 | destructive 防误触 UI 态,非安全边界 |

## STRIDE Threat Register(ASVS L1 基线;high 置 mitigate 且 block_on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-16 | Tampering | createdAt 畸形值(字符串/负数) | mitigate | freshness 键 Number() 收窄 + NaN 防御回退 id(纯函数单测覆盖) |
| T-62-17 | Repudiation | 批量部分失败静默 | mitigate | 逐组 catch 计数,汇总 toast 报实际 N;console.warn 单项 |
| T-62-18 | Tampering | 批量淘汰误触 | mitigate | arm-confirm 两段式 + 5s 解除(UI-SPEC C4 指定);淘汰可恢复非不可逆 |
| T-62-SC | Tampering | npm/pip 安装 | accept | 零新依赖 |
</threat_model>

<verification>
- npm run build + npm test(infinite-canvas)全绿;
- grep:pickLatestActive 双消费点(assetHierarchy/AssetLibrary);6 批量 testid;
- 自动初始化除升级点外 diff 为零。
</verification>

<success_criteria>
- HIER-02 层级化选定成真:组层多选批量两动作 + 手动组规则 + winner 最新规则全域一致;
- D-05/D-06/D-07 三个决策在共享通道上闭环,无第二套事务;
- UI-SPEC selection 断言面(hier-batch-*/hier-group-check/hier-manual-chip/data-armed)就绪供 62-07。
</success_criteria>

<output>
Create `.planning/phases/62-asset-hierarchy-selection/62-05-SUMMARY.md` when done
</output>
