---
phase: 62-asset-hierarchy-selection
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/infinite-canvas/src/components/assetManager/groupCanvasLinkage.ts
  - packages/infinite-canvas/src/components/assetManager/generationConfigKeys.ts
  - packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx
  - packages/infinite-canvas/src/components/assetManager/__tests__/groupCanvasLinkage.test.ts
  - packages/infinite-canvas/src/components/assetManager/__tests__/generationConfigKeys.test.ts
autonomous: true
requirements: [HIER-01, HIER-03, HIER-04]
must_haves:
  truths:
    - "getGroupKey/getGroupDisplayInfo/groupOrder/isSceneGroup/isVoiceGroup/三态判定式 从 AssetLibrary 搬迁为共享导出,资产库路径改 import 后行为逐字节一致(HIER-04 纯移动前提)"
    - "反查 util 兼容双节点 id 约定(asset-{id} 拖入 / a-oasset-{id} 服务端建点)——findVariantGroupForAsset 两前缀都查,不再漏 a-oasset 节点(RESEARCH D 实测)"
    - "键面常量以 RESEARCH F 修正版为准:11 嵌套键 + 3 扁平键 + tts 钉死 + 18 报告/审计不可配(汇总)+ bgm/foley 占位未接线态——不照抄已过时的 27-CONTEXT 12 键快照"
    - "TYPE_DOMAIN 三域指派表 + PHASE_BY_SUBTYPE 阶段映射静态表(reportAudit 标志)成为常量,e2e 契约可锁"
    - "AssetLibrary 既有 vitest/构建零回归;判定式全仓单套"
  artifacts:
    - path: "packages/infinite-canvas/src/components/assetManager/groupCanvasLinkage.ts"
      provides: "getGroupKey/getGroupDisplayInfo/groupOrder/isSceneGroup/isVoiceGroup/isAssetSelected/isAssetPending/isAssetEliminated/parseMetaFields/metaStr 搬迁导出 + canvasNodeIdsForAsset/resolveAssetNodeId/findVariantGroupForAsset(双前缀)"
      contains: "findVariantGroupForAsset"
    - path: "packages/infinite-canvas/src/components/assetManager/generationConfigKeys.ts"
      provides: "14 可配键面(11 嵌套+3 扁平,tier/默认值/preCap1/unwired/gpuHint)+ LOCKED 键(tts+18 报告/审计汇总)+ TYPE_DOMAIN + PHASE_BY_SUBTYPE + PHASE_KEY_LABEL 显示名表 + clampRedundancy"
      contains: "TYPE_DOMAIN"
    - path: "packages/infinite-canvas/src/components/assetManager/__tests__/groupCanvasLinkage.test.ts"
      provides: "分组轴/双前缀反查/三态判定式 单测"
      contains: "a-oasset-"
    - path: "packages/infinite-canvas/src/components/assetManager/__tests__/generationConfigKeys.test.ts"
      provides: "键面口径/域指派/阶段映射/钳制 单测(D-12 契约锁的前端半)"
      contains: "clamp"
  key_links:
    - from: "AssetLibrary.tsx"
      to: "groupCanvasLinkage.ts"
      via: "import { getGroupKey, getGroupDisplayInfo, ... } 纯函数搬迁,组件内定义删除"
      pattern: "from './groupCanvasLinkage'"
    - from: "AssetLibrary.tsx handleGoCanvasSelect"
      to: "findVariantGroupForAsset(graph, assetId)"
      via: "反查提取 + 双前缀覆盖(a-oasset- 补漏)"
      pattern: "findVariantGroupForAsset\\("
    - from: "generationConfigKeys.ts"
      to: "62-02 src/lib/generationConfigService.ts(服务端拷贝)"
      via: "键集相等由 verify-phase-62 S-门锁"
      pattern: "GENERATION_CONFIG_KEYS"
---

<objective>
Phase 62 地基(P1/P2/P8):把资产分组/反查/三态判定提取为无 React 依赖的共享纯函数,
把 khs v2.5 键面(runner 实码口径)固化为常量表,配纯函数单测。后续 62-04/05(UI)、
62-02(服务端拷贝对齐)、62-07(e2e 契约)全部消费本 plan 产物。

Purpose: D-02 唯一分组轴 + D-04 判定式单套 + D-01/D-10 常量化,是 HIER-04 零回归与
D-12 键面漂移暴露的前提。
Output: groupCanvasLinkage.ts + generationConfigKeys.ts 两个新纯函数/常量文件、
AssetLibrary 纯移动改造、两份 vitest。

裁定(plan 内锁定,不再上问):
- UI-GREY-1 落地修正:o_assets 实测**无 updated_at 列**,仅 `createdAt`(integer,
  initDB.ts:471);search 现查询 `select("a.*", ...)`(src/routes/v1/assets-registry/index.ts:109)
  **已透传 createdAt** → 零后端改动。AssetDetail 增 `createdAt?: number | null`(62-05 落);
  winner「最新」排序键 = `updatedAt ?? createdAt ?? id`(现恒落 createdAt;服务端未来加列自动生效),
  max(id) 代理保留在键末位。决策文字「server JOIN 透传 updated_at」按实际 schema 修正为
  「透传既有 createdAt 列」,不加列、零 DDL。
- renderCard 复用机制(留居 AssetLibrary.tsx 导出为模块级 renderAssetCard(d, deps),
  不新建 AssetCard.tsx——P1-P9 无此位点)在 62-04 落地,本 plan 只迁纯函数。
- 层级派生纯函数(域指派/单件桶/计数聚合)后续也放 groupCanvasLinkage.ts(P1 纯函数家,
  62-04 落);本 plan 先落 TYPE_DOMAIN/PHASE_BY_SUBTYPE 常量与其单测。
- PHASE_BY_SUBTYPE.reportAudit 裁定:「报告/审计类」= 管线固定/审计汇总性质产物,当前表标
  `delivery_package`;khs v2.5 交付逐键矩阵后再扩(CONTEXT deferred 已挂「单一真源自动同步」)。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/62-asset-hierarchy-selection/62-CONTEXT.md
@.planning/phases/62-asset-hierarchy-selection/62-RESEARCH.md
@.planning/phases/62-asset-hierarchy-selection/62-UI-SPEC.md
@.planning/phases/62-asset-hierarchy-selection/62-PATTERNS.md

<interfaces>
<!-- 已提取契约——执行者直接消费,无需再探索 -->

VariantGroupV3(packages/flowgraph-v3/ts/src/types.ts:236-245,零改动):
  { id: string; branchId: string; phaseIndex: number; sourceEventId: string;
    variantNodeIds: string[]; winnerNodeId?: string; selectMode: 'single'|'multi'|'locked' }
  组上无 variantGroupSize 字段——size 取 variantNodeIds.length。

双节点 id 约定(RESEARCH D,提取 util 必须两前缀都查):
  客户端拖入 = `asset-${payload.id}`(FlowCanvas.tsx:461);
  服务端 sync-assets 建点 = `a-oasset-${asset.id}`(sync-assets.ts:69,data 袋带 oAssetId)。

AssetDetail(canvasApi.ts:203-222):无 phase 字段(D-01 前提);meta 为 JSON 字符串。
三态判定式原文(AssetLibrary.tsx:499-507):
  selected: !!d.isPrimaryView && d.state !== 'eliminated'
  eliminated: d.state === 'eliminated'
  pending: !d.isPrimaryView && d.state !== 'eliminated'
场景/声纹豁免判定(AssetLibrary.tsx:556-559 自动初始化体内):
  isSceneGroup = items.some(d => d.type === 'scene')
  isVoiceGroup = items.some(d => d.type === 'voice' || d.type === 'audio')
键面修正口径(RESEARCH F,权威=runner.py:2341-2392 实码,27-CONTEXT 快照已过时):
  11 嵌套键:p01_hook.topic_kernel(llm)/p06_script.spatio_temporal(llm)/
    p09_shotlist.shot_list(llm,transition 已并入——注记「转场随分镜表候选整体」)/
    p11_video.video_render(engine,gpuHint)/p07_style.style_vector(deterministic,preCap1)/
    p07_style.color_intent(deterministic,preCap1)/p12_compose.master_timeline(deterministic,preCap1)/
    p12_compose.audio_mix(deterministic,preCap1)/p13_master.master_mp4(deterministic,preCap1)/
    p12_audio.bgm(engine,unwired 占位)/p12_audio.foley(engine,unwired 占位)
  3 扁平键(text 档):p01_hook / p02_outline / p03_script
  默认值以 runner.py:2285-2292 实码为准:扁平 pre 全 3(p02_outline/p03_script final=1;
  p01_hook 扁平 final 缺省=pre——default_final=None 哨兵语义);嵌套键默认全 {pre:1, final:1}
  除 p01_hook.topic_kernel 嵌套仅 final=1、pre 共享扁平(=3)。
  不可配:p10_voice.tts(pre 钉死 1)+ 18 报告/审计类(汇总,**无逐键枚举**——khs 未交付枚举,
  手工枚举 18 键名会引入新漂移面,RESEARCH F 明示建议)。
  钳制:khs resolver 逐字(_vision_review.py:87-91)pre ≥ 1;final = clamp(1, final, pre)。
TYPE_DOMAIN 三域(UI-SPEC L1 指派表):
  setting = character/scene/scene_variant/scene_image/prop/prop_key/prop_consumable/costume/accessory/keyframe
  media = video/clip/audio/voice/storyboard;text = script_phase/outline/topic/storyboard_board/delivery/style/requirement/story/script
  未列类型兜底 = media。keyframe 归 setting(G13 首尾分选候选与设定同域语义)。
PHASE_BY_SUBTYPE 键域 = AssetSubtype 全词表(assetManagerData.ts:341-395);映射裁定:
  P01=pipeline_requirement;P02=story_framework;P03=episode_script;P04=character_concept/
  turnaround_sheet/turnaround_view/costume_temporal_variant/costume_turnaround/character_bible;
  P06=spatio_temporal_script;P07=scene_base/scene_variant/scene_blueprint/scene_temporal_variant/
  scene_view_angle/scene_design;P09=shot_list/e_konte/keyframe_first/keyframe_last/midframe/
  scene_angle_shot;P10=voice_print/voice_profile/voice_clips;P10=rapid_preview(P10b);
  P11=video_clips;P12=bgm_design/bgm_track/foley_stem/audio_stems/master_timeline;
  P13=master_mp4/delivery_package(reportAudit:true)。subtype=unknown 不入表(兜底走 meta 直读)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: groupCanvasLinkage.ts 提取 + AssetLibrary 纯移动改 import</name>
  <files>packages/infinite-canvas/src/components/assetManager/groupCanvasLinkage.ts, packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx</files>
  <read_first>
  - packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx L243-346(parseMetaFields/metaStr/getGroupKey/getGroupDisplayInfo 原文——搬迁体)
  - packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx L378-395(groupOrder)
  - packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx L499-507(三态判定式)与 L536-573(自动初始化——豁免判定式 :556-559,本体不动)
  - packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx L820-872(handleLocateOnCanvas/handleGoCanvasSelect——改调 util 点;handleSelect 本体不动,62-04 提取)
  - packages/flowgraph-v3/ts/src/types.ts L236-245(VariantGroupV3)
  - 62-RESEARCH.md D 节(双前缀实测)、H 节(行号漂移登记)
  </read_first>
  <action>
  (1) 新建 groupCanvasLinkage.ts(P1 近似物 = assetManagerData.ts 纯函数族:模块级纯函数 +
      导出常量,零 React、零 store import;文件头注释注明「62-01:AssetLibrary 共享提取 +
      62-04 层级派生函数家;双前缀反查为 RESEARCH D 实测增量」)。导出:
      a. parseMetaFields/metaStr(连同搬迁,getGroupKey 依赖);
      b. getGroupKey(d: AssetDetail): string 与 getGroupDisplayInfo(d): { title, emoji }
         (逐字搬迁,注释保留原文全部层级语义说明);
      c. groupOrder(key: string): number(搬迁);
      d. isAssetSelected(d)/isAssetPending(d)/isAssetEliminated(d)(:501-506 三式逐字,
         JSDoc 注明「D-04 判定式单套,层级计数与资产库 tab 共用,禁止另造第二套」);
      e. isSceneGroup(items: AssetDetail[])/isVoiceGroup(items: AssetDetail[])(:556-559 式提取);
      f. ASSET_NODE_ID_PREFIX='asset-' / OASSET_NODE_ID_PREFIX='a-oasset-';
         canvasNodeIdsForAsset(assetId: number): string[](两候选 id);
         resolveAssetNodeId(graph: FlowGraphV3 | null, assetId: number): string | null
         (graph.nodes 中实存的第一个候选,无 graph/无命中 → null);
         findVariantGroupForAsset(graph, assetId): { groupId, size, winnerNodeId? } | null
         (variantNodeIds.includes(任一候选) || winnerNodeId === 任一候选;size =
         variantNodeIds.length——RESEARCH D:组上无 variantGroupSize 字段)。
  (2) AssetLibrary.tsx:删除上述函数本地定义,改 `import { ... } from './groupCanvasLinkage'`;
      tabFiltered(:499-507)改用 isAssetSelected/isAssetPending/isAssetEliminated(同式换名,
      行为零变);自动初始化(:536-573)豁免判定改调 isSceneGroup/isVoiceGroup(式不变)。
  (3) handleGoCanvasSelect(:836-847)改调共享 util:
      primary 取法不变;nodeId = resolveAssetNodeId(store.graph, primary.id) ?? `asset-${primary.id}`;
      vg = findVariantGroupForAsset(store.graph, primary.id);后续 setFocusAssetNodeId/
      openWallByGroup/setViewMode('canvas') 原样。注释 pin:行为增量仅「补 a-oasset- 节点覆盖」
      (现只查 asset- 前缀会漏服务端建点,RESEARCH D),既有 asset- 路径行为逐字节一致。
  (4) handleLocateOnCanvas(:820-826)nodeId 同改 resolveAssetNodeId ?? asset- 前缀
      (未命中实存节点时沿用现行为——画布侧 toast 提示)。
  (5) 自检:npm run build(infinite-canvas)通过;grep 确认 getGroupKey/getGroupDisplayInfo/
      groupOrder 函数定义全仓 infinite-canvas src 仅 groupCanvasLinkage.ts 一处。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm run build && grep -rn "function getGroupKey\|function getGroupDisplayInfo\|function groupOrder" src/ | wc -l && grep -c "from './groupCanvasLinkage'" src/components/assetManager/AssetLibrary.tsx</automated>
  </verify>
  <acceptance_criteria>
  - build exit 0;getGroupKey/getGroupDisplayInfo/groupOrder 定义恰 1 处(groupCanvasLinkage.ts);
  - AssetLibrary import 该模块;tabFiltered/自动初始化判定改用共享导出但表达式语义零变;
  - handleGoCanvasSelect/handleLocateOnCanvas 经 resolveAssetNodeId+findVariantGroupForAsset;
  - groupCanvasLinkage.ts 无 React/useCanvasStore import(纯函数家红线)。
  </acceptance_criteria>
  <done>分组轴/反查/判定式成为共享导出,资产库路径纯移动完成,D-04 单套判定式落地。</done>
</task>

<task type="auto">
  <name>Task 2: generationConfigKeys.ts 键面常量(runner 实码口径)+ 域表 + 阶段映射</name>
  <files>packages/infinite-canvas/src/components/assetManager/generationConfigKeys.ts</files>
  <read_first>
  - packages/infinite-canvas/src/components/assetManager/assetManagerData.ts L265-290(REAL_TYPE_GROUPS——TYPE_DOMAIN 形态近似物)与 L341-395(AssetSubtype 全词表)
  - 62-RESEARCH.md F 节(三处漂移修正全文——本表权威来源)
  - 62-UI-SPEC.md Copywriting「phase_key 显示名表」与 C8 行结构
  - 62-CONTEXT.md D-10/D-11(钳制与不可配决策原文)
  </read_first>
  <action>
  (1) 新建 generationConfigKeys.ts(P2 近似物 = REAL_TYPE_GROUPS 导出常量形态;文件头注释
      注明「契约源 = khs runner.py:2341-2392 实码(27-01 cdd12dd 已 shipped);27-CONTEXT
      快照已过时禁照抄;服务端拷贝 src/lib/generationConfigService.ts(62-02)键集必须与本表
      相等——verify-phase-62 S-门锁」)。导出:
      a. type ConfigTier = 'llm' | 'engine' | 'deterministic' | 'text';
         GENERATION_CONFIG_KEYS: 数组/Record,14 键,每键 { phaseKey, tier, label(显示名,
         逐字用 UI-SPEC 显示名表), defaultPre, defaultFinal, preCap1?: true(五确定性键),
         unwired?: true(bgm/foley), gpuHint?: true(p11_video), note?: '转场随分镜表候选整体'
         (shot_list) };
      b. LOCKED_CONFIG_KEYS: { tts: { phaseKey: 'p10_voice.tts', reason: 'TTS 首选即定（防铺轨污染）· pre 钉死 1' },
         reportAudit: { count: 18, reason: '报告/审计类 · 管线固定' } }——汇总形态,
         不枚举 18 键名(RESEARCH F 明示建议,防新漂移面);锁定区总数 = 1 + 18 = 19
         (UI-SPEC「30」按漂移修正 ③ 改 19,62-06/07 同口径);
      c. TYPE_DOMAIN: Record<string, 'setting'|'media'|'text'> + domainOfType(type: string)
         兜底 media(UI-SPEC L1 指派表逐键);
      d. PHASE_BY_SUBTYPE: Partial<Record<AssetSubtype, { phaseCode: string; reportAudit?: boolean }>>
         按 <interfaces> 裁定映射表逐键落(unknown 不入);
      e. clampRedundancy(pre: number, final: number): { pre, final }(pre ≥ 1;
         final = clamp(1, final, pre)——khs resolver 逐字,JSDoc 注明来源)。
  (2) 中文注释与仓内惯例一致(JSDoc + 决策号引用 D-xx)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npm run build && node -e "const m = await import('./src/components/assetManager/generationConfigKeys.ts').catch(()=>null)" 2>/dev/null; npx vitest run src/components/assetManager/__tests__/generationConfigKeys.test.ts 2>/dev/null || echo "test lands in Task 3"; ! grep -q "p09_shotlist.transition" src/components/assetManager/generationConfigKeys.ts</automated>
  </verify>
  <acceptance_criteria>
  - 文件存在且 build 通过;`p09_shotlist.transition` 独立键 0 命中(已并入 shot_list,仅作 note 文案可含「转场」字样);
  - 嵌套键恰 11、扁平键恰 3、preCap1 键恰 5、unwired 键恰 2、reportAudit.count === 18;
  - TYPE_DOMAIN 覆盖 UI-SPEC L1 全部类型 + keyframe∈setting;clampRedundancy 语义逐字。
  </acceptance_criteria>
  <done>键面/域表/阶段映射三组常量固化,漂移修正 ③(11+3+18)在 kap 侧成文。</done>
</task>

<task type="auto">
  <name>Task 3: 纯函数单测(分组轴/双前缀/判定式/键面/钳制)</name>
  <files>packages/infinite-canvas/src/components/assetManager/__tests__/groupCanvasLinkage.test.ts, packages/infinite-canvas/src/components/assetManager/__tests__/generationConfigKeys.test.ts</files>
  <read_first>
  - packages/infinite-canvas/src/components/__tests__/placeNewAsset.test.ts(vitest 形态先例)
  - Task 1/2 产物(groupCanvasLinkage.ts / generationConfigKeys.ts)
  - packages/infinite-canvas/src/services/canvasApi.ts L203-222(AssetDetail 字段——fixture 构造)
  </read_first>
  <action>
  (1) groupCanvasLinkage.test.ts(vitest,零网络零 DOM,fixture 手造 AssetDetail):
      a. getGroupKey:characterId 分层全分支(concept/bible/voice/baseline_tr/costume_tr/
         costume_design 按 episode+scene)、keyframe `_v\d+$` 剥后缀、scene 三 type、
         兜底 `${type}:${name}`——每分支至少 1 断言(与 AssetLibrary 原注释语义逐条对应);
      b. findVariantGroupForAsset 双前缀:graph 含 variantGroup 其 variantNodeIds 有
         'a-oasset-42' 而无 'asset-42' → 命中(现实现会漏的回归锁);winnerNodeId 匹配分支;
         两前缀都无 → null;graph null → null;size = variantNodeIds.length;
      c. resolveAssetNodeId:仅 a-oasset 实存 → 返回之;都实存 → asset- 优先(数组序);
      d. isAssetSelected/Pending/Eliminated:isPrimaryView 整数 0/1(!! 转换)、eliminated
         组合矩阵全 8 格中的关键 5 格。
  (2) generationConfigKeys.test.ts:
      a. 键数断言:嵌套 11/扁平 3/preCap1 5/unwired 2;`p09_shotlist.transition` 不在键集
         (D-12 漂移锁的静态半);扁平键 defaultPre 全 3(p02/p03 final=1,p01 final 缺省=pre),
         嵌套键默认全 {1,1} 除 topic_kernel(final=1、pre=3 共享扁平);
      b. LOCKED:reportAudit.count===18,tts reason 含「钉死 1」;
      c. TYPE_DOMAIN:keyframe→setting、script→text、video→media、未列类型(如 'mystery')→media;
      d. PHASE_BY_SUBTYPE:抽查 delivery_package({phaseCode:'P13',reportAudit:true})、
         keyframe_first→P09、spatio_temporal_script→P06;
      e. clampRedundancy:(0,0)→(1,1);(5,9)→(5,5);(3,2)→(3,2);(1,1)→(1,1)
         (khs resolver 四象限)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/components/assetManager/__tests__/groupCanvasLinkage.test.ts src/components/assetManager/__tests__/generationConfigKeys.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
  - 两测试文件全绿;getGroupKey 分支覆盖含 keyframe 剥后缀与 costume_design episode+scene 细分;
  - a-oasset 反查回归锁在场(现实现缺陷的防回归);
  - 键数断言 11/3/5/2/18 全部程序化(非注释)。
  </acceptance_criteria>
  <done>P8 纯函数单测在位,62-02/04/05/07 的共享地基可编译、可测试、可契约锁。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meta JSON 字符串 → parseMetaFields | o_assets.meta 为外部(管线/Notion 导入)写入的 JSON,解析防御沿用既有 try/catch |
| graph → 反查 util | graph 来自服务端 load,VariantGroupV3 字段按类型收窄访问 |

## STRIDE Threat Register(ASVS L1 基线;high 置disposition=mitigate 且 block_on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Tampering | meta JSON 解析崩溃 | mitigate | parseMetaFields try/catch + 对象校验,既有范式逐字搬迁 |
| T-62-02 | Tampering | 双前缀反查注入错组 | mitigate | 前缀常量白名单拼接(asset-{id} 中 id 必须 number),不做字符串自由拼接 |
| T-62-03 | Information Disclosure | 键面常量含部署路径 | accept | 常量仅 phase_key/文案,无路径无凭据 |
| T-62-SC | Tampering | npm/pip 安装 | accept | 零新依赖(纯提取+常量+vitest 既有),无 install 动作 |
</threat_model>

<verification>
- npm run build(infinite-canvas)通过;
- 两份 vitest 全绿(分组轴全分支/双前缀/判定式矩阵/键数 11-3-5-2-18/钳制四象限);
- grep:getGroupKey 等三函数定义全仓唯一;AssetLibrary 消费共享导出。
</verification>

<success_criteria>
- 共享纯函数家 + 键面常量表落地,后续 plan 可直接 import;
- 资产库行为零变化(纯移动 + 同式换名);
- RESEARCH 三处漂移修正之 ③(键面口径)在常量与测试双重固化。
</success_criteria>

<output>
Create `.planning/phases/62-asset-hierarchy-selection/62-01-SUMMARY.md` when done
</output>
