---
phase: 53-variant-contract-picker-upgrade
plan: 04
type: execute
wave: 3
depends_on: ["53-01", "53-03"]
files_modified:
  - src/lib/manifestWriteback.ts
  - src/lib/writebackQueue.ts
  - src/lib/initDB.ts
  - src/routes/canvas/v2/select-winner.ts
  - scripts/verify-phase-53.ts
autonomous: true
requirements: [VAR-03]
must_haves:
  truths:
    - "select-winner 端点接受可选 frameSlot('first'|'last')+ source(5 源 enum)且向后兼容——不传两字段的行为与 Phase 49 完全一致(D-09 扩展不另开通道)"
    - "manifest 回写 hook 挂在 status==='updated' 段 reviewBridge 调用同位之后,void 不 await + .catch 二 backstop;幂等分支(status==='idempotent')刻意不触发 hook(Pitfall 5,既有语义保持)"
    - "hook 失败绝不影响 200 响应(D-10 体验不阻塞);queue 写入自身失败降级为日志(Pitfall 4)"
    - "回写失败进 canvas_writeback_queue 重试队列:state pending|done|failed、attempts、max_attempts=8、指数退避 base 30s、串行 drain(DR-4)"
    - "Wave A 无传输实现(FS 直写 vs HTTP 是 Wave B 决策)——hook 为 deps 注入 transport 接口;无 transport 配置时 warn-once + no-op,不污染队列(scope constraint 裁定)"
    - "G13 首尾分选回写字段名权威对齐:selected_first_variant / selected_last_variant(p11a0 已写 iframe-manifest.json 的既有名,D-11);G14 用 chosen_variant_id 语义(variantIndex 通用化,Wave B 定字段名)"
    - "D-04: COORD-01『工作树干净』判定范围 = 仅代码文件(pipeline/phases/*.py + plugins/kais_aigc/);episodes/ 运行时产物与 .pipeline-state.json 永脏不算 blocker——Wave B 开工前检查采用此口径"
  artifacts:
    - path: "src/lib/manifestWriteback.ts"
      provides: "ManifestTransport 接口 + attemptManifestWriteback(never-throws)+ enqueueManifestWriteback"
      contains: "ManifestTransport"
    - path: "src/lib/writebackQueue.ts"
      provides: "enqueueWriteback/drainOnce/ensureDrainStarted + canvas_writeback_queue 全部读写"
      contains: "drainOnce"
    - path: "src/lib/initDB.ts"
      provides: "canvas_writeback_queue 表 DDL(relationalCanvasTables family 内)"
      contains: "canvas_writeback_queue"
    - path: "src/routes/canvas/v2/select-winner.ts"
      provides: "schema 扩展(frameSlot/source)+ manifest hook 挂点"
      contains: "enqueueManifestWriteback"
  key_links:
    - from: "src/routes/canvas/v2/select-winner.ts"
      to: "src/lib/manifestWriteback.ts"
      via: "status==='updated' 段 reviewBridge 之后 void enqueueManifestWriteback({...frameSlot, source}).catch(() => {})"
      pattern: "enqueueManifestWriteback"
    - from: "src/lib/manifestWriteback.ts"
      to: "src/lib/writebackQueue.ts"
      via: "transport 投递失败 → enqueueWriteback(action='manifest_writeback')"
      pattern: "enqueueWriteback"
    - from: "src/lib/writebackQueue.ts"
      to: "src/lib/initDB.ts"
      via: "canvas_writeback_queue 表(列/索引按 DR-4)"
      pattern: "canvas_writeback_queue"
    - from: "scripts/verify-phase-53.ts"
      to: "src/routes/canvas/v2/select-winner.ts"
      via: "S3 端点 dispatch + 队列重放断言(替换 FILLED-BY-53-04)"
      pattern: "S3"
---

<objective>
建 VAR-03 的 kap 回写半部(D-09/D-10/D-11):扩展 select-winner 端点(frameSlot/source 可选参数 + manifest 回写 hook,reviewBridge 同位 best-effort 隔离);建 `manifestWriteback.ts`(transport deps 注入接口,Wave B 定 FS/HTTP)+ `writebackQueue.ts`(sqlite 重试队列表 + 指数退避 + 串行 drain)+ initDB `canvas_writeback_queue` DDL;verify S3 填充。

Wave B 门控注记(不在本 plan 实现):manifest 实际传输实现(FS 直写 episode workdir 内 vs HTTP,A2/DR-4 Open Question 1)、kmc 消费闭环 E2E(ROADMAP 成功标准 3 的 khs 半部)= Wave B(gated on khs2 v2.4 Phase 25 验收 — planned separately)。Wave A 队列持有 typed payload,通道抽象为 deps 注入则 Wave B 零返工。

输出:三个 lib/route 文件 + DDL + S3 绿。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/53-variant-contract-picker-upgrade/53-CONTEXT.md
@.planning/phases/53-variant-contract-picker-upgrade/53-RESEARCH.md
@.planning/phases/53-variant-contract-picker-upgrade/53-PATTERNS.md
@.planning/phases/53-variant-contract-picker-upgrade/53-01-SUMMARY.md
@.planning/phases/53-variant-contract-picker-upgrade/53-03-SUMMARY.md

**COORD-01/D-01:本 plan 零 khs 文件。执行者不得修改 files_modified 之外的文件。**

<interfaces>
<!-- 提取自 53-PATTERNS + 源码核读,执行者直接取用 -->

select-winner.ts 现状(核读 2026-08-21):schema L37-41 {projectId, episodesId, winnerNodeId z.string().min(1).max(128)};L82-88 幂等分支 early-return 200 applied:false(刻意不触 o_assets/桥/广播——Pitfall 5 保持);L146-158 reviewBridge void 挂点(扩展位紧随其后);L160-166 broadcast variant:selected;L171-174 500 catch。**扩展三处且仅三处:schema 加两 optional、updated 段挂 hook、response 不变。**

reviewBridge 纪律(P1,照抄):`ReviewBridgeDeps {baseUrl?, fetchImpl?, logger?, timeoutMs?}`;函数 never-throws(try/catch 吞一切,连 broken logger 也不 throw);幂等语义 = "值已相等 → no-op"(队列重放依赖它);params 平面对象。

队列表 DDL(DR-4 锁定,列名精确):canvas_writeback_queue { id(autoincr PK), project_id, episodes_id, action: "manifest_writeback"|"g15_waive"|"g15_requeue"(text), payload(text JSON), state: "pending"|"done"|"failed", attempts(int default 0), max_attempts(int default 8), next_attempt_at(bigint ms), last_error(text nullable), created_at, updated_at } + index (project_id, episodes_id, state, next_attempt_at)。注册在 initDB.ts relationalCanvasTables family(L1257+,canvas_variant_groups builder 形状 L1332-1347);state-enum 列注释照 kv_shotGraph L1122 惯例。53-07 g15-ops 复用 action 枚举两值——本 plan 在 DDL 注释与 writebackQueue 类型里预留。

drain 三路径(DR-4):ensureDrainStarted 幂等单例(首次 enqueue/select-winner 首次调用时启动;setInterval 30s + 立即一次);drainOnce 串行逐条:state=pending 且 next_attempt_at<=now → 调 handler → 成功 done / 失败 attempts+1 且 (attempts>=max_attempts ? failed : next_attempt_at = now + 30000*2^attempts)。drainOnce 导出供 verify 手动驱动(测试不起真 timer)。

53-01 产物:candidateSourceSchema(5 源 enum)——端点 source 字段与 hook payload 直接 import。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: canvas_writeback_queue DDL + writebackQueue.ts 队列服务</name>
  <files>src/lib/initDB.ts, src/lib/writebackQueue.ts</files>
  <read_first>src/lib/initDB.ts (L1257 relationalCanvasTables family + L1332-1347 canvas_variant_groups builder 形状 + L1117-1127 kv_shotGraph state-enum 注释惯例), src/lib/canvasRelationalStore.ts (L367-369 db 参数 docstring + L446-451 + L526-545 事务包裹范式), .planning/phases/53-variant-contract-picker-upgrade/53-RESEARCH.md (DR-4 全节——表结构/退避/drain 三路径锁定值)</read_first>
  <action>
  1. initDB.ts relationalCanvasTables family 追加 canvas_writeback_queue builder:列/默认值/索引逐项照 interfaces 段 DR-4 锁定表(id autoincr、action 三枚举注释、state 三枚举注释照 kv_shotGraph 惯例、index (project_id, episodes_id, state, next_attempt_at))。
  2. 新建 writebackQueue.ts(db 参数惯例 P4,全部函数 db 首参):
     - `type WritebackAction = "manifest_writeback" | "g15_waive" | "g15_requeue"`;`enqueueWriteback(db, {projectId, episodesId, action, payload}): Promise<{id: number}>`——INSERT state='pending' attempts=0 max_attempts=8 next_attempt_at=now+30000;自身 try/catch 由调用方决定降级(本函数可 throw,调用方包装)。
     - `drainOnce(db, handlers: {(row): Promise<boolean>}): Promise<{processed: number; delivered: number}>`——SELECT pending 且 next_attempt_at<=now ORDER BY id 串行逐条;handler 返回 true→state='done';false→attempts+1、last_error 截断 500 字符、attempts>=max_attempts→'failed' 否则 next_attempt_at=now+30000*2**attempts;单条 handler throw 按 false 处理(吞错继续下一条,串行不并发)。
     - `ensureDrainStarted(db, drainFn): void`——模块级单例 flag(进程内一次);启动即 drainFn 一次 + setInterval(()=>drainFn(), 30_000);interval 句柄保存,提供 `stopDrainForTest()`。
     - 常量导出 `WRITEBACK_BACKOFF_BASE_MS = 30_000`、`WRITEBACK_MAX_ATTEMPTS = 8`。
  3. 注意:不写任何端点路由——消费方在 Task 2(select-winner)与 53-07(g15-ops)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc --noEmit 2>&1 | tail -3; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'canvas_writeback_queue' src/lib/initDB.ts` ≥ 1 且在 relationalCanvasTables family 内
    - initDB canvas_writeback_queue builder 含 `next_attempt_at` 与 `idx` 索引列四元组(project_id, episodes_id, state, next_attempt_at)
    - `grep -c 'export.*enqueueWriteback\|export.*drainOnce\|export.*ensureDrainStarted' src/lib/writebackQueue.ts` = 3
    - writebackQueue.ts 含 `30_000` 与 `2 **` 退避字面(或等价 Math.pow)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>持久化重试队列就位:表 + 入队 + 串行 drain + 指数退避 + max_attempts,g15 两 action 枚举已预留。</done>
</task>

<task type="auto">
  <name>Task 2: manifestWriteback.ts 传输抽象 + select-winner 端点扩展</name>
  <files>src/lib/manifestWriteback.ts, src/routes/canvas/v2/select-winner.ts</files>
  <read_first>src/lib/reviewBridge.ts (全文——deps 注入接口 L74-79 / never-throws L114-119+L249-257 / 幂等语义 L237-240,纪律逐条复刻), src/routes/canvas/v2/select-winner.ts (全文 179 行——schema L37-41 / 幂等分支 L82-88 / reviewBridge 挂点 L146-158 / broadcast L160-166), src/lib/writebackQueue.ts (Task 1 产物:enqueueWriteback/drainOnce 签名), src/lib/candidateEnvelope.ts (53-01 产物:candidateSourceSchema)</read_first>
  <action>
  1. 新建 manifestWriteback.ts(reviewBridge 纪律逐条复刻,P1/P2):
     - `export interface ManifestWritebackParams { projectId: number; episodesId: number; groupId: string; winnerNodeId: string; variantIndex: number; frameSlot?: "first" | "last"; source?: string }`
     - `export interface ManifestTransport { writeSelection(params, target: {field: "selected_first_variant" | "selected_last_variant" | "chosen_variant_id"; value: number}): Promise<void> }`——target.field 由 frameSlot 映射:frameSlot='first'→selected_first_variant;'last'→selected_last_variant;undefined(G14 预览等)→chosen_variant_id;value = variantIndex(1-based)。transport 实现自身保证幂等(值已相等 → no-op,docblock 写明重放依赖)。
     - `export function getManifestTransport(): ManifestTransport | null`——读 process.env.KMC_MANIFEST_TRANSPORT;Wave A 无实现返回 null(planner 裁定:未配置 = 通道未开通,warn-once 跳过不入队,避免 Wave A 把每笔选定都灌成 8 次重试的 failed 行;Wave B 在此挂 FS/HTTP 实现)。
     - `export async function enqueueManifestWriteback(params: ManifestWritebackParams): Promise<void>`——never-throws(reviewBridge L114-119 吞错纪律 + broken-logger 双保险):transport 为 null → 模块级 warn-once(`[manifestWriteback] 传输未配置,跳过 manifest 回写(队列未启用)`)后 return;transport 存在 → try writeSelection,成功 return;失败/throw → enqueueWriteback(db, {action: "manifest_writeback", payload: params}) 入队,入队自身再失败 → logger.warn 降级(最坏丢一次回写,canvas 真值已在,Pitfall 4)。db 从 "@/utils/db" 取(与 select-winner 同源)。
     - `export async function replayManifestWriteback(row, transport): Promise<boolean>`——drain handler:按 row.payload 重放 writeSelection,成功 true。
  2. select-winner.ts 三处扩展且仅三处:
     - schema 追加 `frameSlot: z.enum(["first", "last"]).optional()` 与 `source: candidateSourceSchema.optional()`(向后兼容,缺省行为与 Phase 49 逐字节一致)。
     - reviewBridge void 调用之后、broadcast 之前(updated 段 reach 内)挂:`void enqueueManifestWriteback({ projectId, episodesId, groupId, winnerNodeId, variantIndex: result.variantIndex, frameSlot, source }).catch(() => {})`;并在此处调一次 `ensureDrainStarted` 系列(具体:模块加载或首次调用时 ensureDrainStarted(db, () => drainOnce(db, replayWithDefaultTransport))——drain 回调里 transport 为 null 时直接 processed 0 返回,不误标 failed)。
     - 幂等分支(L82-88)零改动——重复选定不触发回写(Pitfall 5)。
     - response 形状不变(不暴露回写状态;回写是 best-effort 附属通道)。
  3. 文件头 docblock:Wave B 决策点(FS vs HTTP,Open Question 1)与 transport 注入理由冻结注明。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc --noEmit 2>&1 | tail -3; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'enqueueManifestWriteback' src/routes/canvas/v2/select-winner.ts` ≥ 1 且同一行或邻行含 `.catch(() => {})`(P1 挂点形状)
    - select-winner.ts schema 含 `frameSlot: z.enum(["first", "last"]).optional()` 与 `source:` 两字段
    - manifestWriteback.ts 含字面 `selected_first_variant`、`selected_last_variant`、`chosen_variant_id`(D-11 权威字段名)
    - manifestWriteback.ts 的 enqueueManifestWriteback 全函数体在 try/catch 内(never-throws;grep 'catch' ≥2)
    - `grep -c 'KMC_MANIFEST_TRANSPORT' src/lib/manifestWriteback.ts` ≥ 1
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>选定 → manifest 回写通道成形:端点一处收口(D-09)、best-effort 隔离、失败入队重放(D-10)、首尾字段名对齐 p11a0(D-11)、Wave B 只需挂 transport。</done>
</task>

<task type="auto">
  <name>Task 3: verify S3 填充 — 端点扩展 + 队列重放集成断言</name>
  <files>scripts/verify-phase-53.ts</files>
  <read_first>scripts/verify-phase-53.ts (53-01/53-03 后的骨架:S3 占位标记), scripts/verify-phase-49.ts (端点 dispatch 派生子进程范式——app-db knex pool 与 :memory: store 段不得同进程), scripts/verify-phase-51.ts (L201-210 import-order guard + L216-311 集成段), src/lib/writebackQueue.ts + src/lib/manifestWriteback.ts (Task 1/2 产物签名)</read_first>
  <action>
  1. 替换 `FILLED-BY-53-04` 为 S3 live 段:
     a. **队列机制**(:memory: knex 建表):enqueue 一条 manifest_writeback → 断言 state='pending' attempts=0 next_attempt_at≈now+30s;drainOnce 用总是失败的 handler → attempts=1 且 next_attempt_at=now+60000(2^1 退避);循环失败 8 次 → state='failed' 且不再被 SELECT;成功 handler → 'done';串行性(两条 pending 按id 序处理)。
     b. **hook 隔离**:enqueueManifestWriteback 在 transport=null 下 → 无队列行产生(warn-once 路径)+ 函数 resolve 不 throw;注入总是抛错的 fake transport → 队列出现 1 行 pending 且函数仍 resolve;注入成功 fake transport → writeSelection 收到的 target.field 满足映射:frameSlot 'first'→selected_first_variant / 'last'→selected_last_variant / 无→chosen_variant_id,value=variantIndex。
     c. **端点集成**(spawn 子进程 dispatch,49-01 范式,绝不与 :memory: 段同进程):seed :memory: 或文件 db 的 canvas_variant_groups + canvas_nodes(经 53-03 推导或直插)→ POST select-winner 带 frameSlot:'first' → 200 applied:true;再 POST 同 winner → 200 applied:false 且队列行数不变(幂等分支不触发 hook,Pitfall 5);不传 frameSlot/source 的旧式 POST → 200(向后兼容)。
     d. **drain 重放闭环**:队列灌入一条 pending + fake 失败 transport 先失败一次,再换成功 transport drainOnce → 行变 done(handler 幂等重放语义)。
  2. `npm run verify:phase-53` 跑到 exit 0(S1+S2+S3 绿)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npm run verify:phase-53 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:phase-53` exit 0,S3 四组断言(队列机制/hook 隔离/端点集成/drain 重放)全绿
    - `grep -c 'FILLED-BY-53-04' scripts/verify-phase-53.ts` = 0
    - S3c 断言含 applied:false 分支的队列行数不变检查(grep 'applied' 于 S3 段 ≥2)
    - 端点 dispatch 在子进程(grep -c 'spawn\|fork' scripts/verify-phase-53.ts ≥1)
  </acceptance_criteria>
  <done>S3 契约门锁死:退避数学/幂等分支隔离/best-effort 不影响 200/重放闭环,四组断言可反复跑。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端 → POST select-winner(frameSlot/source 新入参面,V5) | zod 校验后的受控输入 |
| 队列重放 → transport(Wave B = FS 写 episode workdir / HTTP) | 出站写通道,本 plan 仅接口 |
| queue 表 ← drain 定时器 | 进程内后台消费者 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04-01 | Tampering | frameSlot/source 注入超长/越权值(V5) | mitigate | zod enum + 既有 winnerNodeId max(128);schema 扩展仅两 optional enum |
| T-53-04-02 | DoS | 队列重放风暴(不可达 transport 反复打) | mitigate | max_attempts=8 + 指数退避 + 串行 drain(DR-4);S3a 断言 failed 终态 |
| T-53-04-03 | Tampering | enqueue 失败炸掉选定响应(违反 D-10) | mitigate | hook 整体 void + never-throws + 入队降级 warn;S3b 断言 fake 抛错 transport 下函数仍 resolve |
| T-53-04-04 | Repudiation | 回写丢失无痕 | mitigate | 队列行含 last_error(截断 500 字符)+ state 审计;warn 日志带 [manifestWriteback] 前缀 |
| T-53-04-05 | Elevation | 跨组/跨 episode 选定(既有面) | mitigate | selectWinnerInGroup 复合 scope where 既有;不改动事务段 |
| T-53-04-06 | Tampering | Wave B FS 直写路径穿越(预告) | mitigate(Wave B) | transport 接口 docblock 冻结约束:路径必须限定在 episode workdir 内——Wave B plan 检查项(53-RESEARCH V12) |
| T-53-04-SC | Tampering | package installs | accept | 零新包 |

**结论:无 high 级未缓解威胁;最高风险(队列风暴/响应阻塞)均由 DR-4 参数 + S3 断言收口。Wave B 的 FS 直写穿越风险已冻结为接口约束。**
</threat_model>

<verification>
- `npm run verify:phase-53` exit 0(S1+S2+S3)
- `npx tsc --noEmit` exit 0
- 旧客户端(不传 frameSlot/source)POST select-winner 行为逐字节不变(S3c 向后兼容断言)
</verification>

<success_criteria>
- VAR-03 kap 半部成立:选定 → 端点(事务/幂等/广播复用)→ manifest 回写 hook(best-effort)→ 失败队列重放
- D-09(一处收口)/D-10(canvas 真值+队列)/D-11(首尾字段名+frameSlot 参数面)落地
- Wave B 只剩:transport 实现(FS/HTTP 裁定)+ khs 映射 + E2E 闭环(gated on khs2 v2.4 Phase 25 验收 — planned separately)
</success_criteria>

<output>
Create `.planning/phases/53-variant-contract-picker-upgrade/53-04-SUMMARY.md` when done. 记录:transport=null 不入队裁定及理由、退避参数表、Wave B 挂接点清单(getManifestTransport/replayManifestWriteback)。
</output>
