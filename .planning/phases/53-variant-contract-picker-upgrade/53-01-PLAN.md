---
phase: 53-variant-contract-picker-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/candidateEnvelope.ts
  - scripts/verify-phase-53.ts
  - scripts/fixtures/phase53/candidates-legacy.json
  - scripts/fixtures/phase53/candidates-envelope.json
  - scripts/fixtures/phase53/take-log.json
  - scripts/fixtures/phase53/failed-shots.json
  - package.json
autonomous: true
requirements: [VAR-01]
must_haves:
  truths:
    - "kap 侧 candidate envelope zod 契约存在且能同时解析今日扁平形状与 Wave B 结构化形状,round-trip 字段不丢失(D-02 kap schema 先行)"
    - "今日无 score 的候选解析不报错——score 缺省 = undefined(VAR-01 实锤:score 今天在 khs call site 被丢弃,envelope 不得强制)"
    - "groupKey 词表与 Phase 48 逐字节一致:`shot:{shot_id}:first` / `shot:{shot_id}:last` / `name:{parentDir}/{base}`(Pitfall 2 防漂移)"
    - "G15 归因 taxonomy 固化为 zod enum(9 值,DR-6),take_log 五 verdict 与 failed-shots 形状锁定(D-16 契约面)"
    - "verify:phase-53 npm script 注册且 S1 可跑、forced-failure 自检证明 gate 能失败(v2.0 四道闸复刻,D-02)"
    - "存量数据不受影响——契约只是新增解析层,不改任何既有端点行为(D-03 存量不回填)"
    - "D-01: 双波拆分——本 phase 全部 7 个 plan 均为 Wave A(kap 接收端);Wave B(khs field-map/canvas_sync 映射 + VAR-03 E2E 闭环)等 khs2 v2.4 Phase 25 验收完成后单独 plan,本 phase 零 khs 文件改动"
  artifacts:
    - path: "src/lib/candidateEnvelope.ts"
      provides: "candidateEnvelopeSchema + candidateSourceSchema + candidateScoreSchema + g15ErrorCategorySchema + takeLogEntrySchema + failedShotEntrySchema + normalizeLegacyCandidateData + parseCandidateEnvelope"
      contains: "candidateEnvelopeSchema"
    - path: "scripts/verify-phase-53.ts"
      provides: "S1(envelope round-trip,live)+ S2..S5 占位标记 + forced-failure 自检 + mkdtemp/chdir 隔离"
      contains: "verify-phase-53"
    - path: "package.json"
      provides: "verify:phase-53 npm script"
      contains: "verify:phase-53"
  key_links:
    - from: "scripts/verify-phase-53.ts"
      to: "src/lib/candidateEnvelope.ts"
      via: "S1 动态 import 真实模块(零重实现)"
      pattern: "candidateEnvelope"
    - from: "scripts/verify-phase-53.ts"
      to: "scripts/fixtures/phase53/*.json"
      via: "fixture JSON 读入喂两代形状"
      pattern: "fixtures/phase53"
---

<objective>
建 VAR-01 的 kap 契约半部(D-02:kap schema 先行,khs 映射 Wave B 后补):新增 `src/lib/candidateEnvelope.ts`——统一 candidate 信封 zod schema + 5 源判别 + 今日扁平形状归一化 + G15 归因 taxonomy;落 `scripts/verify-phase-53.ts` 契约门骨架(S1 live,S2..S5 留填充标记)与双代 fixture;注册 `verify:phase-53` npm script。

这是后续所有 Wave A plan 的契约地基:53-03 组推导消费 normalizeLegacyCandidateData,53-04 端点扩展消费 candidateSourceSchema,53-07 G15 面板消费 g15 taxonomy。

Wave B 门控注记(不在本 plan 实现):D-02 khs 侧 = khs canvas_sync/_manifest 按 envelope 形状映射(gated on khs2 v2.4 Phase 25 验收 — planned separately)。

输出:candidateEnvelope.ts + verify-phase-53.ts(S1 绿)+ fixtures ×4 + npm script。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/53-variant-contract-picker-upgrade/53-CONTEXT.md
@.planning/phases/53-variant-contract-picker-upgrade/53-RESEARCH.md
@.planning/phases/53-variant-contract-picker-upgrade/53-PATTERNS.md
@.planning/phases/53-variant-contract-picker-upgrade/53-VALIDATION.md

**COORD-01/D-01:本 plan 零 khs 文件触碰。全部改动在 kais-aigc-platform 仓内;执行者不得修改 files_modified 之外的文件。**

<interfaces>
<!-- 提取自 53-RESEARCH Pattern 1 / DR-2 / DR-6 与 candidateGrouping.ts 模块纪律,执行者直接取用,不再推导 -->

模块纪律(src/lib/candidateGrouping.ts L1-37 模范):
- 文件头 docblock:声明 channels + 词汇表 + "Pure module: no DB, no fs, no network — data in, plans out"
- 类型先行的分段结构(`// ─── Types ───` 分隔条),类型 export 在前,helper 私有在下
- 本模块 = zod schema + 归一化函数,纯模块,**禁止** import db/fs/network

envelope 字段形状(53-RESEARCH Pattern 1,已锁定,勿再推导):
- candidateSourceSchema = z.enum(["p01_hook","p03_nbest","p11a0_flf","p11a_preview","p11b_take"])
- candidateScoreSchema = { overall: number(0-1), dimensions?: record<string,number>, scale: z.enum(["unit","percent"]).default("unit") }
- candidateEnvelopeSchema = { source, groupKey: string min(1), variantId: string, shotId?: string, frameSlot?: z.enum(["first","last"]), selected: boolean default(false), score?: candidateScoreSchema, durationSec?: number min(0), prompt?: string, seed?: number int, filePath?: string, thumbnailUrl?: string, extras: record<string,unknown> default({}) }
- root zod ^4.3.5;z.record 两参数形式 z.record(z.string(), z.number())(P8/Pitfall 8:绝不跨包 import schema 实例)

groupKey 词表(必须与 Phase 48 逐字节一致,candidateGrouping.ts L16-20):
- manifest/条件帧通道: `shot:{shot_id}:first` / `shot:{shot_id}:last`(首尾 = 两个组)
- 命名通道: `name:{parentDir}/{base}`
- a-flf 节点 data 里今日的 groupKey 是 `{shot_id}_{first|last}` 短横格式 —— normalizeLegacyCandidateData 负责映射到 `shot:{sid}:first|last`

今日 5 源 wire 形状(53-RESEARCH §Current Wire Formats,envelope 必须吃下):
- 源 1 p01 hook(c-* variant 节点): data = {id:"variant-{vid}", label, selected, description, score?, data:{标量+frame_breakdown_3sec}};无 groupKey
- 源 2 p03 N-best: 透传 thumbnailUrl/filePath/description/score 四键,score 在 call site 被丢弃
- 源 3 p11a0 条件帧(a-flf-*): data = {label, assetType:"keyframe", frame_type, variant:"v1".."vN", groupKey:"{shot_id}_{first|last}", shot_id, filePath(/oss/), generation_prompt, isPrimaryView, curationState(active|eliminated), state, tags(["★ 选定"|"○ 待选"|"✕ 淘汰"]), description}
- 源 4 p11a 预览变体: JSONL {shot_id, variant_id, structure_delta, clip_path, generation_time_ms, engine}
- 源 5 p11b take-log: {take_n, shot_id, changed_variable, seed, verdict(keep|fix_in_post|edit|re_roll|rewrite), evidence, timestamp} + shot_index

G15 taxonomy(DR-6,9 值 enum,精确字符串): qc_vision_fail / engine_render_error / bgm_trigger / delegate_timeout / delegate_parse / schema_validation / needs_regenerate / take_verdict_* / unknown
- take_verdict_* 是前缀族(五值 keep/fix_in_post/edit/re_roll/rewrite),enum 用字面 "take_verdict_*" 占位值 + 附导出的 verdict→category 映射函数 takeVerdictCategory(verdict): "take_verdict_keep" 等(zod enum 不支持 pattern,占位值 + 映射函数是裁定)

verify 脚本范式(scripts/verify-phase-51.ts,457 行,P7):头 docblock S-section map / assert 收集器 + REPO_ROOT / mkdtemp+chdir 隔离 BEFORE 动态 import(含 package.json copy)/ grepSource 排除 src/routes/canvas/static + data/web / forced-failure 自检(shadow asserts 必须失败,意外 PASS = 失败)/ 汇总 exit 0|1|2
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: candidateEnvelope.ts — 统一信封 zod 契约 + legacy 归一化</name>
  <files>src/lib/candidateEnvelope.ts</files>
  <read_first>src/lib/candidateGrouping.ts (L1-37 模块头纪律 + L16-20 groupKey 词表), src/lib/canvasAssetSchema.ts (确认 optionalTypes L125 跳过 variant 类型——envelope 绝不塞进 assetDataSchemas,53-RESEARCH Anti-Patterns), .planning/phases/53-variant-contract-picker-upgrade/53-RESEARCH.md (§Current Wire Formats 全节 + Pattern 1 + DR-2 + DR-6)</read_first>
  <action>
  1. 新建纯模块(照 candidateGrouping.ts 模块纪律:docblock 声明 5 源通道 + groupKey 词表 + "Pure module: no DB, no fs, no network"),root zod(^4.3.5)。
  2. 导出 schema(字段形状见上方 interfaces,全部锁定勿改):`candidateSourceSchema`、`candidateScoreSchema`、`candidateEnvelopeSchema`(infer 出 `CandidateEnvelope` type)、`takeLogEntrySchema`(verdict enum 五值)、`failedShotEntrySchema`({shot_id, error, timestamp, run_id})、`g15ErrorCategorySchema`(9 值 enum,字面值见 interfaces)。
  3. 导出 `normalizeLegacyCandidateData(data: Record<string, unknown>): CandidateEnvelope | null`——今日扁平形状 → 信封:
     - a-flf 形状判定(有 frame_type 或 groupKey 匹配 `{shot_id}_{first|last}` 模式):source="p11a0_flf";frame_type→frameSlot;variant→variantId;groupKey `{sid}_{slot}`→`shot:{sid}:{slot}`;shot_id→shotId;isPrimaryView===true 或 tags 含 "★ 选定"→selected=true;generation_prompt→prompt;filePath/thumbnailUrl/description 直通;无 score 则 score=undefined(不造 0 分)。
     - c-* p01 variant 形状判定(label+selected+description 且无 frame_type):source="p01_hook";label→variantId 无法推导时用 data.id;description→prompt;selected→selected;返回的 envelope groupKey 允许为空串并带 `derivable: false` 语义——裁定:c-* 节点无组信号,parseCandidateEnvelope 返回的 envelope 中 groupKey 置 "":(53-03 组推导据此跳过,等 Wave B 结构化 groupKey)。注意 zod groupKey min(1) 会拒空串——归一化产物不经 schema 再校验,直接构造对象;文档注释写明。
     - 其余形状返回 null(不猜)。
  4. 导出 `parseCandidateEnvelope(data: unknown): CandidateEnvelope | null`——先 `candidateEnvelopeSchema.safeParse`(Wave B 结构化形状直通,未知键容忍),失败则 `normalizeLegacyCandidateData`(今日形状)。两代形状一个入口。
  5. 导出 `takeVerdictCategory(verdict): "take_verdict_keep" | ... ` 五值映射 + `classifyG15Error(raw: {error?: string; verdict?: string; needsRegenerate?: boolean}): G15ErrorCategory`——按 DR-6 对号:verdict 优先 take_verdict_*;needsRegenerate→needs_regenerate;error 含 "timeout"→delegate_timeout;含 "parse"→delegate_parse;含 "schema"→schema_validation;含 BGM 违禁特征→bgm_trigger;兜底 unknown。纯字符串分类,不做 IO。
  6. 全部新增字段 optional/default——今日形状绝不 400(Pitfall 1)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc --noEmit 2>&1 | tail -3; echo "tsc-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'candidateEnvelopeSchema\|candidateSourceSchema\|g15ErrorCategorySchema\|normalizeLegacyCandidateData\|parseCandidateEnvelope' src/lib/candidateEnvelope.ts` ≥ 5 且均为 export
    - `grep -c 'assetDataSchemas' src/lib/canvasAssetSchema.ts` 不变(envelope 未塞入 per-type 基线;canvasAssetSchema.ts 零修改)
    - src/lib/candidateEnvelope.ts 无 `from "@/utils` / `require(` / `fs` / `fetch` import(纯模块,grep 0 命中)
    - `npx tsc --noEmit` exit 0
    - candidateEnvelope.ts 含字面量 "p01_hook" 与 "p11b_take"(source enum 精确字符串)
  </acceptance_criteria>
  <done>统一信封契约落地:两代形状一个入口,5 源判别 + G15 taxonomy 固化,纯模块纪律成立。</done>
</task>

<task type="auto">
  <name>Task 2: 双代 fixtures + verify-phase-53.ts 骨架(S1 live)+ npm script</name>
  <files>scripts/fixtures/phase53/candidates-legacy.json, scripts/fixtures/phase53/candidates-envelope.json, scripts/fixtures/phase53/take-log.json, scripts/fixtures/phase53/failed-shots.json, scripts/verify-phase-53.ts, package.json</files>
  <read_first>scripts/verify-phase-51.ts (全文——骨架近逐行复刻:头 docblock/assert 收集器/mkdtemp+chdir/grepSource/forced-failure/exit codes), package.json (scripts 区 L30-44 verify:phase-* 注册惯例), .planning/phases/53-variant-contract-picker-upgrade/53-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map), .planning/phases/53-variant-contract-picker-upgrade/53-RESEARCH.md (§Current Wire Formats——fixture 字段照此快照)</read_first>
  <action>
  1. 建 `scripts/fixtures/phase53/` 四个 JSON(字段值照 §Current Wire Formats 快照,勿发明):
     - candidates-legacy.json:4 个节点 data 快照——(a) a-flf-shot_012-first-v1(frame_type:"first", variant:"v1", groupKey:"shot_012_first", shot_id:"shot_012", isPrimaryView:true, tags:["★ 选定"], generation_prompt:"雨夜巷战,主角拔刀…", filePath:"/oss/kmc/ep01/P11/shot_012_first_v1.png");(b) a-flf-shot_012-first-v2(同上但 v2/isPrimaryView:false/tags:["○ 待选"]);(c) a-flf-shot_012-last-v1(frame_type:"last", groupKey:"shot_012_last");(d) c-p01-variant-2(label:"变体 2", selected:false, description:"hook 文案…", 无 groupKey 无 score)。
     - candidates-envelope.json:5 个 Wave B 结构化信封(每源一个:p01_hook/p03_nbest/p11a0_flf/p11a_preview/p11b_take),各含 groupKey(词表格式)、variantId、score:{overall:0.91, dimensions:{...}, scale:"unit"} 其中 p11a0_flf 的 scale:"percent" overall:0.87(源刻度声明)、frameSlot、prompt、seed、durationSec。
     - take-log.json:5 条 takes,verdict 各取 keep/fix_in_post/edit/re_roll/rewrite 一值,含 take_n/shot_id/changed_variable/seed/evidence/timestamp。
     - failed-shots.json:3 条 failures {shot_id:"shot_031", error:"...", timestamp, run_id},error 分别含 "timeout"/"schema"/普通渲染错误字样。
  2. 新建 scripts/verify-phase-53.ts(骨架照 verify-phase-51.ts):头 docblock S-section map——S1 VAR-01 envelope round-trip(live)/ S2 候选组推导(FILLED-BY-53-03 占位)/ S3 select-winner 扩展+队列(FILLED-BY-53-04 占位)/ S4 变体墙源形状(FILLED-BY-53-07 占位)/ S5 G15 桥 + forced-failure 自检(forced-failure 部分本 plan 即 live)。mkdtemp+chdir 隔离 + package.json copy + assert 收集器 + grepSource 排除清单复制。
  3. S1 live 断言(真实 import ../src/lib/candidateEnvelope.ts,零重实现):
     a. 四个 legacy 快照逐一 parseCandidateEnvelope:a→{source:"p11a0_flf", groupKey:"shot:shot_012:first", frameSlot:"first", variantId:"v1", shotId:"shot_012", selected:true, prompt 含"雨夜"};b→selected:false;c→groupKey:"shot:shot_012:last" 且 frameSlot:"last"(首尾两键,Pitfall 6);d→source:"p01_hook" 且 score===undefined(今日无 score 不报错)。
     b. 5 个 Wave B 信封 safeParse 全过且 round-trip:score.overall/dimensions/scale/prompt/seed 逐字段不丢;percent 刻度原样保留(scale:"percent" 不被改写)。
     c. 容忍性:给 legacy a 追加未知键 "foo":"bar" → parse 仍成功(进 extras 或忽略皆可,不 throw)。
     d. take-log 5 verdict round-trip;takeVerdictCategory("re_roll")==="take_verdict_re_roll";classifyG15Error 对 failed-shots.json 三条分别归 delegate_timeout/schema_validation/engine_render_error(或 unknown),断言非空 enum 值。
  4. S5 forced-failure 自检 live:3-4 条 shadow asserts(如断言 groupKey==="wrong-format"、score.overall===999),必须失败;意外 PASS → 整体 exit 1。
  5. package.json scripts 追加 `"verify:phase-53": "npx tsx scripts/verify-phase-53.ts",`(L44 verify:phase-51 之后)。
  6. 运行 `npm run verify:phase-53` 至 exit 0。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npm run verify:phase-53 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:phase-53` exit 0,S1 断言全绿 + forced-failure 自检行为正确(临时把一条 shadow assert 改成真断言会令 exit 1——执行者自测后还原)
    - `grep -c 'FILLED-BY-53-03\|FILLED-BY-53-04\|FILLED-BY-53-07' scripts/verify-phase-53.ts` = 3(占位标记就位)
    - `grep -c 'verify:phase-53' package.json` = 1
    - `ls scripts/fixtures/phase53/ | wc -l` = 4
    - verify-phase-53.ts 含 mkdtempSync 与 chdir(隔离先于动态 import,P7)
  </acceptance_criteria>
  <done>契约门就位:S1 双代 round-trip 锁死 + forced-failure 证明 gate 能失败 + S2..S5 占位待后续 plan 填充。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| khs canvas_sync 同步数据 → kap envelope 解析 | 外部产线写入的 node data,不可信输入面 |
| envelope extras (record<string,unknown>) → 前端展示 | 未知键透传,消费侧不得 eval/innerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01-01 | Tampering | envelope 过严拒收今日形状(V5) | mitigate | 全字段 optional/default + legacy 归一化容忍缺 score;S1c 未知键容忍断言锁死 |
| T-53-01-02 | Tampering | extras 恶意 payload 注入展示层 | mitigate | extras 保持 unknown 类型,契约注释明令消费侧只做字符串展示;本 phase 无 innerHTML 通道 |
| T-53-01-03 | DoS | 超大 node data 打爆解析 | accept | node data 尺寸已由 save 路径既有上限约束(khs 侧 ≤500 字符标量穿透惯例);envelope 不新增放大面 |
| T-53-01-SC | Tampering | package installs | accept | 零新包(53-RESEARCH Package Legitimacy Audit:无新增注册表交互) |

**结论:纯解析层无新端点;主要风险 = 契约过严拒收(Pitfall 1),由 S1 双代 fixture 锁死。无 high 级威胁。**
</threat_model>

<verification>
- `npm run verify:phase-53` exit 0(S1 live + forced-failure)
- `npx tsc --noEmit` exit 0
- `grep -c 'candidateEnvelopeSchema' src/lib/candidateEnvelope.ts` ≥ 1(export)
- `grep -c 'verify:phase-53' package.json` = 1
</verification>

<success_criteria>
- VAR-01 kap 半部契约成立:两代形状一个入口、round-trip 不丢字段、G15 taxonomy 固化
- D-02 kap schema 先行落地;D-03 存量不回填(契约零端点行为变更)
- Wave B 门控注记:D-02 khs 映射侧、D-16 take_log 透传侧 = Wave B(gated on khs2 v2.4 Phase 25 验收 — planned separately)
- 53-03/53-04/53-07 可直接 import 本模块符号开工
</success_criteria>

<output>
Create `.planning/phases/53-variant-contract-picker-upgrade/53-01-SUMMARY.md` when done. 记录:c-* 无组信号的 groupKey 空串裁定、take_verdict_* 占位值+映射函数裁定、classifyG15Error 的字符串特征表。
</output>
