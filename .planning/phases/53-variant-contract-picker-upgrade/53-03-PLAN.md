---
phase: 53-variant-contract-picker-upgrade
plan: 03
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - src/lib/candidateGroupDeriver.ts
  - src/routes/canvas/v2/load-v2.ts
  - scripts/verify-phase-53.ts
autonomous: true
requirements: [VAR-01, VAR-03]
must_haves:
  truths:
    - "kmc 候选节点在图加载后被物化为 canvas variantGroups(selectMode:'single'),select-winner 端点/前端守卫/广播从此对 kmc 候选可用(53-RESEARCH Critical Gap 的解)"
    - "groupKey 词表与 Phase 48 逐字节一致:`shot:{shot_id}:first` / `shot:{shot_id}:last` / `name:{parentDir}/{base}`——a-flf 节点 data 的 `{sid}_{slot}` 短横格式被归一化映射"
    - "G13 首帧/尾帧 = 两个组,永不合并(Pitfall 6/D-11 前提)"
    - "推导幂等:同输入重复 materialize 零变更;确定性组 id = `cand:{groupKey}`;既有 winner 不被覆盖"
    - "只物化 `cand:` 前缀机器组——用户手工建的组永不触碰;成员 <2 的组跳过(单成员无墙义);推导永不 throw(无组信号 = 维持现状)"
    - "仅全量加载路径触发(无 since 参数);增量轮询与空图不触发;物化失败降级为 warn 日志,加载响应不受影响(best-effort,与 khs 重同步互不清组)"
  artifacts:
    - path: "src/lib/candidateGroupDeriver.ts"
      provides: "deriveCandidateGroups(nodes) 纯函数 + materializeCandidateGroups(db, scope, derived) 事务物化"
      contains: "deriveCandidateGroups"
    - path: "src/routes/canvas/v2/load-v2.ts"
      provides: "全量加载路径上的 derive→materialize→合并进响应 graph.variantGroups 钩子"
      contains: "candidateGroupDeriver"
  key_links:
    - from: "src/lib/candidateGroupDeriver.ts"
      to: "src/lib/candidateEnvelope.ts"
      via: "normalizeLegacyCandidateData/parseCandidateEnvelope 提取 groupKey(零逻辑复制)"
      pattern: "candidateEnvelope"
    - from: "src/lib/candidateGroupDeriver.ts"
      to: "src/lib/candidateGrouping.ts"
      via: "parseVariantName 复用(_v{N} 命名通道)+ groupKey 词表对齐"
      pattern: "parseVariantName"
    - from: "src/routes/canvas/v2/load-v2.ts"
      to: "src/lib/candidateGroupDeriver.ts"
      via: "loadFullGraph 返回后 derive(graph.nodes) → materialize → 合并响应"
      pattern: "materializeCandidateGroups"
    - from: "scripts/verify-phase-53.ts"
      to: "src/lib/candidateGroupDeriver.ts"
      via: "S2 真模块 :memory: knex 集成断言(替换 FILLED-BY-53-03 占位)"
      pattern: "S2"
---

<objective>
解 53-RESEARCH Critical Gap:kmc 候选节点不在 canvas variantGroups 里(khs 从不写组、canvasAssetLinkage 头注释确认 variant_group_id NULL),而 select-winner/守卫/广播全以组为操作单元。本 plan 建 kap 侧候选组推导/物化:`deriveCandidateGroups` 纯函数(envelope 归一化 + `_v{N}` 命名通道 → Phase 48 词表 groupKey)+ `materializeCandidateGroups` 事务物化(确定性 id `cand:{groupKey}`,幂等)+ load-v2 全量加载路径钩子;verify-phase-53 S2 填充。

这是 VAR-02 墙对 kmc 候选可操作、VAR-03 端点对候选组可写的前置(53-05/53-06 直接消费)。

Wave B 门控注记(不在本 plan 实现):Wave B khs 结构化 envelope 显式 groupKey 到流后,推导通道自动吃结构化形状(parseCandidateEnvelope 已兼容);p01/p03/p11a/p11b 候选今日无组信号,等 Wave B(gated on khs2 v2.4 Phase 25 验收 — planned separately)。

输出:candidateGroupDeriver.ts + load-v2 钩子 + S2 绿。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/53-variant-contract-picker-upgrade/53-CONTEXT.md
@.planning/phases/53-variant-contract-picker-upgrade/53-RESEARCH.md
@.planning/phases/53-variant-contract-picker-upgrade/53-PATTERNS.md
@.planning/phases/53-variant-contract-picker-upgrade/53-01-PLAN.md
@.planning/phases/53-variant-contract-picker-upgrade/53-01-SUMMARY.md

**COORD-01/D-01:本 plan 零 khs 文件。执行者不得修改 files_modified 之外的文件。**

<interfaces>
<!-- 提取自 53-PATTERNS + 源码核读,执行者直接取用 -->

53-01 产物(本 plan 消费):`parseCandidateEnvelope(data)` 返回 CandidateEnvelope | null——a-flf 形状已归一化出 groupKey(`shot:{sid}:first|last`)/frameSlot/variantId/selected;c-* p01 形状 groupKey 为空串(无组信号,推导据此跳过)。

candidateGrouping.ts 可复用导出:`parseVariantName(filePath)` L146-153 → `{base, variant} | null`(`_v{N}` 解析);命名通道 groupKey 格式 `name:{parentDir}/{base}`(L366,dir-aware WR-03)。

物化写入通道(既有,零新建):`upsertVariantGroup(scope, vg)` canvasRelationalStore.ts L316-345——ON CONFLICT(id, project_id, episodes_id) DO UPDATE;`canvas_variant_groups` 列:id(128)/project_id/episodes_id/phase_index/branch_id/variant_node_ids(JSON text)/winner_node_id/select_mode/created_at/updated_at(initDB.ts L1332-1347);读取处 `listVariantGroups(scope)` L346-354。注意:upsertVariantGroup 用模块级 `db`(非参数)——materializeCandidateGroups 走 db 参数惯例(P4),直接在 db.transaction 内写同形状 INSERT ... ON CONLECTION(照 upsertVariantGroup 的 raw SQL 参数化形式),不绕道模块级单例,verify 才能 :memory: 注入。

db-as-parameter 范式(P4):canvasRelationalStore.ts L367-369 docstring + selectWinnerInGroup(trxDb, scope, ...) L446-451;ingestAssets.ts L87-123(db 参数 + 单事务 + warning 汇总不静默)。

load-v2.ts 现状:POST "/",validateFields({projectId, episodesId, since?});`loadFullGraph({projectId, episodesId})` 返回 FlowGraphV2|null(graph.variantGroups 来自 listVariantGroups);`since` 增量路径与空图(null)路径已存在——推导只挂全量(无 since)且 graph 非 null 的分支,materialize 后把新组 merge 进 graph.variantGroups 再响应。

FlowNodeV2.data 是 JSON 反序列化对象(listNodes 已 parse);节点 type 字符串如 "asset"/"variant"。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: candidateGroupDeriver.ts — 纯推导 + 事务物化</name>
  <files>src/lib/candidateGroupDeriver.ts</files>
  <read_first>src/lib/candidateGrouping.ts (L16-20 词表 + parseVariantName L146-153 + 模块头纪律), src/lib/candidateEnvelope.ts (53-01 产物:parseCandidateEnvelope 签名与 CandidateEnvelope 形状), src/lib/ingestAssets.ts (L87-146 db 参数 + 单事务 + warnings 汇总范式), src/lib/canvasRelationalStore.ts (L316-345 upsertVariantGroup 的 raw SQL 参数化形状 + L446-451 db 参数惯例), src/lib/initDB.ts (L1332-1347 canvas_variant_groups 列形状)</read_first>
  <action>
  1. 文件头 docblock(candidateGrouping.ts 纪律):声明两推导通道 + `cand:` id 约定 + 幂等语义 + "derive 纯函数无 IO;materialize 只写 canvas_variant_groups 的 cand: 前缀行"。
  2. 导出类型:`DerivedGroup { id: string; groupKey: string; selectMode: 'single'; variantNodeIds: string[]; winnerNodeId?: string; source: CandidateSource }` 与 `DeriveResult { groups: DerivedGroup[]; skipped: Array<{nodeId: string; reason: string}> }`。
  3. 导出 `deriveCandidateGroups(nodes: Array<{id: string; type: string; data: Record<string, unknown>}>): DeriveResult` 纯函数,两通道:
     - 通道 A(envelope):每节点 `parseCandidateEnvelope(node.data)`;envelope 非空且 groupKey 非空串 → 按 groupKey 聚合;winner 候选 = selected===true 或 isPrimaryView 的成员(多 selected 取首个,其余记 skipped 警告)。
     - 通道 B(命名):对 filePath 形如 `..._v{N}.png` 的 asset 节点用 `parseVariantName(filePath)`(candidateGrouping 导出);canonical 无后缀兄弟节点(同 dir 同 base)在节点集内存在才成组;groupKey = `name:{parentDir}/{base}`(dir-aware)。通道 A 已收编的节点不重复入通道 B。
     - 共同规则:成员 <2 的组丢弃(记 skipped "single-member");组 id = `cand:${groupKey}`,id 长度 >128 记 skipped "id-too-long"(列宽 string(128),绝不截断造碰撞);groupKey 撞车(两通道同 key)→ 合并成员集;全程不 throw。
  4. 导出 `materializeCandidateGroups(db: Knex, scope: {projectId, episodesId}, derived: DerivedGroup[]): Promise<{created: number; updated: number; unchanged: number}>`:单 db.transaction;每 DerivedGroup 读现有行(SELECT ... WHERE id+scope)——无则 INSERT(id/project_id/episodes_id/phase_index=0/branch_id='main'/variant_node_ids=JSON/ winner_node_id=derived winner ?? NULL/select_mode='single'/时间戳),记 created;有且为 `cand:` 前缀行则 UPDATE:variant_node_ids = derived 成员(机器组成员随现实自愈),winner 保留既有值除非既有为 NULL 且 derived 有 winner;有但**非** `cand:` 前缀(用户手工组撞 id)→ 跳过不碰,记 warning。参数化 SQL 照 upsertVariantGroup 形状(knex raw 或 builder 均可,零拼接)。
  5. 导出 `mergeDerivedGroups(existing: VariantGroupV2[], derived: DerivedGroup[]): VariantGroupV2[]` 纯函数——load 路径合并响应用(既有组优先,derived 只补缺)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc --noEmit 2>&1 | tail -3; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function deriveCandidateGroups\|export async function materializeCandidateGroups\|export function mergeDerivedGroups' src/lib/candidateGroupDeriver.ts` = 3
    - candidateGroupDeriver.ts 含 `parseCandidateEnvelope`(import 53-01 模块)与 `parseVariantName`(import candidateGrouping)各 ≥1 次
    - candidateGroupDeriver.ts 含字面 `` `cand:${` `` 或等价 `cand:` 前缀构造(grep 'cand:' ≥1)
    - deriveCandidateGroups 无 db/fs/network import(纯函数;grep "@/utils/db\|require(" = 0)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>两通道推导 + 幂等物化 + 响应合并三件套落地,groupKey 词表与 Phase 48 同源,机器组永不触碰用户组。</done>
</task>

<task type="auto">
  <name>Task 2: load-v2 全量加载钩子 + verify S2 填充</name>
  <files>src/routes/canvas/v2/load-v2.ts, scripts/verify-phase-53.ts</files>
  <read_first>src/routes/canvas/v2/load-v2.ts (全文——since 分支/空图分支/响应构造点), src/lib/candidateGroupDeriver.ts (Task 1 产物签名), scripts/verify-phase-53.ts (53-01 骨架:S2 占位标记与 assert 收集器结构), scripts/verify-phase-51.ts (L216-311 真模块 :memory: 集成段范式 + L201-210 import-order guard), scripts/verify-phase-49.ts 头部(端点 dispatch 派生子进程范式,STATE 决策在案)</read_first>
  <action>
  1. load-v2.ts:在 `loadFullGraph` 返回非 null 且 `since === undefined` 的全量分支,响应前插入 best-effort 钩子(整段 try/catch,失败 console.warn `[load-v2] 候选组推导失败(不影响加载):` 后照常返回原 graph):
     `const derived = deriveCandidateGroups(graph.nodes)` → `await materializeCandidateGroups(db, {projectId, episodesId}, derived.groups)` → `graph.variantGroups = mergeDerivedGroups(graph.variantGroups, derived)`。`since` 路径与 graph===null 路径零改动。
  2. verify-phase-53.ts 替换 `FILLED-BY-53-03` 占位为 S2 live 段(:memory: knex + 真模块):
     a. **推导**:seed 节点集 = 53-01 fixture candidates-legacy.json 的 a-flf 三节点 + 构造 2 个 `_v{N}` 命名通道节点(如 /oss/kmc/P04/charA_v1.png、charA_v2.png + canonical charA.png)→ deriveCandidateGroups 断言:产出 `shot:shot_012:first`(2 成员)/`shot:shot_012:last`(1 成员→被丢,skipped 含 single-member)/`name:kmc/P04/charA`(3 成员);c-p01 节点不产出组(groupKey 空串);无组信号不 throw。
     b. **首尾两键**:同 shot 的 first/last 是两个不同 groupKey(Pitfall 6 断言)。
     c. **物化幂等**::memory: 建表(照 initDB builder 或手建同列)→ materialize 第一次 created=2 → 手动 UPDATE 设 winner → 第二次 materialize created=0 且 winner 保留 → 第三次改一个成员节点集再 materialize → variant_node_ids 更新而 winner 仍保留(成员自愈语义)。
     d. **用户组保护**:预插一行非 cand: 前缀同 id 组 → materialize 后该行未被修改。
     e. **词表一致**:grepSource 断言 candidateGroupDeriver.ts 含 `shot:` 与 `name:` 前缀格式且不含第三种自造格式(如 `frame:`/`flf:` 前缀,grep 0 命中)。
     f. **端点集成**(spawn 子进程 dispatch,49-01 范式):seed :memory: canvas_nodes(a-flf 节点)→ POST load-v2 → 响应 graph.variantGroups 含 `cand:shot:shot_012:first` 组;再次 POST 幂等(组数不翻倍)。
  3. `npm run verify:phase-53` 跑到 exit 0(S1+S2 绿)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npm run verify:phase-53 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:phase-53` exit 0,S2 六组断言(推导/首尾两键/幂等/用户组保护/词表/端点集成)全绿
    - `grep -c 'FILLED-BY-53-03' scripts/verify-phase-53.ts` = 0(占位已替换)
    - load-v2.ts 含 `deriveCandidateGroups` 与 `materializeCandidateGroups` 调用各 ≥1,且均在 try/catch 内(grep '候选组推导失败' ≥1)
    - `grep -c 'since' src/routes/canvas/v2/load-v2.ts` ≥ 1(增量分支未被破坏;since 路径不触发推导——S2f 或代码断言佐证)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>kmc 候选组在图加载时自动物化,墙/端点/广播的操作单元就位;S2 契约门锁死幂等与词表一致。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 同步 node data → 组推导 → canvas_variant_groups 写入 | 产线数据经推导落库,新增服务端写入面 |
| load-v2 响应 | 物化结果并入公开加载响应 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-03-01 | Tampering | 推导写覆盖用户手工组 | mitigate | 只写 `cand:` 前缀行;非 cand: 同 id 行跳过 + warning;S2d 断言锁死 |
| T-53-03-02 | Elevation | 跨 episode/项目组污染 | mitigate | 全部写入带 scope(project_id, episodes_id)复合条件(列主键即三联合);S2f 端点 scope 断言 |
| T-53-03-03 | DoS | 巨量节点推导拖慢加载 | mitigate | 推导为单遍 O(n) 聚合;整体 try/catch 降级 warn,响应不失败;full-load 才触发 |
| T-53-03-04 | Tampering | SQL 注入(组 id/成员 ids 进 SQL) | mitigate | 参数化 SQL(upsertVariantGroup raw 参数形式),零字符串拼接 |
| T-53-03-05 | Tampering | 组 id 截断碰撞 | mitigate | id >128 记 skipped 拒绝,绝不截断(Task 1 共同规则) |
| T-53-03-SC | Tampering | package installs | accept | 零新包 |

**结论:新增服务端写入面但全部走既有参数化/事务/scope 纪律;用户组保护与幂等由 S2 断言强制。无 high 级未缓解威胁。**
</threat_model>

<verification>
- `npm run verify:phase-53` exit 0(S1+S2)
- `npx tsc --noEmit` exit 0
- load-v2 since 路径与空图路径行为不变(代码断言 + S2f)
</verification>

<success_criteria>
- Critical Gap 解除:kmc 候选(今日可推导的 a-flf 条件帧 + `_v{N}` 命名族)在加载后成为 canvas variantGroups
- VAR-01 kap 半部补齐组推导;VAR-03 的操作单元(组)就绪
- 推荐触发时机采纳 load-time + 确定性幂等 id(53-RESEARCH Open Question 3 裁定),与 khs 重同步互不干扰
</success_criteria>

<output>
Create `.planning/phases/53-variant-contract-picker-upgrade/53-03-SUMMARY.md` when done. 记录:命名通道 canonical 兄弟判定细节、winner 保留语义、load-time 触发裁定理由。
</output>
