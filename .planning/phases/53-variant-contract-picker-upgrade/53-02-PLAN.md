---
phase: 53-variant-contract-picker-upgrade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/infinite-canvas/src/components/variants/wallTransport.ts
  - packages/infinite-canvas/src/components/variants/__tests__/wallTransport.test.ts
  - packages/infinite-canvas/src/components/variants/useWallKeyboard.ts
  - packages/infinite-canvas/src/components/variants/__tests__/useWallKeyboard.test.ts
  - packages/infinite-canvas/src/components/variants/healThumb.ts
  - packages/infinite-canvas/src/components/variants/__tests__/healThumb.test.ts
  - packages/infinite-canvas/src/components/variants/VariantWall.tsx
  - packages/infinite-canvas/src/components/variants/variantPickerStore.ts
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
autonomous: true
requirements: [VAR-02]
must_haves:
  truths:
    - "变体墙 = 全屏审片剧场:全屏 overlay 暗色剧场、上方 N-up 大屏视频墙、底部候选胶片条 + 详情行(D-05,CONTEXT ASCII 版式)"
    - "一条主控 transport(播放/暂停/拖动)驱动所有 take;rAF 主时钟为唯一真值,漂移 >120ms 硬 seek(D-06/DR-5)"
    - "音频 solo:非 solo 卡全部 muted=true,点击卡切换 solo(D-06)"
    - "信息密度分层:卡上 = 缩略/视频 + aiScore 徽章 + 时长 + seed + prompt 单行截断;完整 prompt 在检视卡详情区展开(D-07)"
    - "点卡 = 检视(展开详情/设 solo);每卡显式「选定」按钮才提交(D-08);选定走既有 canvasStore.selectWinner v3 optimistic 路径"
    - "键盘流:1-9 检视、Enter 选定、←/→ 切组、空格 同播、Esc 关闭(D-20;1-9=检视 非 选定,与 D-08 显式选定语义一致——planner 裁定)"
    - "缩略图 404 三段自愈:onError → 一次性 POST /api/canvas/v2/thumbnail(sourcePath 仅 /oss/ 形态)→ 换 URL;失败/无 filePath → 模态 emoji 占位(DR-3)"
    - "所有媒体 URL 经 resolveMediaUrl 解析,绝不手拼(P5)"
    - "旧 VariantPicker 的 open/close 协议保留:牌堆 onStackToggle → variantPickerStore.open 仍开墙(墙取代 Picker 主体);Esc 习惯保留"
  artifacts:
    - path: "packages/infinite-canvas/src/components/variants/wallTransport.ts"
      provides: "createMasterTransport 纯逻辑(rAF 主时钟/120ms 硬 seek/min-duration span/solo mute/stall 对齐),HTMLVideoElementLike 注入接口"
      contains: "createMasterTransport"
    - path: "packages/infinite-canvas/src/components/variants/useWallKeyboard.ts"
      provides: "D-20 键盘流 hook(open 态门控 + cleanup)"
      contains: "useWallKeyboard"
    - path: "packages/infinite-canvas/src/components/variants/healThumb.ts"
      provides: "createThumbHealer(fetch 注入)三段自愈纯逻辑:单次触发保护 + /oss/ 白名单 + _thumbs URL 切换 + 占位回退(never-throws)"
      contains: "createThumbHealer"
    - path: "packages/infinite-canvas/src/components/variants/VariantWall.tsx"
      provides: "全屏剧场组件:N-up 墙 + 胶片条 + 详情行 + 显式选定 + 三段自愈"
      contains: "VariantWall"
    - path: "packages/infinite-canvas/src/components/variants/variantPickerStore.ts"
      provides: "新增 wall:{groupId} 态 + openWallByGroup;既有 open/openPicker 协议不动"
      contains: "openWallByGroup"
  key_links:
    - from: "packages/infinite-canvas/src/components/FlowCanvas.tsx"
      to: "packages/infinite-canvas/src/components/variants/VariantWall.tsx"
      via: "<VariantPicker /> 挂载位替换为 <VariantWall />(L1008)"
      pattern: "<VariantWall"
    - from: "packages/infinite-canvas/src/components/variants/VariantWall.tsx"
      to: "packages/infinite-canvas/src/utils/mediaUrl.ts"
      via: "resolveMediaUrl 解析全部缩略/视频 URL"
      pattern: "resolveMediaUrl"
    - from: "packages/infinite-canvas/src/components/variants/VariantWall.tsx"
      to: "packages/infinite-canvas/src/components/variants/healThumb.ts"
      via: "img onError → healer.heal(candidate) → healed 换 URL / placeholder emoji 占位"
      pattern: "createThumbHealer"
    - from: "packages/infinite-canvas/src/components/variants/VariantWall.tsx"
      to: "packages/infinite-canvas/src/store/canvasStore.ts"
      via: "选定按钮 → 既有 selectWinner(nodeId) v3 optimistic 路径 + 成功后 triggerStaleCascade"
      pattern: "selectWinner"
---

<objective>
建 VAR-02 变体墙的四件引擎:① `wallTransport.ts` 纯逻辑同播主时钟(rAF 主时钟 + 漂移硬校正 + solo 静音,DR-5,可 vitest 假 video 单测);② `useWallKeyboard.ts` D-20 键盘流;③ `healThumb.ts` 缩略图三段自愈纯模块(fetch 注入,vitest 锁死单次触发保护与 /oss/ 白名单——T-53-02-01/02 行为相关缓解);④ `VariantWall.tsx` 全屏审片剧场组件(替换 FlowCanvas 里 `<VariantPicker />` 挂载,协议保留)+ variantPickerStore 扩展 wall 态。

选定交互本 plan 即接通既有 `canvasStore.selectWinner` v3 路径(检视+显式选定,D-08);frameSlot/下一镜串行(D-11/D-17/D-18)归 53-05,入口扩展与旧 Picker 删除(D-12/D-19)归 53-06。

输出:四个新文件(wallTransport/useWallKeyboard/healThumb/VariantWall)+ 三个测试文件 + store 扩展 + FlowCanvas 挂载替换。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/53-variant-contract-picker-upgrade/53-CONTEXT.md
@.planning/phases/53-variant-contract-picker-upgrade/53-RESEARCH.md
@.planning/phases/53-variant-contract-picker-upgrade/53-PATTERNS.md
@.planning/phases/53-variant-contract-picker-upgrade/53-VALIDATION.md

**COORD-01/D-01:本 plan 零 khs 文件、零 root 后端文件。执行者不得修改 files_modified 之外的文件。**

<design_decisions>
<!-- 前端设计纪律(/frontend-design):token/布局/签名元素由 planner 定死,执行者不再决策 -->

**Token 层(全部既有 catppuccin,禁止新造色值):**
- 剧场暗底:`theme.chrome.lightboxOverlay`(catppuccin.ts L196,rgba(0,0,0,0.85))
- 面板/胶片条/头栏:`theme.bg.panel` + `theme.border.default`;卡片底 `theme.bg.card`
- winner/选定强调:`v3theme.signal.select`(冷白 #EDEEF1)——唯一强调色,已选定态填充
- aiScore 徽章:`getScoreColor(overall)`(L244-248 既有阈值:≥0.8 青 / ≥0.5 金 / 其余玫),显示 Math.round(overall×100) 整数;无 score 显示 verdict/状态弱文本,不显示 0 分(DR-1)
- 维度 chips:`v3theme.modalityWeak` 弱底 + 模态色文字(DR-1,只展示不自算加权)
- phase 徽章(墙头栏):`v3theme.phaseGroup` 四色映射

**布局(CONTEXT <specifics> ASCII 版式,用户选定):**
- 头栏:`{组名/shot 标签} 选片 · {shotId}` + [同播 ▶/⏸] + [下一镜 →](本 plan 渲染但禁用,53-05 接通)+ ✕
- 上部 N-up 墙:grid `repeat(auto-fit, minmax(320px, 1fr))` gap 12px;每格 = video + 该 take playhead 进度条 + take 标签 + 时长 + 徽章;一条**共享主 playhead** 横贯 N-up 区下方(签名元素)
- 底部胶片条:横向滚动 160px 卡(缩略/emoji 占位 + 徽章 + seed mono + prompt 单行 ellipsis)+ 检视详情行(完整 prompt + seed + 维度 chips)

**签名元素(唯一):** 跨变体同步走带——一条主控时间轴驱动所有 take,所有进度条镜像主位置。其余一切安静克制(反模板:审片工作台不是 dashboard)。

**交互语义(D-06/D-08/D-20,planner 终裁):**
- 点卡/video 格 = 检视(展开详情 + 设 solo 声);「选定」按钮 = 显式提交,提交后墙不关(D-17 下一镜归 53-05)
- 键盘:1-9 检视第 N take(非选定)、Enter 选定当前检视 take、←/→ 切组(本 plan 渲染回调占位)、空格 同播 播/停、Esc 关闭
- UI copy 中文审片语:检视/选定/下一镜/同播/solo 声/已选定

**zod 纪律(P8):** 包内不 import root schema;WallCandidate 为手写 TS interface(字段:nodeId/label/variantId/frameSlot?/selected/score?{overall,dimensions?}/durationSec/prompt?/seed?/thumbnailUrl?/filePath?/verdict?)
</design_decisions>

<interfaces>
<!-- 提取自 53-PATTERNS,执行者直接取用 -->

variantPickerStore.ts 现状(L10-20):`{ open: { nodeId, stack: VariantStackData } | null, openPicker(nodeId, stack), close() }`——新增 wall 态不改既有字段;FlowCanvas 挂载 `<VariantPicker />` 于 L1008(与 NodeDetailPanel 同级 overlay 区)。

Esc 处理范式(VariantPicker.tsx L25-30):useEffect 内 window.addEventListener('keydown'),open 态门控,return cleanup 移除(P9)。

模态 emoji 占位(VariantPicker.tsx L108-111):video '🎬' / audio '🎵' / 其余 '🖼',fontSize 22 opacity 0.4——DR-3 第三段兜底照抄。

video 元素先例(AssetCardNode.tsx L343-344):`<video muted loop ...>`,src = resolveMediaUrl(asset?.media.proxy ?? asset?.media.original ?? data.filePath)(L272)。

组数据通道(variantOps.ts):`findVariantGroupForNode`(L13)/`getVariantMemberNodes(nodes, group)`(L27-34)——墙从 graph.variantGroups + 成员节点取数,**绝不走 VariantStackData**(deprecated-only 物化,53-RESEARCH Critical Gap)。

选定通道(canvasStore.ts L889-929):`selectWinner(nodeId)` v3 路径已 optimistic+POST+回滚;墙只改"谁调用"(显式按钮);成功后 `triggerStaleCascade([nodeId])`(useStale.ts L48-60,store 路径不自带级联)。

缩略图自愈后端(既有,勿改):POST /api/canvas/v2/thumbnail body {sourcePath},幂等,响应 data.thumbnailUrl 含 /_thumbs/ 即成功(DR-3 三段:onError 一次性 POST → 换 URL → 占位兜底;不做批量预检)。

同播算法(DR-5,已锁定):masterTime = rAF delta 累加,不回读 video.currentTime;每 tick 每 video:muted = i!==soloIdx;|v.currentTime - target| > 0.12 → v.currentTime = target(硬 seek,不用 playbackRate);span = min(所有 take duration),target = masterTime % span;seek(t) 统一 forEach 赋值;任一 video 'waiting' → 全场 pause + masterTime 对齐该 video.currentTime。duration 读 data.duration_sec,fallback media.durationS。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: wallTransport.ts 同播主时钟纯逻辑 + vitest</name>
  <files>packages/infinite-canvas/src/components/variants/wallTransport.ts, packages/infinite-canvas/src/components/variants/__tests__/wallTransport.test.ts</files>
  <read_first>packages/infinite-canvas/src/store/variantOps.ts (纯函数纪律模范:无 React import、返回自足 outcome 对象), .planning/phases/53-variant-contract-picker-upgrade/53-RESEARCH.md (DR-5 全节 + Code Examples 同播主时钟段——算法逐行照此), packages/infinite-canvas/src/vitest.config.ts 或 vitest.config.* (确认测试发现范围)</read_first>
  <behavior>
  - Test: play 后逐帧 tick,masterTime 按 delta 累加,每个 video currentTime 被驱动到 masterTime % span
  - Test: 某 video currentTime 偏差 >120ms → 被硬 seek 到 target;偏差 ≤120ms → 不被赋值
  - Test: solo 模式下只有 soloIdx 的 video muted=false,其余全 muted=true;setSolo 切换即时生效
  - Test: span = min(duration);masterTime 超过 span 后回绕(target 取模)
  - Test: seek(t) 设置 masterTime=t 且所有 video currentTime=t
  - Test: stall——任一 video 触发 'waiting' 回调 → 全场 pause 且 masterTime 对齐该 video currentTime
  - Test: pause 后 tick 不再推进 masterTime
  </behavior>
  <action>
  1. 新建 `wallTransport.ts`:零 React import,导出 `HTMLVideoElementLike` 接口(currentTime/muted/paused/duration/play()/pause()/addEventListener)与 `createMasterTransport(videos: HTMLVideoElementLike[], opts?: {driftHard?: number})`,算法照 interfaces 段 DR-5 锁定版(rAF 主时钟/120ms 硬 seek/min-span 回绕/solo mute/stall 对齐);返回 {play, pause, seek, setSolo, get masterTime, attach(el)/detach(el)}——attach/detach 供 React 层动态注册 video 元素,transport 内部持数组。
  2. rAF 循环由 transport 自持(requestAnimationFrame;无 video 或 paused 时跳过计算);`dispose()` 取消 rAF。
  3. `wallTransport.test.ts` 用 fake video 对象(记录 currentTime 赋值)与手动调用内部 tick(导出 `tickForTest(now, last)` 或注入 clock)驱动上述 7 组断言。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/components/variants/__tests__/wallTransport.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/variants/__tests__/wallTransport.test.ts` exit 0,覆盖 <behavior> 7 组断言
    - `grep -c 'react' packages/infinite-canvas/src/components/variants/wallTransport.ts` = 0(纯模块)
    - wallTransport.ts 含字面 `0.12` 与 `muted`(DR-5 常量与 solo 语义)
    - `cd packages/infinite-canvas && npx tsc -b` exit 0
  </acceptance_criteria>
  <done>同播主时钟纯逻辑可单测落地:漂移硬校正/solo 静音/min-span 回绕/stall 对齐全部有测试锁死。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useWallKeyboard.ts 键盘流 + vitest</name>
  <files>packages/infinite-canvas/src/components/variants/useWallKeyboard.ts, packages/infinite-canvas/src/components/variants/__tests__/useWallKeyboard.test.ts</files>
  <read_first>packages/infinite-canvas/src/components/variants/VariantPicker.tsx (L25-30 Esc 键处理范式——useEffect + window keydown + open 门控 + cleanup), packages/infinite-canvas/src/components/variants/__tests__/(若无则看 src/store/__tests__/selectWinner.test.ts 的 vitest 风格)</read_first>
  <behavior>
  - Test: 数字键 '1'..'9' → onInspect(index) 回调,index = key-1
  - Test: 'Enter' → onConfirmSelection();'Escape' → onClose()
  - Test: 'ArrowRight' → onNextGroup();'ArrowLeft' → onPrevGroup()
  - Test: ' '(Space) → onTogglePlay(),且 preventDefault(防页面滚动)
  - Test: enabled=false 时任意键不触发回调;卸载后 window listener 移除
  </behavior>
  <action>
  1. 新建 `useWallKeyboard(enabled: boolean, handlers: { onInspect, onConfirmSelection, onNextGroup, onPrevGroup, onTogglePlay, onClose })`:照 VariantPicker Esc 范式——useEffect 内 window.addEventListener('keydown'),enabled 门控,cleanup 移除(P9);数字键仅当 index < maxTakes 时触发(handler 侧容许越界忽略);Space/Enter preventDefault。纯 hook 无业务状态。
  2. 测试用例照 <behavior> 五组,直接 window.dispatchEvent(new KeyboardEvent('keydown', {key:'3'})) 驱动。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/components/variants/__tests__/useWallKeyboard.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/variants/__tests__/useWallKeyboard.test.ts` exit 0,五组断言齐
    - `grep -c 'removeEventListener' packages/infinite-canvas/src/components/variants/useWallKeyboard.ts` ≥ 1(cleanup)
    - `cd packages/infinite-canvas && npx tsc -b` exit 0
  </acceptance_criteria>
  <done>D-20 键盘流(1-9 检视/Enter 选定/←→ 切组/空格 同播/Esc 关)成 hook,键映射有测试锁死。</done>
</task>

<task type="auto">
  <name>Task 3: VariantWall.tsx 全屏剧场 + healThumb 自愈模块 + store 扩展 + FlowCanvas 挂载</name>
  <files>packages/infinite-canvas/src/components/variants/healThumb.ts, packages/infinite-canvas/src/components/variants/__tests__/healThumb.test.ts, packages/infinite-canvas/src/components/variants/VariantWall.tsx, packages/infinite-canvas/src/components/variants/variantPickerStore.ts, packages/infinite-canvas/src/components/FlowCanvas.tsx</files>
  <read_first>packages/infinite-canvas/src/components/variants/VariantPicker.tsx (全文 137 行——Esc/backdrop/emoji 占位/closeBtnStyle 要抄;handlePick L41-50 与 「💾 保存」footer L127-128 是 D-12 废弃叙事,绝不复制), packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx (L98-118 安静面板 chrome 范式), packages/infinite-canvas/src/store/variantOps.ts (findVariantGroupForNode L13 + getVariantMemberNodes L27-34), packages/infinite-canvas/src/store/canvasStore.ts (selectWinner v3 路径 L889-929), packages/infinite-canvas/src/hooks/useStale.ts (triggerStaleCascade L48-60), packages/infinite-canvas/src/utils/mediaUrl.ts (resolveMediaUrl L89), packages/infinite-canvas/src/theme/catppuccin.ts (getScoreColor L244-248 / signal L75-80 / modalityWeak L51-56 / chrome.lightboxOverlay L196), packages/infinite-canvas/src/components/nodes/AssetCardNode.tsx (L272/L343-344 video src 与 muted loop 先例), packages/infinite-canvas/src/components/FlowCanvas.tsx (L1008 挂载位上下文)</read_first>
  <action>
  1. variantPickerStore.ts 扩展(既有 open/openPicker/close 不动):新增 `wall: { groupId: string } | null` + `openWallByGroup(groupId: string)` + `close()` 同时清 open 与 wall。
  2. FlowCanvas.tsx L1008 `<VariantPicker />` 替换为 `<VariantWall />`(import 更新;VariantPicker.tsx 文件本 plan 不删,53-06 清除)。
  3. 新建 VariantWall.tsx 按 <design_decisions> 定死的 token/布局/签名元素实现:
     - 开态:`useVariantPickerStore` 的 `open !== null`(open.nodeId → useCanvasStore graph + findVariantGroupForNode 解析组;解析不到显示「未找到变体组」空态)或 `wall !== null`(openWallByGroup 直取组)。取组后 `getVariantMemberNodes` 得成员,映射为手写 `WallCandidate[]` interface(见 design_decisions;score 读 node.data.aiScore 的 {overall,dimensions} 形状,无则 undefined;durationSec 读 data.duration_sec fallback media.durationS)。
     - 剧场壳:position fixed inset-0,背景 `theme.chrome.lightboxOverlay`,z 高于画布低于 Toast;头栏/胶片条 `theme.bg.panel` + `theme.border.default`。
     - N-up 墙:grid auto-fit minmax(320px,1fr);每格 `<video muted loop playsInline>` src=resolveMediaUrl(...);video ref 注册进 createMasterTransport.attach;点格 = 检视+solo;格内下方该 take 的 playhead 进度条(镜像 masterTime/span);N-up 区下方一条共享主 playhead 可拖(seek)——签名元素,其余视觉安静。
     - 头栏控件:[同播 ▶/⏸](transport play/pause)、[下一镜 →](渲染 disabled,53-05 接通)、✕(closeBtnStyle 范式)。
     - 胶片条:160px 卡 = 缩略图(resolveMediaUrl;onError 走 4)+ `getScoreColor` 徽章(Math.round(overall×100);无 score 显示 verdict/状态弱文本)+ seed mono + prompt 单行 ellipsis;检视详情行 = 检视卡完整 prompt + seed + dimensions chips(modalityWeak 底)。
     - 每卡显式「选定」按钮(v3theme.signal.select 描边;已选定的卡显示填充「已选定」):调 `useCanvasStore.selectWinner(nodeId)`,成功后 `triggerStaleCascade([nodeId])`;墙不自动关(D-17)。
     - useWallKeyboard 接线:onInspect=检视+solo、onConfirmSelection=选定当前检视卡、onTogglePlay=transport、onNextGroup/onPrevGroup=占位 no-op(53-05)、onClose=close。
     - Esc 与 backdrop 点击关闭照 VariantPicker 范式。
  4. 缩略图三段自愈(DR-3)抽成独立纯模块 `healThumb.ts`(wallTransport.ts 同款纯模块纪律,fetch 注入可单测——单次触发保护与 /oss/ 白名单是 T-53-02-01/02 行为相关缓解,必须有测试,不能只靠 grep):
     - 导出 `createThumbHealer(fetchImpl: typeof fetch = globalThis.fetch)`,返回 `heal(candidate: {nodeId: string; filePath?: string}): Promise<{kind: 'healed'; url: string} | {kind: 'placeholder'}>`:内部按 nodeId 记单次触发标记(每卡至多一次 POST,DR-3 裁定);仅 filePath 以 `/oss/` 开头才 POST `/api/canvas/v2/thumbnail` body {sourcePath: filePath}(白名单,其余零请求直接 placeholder);响应 data.thumbnailUrl 含 `/_thumbs/` 才返回 healed 换 URL;否则 placeholder;never-throws(fetch 任何异常按 placeholder 兜底)。
     - VariantWall 胶片条卡消费:`<img onError>` → healer.heal(candidate) → healed 用新 URL 重渲;placeholder 走模态 emoji 占位(VariantPicker L108-111 范式:🎬/🎵/🖼)。不做批量预检。
  5. 新建 `healThumb.test.ts`(vitest,注入 fake fetch 记录调用),四组断言:
     - ①单次触发保护:同一卡第二次 heal 不再发 fetch(fake fetch 调用计数不增)且直接 placeholder;
     - ②仅 /oss/:filePath 非 /oss/ 前缀(如 `http://` 外链或相对路径)零 fetch、直接 placeholder(T-53-02-01 路径穿越缓解);
     - ③_thumbs URL 切换:fake fetch 返回 {data:{thumbnailUrl:'/oss/kmc/ep01/_thumbs/shot_012_first_v1.png'}} → 返回 {kind:'healed'} 且 url 已切换;
     - ④占位回退:fetch reject、或 thumbnailUrl 不含 '/_thumbs/' → 返回 {kind:'placeholder'}。
  6. 组件卸载时 transport.dispose() + 全部 video detach。
  7. P10 注记(零代码):墙不做自己的 socket 订阅——选定广播的 echo 由 graph store 既有 variant:selected 守卫吸收,墙渲染随 graph 数据流自然更新;本 plan 零 socket 代码,仅在此记录判定。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/components/variants/__tests__/healThumb.test.ts 2>&1 | tail -4 && npx tsc -b 2>&1 | tail -3 && npm test 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<VariantPicker' packages/infinite-canvas/src/components/FlowCanvas.tsx` = 0 且 `grep -c '<VariantWall' packages/infinite-canvas/src/components/FlowCanvas.tsx` ≥ 1
    - `grep -c 'openWallByGroup' packages/infinite-canvas/src/components/variants/variantPickerStore.ts` ≥ 2(声明+实现)
    - VariantWall.tsx 含 `resolveMediaUrl`、`getScoreColor`、`lightboxOverlay`、`createThumbHealer`(token 与自愈接线);healThumb.ts 含 `/canvas/v2/thumbnail` 与 `/_thumbs/`(自愈通道)
    - VariantWall.tsx 不含 `💾` 与 `variantStack` 读取(反双轨叙事 + 不走 deprecated-only 通道;grep 0 命中)
    - `cd packages/infinite-canvas && npx vitest run src/components/variants/__tests__/healThumb.test.ts` exit 0,四组断言齐:单次触发保护(第二次 heal fetch 计数不增)/ 仅 /oss/(非 /oss/ 前缀零 fetch)/ _thumbs URL 切换 / 占位回退
    - `grep -c 'react' packages/infinite-canvas/src/components/variants/healThumb.ts` = 0(纯模块,fetch 注入)
    - `cd packages/infinite-canvas && npx tsc -b` exit 0;`npm test` 全绿(既有 selectWinner.test.ts 等无回归)
  </acceptance_criteria>
  <done>全屏剧场落地:同播走带/检视+显式选定/键盘流/三段自愈(自愈四行为有 vitest 锁死)全部就位,旧 Picker 挂载位被墙取代而 store 协议保留。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 同步 node data → 墙渲染(缩略/视频 URL) | 产线写入的路径/URL,展示层消费 |
| 墙 → POST /api/canvas/v2/thumbnail(sourcePath) | 自愈回传路径,后端幂等端点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-02-01 | Tampering/Elevation | 自愈 sourcePath 路径穿越(V12) | mitigate | 仅 filePath /oss/ 前缀才发 POST(端点内 needsThumbnailing 前置白名单既有);非 /oss/ 直接占位,不发请求;healThumb.test.ts ② 断言锁死 |
| T-53-02-02 | DoS | onError 自愈循环打爆后端 | mitigate | healTried 一次性标记,每卡至多一次 POST(DR-3 显式裁定);healThumb.test.ts ① 断言锁死 |
| T-53-02-03 | Tampering | XSS 经 prompt/thumbnailUrl 注入 | mitigate | React 文本节点渲染(无 dangerouslySetInnerHTML);URL 只进 img/video src |
| T-53-02-04 | Denial of Service | 同播 rAF 空转/泄漏 | mitigate | open=false 或卸载即 dispose + detach;无 video 时 tick 跳过计算 |
| T-53-02-SC | Tampering | package installs | accept | 零新包 |

**结论:纯展示层 + 既有幂等端点;最高风险是自愈路径穿越与循环,均已在任务内定死缓解且由 healThumb.test.ts 四组断言强制。无 high 级未缓解威胁。**
</threat_model>

<verification>
- `cd packages/infinite-canvas && npx vitest run src/components/variants` exit 0(三个新测试文件:wallTransport/useWallKeyboard/healThumb)
- `cd packages/infinite-canvas && npm test` 全绿 + `npx tsc -b` exit 0
- `grep -c '<VariantWall' packages/infinite-canvas/src/components/FlowCanvas.tsx` ≥ 1
- 同播观感/键盘手感属 HUMAN-UAT(53-VALIDATION Manual-Only 表),不设自动化断言
</verification>

<success_criteria>
- VAR-02 核心体验成立:全屏剧场 + N-up 同播 + 胶片条 + 检视/显式选定 + 键盘流 + 404 自愈(自愈单次触发/白名单行为有测试)
- D-05/D-06/D-07/D-08/D-20 落地;aiScore 口径按 DR-1(overall 徽章 + dimensions chips,前端不自算加权)
- frameSlot/下一镜(53-05)、入口扩展与双轨清除(53-06)留待后续 plan,本 plan 不越界
</success_criteria>

<output>
Create `.planning/phases/53-variant-contract-picker-upgrade/53-02-SUMMARY.md` when done. 记录:WallCandidate view-model 字段表、solo 切换交互裁定、自愈触发条件(/oss/ only)与 healThumb 四断言结果、P10 echo-guard 继承注记。
</output>
