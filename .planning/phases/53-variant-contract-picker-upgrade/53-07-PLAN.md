---
phase: 53-variant-contract-picker-upgrade
plan: 07
type: execute
wave: 4
depends_on: ["53-04", "53-05"]
files_modified:
  - src/lib/g15Bridge.ts
  - src/routes/canvas/v2/g15-ops.ts
  - src/router.ts
  - packages/infinite-canvas/src/components/g15/G15TriagePanel.tsx
  - packages/infinite-canvas/src/components/g15/g15TriageStore.ts
  - packages/infinite-canvas/src/components/g15/__tests__/G15TriagePanel.test.tsx
  - packages/infinite-canvas/src/components/FlowCanvas.tsx
  - scripts/verify-phase-53.ts
autonomous: true
requirements: [VAR-04]
must_haves:
  truths:
    - "G15 分诊面板 = 独立工作台:勾选行 + phase 徽章 + 错误类别徽章 + 原因截断(展开看 take_log 条目 + 原始日志),底部动作条「已选 N + 批量豁免 + 批量重渲」+ 二次确认(D-13/D-14/D-16)"
    - "批量重渲有二次确认且确认层显示计数(贵操作防误触);确认层为组件内 state,绝不用原生 confirm()(D-14)"
    - "G15 操作桥 = reviewBridge 同构独立端点:waive(豁免)走 approve-with-comment 扩展语义、requeue(重渲)为冻结新 action;deps 注入 + never-throws + fail-closed 三维匹配(D-15)"
    - "桥投递失败 → canvas_writeback_queue 入队重放(action g15_waive/g15_requeue,53-04 队列复用);批量操作 optimistic + 失败回滚 + toast(AssetLibrary handleSelect 同构)"
    - "g15-ops 端点 zod:action enum(waive|requeue)+ shotIds 数组每项 min(1).max(128) 且数组 .max(200)(V5 bound)"
    - "真实重渲执行不在断言范围:requeue = 指令送达语义(CONTEXT specifics 口径);Wave A 面板数据 = 契约 fixture 驱动 + Wave B 数据源 seam(今日 take-log/failed-shots 无画布消费者)"
  artifacts:
    - path: "src/lib/g15Bridge.ts"
      provides: "dispatchG15Op(params, deps): Promise<{delivered, reason?}>(never-throws)+ fail-closed 匹配"
      contains: "dispatchG15Op"
    - path: "src/routes/canvas/v2/g15-ops.ts"
      provides: "POST /api/canvas/v2/g15-ops(waive|requeue, shotIds ≤200, 队列降级, g15:ops 广播)"
      contains: "g15-ops"
    - path: "packages/infinite-canvas/src/components/g15/G15TriagePanel.tsx"
      provides: "分诊面板组件(行/展开/动作条/二次确认状态机)"
      contains: "G15TriagePanel"
    - path: "packages/infinite-canvas/src/components/g15/g15TriageStore.ts"
      provides: "开关态 + 行选择状态 + fixture 装载器 + G15Source seam(Wave B 换真实端点)"
      contains: "G15Source"
  key_links:
    - from: "src/router.ts"
      to: "src/routes/canvas/v2/g15-ops.ts"
      via: "import route168 + app.use('/api/canvas/v2/g15-ops', route168)"
      pattern: "g15-ops"
    - from: "packages/infinite-canvas/src/components/g15/G15TriagePanel.tsx"
      to: "packages/infinite-canvas/src/services/canvasApi.ts"
      via: "批量操作 → canvasApi.g15Ops(pid, eid, action, shotIds)(53-05 预置)"
      pattern: "g15Ops"
    - from: "src/routes/canvas/v2/g15-ops.ts"
      to: "src/lib/writebackQueue.ts"
      via: "delivered=false → enqueueWriteback(action g15_waive|g15_requeue)"
      pattern: "enqueueWriteback"
    - from: "scripts/verify-phase-53.ts"
      to: "src/lib/g15Bridge.ts + packages/.../variants/*"
      via: "S4 墙源形状 grepSource + S5 g15 桥断言 + forced-failure(替换 FILLED-BY-53-07)"
      pattern: "S5"
---

<objective>
建 VAR-04 全套:① `g15Bridge.ts`(reviewBridge 同构:waive = approve-with-comment 扩展、requeue = 冻结新 action、deps 注入 + never-throws + fail-closed 三维匹配,返回 delivered 结果供队列判定);② `POST /api/canvas/v2/g15-ops` 独立端点(D-15:豁免不是选定,语义不污染)+ router 注册;③ G15 分诊面板(独立工作台,D-13;勾选+动作条+二次确认,D-14;fixture 驱动 + Wave B seam);④ verify S4/S5 填充(phase 契约门收口)。

依赖注记:本 plan 消费 53-04 产物(writebackQueue 的 enqueueWriteback/ensureDrainStarted、canvas_writeback_queue DDL 的 g15_waive/g15_requeue 枚举、S3 段后的 FILLED-BY-53-07 占位链)与 53-05 产物(canvasApi.g15Ops sibling、FlowCanvas overlay 区先例),故 depends_on 53-04+53-05(均 Wave 3,本 plan Wave 4 成立)。

Wave B 门控注记(不在本 plan 实现):take_log/failed-shots 结构化透传到画布(D-16 主数据源)、kmc 侧 requeue 指令消费端、真实 GPU 重渲执行断言 = Wave B(gated on khs2 v2.4 Phase 25 验收 — planned separately)。review-platform 本机不可达(53-RESEARCH Environment 表)——桥验证走注入 fetch + fixture,真实链路 Wave B。

输出:g15Bridge + g15-ops + 面板三件 + S4/S5 绿。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/53-variant-contract-picker-upgrade/53-CONTEXT.md
@.planning/phases/53-variant-contract-picker-upgrade/53-RESEARCH.md
@.planning/phases/53-variant-contract-picker-upgrade/53-PATTERNS.md
@.planning/phases/53-variant-contract-picker-upgrade/53-01-SUMMARY.md
@.planning/phases/53-variant-contract-picker-upgrade/53-04-SUMMARY.md
@.planning/phases/53-variant-contract-picker-upgrade/53-05-SUMMARY.md

**COORD-01/D-01:本 plan 零 khs 文件。执行者不得修改 files_modified 之外的文件。**

<design_decisions>
<!-- 前端设计纪律(/frontend-design):G15 面板 token/布局由 planner 定死;无签名元素——这是工作台 -->

**Token 层(全部既有):**
- 面板壳:`theme.bg.panel` + `theme.border.default`(NodeDetailPanel 安静 chrome);行底 `theme.bg.card`
- phase 徽章:`v3theme.phaseGroup` 四色映射(catppuccin.ts L67-73)
- 错误类别徽章:`v3theme.signal.*`——错误类(qc_vision_fail/engine_render_error/bgm_trigger)= signal.error 玫;流程类(delegate_timeout/delegate_parse/schema_validation/needs_regenerate)= signal.warn 金;take_verdict_* = 弱文本(modalityWeak 底);unknown = 弱灰
- 展开原始日志:mono 字体 + maxHeight 160px overflow auto + `theme.border.default` 分隔

**布局(CONTEXT <specifics> 用户选定版式):**
- 右侧 dock 面板宽 420px(与 NodeDetailPanel 宽度约定同族),可 Esc/✕ 关闭;头部「G15 失败镜头分诊」+ 待处置计数
- 行 = 勾选框 + shot_id(mono)+ phase 徽章 + 错误类别徽章 + 原因单行截断;点击行展开 = take_log 条目(take_n/changed_variable/seed/evidence)+ 原始 error 全文(截断滚动)
- 底部 sticky 动作条:「已选 N」+ [批量豁免] + [批量重渲];重渲点击后动作条切换为确认层「确认对 N 个镜头下发重渲?[确认重渲] [取消]」(组件内 state;豁免轻操作不需二次确认——planner 裁定:D-14 只点名重渲)

**copy 中文审片语:** 豁免/重渲/失败原因/归因/待处置/已处置。反模板:分诊工作台不是 dashboard,无图表无花哨。

**fixture 数据源(D-16 Wave A 口径):** G15Row = {shotId, phase, category(g15ErrorCategory 9 值), reason, takes?: TakeLogEntry[], rawError?: string};装载器读 53-01 fixture(take-log.json + failed-shots.json)经 classifyG15Error 的 TS 等价映射(包内手写,不跨包 import root zod,P8)生成行;`G15Source` 接口 = seam(Wave B 实现换真实端点)。
</design_decisions>

<interfaces>
<!-- 提取自 53-PATTERNS + 源码核读,执行者直接取用 -->

reviewBridge 三段复刻清单(P1/P2):header docblock 编号列出远端契约 + "DOCUMENTED PROTOCOL GAP" 冻结注(L1-63);deps 接口 {baseUrl?, fetchImpl?, logger?, timeoutMs?}(L74-79)+ never-throws try/catch 吞一切(L114-119, L249-257);fail-closed 三维匹配(L196-208:episode segment 相等 + phase token 全等,无前缀匹配——"A missed bridge is benign; a wrong approve is not")。g15Bridge 的 waiver 通道 = approve-with-comment 扩展(L12-18 已核协议:409=已 resolve 等);requeue = 新 action 无 kmc 消费端(research A3,docblock 冻结此 gap)。

g15-ops 端点模板(select-winner.ts 179 行):imports+router L1-13 / inline safeParse 400 L46-49 / 状态阶梯→HTTP L61-88 / void 桥挂点 + .catch 二 backstop / broadcastToProject(projectId, "g15:ops", {...timestamp: Date.now()}) / 500 catch `[canvas:v2/g15-ops]` 前缀。body zod:{projectId, episodesId, action: z.enum(["waive","requeue"]), shotIds: z.array(z.string().min(1).max(128)).max(200)}。

router.ts 注册位:import 区 L26-29 同族 + mount 区 L188-192;下一空闲编号 route167 已占(select-winner),本 plan 用 route168,`app.use("/api/canvas/v2/g15-ops", route168);`。

writebackQueue(53-04 产物):enqueueWriteback(db, {projectId, episodesId, action: "g15_waive"|"g15_requeue", payload})——g15 两枚举 53-04 已预留。

canvasApi.g15Ops(53-05 预置):`g15Ops(projectId, episodesId, action: 'waive'|'requeue', shotIds: string[])`。

FlowCanvas.tsx:overlay 挂载区(L1007-1010 族,`{iteration.panelOpen && <IterationPanel />}` 先例)——G15 挂 `{g15.open && <G15TriagePanel />}`;工具栏 ToolbarButton(文件内既有 helper)加「失败镜头」入口。

G15 归因 taxonomy(53-01 产物,9 值):qc_vision_fail/engine_render_error/bgm_trigger/delegate_timeout/delegate_parse/schema_validation/needs_regenerate/take_verdict_*/unknown;take verdict 五值 keep|fix_in_post|edit|re_roll|rewrite。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: g15Bridge.ts + g15-ops 端点 + router 注册</name>
  <files>src/lib/g15Bridge.ts, src/routes/canvas/v2/g15-ops.ts, src/router.ts</files>
  <read_first>src/lib/reviewBridge.ts (全文 258 行——header docblock L1-63 / deps L74-79 / never-throws L114-119+L249-257 / fail-closed 匹配 L196-208,三段逐条复刻), src/routes/canvas/v2/select-winner.ts (全文 179 行——端点模板), src/router.ts (L26-29 import 区 + L173 route167 + L188-192 mount 区), src/lib/writebackQueue.ts (53-04 产物:enqueueWriteback 签名与 action 枚举), src/lib/candidateEnvelope.ts (53-01 产物:g15ErrorCategory 类型)</read_first>
  <action>
  1. 新建 g15Bridge.ts:
     - header docblock(reviewBridge L1-63 模范):编号列出 waive(= approve-with-comment 扩展,协议已核 409=已 resolve)与 requeue(新 action,**DOCUMENTED PROTOCOL GAP**:kmc 侧消费端 Wave B 才存在,当前送达 = 指令落队)两通道契约。
     - `G15BridgeDeps` = {baseUrl?, fetchImpl?, logger?, timeoutMs?}(reviewBridge 同款,默认 baseUrl process.env.REVIEW_PLATFORM_URL || "http://review-platform:8090")。
     - `dispatchG15Op(params: {projectId, episodesId, action: "waive"|"requeue", shotIds: string[]}, deps?): Promise<{delivered: true} | {delivered: false, reason: string}>`——never-throws(吞错纪律逐条照 reviewBridge);调用前 fail-closed 三维匹配(scope 校验:episode segment 全等 + 无前缀匹配,照 L196-208);delivered=false 携带 reason(供端点入队与 last_error)。
  2. 新建 g15-ops.ts(select-winner 模板):body zod 照 interfaces 精确形状;处理流:逐 shotId(或整批一次,按桥的批量端点形状——桥对 shotIds 整批发一次)调 `dispatchG15Op`;delivered=true → 计入 applied;delivered=false → `enqueueWriteback(db, {action: action==="waive"?"g15_waive":"g15_requeue", payload: params})`(入队自身 try/catch 降级 warn,Pitfall 4);enqueueWriteback 后 `ensureDrainStarted` 系列已在 53-04 就位则复用;响应 `success({action, shotIds, applied, queued})`;`broadcastToProject(projectId, "g15:ops", {action, shotIds, timestamp: Date.now()})`;外层 500 catch `[canvas:v2/g15-ops]`。
  3. router.ts:import route168 + `app.use("/api/canvas/v2/g15-ops", route168);`(mount 区字母序位置照既有排列惯例)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npx tsc --noEmit 2>&1 | tail -3 && grep -c 'g15-ops' src/router.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export async function dispatchG15Op' src/lib/g15Bridge.ts` = 1 且函数体在 try/catch 内(grep 'catch' ≥2)
    - g15-ops.ts schema 含 `z.enum(["waive", "requeue"])` 与 `.max(200)`(V5 bound)
    - g15-ops.ts 含 `enqueueWriteback` 与 `broadcastToProject(projectId, "g15:ops"` 
    - `grep -c 'g15-ops' src/router.ts` ≥ 2(import+mount)
    - g15Bridge.ts 头部含 'DOCUMENTED PROTOCOL GAP'(requeue 冻结注)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>G15 操作桥 + 独立端点成形:waive/requeue 一桥收口、失败入队重放、广播就位、requeue gap 冻结成文。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: g15TriageStore + G15TriagePanel 组件 + vitest</name>
  <files>packages/infinite-canvas/src/components/g15/g15TriageStore.ts, packages/infinite-canvas/src/components/g15/G15TriagePanel.tsx, packages/infinite-canvas/src/components/g15/__tests__/G15TriagePanel.test.tsx</files>
  <read_first>packages/infinite-canvas/src/components/panel/NodeDetailPanel.tsx (L98-118 面板 chrome), packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx (L839-862 handleSelect optimistic+回滚+toast 范式), packages/infinite-canvas/src/components/variants/variantPickerStore.ts (zustand 开关态先例), packages/infinite-canvas/src/theme/catppuccin.ts (phaseGroup L67-73 / signal L75-80 / modalityWeak L51-56), packages/infinite-canvas/src/services/canvasApi.ts (53-05 预置的 g15Ops 签名), .planning/phases/53-variant-contract-picker-upgrade/53-RESEARCH.md (DR-6 taxonomy + fixture 口径)</read_first>
  <behavior>
  - Test: 行勾选状态机——点 3 行 → 「已选 3」;再点取消 1 行 → 「已选 2」;全选/清空
  - Test: 行展开——点击行 → take_log 条目(take_n/changed_variable/seed/evidence)与原始 error 可见;再点收起
  - Test: 批量豁免——勾 2 行点 [批量豁免] → 行状态乐观变「已豁免」 + canvasApi.g15Ops 被 mock 调用 (action='waive', shotIds=2 项);mock reject → 行回滚 + error toast
  - Test: 重渲二次确认——点 [批量重渲] 不立即调用 api,确认层出现且含计数文案「确认对 2 个镜头下发重渲」;[取消] 关确认层零调用;[确认重渲] → g15Ops(action='requeue') 调用
  - Test: shotIds >200 时前端预拦截(不发起请求,toast 提示)——防御层
  </behavior>
  <action>
  1. g15TriageStore.ts(zustand,variantPickerStore 先例):`{open, setOpen(v), rows: G15Row[], selected: Set<string>, toggle(shotId), selectAll(), clear(), rowState: Map<shotId, 'waived'|'requeued'>, load()}`;`G15Row` 手写 TS interface(包内不 import root zod,P8)照 design_decisions 字段;`G15Source` 接口 seam:`{loadRows(): Promise<G15Row[]>}`——默认实现 = fixture 装载(内联 G15Row[] 字面数据,内容对齐 53-01 take-log.json + failed-shots.json 的形状与 verdict/error 分布;Wave B 换真实端点实现,docblock 注明)。classify 的 TS 等价映射(包内手写小函数,与 53-01 classifyG15Error 同特征表:timeout/parse/schema/BGM/needsRegenerate/verdict)。
  2. G15TriagePanel.tsx 按 <design_decisions> 定死 token/布局:右侧 420px dock;行 = checkbox + shot_id mono + phase 徽章(phaseGroup 四色)+ 类别徽章(signal 分级配色)+ 原因截断;展开段;sticky 底部动作条 + 重渲确认层(组件内 state `confirming: 'requeue' | null`,绝不用原生 confirm);批量操作照 AssetLibrary handleSelect 范式:乐观 rowState 变更 → await canvasApi.g15Ops(projectId, episodesId, action, shotIds) → toast;失败回滚 rowState + toast;Esc/✕ 关面板。projectId/episodesId 取 useCanvasStore 既有上下文。
  3. 测试照 <behavior> 六组(mock canvasApi 模块,手法照 selectWinner.test.ts)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/components/g15 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/g15` exit 0,六组断言齐
    - G15TriagePanel.tsx 含字面「批量豁免」「批量重渲」「已选」(grep 各 ≥1)且 `confirm(` 0 命中(禁原生 confirm)
    - `grep -c 'g15Ops' packages/infinite-canvas/src/components/g15/G15TriagePanel.tsx` ≥ 1
    - g15TriageStore.ts 含 `G15Source`(seam 接口)与 fixture 默认实现
    - `npx tsc -b` exit 0
  </acceptance_criteria>
  <done>分诊工作台落地:勾选/归因/展开/批量/二次确认/乐观回滚全有测试,数据源 seam 为 Wave B 留位。</done>
</task>

<task type="auto">
  <name>Task 3: FlowCanvas 挂载 + 工具栏入口 + verify S4/S5 收口</name>
  <files>packages/infinite-canvas/src/components/FlowCanvas.tsx, scripts/verify-phase-53.ts</files>
  <read_first>packages/infinite-canvas/src/components/FlowCanvas.tsx (overlay 挂载区 L1007-1010 + ToolbarButton helper + 工具栏按钮排列), packages/infinite-canvas/src/components/g15/g15TriageStore.ts (Task 2 产物:setOpen 签名), scripts/verify-phase-53.ts (53-04 后骨架:FILLED-BY-53-07 占位 + S5 forced-failure 段现状), scripts/verify-phase-51.ts (L90-124 grepSource 范式 + L397-423 forced-failure)</read_first>
  <action>
  1. FlowCanvas.tsx:工具栏加「失败镜头」ToolbarButton(onClick → g15TriageStore setOpen(true),badge 可显示 rows 待处置数,数据未到时显示 0);overlay 区加 `{g15.open && <G15TriagePanel />}`(iteration.panelOpen 先例同款)。
  2. verify-phase-53.ts 替换 `FILLED-BY-53-07` 为 S4+S5:
     - **S4 墙源形状**(grepSource):VariantWall.tsx 含 resolveMediaUrl / getScoreColor / useWallKeyboard 接线 / 显式选定按钮(选定 copy);canvasStore.ts 含 frameSlot 透传且 `💾` 0 命中;VariantPicker.tsx 不存在(fs.existsSync 断言);adapter.ts 含 variantGroupIds。
     - **S5 G15 桥**(注入 fetch,真模块 import g15Bridge):fake fetch 返回 200 → delivered=true;返回 409 → 按 reviewBridge 语义视为已处理 delivered=true(或 documented reason,断言不 throw);网络 throw → delivered=false 且 reason 非空;fail-closed:scope 不匹配的 params → 不发请求(fake fetch 计数 0)delivered=false;shotIds 超 200 端点 zod 拒 400(spawn 子进程 dispatch 或直接 zod safeParse 断言)。
     - **forced-failure 收口**:53-01 的 shadow asserts 保持(至少 2 条必失败 + 意外 PASS 即整体失败);追加 1 条 S5 shadow(如断言 delivered===true 恒真式改为必假断言)。
  3. `npm run verify:phase-53` 跑到 exit 0(S1-S5 全绿)——phase 契约门收口。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && npm run verify:phase-53 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:phase-53` exit 0(S1+S2+S3+S4+S5 全绿,forced-failure 行为正确)
    - `grep -c 'FILLED-BY-53-0' scripts/verify-phase-53.ts` = 0(全部占位已填)
    - `grep -c 'G15TriagePanel\|失败镜头' packages/infinite-canvas/src/components/FlowCanvas.tsx` ≥ 2(挂载+入口)
    - `cd packages/infinite-canvas && npx tsc -b && npm test` 全绿
  </acceptance_criteria>
  <done>G15 面板接入画布工具栏,verify:phase-53 五段契约门全绿——Wave A 收口门就位。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端 → POST /api/canvas/v2/g15-ops(shotIds 数组,V4/V5 新入参面) | zod enum + bounded ≤200 + 每项 max(128) |
| g15Bridge → review-platform:8090(waive) | 出站 best-effort 通道,本机不可达(验证走注入 fetch) |
| 队列重放 → g15 动作(g15_waive/g15_requeue) | 53-04 队列的复用消费者 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-07-01 | Tampering | shotIds 超长/注入(V5) | mitigate | zod 每项 min(1).max(128) + 数组 .max(200);前端预拦截层 |
| T-53-07-02 | Elevation | 错误 waive 豁免不该豁免的镜头(fail-closed) | mitigate | g15Bridge 三维全等匹配(episode segment + scope,无前缀);S5 fail-closed 断言 fetch 计数 0 |
| T-53-07-03 | DoS | 重渲指令风暴(贵 GPU 操作误批量) | mitigate | 二次确认 + 计数文案(D-14)+ shotIds ≤200 + 队列串行 drain |
| T-53-07-04 | Repudiation | 豁免/重渲无审计 | mitigate | g15:ops 广播 + 队列行 payload/last_error + rowState 可视化 |
| T-53-07-05 | Tampering | 队列重放重复下发重渲 | mitigate | max_attempts=8 + 指数退避(53-04);requeue 消费端幂等义务写入桥 docblock(Wave B 检查项) |
| T-53-07-SC | Tampering | package installs | accept | 零新包 |

**结论:新端点面全部 zod bounded;最重风险是错误豁免(fail-closed 已 mitigate)与重渲误触(二次确认已 mitigate)。无 high 级未缓解威胁。**
</threat_model>

<verification>
- `npm run verify:phase-53` exit 0——五段契约门(S1 envelope/S2 组推导/S3 端点+队列/S4 墙源形状/S5 g15 桥+forced-failure)
- `cd packages/infinite-canvas && npm test` 全绿 + `npx tsc -b` exit 0
- 批量重渲二次确认观感属 HUMAN-UAT(53-VALIDATION Manual-Only 表)
</verification>

<success_criteria>
- VAR-04 Wave A 全量:分诊面板 + 操作桥 + 独立端点 + 队列降级;ROADMAP 成功标准 4 的 kap 半部成立(fixture 契约驱动,kmc 侧记录一致更新为 Wave B 门控)
- D-13(独立面板+嵌入位 seam)/D-14(勾选+动作条+二次确认)/D-15(一桥收口独立端点)/D-16(take_log 契约面+fixture)落地
- phase 契约门 verify:phase-53 全绿,Phase 53 Wave A 可进 /gsd:verify-work
</success_criteria>

<output>
Create `.planning/phases/53-variant-contract-picker-upgrade/53-07-SUMMARY.md` when done. 记录:G15Source seam 形态、waive 409 语义裁定、requeue gap 冻结文案位置、S4/S5 断言清单。
</output>
