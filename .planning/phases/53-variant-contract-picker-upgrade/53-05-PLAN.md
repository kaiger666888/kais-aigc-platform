---
phase: 53-variant-contract-picker-upgrade
plan: 05
type: execute
wave: 3
depends_on: ["53-02", "53-03"]
files_modified:
  - packages/infinite-canvas/src/services/canvasApi.ts
  - packages/infinite-canvas/src/store/canvasStore.ts
  - packages/infinite-canvas/src/store/variantOps.ts
  - packages/infinite-canvas/src/components/variants/VariantWall.tsx
  - packages/infinite-canvas/src/store/__tests__/selectWinner.test.ts
autonomous: true
requirements: [VAR-02, VAR-03]
must_haves:
  truths:
    - "selectWinner v3 路径透传 frameSlot:墙从组 groupKey 后缀推导 slot(`shot:{sid}:first`→'first'/`:last`→'last'),POST body 含 frameSlot(D-11 前端半部;端点半部 53-04 同 wave 落地)"
    - "G13 首尾 = 两组依次审:墙头栏显示首帧/尾帧 slot 标签,选定后自动进入同 shot 另一 slot 组,两组各自显式选定,绝不成对提交(Pitfall 6)"
    - "审完当前组墙不关、直接载入下一个待审变体组(shot 序、默认跳过已选定组);手动「下一镜」可跳过(D-17/D-18,轻交互不做队列传唤)"
    - "废弃本地 selectWinner RF 双轨(D-12):canvasStore legacy RF 路径删除改为 warn 早退;「💾 保存后持久化」叙事清零;选定 = optimistic + POST + 失败回滚 + toast 一条通道"
    - "canvasApi 新增 g15Ops(projectId, episodesId, action, shotIds) sibling(53-07 G15 面板消费;本 plan 预置避免同文件撞车)"
    - "下一镜排序 = 同 phase 内 shot_id 序;从 gate 视角进入时只列该 gate 的组(D-18;Wave A gate 过滤以 groupKey shot: 前缀为 shot 域,gate 精确过滤归 Phase 54)"
  artifacts:
    - path: "packages/infinite-canvas/src/services/canvasApi.ts"
      provides: "selectVariantWinner body 含可选 frameSlot(spread-omit)+ g15Ops() sibling"
      contains: "g15Ops"
    - path: "packages/infinite-canvas/src/store/canvasStore.ts"
      provides: "selectWinner(nodeId, opts?: {frameSlot?}) 透传 + legacy RF 路径废弃(warn 早退)"
      contains: "frameSlot"
    - path: "packages/infinite-canvas/src/store/variantOps.ts"
      provides: "frameSlotOfGroup(groupKey) + nextReviewGroup(graph, currentGroupId) 纯函数(shot 序/跳已选)"
      contains: "nextReviewGroup"
    - path: "packages/infinite-canvas/src/components/variants/VariantWall.tsx"
      provides: "slot 标签 + 选定后自动下一组 + 「下一镜」按钮接通 + ←/→ 键盘切组"
      contains: "nextReviewGroup"
  key_links:
    - from: "packages/infinite-canvas/src/components/variants/VariantWall.tsx"
      to: "packages/infinite-canvas/src/store/canvasStore.ts"
      via: "选定按钮 → selectWinner(nodeId, {frameSlot: frameSlotOfGroup(group.id)})"
      pattern: "frameSlot"
    - from: "packages/infinite-canvas/src/store/canvasStore.ts"
      to: "packages/infinite-canvas/src/services/canvasApi.ts"
      via: "selectVariantWinner(..., frameSlot) body spread-omit"
      pattern: "frameSlot"
    - from: "packages/infinite-canvas/src/store/__tests__/selectWinner.test.ts"
      to: "packages/infinite-canvas/src/store/canvasStore.ts"
      via: "frameSlot 透传断言(mock canvasApi 断言 body)+ 回滚回归"
      pattern: "frameSlot"
---

<objective>
把 53-02 的墙从"能选定"升级为"93 镜审片流水线":frameSlot 透传链(墙→store→api,D-11 前端半部)、G13 首尾两组依次审、选定后墙不关自动载入下一待审组 + 手动「下一镜」(D-17/D-18)、D-12 双轨清除(canvasStore legacy RF 路径废弃)。同时预置 canvasApi.g15Ops sibling(53-07 消费,避免两 plan 撞 canvasApi.ts)。

与 53-04(端点扩展)同 wave 并行:本 plan 文件全在 packages/infinite-canvas,53-04 全在 root src/,零交集;frameSlot 端到端 round-trip 由 verify:phase-53 S3(53-04)在两 plan 均落地后统一证明。

输出:canvasApi/canvasStore/variantOps/VariantWall 四处接线 + selectWinner.test.ts 扩展。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/53-variant-contract-picker-upgrade/53-CONTEXT.md
@.planning/phases/53-variant-contract-picker-upgrade/53-RESEARCH.md
@.planning/phases/53-variant-contract-picker-upgrade/53-PATTERNS.md
@.planning/phases/53-variant-contract-picker-upgrade/53-02-SUMMARY.md
@.planning/phases/53-variant-contract-picker-upgrade/53-03-SUMMARY.md

**COORD-01/D-01:本 plan 零 khs 文件、零 root 后端文件。执行者不得修改 files_modified 之外的文件。**

<interfaces>
<!-- 提取自 53-PATTERNS + 源码核读,执行者直接取用 -->

canvasApi.ts selectVariantWinner 现状(L463-475):`selectVariantWinner(projectId, episodesId, groupId, winnerNodeId, cancelToken?)` → apiCall body {projectId, episodesId, winnerNodeId}。扩展:第 6 参 frameSlot 可选,body 用 spread-omit:`...(frameSlot ? { frameSlot } : {})`(approveNode L438 `...(winnerId ? { winnerId } : {})` 同款)。g15Ops sibling:`g15Ops(projectId, episodesId, action: 'waive'|'requeue', shotIds: string[]): Promise<void>` → apiCall('/canvas/v2/g15-ops', {...})(docblock 注明 53-07 消费、错误语义照 L452-462 模型)。

canvasStore.selectWinner v3 路径(L889-929):prevGraph 快照 → selectVariant 纯校验 throw(任何 await 之前,不部分应用)→ setGraph → await selectVariantWinner → 失败 setGraph(prevGraph)+error toast。扩展 = 签名加 opts?: {frameSlot?: 'first'|'last'} 透传 api;rollback 逻辑零改动。legacy RF 路径(L931-971)= D-12 废弃对象:整段替换为 console.warn('[canvasStore] legacy RF selectWinner 已废弃(D-12):graph 为空时不再有本地选定') + return(不 throw)。

variantOps.ts 既有:findVariantGroupForNode L13 / getVariantMemberNodes L27-34 / applyWinnerSelection L66(locked/multi/dangling 守卫来自 flowgraph-v3 包,保持权威)。

VariantWall.tsx(53-02 产物):胶片条/详情行/选定按钮/下一镜 disabled 占位/←/→ no-op 占位已在;本 plan 接通。53-03 产物:derived 组 groupKey 形如 `shot:shot_012:first`,id 形如 `cand:shot:shot_012:first`。

groupKey→shot 序解析:`shot:{shot_id}:{slot}` 中 shot_id 按 localeCompare 自然序(shot_002 < shot_010);`name:` 组排在同 phase shot 组之后(无 shot 域);winner 已设(winnerNodeId 非 null)的组默认跳过但手动「下一镜」可进入(D-18:默认跳过 + 手动可越)。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: variantOps 纯函数(frameSlotOfGroup + nextReviewGroup)+ canvasApi 扩展</name>
  <files>packages/infinite-canvas/src/store/variantOps.ts, packages/infinite-canvas/src/services/canvasApi.ts</files>
  <read_first>packages/infinite-canvas/src/store/variantOps.ts (全文——纯函数纪律 + 既有导出), packages/infinite-canvas/src/services/canvasApi.ts (selectVariantWinner L463-475 + approveNode L438 spread-omit + L452-462 docblock 风格)</read_first>
  <action>
  1. variantOps.ts 追加两个纯函数(零 React/IO):
     - `export function frameSlotOfGroup(groupKey: string): 'first' | 'last' | undefined`——groupKey 以 `:first` 结尾→'first'、`:last` 结尾→'last'、其余 undefined(name: 组/G14 组无 slot)。
     - `export function nextReviewGroup(graph, currentGroupId: string, opts?: {includeSelected?: boolean}): VariantGroup | null`——收集 graph.variantGroups;排序:key 为 `shot:{sid}:{slot}` 的按 (shot_id 自然序, slot first<last) 升序,`name:` 组排最后(按 groupKey 字典序);`includeSelected` 缺省 false 时跳过 winnerNodeId 非空的组(已选定),true 时全列(手动「下一镜」语义);返回 currentGroupId 之后的第一个组,循环到头返回 null(显示「本 phase 审完」态,不回绕——planner 裁定:审片是线性流)。
  2. canvasApi.ts:selectVariantWinner 增加可选 frameSlot 参数 + body spread-omit;新增 `g15Ops(projectId, episodesId, action: 'waive' | 'requeue', shotIds: string[], cancelToken?)` sibling POST /canvas/v2/g15-ops(53-07 消费,docblock 注明端点由 53-07 落地,错误语义照 selectVariantWinner 模型)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx tsc -b 2>&1 | tail -3; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function frameSlotOfGroup\|export function nextReviewGroup' packages/infinite-canvas/src/store/variantOps.ts` = 2
    - `grep -c 'g15Ops' packages/infinite-canvas/src/services/canvasApi.ts` ≥ 2(签名+实现)
    - canvasApi.ts 含 `...(frameSlot ? { frameSlot } : {})`(spread-omit 形状)
    - `npx tsc -b` exit 0
  </acceptance_criteria>
  <done>slot 推导与下一镜排序成纯函数;api 层 frameSlot 透传与 g15 预置通道就位。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: canvasStore selectWinner frameSlot 透传 + legacy RF 双轨废弃(D-12)</name>
  <files>packages/infinite-canvas/src/store/canvasStore.ts, packages/infinite-canvas/src/store/__tests__/selectWinner.test.ts</files>
  <read_first>packages/infinite-canvas/src/store/canvasStore.ts (selectWinner 全段 L889-973——v3 路径 L889-929 逐行 + legacy RF 路径 L931-973 逐行 + interface 声明区), packages/infinite-canvas/src/store/__tests__/selectWinner.test.ts (既有 mock canvasApi 手法与断言风格), packages/infinite-canvas/src/services/canvasApi.ts (Task 1 后的 selectVariantWinner 签名)</read_first>
  <behavior>
  - Test: selectWinner(nodeId, {frameSlot:'first'}) → mock selectVariantWinner 收到第 6 参 'first';不传 opts → 收到 undefined(向后兼容)
  - Test: frameSlot 透传时失败路径仍回滚 prevGraph + error toast(既有回滚回归)
  - Test: graph 为 null 时调用 selectWinner → console.warn 且不调任何 api、不 throw(legacy RF 废弃后守卫)
  - Test: 既有用例组(locked/multi/dangling/幂等)全绿无回归
  </behavior>
  <action>
  1. interface 与实现签名改 `selectWinner: (nodeId: string, opts?: { frameSlot?: 'first' | 'last' }) => Promise<void>`;v3 路径内 `await selectVariantWinner(projectId, episodesId, group.id, nodeId, undefined, opts?.frameSlot)`(rollback 段零改动)。
  2. legacy RF 路径整段(L931-971 区域)替换为:console.warn('[canvasStore] legacy RF selectWinner 已废弃(D-12):选定必须经 v3 graph + select-winner 端点') + return。删除该段内所有本地 RF 写逻辑与 `💾` 相关注释;确认段内引用的 RF-only helper 若再无其他消费者,一并从本文件移除(只删本文件内代码,不动其他文件)。
  3. selectWinner.test.ts 追加 <behavior> 四组断言(mock 手法照既有用例)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx vitest run src/store/__tests__/selectWinner.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/store/__tests__/selectWinner.test.ts` exit 0(新四组 + 既有回归)
    - `grep -c 'frameSlot' packages/infinite-canvas/src/store/canvasStore.ts` ≥ 2
    - canvasStore.ts 中 `💾` 与 legacy RF 本地写路径消失(grep '💾' = 0;selectWinner 段无 RF 直写——以废弃 warn 文案存在为准 grep 'legacy RF selectWinner 已废弃' = 1)
    - `npx tsc -b` exit 0
  </acceptance_criteria>
  <done>选定一条通道收口(D-12):v3 optimistic + frameSlot 透传,RF 双轨废死,回滚语义有测试锁死。</done>
</task>

<task type="auto">
  <name>Task 3: VariantWall 接线 — slot 标签 + 选定后下一组 + 「下一镜」+ ←/→</name>
  <files>packages/infinite-canvas/src/components/variants/VariantWall.tsx</files>
  <read_first>packages/infinite-canvas/src/components/variants/VariantWall.tsx (53-02 产物全文——下一镜 disabled 占位/键盘 no-op 占位/选定按钮位置), packages/infinite-canvas/src/store/variantOps.ts (Task 1 产物:frameSlotOfGroup/nextReviewGroup 签名), packages/infinite-canvas/src/store/canvasStore.ts (Task 2 后的 selectWinner 签名)</read_first>
  <action>
  1. 选定按钮改调 `selectWinner(nodeId, { frameSlot: frameSlotOfGroup(group.id 的 groupKey) })`——注意墙持有的是 group 对象,frameSlotOfGroup 消费其 groupKey(cand: 前缀 id 内嵌同 key);成功 toast 后不关墙。
  2. 头栏 slot 标签:frameSlotOfGroup 返回 'first' 显示「首帧」、'last' 显示「尾帧」、undefined 不显示(G14 组无 slot)。
  3. 选定成功后自动流程(D-17):`const next = nextReviewGroup(graph, group.id)`(默认跳已选)——非 null 则墙内切换到 next 组(重置 transport:seek(0) + 检视第一张卡);null 显示头栏「本 phase 审完 ✓」态(不关墙,用户手动 Esc)。G13 两组:first 审完自动进同 shot 的 last 组(nextReviewGroup 的 slot 排序天然保证),两组各自显式选定。
  4. 「下一镜」按钮接通:`nextReviewGroup(graph, group.id, {includeSelected: true})`(手动可越已选,D-18);无下一组时禁用。←/→ 键盘回调同样接 next/prev(对称:prev 取排序序列的前一个,includeSelected: true)。
  5. 切组时保留 transport 实例但 seek(0) + solo 重置到第一张卡(视频元素重挂载由 React 数据流自然处理,detach 旧 attach 新照 53-02 的 attach/detach 协议)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx tsc -b 2>&1 | tail -3 && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - VariantWall.tsx 含 `nextReviewGroup` 与 `frameSlotOfGroup` 调用各 ≥1
    - VariantWall.tsx 含字面「下一镜」与「首帧」「尾帧」中文 copy(grep 各 ≥1;「本 phase 审完」≥1)
    - 「下一镜」按钮无 disabled 硬编码残留(交互态由 nextReviewGroup 结果驱动;grep 'disabled' 出现处均有条件表达式)
    - `npx tsc -b` exit 0;`npm test` 全绿
  </acceptance_criteria>
  <done>93 镜串行审片流成形:选定→自动下一待审组、手动可越、G13 首尾两组各选各的、审完有终态提示。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 墙 → canvasStore.selectWinner → POST select-winner(frameSlot) | 前端受控参数,经端点 zod(53-04)二次校验 |
| legacy RF 废弃 → 旧调用方 | graph 为空的调用方收到 warn 早退(非 throw) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-05-01 | Tampering | frameSlot 伪造跨 slot 写(前端面) | mitigate | frameSlot 只从 groupKey 后缀推导(不信任 UI 自由输入);端点 zod enum 二次校验(53-04) |
| T-53-05-02 | Denial of Service | RF 废弃后旧调用方静默失败 | mitigate | warn 早退 + 测试断言不 throw;53-06 删除 VariantPicker 后唯一调用方消失 |
| T-53-05-03 | Tampering | 双轨残留导致两套真值(D-12 违例) | mitigate | acceptance grep '💾'=0 + RF 写路径删除断言 |
| T-53-05-SC | Tampering | package installs | accept | 零新包 |

**结论:纯前端接线层,新入参受推导+端点双层约束。无 high 级威胁。**
</threat_model>

<verification>
- `cd packages/infinite-canvas && npx vitest run src/store/__tests__/selectWinner.test.ts src/components/variants` exit 0
- `npm test` 全绿 + `npx tsc -b` exit 0
- frameSlot 端到端 round-trip(墙→api→端点 zod→hook)由 `npm run verify:phase-53` S3 统一证明(53-04 同 wave 落地后跑)
</verification>

<success_criteria>
- VAR-02 审片流完整:检视+显式选定+串行下一镜+G13 首尾分选 UI 语义(D-11 前端半部)
- VAR-03 前端接线半部完成(D-12 单通道 + frameSlot 透传);ROADMAP 成功标准 3 的 kap 半部就位,E2E khs 闭环为 Wave B 门控
- g15Ops sibling 预置,53-07 零撞车
</success_criteria>

<output>
Create `.planning/phases/53-variant-contract-picker-upgrade/53-05-SUMMARY.md` when done. 记录:审完不回绕裁定、manual 下一镜可越已选裁定、RF 废弃清理清单。
</output>
