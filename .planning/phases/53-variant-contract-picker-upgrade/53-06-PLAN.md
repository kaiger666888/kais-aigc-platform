---
phase: 53-variant-contract-picker-upgrade
plan: 06
type: execute
wave: 4
depends_on: ["53-05"]
files_modified:
  - packages/infinite-canvas/src/v3/adapter.ts
  - packages/infinite-canvas/src/components/nodes/AssetCardNode.tsx
  - packages/infinite-canvas/src/components/variants/VariantPicker.tsx
  - packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx
autonomous: true
requirements: [VAR-02, VAR-03]
must_haves:
  truths:
    - "kmc 候选组成员节点在画布卡上可见牌堆徽章(变体组 ≥2 成员即显示),点击直接开变体墙——不再依赖 deprecated-only 的 VariantStackData 物化通道(Critical Gap 的前端半部)"
    - "资产中心候选组出现「去画布选片」入口:复用 focusAssetNodeId + setViewMode('canvas') 跳转,并 openWallByGroup 打开该组墙(D-19,不做双宿主内嵌)"
    - "VariantPicker.tsx 删除(D-12 吸收完毕):仓库零引用;variantPickerStore open/openPicker 协议保留(墙仍消费);「💾 保存后持久化」双轨叙事随文件消亡"
    - "牌堆通道迁移:onStackToggle(deprecated 组)与候选组成员点击统一开墙——墙对任何组(手建/推导/deprecated)通用"
  artifacts:
    - path: "packages/infinite-canvas/src/v3/adapter.ts"
      provides: "node data 附 variantGroupIds: string[] + variantGroupSize: number(全部组成员,不限 deprecated)"
      contains: "variantGroupIds"
    - path: "packages/infinite-canvas/src/components/nodes/AssetCardNode.tsx"
      provides: "组徽章渲染 + 点击开墙(variantGroupIds 驱动,兼容既有 stack 通道)"
      contains: "openWallByGroup"
    - path: "packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx"
      provides: "候选组行「去画布选片」链接(focusAssetNodeId + setViewMode + openWallByGroup)"
      contains: "去画布选片"
  key_links:
    - from: "packages/infinite-canvas/src/components/nodes/AssetCardNode.tsx"
      to: "packages/infinite-canvas/src/components/variants/variantPickerStore.ts"
      via: "组徽章点击 → openWallByGroup(variantGroupIds[0])"
      pattern: "openWallByGroup"
    - from: "packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx"
      to: "packages/infinite-canvas/src/store/canvasStore.ts"
      via: "handleLocateOnCanvas 同款块 + openWallByGroup(`cand:${groupKey}`)"
      pattern: "cand:"
---

<objective>
打通变体墙的两个入口并完成 D-12 收尾:① 画布内——adapter 给全部变体组成员节点附 `variantGroupIds/variantGroupSize` 数据通道,AssetCardNode 据此渲染组徽章、点击开墙(kmc 候选组此前无任何牌堆可见性);② 资产中心——候选组行加「去画布选片」跳转(D-19,复用 focusAssetNodeId 机制);③ 删除 VariantPicker.tsx(墙已吸收其职能,53-02 起未挂载)。

输出:adapter 成员通道 + AssetCardNode 徽章 + AssetLibrary 跳转 + VariantPicker.tsx 删除。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/53-variant-contract-picker-upgrade/53-CONTEXT.md
@.planning/phases/53-variant-contract-picker-upgrade/53-RESEARCH.md
@.planning/phases/53-variant-contract-picker-upgrade/53-PATTERNS.md
@.planning/phases/53-variant-contract-picker-upgrade/53-02-SUMMARY.md
@.planning/phases/53-variant-contract-picker-upgrade/53-05-SUMMARY.md

**COORD-01/D-01:本 plan 零 khs 文件、零 root 后端文件。执行者不得修改 files_modified 之外的文件(FlowCanvas.tsx 的挂载替换 53-02 已完成,本 plan 不碰)。**

<interfaces>
<!-- 提取自 53-PATTERNS + 源码核读,执行者直接取用 -->

adapter.ts 现状:VariantStackData L606;stackByWinner Map L679-707——只在组含 curation:'deprecated' 成员时物化 stack(L683-687),挂到 node data L754 `...(stack ? { variantStack: stack } : {})`;seedOfAsset L656。本 plan 在同一 node 映射处并联附 `variantGroupIds`(全部组,不限 deprecated):遍历 graph.variantGroups 建 nodeId→[groupId] 索引,首个组附 `variantGroupSize`(=该组 variantNodeIds.length + (winnerNodeId?1:0))。

AssetCardNode.tsx 现状:props data.variantStack?: VariantStackData(L49);`stackCount = data.variantStack?.count ?? 0`(L208);`{stackCount > 1 && (`(L221)渲染 StackChrome;StackChrome L497-530:layers 视觉 + `handlers.onStackToggle`(registerCInteractions 注入)点击。扩展点:可见性条件扩为 `stackCount > 1 || variantGroupSize > 1`;点击处理:优先 `useVariantPickerStore.getState().openWallByGroup(variantGroupIds[0])`,无 variantGroupIds 时回落 legacy onStackToggle 通道(stack 仍在的 deprecated 场景)。

variantPickerStore(53-02 后):`openWallByGroup(groupId)` 已存在;`open/openPicker` legacy 态仍在且墙消费(open.nodeId→findVariantGroupForNode 解析组)。

AssetLibrary.tsx handleLocateOnCanvas L829-835(D-19 复用块):`const nodeId = \`asset-${a.id}\`; const store = useCanvasStore.getState(); store.navPushCallback?.(); store.setFocusAssetNodeId(nodeId); store.setViewMode('canvas')`。候选组行的 groupKey 与画布 derived 组 id 的映射关系:**确定性** `cand:${groupKey}`(53-03 裁定)——「去画布选片」直接 openWallByGroup(`cand:${groupKey}`),并 focus 组内主资产对应画布节点(组行已有成员资产;节点 id 若不可得则只开墙不聚焦,容忍)。

Phase 48 o_assets 侧 groupKey 与 canvas 侧词表同源(53-03 已断言),故 `cand:${groupKey}` 拼接两侧可对上——这是 D-19 链路成立的前提。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: adapter 变体组成员通道 + AssetCardNode 组徽章开墙</name>
  <files>packages/infinite-canvas/src/v3/adapter.ts, packages/infinite-canvas/src/components/nodes/AssetCardNode.tsx</files>
  <read_first>packages/infinite-canvas/src/v3/adapter.ts (L606-619 VariantStackData + L668-707 stackByWinner 物化段 + L734-754 node data 附加点), packages/infinite-canvas/src/components/nodes/AssetCardNode.tsx (L49 props + L208 stackCount + L221 可见性条件 + L497-530 StackChrome 全段), packages/infinite-canvas/src/components/variants/variantPickerStore.ts (openWallByGroup 签名), packages/flowgraph-v3/ts/src/types.ts (VariantGroupV3 形状:variantNodeIds/winnerNodeId)</read_first>
  <action>
  1. adapter.ts node 映射段并联新增:先一次性遍历 graph.variantGroups 构建 `membership = Map<nodeId, {groupIds: string[]; firstGroupSize: number}>`(成员 = variantNodeIds ∪ winnerNodeId);在既有 `...(stack ? { variantStack: stack } : {})` 旁附加 `...(m ? { variantGroupIds: m.groupIds, variantGroupSize: m.firstGroupSize } : {})`。类型:V3 node data 的扩展字段照 variantStack 同款声明方式(如 data 接口有 variantStack?: VariantStackData,则并列加 variantGroupIds?: string[]; variantGroupSize?: number)。
  2. AssetCardNode.tsx:
     - props/data 类型加 `variantGroupIds?: string[]; variantGroupSize?: number`。
     - 徽章可见性:`{((data.variantStack?.count ?? 0) > 1 || (data.variantGroupSize ?? 0) > 1) && (` 渲染 StackChrome(原 L221 条件扩展)。
     - StackChrome 点击处理(toggleStack 回调内):若 `data.variantGroupIds?.length` → `useVariantPickerStore.getState().openWallByGroup(data.variantGroupIds[0])`(import variantPickerStore);否则保留原 `handlers.onStackToggle(nodeId, stack)` 回落(deprecated stack 无组的极端场景)。StackChrome 的 layers 视觉用 `stack.count ?? variantGroupSize` 保持一致。
  3. 不改 registerCInteractions.ts(其 onStackToggle 注入仍是 deprecated 通道回落路径;墙的 open 态消费已在 53-02 就位)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/packages/infinite-canvas && npx tsc -b 2>&1 | tail -3 && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'variantGroupIds' packages/infinite-canvas/src/v3/adapter.ts` ≥ 2(构建+附加)
    - `grep -c 'openWallByGroup' packages/infinite-canvas/src/components/nodes/AssetCardNode.tsx` ≥ 1
    - AssetCardNode 徽章条件含 `variantGroupSize`(grep 命中)且保留 onStackToggle 回落分支(grep 'onStackToggle' ≥1)
    - `npx tsc -b` exit 0;`npm test` 全绿
  </acceptance_criteria>
  <done>kmc 候选组在画布卡上有可见入口,点击直达墙;deprecated 旧通道保留为回落。</done>
</task>

<task type="auto">
  <name>Task 2: AssetLibrary「去画布选片」(D-19)+ VariantPicker.tsx 删除(D-12 收尾)</name>
  <files>packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx, packages/infinite-canvas/src/components/variants/VariantPicker.tsx</files>
  <read_first>packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx (L824-862:handleLocateOnCanvas 复用块 + handleSelect optimistic 范式;候选组行的渲染段——Phase 48 分组 UI), packages/infinite-canvas/src/components/variants/variantPickerStore.ts (确认 open/openPicker 保留、墙消费关系), packages/infinite-canvas/src/components/variants/registerCInteractions.ts (确认其只引用 variantPickerStore 不引用 VariantPicker 组件——若是,删除组件文件无涟漪)</read_first>
  <action>
  1. AssetLibrary.tsx:候选组行(Phase 48 o_assets 分组渲染处,行级或组头)追加「去画布选片」链接按钮,处理函数照 handleLocateOnCanvas 同款块:`const store = useCanvasStore.getState(); store.navPushCallback?.(); if (canvasNodeId 可得) store.setFocusAssetNodeId(canvasNodeId); store.setViewMode('canvas'); useVariantPickerStore.getState().openWallByGroup(\`cand:${groupKey}\`)`。canvasNodeId 取组内主资产对应的画布节点 id(既有资产→节点联动通道;不可得则跳过 focus 仅开墙——容忍路径写注释)。按钮 copy:「去画布选片」;样式贴既有行内链接按钮(不新造 token)。
  2. 删除 `packages/infinite-canvas/src/components/variants/VariantPicker.tsx`(git rm):53-02 起未挂载,职能已被 VariantWall 吸收。删除前 grep 全仓确认零引用(`grep -rn "VariantPicker" packages/infinite-canvas/src --include="*.tsx" --include="*.ts"` 仅剩 variantPickerStore.ts/注释/无关命中;FlowCanvas 挂载已在 53-02 替换)。若发现残留 import(如类型引用),将引用方改为等价内联类型或从 variantPickerStore 导出——只允许改 files_modified 内文件,若残留引用在其他文件则不得删除组件文件,改为把文件内容缩为 re-export 存根并在 SUMMARY 记录 blocker。
  3. 全仓 `💾 保存` 叙事清点:本 plan 后 UI 内不得再出现「💾 保存后持久化」字样(墙/详情面板的选定即时持久化叙事已就位)。
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && grep -rn "from './VariantPicker'\|from '../variants/VariantPicker'\|<VariantPicker" packages/infinite-canvas/src | wc -l && ls packages/infinite-canvas/src/components/variants/VariantPicker.tsx 2>&1; cd packages/infinite-canvas && npx tsc -b 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `ls packages/infinite-canvas/src/components/variants/VariantPicker.tsx` 报不存在(或按存根路径处理时 SUMMARY 有 blocker 记录且 grep 引用=0)
    - VariantPicker 组件引用计数 = 0(上述 grep 管道输出 0)
    - `grep -c '去画布选片' packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx` ≥ 1 且同文件含 `openWallByGroup` 与 `cand:` 拼接
    - `grep -rn '💾' packages/infinite-canvas/src/components/variants/ packages/infinite-canvas/src/components/assetManager/` = 0
    - `npx tsc -b` exit 0;`cd packages/infinite-canvas && npm test` 全绿
  </acceptance_criteria>
  <done>D-19 资产中心→画布选片链路通;D-12 双轨痕迹清零(组件删除 + 叙事清零),store 协议保留。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 资产中心 groupKey → 画布组 id 拼接(`cand:${groupKey}`) | 两组数据源的确定性映射消费点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-06-01 | Elevation | 伪造 groupKey 打开他 episode 组 | mitigate | openWallByGroup 只在当前画布 graph 内解析组;组不存在的组 id 显示空态(53-02「未找到变体组」),无跨 scope 写面 |
| T-53-06-02 | Tampering | 删除 VariantPicker 引发隐性断裂 | mitigate | 删除前全仓 grep 引用清点 + tsc -b + npm test 三重门;异常时存根降级路径写死在任务里 |
| T-53-06-03 | Tampering | adapter 数据通道体积膨胀(每节点附组列表) | accept | 组数与成员数为个位数量级;仅附加 string[] 与 number,无嵌套对象 |
| T-53-06-SC | Tampering | package installs | accept | 零新包 |

**结论:入口层无新写通道;主要工程风险是删除的涟漪,由 grep+tsc+test 三重收口。无 high 级威胁。**
</threat_model>

<verification>
- `cd packages/infinite-canvas && npm test` 全绿 + `npx tsc -b` exit 0
- `npm run verify:phase-53` 仍 exit 0(S1-S3 无回归)
- 入口可用性(徽章可见/跳转聚焦/墙打开)属 HUMAN-UAT 抽查项
</verification>

<success_criteria>
- VAR-02 入口闭环:画布组徽章 + 资产中心跳转两路进墙,墙单宿主(D-19)
- D-12 彻底收尾:旧 Picker 消亡、双轨叙事清零、单通道选定
- adapter 成员通道成为 Phase 54 gate 面板可复用的组可见性数据源
</success_criteria>

<output>
Create `.planning/phases/53-variant-contract-picker-upgrade/53-06-SUMMARY.md` when done. 记录:资产→画布节点 id 联动的可得性结论(或 blocker)、删除 VariantPicker 的引用清点结果。
</output>
