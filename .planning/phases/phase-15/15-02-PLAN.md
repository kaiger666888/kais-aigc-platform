---
phase: 15-production-stability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.real.yml
  - docker-compose.smoke.yml
  - src/routes/proxy/movieAgent.ts
  - src/routes/v1/pipeline/start.ts
  - src/router.ts
  - .env
  - .env.example
  - build.sh
autonomous: true
requirements:
  - CLN-01
  - CLN-02
  - CLN-03

must_haves:
  truths:
    - "Docker Compose files contain zero movie-agent service definitions"
    - "No source code references to movie-agent remain (imports, configs, env vars)"
    - "OpenClaw Agent can be verified as the replacement for movie-agent orchestration duties"
    - "Application builds successfully after removing all movie-agent code"
  artifacts:
    - path: "docker-compose.real.yml"
      provides: "Compose file without movie-agent service"
      contains_not: "movie-agent"
    - path: "docker-compose.smoke.yml"
      provides: "Smoke test compose without movie-agent service"
      contains_not: "movie-agent"
    - path: "src/routes/proxy/movieAgent.ts"
      provides: "Deleted file"
      exists: false
    - path: "src/router.ts"
      provides: "Router without movieAgent import or route registration"
      contains_not: "movieAgent"
  key_links:
    - from: "src/router.ts"
      to: "src/routes/v1/pipeline/start.ts"
      via: "import and route registration"
      pattern: "pipeline/start"
---

<objective>
Remove all movie-agent dead code from the codebase: Docker Compose service definitions, proxy routes, pipeline start route, env vars, build scripts, and docker directory. Verify OpenClaw Agent (hermes-agent) covers movie-agent's orchestration duties.

Purpose: movie-agent has been replaced by OpenClaw Agent running inside hermes-agent. Keeping dead code causes confusion, bloated builds, and maintenance burden. A clean removal unblocks Phase 16 code merge.

Output: Clean codebase with zero movie-agent references in active source/config files. Verification table documenting OpenClaw Agent coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docker-compose.real.yml
@docker-compose.smoke.yml
@src/router.ts
@src/routes/proxy/movieAgent.ts
@src/routes/v1/pipeline/start.ts
@.env
@.env.example
@build.sh
</context>

<interfaces>
<!-- Files to DELETE entirely -->
<!-- src/routes/proxy/movieAgent.ts — proxy route forwarding to movie-agent:8001 -->

<!-- Files to MODIFY -->
<!-- docker-compose.real.yml — remove movie-agent service block (lines 60-81) -->
<!-- docker-compose.smoke.yml — remove movie-agent service block (lines 43-61) -->
<!-- src/router.ts — remove import line 130 and route registration line 372 -->
<!-- src/routes/v1/pipeline/start.ts — entire file proxies to movie-agent, must be removed or rewritten -->
<!-- .env — remove MOVIE_AGENT_PORT=8001 and comment on line 49 -->
<!-- .env.example — remove MOVIE_AGENT_PORT=8001 -->
<!-- build.sh — remove movie-agent build steps (lines 18-23) -->

<!-- Directory to DELETE -->
<!-- docker/movie-agent/ — entire directory (Dockerfile, src/, lib/, shared/, tests/) -->

<!-- VERIFICATION target -->
<!-- docker/hermes-agent/_hermes_source/hermes_cli/claw.py — OpenClaw Agent orchestration -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove movie-agent from Docker Compose files, source code, env vars, and build scripts</name>
  <files>docker-compose.real.yml, docker-compose.smoke.yml, src/routes/proxy/movieAgent.ts, src/routes/v1/pipeline/start.ts, src/router.ts, .env, .env.example, build.sh</files>
  <read_first>
    - docker-compose.real.yml (movie-agent service at lines 60-81)
    - docker-compose.smoke.yml (movie-agent service at lines 43-61)
    - src/router.ts (import at line 130, route registration at line 372, import at line 224, route registration at line 466)
    - src/routes/proxy/movieAgent.ts (full file, to be deleted)
    - src/routes/v1/pipeline/start.ts (full file, to be deleted)
    - .env (lines 17 and 49 contain movie-agent references)
    - .env.example (line 14 contains MOVIE_AGENT_PORT)
    - build.sh (lines 18-23 contain movie-agent build steps)
  </read_first>
  <action>
    Perform the following removals in order:

    **A. Docker Compose files:**

    1. In `docker-compose.real.yml`: Delete the entire `movie-agent:` service block (lines 60-81, from `movie-agent:` through its closing `retries: 3`). Leave surrounding services intact.

    2. In `docker-compose.smoke.yml`: Delete the entire `movie-agent:` service block (lines 43-61, from `movie-agent:` through its closing `retries: 3`). Leave surrounding services intact.

    **B. Source code deletions:**

    3. Delete file `src/routes/proxy/movieAgent.ts` entirely.

    4. Delete file `src/routes/v1/pipeline/start.ts` entirely. This file's sole purpose is proxying to movie-agent's `/api/v1/pipeline/run` endpoint. With movie-agent removed, this route is dead code.

    **C. Router cleanup (src/router.ts):**

    5. Remove the import line: `import route127 from "./routes/proxy/movieAgent";` (line 130)

    6. Remove the route registration: `app.use("/api/proxy/movieAgent", route127);` (line 372)

    7. Remove the import line: `import route221 from "./routes/v1/pipeline/start";` (line 224)

    8. Remove the route registration: `app.use("/api/v1/pipeline/start", route221);` (line 466)

    **D. Environment variable cleanup:**

    9. In `.env`: Remove the line `MOVIE_AGENT_PORT=8001` (line 17). Update the comment on line 49 from `# LLM 代理配置 (Hermes / OpenClaw — movie-agent 使用)` to `# LLM 代理配置 (Hermes / OpenClaw)`.

    10. In `.env.example`: Remove the line `MOVIE_AGENT_PORT=8001` (line 14).

    **E. Build script cleanup:**

    11. In `build.sh`: Remove lines 18-23 (the movie-agent build block, from `# 2. movie-agent` through the docker build command). Adjust any subsequent step numbering if needed (e.g., `# 3.` becomes `# 2.`, `# 4.` becomes `# 3.`).

    **F. Docker directory deletion:**

    12. Delete the entire `docker/movie-agent/` directory: `rm -rf docker/movie-agent/`

    **G. Rebuild verification:**

    13. Run `npx tsc --noEmit` to verify the TypeScript project compiles without errors after removing the movie-agent imports and routes.
  </action>
  <verify>
    <automated>grep -rn "movie.agent\|movie-agent\|MOVIE_AGENT\|movieAgent\|movie_agent" docker-compose.real.yml docker-compose.smoke.yml src/router.ts src/routes/proxy/movieAgent.ts src/routes/v1/pipeline/start.ts .env .env.example build.sh 2>/dev/null; test $? -ne 0 && echo "PASS: zero movie-agent references" || echo "FAIL: references remain"</automated>
  </verify>
  <done>
    - docker-compose.real.yml has no movie-agent service definition
    - docker-compose.smoke.yml has no movie-agent service definition
    - src/routes/proxy/movieAgent.ts deleted
    - src/routes/v1/pipeline/start.ts deleted
    - src/router.ts has no movieAgent import or route registration
    - .env and .env.example have no MOVIE_AGENT references
    - build.sh has no movie-agent build steps
    - docker/movie-agent/ directory deleted
    - TypeScript compiles without errors (npx tsc --noEmit passes)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify OpenClaw Agent covers movie-agent orchestration and produce verification table</name>
  <files></files>
  <read_first>
    - docker/hermes-agent/_hermes_source/hermes_cli/claw.py (OpenClaw Agent orchestration capabilities)
    - docker/movie-agent/src/index.js (if still exists, for capability comparison -- already deleted in Task 1, so use prior knowledge)
  </read_first>
  <action>
    Verify that hermes-agent's OpenClaw Agent (claw.py and related modules) covers the orchestration duties that movie-agent performed. The key movie-agent duties were:

    1. Pipeline orchestration: Accepting pipeline run requests, executing phases sequentially
    2. Task submission to gold-team: POST /api/v1/tasks with callback URLs
    3. Review platform integration: Submitting shots for review, receiving callbacks
    4. Core-backend data fetching: Reading project/script/asset data
    5. State management: Pipeline state persistence and recovery from disk
    6. Callback handling: GPU task completion callbacks and review result callbacks

    Verify coverage by examining:
    - `docker/hermes-agent/_hermes_source/hermes_cli/claw.py` for orchestration/pipeline capabilities
    - `docker/hermes-agent/_hermes_source/agent/` for agent skill routing
    - `docker/hermes-agent/src/` for the REST API layer that exposes OpenClaw capabilities

    Create a verification comparison in the plan SUMMARY as a markdown table:

    | movie-agent Duty | OpenClaw/hermes Coverage | Status |
    |------------------|--------------------------|--------|
    | Pipeline orchestration (phase execution) | claw.py pipeline commands | Verified / Not Found |
    | Task submission to gold-team | REST API proxy through core-backend | Verified / Not Found |
    | Review platform integration | hermes decision engine + callbacks | Verified / Not Found |
    | State management | claw.py state persistence | Verified / Not Found |
    | Callback handling | hermes-agent REST endpoints | Verified / Not Found |

    Run: `grep -rn "pipeline\|orchestrat\|task_submit\|callback\|review" docker/hermes-agent/_hermes_source/hermes_cli/claw.py | head -30`
    Run: `ls docker/hermes-agent/src/ && grep -rn "pipeline\|callback\|task" docker/hermes-agent/src/main.py | head -20`

    Document findings in the SUMMARY file. If any duty has NO coverage, flag it as a gap that needs addressing in a later phase.
  </action>
  <verify>
    <automated>grep -c "pipeline\|orchestrat\|callback" docker/hermes-agent/_hermes_source/hermes_cli/claw.py 2>/dev/null | awk '{print $1 " matches in claw.py"}'</automated>
  </verify>
  <done>
    - Verification table produced comparing movie-agent duties to OpenClaw/hermes coverage
    - Each movie-agent duty has a "Verified" or "Not Found" status
    - Any gaps are explicitly flagged for future phase action
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Code removal | Removing dead code and services reduces attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Tampering | Dead code removal | mitigate | Removing unused proxy route eliminates potential request smuggling vector through movie-agent proxy |
| T-15-04 | Information Disclosure | .env cleanup | mitigate | Removing MOVIE_AGENT_PORT reduces information leakage about service topology |
</threat_model>

<verification>
1. `grep -rn "movie.agent\|movie-agent\|MOVIE_AGENT\|movieAgent" docker-compose.real.yml docker-compose.smoke.yml src/router.ts .env .env.example build.sh` returns zero matches
2. `test -f src/routes/proxy/movieAgent.ts` returns false (file deleted)
3. `test -f src/routes/v1/pipeline/start.ts` returns false (file deleted)
4. `test -d docker/movie-agent` returns false (directory deleted)
5. `npx tsc --noEmit` passes with zero errors
6. Verification table exists in 15-02-SUMMARY.md with all movie-agent duties mapped
</verification>

<success_criteria>
- Zero movie-agent references in docker-compose.real.yml, docker-compose.smoke.yml, docker-compose.v9.yml (already clean)
- Zero movie-agent references in src/ source files (imports, routes, env vars)
- docker/movie-agent/ directory deleted
- TypeScript project compiles cleanly after removal
- OpenClaw Agent verification table documents coverage of all movie-agent duties
</success_criteria>

<output>
Create `.planning/phases/phase-15/15-02-SUMMARY.md` when done
</output>
