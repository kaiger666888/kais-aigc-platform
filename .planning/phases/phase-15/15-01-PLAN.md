---
phase: 15-production-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.v9.yml
autonomous: true
requirements:
  - FIX-01
  - FIX-02
  - FIX-03

must_haves:
  truths:
    - "ACE-Step container starts healthy and stays running without PermissionError"
    - "Music generation request succeeds through the unified API end-to-end"
    - "Docker reports kais-acestep as healthy via curl healthcheck"
  artifacts:
    - path: "docker-compose.v9.yml"
      provides: "ACE-Step service with user directive and curl healthcheck"
      contains: "kais-acestep"
  key_links:
    - from: "docker-compose.v9.yml"
      to: "ACE-Step Dockerfile"
      via: "build context + volume mount"
      pattern: "kais-acestep"
---

<objective>
Fix ACE-Step container permission errors so it starts healthy and stays running, then verify end-to-end music generation.

Purpose: The ACE-Step container crashes on startup with PermissionError on the /app/checkpoints directory because the volume mount overrides directory ownership. This blocks all music generation functionality.

Output: Modified docker-compose.v9.yml with user directive and healthcheck; verified working ACE-Step service.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docker-compose.v9.yml
</context>

<interfaces>
<!-- ACE-Step service definition in docker-compose.v9.yml (lines 295-331) -->
<!-- Volume mount: ${ACESTEP_MODELS_DIR}:/app/checkpoints -->
<!-- Current healthcheck: ["CMD", "curl", "-sf", "http://localhost:8001/health"] -->
<!-- Other services pattern: gold-team uses user directive not present here -->
<!-- ACE-Step Dockerfile creates dirs at line 77: RUN mkdir -p /app/checkpoints /app/gradio_outputs /app/output -->
<!-- ACE-Step runs as root by default (no USER directive in Dockerfile) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix ACE-Step container permissions in docker-compose.v9.yml</name>
  <files>docker-compose.v9.yml</files>
  <read_first>
    - docker-compose.v9.yml (full file, focus on kais-acestep service at lines 295-331)
  </read_first>
  <action>
    In docker-compose.v9.yml, modify the `kais-acestep` service definition:

    1. Add `user: "1000:1000"` at the same indentation level as `image:`, `container_name:`, etc. This ensures the container process runs as UID 1000, matching the host volume mount ownership for `/data/models/ACE-Step1.5`.

    2. The healthcheck already uses curl (line 327: `test: ["CMD", "curl", "-sf", "http://localhost:8001/health"]`), which matches the CONTEXT.md decision for a curl-based healthcheck. No healthcheck change needed.

    3. Verify the ACE-Step Dockerfile (at /data/workspace/ACE-Step-1.5/Dockerfile, line 77) creates `/app/checkpoints` via `RUN mkdir -p`. Since the volume mount replaces this directory, the `user: "1000:1000"` directive ensures the container process can write to the host-mounted directory owned by UID 1000.

    No Dockerfile changes are required per the CONTEXT.md decision -- the `user: "1000:1000"` in docker-compose is the minimal fix matching other services.
  </action>
  <verify>
    <automated>grep -A5 "kais-acestep:" docker-compose.v9.yml | grep 'user: "1000:1000"'</automated>
  </verify>
  <done>
    - docker-compose.v9.yml kais-acestep service has `user: "1000:1000"` directive
    - curl-based healthcheck remains intact
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify ACE-Step container starts healthy and music generation works end-to-end</name>
  <files>docker-compose.v9.yml</files>
  <read_first>
    - docker-compose.v9.yml (kais-acestep service definition)
  </read_first>
  <action>
    Build and start the ACE-Step container to verify the permission fix:

    1. Build the ACE-Step image:
       `docker compose -f docker-compose.v9.yml --profile ace build kais-acestep`

    2. Start only the ACE-Step service (requires redis healthy, so start infrastructure first):
       `docker compose -f docker-compose.v9.yml up -d redis`
       Wait for redis healthy, then:
       `docker compose -f docker-compose.v9.yml --profile ace up -d kais-acestep`

    3. Wait for the container to reach healthy state (start_period is 300s):
       `docker compose -f docker-compose.v9.yml --profile ace ps kais-acestep`
       Check health status becomes "healthy".

    4. Check container logs for PermissionError:
       `docker logs kais-acestep 2>&1 | grep -i "PermissionError"`
       Must return zero matches.

    5. Check container logs for successful startup:
       `docker logs kais-acestep 2>&1 | grep -E "REST API server|Starting"`
       Must show "Starting REST API server".

    6. Test the health endpoint directly:
       `curl -sf http://localhost:8009/health`
       Must return 200 OK.

    7. Test end-to-end music generation through the gold-team API (FIX-03):
       If gold-team is not running, start it:
       `docker compose -f docker-compose.v9.yml up -d kais-gold-team`
       Then submit a test music generation request:
       `curl -X POST http://localhost:8002/api/v1/tasks -H "Content-Type: application/json" -d '{"type":"AUDIO","priority":"normal","params":{"model":"acestep-v15-xl-turbo","prompt":"test melody","duration":10}}'`
       Verify the response contains a task_id and status is not "failed".

    If the ACE-Step container is already running or gold-team is unavailable, skip the e2e test but ensure the healthcheck and log verification pass.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.v9.yml --profile ace ps kais-acestep 2>/dev/null | grep -v "^NAME" | grep -q "healthy\|running" && echo "PASS: container running" || echo "FAIL: container not running"</automated>
  </verify>
  <done>
    - ACE-Step container status is "healthy" (no PermissionError in logs)
    - Health endpoint responds with 200 at http://localhost:8009/health
    - No PermissionError in container logs
    - Music generation test request submitted through gold-team API (if gold-team available)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host volume mount -> container | /data/models/ACE-Step1.5 mounted into container as /app/checkpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Elevation of Privilege | user: "1000:1000" directive | mitigate | Runs as non-root UID 1000, matches host ownership, limits container escape surface |
| T-15-02 | Tampering | Volume mount read/write | accept | Checkpoints directory needs write access for model caching; models are not sensitive data |
</threat_model>

<verification>
1. `docker compose -f docker-compose.v9.yml --profile ace ps kais-acestep` shows healthy status
2. `docker logs kais-acestep 2>&1 | grep -i "PermissionError"` returns empty
3. `curl -sf http://localhost:8009/health` returns 200
4. docker-compose.v9.yml contains `user: "1000:1000"` under kais-acestep service
</verification>

<success_criteria>
- ACE-Step container starts with `user: "1000:1000"` and no PermissionError in logs
- Container reaches "healthy" state via curl healthcheck
- Music generation request succeeds through unified API (if gold-team available)
</success_criteria>

<output>
Create `.planning/phases/phase-15/15-01-SUMMARY.md` when done
</output>
