---
phase: 08-learning-loop-integration
plan: 02
type: tdd
wave: 2
depends_on: ["08-01"]
files_modified:
  - docker/hermes-agent/src/core/decision_engine.py
  - docker/hermes-agent/src/api/models.py
  - docker/hermes-agent/src/api/routes.py
  - docker/hermes-agent/src/api/deps.py
  - docker/hermes-agent/tests/test_decision_engine.py
  - docker/hermes-agent/tests/test_routes.py
  - docker/hermes-agent/tests/test_integration.py
  - docker/hermes-agent/tests/conftest.py
autonomous: true
requirements: [LEARN-01, LEARN-02, LEARN-03]

must_haves:
  truths:
    - "record_audit() aggregates into audit_history.json via DomainMemory and returns auto_learn_triggered based on EWMA"
    - "decide() reads EWMA confidence from DomainMemory and returns dynamic value for tasks with 3+ audits"
    - "GET /v1/domains/{domain}/memory returns aggregated stats with task_stats and recent_records"
    - "Domain A audits do not affect Domain B confidence values"
  artifacts:
    - path: "docker/hermes-agent/src/core/decision_engine.py"
      provides: "Modified record_audit() and decide() integrating DomainMemory"
      contains: "DomainMemory"
    - path: "docker/hermes-agent/src/api/routes.py"
      provides: "GET /v1/domains/{domain}/memory endpoint"
      contains: "get_domain_memory"
    - path: "docker/hermes-agent/src/api/models.py"
      provides: "MemoryResponse and MemoryTaskStat Pydantic models"
      contains: "MemoryResponse"
  key_links:
    - from: "docker/hermes-agent/src/core/decision_engine.py"
      to: "docker/hermes-agent/src/core/domain_memory.py"
      via: "from src.core.domain_memory import DomainMemory"
      pattern: "from src\\.core\\.domain_memory import"
    - from: "docker/hermes-agent/src/api/routes.py"
      to: "docker/hermes-agent/src/api/models.py"
      via: "import MemoryResponse, MemoryTaskStat"
      pattern: "MemoryResponse"
    - from: "docker/hermes-agent/tests/test_integration.py"
      to: "/v1/decide + /v1/audit + /v1/domains/:domain/memory"
      via: "TestClient HTTP calls"
      pattern: "/memory"
---

<objective>
Wire DomainMemory into DecisionEngine and add GET /v1/domains/{domain}/memory endpoint, completing the audit-memory-decide learning loop.

Purpose: Makes the learning loop operational end-to-end: audit data flows into memory, confidence updates dynamically, auto-learn triggers on low scores.
Output: Modified decision_engine.py, routes.py, models.py with full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-learning-loop-integration/08-CONTEXT.md
@.planning/phases/08-learning-loop-integration/08-RESEARCH.md
@.planning/phases/08-learning-loop-integration/08-01-SUMMARY.md
@docker/hermes-agent/src/core/domain_memory.py
@docker/hermes-agent/src/core/decision_engine.py
@docker/hermes-agent/src/api/routes.py
@docker/hermes-agent/src/api/models.py
@docker/hermes-agent/src/api/deps.py
@docker/hermes-agent/tests/conftest.py
@docker/hermes-agent/tests/test_decision_engine.py
@docker/hermes-agent/tests/test_routes.py
@docker/hermes-agent/tests/test_integration.py

<interfaces>
<!-- DomainMemory class created in Plan 01 -- executor needs these signatures -->

From docker/hermes-agent/src/core/domain_memory.py (Plan 01 output):
```python
class DomainMemory:
    def __init__(self, memory_dir: Path) -> None
    def append_record(self, task: str, record: dict[str, Any]) -> None
    def get_confidence(self, task: str) -> float
    def should_trigger_auto_learn(self, task: str) -> bool
    def get_summary(self) -> dict[str, Any]
```

From docker/hermes-agent/src/core/decision_engine.py (current):
```python
class DecisionEngine:
    def __init__(self, registry: DomainRegistry, agent_factory: AgentFactory | None) -> None
    def record_audit(self, domain: str, decision_id: str, outcome: str, metrics: dict[str, Any]) -> dict[str, Any]
    def decide(self, domain: str, task: str, context: dict[str, Any]) -> dict[str, Any]
    def build_prompt(domain: str, task: str, context: dict[str, Any]) -> str
    def check_health(self) -> dict[str, Any]
```

From docker/hermes-agent/src/api/routes.py (current):
- Router pattern: @router.get/post with Depends() for registry and engine
- Error pattern: HTTPException(status_code=404, detail=...)

From docker/hermes-agent/src/api/models.py (current):
- Pydantic BaseModel pattern with Field(...) for required, default_factory=dict for optional
- AuditResponse has auto_learn_triggered: bool = False and decision_id: str fields

From docker/hermes-agent/src/core/domain_registry.py:
- registry.base_dir: Path attribute
- Domain memory path: registry.base_dir / domain / "memory"

From docker/hermes-agent/tests/conftest.py:
- client fixture provides TestClient with dependency overrides
- decision_engine fixture provides DecisionEngine(registry, agent_factory)
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: TDD RED -- write failing tests for DecisionEngine integration and memory endpoint</name>
  <files>
    docker/hermes-agent/tests/test_decision_engine.py,
    docker/hermes-agent/tests/test_routes.py,
    docker/hermes-agent/tests/test_integration.py
  </files>
  <read_first>
    docker/hermes-agent/tests/test_decision_engine.py
    docker/hermes-agent/tests/test_routes.py
    docker/hermes-agent/tests/test_integration.py
    docker/hermes-agent/tests/conftest.py
    docker/hermes-agent/src/core/decision_engine.py
    docker/hermes-agent/src/api/models.py
    docker/hermes-agent/src/api/routes.py
  </read_first>
  <behavior>
    Tests for DecisionEngine.record_audit() changes:
    - test_record_audit_aggregates_to_history: register domain, call record_audit() with metrics containing {"task": "task-a", "score": 7}, verify that audit_history.json exists in domain memory dir with task-a key
    - test_record_audit_auto_learn_triggered: register domain, call record_audit() 3 times with low scores (1, 2, 1) for same task, verify record_audit() returns {"recorded": True, "auto_learn_triggered": True}
    - test_record_audit_auto_learn_not_triggered: register domain, call record_audit() 3 times with high scores (8, 9, 8) for same task, verify auto_learn_triggered is False
    - test_record_audit_backward_compat: register domain, call record_audit() WITHOUT task in metrics (just {"score": 5}), verify it still writes the individual JSON file and returns recorded=True (task defaults to "unknown")

    Tests for DecisionEngine.decide() changes:
    - test_decide_dynamic_confidence: register domain, create DomainMemory, append 3 records with scores [8, 8, 8] for task "task-a", call decide(domain, "task-a", {}), verify confidence != 0.0
    - test_decide_confidence_zero_below_minimum: register domain, call decide() with no prior audits, verify confidence == 0.0 (backward compat)

    Tests for GET /v1/domains/{domain}/memory endpoint:
    - test_memory_endpoint_returns_summary: register domain, post 3 audits via /v1/audit endpoint with task in metrics, GET /v1/domains/test-domain/memory, verify response has task_stats dict and recent_records list
    - test_memory_endpoint_404_unregistered: GET /v1/domains/nonexistent/memory returns 404
    - test_memory_endpoint_empty_domain: register domain, GET /v1/domains/test-domain/memory, verify returns {task_stats: {}, recent_records: []}

    Integration test for full learning loop:
    - test_learning_loop: register domain -> POST /v1/decide (assert confidence==0.0) -> POST /v1/audit x3 with low scores and task field -> POST /v1/decide again (assert auto_learn_triggered not in decide response, but confidence != 0.0) -> POST /v1/audit (assert auto_learn_triggered: true) -> GET /v1/domains/test-domain/memory (assert task_stats present with record_count >= 4)

    Domain isolation test for learning loop:
    - test_learning_loop_domain_isolation: register domain-a and domain-b. Post 5 audits with low scores to domain-a. Post 5 audits with high scores to domain-b. Decide on domain-a (confidence should be low). Decide on domain-b (confidence should be high). Verify domain-a confidence != domain-b confidence.
  </behavior>
  <action>
    Add new test methods to existing test files. Do NOT modify existing passing tests -- only ADD new test methods/classes.

    In test_decision_engine.py, add to a new class TestDecisionEngineLearningLoop:
    - test_record_audit_aggregates_to_history: Create DecisionEngine with registry. Register domain. Call record_audit("test-domain", "dec-1", "completed", {"task": "task-a", "score": 7}). Read audit_history.json from tmp_hermes_dir/domains/test-domain/memory/ and assert task-a key exists with records list.
    - test_record_audit_auto_learn_triggered: Register domain. Call record_audit 3 times with {"task": "task-a", "score": 1} each. Assert last call returns auto_learn_triggered == True.
    - test_record_audit_auto_learn_not_triggered: Same but with {"score": 9} each. Assert auto_learn_triggered == False.
    - test_record_audit_backward_compat: Call record_audit with {"score": 5} (no task key). Assert recorded=True and individual {decision_id}.json file exists.
    - test_decide_dynamic_confidence: Register domain. Create DomainMemory manually for the domain's memory dir. Append 3 records with score 8 for "task-a". Call engine.decide("test-domain", "task-a", {}). Assert result["confidence"] != 0.0.
    - test_decide_confidence_zero_below_minimum: Register domain. Call engine.decide("test-domain", "task-a", {}). Assert result["confidence"] == 0.0.

    In test_routes.py, add a new class TestMemoryEndpoint:
    - test_memory_endpoint_returns_summary: Use client fixture. Register domain via POST /v1/register. Post 3 audits via POST /v1/audit with body including metrics: {"task": "task-a", "score": 7}. GET /v1/domains/test-domain/memory. Assert 200, response has task_stats and recent_records keys.
    - test_memory_endpoint_404_unregistered: GET /v1/domains/nonexistent/memory. Assert 404.
    - test_memory_endpoint_empty_domain: Register domain. GET /v1/domains/test-domain/memory. Assert task_stats=={} and recent_records==[].

    In test_integration.py, add class TestLearningLoop:
    - test_learning_loop: Full flow per behavior description above. After 3 audits, the 4th audit on a low-score task should return auto_learn_triggered: true.
    - test_learning_loop_domain_isolation: Register two domains, post different score patterns, verify isolation.

    All new tests should FAIL because DecisionEngine does not yet import DomainMemory and the memory endpoint does not exist.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/docker/hermes-agent && python3 -m pytest tests/test_decision_engine.py::TestDecisionEngineLearningLoop tests/test_routes.py::TestMemoryEndpoint tests/test_integration.py::TestLearningLoop -x -q 2>&1 | grep -E "(FAILED|ERROR|NameError|AttributeError|404|500)" | head -10</automated>
  </verify>
  <done>
    New test methods added to test_decision_engine.py (TestDecisionEngineLearningLoop with 6 tests), test_routes.py (TestMemoryEndpoint with 3 tests), test_integration.py (TestLearningLoop with 2 tests).
    All new tests fail (RED state confirmed).
    All 61 original tests still pass.
  </done>
</task>

<task type="tdd">
  <name>Task 2: TDD GREEN -- implement integration and memory endpoint</name>
  <files>
    docker/hermes-agent/src/core/decision_engine.py,
    docker/hermes-agent/src/api/models.py,
    docker/hermes-agent/src/api/routes.py,
    docker/hermes-agent/tests/conftest.py
  </files>
  <read_first>
    docker/hermes-agent/src/core/decision_engine.py
    docker/hermes-agent/src/api/models.py
    docker/hermes-agent/src/api/routes.py
    docker/hermes-agent/tests/conftest.py
    docker/hermes-agent/src/core/domain_memory.py
    docker/hermes-agent/src/api/deps.py
  </read_first>
  <action>
    **1. Modify docker/hermes-agent/src/core/decision_engine.py:**

    Add import: `from src.core.domain_memory import DomainMemory`

    Add a helper method `_resolve_task(self, domain: str, decision_id: str, metrics: dict[str, Any]) -> str`:
    - Return metrics.get("task") if present and non-empty string.
    - Otherwise look for the decision record file at self.registry.base_dir / domain / "memory" / f"{decision_id}.json". If file exists, read it, parse JSON, extract "task" key.
    - Fallback: return "unknown".

    Modify `record_audit()`:
    - Keep existing Phase 7 code that writes individual {decision_id}.json file (lines 64-77).
    - After the existing write, add: resolve task via _resolve_task(). Create DomainMemory(memory_dir). Call append_record(task, record). Call should_trigger_auto_learn(task) to determine auto_learn flag.
    - If auto_learn triggered, log warning: logger.warning("Auto-learn triggered: domain=%s task=%s confidence=%.3f", domain, task, domain_memory.get_confidence(task)).
    - Return {"recorded": True, "auto_learn_triggered": auto_learn}.

    Modify `decide()`:
    - After validating domain exists, before or after agent.chat() call, compute dynamic confidence: create DomainMemory(self.registry.base_dir / domain / "memory"), call get_confidence(task).
    - Replace the hardcoded "confidence": 0.0 with the dynamic value.

    **2. Modify docker/hermes-agent/src/api/models.py:**

    Add two new Pydantic models after the existing models:
    - MemoryTaskStat(BaseModel): fields avg_score: float, record_count: int, ewma_confidence: float, trend_direction: str
    - MemoryResponse(BaseModel): fields task_stats: dict[str, MemoryTaskStat], recent_records: list[dict]

    Follow existing model patterns: from __future__ import annotations, BaseModel from pydantic.

    **3. Modify docker/hermes-agent/src/api/routes.py:**

    Add import for MemoryResponse from src.api.models.

    Add new endpoint after the existing GET /v1/domains/{domain}/skills endpoint:

    @router.get("/domains/{domain}/memory", response_model=MemoryResponse)
    async def get_domain_memory(domain: str, registry: DomainRegistry = Depends(get_registry)) -> MemoryResponse:
    - Check domain_exists(domain), raise 404 if not.
    - Create memory_dir = registry.base_dir / domain / "memory".
    - Create DomainMemory(memory_dir).
    - Call get_summary(), return MemoryResponse(**summary).

    **4. No changes needed to conftest.py** -- existing fixtures support the new tests as-is because DomainMemory is instantiated inside DecisionEngine, not injected.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform/docker/hermes-agent && python3 -m pytest tests/ -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    All new tests (11 from Plan 02 Task 1) pass GREEN.
    All 61 original tests still pass.
    record_audit() now aggregates into audit_history.json and returns auto_learn_triggered based on EWMA threshold.
    decide() returns dynamic EWMA confidence from DomainMemory.
    GET /v1/domains/{domain}/memory endpoint returns aggregated memory stats.
    Full learning loop works: register -> decide (conf=0.0) -> audit x3 -> decide (conf!=0.0) -> audit (auto_learn=true) -> memory query shows stats.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted input via HTTP to /v1/audit and /v1/domains/:domain/memory |
| API -> DecisionEngine | Internal delegation, trusted |
| DecisionEngine -> DomainMemory | Internal delegation, trusted |
| DomainMemory -> Filesystem | Audit history JSON write/read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information Disclosure | GET /v1/domains/:domain/memory | mitigate | Domain name validated by Phase 7 regex; 404 for unregistered domains |
| T-08-05 | Tampering | Audit metrics dict size | accept | Prune cap at 100 records/task; internal trusted callers |
| T-08-06 | Denial of Service | Rapid audit submissions | accept | Single-instance deployment; JSON write is fast |
| T-08-SC | Tampering | pip installs | mitigate | No new packages in this phase |
</threat_model>

<verification>
1. All new tests pass: `cd docker/hermes-agent && python3 -m pytest tests/ -v --tb=short`
2. Full learning loop integration: `cd docker/hermes-agent && python3 -m pytest tests/test_integration.py::TestLearningLoop -v`
3. Existing tests unaffected: `cd docker/hermes-agent && python3 -m pytest tests/test_decision_engine.py tests/test_routes.py -v`
4. Memory endpoint accessible: `cd docker/hermes-agent && python3 -c "from src.api.models import MemoryResponse, MemoryTaskStat; print('OK')"`
</verification>

<success_criteria>
- record_audit() aggregates to audit_history.json grouped by task, returns auto_learn_triggered based on EWMA
- decide() returns dynamic confidence from DomainMemory for tasks with 3+ audits, 0.0 for fewer
- GET /v1/domains/{domain}/memory returns task_stats and recent_records
- Full learning loop test passes: register -> decide -> audit x3 -> decide with dynamic confidence -> auto_learn triggered
- Domain isolation maintained: domain A audits do not affect domain B confidence
- All 61 original tests unchanged and passing
- Total test count: 61 original + ~18 (Plan 01) + ~11 (Plan 02) = ~90 tests all green
</success_criteria>

<output>
Create `.planning/phases/08-learning-loop-integration/08-02-SUMMARY.md` when done
</output>
