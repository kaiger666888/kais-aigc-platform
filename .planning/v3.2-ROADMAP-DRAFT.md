# v3.2 跨仓协调清偿 (Cross-Repo Coordination Debt Clearance) — ROADMAP 草案

> **状态**: DRAFT — 2026-08-25 由 12-agent fan-out review 产出（40 findings 全部经对抗复核确认），待 Kai 裁决后经 /gsd-new-milestone 正式立项。
>
> **⚡ 执行进度(2026-08-25 当天,11/11 phase 全量完成)——待 Kai 过目后 /gsd-new-milestone 正式立项归档**:
> - ✅ **Phase 63 全部** — gold-team 真值源收编(b0e8839 现场保全 + 39bb666 回灌,47/50 md5 + 3 有意保留;重建镜像策略断言过);v9 compose 钉死 real 镜像(kap 938f26e5);khs2 assets 5.0Pro 清零(37631a1)
> - ✅ **Phase 64 全部** — p11a5 注册表 22→23 + TYPE_MAP 契约门 F 组 + 6 处类型分叉修正(kap 736889db);khs no-fold+四工件+zone+retro(b432bba);verify:phase-55 **12/14 红 → 18/18 绿**;khs 11/11
> - ✅ **Phase 65 全部** — 引擎契约对齐(4b182d8d):video 首帧/tts text/bgm+foley 诚实拒绝/ratio 推导/modelVersion 键名翻译/seed 真值。✅ 65-04 余量完成(bc9d783a):bgm→ACE-Step(异步+轮询)/foley→SA3(同步);同 commit 排雷 _promptAnchor 误注册路由
> - ✅ **Phase 66 全部** — 生产通电(4e60b9c8):serve-production.sh + 66-02 真机断点(model_preference 顶层字段,静态 review 双漏真机探针抓到)+ probe-66 零足迹 9/9(40s cloud-jimeng 真渲染)
> - ✅ **Phase 67 全部** — WBX-03 止血(3312a0c1)+ WBX-01 review-platform /api/v1/g15/ops 端点(fail-closed 匹配/union 幂等/approve carry-forward/六路径 decision 补洞,容器已重建部署)+ WBX-02 khs 子集豁免消费端(runner 注入 + p10c/p11c 子集优先,空=legacy 全量)+ kap 桥 episodeRefs 接通(4bf51216/ce1f571/review-platform 7eea588)。⏸ WBX-05 UAT 11 真机 drill 留下集活门(代码链全通)
> - ✅ **Phase 68 全部** — 契约重冻结:v2.5 键(finalists/final_rank/dropped/selection_meta/render_variants)进 schema + 三档 scale(ten=真实 0..10)+ S1f 双源验证门(首跑即抓真漂移:khs take-log 实际 shot_index+seed:null)+ 三 ADR(.planning/adrs/adr-v3.2-variant-domain.md:chosen_variant_id=string finalist id/scale 三档/归组一主两从)+ Wave B 失实理由三处更正。verify-53 97→102
> - ✅ **Phase 69 全部** — FS transport 真实现(KMC_MANIFEST_TRANSPORT=fs 通电:iframe-manifest 覆写/hook-candidates chosen 覆写/原子写/幂等)+ G15 真实数据源(graphG15Source:failed-shots/per_shot/take-log 派生,fixture 降级测试模式)+ khs 五源落盘(p03 script-candidates/p11a preview-candidates 新生产者 + _CANDIDATE_FILES 注册)+ requeue 消费端(runner→p11c→p11b 重渲集并集)。WBI-04 envelope 映射按 68 契约(hook-candidates 形状对齐)
> - ✅ **Phase 70 全部** — 换选四断点修通:choose 载荷按 phase id 空间(p11a0 {sid}:{ft}:v{N}/p11a {sid}:v{N}/p01 v{N})+ variantNumber 真 v{N}(节点解析非数组位置)+ fullPhaseToken 防错批(p11a0→p11c 负向锁)+ selected ADR-1。reviewBridge node:test 3→6
> - ✅ **Phase 71 全部(裁决点2 选 a:画布为配方真值)** — khs:B-4 删建抢救用户 prompt 编辑(_kmc_prompt 哨兵)+ kmc 重同步清 stale(upsert 集中清,n-* 不再残留);kap:canvas-takes.jsonl 产物回流 + import replace 关系层同步(幽灵变体组收口)+ sequence linkType 往返存活(顶层列+data 袋双写/读时重建)
> - ✅ **Phase 72 全部(QVR-01..07)** — F26 判定数组透传(canvas_sync 概览节点整块挂 fidelity_check/per_shot)+ F28 aiScore 生产者(p03/p14 审计分数→节点顶层,雷达数据链从零起步;存量集待下次 sync 回填)+ F29 DIM_LABELS 修真(logic/social_resonance_depth/requirement_conformance/info_package_density)+ 词表门源码锚点提取 + F32 五值 verdict(join/角标/工作台三处)+ F31 registerAuditToken 扩展契约 + F30 声纹 metaSub + F25 G16 嵌套层读取
> - ✅ **Phase 73 全部** — F19 p11b webhook 哨兵(不参与 blocking/「异步哨兵」呈现/26+9 条 APPROVING 残留实清至 0+resolve-stale-gates.py 收官工具)+ F20 红线 reject 墓碑上浮(khs 提交/kap 别名路由/p13 防污染)+ F21 qwen-eye 队列优先(拒绝不绕队+宕机 lease 受控拉起,08-23 死锁根治)+ F38 status-check v2 关系表
>
> **验证矩阵**:verify-53 102/102 · 54 62/62 · 55 18/18 · 56 89/89 · 65 31/31 · canvas vitest 496/496 · khs 三目录 831+ · review-platform 443+(基线外零新失败) · 生产 :10588 pid 2680419(GOLD_TEAM_URL+KMC_MANIFEST_TRANSPORT=fs 双 env 实证)· review-api 容器重建部署
> **四仓 commit 数**:kap 12 · khs 6 · gold-team 2 · review-platform 1(+ 本草案/ADR 未 commit)
> **⏸ 待真机项**(代码链全通,需活体管线):WBX-05 UAT 11(p10c-gate comment 状态)/70-05 G13 换选端到端(manifest 覆写→p11b 渲新帧)/72 存量集 aiScore 回填(下次 episode sync 自然发生)
>
> **审查源**: workflow wf_86c8a137-9ef（journal: ~/.claude/projects/-home-kai/fae37383-c066-4151-af3c-0a4805d60741/subagents/workflows/wf_86c8a137-9ef/journal.jsonl）
> **范围**: kap(kais-aigc-platform) · khs2(kais-hermes-skills) · 引擎(kais-gold-team) · review-platform(kais-review-platform) 四仓协调欠账，全部源自 v3.0(52-56)/v3.1(58-62) ship 后的双向适配缺口。
> **纪律**: 延续 COORD-01——跨仓变更必须双向销账；每个 phase 的 verify 门必须测**端到端数据链**（端点存在/消费端存在/生产数据命中），不再只测契约字面量（本轮 15 条 high 中 8 条属"verify 门绿但链路断"）。

---

## Findings 索引（F01-F40 → phase 映射）

| F# | 严重度 | 一句话 | 归因 | → Phase |
|---|---|---|---|---|
| F01 | high | 生产 10588 无 GOLD_TEAM_URL，重生成 simulateOnly 假成功 | kap改kmc没跟 | 66 |
| F02 | high | Stage→TaskType 映射未对照引擎消费契约（video 缺 image/TTS 缺 text/bgm+foley 被拒收） | kap改kmc没跟 | 65 |
| F03 | high | 图像配方保真断层（ratio 恒 1:1 / modelVersion 键名 / 九键丢弃 / seed 装饰真值） | kap改kmc没跟 | 65 |
| F04 | high | gold-team 仓与活体容器分叉，模型政策只在容器 | 文档声称代码没做 | 63 |
| F05 | high | canvas_sync 删建 a-* 抹画布编辑+误清 stale，n-* stale 永残留，产物不回流 | kap改kmc没跟 | 71 |
| F06 | low | Wave B deferral 理由（gate 未过）已过期未更新 | 语义分叉 | 68 |
| F07 | high | Wave B gate 已满足(08-23)但三件套零启动，文档仍称『验收未过』 | 文档声称代码没做 | 69 |
| F08 | high | select-winner→G13 三重断裂（作用域/编号/相位 token） | 语义分叉 | 70 |
| F09 | high | g15/ops 端点不存在 404；waived_shot_ids 无生产者；批量语义两端相反 | kap改kmc没跟 | 67 |
| F10 | med | khs2 v2.5 候选契约演进（finalists 等）kap 零跟进 | kmc改kap没跟 | 68 |
| F11 | med | 跨组归组三套词表并存无真相源 | 语义分叉 | 68 |
| F12 | med | chosen_variant_id: kap number vs khs string，预览域无消费者 | 语义分叉 | 68/70 |
| F13 | low | candidateScoreSchema max(1) 与 percent 注释自相矛盾 | 文档声称代码没做 | 68 |
| F14 | high | 同 F09（54 视角）：ship 了的豁免通道端点 404 假成功 | 文档声称代码没做 | 67 |
| F15 | med | 逐镜头豁免 kmc 无消费端，子集被静默放大全量 | kap改kmc没跟 | 67 |
| F16 | med | review-platform web UI/batch 绕过 decision 持久化 | 语义分叉 | 67 |
| F17 | med | 换选 kap 发 int vs khs 校验 string finalist id，三层通道全断 | kap改kmc没跟 | 70 |
| F18 | med | reviewBridge leadingPhaseToken 折叠可错批同剧集 p11x 门 | 语义分叉 | 70 |
| F19 | low | p11b webhook tripwire 被当硬门 + 26 条 APPROVING 残留 | 语义分叉 | 73 |
| F20 | low | 红线三门恒显 auto，kmc 真实 rejected 不上浮 | 语义分叉 | 73 |
| F21 | low | qwen-eye KAP 宕机兜底直跑 kap-llm.sh 绕 GPU 队列 | kmc改kap没跟 | 73 |
| F22 | high | p11a5 落地后 kap 注册表未跟进，verify:phase-55 当前红 | kmc改kap没跟 | 64 |
| F23 | high | p11a5 四工件被 canvas_sync 前缀折叠静默丢弃，DAG 恒 pending | 语义分叉 | 64 |
| F24 | med | PHASE_REGISTRY canvasType/assetType 6/22 分叉且 verify 不查 | 语义分叉 | 64 |
| F25 | high | G16 听审工作台 clips 数据链断（嵌套层 vs 顶层） | kap改kmc没跟 | 72 |
| F26 | high | 眼/耳角标 shot_id join 真实数据零命中 | kap改kmc没跟 | 72 |
| F27 | high | 同 F09（56 视角）+ 队列 drain 丢 gate 字段错门豁免 | kap改kmc没跟 | 67 |
| F28 | high | SC1 雷达图链路不存在（aiScore.dimensions 无生产者） | 文档声称代码没做 | 72 |
| F29 | med | DIM_LABELS 键漂移 + verify 门 substring 恒真测不出 | 语义分叉 | 72 |
| F30 | med | voice 剧场 metaSub 键错位，真实数据不可达 | 语义分叉 | 72 |
| F31 | med | QC 角标闭集词表，khs 无扩展接入契约 | kap改kmc没跟 | 72 |
| F32 | low | 非三值 verdict（skipped/error/must_fix）被静默过滤 | 语义分叉 | 72 |
| F33 | med | 52 REGEN-01 真机验证口径建立在 simulate 链上未回溯标注 | 文档声称代码没做 | 66 |
| F34 | med | gold-team 仓停在 jimeng-free-api 旧代理；v9 compose 从分叉仓构建抢 :8002 | kap改kmc没跟 | 63 |
| F35 | med | Wave B khs2 半部解锁 2 天无认领，requeue delivered=true 只是送达 | kap改kmc没跟 | 69 |
| F36 | med | import-from-dir replace 不清 canvas_variant_groups（两实现分叉） | kap改kmc没跟 | 71 |
| F37 | low | sequence 边 data.linkType 全量保存往返即丢（WR-01 codified 未修） | 语义分叉 | 71 |
| F38 | low | canvas-status-check.py 仍 v1 事件视角读图，v2 排障误报 | kap改kmc没跟 | 73 |
| F39 | low | v2.5 候选新键未进 kap 冻结契约认知（与 F10 同根） | kmc改kap没跟 | 68 |
| F40 | low | khs2 assets/ 历史脚本仍传 --model_version=5.0Pro 直连 CLI | kmc改kap没跟 | 63 |

**覆盖核对**: 40/40 findings 全部落 phase，无遗漏。

---

## 推荐执行序（批次依赖图）

```
Wave 1 (地基+止血, 可全并行):
  63 引擎真值源收编 ──┐
  64 p11a5 注册表跟进  │
  67 豁免桥三仓闭环    ├──> (67 的 WB-03 kap 诚实化为当日止血项)
  68 变体域契约重对齐 ─┤
  73 门中心+调度欠账   │
                       │
Wave 2 (引擎链, 串行):  │
  65 重生成引擎契约对齐 <┘ (依赖 63: 契约对照真值源)
       │
  66 生产通电与真机闭环 (依赖 63+65; image-only 灰度可提前)

Wave 3 (变体域实施, gate on 68):
  69 Wave B 实施 ∥ 70 换选通道端到端

Wave 4 (共存语义, gate on 69 + Kai 裁决先行):
  71 画布↔kmc 共存语义

填隙: 72 QC/评分可视化真数据 (无硬依赖, 可任意波次插入)
```

**当日止血清单**（不等立项）:
1. `verify:phase-55` 红 → 64 的 PRG-01 最小修复（注册表加 p11a5 条目）当天可做
2. g15/ops 假成功 → 67 的 WB-03（delivered=false 不再成功 toast + drain 修 gate 字段）先行，端点随后
3. gold-team 仓分叉 → 63 当天回灌 commit（防任何人按仓重建镜像整链断）
4. GOLD_TEAM_URL 不要现在裸开——65 完成前开了 env，video/TTS 路径立刻从"静默假"变" loudly 翻车"

---

## Phase Details

### Phase 63: 引擎真值源收编 (Engine Truth-Source Consolidation)

**Goal**: 结束"引擎真值只活在一个未溯源的容器镜像里"的状态。把容器 /app 内的模型政策代码（t2i 白名单 {5.0, 5.0lite}、i2i 强制 4.6、cloud 直通 passthrough、image_refine ref_images 兜底）回灌 kais-gold-team 仓并留下 git 历史；处置 v9 compose 从分叉仓构建抢 :8002 的隐患；顺手清掉 khs2 assets/ 的 5.0Pro 残留。回灌后"按仓重建镜像"必须与现役容器行为等价。
**Depends on**: Nothing（全里程碑地基，Wave 1）
**Repo ownership**: kais-gold-team（主）· kap（compose）· khs2（assets 清零）
**Requirements**: ETS-01, ETS-02, ETS-03, ETS-04
**Success Criteria** (what must be TRUE):

  1. 容器内四文件（executor.py / cloud_jimeng.py / cloud_base.py / workflow_builder.py）与仓 HEAD 的 md5 逐一相等；`git log -S '_I2I_MODEL'` 与 `-S 'model_version'` 在仓内有历史（不再是空）。
  2. 按仓 rebuild 镜像并在隔离端口拉起，容器内实测 `_T2I_ALLOWED_MODELS={'5.0','5.0lite'}`、`_I2I_MODEL='4.6'`，且一次 image_refine 冒烟任务 completed——重建即等价。
  3. docker-compose.v9.yml 不再从 ../kais-gold-team 分叉仓构建 gold-team：要么改 build context 到 kap docker/gold-team 副本、要么 pin `kais-gold-team:real` 镜像并注释废弃；v9 误起不再能静默替换掉白名单实现。
  4. khs2 全仓 `grep -rn '5\.0Pro'` 在活代码路径零命中（assets/generate_p04_assets.py:72,93 与 assets/P04/generate_p04_octopus.py:154,167 清零；deprecated 留档例外需带标注）。

**Plans 建议**: 2 plans（63-01 回灌+重建验证；63-02 compose 处置+assets 清零）

---

### Phase 64: p11a5 相位跟进与注册表契约门 (p11a5 Registry Catch-up)

**Goal**: khs2 已于 08-24 (a046d47) 落地第 23 个活跃相位 p11a5_preview_audio，kap 22-phase 注册表与 canvas_sync 工件映射双双未跟进——verify:phase-55 当前 12/14 红，p11a5 真实产物在画布零节点、DAG 恒 pending。本 phase 让跨仓相位漂移告警门回绿且测得出更多漂移（canvasType/assetType 两字段纳入门内）。
**Depends on**: Nothing（Wave 1，快速止血项）
**Repo ownership**: kap（注册表+verify 门）· khs2（前缀正则+工件映射+retro 测试）
**Requirements**: PRG-01, PRG-02, PRG-03
**Success Criteria** (what must be TRUE):

  1. `npm run verify:phase-55` 14/14 exit 0：phaseRegistry 含 p11a5 条目（phaseIndex/zone/label/sortKey 全套），khs 23 活跃 id 与 kap 注册表等值，only-in-khs 为空。
  2. khs `_PHASE_PREFIX_RE` 对 `p11a5_preview_audio` 提取 `p11a5`（不再折叠为 p11a），`_PHASE_OUTPUT_MAP` 注册其四工件（ambient_stems/preview_mix_path/roughcut_path/roughcut_meta）；`test_retro_order_covers_registry` 与五个 registry↔map 一致性测试全绿；真实 episode 跑 p11a5 后画布出现四工件节点、kap 管线监控 DAG P11a.5 子图状态不再是恒 pending。
  3. PHASE_REGISTRY 的 canvasType/assetType 22(+1) 条目与 khs `_PHASE_TYPE_MAP`/`_PHASE_OUTPUT_MAP` 实际值逐条相等（p09b/p11c/p12a/p12b/p13/p15 六处历史分叉修正）；verify:phase-55 新增两字段断言组，khs 侧改值必变红。

**Plans 建议**: 2 plans（64-01 kap 注册表+门扩展；64-02 khs 前缀+映射+DAG 派生验证）

---

### Phase 65: 重生成引擎契约对齐 (Regen Engine Contract Alignment)

**Goal**: 59-01 的 NODE_TYPE_TO_TASKType 映射只验证了字面量存在，从未对照引擎对每类 TaskType 的实际消费参数。补齐四类断点：video 缺 image 参必失败、TTS 引擎读 text 而 kap 只送 prompt、bgm/foley 被引擎 v1.5 起 direct reject、图像配方几何/键名/丢弃/seed 四处保真断层。修完之后"补上 env"才有意义。
**Depends on**: Phase 63（契约对照的权威面=收编后的仓，不是容器黑盒）
**Repo ownership**: kap（_simulate.ts/_engine.ts/canvasApi extra 通道）
**Requirements**: REA-01, REA-02, REA-03, REA-04, REA-05, REA-06
**Success Criteria** (what must be TRUE):

  1. 映射表驱动的契约测试：verify 断言逐 TaskType 对照引擎 executor 的消费参数（image/text/ratio/model_version/ref_images），映射表新增类型而参数不满足时断言红——终结"五键命中"式字面量验证。
  2. video/video_final 重生成请求体携带 image 参（referenceImages 通道从 canvasApi extra 打通到 submitEngineTask），引擎侧 workflow 构建不再因缺参 FAILED。
  3. audio/voice→tts 请求体携带引擎读的 text 键（prompt→text 映射显式化），语音重生成不再对空文本合成。
  4. bgm/foley 路由按 Kai 裁决执行（见裁决点 1）：改走 kap 自家 /api/v1/ace/generate 与 /stableaudio/generate，或映射下线+UI 明示"画布暂不支持该类型重生成"——不再投递引擎必拒的任务还报"已提交"。
  5. 图像重生成请求体携带 ratio（九键配方补几何键或从节点资产尺寸推导）；modelVersion 经 recipe.ts 映射为引擎的 model_version 键名送出；cloud 不消费的键在 UI 配方面板标注"cloud 引擎不生效"，不再静默丢弃。
  6. seed 语义修正：cloud 路径 seed 不影响产物时，EventParamsPopover 不再把新 seed 回写 canonical 当真值（或回写时带 `decorative:true` 标注），画布配方不再记录从未影响产物的装饰性 seed。

**Plans 建议**: 3 plans（65-01 映射契约测试地基；65-02 video/TTS/bgm 参数通道；65-03 图像配方保真+seed）

---

### Phase 66: 生产通电与真机闭环 (Production Power-On)

**Goal**: 把已经修好的 canvas→引擎链（08-23 四断点修复+59 rethrow）真正插上电：生产 10588 进程加载 GOLD_TEAM_URL，分类型灰度放开重生成，用真机证据（:8002 出现 canvas-* 任务、node.data.filePath 更新、产物可见）关掉"有 success 无产物"的假成功时代；同时回溯标注 52 时代的验证口径。
**Depends on**: Phase 63 + Phase 65（顺序不能反：先修映射再通电，否则 video/TTS 从静默假变 loudly 翻车）
**Repo ownership**: kap（部署契约+文档）
**Requirements**: PWR-01, PWR-02, PWR-03, PWR-04
**Success Criteria** (what must be TRUE):

  1. 生产启动路径显式加载引擎 env（systemd Environment= 或启动脚本 export 或 app 启动时 dotenv——三选一裁决后落死）；重启 10588 后 `/proc/<pid>/environ` 含 GOLD_TEAM_URL，verify 断言锁死"env 缺失时启动告警"而非静默 simulateOnly。
  2. 灰度次序留档：image 类型先行（65 完成即开），video/tts 待 65-02 完成后放开；灰度开关（env 或配置）显式存在，未放开类型在 UI 明示而非假提交。
  3. 真机 e2e：画布发起图像重生成 → :8002 任务列表出现 `canvas-<nodeId>-<ts>` 前缀任务并 completed → 产物落 /oss/canvas-*/ → node.data.filePath 更新 → 前端新图回贴。全程 probe 脚本可重跑。
  4. 文档回溯：52-07-SUMMARY:81 的"真机闭环"证据段与 v3.0-MILESTONE-AUDIT TD 注记补注"当时建立在 simulate 链上，真实闭环 66 完成"，后续读档者不再被误导。

**Plans 建议**: 2 plans（66-01 env+灰度+verify；66-02 真机 probe+文档回溯）

---

### Phase 67: G15/G16 豁免桥三仓闭环 (Waive Bridge 3-Repo Closure)

**Goal**: 豁免通道 ship 了 UI 却投递到全网不存在的 /api/v1/g15/ops（404→队列无限重放→toast 报"已豁免 N 条"）。三仓合围：review-platform 补服务端、khs2 补逐镜头豁免消费端（终结"子集被放大成全量"的错放行）、kap 桥诚实化。附带修掉 review-platform web UI/batch 绕过 decision 持久化的缺口。
**Depends on**: Nothing（Wave 1；WB-03 为当日止血项先行）
**Repo ownership**: kais-review-platform（端点）· khs2（消费端）· kap（桥诚实化）
**Requirements**: WBX-01, WBX-02, WBX-03, WBX-04, WBX-05
**Success Criteria** (what must be TRUE):

  1. 豁免操作的服务端端点真实存在（形态按裁决点 4）：kap g15Bridge 的 POST 不再 404；端点行为（per-shot waive / requeue）有 API 契约文档与测试。
  2. khs2 逐镜头豁免消费端成立：waived_shot_ids 有了生产者（端点写入），p11c/p10c 读 per-shot 子集豁免（operator 豁免 5 个失败镜头中的 2 个 → 只 2 个进 waived，其余照常阻塞），不再 approve=全量一刀切。
  3. kap 桥诚实化：delivered=false 时 UI 不再显示成功 toast（乐观标记回滚），队列重放有上限与死信可见；drain 读取 payload 不再丢 gate 字段（排队的 p10c-gate 豁免不会被错发成缺省 p11c-gate）。
  4. review-platform web UI（htmx 单条 reject / batch approve+reject / API batch）全部写 metadata.review_result.decision；"web UI reject → kmc 不阻、kap 反显示 approve"的三方读法不一致消除。
  5. v3.0 UAT 登记项 11（khs p10c-gate 收到 comment 后状态正确）真机通过；G16 听审批量豁免端到端：工作台勾选 → khs p10c 下一轮 waived 状态正确。

**Plans 建议**: 3 plans（67-01 kap 诚实化止血；67-02 review-platform 端点+decision 补洞；67-03 khs 消费端+UAT 11）

---

### Phase 68: 变体域契约重对齐 (Variant Domain Contract Realignment)

**Goal**: Wave A 契约冻结于 08-21，khs2 v2.5（08-24）演进出了 finalists/final_n/final_rank/dropped/selection_meta/render_variants，kap 信封契约/fixture/推导器零跟进；加上 score schema 自相矛盾、chosen_variant_id 类型相悖、归组键三套词表并存——Wave B 若照旧契约开工必然返工。本 phase 只做纸面/契约层：重冻结+三项裁定+文档销账。
**Depends on**: Nothing（Wave 1，纸面先行，可与所有 phase 并行）
**Repo ownership**: kap（契约/schema/verify）· khs2（词表真相源确认）
**Requirements**: VDR-01, VDR-02, VDR-03, VDR-04
**Success Criteria** (what must be TRUE):

  1. candidateEnvelope 契约重冻结并对 khs2 v2.5 实际产出形状逐源验证：finalists/final_rank/dropped/selection_meta 进入五源信封 schema，take-log 信封含 render_variants；dropped（淘汰留痕）候选在墙上与普通候选有区分，final_rank 名次语义保留。
  2. 三项裁定落 ADR：①chosen_variant_id 最终类型（string "{sid}:{ft}:v{N}" 全线统一 vs 平台加 string 字段——裁决点 3）；②candidateScoreSchema percent 域修正（max(1) 放宽或统一归一 unit，p11a0 的 0..100 信封不再被 schema 拒收整条丢弃）；③跨组共享候选归组真相源唯一化（三套词表：khs meta.groupKey 短横线形 / kap cand: 规范形 / 资产中心 keyframe: 推导形 → 指定一主两从）。
  3. verify 门补端到端形状断言：fixture 与 khs2 真实产出（canvas_sync 落库样本）双源校验，schema 拒收即红——不再只有 fixture 自说自话。
  4. 文档销账：REQUIREMENTS/ROADMAP 中 Wave B"验收未过 (TD-1)"的失实理由更新为"gate 已于 08-23 满足，待排期"；52-VERIFICATION 跨组归组遗留指向本 phase ADR。

**Plans 建议**: 2 plans（68-01 契约重冻结+schema 修正；68-02 三 ADR+verify 门+文档销账）

---

### Phase 69: Wave B 实施——manifest transport 与真实数据源 (Wave B Implementation)

**Goal**: Wave B 三件套从零到一：manifest 回写通道（getManifestTransport 恒 null → 真实现，画布换选真正到达 kmc manifest）、G15 分诊面板真实数据源（fixture 硬编码行 → take-log/failed-shots 真实消费，khs _CANDIDATE_FILES 五源注册）、requeue khs 消费端（delivered=true 不再只是送达语义）。khs2 半部（envelope/field-map）一并认领。
**Depends on**: Phase 68（契约重冻结后开工，防二次返工）
**Repo ownership**: kap（transport/数据源）· khs2（envelope 映射+requeue 消费）
**Requirements**: WBI-01, WBI-02, WBI-03, WBI-04
**Success Criteria** (what must be TRUE):

  1. getManifestTransport 按 env/配置返回真实实现：画布变体墙换选 winner → khs manifest 的 selected_first/last_variant 被覆写 → 下一轮 p11b 消费新选定渲染——全链留痕可断言，"画布已选定但管线用旧的"假闭环消除。
  2. G15TriagePanel 数据源切换为真实 take-log/failed-shots（五源在 khs canvas_sync _CANDIDATE_FILES 全注册，p03_nbest/p11a_preview/p11b_take 三源补生产路径），fixture 降级为显式测试模式而非默认。
  3. requeue 消费端在 khs2 落地：面板点"重渲"→ 对应镜头真实进入重渲队列，delivered=true 从送达语义升级为消费确认。
  4. khs2 侧 envelope 映射（field-map）按 68 契约完成，活体 episode 数据回放验证信封解析零 fallback-null。

**Plans 建议**: 3 plans（69-01 transport；69-02 真实数据源+五源注册；69-03 khs requeue 消费端+envelope 映射）

---

### Phase 70: 换选通道端到端 (Choose/Swap Channel E2E)

**Goal**: operator 从任何面（变体墙 select-winner / gate-ops / 平台）换选今天全部静默无效：choose:v{N} 缺 {sid}:{ft} 作用域、variantIndex=数组位置≠v{N} 真编号、reviewBridge 相位 token 折叠恒失配、selected 是 int 而 khs 校验 string。四处断点一次修通，换选→manifest→渲染全链闭环。
**Depends on**: Phase 68（chosen_variant_id 类型裁定先行）
**Repo ownership**: kap（reviewBridge/gate-ops/变体索引）· review-platform（selected 类型，视裁定）
**Requirements**: CHS-01, CHS-02, CHS-03, CHS-04, CHS-05
**Success Criteria** (what must be TRUE):

  1. choose 载荷携带完整作用域 id（{sid}:{frame_type}:v{N}），khs p11a0 的 rsplit 解析真实命中（count(":")>=2 恒满足），manifest 覆写发生。
  2. 变体索引改为从节点 variant 字段解析的真 v{N} 编号（非组成员数组位置），变体缺失/淘汰时不再错位选错片——构造缺员组的负向断言。
  3. reviewBridge 相位匹配迁移 fullPhaseToken（/^p\d+[a-z0-9]*/），p11a0/p11a/p11b/p11c 各自独立匹配；"资产点击静默错批同剧集 open 的 p11c 门（=全量豁免放行）"的错批路径被负向断言锁死。
  4. selected 通道类型按 68 裁决落地（string finalist id 或平台并列字段），khs chosen_from_outcome 等值校验通过，不再 warn 回落 rank#1 却表面 approve 成功。
  5. 端到端真机断言：G13 条件帧在变体墙换选 → gate 批准 → manifest selected_* 覆写 → p11b 下一轮渲的是新帧。

**Plans 建议**: 3 plans（70-01 作用域+编号解析；70-02 token 迁移+错批负向锁；70-03 类型落地+端到端）

---

### Phase 71: 画布↔kmc 共存语义 (Canvas↔kmc Coexistence)

**Goal**: 两条全量替换写路径互相踩踏：khs canvas_sync 删建 a-* 节点抹掉画布 prompt 编辑并误清 stale，n-* 节点 stale 又永久残留；画布重生产物从不回流 kmc。本 phase 先裁决共存模型（裁决点 2），再实现 merge 语义、stale 生命周期两仓统一、产物回流（复用 69 的 transport），并收口两条 replace 实现分叉。
**Depends on**: Phase 69（产物回流复用 manifest transport）；Kai 裁决先行（discuss-phase 灰区）
**Repo ownership**: khs2（canvas_sync merge）· kap（stale 链/replace 统一）
**Requirements**: COX-01, COX-02, COX-03, COX-04, COX-05
**Success Criteria** (what must be TRUE):

  1. 共存模型按裁决落地并有用例锁死：用户画布改 prompt 重生成 → kmc 重跑该 phase → 画布 prompt 编辑不再被静默蒸发（a-* 节点 upsert 保留编辑，或画布编辑显式导出 kmc 消费）；"stale 消失但 kmc 用旧配方渲染=假性解决"场景被负向断言排除。
  2. stale 生命周期两仓统一：kmc 重跑完成 → kap 侧对应 stale 角标真实消除（经 manifest 变更通知或 khs 回调触发 kap socket 清除链）；n-* 节点不再永久残留 stale。
  3. 画布重生产物回流 kmc：node.data.filePath 变更经 transport 写入 kmc 可感知位置（take-log/manifest），kmc 后续 phase 可选消费画布 take（R-40-01 skip-if-mp4 与画布 take 的关系显式化）。
  4. import-from-dir 的 replace 与 saveGraph 全量路径行为统一：canvas_variant_groups 同步清理（chunkedDelete 复用），重导入后无幽灵变体组/winner 悬空。
  5. sequence 边语义往返存活：FlowLinkV2Schema 放行 data.linkType（或 import 改产顶层 linkType），khs 导入→用户保存→序列蓝线不丢；往返保真断言入 verify。

**Plans 建议**: 4 plans（71-01 裁决落地+merge；71-02 stale 统一；71-03 产物回流；71-04 replace/sequence 收口）

---

### Phase 72: QC 与评分可视化真数据 (QC & Score Visualization Real-Data)

**Goal**: 56 的四条"真数据"链路三条断在生产数据上：G16 工作台只认顶层 clips（khs 写嵌套层）、眼/耳角标 join 零命中（per-item 数组被 canvas_sync 打散）、雷达图链路无生产者（aiScore.dimensions 全库 0 行）。本 phase 打通判定数组透传契约、评分生产者、词表对齐与扩展面——56 的 SC 从"e2e 注入形状通过"升级为"生产库命中>0"。
**Depends on**: Nothing（填隙波次；词表部分弱依赖 68 的裁定）
**Repo ownership**: khs2（canvas_sync 透传/aiScore 写入侧）· kap（join/词表/雷达生产者）
**Requirements**: QVR-01, QVR-02, QVR-03, QVR-04, QVR-05, QVR-06, QVR-07
**Success Criteria** (what must be TRUE):

  1. 判定数组透传契约成立（形态按裁决点 5）：p10c fidelity_check.clips 与 p11c per_shot 以 kap join 可读的形状落画布（不打散 or sidecar），生产库存在携带 clips/per_shot 的节点——契约测试用生产样本而非注入形状。
  2. 眼/耳 verdict 角标对真实 kmc 数据命中：活体 episode 同步后，storyboard/资产节点上角标渲染数 > 0（现生产库 11 skipped/2 error/1 must_fix/1 pass 应各有呈现），shot_id join 有命中率断言。
  3. aiScore.dimensions 有了真实生产者（khs canvas_sync 写入或 kap convert 层落库），生产库 ai_score 非空行 > 0；ScoreRadar 对 p03/p14 真实审计分数渲染，≥3 维真实到达。
  4. DIM_LABELS 与 khs 实际键逐一对齐（p03 补 logic、social_resonance→social_resonance_depth；p14 第 8 维改 requirement_conformance）；verify 门修真：includes('master') 恒真与后缀正则漏检修正，词表漂移必红。
  5. verdict 词表从三值扩到 khs 真实五值+未评态：skipped/error 呈现为"未评"（非静默消失）、must_fix 呈现为"必修"，不再与 pass/warn/fail 混同或丢弃。
  6. QC 接入扩展契约发布：AUDIT_VOCAB/DIM_LABELS 从 kap 源码闭集改为可注册词表（配置或契约文件），khs 后续新增审计 phase（storyboard-qc/master-qc 已在画布上的两槽先行接入）无需改 kap 源码。
  7. voice 剧场入口键对齐：subtype 从 o_assets.meta 同步到画布节点（或 kap 改读资产 meta），声纹两级试听对真实数据可达（生产库 metaSub=0 → 命中 > 0）。

**Plans 建议**: 4 plans（72-01 判定数组契约+角标命中；72-02 aiScore 生产者+雷达；72-03 词表对齐+verify 修真；72-04 扩展契约+voice 入口）

---

### Phase 73: 门中心语义细化与调度欠账 (Gate Center Semantics & Scheduling Debt)

**Goal**: 收尾批：门中心对 p11b webhook tripwire 与硬门无差别对待（reject 承诺回滚但 kmc 根本没停车）+26 条 APPROVING 残留参与阻塞竞争；红线三门恒显 auto 而真实 rejected 不上浮；qwen-eye KAP 宕机兜底绕 GPU 队列（08-23 死锁五层根因的未根治项）；khs2 status-check 脚本 v1 读法误报。四件低危但真实的欠账一次清完。
**Depends on**: Nothing（Wave 1 可并入任意批次）
**Repo ownership**: kap（门中心呈现）· khs2（残留清理/兜底入队/status-check）
**Requirements**: GCX-01, GCX-02, GCX-03, GCX-04
**Success Criteria** (what must be TRUE):

  1. p11b(webhook mode)在门中心以"异步哨兵"形态呈现、不参与 blocking 竞争；GateCenterBlock 对 webhook 门不再承诺"驳回将回滚重跑"；存量 26 条 ep-zhongkui-ep01 p11b APPROVING 残留清理（收官剧集门收敛工具或脚本）。
  2. 红线三门呈现真实态：kmc 红线 reject（真阻/真回滚）时 kap 门中心可见（display 从静态 auto 升级为 inspecting/rejected 联动，或至少 rejected 时显式上浮）。
  3. qwen-eye KAP 宕机兜底改走 GPU 队列（或带 lease 检查的受控拉起），不再裸 subprocess kap-llm.sh 绕队——08-23 接力死锁的根治项销账。
  4. canvas-status-check.py 改读 v2 关系表（或 kv_canvasEvent 加 type 过滤只取整图行），v2 项目排障不再误报。

**Plans 建议**: 2 plans（73-01 kap 门中心两态；73-02 khs 三项欠账）

---

## 待 Kai 裁决点（discuss-phase 输入）

| # | 裁决 | 影响 phase | 选项 |
|---|---|---|---|
| 1 | bgm/foley 画布重生成的路由 | 65 REA-04 | a) 改走 kap 自家 /ace + /stableaudio 端点（保功能）b) 映射下线+UI 明示不支持（保诚实，减工作量） |
| 2 | canvas_sync 与画布编辑共存模型 | 71 COX-01 | a) khs 侧 a-* upsert 保留画布编辑（画布为配方真值）b) 画布编辑显式导出 kmc 消费（kmc manifest 为真值）c) 分字段双真值+同步协议（最重） |
| 3 | chosen_variant_id 最终类型 | 68 VDR-02 / 70 CHS-04 | a) string "{sid}:{ft}:v{N}" 全线统一（khs 现势，kap/平台改）b) 平台并列加 string 字段 selected_variant_id（兼容 int 现状） |
| 4 | 豁免端点形态 | 67 WBX-01 | a) review-platform 新增 /api/v1/g15/ops（kap 桥零改）b) 扩展既有 /reviews/{id}/waive 带 per-shot payload（复用 R1 持久化，桥改打既有端点） |
| 5 | QC 判定数组透传形态 | 72 QVR-01 | a) canvas_sync 对判定数组保留不展开（画布节点带 clips/per_shot）b) o_assets sidecar 承载，kap 读资产侧（画布节点保持轻） |
| 6 | v3.2 立项范围 | 全局 | 11 phase 全量 vs 先立 Wave1+引擎链（63-68）变体域与共存（69-71）留 v3.3——影响里程碑时长 |

## 里程碑级守护（延续传统）

- **COORD-02（新）端到端数据链验证纪律**: 每个跨仓 phase 的 verify 门必须含"生产数据命中"断言（端点 curl 非 404 / 消费端 grep 有生产者 / 生产库行数>0），"映射表字面量命中"不再单独作为通过依据——本轮 15 条 high 中 8 条由此漏网。
- **COORD-03（新）契约冻结时效**: 任何冻结契约引用对侧仓状态时（如 Wave B gate），verify 门定期复查引用是否过期（gate 满足后 deferral 文档自动标红）。
- 回归锁: 52/55/59/62 既有 verify 门全绿为每 phase 完成前置；64 完成后 verify:phase-55 恢复其"跨仓漂移告警"设计职能。
