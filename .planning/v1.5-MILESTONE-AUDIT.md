---
milestone: v1.5
name: Architecture Hardening + Code Hygiene
audited: 2026-06-14T00:00:00Z
status: tech_debt
scores:
  requirements: 9/9 satisfied
  phases: 5/5 verified
  integration: 5/5 independent (no cross-phase wiring required)
  flows: 5/5 code-level verified (live runtime tests deferred)
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 23-gpuscheduler-redis-migration
    items:
      - "Deferred: live Redis integration test (SC-1, SC-4 redis path) — coded & ready, awaits `docker compose up redis` in production"
      - "Deferred: migrate existing sync `getGpuScheduler()` callers to `getGpuSchedulerAsync()` (currently 5 call sites in src/routes/v1/ace/*.ts use sync version)"
      - "Deferred: cross-process service state sync (only locks are cross-process visible; services use in-process Map + mirror-to-store pattern)"
      - "Note: project has no test framework (jest/mocha) — verify-phase-23.ts is a standalone tsx script, not part of CI test suite"
  - phase: 24-goldteam-python-cleanup
    items:
      - "Deferred: live `docker compose build kais-gold-team` verification (SC-3, SC-4, SC-5 redis path) — requires Docker daemon with GPU"
      - "Deferred: remove `additional_src/ACE-Step-1.5/` from build context (no longer COPYed, but directory still exists)"
      - "Behavioral change: MUSIC/SFX tasks sent to gold-team now fail with redirect to /api/v1/ace/generate (intentional but callers must adapt)"
  - phase: 25-output-path-convention
    items:
      - "Deferred: migrate remaining 32 ComfyUI routes to paths.ts (documented in docs/OUTPUT-PATH-CONVENTION.md as opportunistic)"
      - "Could add ESLint rule warning on direct `process.env.OUTPUT_DIR` reads outside paths.ts"
      - "Verification: only ace/config.ts migrated as demo; others continue using legacy env vars (works via override)"
  - phase: 26-hermes-ts-exclude
    items: []  # No debt — single-line config change, fully verified
  - phase: 27-router-autogen-fix
    items:
      - "Deferred: live `yarn dev` startup observation (would confirm hash stability)"
      - "Deferred: live HTTP test of representative endpoints (ace/flux/tts status routes)"
      - "Could add ESLint rule preventing new config.ts files from exporting default (defense-in-depth)"
nyquist:
  compliant_phases: []
  partial_phases: []
  missing_phases: ["23", "24", "25", "26", "27"]
  overall: "MISSING — no VALIDATION.md files (project does not use Nyquist validation framework)"
requirements_bookkeeping:
  requirement: "All 9 requirements marked [x] Complete in REQUIREMENTS.md after audit"
  status: "up-to-date"
---

# v1.5 Milestone Audit — Architecture Hardening + Code Hygiene

**Audited:** 2026-06-14
**Status:** ⚡ tech_debt
**Phases:** 5 (23-27)
**Tests:** `yarn lint` 0 errors (was 12,447 before Phase 26)

## Executive Summary

The v1.5 milestone delivered all 9 requirements across 5 phases. Every phase has a VERIFICATION.md reporting ✅ passed status. No critical blockers, no broken integrations, no unsatisfied requirements.

The work consists primarily of **code hygiene and infrastructure hardening** rather than new features — closing gaps exposed by the v1.4 ACE route convergence (commits e3d649e + e817e18). The largest deliverable is Phase 23 (GpuScheduler Redis backend, ~485 lines of new code); the smallest is Phase 26 (single-line tsconfig exclude that eliminated 12,447 TypeScript errors).

**Accumulated tech debt is documented per-phase** but consists entirely of "live runtime verification pending" items (require production Docker/GPU/Redis) and "opportunistic migration" follow-ups (32 ComfyUI routes to migrate to paths.ts over time). None are blockers.

## Requirements Coverage (3-Source Cross-Reference)

Sources cross-referenced:
- **V** = Phase VERIFICATION.md status
- **T** = REQUIREMENTS.md traceability table
- **R** = REQUIREMENTS.md checkbox state

### SCHED — GpuScheduler 多进程协调 (Phase 23)

| Req | V | T | R | Final | Evidence |
|-----|---|---|---|-------|---------|
| SCHED-01 | passed | ✅ Complete | [x] | **satisfied** | `src/services/gpu/stateStore.ts` + `redisStateStore.ts` (Lua atomic release); commit f302758 |
| SCHED-02 | passed | ✅ Complete | [x] | **satisfied** | `getGpuSchedulerAsync()` factory with `makeStore()` fallback + WARN log; commit f302758 |

### GOLD — gold-team Python 退役 (Phase 24)

| Req | V | T | R | Final | Evidence |
|-----|---|---|---|-------|---------|
| GOLD-01 | passed | ✅ Complete | [x] | **satisfied** | Deleted acestep.py + docker_polling.py + 5 cleanup sites; commit 318e489 |
| GOLD-02 | passed | ✅ Complete | [x] | **satisfied** | Dockerfile stripped (lines 70-82 install block + 7 ENV ACESTEP_*); commit 318e489 |

### PATH — 输出路径变量统一 (Phase 25)

| Req | V | T | R | Final | Evidence |
|-----|---|---|---|-------|---------|
| PATH-01 | passed | ✅ Complete | [x] | **satisfied** | `src/lib/paths.ts` exports `engineOutputDir()`, `EngineKind` union, `ALL_ENGINE_KINDS`; commit 25225a2 |
| PATH-02 | passed | ✅ Complete | [x] | **satisfied** | `LEGACY_ENV_OVERRIDE` map preserves FLUX_OUTPUT_DIR/COMFYUI_OUTPUT_DIR/etc as aliases; commit 25225a2 |

### HERMES — hermes-agent 类型卫生 (Phase 26)

| Req | V | T | R | Final | Evidence |
|-----|---|---|---|-------|---------|
| HERMES-01 | passed | ✅ Complete | [x] | **satisfied** | `tsconfig.json:27-36` exclude array extended with 4 vendored dirs; commit a60b192 |

### CORE — router.ts 自动生成机制 (Phase 27)

| Req | V | T | R | Final | Evidence |
|-----|---|---|---|-------|---------|
| CORE-01 | passed | ✅ Complete | [x] | **satisfied** | `src/core.ts:9-24` SKIP_PATTERNS regex; commit 34393f1 |
| CORE-02 | passed | ✅ Complete | [x] | **satisfied** | 12 config/shared files no longer export default; commit 34393f1 |

**Summary:** 9 satisfied + 0 partial + 0 unsatisfied + 0 orphaned.

## Phase Verification Status

| Phase | Status | Score | Critical Gaps |
|-------|--------|-------|---------------|
| 23. GpuScheduler Redis Migration | passed | 4/4 SC verified (memory); redis path coded | None — Redis live test deferred |
| 24. gold-team Python Cleanup | passed | 5/5 SC verified (code-level); Docker build deferred | None — MUSIC/SFX behavior change is intentional |
| 25. Output Path Convention | passed | 4/4 SC verified; ace/config migrated as demo | None — 32 routes deferred per migration guide |
| 26. Hermes TS Exclude | passed | 4/4 SC verified | None |
| 27. router.ts Auto-gen Fix | passed | 5/5 SC verified (code-level); live startup deferred | None |

## Cross-Phase Integration

**5 of 5 phases are independent cleanup tasks** — no cross-phase wiring required. Each phase addresses a distinct concern:

- Phase 23 (TypeScript GPU scheduler) ↔ Phase 24 (Python gold-team) — different languages, different subsystems
- Phase 25 (paths.ts) ↔ Phase 23/24 — no dependency (paths.ts is used by Node routes, not by GpuScheduler internals or gold-team)
- Phase 26 (tsconfig exclude) ↔ Phase 27 (core.ts skip) — both address "skip non-route files" but at different layers (TypeScript compiler vs auto-generator); independent fixes

**No integration checker spawned** because:
1. No phase consumes another phase's output
2. No E2E flow spans multiple phases (each phase's verification is self-contained)
3. v1.3 audit precedent: independent cleanup phases don't require integration check

## End-to-End Flows

Code-level verification of key user flows:

1. **ACE music generation via ComfyUI** (post v1.4 convergence):
   - `POST /api/v1/ace/generate` (Node) → ComfyUI `/prompt` → `GET /api/v1/ace/status/:promptId` → optional callback via `_shared/asyncCallback.ts` tracker
   - v1.5 change: paths resolved via `engineOutputDir("ace")` instead of direct env var
   - Status: code-level verified, live runtime test deferred

2. **GpuScheduler multi-process coordination** (new in Phase 23):
   - Two Node processes → both call `getGpuSchedulerAsync()` → both connect to Redis → `store.acquireLock()` atomic across processes
   - Status: 13/13 memory tests pass, Redis cross-process test coded & ready

3. **gold-team MUSIC task rejection** (changed in Phase 24):
   - Client POSTs MUSIC to gold-team → `executor.py` returns task FAILED with redirect message
   - Client should use `/api/v1/ace/generate` instead
   - Status: code-level verified

4. **router.ts generation** (fixed in Phase 27):
   - `yarn dev` boot → `core.ts` scans `src/routes/**/*.ts` → skips config/_shared files → writes router.ts with 236 real routes (was 248)
   - Status: regen verified via standalone script

5. **TypeScript compile** (cleaned in Phase 26):
   - `yarn lint` runs `tsc --noEmit` → excludes docker/hermes-agent + 3 other vendored dirs → 0 errors (was 12,447)
   - Status: ✅ verified

## Tech Debt Summary

**Total: 11 deferred items across 5 phases** (no critical blockers)

### Phase 23 (GpuScheduler Redis Migration) — 4 items
- Live Redis integration test pending (coded, awaits `docker compose up redis`)
- Sync `getGpuScheduler()` callers not migrated to async version
- Cross-process service state sync (only locks cross-process today)
- Project lacks test framework (verify script is standalone)

### Phase 24 (gold-team Python Cleanup) — 3 items
- Live `docker compose build kais-gold-team` verification pending
- `additional_src/ACE-Step-1.5/` directory still in build context (not COPYed)
- MUSIC/SFX callers must adapt to new rejection behavior

### Phase 25 (Output Path Convention) — 3 items
- 32 ComfyUI routes to migrate opportunistically
- ESLint rule for direct env var reads not added
- Only ace/config migrated as demo

### Phase 26 (Hermes TS Exclude) — 0 items
- Fully complete, no debt

### Phase 27 (router.ts Auto-gen Fix) — 3 items
- Live `yarn dev` startup observation pending
- Live HTTP test of representative endpoints pending
- ESLint rule for new config.ts files not added (defense-in-depth)

## Nyquist Validation Coverage

| Phase | VALIDATION.md | Status |
|-------|---------------|--------|
| 23 | missing | Project does not use Nyquist framework |
| 24 | missing | Same |
| 25 | missing | Same |
| 26 | missing | Same |
| 27 | missing | Same |

**Overall:** MISSING — project has never used Nyquist VALIDATION.md. Not a regression; consistent with v1.0-v1.4 history.

## Recommendations

1. **Accept tech debt and complete milestone** — recommended. All deferred items are runtime verification or opportunistic migrations, not blockers.
2. **Run live verification in production** — execute the deferred Redis/Docker/HTTP tests on the production host where Redis + GPU + Docker are available.
3. **Migrate ComfyUI routes opportunistically** — per `docs/OUTPUT-PATH-CONVENTION.md`, migrate when touching each route for other reasons.

## Commit Chain (v1.5)

```
34393f1 fix(27): router.ts auto-gen skips config/_shared files (CORE-01/02)
a60b192 chore(26): exclude vendored docker subdirs from main tsconfig (HERMES-01)
25225a2 feat(25): unified output path convention with engine subdirs (PATH-01/02)
318e489 refactor(24): retire gold-team ACE-Step engine (GOLD-01/02)
f302758 feat(23): GpuScheduler Redis backend with memory fallback (SCHED-01/02)
3bdff0c docs(23): auto-generated context (discuss skipped)
402acd5 docs: create milestone v1.5 roadmap (5 phases, 9 reqs mapped)
cdd8e72 docs: define milestone v1.5 requirements (9 reqs across SCHED/GOLD/PATH/HERMES/CORE)
6ca8313 docs: start milestone v1.5 Architecture Hardening + Code Hygiene
```

---

*Audit completed: 2026-06-14*
*Status: tech_debt — 9/9 requirements satisfied, 0 blockers, 11 deferred items documented*
