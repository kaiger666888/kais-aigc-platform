# Task: 排查资产管理页面看不到角色变体的问题

## 问题
无限画布资产管理页面看不到新生成的 27 个角色/场景变体。API `/api/v1/assets-registry/search` 正确返回 27 条数据（已验证），但前端不显示。

## 已验证
- API POST /api/v1/assets-registry/search { projectId: 1785119845700, limit: 200, includeFile: true } → 返回 27 条
- 每条都有 filePath（如 `/oss/1785119845700/p04/turnaround_sheets/base_turnaround_shenzhiyi_v1.png`）
- OSS 文件可访问（curl HTTP 200）

## 需要排查的方向

### 1. 前端缩略图渲染
文件：`packages/infinite-canvas/src/components/assetManager/AssetLibrary.tsx`
- `Thumb` 组件怎么从 AssetDetail 构建 img src？
- filePath `/oss/...` 是否需要加 base URL 前缀？
- 是否因为 filePath 格式不同导致图片不显示，进而卡片看起来为空？

### 2. scope 过滤
文件：`AssetLibrary.tsx` 第 61-67 行
```js
if (scope === 'library') return d.projectId == null
if (scope === 'current') return projectId != null && d.projectId === projectId
```
- 当 scope='current' 时，前端 store 的 projectId 是否等于 1785119845700？
- 如果 store.projectId 不对，所有资产都会被过滤掉

### 3. assetDetailToItem 转换
- AssetDetail → AssetItem 的转换是否丢失了数据？
- uuid 字段是否为空导致 React key 冲突？

### 4. 缩略图 URL 构造
文件：`useRealAssets.ts` 或 `assetManagerData.ts`
- `filePath` 从 API 拿到后怎么转成 `<img src=...>` 的？
- `/oss/1785119845700/...` 需要变成 `http://localhost:10588/oss/1785119845700/...` 吗？

## 调试方法
1. 在 AssetLibrary.tsx 的 `filtered` useMemo 后面加 `console.log('[AssetLibrary] assets:', assets.length, 'filtered:', filtered.length, 'projectId:', projectId, 'scope:', scope)`
2. 在 Thumb 组件里加 `console.log('[Thumb] item:', item)`
3. 浏览器打开画布，打开 DevTools Console，切换到资产管理页面，看日志

或者直接读代码找到 bug 并修复。

## 工作目录
```
cd /data/workspace/kais-aigc-platform
```

## 修复后
1. `cd packages/infinite-canvas && npm run build`
2. `cp -r dist/* data/web/infinite-canvas/`
3. `curl -s http://localhost:10588/infinite-canvas/ | grep -o 'index-[^"]*\.js'` 验证新 hash
