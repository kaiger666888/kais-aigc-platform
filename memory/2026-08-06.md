# 2026-08-06

## KAP 路径 B:H3 提示词增强服务(enhance-prompt)
- 新增 `src/routes/production/minimax-h3/enhance-prompt.ts`:POST `/api/production/minimax-h3/enhance-prompt`,移植 TE MAN v3.6 H3 提示词增强节点(.pyd 内嵌字符串)。
- 系统提示词 5 套(全参考自足六字段;T2VA/I2VA/FL2VA/L2VA = 通用公共 + 帧公共 + 专属),逐字取自 `references/te-h3-prompt-enhancer-strings.txt`。
- enhanceMode 两种:本地模板(`buildLocalTemplate` 纯函数)/ API增强(ZHIPU GLM,默认 `glm-4.6`,chat/completions 格式)。
- router.ts 手动注册 route157;注意 `src/router.ts` 由 `src/core.ts` 的 `generateRouter()` 自动生成(NODE_ENV=dev 启动时),`enhance-prompt.ts` 会被自动扫描,dev 启动会重写 router.ts 并重编号,挂载路径不变。
- 验证:`npm run build:server` ✓、`npx tsc --noEmit` ✓、本地模板 curl ✓(三字段/六字段/JSON/multipart 均通过);API 增强请求链路 ✓,但 `.env` 中 ZHIPU_API_KEY 身份验证失败(需有效 key 才能端到端确认)。
- curl 注意:`-F 'referenceLabels=<Picture 1>'` 会被 curl 当作文件读取(值以 `<` 开头),需用 `--form-string`。
