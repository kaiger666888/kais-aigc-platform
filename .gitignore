.devtools
# dependencies
node_modules
.vscode

# output
out
dist
build/*
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
*.log
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json
server.log

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# uploads
upload/*
uploads/*

# databases
*.sqlite
db.sqlite
db.sqlite-shm
db.sqlite-wal
database.d.ts

# auto-generated
router.ts
version.txt
__pycache__/

# frontend build (root level)
web/*

# ─── Data directories (runtime/generated) ───────────────────
data/oss/*
data/test.sqlite
data/web/
data/serve/
data/models/
data/assets/
data/workspace/
data/db2*.sqlite*

# Test outputs
test-output/

# Skill example images (large)
data/skills/*/images/

# ─── Large binary files ────────────────────────────────────
*.glb
*.mp4
*.onnx
*.pt
*.pth

# Phase 3 (canvas-consumer): golden fixture needs a real video.mp4 container
# so the verify script can exercise ffprobe resolution probing.
!scripts/fixtures/shot-timeline-ep01/video.mp4
*.safetensors
*.bin
*.pkl

# ─── Runtime output / demo image assets ─────────────────────
/output/
/data/output/
workflows/demo_wf*.png
workflows/*.png

# ─── LoRA training assets (large, keep config only) ─────────
kohya-lora-training/venv/
kohya-lora-training/pretrained_model/
kohya-lora-training/sd-scripts-repo/
kohya-lora-training/dataset/

# ─── Node lockfile (project uses yarn) ──────────────────────
package-lock.json

# ─── Independent repos (symlinks) ───────────────────────────
# (docker/gold-team and docker/review-platform were previously listed here
#  as symlinks, but gold-team is now a real tracked directory and
#  review-platform no longer exists. Re-add `<path>` here if you bring
#  back a symlink.)

# ─── Claude Code local state ────────────────────────────────
.claude/

# ─── Build artifacts ────────────────────────────────────────
docker/movie-agent/src/
demo_qwen*.png
docker/lora-trainer/sd-scripts/

# ─── Large / transient docker build contexts (do NOT commit) ──
# docker/comfyui-primary/ ships ComfyUI custom_nodes (1.6GB) + output dir
# docker/hermes-agent/_hermes_source/ is a hermes source mirror (~1MB)
docker/comfyui-primary/
docker/hermes-agent/_hermes_source/

# ─── Backup files & test artifacts ──────────────────────────
*.bak.*
*.bak
test-results/
playwright-report/


# Python venvs (BGM detector etc.)
.venvs/

# ─── Local runtime state (do NOT commit) ───────────────────
# Empty DB placeholders / dev sqlite files
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Local pipeline state
.pipeline-assets/

# Local planning reports (keep top-level .planning/*.md tracked if desired,
# but auto-generated reports under .planning/reports/ are scratch)
.planning/reports/
workflows/ltx-2.3/shot-timeline-ep01-output/
.env.bak-*
.env.local
