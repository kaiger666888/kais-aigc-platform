.devtools
# dependencies
node_modules
.vscode

# output
out
dist
build/*
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
*.log
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json
server.log

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# uploads
upload/*
uploads/*

# databases
*.sqlite
db.sqlite
db.sqlite-shm
db.sqlite-wal
database.d.ts

# auto-generated
router.ts
version.txt
__pycache__/

# frontend build (root level)
web/*

# ─── Data directories (runtime/generated) ───────────────────
data/oss/*
data/test.sqlite
data/web/
data/serve/
data/models/
data/assets/
data/workspace/
data/db2*.sqlite*

# Test outputs
test-output/

# Skill example images (large)
data/skills/*/images/

# ─── Large binary files ────────────────────────────────────
*.glb
*.mp4
*.onnx
*.pt
*.pth
*.safetensors
*.bin
*.pkl

# ─── Runtime output / demo image assets ─────────────────────
/output/
/data/output/
workflows/demo_wf*.png
workflows/*.png

# ─── LoRA training assets (large, keep config only) ─────────
kohya-lora-training/venv/
kohya-lora-training/pretrained_model/
kohya-lora-training/sd-scripts-repo/
kohya-lora-training/dataset/

# ─── Node lockfile (project uses yarn) ──────────────────────
package-lock.json

# ─── Independent repos (symlinks) ───────────────────────────
# (docker/gold-team and docker/review-platform were previously listed here
#  as symlinks, but gold-team is now a real tracked directory and
#  review-platform no longer exists. Re-add `<path>` here if you bring
#  back a symlink.)

# ─── Claude Code local state ────────────────────────────────
.claude/

# ─── Build artifacts ────────────────────────────────────────
docker/movie-agent/src/
demo_qwen*.png
docker/lora-trainer/sd-scripts/
