# hermes-agent is installed from local fork in Dockerfile (COPY + pip install)
# Only list platform wrapper dependencies here
fastapi>=0.136.1,<1
uvicorn>=0.46.0,<1
pydantic>=2.13,<3
pytest>=7.0
pytest-asyncio>=0.21
httpx>=0.24
