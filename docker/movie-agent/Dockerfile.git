FROM kais-movie-agent:latest

# Add git for lib/git-stage-manager.js (post-production phase)
USER root
RUN apt-get update && apt-get install -y --no-install-recommends git && rm -rf /var/lib/apt/lists/*
USER kais
